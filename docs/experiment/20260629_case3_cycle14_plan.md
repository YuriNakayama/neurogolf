# 20260629 case3 cycle14 — plan: 整数コード符号化プリミティブ + 高コスト順 per-task 最適化

## 方針（ユーザー指示）
「効果の大きなタスクから1タスクずつ地道に最適化」「確認を取らず自律実行」。
cycle13 で公開 net の安さの核心が**整数コード符号化**と判明したのを受け、(A) 整数コード回路を実装して
floor 接近を試み、(B) 高コスト上位タスクを構造解析して具体的削減点を探し、(C) surgery を上位50に再走。

## 仮説と対象
- A: one-hot→Conv で code[1,1,H,W] に潰し小整数空間で変換、Equal で one-hot 復元（10ch 出力は cost 除外）。
  対象 = 対称修復 t287(rot180,1994) / colormap タスク。
- B: 高コスト t233/t286/t018/t187/t118 の cost 内訳を faithful 計測し、float32 中間や冗長 op を特定。
- C: 上位50高コストタスクに surgeon_task を適用、floor 未満 & n_fail=0 のみ採用。

## 期待
- 整数コードまたは surgery で高コストタスクの cost を削減し floor>7180.55 を狙う。
