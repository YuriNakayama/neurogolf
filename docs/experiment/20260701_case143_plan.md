# 2026-07-01 case143 plan

## 方針

- floor: Public Score 7181.16
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task093.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task093.onnx`
- baseline: params 103, memory 3456, cost 3559, points 16.822765114489805, n_pass 265, n_fail 0

## 仮説

`task093` は 14x14 の内部領域から row/col 方向の境界を検出し、境界外を sentinel color 5 にして 30x30 へ pad する短い graph。`ReduceMax(w/x)` から作る存在フラグ `D/E` が、境界初期値 `L/M/N/O` と sentinel seed `ab/ac` に使われている。

実データで `w` / `x` の対象領域が常に非空、または後段の比較で同じ結果に吸収されるなら、`Where(D/E, ..., default)` を一部外せる。graph は小さいが `Where` が13個あり、1個でも 14 要素級の中間を削れれば相対削減が期待できる。

## 手順

1. `/tmp` 上の candidate ONNX で `L/M/N/O/ab/ac` をそれぞれ入力側へ差し替える。
2. `QLinearMatMul` の左右・上下候補選択 `ah/ap` が片側で等価にならないか個別確認する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task093.onnx` に反映する。
4. submit し、Public Score が 7181.16 を上回れば採用、上がらなければ破棄する。
