# 20260703 case568 plan - task133/task077 repair

Accepted Public Score:

- `7187.57`

## Background

Case567 found no submit-ready candidate in `task148`, `task364`, or `task279`.
The fresh ranking points to the next two non-risk low-fail repair targets:

```text
task133: 33562 -> 31742, gain=0.055754, n_fail=5
task077: 14834 -> 14414, gain=0.028722, n_fail=4
```

`task133` candidate:

```text
/private/tmp/neurogolf_agent_case537_alias/near_selected/task133_task133_bypass_ScatterND_corr_overlay_TO_border_grid.onnx
```

`task077` candidates:

```text
/private/tmp/neurogolf_case410_lowgain/task077_008_MaxPool_b1mn_p_TO_mn0.onnx
/private/tmp/neurogolf_case410_lowgain/task077_010_MaxPool_b1mx_p_TO_mx0.onnx
/private/tmp/neurogolf_case410_lowgain/task077_012_MaxPool_b2mn_p_TO_b1mn.onnx
/private/tmp/neurogolf_case410_lowgain/task077_014_MaxPool_b2mx_p_TO_b1mx.onnx
```

## Hypothesis

`task133` is more promising because the known candidate passes all arc-gen and
fails only train/test correction-overlay cases. If the correction overlay can be
replaced by a cheaper narrow detector, a medium gain may survive.

`task077` is lower priority because earlier notes say restoring sparse color-4
fills restores the removed propagation, but the fail count is small enough to
justify one short repair pass.

## Method

1. Re-gate the listed candidates against current `data/output/onnx`.
2. Inspect failing examples.
3. Try one bounded repair per task:
   - `task133`: synthesize only the missing correction overlay.
   - `task077`: patch missing color-4 cells without restoring full MaxPool.
4. Submit only if:

```text
n_fail=0
candidate cost < current baseline cost
functions=0
forbidden ops=[]
not a known Kaggle ERROR/regression family
```

