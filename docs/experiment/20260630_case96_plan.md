# 20260630 case96 plan — t319 の fallback/source-choice branch 削減を検証する

## 背景

- 現 Public best は **7180.87**。
- `task319.onnx` は faithful baseline で cost **22097**（params 269, memory 21828）。
- case78 では global `input_u8` Cast の局所化を試し、正答だが cost 悪化で棄却した。
- 現 graph は 3 object の signature と edge match を計算し、`possible_idx` を求めた後、`fallback_sig_table` に一致すれば `fallback_idx` を優先して source を選ぶ。

## 仮説

fallback table や pair-match branch が arc-gen 上の例外対応でなければ、source-choice 周辺の `Equal/ReduceMin/ArgMax/Gather/Where` を削除できる。特に `fallback_or_default -> possible_idx` が通れば小さいが確実に cost を下げられる。

## 対象タスク

- `task319`

## 手法

1. ORT profile で memory 上位 tensor を確認する。
2. `/tmp` variant で以下を試す。
   - `fallback_or_default -> possible_idx`
   - `possible_idx -> one_i64` / `two_i64`
   - `src2_possible -> p20_pair_match` / `p21_pair_match`
   - `p10_pair_match` / `p20_pair_match` / `p21_pair_match` の個別削除
3. faithful `audit_one` が `n_fail=0` の候補だけ cost を比較する。

## 採否

- faithful `audit_one` が `n_fail=0` かつ cost が 22097 未満なら `task319.onnx` を差し替えて submit する。
- Public Score が 7180.87 を上回れば採用し、DVC push と git commit/push を行う。
- 壊れる候補、または cost が変わらない候補は破棄する。
