# 20260701 case166 plan

## Context

- Current accepted Public Score: 7181.17
- Recent valid local reductions of about 10 cost did not move Public LB.
- Prioritize exact-preserving dtype or shape reductions on higher-cost tasks.

## Target

- Task: `task017`
- Baseline audit:
  - params: 1804
  - memory: 10922
  - cost: 12726
  - points: 15.548597576207262
  - n_pass: 266
  - n_fail: 0
  - filesize: 13998

## Hypothesis

`task017` selects one of 106 param candidates by comparing sampled input labels:

- `matches_b` is bool `[1,106,9]`
- `matches_h` casts it to float16
- `scores = ReduceSum(matches_h, axis=2)`
- `ArgMax(scores)` selects the candidate

The cast tensor alone is about 1908 bytes. If `ReduceSum` can operate on a smaller integer dtype while preserving candidate ordering, or if the match scoring can bypass a float16 intermediate, the task-local reduction could be large enough to matter.

## Method

1. Probe one dtype or scoring replacement at a time on temporary `task017` ONNX copies.
2. Run `audit_one` only for `task017`.
3. Apply and submit only if `n_fail=0` and cost decreases materially.
4. If Public Score does not exceed 7181.17, revert the ONNX/DVC change and document rejection.
