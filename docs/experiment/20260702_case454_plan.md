# 20260702 case454 plan - task062 ref/object candidate gate

## Context

Cases452 and 453 closed `task102` and `task190`: both had attractive
low-cost-task reductions, but every positive reduction failed local correctness.

The remaining small Pad/output-mask candidate from the case436/case438 list is
`task062`.

## Target

`task062.onnx`

Current baseline:

```text
params=71
memory=2465
cost=2536
points=17.1616566844
status=ok
```

## Hypothesis

Existing ref/object simplifications likely fail because the selected horizontal
and vertical reference branches are required. Still, the low absolute cost
means any real tensor deletion has high relative value, so a bounded
current-baseline gate is worthwhile.

## Method

1. Gather existing `task062*.onnx` scratch candidates.
2. Run cost-only screening against the accepted baseline.
3. Run correctness gate only for positive cost reductions.
4. Submit only if a candidate reaches `submit-candidate`; otherwise close the
   task062 stale-pool family as no-submit.

## Gate

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
no forbidden ops/functions
single task only
```
