# case208 plan: task118 dtype memory trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task118` は未着手で、baseline は `cost=39237` (`params=4614`, `memory=34623`, `n_fail=0`)。
- graph 冒頭に `Cast(input) -> input_u8` があり、30x30x10 の 9000-byte tensor を追加している。
- 後段にも `sel_bool_pair -> float16`, `red_pair -> float16`, `plus16 -> uint8` など大きい dtype 変換がある。

## 対象

- `data/output/onnx/task118.onnx`
- red/gray channels を uint8 化して QLinearConv と mask update を行い、最後に `ScatterElements(input_u8, channel_indices, updates8)` で output を作る。

## 仮説

`input_u8` が最後の ScatterElements data input でしか本質的に必要ないなら、最後だけ `input` を使う、または一部の cast を alias して scorer memory を下げられる可能性がある。ただし ONNX op の dtype 制約が強いため、まず temp ONNX で valid/correctness を確認する。

## 検証

- temp ONNX で `input_u8 -> input`、`sel_pair -> sel_bool_pair`、`red_pair16 -> red_pair`、`plus8 -> plus16` などを 1 点ずつ probe する。
- `src.evaluate.scorer.audit_one` で `task118` の全ペア完全一致と scoreability を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
