# 20260702 case450 plan - task319 same-task continuation re-gate

## Context

Accepted Public Score floor remains `7186.71`.

`task319` has prior Kaggle-valid same-task work:

- case345 adopted `p10_pair_match -> p10_top_eq_all`;
- case384 adopted a stronger unary/value-path bundle;
- Public improved after the isolated case384 submit.

Current baseline:

```text
task319.onnx
params=132
memory=18948
cost=19080
status=ok
```

This makes `task319` a better target than unvalidated cross-task micros: the
family has already moved Public when submitted in isolation.

## Hypothesis

The old case382/case383 scratch pool may still contain one compatible residual
same-task value-path cleanup that was not included in the accepted case384
bundle. If a residual exact candidate saves at least about 378 cost, it clears
the standalone `gain >= 0.020` gate from the current baseline.

Known traps:

- the high-gain `src2_possible -> p20_pair_match` shortcut failed local repair;
- broad predicate shortcuts are hidden-sensitive;
- stale candidates built before case384 may be no-gain or invalid against the
  current graph.

## Method

1. Re-gate existing `task319` candidates from case382/case383/probe scratch
   pools against the current accepted baseline.
2. Submit only if a current-baseline exact candidate clears `submit-candidate`
   under the post-regression gate.
3. Otherwise record no-submit and deprioritize residual `task319` aliases.
