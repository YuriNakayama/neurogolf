# 20260702 case420 plan - dense structural compression search

## Context

Accepted Public Score remains `7186.71`. Case419 added a mechanical
pre-submit gate so future candidates are classified before Kaggle attempts.

Recent regression analysis changed the priority:

- do not force low-gain cadence submits;
- do not bundle unisolated micros;
- prefer isolated, mechanically explainable `gain >= 0.020` candidates;
- prioritize relative reduction, not absolute cost.

## Hypothesis

The safest remaining improvement surface is not broad local-equivalence aliasing
but structural compression with prior Public evidence:

- dense kernel or selector-table support cropping;
- replacing redundant selector kernels/tables with compact equivalents;
- same-task value-path reductions that prune an entire chain and preserve
  candidate coverage.

This family has stronger history than generic micro bundles:

- `task367` kernel crop was adopted with gain around `0.040`;
- `task335` selector rewrite was adopted with gain around `0.154`;
- `task066` same-task score/value alias was adopted with gain around `0.029`.

## Search Scope

Use docs-directed inspection and current-baseline graph statistics to find a
single first target. Avoid:

- known hidden-unsafe tasks/families from `20260702_regression_analysis.md`;
- uint8 `TopK` families;
- stale unisolated case414 micro candidates;
- standalone `gain < 0.010` candidates.

## Gate

Before any submit:

```text
uv --project backend run python -m submit gate CANDIDATE.onnx \
  --baseline-dir data/output/onnx \
  --task-dir data/lake/neurogolf-2026
```

Submit only if the decision is `submit-candidate` and the transformation is
single-task, exact, static-shape, and mechanically explainable.
