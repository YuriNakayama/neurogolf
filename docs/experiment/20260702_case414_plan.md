# 20260702 case414 plan - submit-saving exact micro bundle

## Context

Accepted Public Score is `7186.71` after case410. Cases 411-413 isolated
small exact candidates, but all three left Public Score unchanged. Because the
metric is logarithmic,

```text
gain = ln(old_cost / new_cost)
```

the submit plan should prioritize relative reduction and avoid spending one
Kaggle submission on each sub-threshold micro.

## Target

Bundle low-risk exact micro reductions that are individually too small to
submit, while excluding known hidden-unsafe or Kaggle-ERROR families.

Selected candidates:

| task | current cost | candidate cost | gain | source |
|---:|---:|---:|---:|---|
| 090 | 3073 | 3030 | 0.0140916636 | `/tmp/neurogolf_case413_task090_greedy.onnx` |
| 209 | 28047 | 27927 | 0.0042877116 | `/tmp/neurogolf_case398_bundles/task209_bundle_final_28047_to_27927.onnx` |
| 370 | 10393 | 10355 | 0.0036630078 | `/tmp/task370_case369_probes/direct_valid_selector.onnx` |
| 308 | 1488 | 1484 | 0.0026917917 | `/tmp/neurogolf_case382_main/task308_38_And_in1.onnx` |
| 293 | 1288 | 1286 | 0.0015540019 | `/tmp/neurogolf_case382_main/task293_29_Squeeze_in0.onnx` |

Combined expected gain:

```text
0.026288176493078214
```

## Rationale

This is a submit-saving batch: each candidate is `n_fail=0` locally, but the
single-task gains are below the practical Public movement threshold. Bundling
keeps the submit cadence useful without touching high-risk families:

- exclude `task017`, `task101`, `task118`, `task233`, `task285` uint8 TopK,
  broad `task286`, and the recently unchanged case411-413 isolated micros;
- keep all changes as graph-equivalence/cost reductions, not semantic solver
  replacements;
- audit only the five touched tasks before submit.

## Gate

Submit only if every touched task passes targeted `evaluate.audit_one`:

```text
status=ok
n_fail=0
cost decreases as listed above
forbidden ops=[]
functions=0
```

Adopt only if Kaggle Public Score improves over `7186.71`; otherwise restore
the accepted DVC baseline and keep the exact candidates in the micro backlog.
