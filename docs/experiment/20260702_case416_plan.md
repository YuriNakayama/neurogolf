# 20260702 case416 plan - same-task micro strengthening probe

## Context

Accepted Public Score remains `7186.71`. Case415 found no candidate clearing
the post-regression gate. The best banked candidates were:

```text
task213: 1851 -> 1834, gain 0.0092266598
task255: 22731 -> 22541, gain 0.0083937584
task046: 2286 -> 2285, gain 0.0004375410
```

Cross-task bundling is disabled after case414 unless each member is already
Kaggle-validated. However, strengthening a candidate within the same task is
still acceptable if it remains isolated and reaches the submit threshold.

## Target

Probe whether the best banked micros can be strengthened within the same task:

- `task213` predicate-input alias family from
  `/tmp/neurogolf_case410_alias_scan_200_400`;
- `task255` geometry/OR alias family from
  `/tmp/neurogolf_case410_alias_scan_200_400` and
  `/tmp/neurogolf_case381_manual`.

## Gate

Submit only if a same-task candidate reaches:

```text
status=ok
n_fail=0
functions=0
forbidden ops=[]
gain >= 0.020
```

Otherwise write a no-submit result and keep the exact candidates banked.
