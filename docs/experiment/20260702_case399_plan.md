# 20260702 case399 plan - shape-compatible three-task batch

## Context

Accepted Public Score is `7186.29` after case398 adopted the isolated
`task157` shape-compatible bundle.

Case398 also produced several exact current-baseline bundles. The safest
submit-saving set excludes `task209`, because that candidate matches the
case395 recovery family that returned Kaggle `ERROR`.

## Candidate Set

Apply these three shape-compatible bundles:

```text
task188 1187 -> 1166 gain 0.017850
task213 1882 -> 1851 gain 0.016609
task219 15060 -> 14838 gain 0.014851
```

Expected combined gain:

```text
0.04930979987547116
```

Scratch files:

```text
/tmp/neurogolf_case398_bundles/task188_bundle_final_1187_to_1166.onnx
/tmp/neurogolf_case398_bundles/task213_bundle_final_1882_to_1851.onnx
/tmp/neurogolf_case398_bundles/task219_bundle_final_15060_to_14838.onnx
```

## Gate

Submit only if each changed task has:

```text
n_fail=0
no forbidden ops/functions
combined gain >= 0.020
```

Adopt only if Public Score improves over `7186.29`; otherwise restore the
case398 accepted bundle.
