# 20260701 case289 result - adopt task367 tail zero Pad

## Summary

- Target: `task367`
- Change: move four trailing zero channels from `Concat` into final `Pad`
- Baseline: `params=5127`, `memory=19600`, `cost=24727`, `n_fail=0`
- Candidate: `params=5127`, `memory=18000`, `cost=23127`, `n_fail=0`
- Submit ref: `54224405`
- Public Score: `7182.15 -> 7182.22`
- Decision: adopted

## Change

The previous graph materialized four zero channels in `out20`:

```text
out20 = Concat(
  ch0m,
  zero20,
  zero20,
  zero20,
  mask20,
  c5u,
  zero20,
  zero20,
  zero20,
  zero20,
  axis=1
)

output = Pad(out20, sp=[0,0,0,0,0,0,10,10], value=0)
```

The adopted graph leaves `out20` at six channels and lets `Pad` create the
zero channels 6..9:

```text
out20 = Concat(ch0m, zero20, zero20, zero20, mask20, c5u, axis=1)
output = Pad(out20, sp=[0,0,0,0,0,4,10,10], value=0)
```

This preserves the multi-hot output representation and only changes where
known-zero channels are created.

## Verification

Individual audit:

```text
task367.onnx
status=ok
params=5127
memory=18000
cost=23127
n_pass=266
n_fail=0
```

Kaggle:

```text
ref: 54224405
status: COMPLETE
publicScore: 7182.22
```

## Adoption

Adopt. The real Public Score increased by `+0.07`, consistent with a
structural `1600B` memory reduction.

## Next

Continue with structural geometry rewrites. The next best known candidate is
`task209`: crop the label Conv at source using a sparse 11x11 top-left-tap
kernel, expected `31187 -> 29487`.
