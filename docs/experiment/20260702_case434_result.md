# 20260702 case434 result - no-submit task355 row/col reduction alias probe

## Outcome

No submit.

Current baseline:

```text
task355 params=27 memory=2688 cost=2715 n_fail=0 status=ok
```

Dominant tensors:

```text
row_mx float [1,10,30] = 1200B
col_mx float [1,10,30] = 1200B
```

## Candidates

Tested the only narrow symmetric aliases:

```text
row_cnt = ReduceSum(col_mx, ax2)
col_cnt = ReduceSum(row_mx, ax2)
```

Gate results:

```text
/private/tmp/neurogolf_case434_task355_rowcnt_from_colmx.onnx
  decision=blocked-local-fail base=2715 cand=2715 gain=0 n_fail=182

/private/tmp/neurogolf_case434_task355_colcnt_from_rowmx.onnx
  decision=blocked-local-fail base=2715 cand=2715 gain=0 n_fail=178
```

## Decision

Reject for submission. The row and column occupancy reductions are both
semantically required for the bounding-box score. Direct aliasing fails many
examples and does not produce a cost-reducing candidate.

## Next

Continue the fresh-scan list. `task131` has a large `scatter_idx int64
[1,50,4]` tensor, but it may hit the same `ScatterND` int64 constraint as
`task284`; use a short dtype check only. If blocked, move to `task162` where the
large tensor is a 20x20 source slice rather than a ScatterND index.
