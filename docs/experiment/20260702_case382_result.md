# 20260702 case382 result - task055 Sub bypass bundle

## Decision

Adopted.

```text
ref: 54247673
description: case382 task055 sub bypass bundle gain 0.03803
previous accepted Public Score: 7185.92
Public Score: 7185.95
```

The real Public Score increased by `+0.03`, so keep the submitted
`task055.onnx`.

## Local Gate

Baseline:

```text
params 276
memory 2940
cost 3216
points 16.924106369701143
n_pass 263
n_fail 0
filesize 1775
```

Submitted candidate:

```text
params 276
memory 2820
cost 3096
points 16.962133765290382
n_pass 263
n_fail 0
filesize 1737
```

Local gain:

```text
ln(3216 / 3096) = 0.03802739558923932
```

This clears the standalone low-risk submit gate `0.020`.

## Change

Two compatible `Sub` outputs were bypassed:

- `rb -> frcum`
- `cb -> fccum`

Each individual bypass was exact at `3216 -> 3156`, `gain 0.018833`. Applying
both together remained exact and removed two nodes:

```text
nodes 31 -> 29
forbidden ops []
functions 0
```

## Deferred Findings

The split continuation scan found additional material that was not submitted in
this cycle:

- `task285`: `color_idx_flat_f -> color_idx_flat`, `24535 -> 22702`,
  `gain 0.077648`, exact locally but high-risk because it feeds uint8 directly
  into `TopK`, the same compatibility class that caused the `task233` Kaggle
  ERROR.
- `task319`: compatible observed bundle, `19569 -> 19232`,
  `gain 0.017371`, exact locally but below standalone submit gate.
- `task293`: `col_valid -> col_valid_k`, `1288 -> 1286`,
  `gain 0.001554`, exact micro.
- `task308`: `diamond -> lt_full`, `1488 -> 1484`,
  `gain 0.002692`, exact micro.
- Tasks `141-280` scan found no fresh exact candidate `>= 0.010` after case381.

## Conclusion

Adopted. This again supports the submit-saving rule: wait for compatible
same-task bundles that clear the relative-gain gate instead of spending submits
on isolated micros.

Next action: continue split unary/value-path scans, but keep `TopK` dtype/input
bypasses in a high-risk bucket unless Kaggle runtime compatibility is proven.
