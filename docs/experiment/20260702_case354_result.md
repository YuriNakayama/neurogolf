# 20260702 case354 result - adopt task285 bridge guard

## Submission

```text
ref=54242982
description=case354 task285 bridge guard cost 25630 to 24537
status=COMPLETE
Public Score=7185.58
```

Previous accepted Public Score:

```text
7185.53
```

Result:

```text
+0.05 Public Score
adopt
```

## Local Gate

Changed task only:

```text
task285.onnx
params=573
memory=23964
cost=24537
n_pass=265
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=550
memory=25080
cost=25630
n_fail=0
```

Expected local gain:

```text
ln(25630 / 24537) = 0.043581
```

## Change

Replaced the task285 connectivity expansion with a shortcut plus bridge guard:

```text
data/output/onnx/task285.onnx
```

Source scratch candidate:

```text
/tmp/neurogolf_case354_struct/task285_conn_shortcut_bridge_guard.onnx
```

The earlier raw shortcut variants had one local failure. The adopted candidate
keeps enough guard structure to pass all local task285 train/test/arc-gen pairs
while still pruning most of the connectivity chain.

## Notes

An independent repair probe found more conservative exact variants:

```text
/tmp/neurogolf_case354_t285/repair_col4_left_or_ul_bcast__task285_conn_b_4_TO_pivot_src_grid.onnx
cost=24766
gain=0.034292 from old baseline
```

Those are now weaker than the adopted `24537` graph and should not replace it.

## Conclusion

Adopt case354. The result confirms that repaired medium-high structural
simplifications can be worth isolated submits when local gain clears `0.030` and
the candidate is not in a known hidden-unsafe family.

Next action: continue searching for non-task118 structural candidates with
`gain >= 0.030`; keep task118 stronger variants banked unless they clear the
high-risk standalone threshold from the current baseline.
