# 2026-07-01 case141 result

## 対象

- `task037.onnx`
- baseline: params 132, memory 4614, cost 4746, points 16.534942563004293, n_pass 266, n_fail 0

## 実施内容

`slot_present` / `valid_offsets_raw` による scatter index/update mask と、slot 数・offset 数の縮小を `/tmp` 上の候補 ONNX で確認した。

## 足切り結果

`n_fail=0` で cost が下がる候補はなし。

mask 省略:

- `slot_color_present -> slot_color`: cost 4733, n_fail 232
- `linear_idx_u8 -> linear_cols`: cost 4704, n_fail 171
- `updates_3d -> slot_color_present`: cost 4704, n_fail 266
- `updates_3d -> slot_color`: cost 4691, n_fail 266
- `valid_offsets_raw -> slot_present`: cost 4668, n_fail 266
- `col1 -> min_col`: cost 4734, n_fail 222
- `col1 -> max_col`: cost 4740, n_fail 217
- `diag_step -> eleven_u8`: cost 4704, n_fail 222
- `diag_step -> nine_u8`: cost 4704, n_fail 217

slot/offset 縮小:

- `k6 -> 5`: cost 4647, n_fail 4
- `k6 -> 4`: cost 4548, n_fail 31
- `offsets 7 -> 6`: cost 4679, n_fail 112
- `offsets 7 -> 5`: cost 4612, n_fail 185
- `k5 + offset6`: cost 4591, n_fail 113

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

invalid offset の index/update mask は scatter の範囲外防止と不要更新抑制に必要だった。slot 数は5までなら4 failまで近づくが、Public submit できる完全一致には届かない。

## 次の一手

`task096` を見る。`Where(..., zero)` が複数あり、row/span/present branch が中 cost に効いているため、局所的な相対削減候補を探す。
