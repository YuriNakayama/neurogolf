# 20260702 case472 plan - task014 bbox crop current re-gate

## Context

`task014` is a mid-cost current-rank target:

```text
task014 cost about 7985
```

Historical evidence:

- case132 inspected the rarest-color bbox crop graph and concluded that the
  terminal `ArgMax`, crop index clip, rect mask, inside/class, and pad sentinels
  are required;
- current scratch contains many aliases around those same components, plus
  newer case382/case406/case410 candidates.

## Target

```text
task014
baseline: data/output/onnx/task014.onnx
task json: data/lake/neurogolf-2026/task014.json
```

## Hypothesis

The known guard/sentinel aliases are likely semantic, but current-baseline
cost-only filtering may reveal a small mechanical or dtype cleanup. Submit only
if an exact candidate clears the material threshold.

## Method

1. Audit accepted `task014.onnx`.
2. Gather `task014*.onnx` candidates from `/private/tmp` and `/tmp`.
3. Run cost-only scan against the current baseline.
4. Correctness-gate current positives:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

5. Bank exact low-gain candidates only; otherwise close as no-submit.
