# 20260702 case373 plan - task009 onehot/content dtype probe

## Context

Current accepted Public Score is `7185.86`. Recent skipped cycles eliminated
small `GridSample` renderers, packed template expansion, and already-cropped
sparse kernels as promising surfaces.

Case369's accepted improvement came from an integer-valued float16 patch core
that could be represented as uint8. This cycle tests the same idea on another
medium-cost graph.

## Target

Primary target:

```text
data/output/onnx/task009.onnx
```

Current local audit:

```text
params=1081
memory=7329
cost=8410
n_fail=0
```

Graph surface:

```text
eq_b -> Cast(float16) -> onehot16[1,9,10,10]
Einsum(onehot16, onehot16, color_values16, intervals16)
  -> h_content_3d/v_content_3d -> Cast(uint8)
```

`onehot16`, `h_content_3d`, and `v_content_3d` are integer-valued, so a dtype
compression would remove roughly 1100 float16 elements if ONNX Runtime accepts
the equivalent representation.

## Prior Logs

Earlier task009 work only covered suffix-false `Concat -> Pad` micro cleanups.
Those were exact but too small and did not move Public Score. This cycle avoids
that already-rejected params-only surface and tests the content computation.

## Method

1. Build scratch ONNX files under `/tmp`.
2. Test dtype variants one at a time:
   - feed bool/uint8 onehot into the content computation if `Einsum` allows it;
   - move `color_values16` / `intervals16` to narrower integer tables if all
     inputs can share a valid type;
   - otherwise test whether post-Einsum casts or tail masks have any exact
     low-risk cleanup left.
3. Audit only task009 with `evaluate.audit_one`.

## Submit Gate

Use the submit-saving policy:

```text
low risk:    submit if gain >= 0.020, otherwise bank
medium risk: submit if gain >= 0.030, otherwise bank
```

If the best exact result is only the known `Concat -> Pad` micro class, skip
submit and record the result.
