# 20260701 case274 plan - task255 bundled interior guard aliases

## Context

- Current accepted Public Score floor: `7181.69`.
- The score is logarithmic, so the priority is `n_fail=0` first and then relative reduction.
- case273 succeeded by aliasing a redundant guard in a memory-heavy task. This cycle continues that exact alias-probe strategy.

## Target

- Task: `task255`
- Baseline local cost: `params=261`, `memory=22756`, `cost=23017`

## Hypothesis

An alias probe found several local-exact guard simplifications in `task255`. Individually, only `r_full_interior -> r_full_center` is close to worthwhile, but bundling the same-task aliases crosses the relative-reduction threshold:

```text
r_full_interior  -> r_full_center
c_top_interior   -> c_top_center
c_bottom_interior -> c_bottom_center
right_top0_a     -> right_p0
right_bottom29_a -> right_p29
top_bottom29_a   -> top_p29
```

These replacements should prune dead `And` guard intermediates and reduce memory without changing the output on all local examples.

## Precheck

Temporary candidate:

```text
/tmp/task255_bundle_r_full_interior_c_top_interior_c_bottom_interior_right_top0_a_right_bottom29_a_top_bottom29_a.onnx
```

Local audit:

```text
params=261
memory=22470
cost=22731
n_pass=265
n_fail=0
status=ok
```

Relative reduction: `23017 / 22731 = 1.0126`.

## Method

1. Apply all six aliases to `task255.onnx`.
2. Prune dead nodes, unused initializers, and stale value_info.
3. Run `audit_one` only for `task255`.
4. Submit if `n_fail=0` and cost remains reduced.
5. Adopt only if actual Kaggle Public Score increases.
