# 2026-06-30 case115 result

## 実 LB

- submit なし
- floor: Public Score 7180.97

## 対象

- `task265.onnx`
- baseline: params 34, memory 4039, cost 4073, n_pass 266, n_fail 0

## 試行

### pair 補正 / ScatterND

| 変更 | nodes | params | memory | cost | n_pass | n_fail | 備考 |
|---|---:|---:|---:|---:|---:|---:|---|
| `anchorf_u8 -> anchor_u8` | 8 | 26 | 3746 | 3772 | 265 | 1 | 約 7.4% 減だが 1 fail |
| `pair_keep = 0` | 9 | 31 | 4035 | 4066 | 260 | 6 | 動的補正が必要 |
| `pair_keep = 1` | 9 | 31 | 4035 | 4066 | 265 | 1 | bypass と同等に 1 fail |
| `ScatterND` を const mask `Where` に置換 | 12 | 319 | 4039 | 4358 | 7 | 259 | params 増かつ不正 |

`ScatterND` は固定位置 `(0,0,8,11)` だけを補正するが、更新値 `pair_keep = 1 - ReduceMin(anchor[:, :, 8:9, 10:12])` が hidden/arc-gen 上必要だった。

### MaxPool kernel / bypass

| 変更 | 結果 |
|---|---|
| `red_u8 -> anchorf_u8` | invalid shape (`Where` incompatible dimensions) |
| `red_u8 -> anchor_u8` | invalid shape |
| `Pad` bypass | invalid shape |
| MaxPool kernel 変更 | output shape が崩れて invalid |

## 採否

- 不採用
- 理由: 正答候補なし。最良の相対削減候補は `n_fail=1`。

## 結論

`task265` は相対削減率の観点では良い候補だったが、1 セルの pair 補正が完全一致に必要。`ScatterND` を軽い `Where` に置換する案も params 増と不一致で成立しない。

## 次の一手

- `task277` / `task350` / `task359` / `task396` の中 cost memory 支配タスクへ移る。
