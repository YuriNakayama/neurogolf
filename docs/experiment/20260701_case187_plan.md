# 20260701 case187 plan

## Context

- Current accepted Public Score: 7181.21
- The refined mask-arithmetic scan has mostly exhausted valid `Sub` candidates.
- A full initializer-use scan found one unused initializer in `task379`.

## Target

- Task: `task379`
- Baseline audit:
  - params: 1274
  - memory: 9500
  - cost: 10774
  - points: 15.71510889675952
  - n_pass: 266
  - n_fail: 0
  - filesize: 12738

## Hypothesis

`task379` contains an initializer:

```text
neg_one_f float16[1] = [-1.0]
```

No node consumes it. The local audit also emits an ONNXRuntime warning that this
initializer is unused. Removing it should preserve correctness and reduce params
by one:

```text
cost 10774 -> 10773
```

The expected score gain is tiny, but exact and risk-free if `audit_one` passes.

## Method

1. Remove only `neg_one_f` from `task379.onnx`.
2. Run `audit_one` only for `task379`.
3. Submit if `n_fail=0` and cost decreases.
4. Adopt only if real Kaggle Public Score improves over 7181.21; otherwise
   revert the ONNX/DVC change and record rejection.
