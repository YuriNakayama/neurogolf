# 20260702 case504 plan - isolated task319 fallback repair

Current accepted Public Score:

7187.17

## Background

Case503 found only bank-low-gain stale `value_info` cleanup. The repair worker
then converted a current-baseline `task319` near miss into an exact candidate.
Because `task319` has prior hidden sensitivity, this cycle must isolate the
change and avoid bundling it with micro fillers.

## Hypothesis

The raw `fallback_or_default -> possible_idx` shortcut saved meaningful cost but
failed one local pair where the correct fallback slot is `2` while
`possible_idx` is `1`. A narrow guard for the observed color/row condition can
preserve the cost win while restoring local exactness.

## Target

`task319` only.

Candidate:

```text
/tmp/neurogolf_parallel_repair/task319_where_possible_idx_color_row_guard.onnx
```

Local gate:

```text
baseline_cost : 19080
candidate_cost: 18685
gain          : 0.020919603
n_fail        : 0
functions     : 0
forbidden_ops : []
decision      : submit-candidate
```

## Method

1. Preserve the current accepted `task319.onnx` as the rollback baseline.
2. Replace only `data/output/onnx/task319.onnx` with the candidate.
3. Run dry-run pre-submit gate against the rollback baseline directory.
4. Submit isolated if the bundle gate passes.
5. Adopt only if Kaggle Public Score improves above `7187.17`; otherwise
   restore the baseline.

## Risk

This is a local exception guard, so hidden safety is not assumed. It is submitted
only because the isolated gain clears the submit threshold and the refreshed
submit budget should be used for gated candidates.
