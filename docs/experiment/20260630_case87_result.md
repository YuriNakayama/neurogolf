# 20260630 case87 result — t366 の TopK/component 候補数削減は採用不可（提出なし）

## 対象

- `data/output/onnx/task366.onnx`
- baseline: params **488**, memory **35897**, cost **36385**, `n_fail=0`

## trace

主な memory 支配テンソル:

- `label30`: float `[1,1,30,30]` = 3600B
- `label30_u8`: uint8 `[1,1,30,30]` = 900B
- `out_label30`: uint8 `[1,1,30,30]` = 900B
- 15x17 / flat255 系の bool/uint8/int32 中間が多数
- `bank_target`: int32 `[3,6,4]` = 288B

`label30` は full-grid color decode で、後段の crop/gather 両方に使われる。input 側で crop してから decode する案は、float one-hot slice や複数 decode が増えるため悪化しやすい。

## 検証

### no-op prune

dead node / stale `value_info` / unused initializer を剪定する no-op prune:

- nodes **634**
- value_info **639**
- cost **36385**
- `n_fail=0`

cost 変化なし。

### TopK 候補数

| 変更 | 結果 |
|---|---|
| `topk_eight: 8→7` | ShapeInferenceError |
| `topk_eight: 8→6` | ShapeInferenceError + `n_fail=14` |
| `topk_eight: 8→5` | ShapeInferenceError + `n_fail=15` |
| `topk_pos: 6→5/4` | downstream `Sub` incompatible dimensions |
| `topk_anchor: 4→3/2` | downstream `Sub` incompatible dimensions |
| `topk_three: 3→2` | ShapeInferenceError + `n_fail=255` |

TopK 出力 shape は後段の `GatherElements` / `Reshape` / component bank と固定結合しており、単純な k 削減は不可。

### 機械的削減

- dead output: なし
- 重複 node: なし
- 未使用 initializer: なし
- 重複 initializer: なし

## 採否

- Kaggle submit: **なし**
- 採否: **不採用**
- Public Score floor: **7180.87 維持**

## 結論

t366 は TopK/component bank の shape が固定されており、候補数削減は graph 全体の再設計なしには成立しない。現行 graph には機械的削減余地もない。

## 次の一手

次は t158/t133/t209 の memory 支配 graph を同じ手順で確認する。
