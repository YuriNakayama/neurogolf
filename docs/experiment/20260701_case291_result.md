# case291 result: task187 dual crop-at-source Conv

## Summary

- Submission ref: `54224665`
- Previous Public Score: `7182.27`
- New Public Score: `7182.37`
- Decision: adopt

## Local Gate

- Task: `task187`
- Before cost: `35564`
- After cost: `32267`
- Relative reduction: `1.1022`
- Local points: `14.61819968487829`
- Correctness: `n_pass=266`, `n_fail=0`
- Status: `ok`

## Change

Removed two full-size `30x30 -> 25x25` materialization paths:

1. `1x1 Conv -> Cast -> Slice` for `label_u8_25`
2. `ReduceSum(channel) -> Cast -> Slice` for `grid_b_25`

Both are now direct sparse `6x6` Convs with only top-left taps active, followed by the
existing Casts. A no-padding `6x6` Conv over the `30x30` input produces the same
top-left `25x25` tensors.

## Conclusion

Accepted. This is the clearest current method for increasing per-cycle improvement:
bundle multiple structurally equivalent tensor-size deletions inside one task, instead
of submitting isolated tiny aliases.

## Next Step

Continue prioritizing proof-grade structural rewrites. The parallel scan found no
currently ready unapplied `>=5%` candidate, but the next areas to inspect are:

- terminal fp16/final-op reductions like the earlier `task055` win,
- zero-channel materialization removal when channels are trailing or can be made
  trailing cheaply,
- interleaved zero-channel layouts only if the reconstruction does not spend the saved
  memory,
- connectivity/fill solver rewrites only with a real task-rule proof.
