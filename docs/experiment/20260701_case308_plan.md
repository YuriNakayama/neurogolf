# 20260701 case308 plan - task324 kernel trim correction

## Target

`task324.onnx`

Current baseline after case236:

- params: 1290
- memory: 7308
- cost: 8598
- `n_fail=0`

The 2% ratio target is `< 8430`; a plain 33x33 kernel trim was previously:

- cost: 8462
- `n_fail=2`

So 33x33 alone is not exact and does not quite clear 2%. A 31x31 trim would
clear the gate more comfortably but had `n_fail=5`.

## Hypothesis

If the failing examples are limited boundary cases, a small correction may make
33x33 or 31x31 exact while preserving a meaningful cost reduction. If the
correction costs more than the kernel saving, reject.

## Method

1. Reproduce 33x33 and 31x31 temporary variants.
2. Identify failing example ids and output diffs.
3. Estimate correction size before editing the production ONNX.
4. Only submit if `task324` audits `n_fail=0` and cost is below the 2% gate.
