# 20260703 case525 result - adopt task184 review-mid compression

## Result

Adopted.

```text
Kaggle ref: 54272777
Public Score: 7187.37 -> 7187.38
status: COMPLETE
```

## Submitted Candidate

```text
task184
candidate: /private/tmp/neurogolf_parallel_AN/task184_405db6cfab_2520_to_2491.onnx
old_cost: 2520
new_cost: 2491
gain: 0.011574665
n_fail: 0
decision: review-mid-gain
```

The candidate is below the normal standalone `0.020` submit threshold, but it
was exact, non-risk, and the submit cadence had stalled for several no-submit
cycles. It was submitted with explicit `--allow-review-gate`.

## No-submit Cause Analysis

The recent submit gap was not a Kaggle or CLI issue. Cases 518-524 skipped
submit because no safe candidate cleared the gate:

- broad same-shape alias scans produced high-gain semantic collapses with large
  `n_fail`
- low-fail repairs for task033, task161, task243, and task379 were structurally
  hard to repair without losing the saving
- exact micro candidates existed, but the current safe pool was too small for
  the configured micro-bundle gate

For case525, six workers were used in parallel:

- curated `golf_wins` re-gate: no positive current-baseline candidate
- accepted-family mining: identified task033/task243/task184/task066 targets
- bounded alias/DCE scan: no exact candidate
- task033 repair: best `1639 -> 1603`, gain `0.022209`, still `n_fail=2`
- task066 strengthening: exact `16351 -> 16268`, gain `0.005089`, banked
- task161 repair: exact `3657 -> 3651`, gain `0.001642`, banked

Parent re-gated 399 positive scratch candidates from recent `/tmp` pools. The
only exact mid-gain candidate was task184; the best low-fail near misses were
task379 (`10753 -> 10353`, gain `0.037908`, `n_fail=2`) and task161
(`3657 -> 3548`, gain `0.030259`, `n_fail=3`).

## Decision

Adopt `task184.onnx`.

Keep the improved task066 and task161 exact reductions in the micro backlog.
Continue task379 repair as the next standalone target while also replenishing
micro candidates. Do not submit bank-low micro bundles until the exact low-risk
pool reaches the configured threshold.

