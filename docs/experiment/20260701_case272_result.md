# 20260701 case272 result - reject task023 duplicate kernel alias

## Result

- Target: `task023`
- Baseline: `params=157`, `memory=12843`, `cost=13000`, `n_fail=0`
- Candidate: `params=147`, `memory=12843`, `cost=12990`, `n_fail=0`
- Relative reduction: `13000 / 12990 = 1.00077`
- Submit ref: `54221367`
- Public Score: `7181.66 -> 7181.66`
- Adoption: rejected

## Change Tested

Three byte-identical uint8 kernel initializer pairs were aliased:

```text
Ksc -> Ksb
Khc -> Khb
Kvc -> Kvb
```

The duplicate initializers `Ksc`, `Khc`, and `Kvc` were removed in the submitted candidate.

## Verification

Local `audit_one` for the candidate:

```text
params=147, memory=12843, cost=12990, n_pass=266, n_fail=0
```

After the Kaggle result showed no Public Score increase, `data/output/onnx` was restored with DVC checkout. Restored `task023` baseline:

```text
params=157, memory=12843, cost=13000
```

## Conclusion

The rewrite is exact and locally cheaper, but the relative reduction is too small to move the displayed Public Score at the current floor. Do not pursue single-task params-only reductions below roughly `0.01` points unless they can be bundled within the same task.

## Next Step

Continue searching for memory reductions or same-task bundled exact reductions with `old_cost / new_cost >= 1.01`. Avoid:

- bool `Pad` on older opsets;
- integer `TopK` input dtype changes;
- standalone duplicate-initializer aliases of this size.
