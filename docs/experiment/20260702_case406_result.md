# 20260702 case406 result - task335 cleanup plus filler bundle

## Submission

- Kaggle ref: `54254848`
- Message: `case406 task335 cleanup plus filler bundle gain 0.02109`
- Public Score: `7186.58 -> 7172.11`
- Decision: rejected and restored to case405 DVC state

## Local Gate

```text
task335: cost 3002  -> 2952,  n_fail=0
task396: cost 8078  -> 8074,  n_fail=0
task305: cost 636   -> 635,   n_fail=0
task366: cost 36385 -> 36304, n_fail=0
```

Combined current-relative gain:

```text
0.021093359392510464
```

## Outcome

Rejected. The local gate passed, but Public Score regressed sharply to
`7172.11`. The accepted `case405` DVC state was restored immediately.

Likely cause is one of the filler candidates rather than the low-risk duplicate
initializer cleanup:

- `task335`: duplicate initializer cleanup only; lower hidden risk.
- `task396`: terminal dtype/table cleanup; low-medium hidden risk.
- `task305`: suffix row pruning; hidden-risky and should not be bundled again.
- `task366`: broad 10-alias predicate bundle; hidden-risky and should not be
  bundled again without isolation.

## Next Step

Do not reuse `task305` or `task366` filler candidates in mixed bundles. The
repaired `task343` candidate is a better next isolated target:

```text
/tmp/neurogolf_task343_repair_p4col0_guard.onnx
task343 2015 -> 1895, gain 0.061400356864
n_fail=0
```
