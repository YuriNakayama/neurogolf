# 20260702 case463 plan - task074 symmetry graph re-gate

## Context

Case462 rejected `task340`. The next simple current graphs by relative
opportunity are `task074` and `task350`, both around cost 9000 and dominated by
full 30x30 intermediates. `task074` has only eight nodes, so a quick current
re-gate can determine whether any stale symmetry alias is exact.

Historical case113 found that all large `task074` reductions failed local
correctness, but there are many later scratch candidates from alias scans and
generic surgery.

## Target

```text
task074
params=50
memory=9000
cost=9050
n_fail=0
```

## Method

1. Gather shallow `task074*.onnx` candidates from `/private/tmp` and `/tmp`.
2. Cost-screen against current accepted baseline.
3. Audit positive reductions with task074 examples.
4. Submit only if:

```text
n_fail=0
gain >= 0.020
no forbidden ops/functions
```

If no exact positive appears, move to `task350`.
