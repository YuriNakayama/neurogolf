# 20260702 case345 result - task319 p10 predicate alias

## Result

- Previous accepted Public Score: `7185.28`
- New Public Score: `7185.33`
- Kaggle submission ref: `54237222`
- Submission time: `2026-07-01 15:43:52 UTC` (`2026-07-02 00:43:52 JST`)

Accepted.

The submit interval from case344 was about 30 minutes, matching the
submit-saving cadence.

## Local Gate

`task319` only:

```text
baseline:  params=133, memory=20386, cost=20519
candidate: params=132, memory=19437, cost=19569
n_fail=0, status=ok
gain = ln(20519 / 19569) ~= 0.0474
```

## Change

Replaced:

```text
p10_pair_match -> p10_top_eq_all
```

This is a compact medium-risk predicate alias. The broader `p10_*` alternatives
were similar in gain but changed more graph surface, so the isolated alias was
preferred for diagnosability.

## Deferred Candidates

`task118` patch lookup pruning was not submitted:

- rank1 was locally exact and large (`39237 -> 35807`, gain `0.0915`)
- risk audit showed the positive-table savings delete many lookup targets,
  including singleton targets, rather than duplicate-only cleanup
- keep it out of normal cadence unless explicitly spending a high-risk attack
  submit

`task209` was strengthened but still below threshold:

```text
alignment_fits -> key_b
kern_src_r_cl -> kern_src_r
kern_src_c_cl -> kern_src_c
28683 -> 28047, gain ~= 0.0224, n_fail=0
```

Bank this version, but do not submit standalone.
