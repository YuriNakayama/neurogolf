# 20260630 case82 plan — t233 の memory 支配箇所を一点検証する

## 背景

- 現 floor は case74 採用後の Public best **7180.85**。
- case75 以降は微小削減・候補数削減・dtype 遅延を試したが、実 LB 不変または faithful 監査で棄却。
- t233 は cost **51068** の最高コスト級タスクで、過去に community harvest で大きく改善済み。ただし現在も memory **50512** / params **556** と memory 支配であり、局所削減 1 件でも LB に効く可能性がある。

## 仮説

t233 は多段の `TopK` / `Gather` / `Scatter` / `ReduceMin` ブロックで構成されている。まず runtime trace で大きい中間テンソルを特定し、次のどちらか一点に絞って削減可能性を調べる。

1. `TopK` の候補数 (`k5` または `k3`) を減らせるか。
2. 早期の bool→uint8 `Cast` を遅延または削除できるか。

ONNX 型制約上、`QLinearConv` 入力や `ReduceMin` 入力は uint8 が必要になる可能性が高い。したがって、まず faithful 監査で壊れない候補だけを残し、n_fail=0 かつ cost 減少の候補が出た場合のみ submit する。

## 対象

- `data/output/onnx/task233.onnx`

## 足切り

- `src.evaluate.scorer.audit_one` で `task233` の train/test/arc-gen 全ペア完全一致を確認する。
- 変更していないタスクの全数監査は行わない。

## 採否

- n_fail=0 かつ cost 減少なら Kaggle submit。
- Public Score が 7180.85 を上回れば採用し、DVC push と git commit/push を行う。
- 上がらなければ ONNX を DVC checkout で戻し、result のみ記録して commit/push する。
