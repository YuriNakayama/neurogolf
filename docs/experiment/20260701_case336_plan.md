# 20260701 case336 plan - task055 row/column alias bundle

## Context

- Current accepted Public Score floor: `7185.01` after case335.
- Priority rule: `n_fail=0` first, then relative reduction
  `old_cost / new_cost`.
- A fresh equivalence scan after case335 found one new exact candidate above
  `ln(old/new) > 0.02`: task055 aliases `rbC` and `cbC` to earlier tensors.

## Target

- `data/output/onnx/task055.onnx`

## Baseline

Current local audit:

```text
params=277
memory=3060
cost=3337
n_fail=0
status=ok
```

## Hypothesis

The current graph materializes `rbC` and `cbC`, but their audited values match
the earlier `rb` and `cb` tensors at all consumers. Rewiring both consumers to
the earlier tensors should preserve outputs while pruning the redundant aliases.

Exact bundle:

```text
rbC -> rb
cbC -> cb
```

## Expected Gain

Temporary audit:

```text
params=276
memory=2940
cost=3216
n_fail=0
status=ok
```

Expected point gain:

```text
3337 / 3216 = 1.0376
ln(old/new) ~= 0.0369 pt
```

## Verification Plan

1. Apply only the `task055` alias bundle.
2. Run individual `evaluate.audit_one` for `task055`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
