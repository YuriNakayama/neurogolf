# case218 plan: task181 mirror coeff branch trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task181` baseline は `cost=369` (`params=168`, `memory=201`, `n_fail=0`)。
- graph は `Slice(mirror8) -> GatherND(flag) -> Cast -> Where(coeff_left/right) -> Mul -> ScatterND`。

## 対象

- `data/output/onnx/task181.onnx`

## 仮説

`flag_index` で左右どちらの係数を使うかを選び、mirror した 3x3 patch を `coeff_left` または `coeff_right` で channel 変換して scatter している。もし実データ上で片側 branch に固定できるなら、`GatherND/Cast/Where` を削れて `369 -> 300` 前後まで下がる可能性がある。相対削減率が十分あるため一点検証する。

## 検証

- temp ONNX で `coeff -> coeff_left`、`coeff -> coeff_right` を probe する。
- `updates -> coeff_left * mirror8` / `coeff_right * mirror8` の固定 branch を probe する。
- `flag_index` 固定や `left_bool` 固定も確認する。
- `src.evaluate.scorer.audit_one` で `task181` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
