# 20260628 case3 plan — 隔離コンテナでの floor 再検証 + 残存レバー探索

## 現状把握（git log + 過去ログ + faithful 実測）

- floor bundle = `data/output/onnx`（DVC 管理, combined_best, **実 LB 7172.43**）。
- この環境は**隔離コンテナ**: sibling worktree 無し → 過去の主力レバー **dsl-harvest が利用不可**。
- faithful スタック（onnx 1.20.0 / ort 1.24.1, offset +0.11）で全 400 タスクを cost-only 再ランキング:
  - sum_points(no-correctness) = **7172.74**（floor 7172.43 と整合、ランキングは真値）。
  - top cost: t233(70602) t018(66094) t286(52015) t118(42418) t187(40446) t349(38986)…
  - 0点(未解決)タスクは無し。146/149/240/384 は repo-default 版差の false-negative（実 LB は正常採点）。

## 仮説と検証（本サイクルで潰した dead end）

1. **fp32 中間テンソル一括 narrowing**: repo-default 自前計測で「fp32 が全メモリの65%」と出たが、
   **faithful audit_one では task400=1866**（repo-default 自前計測 37800 は私のトレース解析バグ）。
   → 偽の機会。faithful ランキングが真値で、bundle は既に golf 済み。
2. **whole-graph fp16 変換**（onnxconverter_common, keep_io_types）: 上位タスクの多くは
   「already fp16」で変換拒否＝bundle が fp16 適用済み。変換できた t054/t187 も **cost 増**
   （Cast 挿入で 27133→61313, 40446→58446）。t133/t158 は invalid model 化。→ dead end。
3. **top タスク redesign**: t233/t018/t286 は可変出力形状の非局所抽出（TopK/ArgMax/ScatterND/BitShift）
   ＝専門 golf 済み。t243 は 0→1 fill だが連結性依存の非局所伝播（28-QLinearConv）で安価ルール無し。

## 本サイクルで回す 1 改善

- **既存 surgery スイート（意味保存・正答&cost減のみ採用）を floor 全体に適用**。
  floor は cherry-pick の combined_best で、この repo の surgery 群（index_surgery /
  broadcast_compress / conv1x1_to_gather / fp16_surgery / identity 除去）が未適用の可能性。
- 期待: cost は memory 支配だが params 大のタスク（t367=5127, t118=4769, t018=3037,
  t158=2343, t187=1546, t133=1278）で param 削減 → 各 +0.1〜0.2 の可能性。
- 採否: surgery_out を **faithful で全数 audit** し、floor 比で総点が上がった場合のみ提出 →
  実 LB で退行ゼロを確認して採用。下がれば破棄。
