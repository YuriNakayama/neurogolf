# 20260701 case317 plan - task135 fixed crop replacement

## Context

- Current accepted Public Score floor: `7183.26` after case315.
- case316 found no submit-worthy cost-reduction candidate and only updated the
  micro backlog.
- Priority rule remains: `n_fail=0` first, then maximize relative improvement.

## Target

- `data/output/onnx/task135.onnx`

## Baseline

Current local audit:

```text
params=None
memory=None
cost=None
n_pass=266
n_fail=0
status=score_error:[ShapeInferenceError] Conv pads contains negative values
```

The current graph is a one-node `Conv` using negative pads. It is functionally
correct locally but unscorable by the local mirror. Kaggle may still score it,
so adoption must be decided by real Public LB.

## Hypothesis

All 266 available examples satisfy:

```text
output = input[0:3, 6:9]
```

Replacing the negative-pad `Conv` with a standards-compliant fixed crop:

```text
Slice(input, rows 0:3, cols 6:9) -> Pad(bottom=27, right=27)
```

should preserve correctness and make the task locally scorable with very low
cost. Because this changes a locally unscorable but possibly Kaggle-valid graph,
the ratio is not defined; the expected value is judged by real LB.

## Verification Plan

1. Generate a two-node ONNX candidate for `task135`.
2. Run individual `evaluate.audit_one` for `task135` with correctness enabled.
3. Submit only if `n_fail=0`, `status=ok`, and local cost is finite.
4. Adopt only if Kaggle Public Score improves; otherwise restore the previous
   DVC-tracked ONNX and record rejection.
