# 20260703 case544 plan - repair high-gain scorable local fails

Accepted Public Score:

- `7187.43`

## Background

Case542 adopted task023 and raised Public Score to `7187.43`. Case543 found no
submit-ready follow-up, but it did expose several large scorable local-fail
candidates:

```text
task192  8621 ->  6820 gain=0.234342 n_fail=19
task219 14823 -> 11787 gain=0.229183 n_fail=94
task255 22541 -> 16037 gain=0.340437 n_fail=138
```

The task023 follow-up candidate is exact but below standalone threshold:

```text
task023 8548 -> 8476 gain=0.008459 n_fail=0
```

The best max-5 micro bundle remains `0.003669`, below the `0.015` gate.

## Hypothesis

Repairing the lowest-fail high-gain candidate, task192, has the best expected
payoff. If an exact repair can preserve even a fraction of the `0.234` gain
above `0.020`, it is worth submitting. Task219 and task255 are secondary repair
targets because their fail counts are higher.

## Method

1. Inspect task192 failure patterns from the case543 candidate.
2. Try bounded repairs that preserve scorable cost and static shapes.
3. In parallel, inspect task219/task255 failure summaries for cheap exact
   repairs.
4. Recheck task023 `asg3 -> asgs3` only as banked material; do not submit below
   threshold unless combined bundle reaches the normal micro gate.

## Submit Rule

Use normal pre-submit gate only. Do not use `--skip-pre-submit-gate`.

Submit only if:

- candidate has `n_fail=0`;
- baseline and candidate costs are both known;
- no forbidden ops/functions;
- standalone gain `>=0.020` or micro-bundle gain `>=0.015`.
