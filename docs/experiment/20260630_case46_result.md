# 20260630 case46 result — locally-0点8タスクは Kaggle で最適得点済み、加点余地なし（提出せず=退行回避）

## independent_impl（必須欄）
未試行路線「未解答タスクへの加点」を再検証:
- **t347 の正答 net を新規構築**: 規則 3×6→3×3 左右半分 OR→色6（269/269 厳密一致）。
  crop技法で [1,10,3,3] 処理→Pad。**faithful n_fail=0, cost=11410**（私の自作・正答）。
- **floor t347 の Kaggle 実挙動を精査**（case9 実測履歴）:
  - floor t347 は cost **143**、ローカル ORT では MaxUnpool クラッシュ（=local偽陽性で「0点」に見える）。
  - **Kaggle 実 LB では正常採点され ~19.7点**（case9 で実証済み）。
  - case9 で OR-fold net(cost 12014)に置換 → **実 LB −4.43**（floor 19.7点 → 自作 15.6点で損）。
- **採否判断**: 私の net(cost 11410=~15.65点) < floor t347(cost 143=~19.7点)。**swap は退行**。
  → **submit しない**（退行ゼロ優先）。

## 確定（最後の未試行路線も閉じた）
- locally-0点の8タスク(t045/127/135/146/149/240/384/347)は **すべて Kaggle 実 LB では
  最適に得点済み**（floor net がローカル ORT 固有のクラッシュをするだけ、cost も極小: t347=143）。
- **「未解答タスク加点」路線は加点余地ゼロ**。これらは未解答でなく、ローカル偽陽性。
  case9 で −4.43 を実測済み、本 case46 で自作正答 net(cost 11410≫143)を構築して再確認。
- floor を local 監査根拠で置換しない運用則（case9）を再実証。

## 12サイクルの最終確定
独自実装の全路線を検証:
- cost削減（規則decode/手構築/学習fit/sparse/crop/param/channel/構造クラスタ/scorer精読）→ floor が下限。
- **加点（未解答タスク正答化）→ 該当タスクは Kaggle で既に最適得点（local偽陽性）**。
- ∴ 現 op セット・cost モデル・floor の最適性の下で、独自実装の純増余地は cost削減・加点とも枯渇。
- 恒久方針に従い停止提案はせず、新規 op/規則表現/仕様変更があれば即試行を継続する。

## 次の一手
- floor 7180.78（DVC 6e8d4642）退行なし。新しい角度（未知の op 組合せ等）を次サイクルで探索継続。
