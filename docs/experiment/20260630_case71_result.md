# 20260630 case71 result — t349 の冗長 Cast を除去し cost 26189→25289、Public 7180.81 採用

## independent_impl:
t349（9ブロック間の bridge/beam 描画、現行 floor cost 26189）を graph surgery で独自最適化:
- memory trace を確認: 24 nodes、`[1,1,30,30]` 中間が大半で memory 26100。
- 5本 `QLinearConv` detector は各1本削除すると n_fail>0（drop c0..c4: 189/117/78/42/17 fail）で全て必要。
- 一方で `beam_u8` は `MaxPool(ch9_u8)` 由来の 0/1 uint8。既存 graph は
  `beam_u8 -> Cast(bool) -> Cast(uint8=color1)` と戻してから `Where` に入れており、後段の色1入力として
  `beam_u8` を直接使える。
- `Cast color1` を除去し、`Where(halo_b, three_u8, beam_u8)` に変更。

## faithful 検証
- before: cost 26189, params 89, memory 26100, n_pass 267, n_fail 0
- after: **cost 25289, params 89, memory 25200, n_pass 267, n_fail 0**
- 削減: **900 cost**（1個の `[1,1,30,30]` uint8 中間）

## submit
- ref: `54190398`
- message: `case71 t349 remove redundant cast cost 26189 to 25289`
- status: `COMPLETE`
- Public Score: **7180.81**（floor 7180.78 から **+0.03**）

## 採否
- **採用**。ローカル faithful n_fail=0 かつ実 LB が 7180.78→7180.81 に上昇。

## 次の一手
- t349 はさらに `beam_b` / `ch9_b` / `valid_b` 周辺に bool/uint8 変換が残るが、`Where` 条件には bool が必要。
  次は同種の「bool→uint8 に戻す冗長 Cast」パターンを高 cost task 全体から機械検索する。
