# 20260630 case84 plan — t018 の MaxPool/TopK 反復を一点検証する

## 背景

- 現 floor は Public best **7180.85**。
- case82 t233、case83 t286 はいずれも局所削減候補なし。
- t018 は cost **45211**（params 2087 / memory 43124）で、929 node の多段変換 graph。過去ログでは per-object 回転/marker 整列型で、ONNX は非局所プログラム相当とされている。

## 仮説

t018 は序盤に `TopK` と 5x5 `MaxPool` 反復で object/marker 周辺を抽出している。以下のどれかが可能なら memory を削れる。

1. `TopK` の候補数を下げる。
2. 5x5 `MaxPool` 反復を 1 段減らす。
3. bool→uint8 / uint8→float16 `Cast` を遅延または削除する。

まず trace で大きいテンソルを特定し、候補は temporary ONNX で `audit_one` の full train/test/arc-gen 完全一致を確認する。

## 対象

- `data/output/onnx/task018.onnx`

## 足切り

- `src.evaluate.scorer.audit_one` で t018 の train/test/arc-gen 全ペアを完全一致監査。
- n_fail=0 かつ cost 減少の場合のみ submit。

## 採否

- Public Score が 7180.85 を上回れば採用。
- 上がらない、または faithful で不一致なら破棄。
