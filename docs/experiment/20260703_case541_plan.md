# 20260703 case541 plan - task173 risk rehabilitation and structural search

Accepted Public Score:

- `7187.41`

## Background

Case540 increased candidate-supply workers to six, but all safe non-risk hits
collapsed to the same five tiny micro reductions:

- task117, task234, task328, task206, task005.
- Aggregate gain: about `0.001982`, below the `0.015` micro-bundle gate.

The only standalone-size exact hit was task173:

- `15664 -> 15020`
- gain `0.041982`
- `n_fail=0`

It was not submitted because case510 already submitted the same dtype-shrink
family and Kaggle returned `ERROR`.

## Hypothesis

Submit cadence is blocked by safe candidate scarcity, not by worker count.
The next high-yield path is to either:

1. rehabilitate task173 by isolating the Kaggle ERROR mechanism and producing a
   variant that avoids the risky graph feature; or
2. find a non-risk structural replacement whose gain clears the standalone gate.

## Method

Run work in parallel:

1. Compare current task173, the case510 dtype-shrink candidate, and older
   accepted task173 variants for opset, type constraints, TopK/Gather/Scatter
   paths, and checker/shape-inference warnings.
2. Try task173 repair variants that preserve most of the dtype-shrink gain while
   avoiding the previously suspected runtime hazards.
3. Search non-risk tasks for structural simplifications with target gain
   `>= 0.020`, avoiding already-depleted one-byte initializer edits.
4. Keep accumulating exact low-risk micros, but do not submit them until the
   current bundle gate is met.

## Submit Rule

Submit only if:

- exact local gate has `n_fail=0`;
- no forbidden ops/functions;
- the task is non-risk, or a review-risk task has a new isolated safety
  argument that directly addresses the prior Kaggle failure;
- standalone gain is `>= 0.020`, or a safe micro bundle reaches `0.015`.

If no candidate clears this, record no-submit and keep the task173 analysis as
future risk evidence.
