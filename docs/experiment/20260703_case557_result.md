# 20260703 case557 result - task349 halo repair

## Summary

- Submit: `case557 full task349 halo repair`
- Public Score: `7187.54 -> 7187.57`
- Decision: adopt

## Candidate

- Task: `task349`
- Candidate: `/private/tmp/neurogolf_agent_case556_B/candidate/task349.onnx`
- Baseline cost: `24369`
- Candidate cost: `23612`
- Relative gain: `0.031557`
- Gate: `n_fail=0`, `functions=0`, forbidden ops `[]`

## Change

The previous high-gain candidate collapsed `halo_c4` into the color-3 halo and
failed 17 `arc-gen` examples. The repaired model restores the necessary
`halo_c4` source while keeping the reduced gather structure.

Allowed ops only:

```text
Cast, Equal, Gather, Max, MaxPool, QLinearConv, ReduceMax, Where
```

## Verification

Full 400-task dry-run passed with one changed task:

```text
task349 24369 -> 23612 gain 0.031557 n_fail=0 decision=submit-candidate
bundle decision: submit-bundle
```

## Adoption

The Public Score improved to `7187.57`, so the candidate was copied to:

```text
data/output/onnx/task349.onnx
```

## Next Step

Continue standalone search. The `task364` guarded repair is exact but has no
cost gain, so deprioritize it unless a new structural saving appears.
