# 20260703 case540 result - parallel candidate supply expansion

Accepted Public Score:

- `7187.41`

## Summary

No submit.

The number of parallel exploration workers was increased to six to restore
candidate supply for the requested high submit cadence. The workers covered
mechanical simplification, near-miss repair, solver/custom replacement, micro
accumulation, dtype/initializer compression, and backlog mining.

## Worker Results

Worker A, low/mid-cost mechanical simplification:

- Found only the existing five exact low-risk micros.
- Best bundle remains:
  - task117: `4148 -> 4145`, gain `0.000723502`.
  - task234: `6845 -> 6841`, gain `0.000584539`.
  - task328: `6904 -> 6902`, gain `0.000289729`.
  - task206: `4183 -> 4182`, gain `0.000239091`.
  - task005: `6882 -> 6881`, gain `0.000145317`.
- Aggregate gain: `0.001982178`, below the `0.015` micro-bundle gate.
- Artifact: `/private/tmp/neurogolf_agent_case540_A/exact_candidates.csv`.

Worker B, near-miss repair refresh:

- Found task173 exact dtype-shrink:
  - path: `/private/tmp/neurogolf_agent_case540_B/task173_cast10_to2.onnx`.
  - `15664 -> 15020`, gain `0.041982`, `n_fail=0`.
  - Not submitted because task173 is in a known-risk family from case510
    Kaggle ERROR.
- Non-risk repair attempts did not produce an eligible exact candidate:
  - task343 faithful repair: `1895 -> 1895`, exact but no gain.
  - task343 cheaper repair: `1895 -> 1847`, gain `0.025656`, `n_fail=13`.
  - task243 scatter/local repairs: gain `0.014184`/`0.013199`, still failing.

Worker C, solver-bank/custom replacement:

- No exact lower-cost candidate.
- task127 case3 candidate was exact but baseline cost was unscorable.
- task135 custom crop candidate was exact but not proven lower-cost.
- task187 exact no-gain; task192 exact but worse.
- Artifact: `/private/tmp/neurogolf_agent_case540_C/summary.json`.

Worker D, micro accumulator:

- Re-validated the five-task micro queue.
- No additional non-risk exact micro improved the bundle.
- task263 `680 -> 679` was positive but review-known-risk, so excluded.
- Artifacts:
  `/private/tmp/neurogolf_agent_case540_D/bundle_proposal.md`,
  `/private/tmp/neurogolf_agent_case540_D/bundle_proposal.json`.

Worker E, dtype/initializer compression:

- Re-found exact low-gain task117 and task206 only.
- No standalone or bundle-clearing candidate.
- Artifact: `/private/tmp/neurogolf_agent_case540_E/exact_lower.json`.

Worker F, backlog mining:

- No exact current-baseline non-risk candidate with individual gain `>= 0.010`.
- Confirmed the same below-gate safe micro bundle.
- Artifact: `/private/tmp/neurogolf_agent_case540_F/report.md`.

## Submit Decision

No submit.

The only large exact improvement found was task173, but that task family is
known to have produced Kaggle ERROR despite local exactness. Submitting it would
optimize cadence at the expense of the current anti-regression rule. The safe
low-risk micro bundle is still far below the current bundle threshold.

## Conclusion

Increasing subagents improved coverage but did not produce a safe submit-ready
candidate in this cycle. The practical bottleneck is no longer worker count; it
is the scarcity of exact non-risk candidates that clear either:

- standalone gain `>= 0.020`; or
- five-task low-risk bundle gain `>= 0.015`.

Next cycle should focus on one of two higher-yield directions:

1. Isolated review-risk rehabilitation for task173, proving why the case510
   Kaggle ERROR occurred before any submit.
2. A new structural search over non-risk tasks that can produce larger exact
   reductions than the current one-to-four-byte micro edits.
