# 2026-06-30 case115 plan

## 現状

- floor: Public Score 7180.97
- 優先基準: 相対削減率 `old_cost / new_cost` を重視。
- case114 `task196`: 10% 削減候補は最良 `n_fail=6` で不採用。

## 対象

- `task265.onnx`
- baseline: params 34, memory 4039, cost 4073, n_pass 266, n_fail 0
- graph:

```text
g5_u8 -> MaxPool -> gany_u8
anchor_u8 = 1 - gany_u8
pair_u8 = Slice(anchor_u8)
pair_flag = ReduceMin(pair_u8)
pair_keep = 1 - pair_flag
anchorf_u8 = ScatterND(anchor_u8, fix_idx, pair_keep)
red_u8 = MaxPool(anchorf_u8)
Pad(red_b) -> Where(color2,input)
```

## 仮説

`ScatterND` の pair 補正、または最後の `MaxPool` が一部データだけの保険なら、1 個の 30x30 級中間を消して 5% 以上の相対削減が見込める。

## 手法

1. `anchorf_u8` を `anchor_u8` に bypass し、pair 補正全体を prune。
2. `red_u8` を `anchorf_u8` / `anchor_u8` に差し替え、最後の MaxPool を prune。
3. `pair_keep` を constant 0 / 1 に固定して補正値の必要性を確認する。
4. `src.evaluate.scorer.audit_one` で `task265` の完全一致を確認する。
5. `n_fail=0` かつ cost が下がる候補のみ submit する。

## 足切り

- 変更対象は `task265.onnx` のみ。
- 禁止 op は追加しない。
- 実 Public Score が上がれば採用、上がらなければ破棄する。
