# 20260702 case455 plan - task230 conv-kernel crop re-gate

## Context

Cases452-454 closed the small Pad/output-mask stale-pool targets
`task102`, `task190`, and `task062`. The next pass should avoid another broad
alias pool and instead use current-graph structure.

An internal-tensor scan, excluding tasks already closed by current-baseline
results or known-risk gates, ranked several low-cost Conv-weight tasks highly.
`task230` has no dedicated current-baseline result and has existing kernel-crop
scratch candidates.

## Target

`task230.onnx`

Current baseline:

```text
params=900
memory=0
cost=900
points=18.1976052367
n_fail=0
status=ok
```

The graph is dominated by a single Conv initializer:

```text
conv_weight float [10,10,3,3] = 900 params
```

## Hypothesis

If one border of the 3x3 kernel is unused for this task, replacing it with a
smaller equivalent kernel would produce a large relative score gain on a low
cost task. However, kernel cropping often removes real neighborhood semantics,
so it must be gated strictly.

## Method

1. Gather existing `task230*.onnx` candidates, especially case362 kernel crops.
2. Run current-baseline `evaluate_candidate_gate` on every cost-reducing
   candidate.
3. Submit only if a candidate reaches `submit-candidate`; otherwise record the
   no-submit result and move to the next structural target.

## Gate

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
no forbidden ops/functions
single task only
```
