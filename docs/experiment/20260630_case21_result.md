# 20260630 case21 result — t048 は「8個の2マーカー配置で0/8を符号化」する複雑構造規則, 再構築不可（提出なし）

## 結論（提出なし、floor 7180.62 維持）
- over-built 候補スキャン: cost 911-2500 × node>=8 で 85 タスク。最多ノードは **t048(1146, 113nodes,
  1×1出力)**。
- t048 規則精査: 入力に色 2 が**常に 8 個**、色 0/8、出力は 0 or 8 の 1×1。8 の個数は判定材料でない
  （13→0, 10→8 と非単調）。**8 個の「2」が形成する配置/パターンが答え(0/8)を符号化**する構造規則
  （digit/shape 読み取り系）。113-node net がこのデコードを実装。clean な閉形式なし → 再構築不可。
- 低コスト帯でも規則は intricate で、node 数は真のデコードロジックを反映（over-built ではない）。

## 確定（全コスト帯で floor 最適を確認）
- 高コスト(t233/t118/t364) / 中コスト(t293/t243/t077) / 低コスト(t048/t171) の全帯域で、floor net の
  複雑度は規則の真の複雑度に一致。**over-built タスクは存在しない**。
- 本日 case1-21 で hand-golf 全経路 + harvest 全バンドルを網羅棄却。floor 7180.62 = 全レバー飽和点。

## 次の一手
- 新規 publish 監視継続（外部レバー, +0.07 実証）。floor 7180.62 維持。7950 まで −769.38。
