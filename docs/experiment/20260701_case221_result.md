# case221 result: task393 TopK rank trim

## 結果

- 対象: `task393`
- baseline: `cost=144` (`params=19`, `memory=125`, `n_fail=0`)
- submit: なし
- 採否: 不採用

## 検証内容

temp ONNX で以下を個別に差し替え、`src.evaluate.scorer.audit_one` で検証した。

- `TopK k=3/4/5/6/9`
- Slice range variants
- `s` / `st` の固定色 triples

## 監査結果

| 変更 | cost | n_pass | n_fail | 判定 |
| --- | ---: | ---: | ---: | --- |
| baseline `k=4`, rank `1:4` | 144 | 265 | 0 | ok |
| `k=3`, rank `0:3` | 131-132 | 0 | 265 | reject |
| `k=4`, rank `0:3` | invalid | 0 | 265 | reject |
| `k=5/6/9`, rank `1:4` | 158-210 | 265 | 0 | reject: cost worsens |
| other ranges | invalid or incorrect | 0 | 265 | reject |
| fixed `s` / `st` triples | 47-50 | 0-1 | 264-265 | reject |

## 結論

TopK の rank `1:4` が全ペア正答に必要で、`k=3` への縮小は不可だった。固定色なら `144 -> 47` まで下がるが足切りを満たさない。実 ONNX は変更せず、Kaggle submit もしない。

次は `TopK` 以外で、固定 index table の shape や scatter index を削れる候補を探す。
