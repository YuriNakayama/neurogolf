# 20260702 case374 result - skip task025 final feature dtype probe

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.86
```

## Baseline

Target:

```text
data/output/onnx/task025.onnx
```

Current local audit:

```text
params=144
memory=11520
cost=11664
n_fail=0
```

## Probes

### Final output bool

Added:

```text
output_f16 = Einsum(...)
output = Greater(output_f16, 0)
```

Result:

```text
cost=29665
n_fail=0
```

This is exact, but the original full `[1,10,30,30]` f16 output remains as an
intermediate and a new bool output is added, so memory increases sharply.

Declaring the existing f16 output as bool without a conversion is rejected by
ORT due output type mismatch.

### `pos_onehot` float16

Changing `onehot_values` to float16 makes `pos_onehot`, `left`, and `right`
float16, but downstream color-count `Einsum` also consumes `input` float32.
ORT rejects the mixed float/float16 `Einsum`.

### Generic cleanup

One-file case3 surgery:

```text
surgery done: 0/1 tasks improved
```

## Conclusion

Skip submit. Task025's large f16 surface is tied together by final and
intermediate `Einsum` type constraints. The output can be converted to bool only
by adding a full-size tensor, which is worse than the baseline.

Next action: avoid f16 feature graphs where the input tensor participates
directly in the same `Einsum`; prefer graphs with an isolated integer-valued
tail like case369.
