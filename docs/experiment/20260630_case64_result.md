# 20260630 case64 result — line-drawing t356(connect-same)を独自構築, floor の code+ConvInteger 出力技法を解明（提出なし）

## independent_impl（必須欄・floor の出力符号化を解明）
未試行角度「line-drawing(connect-same-color)」を全 floor から探索:
- connect-same solvable: **t041(1789, 全10色), t356(1319, 単色{0,8})** の2件。
- **t356 構築**（between aligned 8-pairs, 266/266, 10×10）:
  - 方向別 MaxPool(above/below/left/right)+AND/OR で between mask。uint8。
  - 版1（10ch Concat 出力）: cost **2423**。`small[1,10,10,10]`=1000B が overhead。
  - 版2（segmented channel-pad）: **4038**（pad チェーンで悪化）。
  - 版3（Conv 出力, float MaxPool）: **7242**（float 中間で悪化）。
- **floor の出力技法を解明**: floor は方向別 MaxPool(私と同一: has_left/right/up/down, row/col_span)
  の後、**単一 uint8 channel `code`** に出力色を符号化し、**ConvInteger 1発で 10ch に展開**。
  → **10-channel 中間を一切作らない**。これが私の 1000B overhead を回避する floor の鍵。

## 確定（floor の核心技法）
- floor total 1300（crop_f 400 + uint8 単channel群）vs 私 2400（+10ch small 1000）。
- **出力 one-hot 展開を Concat でなく ConvInteger(単channel→10ch)で行うと 10ch 中間が消える**。
  これが floor が私より一段安い理由。私の獲得技法に**「ConvInteger 出力展開」**を追加。
- ただし floor の構造を完全模倣すれば parity（1300前後）で、純増には至らない見込み。

## 本セッションの獲得技法（更新）
crop / 単channel / uint8中間・出力 / frame seed / CumSum→MaxPool / CC-free分類 /
**ConvInteger 単channel→10ch 出力展開（10ch中間回避）**。
→ これらで独自実装は floor と parity 到達可能。だが floor も全適用済みで純増せず。

## 恒久方針の遵守
- 停止せず t356 を構築し floor の code+ConvInteger 出力技法を解明（重要な技法獲得）。floor 7180.78 退行なし。
- 各タスクの実 build/measure に基づく。次も継続。

## 次の一手
- ConvInteger 出力展開を含む完全 floor 模倣で t356/t041 を parity 化（純増は出力前 op が floor より
  少ないタスクが必要）。または ConvInteger 技法で他タスクの 10ch 中間を削れるか横展開。
