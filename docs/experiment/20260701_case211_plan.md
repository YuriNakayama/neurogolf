# case211 plan: task103 compact equality concat trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task103` は未着手で、baseline は `cost=60` (`params=24`, `memory=36`, `n_fail=0`)。
- `h` と `i=Not(h)` を `Concat(h, z, z, z, z, z, i)` して Pad する小型 graph。

## 対象

- `data/output/onnx/task103.onnx`
- 左右 3x1 slice の完全一致 `h=ReduceMin(Equal(l,r))` とその反転 `i` で 7-channel vector を作る。

## 仮説

`h` または `i` が実データ上で固定、または `i` を既存 bool に alias できれば `Not` や比較 chain の一部を削れる。定数化は params 増があるため、cost も合わせて確認する。

## 検証

- temp ONNX で `h/i` の fixed/alias と `j` の fixed vector を probe する。
- `src.evaluate.scorer.audit_one` で `task103` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
