# 2026-07-01 case148 plan

## 方針

- floor: Public Score 7181.16
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task110.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task110.onnx`
- baseline: params 632, memory 13155, cost 13787, points 15.468518601828372, n_pass 266, n_fail 0

## 仮説

`task110` は Conv で label / inverse label を作り、period 5-9 の MaxPool table から有効な period を選択する。最後に public row 7/9 の特例 table を `use_public` で上書きする。

period selector (`p6_valid` / `p7_valid` / `p8_max_ok` / `p9_max_ok`) や public override が実データで限定されていれば、大きい table branch を外せる可能性がある。cost が 13787 と大きく、1 branch でも相対削減が submit に反映しやすい。

## 手順

1. `/tmp` 上の candidate ONNX で各 period selector を片側へ差し替え、完全一致する branch を探す。
2. `use_public` / `is_public_row7_s` の public override を固定または既存 period 側へ差し替える。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task110.onnx` に反映する。
4. submit し、Public Score が 7181.16 を上回れば採用、上がらなければ破棄する。
