# 20260703 case519 result - task243 repair blocked

## Result

No submit.

Current accepted Kaggle score remains:

```text
Public Score: 7187.37
latest accepted ref=54271014
```

## Target

Task243 was selected from case518 high-upside near misses:

```text
baseline cost=22650
fill26 pruned cost=22002 gain=0.029026 n_fail=1
fill17 pruned cost=16170 gain=0.337002 n_fail=2
```

## Findings

The near misses are late flood-fill truncations.

```text
fill26 candidate:
  misses one arc-gen example
  wrong cell: row=0 col=10
  expected color 1, candidate keeps color 0

fill17 candidate:
  misses two arc-gen examples
  both require late frontier propagation
```

## Repair Attempts

Exact but not cost-reducing:

```text
/tmp/neurogolf_agent_V1/task243_fill26_restore_full_final_step.onnx
cost=22650
gain=0.000000
n_fail=0
```

Exact but worse than baseline:

```text
/tmp/neurogolf_case519_task243_repair/task243_fill26_plus_toprow_from_right.onnx
cost=23015
n_fail=0

/tmp/neurogolf_agent_V1/task243_repair_toprow_leftshift_gate1.onnx
cost=23003
n_fail=0
```

Still incorrect:

```text
/tmp/neurogolf_agent_U5/gated_candidates/243_0132_task243_fill27_to_fill26_pruned.onnx
cost=22002
gain=0.029026
n_fail=1

scalar coordinate repair:
cost=22355
gain=0.013110
n_fail=48
```

## Decision

Do not submit. The task243 saving is real only if the final flood-fill step can
be repaired cheaply. Every exact repair found either restores baseline cost or
exceeds it. The `n_fail=1` candidate remains useful for future structural work,
but it is not submit-eligible.

Next cycle: continue standalone search outside task243 unless a cheaper
task243-specific boundary repair is found.
