# 20260701 case171 result

## Target

- Task: `task303`
- Baseline cost: 1620
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task303`.

No exact cost-reducing candidate was found.

Row/column code simplifications:

- `row_code -> row_active_u8`: cost 1556, `n_fail=209`
- `row_code -> two_u8`: cost 656, `n_fail=265`
- `row_code -> zero_u8`: cost 656, `n_fail=265`
- `col_code -> col_nonmarker_code`: cost 1555, `n_fail=210`
- `col_code -> zero_u8`: cost 626, `n_fail=265`
- `col_code -> two_u8`: cost 625, `n_fail=265`
- `col_code -> col_active_u8`: cost 1525, `n_fail=260`

Mask aliases:

- `red_mask -> marker_rows`: cost 414, `n_fail=260`
- `red_mask -> marker_cols`: cost 414, `n_fail=263`
- `marker_rows -> marker_cols`: cost 2456, `n_fail=263`
- `marker_cols -> marker_rows`: cost 2456, `n_fail=260`

Dimension aliases:

- `height -> width`: cost 1616, `n_fail=209`
- `width -> height`: cost 1616, `n_fail=206`
- `row_sum -> col_sum`: cost 630, `n_fail=265`
- `col_sum -> row_sum`: cost 630, `n_fail=265`

Combined branch removals:

- row no-marker plus col non-marker: cost 1491, `n_fail=265`
- row all-2 plus col non-marker: cost 351, `n_fail=265`
- row no-marker plus col zero: cost 321, `n_fail=265`

Type/shape failures:

- float masks into final `Where`: invalid condition type
- `row_active_u8 -> row_active_f`: invalid `Where` type binding
- `col_active_u8 -> col_active_f`: invalid `Sub` type binding
- graph output set to `input`: unscorable, `n_fail=265`

## Submission

- Not submitted.
- Reason: all cost reductions failed exact-match auditing or produced invalid ONNX.

## Decision

Rejected. Keep the existing `task303.onnx`.

## Next

This row/column marker code is not a promising trim target. Continue toward tasks
where first-order simplification fails on only a handful of examples, or where an
exact dtype/shape rewrite changes scored memory rather than only file size.
