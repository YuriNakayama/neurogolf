# 20260701 case277 plan - task018 bundled safe-name aliases

## Context

- Current Public Score floor: `7181.72`.
- case276 confirmed that a `1.0067` single alias did not move Public Score.
- For this cycle, the priority is a same-task bundle with a larger relative
  reduction while preserving `n_fail=0`.

## Candidate

Target: `task018`

Baseline local audit:

```text
params=2087
memory=41096
cost=43183
n_pass=266
n_fail=0
```

Shape-guided alias scan found three exact aliases:

```text
safe_name_77 -> safe_name_45
safe_name_76 -> safe_name_75
safe_name_74 -> safe_name_72
```

Bundle audit:

```text
params=2087
memory=37496
cost=39583
n_pass=266
n_fail=0
status=ok
```

Relative reduction: `43183 / 39583 = 1.0909`.

## Plan

Apply all three aliases to `data/output/onnx/task018.onnx`, then prune dead nodes
and stale metadata.

Gate with `evaluate.audit_one` for `task018` only. Submit only if:

- `status=ok`
- `n_fail=0`
- cost remains `39583`

This is a stronger candidate than the previous rejected micro aliases because it
removes four 900-byte memory-counted intermediates from one task.
