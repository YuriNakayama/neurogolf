# 20260702 case344 result - task187 sentinel label conv

## Result

- Previous accepted Public Score: `7185.13`
- New Public Score: `7185.28`
- Kaggle submission ref: `54236314`
- Submission time: `2026-07-01 15:13:26 UTC` (`2026-07-02 00:13:26 JST`)

Accepted.

This submit was only about 16 minutes after case342, shorter than the default
30-minute cadence. It was still justified by the submit-saving policy because
the candidate was isolated, locally exact, and had large expected gain
(`~0.1587`), far above the standalone threshold.

## Local Gate

`task187` only:

```text
baseline:  params=1367, memory=30900, cost=32267
candidate: params=1008, memory=26525, cost=27533
n_fail=0, status=ok
gain = ln(32267 / 27533) ~= 0.1587
```

## Change

Folded separate label and occupancy computation into one sentinel-label
convolution:

- valid one-hot cells emit labels `0..9`
- zero-vector padded cells emit sentinel `10`
- `free_b_25` is derived from `label_grid_25 == 0`
- `not_grid_b_25` is derived from `label_grid_25 == 10`

This removes the separate occupancy `Conv` path and its downstream `Where` /
mask construction while preserving local correctness.

## Follow-Up

Parallel probes found additional material but no immediate low-risk submit:

- `task118` has table-pruning candidates with gains up to `0.0915`, but they are
  patch lookup-table reductions and are hidden/private sensitive. Treat them as
  a future isolated attack candidate, not a batch item.
- `task209 alignment_fits -> key_b` is exact locally with gain `0.0213`, below
  the medium standalone threshold. Bank it for a future compatible bundle.
- `task054`, `task255`, and `task364` high-gain aliases failed correctness and
  should not be submitted.
