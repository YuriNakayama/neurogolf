# 20260701 case184 result

## Summary

- Target: `task243`
- Baseline: params 42, memory 22608, cost 22650, points 14.97208486908882
- Baseline correctness: n_pass 265, n_fail 0
- Decision: reject, no submit

## Probe

Attempted to replace all 28 fill-chain nodes:

```text
fill_i = Where(Cast(ch0), fc_i, ZERO_U8)
```

with:

```text
fill_i = Mul(ch0, fc_i)
```

Temporary ONNX result:

```text
changed: 28 nodes
status: session_error
error: Type parameter (T) of Optype (Mul) bound to different types
       (tensor(float) and tensor(uint8))
filesize: 4928
```

## Analysis

Unlike case183, the mask source `ch0` is a float input slice:

```text
Slice input -> ch0        float[1,1,18,18]
Cast ch0 -> is_zero_b     bool[1,1,18,18]
Where is_zero_b, fc_i, 0  uint8[1,1,18,18]
```

The fill tensors `fc_i` are uint8. ONNX `Mul` cannot mix float and uint8. Adding
`Cast(ch0 -> uint8)` would replace the existing bool cast with a uint8 cast of
the same 18x18 shape, so memory would stay effectively unchanged while adding
risk and no score gain.

## Conclusion

No exact cost-reducing variant was found. The case183 arithmetic-mask pattern
does not transfer to `task243` because the mask is not already uint8.

No ONNX was changed and no Kaggle submission was made.

## Next

Continue scanning the extracted pattern list for cases where both the mask and
base tensors are already uint8 0/1, especially `Where(Cast(mask), zero, base)`
forms that can become `Sub(base, mask)`.
