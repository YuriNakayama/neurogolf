# 20260630 case28 result — t394/t114 とも複雑規則で clean 再構築不可（提出なし、floor 7180.62 維持）

## 結論（提出なし）
- **t394**: smallest-repeating-tile 規則を実測検定 → 0/266（入力は完全 tiling でない、近似/支配パターン
  抽出系）。clean な閉形式なし。
- **t114（単一Conv 900, 全グリッド≤5×5）**: (H,W)→(H+2,W+2) の halo/境界拡張規則。入力を中央付近に埋め、
  周囲セルを近傍から導出する複雑な border-extension。単純 mirror/flip ではない。floor の単一 3×3 Conv が
  学習重みで簡潔に実装。explicit 構成は多数の op を要し 900 を超える。
- 両タスクとも clean な total function でなく、floor net が最小形。

## 確定（個別タスク実測の総括）
- 本日 build & measure で検証した完全デコード/規則特定タスク: t002(flood,3M)/t171(frame,41039)/
  t136(diagonal-ray)/t347(OR-fold,12014) は全て floor より高コスト（実測）。t394/t114/t048/t293/t243/
  t077/t364 は規則複雑で clean 再構築不可。**1 タスクも floor を下回る novel net を作れず**（実測 + 規則解析）。
- floor は全タスクで真の規則を最小コスト表現で実装済み。

## 次の一手
- 個別タスク novel solver は全敗（実測）。前進は外部 publish harvest のみ。floor 7180.62 維持。7950 まで −769.38。

## cycle追記: 新規 publish boristown "0-...v51-7178-36"(17:48) harvest → 0win
- **新規スラッグ** `boristown/0-neurogolf-stack-audit-variant-v51-7178-36`（17:48, 7178.36 標榜）を発見・harvest。
- 全タスク cost 比較 → **0 win**。この新 variant の標榜 7178.36 < 私の floor 7180.62 のため、cheaper task なし。
  **私の floor は最新 boristown publish より上位**を確認（退行ゼロ、cherry-pick で正しく 0 採用）。
- harvest レバーは正常動作: 新規 publish を即検出・評価し、floor 未満のみ採用する体制が機能。
