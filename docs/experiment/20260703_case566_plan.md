# 20260703 case566 plan - task379/task074 low-fail repair

Accepted Public Score:

- `7187.57`

## Background

Case565 widened parallel search but found no submit-ready candidate. Generic
structural, scratch, solver-bank, and safe dtype sweeps are exhausted against the
current baseline. The next cycle should focus on low-fail repair targets with
meaningful relative gain.

Best current targets from case565 ranking:

```text
task379: 10753 -> 10353, gain=0.037908, n_fail=2
task074:  9050 ->  8150, gain=0.104747, n_fail=15
```

Historical task379 candidate:

```text
/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Where_color20_TO_color20_c_400.onnx
/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Transpose_color20_t_TO_color20_c_400.onnx
```

Historical task074 best low-fail family:

```text
/private/tmp/neurogolf_agent_Y1/candidates/task074_alias_tR_TO_c0.onnx
```

## Hypothesis

The recent no-submit streak is caused by candidate scarcity, not submit
mechanics. The best chance to restore a useful submit cadence is to repair a
bounded low-fail candidate rather than expanding broad mechanical sweeps.

For `task379`, the two failures may be narrow orientation/color20 exceptions.
For `task074`, the fifteen failures may identify a symmetry branch condition
that can be encoded more cheaply than restoring the full 30x30 intermediate.

## Method

1. Re-gate the historical candidates against current `data/output/onnx`.
2. Compare baseline/candidate/expected outputs on failing examples.
3. Try one bounded repair at a time:
   - `task379`: local exception around `color20`, `color20_c`, `color20_t`.
   - `task074`: symmetry/mirror branch exception around `tR`, `tT`, `tC`, `c0`.
4. Run individual `submit gate` for changed task only.
5. Submit immediately if a candidate satisfies:

```text
n_fail=0
candidate cost < current baseline cost
functions=0
forbidden ops=[]
not a known Kaggle ERROR/regression family
```

## Parallel work

Use subagents for:

- task379 failure-diff and repair;
- task074 failure-diff and repair;
- queue watcher for submit-ready candidates;
- fallback task364/task076 diagnosis if the first two lanes stall.

