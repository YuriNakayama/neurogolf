# 20260701 case332 plan - task348 unused zero cleanup

## Context

- Current accepted Public Score floor: `7184.82` after case331.
- Priority rule: `n_fail=0` first, then relative reduction
  `old_cost / new_cost`.
- `task348` was already improved by case325, but that packed-tail rewrite left a
  dead `zero_b` initializer in the current graph. This is a separate cleanup on
  the accepted graph, not a replacement of the previous change.

## Target

- `data/output/onnx/task348.onnx`

## Baseline

Current local audit:

```text
params=194
memory=1774
cost=1968
n_fail=0
status=ok
```

## Hypothesis

The current graph no longer consumes the old zero-plane initializer:

```text
zero_b: bool[1, 1, 10, 11]
```

Removing it should reduce parameter cost without changing any node inputs,
outputs, tensor shapes, or runtime behavior.

## Expected Gain

Temporary audit:

```text
params=84
memory=1774
cost=1858
n_fail=0
status=ok
```

Expected point gain:

```text
1968 / 1858 = 1.0592
ln(old/new) ~= 0.0575 pt
```

## Verification Plan

1. Apply only the `task348` initializer cleanup.
2. Run individual `evaluate.audit_one` for `task348`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
