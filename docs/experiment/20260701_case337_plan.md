# 20260701 case337 plan - task286 bitwise equivalence bundle

## Context

- Current accepted Public Score floor: `7185.05` after case336.
- Priority rule: `n_fail=0` first, then relative reduction
  `old_cost / new_cost`.
- A post-case336 equivalence scan found a new task286 candidate with larger
  relative gain than the recent accepted cases. Prior task286 failures were
  final bitset-lane reductions; this candidate is a different bitwise-chain
  equivalence rewrite.

## Target

- `data/output/onnx/task286.onnx`

## Baseline

Current local audit:

```text
params=741
memory=46272
cost=47013
n_fail=0
status=ok
```

## Hypothesis

The current graph contains many bitwise-chain intermediates that are locally
equivalent to earlier tensors at their consumers. Rewiring consumers to the
earlier tensors should preserve audited outputs while pruning redundant
`BitwiseAnd`, `BitwiseOr`, and shift nodes.

The candidate bundle applies 395 local-equivalence consumer rewires and then
prunes dead nodes. It reduces the graph from roughly 3036 nodes to 2279 nodes.

## Expected Gain

Temporary audit:

```text
params=741
memory=43244
cost=43985
n_fail=0
status=ok
```

Expected point gain:

```text
47013 / 43985 = 1.0688
ln(old/new) ~= 0.0666 pt
```

## Verification Plan

1. Apply only the `task286` equivalence bundle.
2. Run individual `evaluate.audit_one` for `task286`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
