# case220 result: task262 column reorder trim

## 結果

- 対象: `task262`
- baseline: `cost=106` (`params=16`, `memory=90`, `n_fail=0`)
- submit: なし
- 採否: 不採用

## 検証内容

temp ONNX で以下を個別に差し替え、`src.evaluate.scorer.audit_one` で検証した。

- `Concat(col0,col2,col1)` の 6 permutations
- `local_output` repeat 数の 1/2/3 variants
- `Cast(gray_patch -> uint8)` の省略

## 監査結果

| 変更 | cost | n_pass | n_fail | 判定 |
| --- | ---: | ---: | ---: | --- |
| baseline | 106 | 11 | 0 | ok |
| column order `0,2,1` | 106 | 11 | 0 | no improvement |
| other column orders | 106 | 0-1 | 10-11 | reject |
| repeat 1/2 | invalid | 0 | 11 | reject |
| repeat 3 variants | 106 | 11 | 0 | no improvement |
| split `gray_patch` direct | invalid | 0 | 0 | reject |

## 結論

現行の列順 `0,2,1` と 3 channel repeat が必要だった。`Cast` 省略は後続 output type と合わず invalid。実 ONNX は変更せず、Kaggle submit もしない。

次は、同じく小コストでも `TopK` や `Gather` の候補数を削れる task を優先する。
