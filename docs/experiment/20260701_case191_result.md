# 20260701 case191 result

## Summary

- Target: `task374`
- Baseline: params 42, memory 2262, cost 2304, points 17.25759797818422
- Baseline correctness: n_pass 267, n_fail 0
- Decision: reject, no submit

## Probes

All probes were run on temporary `task374` ONNX copies and audited only with
`src.evaluate.scorer.audit_one`.

Row branch aliases:

- `row_long_color -> row_base_color`: cost 2284, n_fail 125
- `row_long_color -> color_one`: cost 2274, n_fail 136
- `row_color -> row_long_color`: cost 2284, n_fail 134
- `row_color -> row_base_color`: cost 2264, n_fail 198

Column branch aliases:

- `col_long_color -> col_base_color`: cost 2284, n_fail 142
- `col_long_color -> color_one`: cost 2274, n_fail 131
- `col_color -> col_long_color`: cost 2284, n_fail 133
- `col_color -> col_base_color`: cost 2264, n_fail 206

Final aliases:

- `cell_color -> row_color`: cost 2154, n_fail 244
- `cell_color -> col_color`: cost 2154, n_fail 237
- `color_grid -> cell_color`: cost 2204, n_fail 244
- `color_grid -> color_zero`: invalid output shape after Pad

Predicate aliases:

- `row_is_min -> row_is_max`: cost 2294, n_fail 198
- `col_is_min -> col_is_max`: cost 2294, n_fail 206
- `row_is_max -> row_is_min`: cost 2294, n_fail 125
- `col_is_max -> col_is_min`: cost 2294, n_fail 142
- `row_has -> row_is_max`: invalid topological order after alias/prune and
  semantically failing examples
- `col_has -> col_is_max`: invalid topological order after alias/prune and
  semantically failing examples

## Analysis

The row and column max/min branches are both required. Public examples cover
all three output classes for active rows/columns:

- longest line: color 1
- shortest line: color 2
- middle/ordinary line: color 4

Aliases that remove any one of these branch decisions reduce cost locally but
immediately lose many examples.

## Conclusion

No exact cost-reducing variant was found. The baseline ONNX was unchanged and no
Kaggle submission was made.

## Next

Move away from max/min classification graphs where all branch outcomes are
represented in public examples. Prefer candidates with mechanically redundant
casts, shape ops, or duplicated masks.
