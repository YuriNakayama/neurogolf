# 20260703 case534 result - no-submit safe candidate replenishment

## Result

No submit.

Accepted score remains:

```text
Public Score: 7187.41
latest accepted ref: 54273472
latest adopted case: case530 task064
```

## Broad Exact Search

After excluding all hard/review/runtime risk tasks and recent blocked targets,
the only current-positive exact non-risk candidates found were tiny:

| task | cost | gain | decision |
|---|---:|---:|---|
| 328 | `6904 -> 6902` | `0.000290` | bank-low-gain |
| 005 | `6882 -> 6881` | `0.000145` | bank-low-gain |

High-gain task005 scratch variants failed local correctness with hundreds of
failures. Task213 was a stale lead; the current baseline is already `1834`, so
the candidate is `1834 -> 1834` no-gain.

## Task033 Repair Audit

The task033 near miss remains:

```text
candidate: /tmp/neurogolf_agent_V3/task033_alias_018_1_line_cell_1_0_TO_line_cell_0_1_dce.onnx
cost: 1639 -> 1594
gain: 0.027840
n_fail: 3
```

Failure cause is the alias in `expand_row_1`:

```text
baseline input slot 1: line_cell_1_0
candidate input slot 1: line_cell_0_1
```

The three failures require `line_cell_1_0` differences that are non-monotonic
relative to `line_cell_0_1`: some cells need added true bits and others need
removed true bits. One-op repairs keep gain above `0.020` but only reduce to
`n_fail=2`; two-op repair falls below the standalone threshold unless it enables
additional DCE.

## Decision

Skip submit. Do not spend another immediate cycle on this exact task033 alias
unless a new one-op expression can exactly reconstruct `line_cell_1_0`.

Keep task005/task328 banked only. Next cycle should search a different family
or attempt a broader DCE-aware repair, not a direct restore of deleted tensors.
