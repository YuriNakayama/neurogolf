# 20260702 case468 plan - task157 current shape-compatible re-gate

## Context

`task157` is the next target after `task145`:

- case271 rejected bool `Concat -> Pad` because opset/runtime did not support
  the needed bool `Pad` route without erasing the savings;
- case398 adopted an isolated shape-compatible bundle, reducing
  `8163 -> 7796`;
- the case398 submit moved Public Score by `+0.05`.

This makes `task157` worth a current-baseline re-gate, but only for numeric or
shape-compatible candidates. Old bool-pad and already-adopted bundle files are
not submit material unless they reduce the current graph further.

## Target

```text
task157
current baseline: data/output/onnx/task157.onnx
known accepted cost: 7796
task json: data/lake/neurogolf-2026/task157.json
```

## Hypothesis

The case398 bundle may not have exhausted every compatible alias. If a remaining
same-family candidate reduces the current accepted graph and remains exact, it
has better hidden-transfer evidence than unrelated alias-pool micros.

## Method

1. Audit the accepted `task157.onnx`.
2. Gather `task157*.onnx` candidates from `/private/tmp` and `/tmp`.
3. Run cost-only scan against the current baseline to drop stale pre-case398
   candidates.
4. Correctness-gate only current positive reductions.
5. Submit only if:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

Bank exact low-gain candidates only if they are current-baseline positives.
