# 20260702 case437 result - task070 Pad/mask probe is no-submit

## Decision

- Task: `task070.onnx`
- Submit: no
- Adoption: none
- Reason: no current-baseline candidate is both exact and meaningfully
  cost-reducing.

Baseline:

```text
params=31
memory=2977
cost=3008
n_pass=266
n_fail=0
```

## Current Graph Check

`task070` already slices only the required source area:

```text
c8_starts = [0, 8, 0, 0]
c8_ends   = [1, 9, 17, 17]
```

The dominant memory entries are not stale intermediates:

```text
c8_f      FLOAT [1,1,17,17]  1156 bytes
fill_full BOOL  [1,1,30,30]   900 bytes
rect_mask BOOL  [1,1,17,17]   289 bytes
fill      BOOL  [1,1,17,17]   289 bytes
c8        BOOL  [1,1,17,17]   289 bytes
```

`fill_full` is the condition for the final full-size `Where`, so a direct
removal would change output semantics rather than only cleanup memory.

## Gate Results

Known semantic aliases:

```text
task070_006_ScatterElements_col_mask_TO_col_mask_raw.onnx
  baseline cost  : 3008
  candidate cost : 2985
  gain           : 0.007676
  status         : INCORRECT
  n_fail         : 1
  decision       : blocked-local-fail

task070_008_Xor_fill_TO_c8.onnx
  baseline cost  : 3008
  candidate cost : 2371
  gain           : 0.237964
  status         : INCORRECT
  n_fail         : 266
  decision       : blocked-local-fail

task070_008_Xor_fill_TO_rect_mask.onnx
  baseline cost  : 3008
  candidate cost : 2719
  gain           : 0.101011
  status         : INCORRECT
  n_fail         : 266
  decision       : blocked-local-fail
```

Bounded dtype probes:

```text
idx_col2 int64 -> int32
  baseline cost  : 3008
  candidate cost : 3008
  gain           : 0
  status         : ok
  n_fail         : 0
  decision       : blocked-no-gain

pads_fill int64 -> int32
  status         : session_error / invalid graph
  reason         : Pad rejects int32 pads in this runtime path
  decision       : blocked-local-fail
```

Current-baseline optimizer cleanup candidates:

```text
task070_optimizer_cleanup.onnx       cost 3008 -> 3008, n_fail=0, blocked-no-gain
task070_cleanup_3008.onnx            cost 3008 -> 3008, n_fail=0, blocked-no-gain
task070_broadcast_compress_3008.onnx cost 3008 -> 3008, n_fail=0, blocked-no-gain
task070_index_surgery_3008.onnx      cost 3008 -> 3008, n_fail=0, blocked-no-gain
```

## Conclusion

No submit. `task070` is now a low-value target unless a new formulation removes
the full-size terminal mask while preserving the exact hidden-fill semantics.

The column-gap shortcut is also below the standalone threshold even before its
local failure, so it should not be revisited as a submit candidate.

## Regression Countermeasure

This cycle confirms the updated policy is working: large-looking aliases that
change selector or fill semantics are stopped by `blocked-local-fail`, and exact
cleanup candidates with no current-baseline cost reduction are stopped by
`blocked-no-gain`. The 15-minute cadence should remain a polling cadence; it
should not force a Kaggle submit when every candidate is in these buckets.
