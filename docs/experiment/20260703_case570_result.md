# 20260703 case570 result - adopt task279 row7 patch

Accepted baseline before cycle:

- Public Score: `7187.59`

Submission:

- description: `case570 full task279 row7 patch`
- status: `SubmissionStatus.COMPLETE`
- Public Score: `7187.64`
- decision: adopted

## Adopted change

`task279`:

```text
candidate: /private/tmp/neurogolf_case569_task279/candidates/task279_row7_c4c7_gather_scatternd_patch.onnx
cost:      4827 -> 4597
gain:      0.048821
n_fail:    0
functions: 0
forbidden: []
gate:      submit-candidate
```

The prior cheap `4315` family missed two row-7 cells. This patch restores exact
correctness with a targeted row-7 c4/c7 repair while retaining most of the
relative gain. Full dry-run changed only `task279` and passed the bundle gate.

## Parallel work during submit wait

`task364`:

```text
best failing: 23013 -> 21253, gain 0.079561, n_fail=21
repair attempts: 22573, still n_fail=21 or worse
exact restore: 23013, no gain
decision: closed/no-submit
```

The lower-cost variants lose required `PB7`/`PA7` propagation. Restoring it
returns to baseline cost.

`micro bundle`:

```text
task118: 35807 -> 35803, gain 0.000112, n_fail=0
task319: 18685 -> 18669, gain 0.000857, n_fail=0
total gain: 0.000968
dry-run: blocked-bundle-gate
decision: bank
```

The bundle remains far below the `0.015` micro-bundle floor, so it should not
consume a submit.

`task354`:

```text
candidate: /tmp/neurogolf_agent_T3/generated/alias_scans/task354_alias_left_seed_id_TO_one_col.onnx
current gate: 2933 -> 2933, n_fail=0, blocked-no-gain
```

The fresh-rank candidate was already absorbed into the current accepted baseline.

## Conclusion

Adopt task279. This is the first high-relative-gain repair after the no-submit
streak and confirms the expanded parallel search is useful when workers focus on
low-fail repairable candidates rather than raw absolute cost.

Next cycle:

- continue waiting for task187 and broad safe artifact mining worker outputs
- inspect fresh-ranked task243 one-fail and task319 one-fail candidates
- keep micro fillers banked until a safe bundle reaches the gate threshold
