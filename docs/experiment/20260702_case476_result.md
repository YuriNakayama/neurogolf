# 20260702 case476 result - quarantine case475 regression members

## Decision

No Kaggle submit. Adopt the submit-gate guardrail.

## Change

Added all case475 regression bundle members to `REVIEW_RISK_TASKS`:

```text
task044
task046
task064
task110
task378
```

This changes future gates for those tasks from generic `bank-low-gain` to:

```text
decision=review-known-risk
reason=case475 regressing micro bundle member
```

Because the micro bundle lane accepts only `bank-low-gain` and
`submit-candidate`, these tasks can no longer pass an automatic reviewed micro
bundle without a fresh isolated repair plan.

## Verification

```text
uv --project backend run pytest backend/tests/unit/src/submit/test_gate.py
21 passed

uv --project backend run ruff check \
  backend/src/submit/gate.py \
  backend/tests/unit/src/submit/test_gate.py
All checks passed
```

Pytest emitted repository coverage no-data warnings, but all gate tests passed.

## Conclusion

Case475 proved that a low aggregate threshold with unrelated bank-low-gain
members can still hide a Public regression. Keep the case473 micro lane for
carefully validated bundles, but do not use case475 members as filler again.
