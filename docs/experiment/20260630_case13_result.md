# 20260630 case13 result — t243 規則は4方向blockで近分離だが非自明、kok harvest 0win（提出なし）

## 結論（提出なし、floor 7180.62 維持）

### novel solver 試行: t243（22650, row-separable, 10色, interior-fill 系）
- 変換は「0-cell を 1 で埋める」系の 2D 規則。試した規則と arc-gen 一致:
  - per-row interior fill(→1): 0/265
  - per-col interior fill: 0/265
  - between row OR/AND col: 0/265
  - 4方向 ray-cast all-blocked: 0/265
- 4方向 block 署名 (L,R,U,D) で fill/not-fill を分類すると**ほぼ分離**（filled は (1,1,1,1):37 等、
  not-filled は各署名 1-2 件のみ）が、**完全分離せず例外あり**（署名だけの関数でない）。追加文脈
  （ray 内 nonzero 数 or parity 等）が必要で、partial 規則は hidden 落ちリスク。**厳密化できず棄却**。
- t243 floor net(22650) は真の規則を実装済み。row-sep は観測データ上の一貫性で、真の規則は 2D。

### harvest: kokinnwakashuu(16:59 再走)
- 全タスク cost 比較 → **0 win**（floor 7180.62 に収束）。

## 教訓
- row/col-separable 判定は「観測データで行が一貫」を示すのみで、真の規則が 2D（ray-cast 系）の
  場合がある。t243 は 4方向 block で近いが非自明な例外を持ち、現プリミティブでの厳密 novel net 化は困難。
- harvest: cherry-pick 後フロントは速やかに再収束（bor3/kok とも 0win）。次の純増は次の新規 publish 待ち。

## 次の一手
- 監視 harvest 継続（新 revision 毎 cross-bundle cherry-pick）。
- novel solver は row/col-sep の残りタスク（t338/t077/t110 等）も同様に 2D 規則の可能性。clean に
  特定できるものがあれば実装。floor 7180.62 維持。7950 まで −769.38。
