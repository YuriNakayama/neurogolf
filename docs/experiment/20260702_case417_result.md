# 20260702 case417 result - no-submit targeted high-cost inspection

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7186.71
```

## Main Target: task018

Current baseline:

```text
cost=38681
params=2085
memory=36596
n_fail=0
```

Graph observations:

```text
nodes=912
initializers=41
functions=0
```

Dominant intermediates:

```text
safe_name_44   float32[1,1,30,30]  3600 bytes
safe_name_88   float16[900]        1800 bytes
safe_name_84   float16[900]        1800 bytes
many bool/uint8/full-grid tensors   900 bytes each
```

`safe_name_44` is produced by the initial `Conv(input, safe_name_0)` and then
cast to `uint8` as `safe_name_45`. This path converts the one-hot input to a
color-index grid. A naive `ArgMax` replacement would materialize an int64
grid and is larger than the current `Conv + Cast` path, so it is not a useful
reduction.

Docs history also shows task018 has already received the large safe cleanup:

```text
case355: 39583 -> 38683, adopted
case400: 38683 -> 38681, adopted micro
```

No current `gain >= 0.020` task018 candidate was found.

## Subagent Target: task133

Current baseline:

```text
cost=33562
params=1278
memory=32284
n_fail=0
```

Findings:

- current graph already matches the adopted case398 bundle;
- old case357 alias candidate audits exact but is worse than current
  (`33570 > 33562`);
- material branch removals are correctness-critical and previously failed.

Best current-baseline improvement found:

```text
none
```

## Subagent Target: task187

Current baseline:

```text
cost=27533
params=1008
memory=26525
n_fail=0
```

Best threshold-clearing candidate checked:

```text
/private/tmp/neurogolf_case356_struct/task187_reach12.onnx
27533 -> 26283
gain = 0.046463
n_pass=262
n_fail=4
status=INCORRECT
functions=0
forbidden=[]
```

The final reach step is required. The exact/current-baseline artifact
`/private/tmp/neurogolf_case359_cleanup_scan/task187.onnx` is byte-identical to
the current baseline and has no gain.

## Conclusion

Skip submit. None of the targeted high-cost tasks produced a single-task,
mechanically explainable, locally exact candidate clearing `gain >= 0.020`.

Next action: move to other high-cost non-excluded targets:

```text
task158, task054, task349, task364, task243, task367, task002, task319
```

Use docs-directed inspection only; avoid broad `/tmp` scans and do not submit
sub-threshold micros.
