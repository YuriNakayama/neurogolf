# 20260630 case107 plan - t173 後段 slot/center/pair 更新の削減検証

## 現状
- floor/Public: 7180.95
- 対象: `task173`
- baseline faithful: cost 17646, params 112, memory 17534, n_fail 0

## 背景
case81 では `topk_pixels=19` を 18 以下に下げると `n_fail>=1` になり、pixel 候補数は削減不可だった。

一方、後段にはまだ以下の候補幅・更新 branch がある。

- `topk_slots=3`
- `topk_centers=7`
- final scatter: `outer_updates_flat`, `pair_h_color`, `pair_v_color`, `pair_dr_color`

## 仮説
pixel 候補数は必要でも、slot/center 幅や pair branch の一部が冗長なら memory/cost を下げられる。

## 手法
1. `topk_slots: 3 -> 2` を一時候補で監査する。
2. `topk_centers: 7 -> 6` を一時候補で監査する。
3. final scatter の `pair_h` / `pair_v` / `pair_dr` 更新を個別にゼロ化または既存 branch から外して監査する。
4. `n_fail=0` かつ cost 減なら実ファイルへ適用して submit する。

## 足切り
- 変更対象は `task173` のみ。
- 全数 `audit_dir` は使わず、`audit_one` のみ行う。
