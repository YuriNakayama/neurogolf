# 20260702 case464 result - task350 span graph re-gate

## Summary

No submit.

`task350` still has large-looking relative reductions, but every positive
cost-reducing candidate fails local correctness. The current scratch pool
reconfirms the case117 conclusion: all four directional span branches are
semantically required.

## Baseline

```text
task350
params=24
memory=9012
cost=9036
points=15.8910281224
n_pass=267
n_fail=0
status=ok
```

## Gate Result

```text
candidates=74
positive_cost_reductions=26
errors=1
exact_positive=0
blocked=26
exceptions=1
```

Representative blocked candidates:

```text
9036 -> 6540  gain=0.323279  n_fail=260
  span -> hs

9036 -> 6540  gain=0.323279  n_fail=262
  span -> vs

9036 -> 7788  gain=0.148633  n_fail=267
  hs -> hl/hr
  vs -> vd/vu

9036 -> 8412  gain=0.071557  n_fail=260..262
  directional MaxPool -> m
```

One stale terminal `Where` candidate reported an unusable `candidate_cost=0`
and `n_fail=267`; it is not a scoring candidate.

## Decision

Rejected for submission. No candidate met:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

Do not spend a Kaggle slot on `task350` span aliases. They are high relative
gain only because they delete required geometry.

## Next

Move away from cost-9000 symmetry/span graphs and switch to mid-cost selector
or table tasks with a different failure surface. Micro backlog should not be
bundled from old alias pools after case414; only params-only/current-baseline
cleanups such as the `task046` one-byte initializer removal are safe filler,
and even that is below a standalone submit threshold.
