# 2026-07-01 case154 result

## 結果

- 対象: `task368.onnx`
- 採否: 不採用
- submit ref: `54206863`
- Public Score: `7181.17`
- floor: `7181.17`

## baseline

```text
task368.onnx
params 203
memory 6022
cost 6225
points 16.26367127866709
n_pass 265
n_fail 0
filesize 6768
```

## 試行

probe guard、offset guard、3個目 placeholder fallback、color split を単独で差し替えた。

- `h_probe_idx_i64 -> h_probe_raw_i64`: cost 6217, n_fail 1
- `h4_b -> h4_raw_b`: cost 6224, n_fail 0
- `w_probe_idx_i64 -> w_probe_raw_i64`: cost 6217, n_fail 0
- `w4_b -> w4_raw_b`: cost 6224, n_fail 21
- `valid_offset_i32 -> offset_flat_i32`: cost 6177, n_fail 89
- `valid_offset_i32 -> zero12_i32`: cost 5737, n_fail 265
- `ph3_base_i32 -> ph3_idx_i32`: cost 6221, n_fail 123
- `ph3_base_i32 -> ph1_idx_i32`: cost 6221, n_fail 138
- `src_colors_u8 -> color_a_u8`: cost 6169, n_fail 265
- `src_colors_u8 -> color_b_u8`: cost 6169, n_fail 265

有効候補の組み合わせ:

```text
h4_b -> h4_raw_b
w_probe_idx_i64 -> w_probe_raw_i64
```

個別 audit:

```text
params 203
memory 6013
cost 6216
points 16.265118107952517
n_pass 265
n_fail 0
filesize 6659
```

追加で `h_probe_idx_i64 -> h_probe_raw_i64` または `w4_b -> w4_raw_b` を重ねると n_fail 1 になった。

## 採否

ローカルでは cost `6225 -> 6216` の小削減で完全一致だったが、実 LB は `7181.17 -> 7181.17` で上がらなかった。ルール通り `data/output/onnx/task368.onnx` と `data/output/onnx.dvc` は baseline に戻した。

戻し後 audit:

```text
params 203
memory 6022
cost 6225
points 16.26367127866709
n_pass 265
n_fail 0
filesize 6768
```

## 結論

`task368` の小削減は LB に反映しないため不採用。次はより大きい相対削減が見込める `task378` / `task201` / `task370` などを優先する。
