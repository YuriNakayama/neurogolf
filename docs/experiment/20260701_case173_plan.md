# 20260701 case173 plan

## Context

- Current accepted Public Score: 7181.17
- Recent cases showed that approximate branch deletion often fails many examples.
- Continue prioritizing relative cost reduction: find tasks where a small graph has
  one or two large memory tensors that may be exactly redundant.

## Target

- Task: `task222`
- Baseline audit:
  - params: 45
  - memory: 8276
  - cost: 8321
  - points: 15.9732777643546
  - n_pass: 266
  - n_fail: 0
  - filesize: 1245

## Hypothesis

`task222` converts the input to a color code, builds a complement code, detects
monochrome neighborhoods with two `MaxPool` branches, multiplies the code by the
combined coverage, then pads back to the 30x30 output.

The graph has two similar coverage branches:

- `mx_a/nm_a/sum_a/mono_a/sp_a`
- `mx_b/nm_b/sum_b/mono_b/sp_b`

If one branch is redundant, or if `cov_u8` can be replaced by one branch, a large
intermediate tensor can be removed and the relative cost reduction should be
meaningful.

## Method

1. Probe one alias at a time on temporary `task222` ONNX copies.
2. Run `audit_one` only for `task222`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record failed branch aliases, especially low-fail candidates.
