# 20260702 case403 plan - task017 candidate-row prune

## Context

Accepted Public Score is `7186.50` after case401. Case402 was rejected because
the isolated `task009` micro left Public unchanged.

The next candidate should be isolated and above the submit threshold. A current
graph probe for `task017` shows that only `88 / 106` candidate rows are selected
as `best_id` over all local train/test/arc-gen examples.

## Candidate

```text
task017 12726 -> 11970 gain 0.061243625240718594
```

Scratch file:

```text
/tmp/neurogolf_case403_task017_prune/task017_prune_unused_candidate_rows_novi.onnx
```

## Method

Prune unused rows from both candidate tables:

```text
candidate_params:            106 x 4  -> 88 x 4
matches_b_castfold_const: 1 x 106 x 9 -> 1 x 88 x 9
```

Clear stale intermediate `value_info` so scorer shape inference sees the new
candidate dimension. The graph logic remains otherwise unchanged.

## Gate

Submit only if `task017` passes targeted `audit_one`:

```text
n_fail=0
functions=0
forbidden ops=[]
cost=11970
```

Risk: local candidate-row pruning can be hidden-sensitive if private tests need
a row that local examples never select. Submit isolated and adopt only if Public
Score improves over `7186.50`.
