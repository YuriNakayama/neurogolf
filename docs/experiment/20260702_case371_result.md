# 20260702 case371 result - skip task328 packed template probe

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.86
```

## Baseline

Target:

```text
data/output/onnx/task328.onnx
```

Current local audit:

```text
params=1940
memory=4964
cost=6904
n_pass=267
n_fail=0
status=ok
```

The target was selected because the graph has a large packed template bank:

```text
template_bank int64[1717]
decode_pair_powers int64[1,1,9]
packed_rows -> Unsqueeze -> Div -> Cast(uint8) -> Mod(64)
```

## Probes

### Predecoded uint8 pair bank

Replaced the packed int64 rows and runtime `Div/Mod` decode with a predecoded
uint8 table:

```text
template_pair_bank uint8[1717,9]
Gather(template_pair_bank, selected_row_indices) -> Unsqueeze -> pair_codes_u8
```

Local result:

```text
cost=19047
n_fail=0
```

This is functionally exact, but far worse. The current packed int64 bank has a
large raw byte size, yet the scorer's parameter accounting and the smaller table
cardinality beat the predecoded representation.

### Narrow dtype attempts

| candidate | result | decision |
|---|---|---|
| `base_offsets`, `row_offsets`, `sentinel_row_index` to int32 | ORT type mismatch in `Less`/index math | reject |
| `decode_pair_powers` to uint64 | ORT type mismatch in `Div` | reject |
| generic case3 surgery cleanup | `0/1` improved | reject |

## Existing Micro

The known exact task328 micro remains only a batch candidate:

```text
subset_gt8_u8 -> br_active_u8
6904 -> 6902
gain = 0.000290
```

It is already recorded in `micro_backlog.md`; this cycle found no stronger
compatible task328 reduction.

## Conclusion

Skip submit. The packed template representation is still better than a decoded
uint8 bank, and ORT type constraints block the narrower index-table variants.

Next action: avoid broad packed-template rewrites unless a proof removes runtime
int64 tensors without expanding the initializer. Search instead for isolated
dtype cleanups like case369, but on medium-cost graphs with larger live float
patch tensors.
