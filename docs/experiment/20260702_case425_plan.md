# 20260702 case425 plan - task255 source-window narrowing probe

## Context

Case424 found no usable `task198` label-path candidate. The next memory-heavy
target from case423 is:

```text
task255 cost=22731
```

It has prior accepted same-task work and a known small exact residual, but
blanket dtype changes were rejected or insufficient. This cycle checks whether
the dominant source-window path can be narrowed without changing selector
coverage.

## Hypothesis

If the large source-window tensor is only consumed through smaller downstream
windows or bool/integer predicates, a local shape/dtype narrowing may reduce
memory enough to clear `gain >= 0.020`. If all consumers require the full float
window, record no-submit.

## Method

1. Inspect current `task255.onnx` dominant tensors and consumers.
2. Review recent docs for accepted/rejected `task255` changes.
3. Build at most one narrow candidate if there is a direct consumer-preserving
   rewrite.
4. Run `submit gate`.

## Gate

Submit only `decision=submit-candidate`, `n_fail=0`, and `gain >= 0.020`.
