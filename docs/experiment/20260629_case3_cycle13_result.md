# 20260629 case3 cycle13 — result: small_residual 0 win / floor 400全解決 / 高コスト帯は非局所

## 実測結果（faithful: onnx 1.20.0 / ort 1.24.1 / official neurogolf_utils）

### 1. small_residual（原設計, Slice→小空間残差→Pad）
- 全 400 タスクで構成試行 → **47 タスクで構成成功**、うち **floor net 未満は 0**。wins=0、gain +0.000。
- 結論: 公開 net は k-local タスクで既に最小実装。中間テンソルを h×w に縮めても公開包絡線を破れない。
  （`smallres_wins.json` 参照）

### 2. floor カバレッジ全数監査
- **400/400 全タスクが faithful n_fail=0 で正答**、sum_pts=7180.45（LB 7180.55 と整合、offset +0.1）。
- **gap（未解決/不正答タスク）= 0**。よって「新規ソルバ追加で +25点」の余地はゼロ。
  前進は既存タスクで公開 net を下回る独自回路を作る以外にない。

### 3. 高コスト上位タスクの特徴づけ
- 上位: t233(58327) t286(47013) t018(45211) t118(39237) t187(36814) t366 t158 t133 t209 …
- いずれも **単純大域変換ではない**（geom/colormap/tile 全て不一致）。
- t219(18417) 実ルール解析: 「8 の斜め階段を色1の水平 ray で延長」= **非局所パターン追従**。
- 共通: 高コスト帯は真に複雑な非局所ルール。公開 net はこれを near-optimal に実装しており、
  k-local/単純置換の独自回路では下回れない（過去 ~10 セッション + 本サイクルで再確認）。

## 採否
- 提出候補（floor 未満の独自 net）が 0 のため **submit せず**（floor 7180.55 維持、退行ゼロ）。
- small_residual ソルバ自体はテスト緑で原設計ツールバンクに追加（floor は変えないが from-scratch
  パイプラインの一次プリミティブを拡充）。

## 次の一手（原設計の継続レバー）
- **ray/projection プリミティブ**（CumSum 軸方向→clamp で「色を端まで投射」）は禁止 op を使わず
  実装可能。t219 のような ray 系タスク群に対し、公開 net より安い独自回路になり得る候補。次サイクルで
  ray 系タスクを列挙し CumSum ベース回路を試作する。
- 高コスト非局所タスク（t233/t286/t018）は引き続き個別に真ルールを解析し、最小回路の余地を探る。
