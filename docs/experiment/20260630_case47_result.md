# 20260630 case47 result — t002 flood-fill を正しい4連結で再構築、correctness前進も iteration cost で floor 不可（提出なし）

## independent_impl（必須欄）
case42 crop技法 + 正しい4連結 dilation で t002 flood-fill を再挑戦（前回 case35 は correctness バグ）:
- **4連結 cross-dilation を numpy で実装**: ext(out-of-grid)から種、cross(中心+4近傍)で free を伝播。
- correctness 前進: niter=20 で 122/268 fail → niter=30 で 64 → **niter=40-60 で 60 fail で頭打ち**。
- **60例が depth でなく規則差で残る**: 私の ext-seed（30×30枠外起点）は、grid の真の縁に接しない
  内容のとき scipy の border-label（case35 で 268/268）と挙動が異なる。真edge seed が必要。

## 確定（cost 障壁は correctness 達成後も不変）
- 完全正答化には真-edge seed の追加 op が要り、かつ **30-60 反復が必須**（grid 最大20×20 で蛇行 path）。
- 反復ごとに [1,1,S,S] 中間が memory に加算 → case35 で実測 cost ~205k ≫ floor 21089。
- **t002 flood-fill は correctness を達成しても iteration cost で floor を超える**。floor の 157ノード
  閉形式（反復回避の bit/Gather 構成）が本質的に優れる。crop技法でも反復数は減らせない。

## 学び
- flood-fill/reachability は逐次伝播が本質で、ONNX の許可 op（MaxPool=8連結, Conv-cross+clip=3op/iter）
  では反復を畳めない。memory=Σ中間 が反復を直接ペナルティ化する cost モデルでは、
  反復アルゴリズムは閉形式 floor に原理的に劣る。これは flood-fill 系全タスク(t002/251/336/367 等)に共通。

## 恒久方針の遵守
- 停止提案せず、t002 を deeper に再構築（correctness 122→60 fail へ前進）し cost 障壁を再実証した。
- floor 7180.78（DVC 6e8d4642）退行なし。次サイクルも継続。

## 次の一手
- flood-fill 以外で iteration 不要な規則のタスクに注力。または許可 op の中で reachability を
  非反復で解く表現（行列累乗/transitive closure を MatMul で？ただし 900×900 行列は params 過大）を探索。

## 追記: 60 failの真因特定（depth でなく seed 不完全）
- 400 反復でも 60 fail 不変 → depth でなく **reach の seed 不完全**。
- 失敗20×20例を精査: 私の出力は **grid 左上の開放領域を誤って enclosed と判定して過剰 fill**。
  期待は壁で囲まれた領域のみ fill。私の `ext=1-hot` は grid 内部で 0（one-hot で hot=1）のため、
  seed は 30×30 枠外隣接セルのみ起点 → bbox が原点からずれた grid で内部開放領域に reach が届かない。
- **bbox-offset を考慮した真-border seed が必要**（修正可能だが intricate）。
- ただし**修正しても 20+ 反復必須は不変** → cost ≫ floor 21089。t002 flood-fill は correctness 達成しても
  cost で勝てないことを再々確認。t002 個別追求は打ち切り、iteration 不要タスクへ移る。
