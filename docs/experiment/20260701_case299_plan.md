# 20260701 case299 plan - task023 duplicate initializer cleanup

## Context

- Current Public Score floor: `7183.03` after case298.
- Source-crop scans after case298 found no remaining proof-grade candidate above
  roughly `50` cost reduction:
  - `task063` / `task237` Conv crops require large kernel expansion and lose on
    params;
  - `task096` / `task268` ReduceSum crops would add larger float input slices;
  - fixed safe Pad->Slice removal scan found no candidates.
- A parallel explorer is still searching for a larger structural candidate; this
  cycle uses the best verified local cleanup candidate available now.

## Target

- `task023`
- Baseline audit:
  - `params=157`
  - `memory=11835`
  - `cost=11992`
  - `n_pass=266`
  - `n_fail=0`

## Hypothesis

`task023` has three duplicate initializer pairs with identical dtype, shape, and
contents:

```text
Ksc -> Ksb
Khc -> Khb
Kvc -> Kvb
```

Rewire all consumers of the duplicate names to the first initializer and delete
the duplicate initializers.  This is a pure graph storage cleanup; node behavior
does not change.

Temporary probe:

- `params=147`
- `memory=11835`
- `cost=11982`
- `n_fail=0`

## Gate

- Edit only `data/output/onnx/task023.onnx`.
- Verify with `src.evaluate.audit_one` for `task023` only.
- Submit only if `status=ok`, `n_fail=0`, and cost decreases.
