# 20260703 case556 plan - repair low-fail structural candidates

Accepted Public Score:

- `7187.53`

## Background

Case555 found no standalone exact candidate, but it identified several
lower-cost candidates with localized local failures. Since the score is
logarithmic, repair work should still prioritize relative gain, but correctness
is the hard gate.

## Targets

Primary repair targets:

```text
task187 /private/tmp/neurogolf_case356_struct/task187_reach12.onnx
  26396 -> 26283, gain 0.004290, n_fail=4

task349 /private/tmp/neurogolf_parallel_U/candidates/0372_task349_cost22576_gain0.077245_task349_halo_c4_to_c3.onnx
  24369 -> 22576, gain 0.076424, n_fail=17

task364 /private/tmp/neurogolf_agent_case548_C/task364_runtime_equiv_alias_dce.onnx
  23013 -> 22133, gain 0.038990, n_fail=21
```

## Method

1. For each candidate, extract failed train/test/arc-gen cases with expected and
   actual outputs.
2. Identify whether failures are guarded by a small shape/color branch.
3. Try one bounded repair per task, preserving most of the cost reduction.
4. Gate repaired candidates individually.

## Submit Rule

Submit a full 400-task bundle only if a repaired candidate reaches `n_fail=0`,
`functions=0`, forbidden ops `[]`, and positive cost reduction. If no repaired
candidate is found, record no-submit and keep the failed-pattern notes for the
next cycle.

Cadence fallback: if the repair workers are still running near the submit
cadence target, submit an exact low-risk micro bundle only with explicit
`--allow-micro-bundle-gate` flags. Adopt it only if Public Score improves.
