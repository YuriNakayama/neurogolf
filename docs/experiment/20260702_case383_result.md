# 20260702 case383 result - task354 segment bypass bundle

## Decision

Adopted.

```text
ref: 54247936
description: case383 task354 segment bypass bundle gain 0.02401
previous accepted Public Score: 7185.95
Public Score: 7185.98
```

The real Public Score increased by `+0.03`, so keep the submitted
`task354.onnx`.

## Local Gate

Baseline:

```text
params 79
memory 2998
cost 3077
points 16.968289624677958
n_pass 266
n_fail 0
filesize 6439
```

Submitted candidate:

```text
params 79
memory 2925
cost 3004
points 16.992299987115974
n_pass 266
n_fail 0
filesize 3907
```

Local gain:

```text
ln(3077 / 3004) = 0.024010362438016374
```

This clears the standalone submit gate `0.020`.

## Change

Four compatible value-path bypasses were applied:

- `seg_c1 -> one_col`
- `seg_c8 -> seg_c7`
- `seg_c9 -> seg_c8`
- `left_seed_seg -> left_seg`

No forbidden operators were introduced:

```text
nodes 48
forbidden ops []
functions 0
```

## Parallel Findings

The continued `320-400` scan found smaller exact candidates:

- `task340`: `10994 -> 10918`, `gain 0.006937`
- `task342`: `1585 -> 1576`, `gain 0.005694`
- `task338`: known single aliases `17939 -> 17819`, `gain 0.006712`
- `task366`: many exact tiny value-path bypasses, all below `gain 0.002`

The parallel `task319` probe found a stronger exact bundle that was not included
in this submission:

```text
task319: 19569 -> 19080
gain 0.025306
n_fail 0
path /tmp/neurogolf_task319_case383_probe_pruned/combo_0_1_2_3_4_5.onnx
```

Additional rewires on top of the case382 banked bundle:

- `p10_top_eq_u8 -> obj0_local10`
- `obj0_pack_b -> obj0_pack_a`
- `obj2_top_row_mask -> obj1_top_row_gt0`
- `obj2_left_col_mask -> obj1_top_row_gt0`
- `board_h -> row_tail`
- `obj0_obj_w -> obj0_col_max_p1`

This is submit-worthy by gain, but task319 predicate/value-path rewrites have
some hidden sensitivity. Handle it as the next isolated case rather than mixing
it into case383 after the fact.

## Conclusion

Adopted. Continue with the fresh `task319` bundle next, unless a lower-risk
candidate with comparable gain appears first.
