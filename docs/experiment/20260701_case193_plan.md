# case193 plan: task196 propagation tail shortener

## 現状

- 採用済み Public Score floor: 7181.21
- 優先度は cost 絶対値ではなく `old_cost / new_cost` の比率で見る。
- `task196` は未着手で、baseline は `cost=4538` (`params=38`, `memory=4500`, `n_fail=0`)。

## 対象

- `data/output/onnx/task196.onnx`
- graph は `15x15` の青チャネルから seed を作り、`MaxPool -> Min(blue)` を 5 段反復してから `Less(poolL, blue)` で緑マスクを作る。
- 各中間 uint8/bool tensor は 225 bytes なので、反復末尾を 1 段でも落とせれば約 5% の memory 削減になる。

## 仮説

後段の伝播が train/test/arc-gen では過剰な可能性がある。`Less(poolL, blue)` の左入力を `bad4`, `pool3`, `bad3`, `pool2` など前段 tensor に差し替え、未使用 tail node を prune する。

## 検証

- temp ONNX で `poolL -> earlier tensor` を 1 点ずつ probe する。
- `src.evaluate.scorer.audit_one` で `task196` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
