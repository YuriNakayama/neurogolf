# 20260703 case513 result - adopt task187 scoped solver reduction

## Result

Adopt.

Kaggle submission:

```text
ref=54269821
status=COMPLETE
Public Score: 7187.23 -> 7187.27
```

## Change

```text
task187.onnx
candidate: /tmp/neurogolf_agent_R7/candidates/task187.onnx
old_cost=27533
new_cost=26396
gain=0.042172802
n_fail=0
decision=submit-candidate
```

Pre-submit bundle gate:

```text
changed=1
total_gain=0.042173
decision=submit-bundle
```

The accepted file is now:

```text
data/output/onnx/task187.onnx
```

## Parallel Search

The submit gap after case510 was caused by post-regression gating: case511 and
case512 had no non-risk candidate that cleared the standalone or micro-bundle
threshold. This cycle increased parallel search and used the cadence as a
readiness check rather than forcing weak submits.

Workers:

```text
R3 task184 strengthening: no submit; best remains 2520 -> 2491, gain 0.011575
R4 bounded non-risk scan: no exact reduction
R5 tasks 001-080: no exact reduction
R6 tasks 081-140: banked low-gain task092/task096 only
R7 tasks 141-220: task187 submit-candidate found
R8 tasks 221-300: banked low-gain task260 only
parent tasks 301-400: no exact reduction
```

Banked exact low-gain material:

```text
task092 6872 -> 6807 gain 0.009504
task096 8798 -> 8758 gain 0.004557
task260 2110 -> 2109 gain 0.000474
```

Review-only material:

```text
task184 2520 -> 2491 gain 0.011575
task238 2355 -> 2325 gain 0.012821
```

These were not submitted because they do not meet the standalone threshold and
should be accumulated only into a clean micro-bundle if the gate permits it.

## Decision

Keep `task187`. It is isolated, locally exact, passed the current-baseline gate,
and improved the real Public Score by `+0.04`.

Next cycle: use the banked task092/task096/task260 material as bundle fill only
after finding enough additional non-risk exact gain to clear the micro-bundle
threshold, or continue standalone search for `gain >= 0.020`.
