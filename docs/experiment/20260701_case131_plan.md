# 2026-07-01 case131 plan

## 方針

- floor: Public Score 7181.11
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task092.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task092.onnx`
- baseline: params 182, memory 6810, cost 6992, n_pass 265, n_fail 0

## 仮説

`task092` は bounding box から水平 span と垂直 span を抽出し、`h_canvas` と `scalar_color_u8` を順に作る。TopK の slot 数、水平/垂直片側 branch、または active guard が冗長なら、Scatter/Expand/Where 中間を削って数 % 以上の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `k_five`、`h_canvas`、`scalar_color_u8`、`h/v` active guard を簡略化する。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task092.onnx` に反映する。
3. submit し、Public Score が 7181.11 を上回れば採用、上がらなければ破棄する。
