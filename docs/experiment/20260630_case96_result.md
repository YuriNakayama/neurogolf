# 20260630 case96 result — t319 の fallback table を 38行→4行に縮小し Public 7180.89 採用

## 対象

- `task319.onnx`
- baseline: params **269**, memory **21828**, cost **22097**, faithful `n_fail=0`
- Public Score floor: **7180.87**

## 実施内容

t319 は 3 object の signature と edge match から source object を選び、5x5 出力を 30x30 に pad する graph。case78 では `input_u8` の局所化は cost 悪化だったため、今回は source-choice 周辺を検証した。

### source-choice probe

| 候補 | cost | 結果 |
|---|---:|---|
| `fallback_or_default -> possible_idx` | 21409 | `n_fail=1` |
| source 1 固定 | 13162 | `n_fail=164` |
| source 2 固定 | 13162 | `n_fail=103` |
| `src2_possible -> p20_pair_match` | 20701 | `n_fail=14` |
| `src2_possible -> p21_pair_match` | 21281 | `n_fail=149` |
| `p10_pair_match` 削除 | 19641 | `n_fail=149` |
| `p20_pair_match` 削除 | 21282 | `n_fail=149` |
| `p21_pair_match` 削除 | 20702 | `n_fail=14` |

fallback 自体は `n_fail=1` を救うため必要。ただし全ペアで実際に使う fallback row は 38行中 **0, 22, 26, 36** の4行だけだった。

### 採用差分

- `fallback_sig_table`: shape `(38,3)` → `(4,3)`
- `fallback_slots`: shape `(38,)` → `(4,)`
- selected rows:
  - row 0: slot 2
  - row 22: slot 1
  - row 26: slot 1
  - row 36: slot 2

## faithful 検証

- before: params **269**, memory **21828**, cost **22097**, `n_fail=0`
- after: params **133**, memory **21590**, cost **21723**, `n_fail=0`
- cost delta: **-374**

## submit

- ref: `54192133`
- description: `case96 t319 shrink fallback table cost 22097->21723`
- status: `COMPLETE`
- Public Score: **7180.87 → 7180.89**

## 採否

- **採用**。ローカル faithful `n_fail=0` かつ実 Public Score が上昇。
- `data/output/onnx/task319.onnx` を差し替え、DVC push 済み。

## 結論

t319 の fallback branch は必要だったが、fallback table は floor に未使用 row が多く残っていた。4行への縮小で params と memory の両方が下がり、実 LB でも +0.02 を確認した。次は同じ table/fallback 型の未使用 row が他タスクにないか、高 cost graph を横断して探索する。
