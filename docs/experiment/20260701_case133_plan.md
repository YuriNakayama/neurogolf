# 2026-07-01 case133 plan

## 方針

- floor: Public Score 7181.11
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task182.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task182.onnx`
- baseline: params 98, memory 6695, cost 6793, points 16.176352050808696, n_pass 267, n_fail 0

## 仮説

`task182` は `Conv` で色 ID を作り、`AveragePool` で対象 3x3 patch 位置を決め、patch 由来の kernel と threshold で `QLinearConv`/`MaxPool` による dilation recolor を行う。

中間は `tmask_b -> tmask_u8 -> tmask_i32 -> ReduceSum -> thr_u8`、`tmask_u8 + tmask_u8 -> kern`、`score -> score_dil -> dil_b -> recolor_b` に集中している。patch の形状や threshold が局所データで固定的なら、mask cast / threshold / dilation guard のどこかを畳めて相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で、`thr_u8`、`tmask_u8`、`kern`、`score_dil`、`recolor_b` 周辺の入力差し替えを個別に試す。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task182.onnx` に反映する。
3. submit し、Public Score が 7181.11 を上回れば採用、上がらなければ破棄する。
