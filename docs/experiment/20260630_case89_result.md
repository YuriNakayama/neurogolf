# 20260630 case89 result — t133 の marker/correction branch は全て必要（提出なし）

## 対象

- `data/output/onnx/task133.onnx`
- baseline: params **1278**, memory **32295**, cost **33573**, `n_fail=0`

## trace

主な memory 支配テンソル:

- `border_grid_f`: float `[1,1,30,30]` = 3600B
- `marker_1..4` / `out_grid` / full-grid intermediates: uint8 `[1,1,30,30]` = 900B each
- `ref_kernel_4`: uint8 `[1,1,12,12]` = 144B
- correction update vector: uint8 `[232]` = 232B

## 検証

### branch 削除

`out_grid = Max(marker_1, marker_2, marker_3, marker_4, corr_overlay)` から 1 branch を削除し、未使用ノード/initializer を剪定。

| 変更 | params | memory | cost | 結果 |
|---|---:|---:|---:|---|
| drop `marker_1` | 1278 | 30495 | 31773 | `n_fail=180` |
| drop `marker_2` | 1273 | 29559 | 30832 | `n_fail=126` |
| drop `marker_3` | 1273 | 29514 | 30787 | `n_fail=73` |
| drop `marker_4` | 1273 | 29451 | 30724 | `n_fail=26` |
| drop `corr_overlay` | 83 | 30905 | 30988 | `n_fail=267` |

`marker_4` 削除が最も近いが 26 failure で不採用。correction overlay は全例で必要。

### branch 単独化

| 変更 | cost | 結果 |
|---|---:|---|
| only `marker_1` | 22612 | `n_fail=267` |
| only `marker_2` | 23553 | `n_fail=267` |
| only `marker_3` | 23598 | `n_fail=267` |
| only `marker_4` | 23661 | `n_fail=267` |
| only `corr_overlay` | 8006 | `n_fail=263` |

単独 branch は成立しない。

### 機械的削減

- unused initializer: なし
- duplicate initializer: なし

## 採否

- Kaggle submit: **なし**
- 採否: **不採用**
- Public Score floor: **7180.87 維持**

## 結論

t133 は scale 1/2/3/4 marker と correction overlay がそれぞれ arc-gen 正答性に必要。branch 削除は大きく cost を下げるが faithful を保てない。

## 次の一手

次は t209/t054/t285/t367 の高コスト graph を順に一点検証する。
