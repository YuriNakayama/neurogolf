# 20260701 case171 plan

## Context

- Current accepted Public Score: 7181.17
- Recent geometry and scatter tasks had large approximate reductions but no exact
  candidate. Continue with compact medium-cost tasks and focus on exact guard
  removal.

## Target

- Task: `task303`
- Baseline audit:
  - params: 22
  - memory: 1598
  - cost: 1620
  - points: 17.60981857177357
  - n_pass: 265
  - n_fail: 0
  - filesize: 1177

## Hypothesis

`task303` builds row and column activity codes from black-cell row/column sums:

- `marker_rows = row_sum == width`
- `marker_cols = col_sum == height`
- `row_code = marker_rows ? 2 : row_active`
- `col_code = marker_cols ? 0 : 2 - col_active`
- `red_mask = row_code > col_code`

If marker rows or marker columns are redundant for the public examples, one `Where`
branch or a code tensor can be removed. Because the graph is memory-dominated, an
exact row/column code alias could produce a useful relative reduction.

## Method

1. Probe one alias at a time on temporary `task303` ONNX copies.
2. Run `audit_one` only for `task303`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record low-fail aliases and leave the baseline ONNX unchanged if no exact win is
   found.
