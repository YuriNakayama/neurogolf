# 20260701 case174 plan

## Context

- Current accepted Public Score: 7181.17
- case173 showed both coverage branches in `task222` are required.
- Continue prioritizing memory-heavy short graphs where one large tensor might be
  exactly redundant.

## Target

- Task: `task278`
- Baseline audit:
  - params: 47
  - memory: 6084
  - cost: 6131
  - points: 16.277611983873844
  - n_pass: 266
  - n_fail: 0
  - filesize: 1249

## Hypothesis

`task278` slices two channels, expands red cells through a quantized 3x3
convolution, combines the halo with channel 0, scales the red/green composite, then
pads and one-hot encodes the result.

Potential exact reductions:

- `halo_u` or `green_u` may be replaceable by an existing channel mask.
- `rg`, `rg3`, or `cguK` may have redundant arithmetic for public examples.
- The final pad or one-hot staging may expose a shape-preserving alias.

## Method

1. Probe one rewrite at a time on temporary `task278` ONNX copies.
2. Run `audit_one` only for `task278`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record all failed aliases and leave baseline ONNX unchanged otherwise.
