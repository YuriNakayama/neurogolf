# 20260702 case356 plan - task219 pad branch alias

## Context

Accepted Public Score is `7185.62` after case355. Remaining low-risk backlog
material is below the submit threshold, so case356 continues structural search
from the current accepted baseline.

Best candidate:

```text
task219 baseline:  params=91 memory=15873 cost=15964 n_fail=0
task219 candidate: params=91 memory=14969 cost=15060 n_fail=0
gain:              ln(15964 / 15060) = 0.058294
```

## Hypothesis

The later `pad_445` branch is equivalent to earlier `pad_293` for all local
task219 train/test/arc-gen examples. Rewiring later consumers from `pad_445` to
`pad_293` prunes the now-dead second selection/pad branch, reducing memory by
`904` without changing params.

## Implementation

Replace only:

```text
data/output/onnx/task219.onnx
```

with:

```text
/tmp/neurogolf_case356_struct/task219_Pad_pad_445_TO_pad_293.onnx
```

## Gate

Run `src.evaluate.audit_one` for task219 only:

```text
n_fail=0
cost=15060
```

Operator check on the candidate:

```text
ops = Add, ArgMax, Cast, Concat, CumSum, Equal, Gather, Max, Mul, Pad,
      ReduceMax, ReduceSum, Reshape, Slice, Squeeze, Sub, Unsqueeze
functions = 0
```

No forbidden operator is introduced.

## Risk

Medium. This is a structural local-equivalence alias in task219. It is not a
known hidden-unsafe family, but it relies on local equivalence of two branch
outputs. Submit as an isolated single-task change so Public LB attribution is
clean.
