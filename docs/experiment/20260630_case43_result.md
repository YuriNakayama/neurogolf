# 20260630 case43 result — crop技法を活かす対象探索: 未cropの高コストfloorを特定も規則が非局所（提出なし）

## independent_impl（必須欄）
case42 の crop技法を活かすため、**floor が 30×30 中間を残す高コストタスク**を shape-inference で洗い出し:

| task | cost | 30×30中間 | maxgrid | 規則(decode) | 勝てない理由 |
|---|---|---|---|---|---|
| t233 | 51068 | 21 | 30×20 | 多段(golf済) | 規則複雑 |
| t018 | 45211 | 18 | 24×24 | per-object 回転(marker整列) | 非局所・data依存 |
| t319 | 22097 | 4 | 19×5 | object選択+crop(可変出力shape) | 出力shape可変で静的net困難 |

- crop技法は「中間を実gridに縮小」できるが、**勝つには安価な正答規則も必要**。上記はいずれも
  規則が per-object 幾何/選択で、crop しても多op が要り floor を超える。
- t319 は出力 shape が例ごとに可変(5×3 等)で、静的形状 net では表現困難。

## 確定 + 技法の位置づけ
- crop技法(case42)は獲得済みで有効だが、適用先は「規則が安価 かつ floor が未crop」の積集合。
  本日検証範囲ではその積が空（安価規則タスクは floor が既に crop+最小化済み、未crop タスクは規則が複雑）。
- 高コストタスクの非局所性は case30/35-43 で一貫。floor はコミュニティ最適化済み。

## 恒久方針の遵守
- 「枯渇」と結論せず crop技法という新ツールを獲得し、その適用先を網羅探索した。停止提案なし。
- 探索は継続。次は中コスト帯で floor が未crop かつ局所規則のタスクに技法適用を試す。

## 次の一手
- 中コスト(3k-8k)で floor が 30×30 中間を持ち、規則が k×k 局所のタスクを抽出 → crop+CNN/手構築。
- floor 7180.78（DVC 6e8d4642）退行なし。
