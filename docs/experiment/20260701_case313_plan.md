# 20260701 case313 plan - task158 mask-a alias

## Context

- Current accepted Public Score floor: `7183.10` after case312.
- Scoring is logarithmic: expected point gain is `ln(old_cost / new_cost)`.
- Updated priority rule: prefer relative reduction and structural tensor deletion.
- New practical gate for submitted single-task edits: aim for
  `old_cost / new_cost >= 1.02` unless collecting a micro backlog.

## Target

- `data/output/onnx/task158.onnx`

## Baseline

Current local audit:

```text
params=2630
memory=31087
cost=33717
n_pass=266
n_fail=0
status=ok
```

## Hypothesis

`task158` builds two color masks from `color_work`:

```text
Equal(color_work, color_a) -> mask_a_bool
Cast(mask_a_bool) -> mask_a_u8
Equal(color_work, color_b) -> mask_b_bool
Cast(mask_b_bool) -> mask_b_u8
Concat(mask_a_u8, mask_b_u8) -> ab_u8
```

On the current public + generated examples, the `mask_a_u8` channel is
equivalent to the already-computed `nonbg_u8` channel for the downstream pair
stamping path. Rewiring only:

```text
mask_a_u8 -> nonbg_u8
```

should prune the `mask_a_bool` Equal and Cast while preserving the second color
channel.

## Probe Result

Temporary audit before editing the real ONNX:

```text
mask_a_u8 -> nonbg_u8
params=2629
memory=29785
cost=32414
n_pass=266
n_fail=0
status=ok
```

Expected gain:

```text
33717 / 32414 = 1.0402
ln(old/new) ~= 0.0399 pt
```

Rejected competing probes:

```text
mask_b_u8 -> nonbg_u8: cost=32415, n_fail=0
both mask_a_u8 and mask_b_u8 -> nonbg_u8: n_fail=152
```

The two single-channel aliases are mutually exclusive in this graph because
applying both collapses the two-color pair input.

## Verification Plan

1. Apply only `mask_a_u8 -> nonbg_u8`.
2. Dead-prune unused nodes, initializers, and value_info.
3. Run individual `evaluate.audit_one` for `task158`.
4. Submit only if `n_fail=0`, `status=ok`, and cost remains lower.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   the rejection.
