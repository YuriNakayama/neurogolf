# 2026-07-01 case130 plan

## 方針

- floor: Public Score 7181.11
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task008.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task008.onnx`
- baseline: params 65, memory 5561, cost 5626, n_pass 266, n_fail 0

## 仮説

`task008` は channel 2 の形状を、channel 8 の位置に合わせて row/col 方向に移動し、最後に row/col base と 8 mask を合成する。`colov` の片側固定、negative wrap 補正、row/col base、または final 8 overlay が冗長なら、16x16 級中間を削れて数 % の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `colov`、`dr/dc`、`drg/dcg`、`base/lab2/lab` の簡略化を試す。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task008.onnx` に反映する。
3. submit し、Public Score が 7181.11 を上回れば採用、上がらなければ破棄する。
