# 20260701 case333 plan - task274 zero_small broadcast compression

## Context

- Current accepted Public Score floor: `7184.88` after case332.
- Priority rule: `n_fail=0` first, then relative reduction
  `old_cost / new_cost`.
- This is a small params-only cleanup on a previously accepted task. The
  relative gain is still meaningful for the current scoring function.

## Target

- `data/output/onnx/task274.onnx`

## Baseline

Current local audit:

```text
params=94
memory=83
cost=177
n_fail=0
status=ok
```

## Hypothesis

The current graph uses `zero_small` only as the false branch in:

```text
Where(out8_small_b, zero_small, one_u8) -> out0_small
```

`zero_small` is currently:

```text
uint8[1, 1, 3, 3]
```

Because `Where` broadcasts inputs, it can be represented as:

```text
uint8[1, 1, 1, 1]
```

without changing output values.

## Expected Gain

Temporary audit:

```text
params=86
memory=83
cost=169
n_fail=0
status=ok
```

Expected point gain:

```text
177 / 169 = 1.0473
ln(old/new) ~= 0.0463 pt
```

## Verification Plan

1. Apply only the `task274` `zero_small` initializer compression.
2. Run individual `evaluate.audit_one` for `task274`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
