# 20260703 case560 result - candidate scan tooling

## Submission

- Submitted: no
- Accepted Public Score remains: `7187.57`
- Decision: no-submit / tooling adopted

## Changes

Added a bounded candidate scanning path to the existing submit gate:

```text
backend/src/submit/gate.py
backend/src/submit/__main__.py
backend/tests/unit/src/submit/test_gate.py
```

New command:

```bash
cd backend
uv run python -m submit scan-candidates <candidate-file-or-dir> \
  --baseline-dir ../data/output/onnx \
  --task-dir ../data/lake/neurogolf-2026
```

The scanner:

- recursively accepts candidate directories;
- evaluates each `task*.onnx` independently with the existing gate logic;
- converts runtime/model exceptions into `blocked-exception` rows;
- keeps scanning after invalid candidates;
- sorts submit-ready and bank-low candidates to the top.

## Verification

```text
cd backend && uv run ruff check src/submit tests/unit/src/submit/test_gate.py
cd backend && uv run pytest tests/unit/src/submit/test_gate.py
```

Both passed.

Smoke scan over recent candidate pools:

```text
scanned: 40
submit-candidate: 0
bank-low-gain: 2
```

The two bank-low results were the already rejected `task319` family, so no new
Kaggle submission was made.

## Parallel Repair Result

No exact positive repair was found for `task260` or `task292`.

```text
task260: 2109 -> 2108, gain 0.000474, n_fail 2
task292: 434 -> 252, n_fail 28
```

Exact repairs for these families restore or exceed baseline cost, so they are
not submit candidates.

## Conclusion

Case560 improves throughput rather than score. It directly addresses the recent
multi-hour no-submit issue by making broad scratch scans interruptible and
non-fatal. Future subagents should write candidates into directories and the main
thread should run `scan-candidates` every cadence window, submitting only exact
positive results.

## Next Step

Case561 should use the scanner on fresh task ranges and stop scans early when no
exact positive appears. Keep close misses like `task118 n_fail=1` documented, but
do not spend Kaggle submissions on local-fail candidates.
