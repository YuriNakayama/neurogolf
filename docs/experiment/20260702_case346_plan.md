# 20260702 case346 plan - task158 diagonal slice alias

## Context

- Current accepted Public Score floor: `7185.33` after case345.
- Submit-saving cadence is about one submit window every 30 minutes.
- The current low-risk micro batch is still below threshold (`~0.0212`).
- `task233` has a viable score-table compression candidate (`gain ~= 0.0339`),
  but `task158` has a slightly stronger and simpler two-alias candidate.

## Candidate

Target `task158`.

Apply the diagonal slice alias bundle:

```text
tr28 -> tl28
br28 -> bl28
```

Expected local change:

```text
baseline:  params=2629, memory=29785, cost=32414
candidate: params=2613, memory=28681, cost=31294
n_fail=0, status=ok
gain = ln(32414 / 31294) ~= 0.0352
```

Temp ONNX:

```text
/tmp/neurogolf_task158_133_case346/task158_diagbundle_tr_TO_tl__br_TO_bl.onnx
```

This clears the medium-risk standalone threshold of `0.030`.

## Alternatives

- Symmetric alternative `bl28 -> tl28`, `br28 -> tr28` has the same local cost
  and audit result. Treat it as mutually exclusive, not cumulative.
- `task233 scorew uint8` is exact locally with `49454 -> 47804`, gain `0.0339`;
  bank it for a later cadence window if no stronger candidate appears.

## Gate

1. Wait for the next submit cadence window after case345 unless a stronger,
   safer candidate returns first.
2. Copy the candidate to `data/output/onnx/task158.onnx`.
3. Run `src/evaluate.audit_one` only for `task158`.
4. Submit if `n_fail=0` and cost is lower.
5. Adopt only if Kaggle Public Score improves over `7185.33`; otherwise restore
   the DVC baseline and record rejection.
