# 20260701 case338 plan - task266 dead BitwiseXor prune

## Context

- Current accepted Public Score floor: `7185.05` after case336.
- Case337 was rejected because a broad local-equivalence bundle was
  hidden-unsafe. The new submit-saving policy prefers low-risk candidates unless
  a higher-risk candidate has exceptional expected gain.
- A low-risk cleanup scan found a task266 dead-node prune with expected gain
  above the standalone submit threshold.

## Target

- `data/output/onnx/task266.onnx`

## Baseline

Current local audit:

```text
params=79
memory=255
cost=334
n_fail=0
status=ok
```

## Hypothesis

The current graph contains a dead `BitwiseXor(ch3, ch3) -> zero` node that is not
on the output dependency path. Removing it should preserve outputs while
reducing profiled memory.

This is a low-risk structural cleanup: no consumer semantics are changed and no
new ONNX operators are introduced.

## Expected Gain

Temporary audit:

```text
params=79
memory=240
cost=319
n_fail=0
status=ok
```

Expected point gain:

```text
334 / 319 = 1.0470
ln(old/new) ~= 0.0460 pt
```

## Verification Plan

1. Apply only the `task266` dead-node prune.
2. Run individual `evaluate.audit_one` for `task266`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
