# 20260701 case164 plan

## Context

- Current accepted Public Score: 7181.17
- Recent mask branch deletion attempts failed exact-match auditing.
- Shift to a higher-cost index/update graph where dtype or guard simplification might preserve semantics exactly.

## Target

- Task: `task131`
- Baseline audit:
  - params: 675
  - memory: 3312
  - cost: 3987
  - points: 16.709205652618643
  - n_pass: 266
  - n_fail: 0
  - filesize: 11222

## Hypothesis

`task131` gathers green/red anchors, shifts selected green cells, and writes updates with `ScatterND`. Potential reduction points:

- `scatter_idx8 -> Cast -> scatter_idx` contributes a large index tensor; if a smaller index dtype is accepted or a cast can be folded safely, memory may drop.
- `has_public_tenth` controls special old/new rows and duplicate update values; if the public distribution always has or never has the tenth case, several guards and updates can be removed.
- `green_valid` masks old/new rows and columns; if TopK always returns valid green cells, these guards may be redundant.

The task has enough cost that a successful exact-preserving change could move the Public LB if the reduction is not tiny.

## Method

1. Probe one dtype/guard replacement at a time on temporary `task131` ONNX copies.
2. Run `audit_one` only for `task131`.
3. Apply and submit only if `n_fail=0` and cost decreases materially.
4. If no exact-match reduction exists, write result and commit docs only.
