# 2026-06-30 case126 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task398.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task398.onnx`
- baseline: params 190, memory 5417, cost 5607, n_pass 268, n_fail 0

## 仮説

`task398` は `rkey/ckey` を `Where(rin/cin, value, ok49)` で guard して `table` を Gather している。hidden/arc-gen で guard が常に true、または row/col の片側が冗長なら、`Where` か index 計算を削って相対削減できる。

## 手順

1. `/tmp` 上の candidate ONNX で `rkey/ckey` guard、`N/offv`、`table` index を簡略化する。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task398.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
