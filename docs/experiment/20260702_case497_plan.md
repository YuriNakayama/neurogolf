# 20260702 case497 plan - task013 low-fail repair scan

## Context

Current accepted Public Score:

```text
7187.10
```

Case496 found no submit-ready candidate in the `task008/task013/task099/task105/task117`
owned band. The nearest meaningful failures were on `task013`:

```text
1870 -> 1840, gain=0.016173, n_fail=6
1870 -> 1810, gain=0.032612, n_fail=10
```

## Target

```text
task013.onnx
```

Reason:

- low failure counts compared with the broad-fail families in recent scans;
- medium relative gain if repaired;
- small graph, so targeted repair may be feasible.

## Method

1. Re-gate current `task013` scratch candidates.
2. Rank by low `n_fail` first, then relative gain.
3. Inspect the best low-fail candidates and attempt only targeted repairs that
   preserve positive cost delta.
4. Submit only if a repaired or existing candidate reaches `submit-candidate`.

## Acceptance

Adopt only if Kaggle Public Score improves above `7187.10`.
