# 20260630 case16 plan — 単一Conv floor net の param 削減（疎カーネル活用）

## 仮説
floor の single-Conv net（34 タスク, 多くが 10×10×3×3=900 params）で、使う色が少ないタスクは
カーネルに all-zero 入力/出力チャネルがあり、入力を Slice して in_channels を減らせば params 削減＝
cost 減になるか。

## 手法
t171（input all-0, nz_in=[0] のみ）で Conv[10,1,3,3]=90 params に削減（入力を ch0 に Slice）。
faithful & cost 比較。
