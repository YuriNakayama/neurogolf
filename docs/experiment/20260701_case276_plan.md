# 20260701 case276 plan - task338 single pair alias

## Context

- Current Public Score floor: `7181.72` after case275.
- Priority rule is now relative reduction: score gain is `ln(old_cost / new_cost)`, so selection uses `old_cost / new_cost` rather than absolute cost.
- Recent accepted cases were exact same-task aliases:
  - case273 `task066`: `17295 -> 16838`, ratio `1.0271`
  - case274 `task255`: `23017 -> 22731`, ratio `1.0126`
  - case275 `task080`: `17136 -> 16836`, ratio `1.0178`

## Candidate

Target: `task338`

Local audit baseline:

```text
params=689
memory=17250
cost=17939
n_pass=267
n_fail=0
```

Alias probe:

```text
tr_pair -> tr_pair_0: cost 17939 -> 17819, n_fail=0, ratio 1.0067
bl_pair -> bl_pair_0: cost 17939 -> 17819, n_fail=0, ratio 1.0067
both aliases: cost 17939 -> 17689, n_fail=9, reject
```

`task064` has an exact two-alias bundle (`12478 -> 12430`) but the ratio is only
`1.0039`, so `task338` is the better relative-reduction candidate.

## Plan

Apply only `tr_pair -> tr_pair_0` to `data/output/onnx/task338.onnx`, then prune dead nodes and stale metadata.

Gate with `evaluate.audit_one` for `task338` only. Submit only if:

- `status=ok`
- `n_fail=0`
- cost remains `17819`

Do not bundle `bl_pair -> bl_pair_0` because the two-alias combination fails local correctness.
