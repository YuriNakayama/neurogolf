# case223 result: task290 TopK spatial table trim

## 結果

- 対象: `task290`
- baseline: `cost=355` (`params=63`, `memory=292`, `n_fail=0`)
- submit: なし
- 採否: 不採用

## 検証内容

temp ONNX で以下を個別に差し替え、`src.evaluate.scorer.audit_one` で検証した。

- `topk_k=1/2/3/4/5/9`
- `color_cols` variants
- `outer_col` variants
- `size_match` 固定
- `spatial6` row 固定

## 監査結果

| 変更 | cost | n_pass | n_fail | 判定 |
| --- | ---: | ---: | ---: | --- |
| baseline `k=3`, `cols=(2,1)`, `outer=1` | 355 | 266 | 0 | ok |
| `k=1/2` variants | 331-343 | 0 | 266 | reject |
| `k=3`, other `cols/outer` | 355 | 0 | 266 | reject |
| `k=4/5/9`, baseline ranks | 367-427 | 266 | 0 | reject: cost worsens |
| `size_match` fixed row | 346 | 58-72 | 194-208 | reject |
| `spatial6` fixed row | 274 | 58-72 | 194-208 | reject |

## 結論

TopK rank 2/1 と `outer_col=1` の組み合わせ、および 4 row の spatial table が全ペア正答に必要だった。`spatial6` 固定は `355 -> 274` まで下がるが足切りを満たさない。実 ONNX は変更せず、Kaggle submit もしない。

次は、候補数削減よりも dtype/table 表現を保ったまま params を減らせる task を探す。
