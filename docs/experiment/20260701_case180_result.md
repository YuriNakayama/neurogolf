# 20260701 case180 result

## Target

- Task: `task346`
- Baseline cost: 1254
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task346`.

No exact cost-reducing candidate was found.

Ring decision aliases:

- `ch0_ring -> true_b`: cost 1239, `n_fail=123`
- `ch0_ring -> false_b`: cost 1239, `n_fail=144`
- `ch0_answer -> ch0_ring`: cost 1254, `n_fail=267`
- `ch0_answer -> true_b`: cost 1255, `n_fail=144`
- `ch0_answer -> false_b`: cost 1255, `n_fail=123`
- `ring_center_score -> ring_score`: cost 1254, `n_fail=144`
- `ring_center_score -> center1i`: cost 1242, `n_fail=144`
- `actual_idx -> top_idx`: cost 1254, `n_fail=144`

Scatter/output aliases:

- `color_pair_bool -> ch0_ring`: cost 1254, `n_fail=267`
- `color_pair_bool -> ch0_answer`: cost 1254, `n_fail=267`
- `color_row_bool -> present_bool`: cost 1237, `n_fail=267`
- `color_row_bool -> color_pair_bool`: invalid `Unsqueeze` shape
- `color_1x1_bool -> color_row_bool`: invalid `Pad` rank
- graph output set to internal bool tensors: invalid declared output shape
- graph output set to `input`: unscorable, `n_fail=267`

Type/shape failures:

- `ch0_ring -> ring_center_max`: invalid `Not` int8 input
- `ring_center_max -> ring_center_score`: invalid `Equal` rank
- `patch0i -> patch0`: invalid `QLinearConv` float input
- `center1i -> center1`: invalid `Add` type binding
- `top_idx -> present`: invalid `Add` type binding
- `actual_idx -> present`: invalid `Split` shape
- `present_bool -> present`: invalid `ScatterElements` type binding

Dtype probes:

- `ring_w -> int8`: cost 1254, `n_fail=0`, score-neutral because it is already int8-sized
- `ring_w -> uint8`: invalid `QLinearConv` weight zero-point type binding
- `nine_i8 -> int8`: cost 1254, `n_fail=0`, score-neutral
- `nine_i8 -> uint8`: invalid `Equal` type binding

## Submission

- Not submitted.
- Reason: all scored cost reductions failed exact-match auditing or produced invalid
  ONNX.

## Decision

Rejected. Keep the existing `task346.onnx`.

## Next

The ring decision is balanced across public examples and cannot be fixed to one
branch. Avoid compact ScatterElements tasks unless there is a clear shape-preserving
index or update dtype reduction.
