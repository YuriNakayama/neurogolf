# 20260702 case498 result - task284 cadence scan

## Summary

- Target: `task284.onnx`
- Baseline cost: `3735`
- Candidate pool: `195` unique existing `task284*.onnx` files from `/tmp` and
  `/private/tmp`
- Positive cost candidates: `61`
- Gate result: no submit

## Gate

```text
COUNTS {'blocked-local-fail': 61}
```

All cost-reducing candidates failed local correctness. No candidate reached
`submit-candidate` or `bank-low-gain`.

Best low-fail group:

```text
3735 -> 3734, gain 0.000267773, n_fail=126
/tmp/neurogolf_same_shape_261_400/task284_38_Add_sep_TO_dx.onnx
/tmp/neurogolf_same_shape_261_400/task284_40_Div_drow_TO_dy.onnx
/tmp/neurogolf_same_shape_261_400/task284_45_Mul_stem_along_row_TO_drow.onnx
/tmp/neurogolf_same_shape_261_400/task284_46_Mul_stem_along_col_TO_stem.onnx
```

Several larger ratio cuts, including bcast kernel and selected color/channel
aliases, produced high local fail counts. The stderr stream also showed many
`ScatterND` invalid-index failures, so this family is not submit-safe without a
structural repair.

## Decision

Rejected locally. No Kaggle submit was made for this cycle because every
cost-reducing candidate violated the `n_fail=0` gate.

## Submit Cadence Diagnosis

The previous hours had fewer submits because cases495-498 produced no
`n_fail=0` candidate after local gating. This is intentional for regression
control, but it underuses the refreshed submit budget when the search is too
serial.

## Next Action

Increase parallel exploration breadth and keep the main process dedicated to:

1. integrating the first reported `submit-candidate`;
2. bundling multiple exact micro candidates only after local `n_fail=0`;
3. submitting on a roughly 15-minute cadence when any gated candidate exists.

Worker bands should scan disjoint task ranges and report immediately on
`submit-candidate`, while no-submit scans still bank exact low-gain candidates.
