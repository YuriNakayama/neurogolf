# case290 result: task209 crop-at-source label Conv

## Summary

- Submission ref: `54224535`
- Previous Public Score: `7182.22`
- New Public Score: `7182.27`
- Decision: adopt

## Local Gate

- Task: `task209`
- Before cost: `31187`
- After cost: `29483`
- Relative reduction: `1.0578`
- Local points: `14.708430894966588`
- Correctness: `n_pass=266`, `n_fail=0`
- Status: `ok`

## Change

Replaced the initial full-size label materialization plus crop:

`1x1 Conv -> Cast -> Slice(20x20)`

with a direct cropped label materialization:

`11x11 sparse Conv -> Cast`

The sparse kernel only has top-left taps, so the no-padding Conv over the `30x30` input
produces the same top-left `20x20` label image that the removed Slice previously
selected.

## Conclusion

Accepted. This confirms that structural crop-at-source rewrites are worth prioritizing
over tiny alias substitutions when they clear about 5% relative cost reduction and have
a shape/operator proof.

## Next Step

Continue looking for:

- terminal dtype reductions like `task055`,
- zero-channel Concat-to-Pad rewrites like `task367`,
- crop-at-source rewrites like `task209`,
- bundled alias removals only when the bundle removes whole intermediate tensors.
