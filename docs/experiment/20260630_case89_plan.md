# 20260630 case89 plan — t133 の marker scale/correction branch 削減を検証する

## 背景

- 現 Public best は **7180.87**。
- t133 は cost **33573**（params 1278 / memory 32295）で、template-based stamping graph。
- graph は key shape から `ref_kernel` を作り、scale 1/2/3/4 の `marker_1..4` と correction overlay を `Max` で合成している。

## 仮説

1. 一部 marker scale (`marker_1..4`) が arc-gen 上不要なら branch を削れる。
2. `corr_overlay` が稀な補正なら、削除しても full audit が通る可能性がある。
3. initializer 重複や unused value_info があれば機械的削減できる。

## 対象

- `data/output/onnx/task133.onnx`

## 足切り

- `src.evaluate.scorer.audit_one` で t133 の train/test/arc-gen 全ペア完全一致。
- n_fail=0 かつ cost 減少の場合のみ submit。

## 採否

- Public Score が 7180.87 を上回れば採用。
- 上がらない、または faithful で不一致なら破棄。
