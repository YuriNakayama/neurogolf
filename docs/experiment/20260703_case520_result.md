# 20260703 case520 result - no submit standalone search

## Result

No submit.

Current accepted Kaggle score remains:

```text
Public Score: 7187.37
latest accepted ref=54271014
```

## Search Summary

No standalone candidate met submit conditions:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
not in known-risk task lists
```

Workers:

```text
V3 low/mid-cost current-baseline scan: no exact cost-reducing candidate
V4 targeted scratch re-gate: no submit-candidate
V2 high-cost follow-up: stopped after timeout; no result used for this cycle
parent alias scan: only task066 bank-low exact candidates
```

## Banked Exact Micro

Parent scan:

```text
task066 16351 -> 16309
gain=0.002571955
n_fail=0
decision=bank-low-gain
candidate=/tmp/neurogolf_case520_parent_alias/task066_active_row_max_TO_hi_f.onnx
candidate=/tmp/neurogolf_case520_parent_alias/task066_active_row_max_TO_hundred_f.onnx
```

V4 scratch re-gate:

```text
task349 24389 -> 24369
gain=0.000820
n_fail=0
candidate=/private/tmp/neurogolf_parallel_AD/bundle_clean_best5/task349.onnx

task054 27089 -> 27073
gain=0.000591
n_fail=0
candidate=/private/tmp/neurogolf_parallel_AD/bundle_clean_best5/task054.onnx
```

These remain banked only. Do not bundle them blindly after case514's Kaggle
`ERROR`.

## Near Misses

Closest low-fail candidates:

```text
task033 1639 -> 1594
gain=0.027840
n_fail=3
candidate=/tmp/neurogolf_agent_V3/task033_alias_018_1_line_cell_1_0_TO_line_cell_0_1_dce.onnx

task033 1639 -> 1594
gain=0.027840
n_fail=4
candidate=/tmp/neurogolf_agent_V3/task033_alias_017_11_line_cell_0_2_TO_line_cell_0_1_dce.onnx
```

Highest-gain near misses were broad semantic collapses with `n_fail > 260` and
are not useful for immediate submit.

## Decision

Skip submit to preserve budget. The cycle produced only micro exact candidates
and failed near misses. The next useful repair target is task033 because it has
`gain > 0.020` with only 3 failing examples.
