# 20260703 case551 plan - post task066 next-candidate scan

Accepted Public Score:

- `7187.51`

## Background

Case550 adopted `task066` broadcast-pad simplification. During that pending
window, a worker searched non-risk structural follow-up targets and mined
current-baseline candidates.

## Target

Look for a standalone non-risk candidate among:

```text
task025, task005, task208, task333, task361, task020
```

Avoid `task064` and `task370` because they are in the review-risk set.

## Submit Rule

Submit only if a current-baseline candidate has:

```text
n_fail = 0
gain >= 0.020
forbidden ops = []
functions = 0
```

Otherwise bank exact micros but do not submit unless a max-5 bundle totals at
least `0.015`.
