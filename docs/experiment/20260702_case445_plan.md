# 20260702 case445 plan - task341 bridge mask re-gate

## Context

Accepted Public Score remains `7186.71`. After several small dynamic-crop and
ScatterND dtype probes failed, this cycle switches to a terminal-mask task.

Current `task341` audit:

```text
params=35
memory=1394
cost=1429
status=ok
```

Dominant tensor:

```text
bridge30 BOOL [1,1,30,30] 900 bytes
```

Graph tail:

```text
And(bridge_rows6, bridge_cols6) -> bridge10
Pad(bridge10, pads_bridge) -> bridge30
Where(bridge30, cyan, input) -> output
```

Prior case160 already found that orientation and bridge-mask simplifications are
not exact, with the closest failures at `n_fail=18` and `n_fail=27`.

## Hypothesis

The terminal `bridge30` mask is likely required by the final `Where`, so direct
removal will fail. Still, the current scratch pool contains many branch aliases
and cleanup candidates; re-gating them verifies whether any exact reduction
survived later baseline changes.

## Method

1. Re-gate existing `task341` branch, guard, and cleanup candidates against the
   current baseline.
2. Submit only if an exact candidate clears the post-regression gate.
3. Otherwise record no-submit and deprioritize this task.
