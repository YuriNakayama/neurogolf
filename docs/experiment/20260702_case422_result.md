# 20260702 case422 result - current-baseline cleanup scan

## Summary

No submit.

Accepted Public Score remains `7186.71`. A current-baseline cleanup scan found
one exact one-parameter improvement, but it is far below the submit threshold.

## Initializer Scan

Scanned all current accepted ONNX files for:

- duplicate initializers with identical dtype/shape/value;
- unreferenced initializers.

Result:

```text
task046.onnx total_saving_params=1 dup=0 unused=1
  unused elem=1 offset_-2 shape=() dt=10
```

No other current task had duplicate or unused initializer savings.

## Candidate

Built:

```text
/private/tmp/neurogolf_case422_cleanup/task046_remove_unused_offset_m2.onnx
```

Gate:

```text
task          : 046
baseline cost : 2286
candidate cost: 2285
gain          : 0.000438
status        : ok
n_fail        : 0
functions     : 0
forbidden ops : []
decision      : bank-low-gain
```

This was added to `docs/experiment/micro_backlog.md`.

## Constant Scan

Scanned all current accepted ONNX files for duplicate and unused `Constant`
nodes counted by the scorer. No candidate savings were found.

## Decision

Do not submit. The only exact candidate is safe but negligible.

## Next Step

Continue with fresh current-baseline graph analysis. Since duplicate/unused
constant cleanup is exhausted, the next promising direction is task-local
value-path simplification where a whole intermediate chain can be pruned without
changing selector/table coverage.
