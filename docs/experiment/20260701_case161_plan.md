# 20260701 case161 plan

## Context

- Current accepted Public Score: 7181.17
- Recent bridge/path mask probes found no exact-match reductions.
- Continue with a single middle-cost task and focus on guard branches that may be distribution-specific.

## Target

- Task: `task273`
- Baseline audit:
  - params: 51
  - memory: 2109
  - cost: 2160
  - points: 17.32213649932179
  - n_pass: 266
  - n_fail: 0
  - filesize: 4316

## Hypothesis

`task273` fills up to two rectangles. It uses `TopK` row candidates and a guard:

- `has_rect2 = Cast(rect2_value)`
- `r3 = Where(has_rect2, r3_raw, r2)`
- `fill = rect1_fill | rect2_fill`

If public examples always contain the second rectangle or if the second fill is redundant in a subset, removing the guard or one fill branch may reduce several 20x20 mask tensors. This task has enough memory for a useful relative reduction if the replacement is exact.

## Method

1. Probe one rectangle/guard replacement at a time on temporary `task273` ONNX copies.
2. Run `audit_one` only for `task273`.
3. Apply and submit only if `n_fail=0` and cost decreases meaningfully.
4. If no exact-match reduction exists, write result and commit docs only.
