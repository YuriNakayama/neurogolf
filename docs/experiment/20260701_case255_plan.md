# 20260701 case255 plan — task343 zero_rows Concat→Pad

## 現状

- 直近採用: case249 `task055` final `Einsum` float16 化で Public Score `7181.59`
- 直近 probe: case254 `task157` bool Pad 化は opset13 変換 overhead で cost 悪化
- 優先基準: `n_fail=0` を絶対条件にし、`old_cost / new_cost` の相対削減率を優先する。

## 対象

- `task343`
- baseline audit: `params=110`, `memory=1927`, `cost=2037`, `n_fail=0`

## 仮説

`task343` は opset 13 で、終端付近に `Concat(zero_rows, output_body_ids, axis=2) -> output_ids` がある。`zero_rows` は `(1,1,2,15)` の all-zero uint8 initializer で、prefix zero padding と等価。

これを `Pad(output_body_ids, pads=[0,0,2,0,0,0,0,0])` に置換すれば、`zero_rows` 30 要素を削り、pads 8 要素だけを追加するため、params-only で約 22 cost 削減できる。

## 手法

1. `Concat(zero_rows, output_body_ids)` を `Pad(output_body_ids, pads=[0,0,2,0,0,0,0,0])` に置換する。
2. `zero_rows` initializer を削除する。
3. `src.evaluate.scorer.audit_one` で `task343.onnx` のみ検証する。
4. `n_fail=0` かつ cost 減なら submit し、Public Score が上がれば採用する。
