# 20260702 case458 plan - current-graph low-risk surgery sweep

## Context

Case457 screened old scratch aliases for `task064`, `task076`, `task077`,
`task110`, `task173`, and `task219`. The large reductions were all local-fail
semantic cuts, and only already-banked `task064` micros were exact.

The next candidate surface should avoid stale alias pools. Use current accepted
ONNX graphs and deterministic low-risk surgery passes to find any remaining
cleanup that actually lowers scorer cost.

## Method

Run cost-only screening on every current `data/output/onnx/task*.onnx` with
these existing surgery passes:

```text
cleanup
identity_elementwise
index_surgery
broadcast_compress
conv1x1_to_gather
```

This is not a 400-task correctness audit. It is a cost-only ranker. After
ranking, audit only promising individual tasks with `audit_one`.

## Submit Gate

Submit only if a single-task candidate is:

```text
n_fail=0
gain >= 0.020
not a known hard/review/runtime risk family
mechanically explainable
no forbidden ops/functions
```

If only low-gain exact cleanup appears, keep it banked and skip submit.
