# 20260701 case157 plan

## Context

- Current accepted Public Score: 7181.17
- case155 showed that local +0.0016 points can be hidden by LB rounding.
- case156 showed `task284` ScatterND guards are required.
- Continue prioritizing `n_fail=0` first, then relative cost reduction large enough to move LB.

## Target

- Task: `task397`
- Baseline audit:
  - params: 200
  - memory: 2648
  - cost: 2848
  - points: 17.045627727468133
  - n_pass: 266
  - n_fail: 0
  - filesize: 3347

## Hypothesis

`task397` finds 2x2 color-change motifs, builds a boolean update mask with `ScatterElements`, pads it to 30x30, and applies `Where(cond30, green, input)`. The final padded condition and multi-level `valid/m2/m3/m4` update masks are the largest reducible area.

Potential safe reductions:

- replace `cond30` with a smaller or earlier mask if padding is redundant for the examples
- simplify `updates` or `scatter_idx_safe` if invalid scatter positions are already harmless
- drop one of the `m2/m3/m4` motif levels if the dataset never uses it

Expected useful threshold: aim for at least ~30 cost reduction; otherwise do not submit.

## Method

1. Probe only `task397` temporary ONNX edits.
2. Run `audit_one` after each candidate.
3. Apply and submit only if `n_fail=0` and cost reduction is meaningful.
4. Revert if Public Score does not exceed 7181.17.
