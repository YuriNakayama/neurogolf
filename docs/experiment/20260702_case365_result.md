# 20260702 case365 result - skip submit after recheck

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.78
```

## Checks

### task077 near miss

Parallel repair probe found no exact cost-reducing repair.

Best known cheap aliases remain incorrect:

| candidate | cost | n_fail | decision |
|---|---:|---:|---|
| baseline | `14834` | 0 | keep |
| `b2mn -> b1mn` | `13994` | 4 | reject |
| `b2mx -> b1mx` | `13994` | 4 | reject |
| `b2mn/b2mx -> b1mn/b1mx` | `13154` | 8 | reject |
| generic surgery | `14834` | 0 | exact but neutral |

The second propagation is required for sparse color-4 fills. Cheap widening or
threshold-bias repairs either underfill or overfill.

### task110 near miss

Parallel repair probe found no sub-baseline exact candidate.

| candidate | cost | n_fail | decision |
|---|---:|---:|---|
| baseline | `13591` | 0 | keep |
| `public_row_table -> row9_public_table` | `13069` | 1 | reject |
| smallest exact selector repair | `13592` | 0 | worse |

The failing train example needs the full row7 public table. Restoring the
required selector and table path costs at least one byte more than baseline.

### current-baseline surgery

Ran:

```text
uv run python -m pipeline.case3 surgery \
  --base ../data/output/onnx \
  --out /tmp/neurogolf_case365_surgery \
  --task-dir ../data/lake/neurogolf-2026
```

Result:

```text
surgery done: 1/400 tasks improved
```

The only exact improvement was the known task285 cleanup:

| task | current | candidate | gain | n_fail | path |
|---:|---:|---:|---:|---:|---|
| 285 | `24535` | `24512` | `0.000937876` | 0 | `/tmp/neurogolf_case365_surgery/task285.onnx` |

This is below every submit threshold and duplicates the case363 banked
candidate, so it remains backlog material.

## Conclusion

Skip the submit window. The threshold-sized near misses are structurally
required by correctness, while the only exact cleanup is far too small to spend
a Kaggle submission.

Next action: change search shape rather than repeating generic surgery:

- prioritize medium-cost tasks with high fanout tables or duplicated selector
  branches where a whole table/branch can be removed without hidden-sensitive
  topology pruning;
- continue banking exact micros, but submit them only as a low-risk batch above
  `gain >= 0.030`;
- avoid known unsafe families: task002 bit topology, task233 `uint8 TopK`,
  broad task286 local-equivalence bundles, and task118 hidden-sensitive table
  pruning below high-risk threshold.
