# 20260702 case397 plan - bounded unary bypass scan

## Context

Accepted Public Score is `7186.24`. The current backlog has useful micros, but
case396's fresh exact set totals only:

```text
0.004344483316347677
```

Submitting that alone is unlikely to move the displayed Public Score. The next
cycle should use the waiting window for fresh current-baseline search instead
of spending a submit on known-too-small changes.

## Hypothesis

Some lower-numbered current graphs still contain removable unary/value-path
nodes. Prior case381/case385 producer scans were productive, but the backlog is
now stale; run a bounded current-baseline scan over tasks `001..120` for local
rewires of single-output nodes such as:

```text
Cast, Clip, Squeeze, Unsqueeze, Reshape, Identity, Not, Abs, Neg
```

For each candidate, rewire consumers of the node output to one node input, prune
stale metadata, and task-local audit with correctness enabled.

## Gate

Submit only if a fresh exact candidate or compatible same-task bundle satisfies:

```text
n_fail=0
no forbidden ops/functions
gain >= 0.020
```

Otherwise, record exact micros and skip submit.
