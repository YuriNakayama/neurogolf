# 20260703 case539 result - solver-bank replacement refresh

Accepted Public Score:

- `7187.41`

## Summary

No submit.

This cycle refreshed existing `backend/pipeline/case3` solver-bank candidates
against the current accepted ONNX baseline. The goal was to find a clean
non-risk solver replacement with a meaningful relative cost reduction, because
the exact micro backlog was still only about `0.001982` aggregate gain.

## Local checks

Low/mid-cost non-risk sweep:

- Target scope: non-risk current tasks with baseline cost `<= 6000`.
- Generated solver-bank outputs: `47`.
- Gate result: `0` lower-cost exact candidates.
- Artifact directory: `/private/tmp/neurogolf_case539_case3_solve`.

High-cost non-risk sweep:

- Target scope: non-risk current tasks with baseline cost `> 6000`.
- Generated solver-bank outputs: `2`.
- Gate results:
  - task187: `26396 -> 26396`, `n_fail=0`, gain `0.000000`; blocked no-gain.
  - task192: `8621 -> 10987500`, `n_fail=0`, gain `-7.150312`; blocked worse.
- Skipped baseline-audit tasks while determining high-cost scope:
  `045,127,135,146,149,240,384`.
- Artifact/report:
  `/private/tmp/neurogolf_agent_case539_solver_high/report.json`.

## Submit decision

No candidate met the submit rule:

- standalone exact gain `>= 0.020`; or
- safe low-risk micro bundle gain `>= 0.015`.

The current micro queue remains banked and should not be submitted alone:

- task117: `4148 -> 4145`, gain `0.000724`.
- task234: `6845 -> 6841`, gain `0.000585`.
- task328: `6904 -> 6902`, gain `0.000290`.
- task206: `4183 -> 4182`, gain `0.000239`.
- task005: `6882 -> 6881`, gain `0.000145`.
- Aggregate: about `0.001982`.

## Conclusion

The existing solver bank does not currently provide a submit-worthy replacement
for the non-risk baseline set. To restore submit cadence without increasing
regression risk, the next cycle should increase parallel candidate supply rather
than lower the gate:

1. Run separate workers for mechanical ONNX simplification, near-miss repair,
   solver-bank/custom replacements, micro accumulation, dtype/initializer
   compression, and backlog mining.
2. Continue submitting only exact non-risk candidates that clear the standalone
   or bundle threshold.
3. Keep review-risk candidates isolated and avoid bundling them with low-risk
   micro changes.
