# 2026-07-01 case140 result

## 対象

- `task382.onnx`
- baseline: params 120, memory 5652, cost 5772, points 16.339226080106236, n_pass 266, n_fail 0

## 実施内容

vertical/horizontal pattern branch、red branch、outside sentinel branch、方向選択 (`vbank/hbank`, `row_cnt/col_cnt`) を `/tmp` 上の候補 ONNX で個別に差し替えた。

## 足切り結果

`n_fail=0` で cost が下がる候補はなし。

大 branch 省略:

- `vpat -> zero`: cost 4491, n_fail 130
- `hpat -> zero`: cost 4491, n_fail 136
- `red -> zero`: cost 5311, n_fail 266
- `row_out -> zero`: cost 5712, n_fail 245
- `col_out -> zero`: cost 5712, n_fail 247

branch 代替:

- `vpat -> hpat`: cost 4491, n_fail 130
- `hpat -> vpat`: cost 4491, n_fail 136
- `red -> vpat` / `hpat`: cost 5311, n_fail 266
- `cidx -> red`: cost 2506, n_fail 266
- `cidx -> vpat` / `hpat`: cost 3510, n_fail 266

方向選択省略:

- `vbank -> vpos_bank`: cost 5490, n_fail 57
- `vbank -> vneg_bank`: cost 5490, n_fail 73
- `hbank -> hpos_bank`: cost 5490, n_fail 66
- `hbank -> hneg_bank`: cost 5490, n_fail 70
- `row_cnt -> rcf/rcr`: cost 5610, n_fail 67/63
- `col_cnt -> ccf/ccr`: cost 5610, n_fail 63/73

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

`task382` は v/h branch、red overlay、outside sentinel の全要素が実データ上必要だった。大きい削減候補はあるが、完全一致を保てない。

## 次の一手

`Where(..., zero)` 横断で残る `task096` または `task037` を見る。特に `task037` は cost 4746 で scatter/update 系の branch があり、slot/color update の片側削除が通れば相対削減が見込める。
