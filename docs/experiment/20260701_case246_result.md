# 20260701 case246 result — task349 horizontal detector kernel crop は Public 不変で不採用

## 対象
- `task349.onnx`
- 採否: 不採用
- floor: Public Score `7181.28`
- submit ref: `54211879`
- submitted message: `case246 task349 crop h detector kernels cost 24389 to 24369`

## 実施内容
`h_kernel_i8_c0..c3` の右端ゼロ列を crop し、対応する QLinearConv の `kernel_shape` と右 padding を更新した。

| kernel | shape | pads |
|---|---:|---:|
| `h_kernel_i8_c0` | `1x1x1x12 -> 1x1x1x4` | `[0,1,0,10] -> [0,1,0,2]` |
| `h_kernel_i8_c1` | `1x1x1x12 -> 1x1x1x6` | `[0,1,0,10] -> [0,1,0,4]` |
| `h_kernel_i8_c2` | `1x1x1x12 -> 1x1x1x8` | `[0,1,0,10] -> [0,1,0,6]` |
| `h_kernel_i8_c3` | `1x1x1x12 -> 1x1x1x10` | `[0,1,0,10] -> [0,1,0,8]` |

## 足切り
`src.evaluate.scorer.audit_one`:

```text
baseline: params=89, memory=24300, cost=24389, n_fail=0
probe:    params=69, memory=24300, cost=24369, n_fail=0
```

相対削減率は `24389 / 24369 = 1.00082`。局所的には完全一致かつ cost 減。

## 実 submit
- ref: `54211879`
- status: `COMPLETE`
- Public Score: `7181.28 -> 7181.28`

## 採否
不採用。

ローカル cost は 20 減ったが、実 LB の表示スコアは上がらなかった。ルール通り `task349.onnx` と `data/output/onnx.dvc` は baseline に戻した。

## 追加メモ
case246 の候補探索中に `task224` の `row_kernel` / `col_kernel` を `Slice + ReduceSum` に置換する案も `/tmp` で確認した。

```text
task224 baseline: params=371, memory=2460, cost=2831, n_fail=0
task224 probe:    params=75,  memory=6300, cost=6375, n_fail=0
```

正答だが Slice 中間が増えて大幅悪化するため不採用、提出なし。

## 次の一手
params-only の 20B 級削減は Public 表示に出にくい。次は small absolute ではなく、`old_cost / new_cost` が少なくとも 1.002 以上になりやすい候補、特に memory 中間を消せる中 cost task を優先する。
