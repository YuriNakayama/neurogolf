# 20260702 case368 plan - task158 branch-surface probe

## Context

Accepted Public Score remains `7185.78`. Recent skip cycles showed that
flood-fill/reach depth trimming is mostly exhausted: removed frontier steps are
exactly needed for correctness.

This cycle targets selector/branch surface instead of propagation depth.

## Primary Target

Task158 current local cost:

```text
params=2613
memory=27574
cost=30187
```

Task158 has already accepted two narrow alias reductions:

- case346 diagonal slice alias;
- case348 `tr_ne -> tl_ne` plus `fill6 -> fill7`.

The remaining cost is still high enough that a branch-level alias or dead
selector removal may clear the medium-risk submit threshold if it is exact.

## Side Target

Task319 has a known high-gain predicate near miss:

```text
src2_possible -> p20_pair
19569 -> 18173
gain ~= 0.074010
n_fail=14
```

Recheck only as a side probe. It is useful if a cheap exact repair exists, but
do not spend a submit on another broad hidden-sensitive rewrite.

## Gate

Submit only if a touched candidate has:

- `n_fail=0`;
- no forbidden ops and no functions;
- cost below current baseline;
- risk-adjusted gain above threshold:
  - low-risk standalone `>=0.020`;
  - medium-risk standalone `>=0.030`;
  - high-risk standalone `>=0.080`.

Otherwise record the findings, bank exact micros if any, and keep
`data/output/onnx` unchanged.
