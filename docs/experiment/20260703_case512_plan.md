# 20260703 case512 plan - fresh non-risk structural search

Current accepted Public Score:

```text
7187.23
```

## Background

Case508 adopted a clean micro bundle. Case509 and case510 then confirmed that
local exactness alone is not enough:

```text
case509 task175 guarded index repair  -> Public 7187.23 -> 7169.38
case510 task173 grid-score dtype cut  -> Kaggle ERROR
```

Both task families are now in the review-risk gate. The next cycle should avoid
index/table/value-path dtype rewrites unless a new isolated repair plan explains
the runtime and hidden-safety risk.

## Hypothesis

The best expected value is a fresh standalone candidate with:

```text
n_fail = 0
gain >= 0.020
not in hard/review/runtime risk sets
mechanically explainable as low-risk cleanup or structural simplification
```

If no standalone exists, only submit a micro bundle if it passes the hardened
`--allow-micro-bundle-gate` lane with no review-risk members.

## Parallel Search

Workers launched:

```text
AL tasks 001-080
AM tasks 081-160
AN tasks 161-240
AO tasks 241-320
AP tasks 321-400
AQ clean banked micro-bundle assembly
```

All workers are restricted to `/tmp/neurogolf_parallel_*`, with no Kaggle
submit, no commit, and no edits to `data/output/onnx`.

## Gate

Submit only if the current-baseline gate returns:

```text
standalone: decision submit-candidate
bundle    : decision submit-micro-bundle or submit-bundle
```

Do not force a submit merely to satisfy cadence. Cadence is treated as a
readiness check, not a Kaggle-spend requirement.
