# 20260701 case184 plan

## Context

- Current accepted Public Score: 7181.18
- case183 succeeded by replacing a boolean-mask `Where` with arithmetic on a
  uint8 0/1 mask.
- A scan over current ONNX graphs found similar `Where(Cast(mask), value, zero)`
  patterns.

## Target

- Task: `task243`
- Baseline audit:
  - params: 42
  - memory: 22608
  - cost: 22650
  - points: 14.97208486908882
  - n_pass: 265
  - n_fail: 0
  - filesize: 5198

## Hypothesis

`task243` has many fill-chain nodes of the form:

```text
fill_i = Where(Cast(ch0), fc_i, zero)
```

If `ch0` is already a uint8 0/1 mask and each `fc_i` is uint8, this is
equivalent to:

```text
fill_i = Mul(ch0, fc_i)
```

Replacing all such `Where` nodes should remove the shared bool cast of `ch0`.
The fill outputs remain, so the expected gain is small but exact:

- one removed 18x18 bool intermediate: about 324 cost

## Method

1. Build a temporary `task243` variant replacing the matching fill `Where` nodes
   with `Mul(ch0, fc_i)`.
2. Prune the unused bool cast.
3. Run `audit_one` only for `task243`.
4. Apply and submit only if `n_fail=0` and cost decreases.
