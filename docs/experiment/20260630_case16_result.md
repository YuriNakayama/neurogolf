# 20260630 case16 result — param削減はSlice中間がparam節約を上回り cost増、REJECT（提出なし）

## 結論（提出なし、floor 7180.62 維持）
- t171 の Conv は **nz_in=[0] のみ**（810 params が dead）。入力を ch0 に Slice → Conv[10,1,3,3]
  =90 params に削減 → faithful n_fail=0/54 だが **cost 3703 > floor 910**。
- 原因: Slice 中間テンソル [1,1,30,30]=3600B が、節約した 810 params を上回る。floor の single-Conv は
  入力を直接読み中間ゼロ＝memory ~10 で 900 params のまま 910 が最安。
- t232（1×14, 1400 params）は dense（全 in/out/kw 使用）で削減余地なし。

## 確定（hand-golf 完全棄却の最終証明）
- **single-Conv の param 削減も memory metric で不可**: 入力チャネル削減は Slice 中間を生み、それが
  param 節約を上回る。floor の「全 10ch 入力を直接読む単一 Conv」が構造的に最安。
- case15(明示構成) + case16(param削減) で、**hand-construction が floor を破れないことを 2 方向から
  根本証明**。floor の単一 Conv/Gather/bit-pack は memory metric 下の真の最小。

## 本日（20260630）最終確定
- 純増 **+0.07**（harvest×2: 7176.49→7180.62）。
- **hand-golf 全棄却**（case5/9/10/11/13/14/15/16, 実 LB or memory metric で根本証明）。
- **harvest が唯一の前進レバー**。新 revision 毎 cross-bundle cherry-pick で per-task 最安を追う。
- 7950 まで −769.38。

## cycle追記: lucifer(17:10)再走 harvest → 0win
- lucifer 17:10 再走（luc3, cycle1 t233 の元ソース）を harvest → **0 win**（floor 7180.62 に収束）。
- 全 5 アクティブバンドル（boristown/frank/lucifer/kokinn/nikita）の最新 revision を再確認、全て floor 以上。
  公開フロントは 7180.62 で安定収束。
