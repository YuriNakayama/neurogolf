# case199 plan: task145 area/source branch trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task145` は未着手で、baseline は `cost=13215` (`params=111`, `memory=13104`, `n_fail=0`)。
- 20x20 の uint8/bool/float16 tensor が多く、1 個の branch を削るだけでも相対削減が見込める。

## 対象

- `data/output/onnx/task145.onnx`
- channel 0 の 20x20 mask `z_bool` から左右上下の距離 source を作り、bbox area の max/min mask と inside mask で class code を合成する。
- 大きい候補は `area_for_min = Where(z_bool, area, max_area)` と、`left_src/right_src/up_src/down_src = Where(z_bool, zero, pos_*)`。

## 仮説

`area_for_min` の sentinel branch または方向 source の zeroing が実データ上過剰なら、20x20 tensor を削れる。`color0 = Where(z_bool, zero, two)` の算術化は `z_bool` が他用途で残るため逆効果になりやすく、今回は alias 削減だけを試す。

## 検証

- temp ONNX で `area_for_min -> area` / `max_area`、方向 source の `pos_*` alias、終端 class branch の alias を 1 点ずつ probe する。
- `src.evaluate.scorer.audit_one` で `task145` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
