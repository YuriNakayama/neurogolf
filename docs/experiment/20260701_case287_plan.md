# 20260701 case287 plan - task101 exact alias bundle

## Context

- Current accepted Public Score floor: `7182.12`.
- Current head: `8221fc4` (`case286 adopt t157 top merge aliases`).
- The scoring rule is logarithmic:
  `points = max(1, 25 - ln(max(1, params + memory_bytes)))`.
- Therefore this cycle continues to prioritize relative reduction and `n_fail=0`
  over absolute task cost.

## Large-candidate search

Before choosing this target, I rechecked the larger-ratio routes suggested by
the recent priority update:

- Historical redesign candidates `task202`, `task359`, `task002`, `task319`,
  `task364`, and `task396` have no credible remaining `>=1.05` local surgery.
  The only large-looking edits are already known to fail correctness or public
  scoring.
- High-cost heatmap candidates `task233`, `task366`, `task158`, `task133`, and
  `task054` expose a `3600B` float label tensor, but replacing it with a
  quantized label path would require a new full `input -> uint8` cast
  (`9000B`), so the apparent saving is not real.
- `task118` already has `input_u8`, but a temp rewrite of the support scoring
  path failed ONNX type constraints because `ReduceSum` does not accept uint8.

Given those blockers, this cycle uses the best currently verified exact
candidate rather than spending a submit on a known risky large prune.

## Target

- `data/output/onnx/task101.onnx`
- Baseline audit: `params=905`, `memory=16850`, `cost=17755`, `n_fail=0`.

## Hypothesis

Several later tensors in `task101` are exact aliases of earlier guard/target
tensors across train/test/arc-gen. Rewiring all consumers and pruning dead
producers removes 25 nodes while preserving static shapes and existing op
semantics.

Alias bundle:

- `row_off -> row_off1`
- `off11 -> near_ul_val`
- `copy1_cov_rs0 -> copy1_target_r`
- `copy1_cov_cs0 -> copy1_target_c`
- `copy2_cov_rs0 -> copy2_target_r`
- `copy2_cov_cs0 -> copy2_target_c`
- `near_ur -> near_ur_val`
- `near_ur_ib -> near_u1_rok`
- `near_u2 -> near_u2_val`
- `near_l2 -> near_l2_val`
- `near_u1 -> near_u1_val`
- `near_l1 -> near_l1_val`
- `copy1_target_active -> near_ur_cok`

Temp audit already reproduced:

```text
task101_alias_bundle.onnx
params=905
memory=16801
cost=17706
n_pass=266
n_fail=0
status=ok
```

Expected relative reduction: `17755 / 17706 = 1.00277`.

## Gate

1. Apply only `task101.onnx`.
2. Run `evaluate.audit_one` on `task101` only.
3. Submit and use Kaggle Public Score for adoption.
4. Adopt only if Public Score rises; otherwise restore ONNX through DVC and
   commit the rejection record.
