# 20260701 case309 plan - task374 zero columns to Pad

## Target

`task374.onnx`

## Motif

Reuse the successful zero `Concat` -> `Pad` motif from:

- case255 `task343`
- case256 `task112`

`task374` has two horizontal zero-column concats:

```text
left_pair_cells  = Concat(zero_col, h_pairs, axis=3)
right_pair_cells = Concat(h_pairs, zero_col, axis=3)
```

If `zero_col` is an all-zero bool tensor, these are equivalent to left/right
constant padding of `h_pairs` by one column.

## Gate

This is a small motif, so adopt only if:

- local `audit_one(task374)` has `n_fail=0`;
- cost decreases;
- Kaggle Public Score improves.

If Public Score is unchanged, restore the baseline.
