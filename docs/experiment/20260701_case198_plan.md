# case198 plan: task077 propagation depth trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task077` は未着手で、baseline は `cost=14834` (`params=74`, `memory=14760`, `n_fail=0`)。
- 20x21 の uint8/bool tensor が多数あり、1 段でも伝播を削れれば約 420 bytes の削減になる。

## 対象

- `data/output/onnx/task077.onnx`
- `x2b` mask 上で min/max range を `MaxPool` と `Where(..., zero)` で伝播し、`f2b` と `col_sup` から `inside` を作る。
- 最後に `Xor(x2b, inside)` を Pad し、color 4 を overlay する。

## 仮説

後段の `f2` 判定または `b2` 伝播が過剰なら、`inside` を `f2b` ではなく `f1b`、または `f1` 系を `b2`/`b1` 系へ alias できる。各 alias は 20x21 tensor を複数 prune でき、相対削減率が見込める。

## 検証

- temp ONNX で `f2b -> f1b`、`f2mn_p/f2mx_p -> f1mn/f1mx`、`f1mn/f1mx -> b2mn/b2mx` などを 1 点ずつ probe する。
- `src.evaluate.scorer.audit_one` で `task077` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
