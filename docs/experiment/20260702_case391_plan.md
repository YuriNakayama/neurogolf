# 20260702 case391 plan - case390 recovery without task037

## Context

Accepted Public Score remains `7186.15`. Case390 submitted an 8-task low-risk
micro batch, but Kaggle returned `SubmissionStatus.ERROR` with no Public Score.

The local audit warning was only observed for `task037`:

```text
Removing initializer 'safe_name_18'. It is not used by any node
```

This is not proof that `task037` caused the Kaggle error, but it is the most
actionable suspect from local output.

## Initial Target

Retry a smaller recovery batch that excludes `task037`:

```text
task035 2262 -> 2255 gain 0.003099
task046 2318 -> 2286 gain 0.013901
task293 1288 -> 1286 gain 0.001554
task308 1488 -> 1484 gain 0.002692
task342 1585 -> 1576 gain 0.005694
task392 1951 -> 1941 gain 0.005139
task397 2848 -> 2845 gain 0.001054
```

Expected combined gain:

```text
0.033133
```

This still clears the low-risk batch gate `>= 0.030`.

## Adjustment

Re-audit showed the ORT unused-initializer warning comes from `task046`, not
`task037`. Excluding both `task037` and `task046` drops the actionable current
gain below the low-risk submit gate unless stale/already-adopted candidates are
counted incorrectly.

## Method

Apply candidates only in the working tree, re-run `audit_one`, and submit only
if the final current-relative gain still clears the gate after excluding warning
or stale candidates.

## Gate

Submit only if:

- all seven tasks have `n_fail=0`
- no forbidden ops and no ONNX functions
- no local warning analogous to the task037 unused-initializer warning appears
- submit cadence is acceptable relative to case390

Adopt only if Kaggle returns `COMPLETE` and Public Score improves over
`7186.15`.
