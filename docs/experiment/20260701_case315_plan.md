# 20260701 case315 plan - task174 inside-mask alias

## Context

- Current accepted Public Score floor: `7183.22` after case314.
- Candidate source: case313 explorer scan, recorded in
  `docs/experiment/micro_backlog.md`.
- Priority rule: choose exact local candidates by `old_cost / new_cost`; keep
  `n_fail=0` as the hard gate.

## Target

- `data/output/onnx/task174.onnx`

## Baseline

Current local audit:

```text
params=178
memory=9148
cost=9326
n_pass=266
n_fail=0
status=ok
```

## Hypothesis

The graph computes a mismatch mask and then gates it by the dynamic valid crop:

```text
Not(match_41) -> mismatch_42
And(mismatch_42, inside_40) -> mismatch_inside_43
Cast(mismatch_inside_43) -> mismatch_i32_44
```

The explorer audit found `mismatch_42 == mismatch_inside_43` on all valid
examples, with no true mismatch outside `inside_40`. Rewiring:

```text
mismatch_inside_43 -> mismatch_42
```

at the `Cast` consumer should prune the inside-mask construction:

```text
height_15 -> Unsqueeze -> row_inside_37
width_17  -> Unsqueeze -> col_inside_39
And(row_inside_37, col_inside_39) -> inside_40
And(mismatch_42, inside_40) -> mismatch_inside_43
```

`height_15` and `width_17` remain live for later output shape selection, so only
the mask chain should disappear.

## Expected Gain

Temporary audit result:

```text
params=178
memory=8800
cost=8978
n_pass=266
n_fail=0
status=ok
```

Expected point gain:

```text
9326 / 8978 = 1.0388
ln(old/new) ~= 0.0380 pt
```

## Verification Plan

1. Apply only `mismatch_inside_43 -> mismatch_42`.
2. Dead-prune unused nodes, initializers, and value_info.
3. Run individual `evaluate.audit_one` for `task174`.
4. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
