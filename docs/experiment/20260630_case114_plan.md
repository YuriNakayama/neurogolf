# 2026-06-30 case114 plan

## 現状

- floor: Public Score 7180.97
- 優先基準は case113 から相対削減率重視へ変更済み。
- case113 `task074`: 10〜30% 削減候補は全て `n_fail>0` で不採用。

## 対象

- `task196.onnx`
- baseline: params 38, memory 4500, cost 4538, n_pass 266, n_fail 0
- graph は 5 段の `MaxPool -> Min(blue)` 伝播鎖で、最後に `Less(poolL, blue)` を mask として `Where` する。

```text
bad -> pool0 -> bad1 -> pool1 -> bad2 -> pool2 -> bad3 -> pool3 -> bad4 -> poolL -> green
```

## 仮説

`task196` は cost 4538 の中 cost タスクで、各 30x30 近い中間が支配的。最後の伝播段や一部 `Min` が冗長なら、1 段削除で 10% 以上の相対削減が見込める。

## 手法

1. `green = Less(poolL, blue)` の入力を `bad4` / `pool3` / `bad3` へ差し替えて、最後の段を短縮する。
2. 中間 `Min(pool, blue)` の bypass を 1 点ずつ試す。
3. 各候補を prune し、`src.evaluate.scorer.audit_one` で `task196` の完全一致を確認する。
4. `n_fail=0` かつ cost が下がる候補のみ submit する。

## 足切り

- 変更対象は `task196.onnx` のみ。
- 禁止 op は追加しない。
- 実 Public Score が上がれば採用、上がらなければ破棄する。
