# 20260702 case499 plan - task031 squeeze cleanup

## Context

Current accepted Public Score:

```text
7187.10
```

The previous no-submit gap was caused by cases495-498 failing to find any
`n_fail=0` candidate. To recover cadence, workers now scan disjoint task bands
while the main process owns submit gating.

## Target

```text
task031.onnx
```

Candidate:

```text
/private/tmp/neurogolf_case401_unary_001_200/task031_27_Squeeze_in0.onnx
```

Current-baseline gate:

```text
1472 -> 1430
gain = 0.028947576034868503
n_fail = 0
functions = 0
forbidden_ops = []
decision = submit-candidate
```

## Hypothesis

The candidate removes a redundant squeeze/input path in a small graph. The gain
is above the standalone submit threshold and the task is not listed in the
current hard-reject families.

## Method

1. Save the current `task031.onnx` baseline outside `data/output`.
2. Copy the candidate into `data/output/onnx/task031.onnx`.
3. Run single-task submit gate and dry-run bundle gate.
4. Submit if the dry-run reports exactly one changed task and no local failure.
5. Adopt only if Public Score improves above `7187.10`; otherwise restore the
   baseline and record the rejection.
