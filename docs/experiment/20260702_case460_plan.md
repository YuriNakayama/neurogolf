# 20260702 case460 plan - task025 current alias re-gate

## Context

Case459 rejected the strongest known near-miss repair targets
(`task077`, `task110`, `task187`). A fresh normal-risk ranking still shows
`task025` as a memory-heavy unclosed task:

```text
task025 cost=11664 params=144 memory=11520
```

Prior failed attempts:

- case253: `TopK` slot `4 -> 3` failed with `n_fail=35`;
- case374: final feature dtype probe was skipped/rejected.

However, there are many current scratch aliases around line position, line
color counts, active-color tensors, and final `Einsum` descriptors. Re-gate
them against the current accepted baseline before attempting a manual rewrite.

## Target

```text
task025
baseline cost=11664
```

## Method

1. Gather existing `task025*.onnx` candidates from shallow `/private/tmp` and
   `/tmp` scratch pools.
2. Run cost-only screening against current `data/output/onnx/task025.onnx`.
3. Audit positive reductions for task025 correctness.
4. Submit only if an isolated candidate reaches:

```text
n_fail=0
gain >= 0.020
no forbidden ops/functions
single task only
```

5. Bank exact low-gain material; reject all local-fail semantic aliases.
