# 20260703 case520 plan - standalone search after task243 block

## Context

Current accepted Kaggle score:

```text
Public Score: 7187.37
latest accepted ref=54271014
```

Recent cycles:

```text
case517: adopted task238 2355 -> 2185, Public +0.08
case518: no-submit parallel sweep
case519: task243 repair blocked; exact repairs are not cost-reducing
```

## Priority Rule

The score delta is:

```text
ln(old_cost / new_cost)
```

So this cycle should not chase absolute cost alone. It should promote only
standalone candidates with meaningful relative reduction and `n_fail=0`.

## Hypothesis

task243's high-upside near miss is blocked by an expensive final flood-fill
repair. The next best use of submit budget is to search outside task243 for a
fresh standalone candidate, using current-baseline alias/prune scans and stale
scratch re-gates.

## Targets

Avoid known-risk tasks and recent blocked targets. Focus on non-risk tasks with
medium/high cost or prior near misses:

```text
task066, task080, task118, task133, task145, task158, task191, task198,
task219, task255, task328, task349, task364, task367
```

## Submit Policy

Submit immediately only if:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
forbidden ops absent
functions absent
```

Bank lower-gain exact candidates, but do not bundle them after case514's
Kaggle `ERROR`.
