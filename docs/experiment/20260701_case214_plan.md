# case214 plan: task249 active index trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task249` baseline は `cost=287` (`params=39`, `memory=248`, `n_fail=0`)。
- graph は `Slice -> ReduceSum -> Cast -> Gather(active_table) -> Pad -> Gather(input)`。

## 対象

- `data/output/onnx/task249.onnx`

## 仮説

`task249` は flag 由来の `row_i32` で `active_table_i32` の 3 パターンから gather row index を選び、`Pad` で 30 要素へ広げて input row を gather している。小コスト task だが、`row_i32` または `active_idx_i32` を固定できるなら `287 -> 150` 前後まで下がる可能性があり、相対削減率が大きい。

## 検証

- temp ONNX で `row_i32 -> 0/1/2`、`active_idx_i32 -> active_table_i32[row]`、`dup_idx_i32 -> padded active index` を probe する。
- 追加で `five_i32` の dtype/shape 変更や `idx_pads` の置換が cost を下げるか確認する。
- `src.evaluate.scorer.audit_one` で `task249` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
