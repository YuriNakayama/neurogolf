# case240 result: task117 local mask horizontal crop

## 結果
- 対象: `data/output/onnx/task117.onnx`
- baseline: `cost=4165` (`params=243`, `memory=3922`, `n_pass=265`, `n_fail=0`)
- 変更後: `cost=4158` (`params=223`, `memory=3935`, `n_pass=265`, `n_fail=0`)
- local gain: `ln(4165 / 4158) = 0.00168`
- Kaggle submit: `54211339`
- Public Score: `7181.24 -> 7181.25`
- 採否: 採用

## 変更内容
- `body_local_mask` を 13x13 から左右1列を落とした 13x11 に crop。
- `fg_local` 後に `Slice` を追加し、`leg_local` 以降の local condition を 13x11 で処理。
- 最終 `condition` の左右 Pad を `cx-4` / `23-cx` に補正して出力 30x30 を維持。
- 固定 shape の `value_info` を追加し、公式 scorer の memory 計算で `?` shape が出ないようにした。

## 検証
`src.evaluate.scorer.audit_one`:

| 候補 | cost | params | memory | n_pass | n_fail | 判定 |
|---|---:|---:|---:|---:|---:|---|
| baseline | 4165 | 243 | 3922 | 265 | 0 | ok |
| 11x11 crop + restore | - | - | - | 264 | 1 | reject |
| horizontal 13x11 crop + restore | 4333 | 229 | 4104 | 265 | 0 | reject: cost worse |
| horizontal 13x11 direct pad | 4158 | 223 | 3935 | 265 | 0 | adopt |

## 結論
左右端1列は local condition 生成では不要で、restore Pad を挟まず最終 Pad の左右オフセットだけ補正すれば完全一致を維持できる。改善幅は小さいが、実 LB で `+0.01` 表示改善が確認できたため採用する。

## 次の一手
同種の local mask / condition window を持つ中コスト task で、restore なしに最終 Pad 補正だけで縮小できる候補を優先する。特に一方向だけの crop は追加 tensor が少なく、比率改善が出やすい。
