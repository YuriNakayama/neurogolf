# case203 plan: task231 gather index branch trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task231` は未着手で、baseline は `cost=219` (`params=39`, `memory=180`, `n_fail=0`)。
- 8 要素の `mid_idx` branch や `Concat` 入力を削れれば、小型 task として相対改善が見込める。

## 対象

- `data/output/onnx/task231.onnx`
- `row0_ch0` の `ReduceSum` から `width_minus6` を作り、`active_mid = Less(mid_floor, width_minus6)` で middle index を選ぶ。
- `head_idx`, `mid_idx`, `tail_idx` を concat して `Gather(input, gather_idx)` を行う。

## 仮説

実データ上で middle 部分が常に active または always zero なら、`Where(active_mid, phase_mid, zero_col)` を固定できる。さらに head/tail が固定的なら concat 入力を短縮できる可能性がある。

## 検証

- temp ONNX で `mid_idx -> phase_mid/zero_col/fixed vector`、`gather_idx` 周辺の alias を probe する。
- `src.evaluate.scorer.audit_one` で `task231` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
