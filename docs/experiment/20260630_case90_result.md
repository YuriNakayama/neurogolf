# 20260630 case90 result — t209 の scale/crop 削減は faithful 不一致または cost 悪化（提出なし）

## 対象

- `task209.onnx`
- baseline: params **185**, memory **31867**, cost **32052**, faithful `n_fail=0`
- Public Score floor: **7180.87**

## 実施内容

`task209` は 4 の bbox 内テンプレートと bbox 外 key を対応させ、`S = s4 ? 4 : s3 ? 3 : 2` で scale を選ぶ構造。memory 上位は最終 one-hot `Equal`、初期 `Conv(float)`、後段 index `Add` で、uniform branch は 400 byte 級 tensor の積み上げだった。

### scale branch 削減 probe

| 候補 | cost | 結果 |
|---|---:|---|
| `S -> s_pre`（s4 判定削除） | 29240 | `n_fail=33` |
| `S -> THREE_I` | 21236 | `n_fail=104` |
| `S -> TWO_I` | 21236 | `n_fail=195` |
| `S -> FOUR_I` | 21236 | `n_fail=233` |
| `s_pre -> THREE_I` | 29240 | `n_fail=71` |
| `s_pre -> TWO_I` | 29240 | `n_fail=162` |

全候補で train/test/arc-gen のいずれかが壊れる。s=2/3/4 は全て必要。

### crop/label 生成 probe

- 入力 one-hot を先に 20x20 slice してから `Conv` する案:
  - faithful `n_fail=0`
  - ただし `[1,10,20,20]` float crop が追加され、cost **45152** に悪化
  - baseline の `Conv 30x30 -> Cast -> Slice` の方が memory metric 上安い

### final output probe

- 最終 `Equal` を省いて label grid を直接 output:
  - params は 10 減るが one-hot 形状ではなくなるため `n_fail=266`
  - 採点側では `[1,10,30,30]` one-hot bool 出力が必要

### 機械的 prune

- node: 179 → 179
- initializer: 34 → 34
- value_info: 0 → 0
- duplicate/unused initializer なし
- faithful cost 変化なし

## 採否

- **不採用、提出なし**。
- 正答性を保って cost を下げる候補は見つからなかった。
- `data/output/onnx/task209.onnx` は未変更。

## 結論

t209 は scale 判定 branch が全て実データで必要で、最大 tensor の final one-hot と label 生成も現 metric ではこれ以上単純化できなかった。次は cost 上位の未再検証タスク（例: t054/t285/t367/t349 以降）へ移る。
