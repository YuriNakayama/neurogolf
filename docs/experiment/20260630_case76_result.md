# 20260630 case76 result — 全400 graph 局所冗長の再監査、追加採用なし（提出なし）

## independent_impl:
case71/74 の t349 成功を受け、全400タスクへ機械的局所冗長探索を横展開した。

## 検証内容
- `onnxoptimizer` 各 pass 単独適用:
  - 43 pass × 400 task を probe。
  - faithful かつ cost 減は t066 `eliminate_duplicate_initializer` のみ（cost 18386→18380）。
  - t066 は case75 で実 submit 済みだが Public 7180.85 から変化なしのため不採用。
- `Slice axes` 省略:
  - 12候補（t064/t076/t157/t198/t237/t281/t363）を probe。
  - 全て n_fail=0 だが cost 差 0。initializer 共有等で公式 cost は下がらず。
- dead-end node / unused initializer / unused Constant:
  - 0件。
- duplicate node:
  - 0件。
- duplicate initializer:
  - t066 の shape initializer 3本のみ。case75 と同一で追加採用なし。

## 確定
- case71/74 の t349 は例外的に graph surgery の取り残しがあった。
- 現行 bundle では、機械的な optimizer / dead-code / Slice axes / duplicate-node 系の安全 win は枯れている。
- floor 7180.85 維持。提出なし。

## 次の一手
- 機械的局所冗長から、タスク固有の memory-dominant graph へ戻る。
- 高 cost かつ memory 支配の t285/t319/t338/t173/t080 などを対象に、`Where` 値入力・mask 合成・TopK/Gather 周辺の中間削減を個別精査する。
