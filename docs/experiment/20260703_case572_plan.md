# 20260703 case572 plan - task243 scalar scatter repair

Accepted Public Score:

- `7187.66`

## Background

The task243 one-fail candidate from the fresh target ranking was repaired during
the case571 submit wait.

Original candidate:

```text
task243: 22650 -> 22002
gain:    0.029026
n_fail:  1
```

Best repaired candidate:

```text
path:    /private/tmp/neurogolf_case571_task243/candidates/task243_fill26_scalar_scatter_reduction_add.onnx
task243: 22650 -> 22337
gain:    0.013915
n_fail:  0
```

## Hypothesis

The original `fill27 -> fill26` reduction removed one required final propagation
cell at `(row=0, col=10)` in `arc-gen[239]`. A scalar scatter repair restores
that single missing cell while keeping more than half of the relative gain.

The candidate is below the normal standalone threshold but outside the current
known-risk sets and passes local correctness, so an isolated review submit is
reasonable for the requested higher cadence.

## Method

1. Re-gate the candidate against the current accepted baseline after case571.
2. Build a full 400-task bundle replacing only `task243`.
3. Run full dry-run with review gate enabled.
4. Submit if dry-run passes; adopt only if Public Score improves over `7187.66`.
