# 20260702 case347 result - task233 scorew uint8 compression rejected

## Result

- Previous accepted Public Score: `7185.37`
- Kaggle submission ref: `54238678`
- Submission status: `SubmissionStatus.ERROR`
- Public Score: none
- Submission time: `2026-07-01 16:40:38 UTC` (`2026-07-02 01:40:38 JST`)

Rejected.

The ONNX bundle was restored to the accepted DVC baseline. Re-audit of restored
`task233`:

```text
params=520, memory=48934, cost=49454
n_fail=0, status=ok
```

## Local Candidate

Submitted candidate:

```text
/tmp/neurogolf_case347_alt/task233_scorew_u8_top255_unique_tail1.onnx
```

Local gate:

```text
baseline:  params=520, memory=48934, cost=49454
candidate: params=520, memory=47284, cost=47804
n_fail=0, status=ok
gain = ln(49454 / 47804) ~= 0.0339
```

## Diagnosis

Kaggle returned `SubmissionStatus.ERROR`, not a lower Public Score. The most
likely issue is scorer/runtime compatibility with the uint8 `TopK` value path,
even though local `audit_one` accepted the model. Treat this `task233 scorew`
uint8 family as not submit-safe unless a later candidate is explicitly tested
for Kaggle validator compatibility.

The planned risk mitigation (`top255_unique_tail1`) reduced local tie hazards
relative to the original `floor255` mapping, but the Kaggle failure happened
before score attribution, so tie behavior is not the immediate adoption issue.

## Next Step

Return to medium-risk structural aliases that do not alter `TopK` input dtype.
Keep the 30-minute cadence and avoid resubmitting this uint8 scorew family.
