# 20260702 case352 plan - task118 active hash remap risk attack

## Context

- Current accepted Public Score: `7185.50`
- Last accepted change: case351 `task209 + task090 + task213` submit-saving
  batch
- Last submit time: `2026-07-01 18:39:36 UTC`
  (`2026-07-02 03:39:36 JST`)

The strongest current candidate above the standalone threshold is another
task118 lookup-table pruning variant. This is high risk because it can overfit
observed train/test/arc-gen examples, so it must be isolated.

## Target

`task118`

Current accepted audit after case350:

```text
params=3782, memory=34207, cost=37989
n_fail=0, status=ok
```

Candidate:

```text
/tmp/neurogolf_case352_search/task118_pos_active4_hash6_remap.onnx
```

Candidate audit:

```text
params=3029, memory=33547, cost=36576
n_fail=0, status=ok
gain = ln(37989 / 36576) ~= 0.0379
```

## Hypothesis

The accepted case350 keep-first variant showed that conservative task118 table
pruning can move Public LB. This candidate additionally keeps only four locally
active positive rows and remaps the hash lookup table to five locally active
hashes plus a dummy entry.

This is not a proof-grade semantic reduction. It is a deliberate isolated risk
attack to test whether the stronger local table pruning remains compatible with
the hidden suite.

## Risk Controls

- Submit only `task118`; do not bundle with micros.
- Do not include the weaker partial variants in the same submission.
- Adopt only if Kaggle Public Score improves over `7185.50`.
- If Public Score does not improve, restore the accepted DVC baseline and mark
  this stronger task118 family rejected/hidden-unsafe.

## Gate

Run targeted validation only:

```text
evaluate.audit_one(data/output/onnx/task118.onnx, task118.json, run_correctness=True)
```

Then run packaging dry-run and submit at the next cadence window
(`2026-07-02 04:10 JST` or later).
