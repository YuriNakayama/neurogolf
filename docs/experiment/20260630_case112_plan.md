# 2026-06-30 case112 plan

## 現状

- floor: Public Score 7180.97
- case111: `task118` の support 補正削除は local cost 17 減だが実 LB 不変で不採用。
- 全 ONNX を単純な値保存候補（`+0`, `*1`, 同値 `Where` など）で走査したところ、正しく削減できた候補は `task090` の同値 `Where` のみ。

## 対象

- `data/output/onnx/task090.onnx`
- baseline: params 82, memory 3009, cost 3091, n_pass 267, n_fail 0
- node 334:

```text
Where i1_best_row_start ['i1_area_better', 'zero_u8', 'zero_u8'] -> ['i1_best_row_start']
```

## 仮説

`Where(cond, zero_u8, zero_u8)` は条件に依存せず常に `zero_u8` と等価。後続入力を `zero_u8` に差し替えて該当 node を prune すれば、memory 1 を削減できる。

## 手法

1. `i1_best_row_start` の全利用を `zero_u8` に差し替える。
2. dead node / unused initializer / value_info を prune する。
3. `src.evaluate.scorer.audit_one` で `task090` の完全一致を確認する。
4. `n_fail=0` かつ cost 減少なら submit する。

## 足切り

- 変更対象は `task090.onnx` のみ。
- 禁止 op は追加しない。
- 実 Public Score が上がれば採用、上がらなければ破棄する。
