# 20260702 case426 plan - task054 index-family narrowing

## Context

Case425 rejected `task255` source-window narrowing: the dominant tensor is a
primary source slice with many required consumers. Case423 recorded `task054` as
the largest remaining memory-heavy candidate that is not on the hard-reject
list:

```text
task054 params=238 memory=26851 cost=27089 n_fail=0
```

The score model is logarithmic, so the target is a relative cost reduction.
For this cycle the submit threshold remains:

```text
gain = ln(old_cost / new_cost) >= 0.020
```

For `task054`, that means cost must fall below roughly `26552`, or by about
`537` cost units.

## Hypothesis

The largest `task054` value path (`label_f32` 3600B) is a primary `Conv(input)`
label map and is risky to replace directly. The safer target is the derived
index family:

```text
fill_indices_4_8_4 int64 [4,8,4]      1024B
v_indices_1_1_30_4 int64 [1,1,30,4]    960B
```

If either index family can be narrowed, aliased, or generated with smaller
static intermediates while preserving `GatherND`/`ScatterND` semantics, the
single-task gain could clear the submit threshold.

## Method

1. Inspect current graph consumers/producers for the two index families.
2. Prefer local rewrites that delete a complete int64 index tensor family.
3. Avoid broad value-path rewrites of `label_f32` unless the graph shows an
   isolated replacement.
4. Run `submit gate` for any candidate.

## Gate

Submit only if:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```

Otherwise record as no-submit or bank-only.
