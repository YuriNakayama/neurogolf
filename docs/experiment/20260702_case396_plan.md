# 20260702 case396 plan - fresh surgery scan

## Context

Accepted Public Score is `7186.24` after case394. Case395's no-`task046`
recovery batch still returned Kaggle `ERROR`, so the case390 recovery family is
not submit-safe as a bundle.

Recent backlog rows are increasingly stale because many small cleanups have
already been adopted. Re-audit current baselines before planning submits.

## Hypothesis

The generic graph-surgery passes may still find small exact reductions on the
current accepted bundle:

- cleanup / initializer dedup
- redundant Slice index input removal
- default Slice axes hole-punching
- index surgery
- broadcast-compress

These are lower Kaggle `ERROR` risk than dtype/runtime changes, but likely too
small individually. Bank every exact current-baseline win and submit only if the
combined fresh gain clears a practical threshold.

## Gate

Run task-local `audit_one` for each changed candidate. Submit only if:

```text
n_fail=0
no forbidden ops/functions
combined fresh gain >= 0.020
```

Otherwise, do not submit; record exact candidates in `micro_backlog.md`.
