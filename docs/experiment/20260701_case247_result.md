# 20260701 case247 result — task370 repeat candidate branch prune は完全一致せず不採用

## 対象
- `task370.onnx`
- 採否: 不採用
- submit: なし
- floor: Public Score `7181.28`

## baseline

```text
task370.onnx
params=1267
memory=9126
cost=10393
n_pass=266
n_fail=0
```

## 試行
`/tmp` の probe ONNX で d5/d4/d3 候補を個別に無効化した。各候補では `valid_d*` を false 定数に差し替え、不要になった score/repeat producer を削れる範囲で除いた。

```text
no_d5: cost=10392, n_pass=265, n_fail=1
no_d4: cost=10392, n_pass=136, n_fail=130
no_d3: cost=10392, n_pass=133, n_fail=133
```

## 結論
不採用。

`valid_d5` は fail 1 まで近いが、完全一致条件を満たさない。d4/d3 は多数失敗し、候補 repeat は実データ上必要だった。ONNX 本体は変更しない。

## 次の一手
`task370` の候補 branch prune は停止する。次は同じ memory 支配でも、候補全体を消すのではなく、出力形状を保ったまま中間 dtype や terminal mask を減らせる task を優先する。
