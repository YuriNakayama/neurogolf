# 20260630 case42 result — 重要技法発見「static crop→処理→pad」+ enclosed-fill タスクで検証（提出なし）

## independent_impl（必須欄・新技法の発見）
1. **enclosed-fill タスクを全400から特定**: t002(depth20, floor21089), **t251(depth7, floor4920)**。
2. **重要発見: floor net は [1,1,30,30] でなく [1,1,12,12] で処理している**。
   - floor t251 を shape-inference → 全中間が **[1,1,12,12]**（タスクの最大grid 12×12 に static Slice）。
   - 末尾で Pad で 30×30 に戻す。**memory=Σ中間 は実 grid サイズで効く** → 30×30 の私の net より
     (30²/12²)=6.25倍 有利だった。これが私の独自 net が常に高コストだった主因の一つ。
3. **技法を実装**: 入力を static Slice [1,10,12,12] → 12×12 で flood-fill → Pad 30×30。
   - cost 111624(30×30) → **34039(12×12)** に大幅減（6×弱）。技法は有効。
   - ただし t251 floor 4920 には未達（nfail もあり未完成）: 4連結 dilation を Conv十字+Clip+Mul の
     3op/iter で実装したため中間が floor(MaxPool 1op/iter, 単channel) より多い。
   - floor は 8 MaxPool・単channel・12×12 で near-optimal。

## 確定 + 前進
- **新技法「static crop→処理→pad」を獲得**。タスクの max grid extent に static Slice すれば
  中間 memory を実サイズに圧縮できる（静的形状で合法）。今後の独自 net 構築の標準手段とする。
- t251 では floor が既にこの技法 + MaxPool最小構成で 4920 に到達済みのため未勝利。
- だが **floor がこの crop 技法を使っていない高コストタスクがあれば、技法適用で勝てる可能性**。
  → 次サイクルで「floor が 30×30 中間を持つ高コストタスク」を shape-inference で洗い出す。

## 恒久方針の遵守
- 「枯渇」と言わず再探索を継続し、**新技法を発見**。停止提案なし。次サイクルで技法を活かす対象を探す。

## 次の一手
- 全400 floor net を shape-inference し、中間が 30×30(=未crop)のまま高コストなタスクを特定。
  そこに static-crop 技法 + 独自構成を適用して floor 未満を狙う。
- floor 7180.78（DVC 6e8d4642）退行なし。
