# 20260702 case364 result - skip submit after broad search

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.78
```

## Checks

### task233 compatibility probe

The known locally exact candidate remains:

```text
task233: 49454 -> 47804
gain ~= 0.03393
n_fail=0
path: /tmp/neurogolf_case347_alt/task233_scorew_u8_top255_unique_tail1.onnx
```

But it changes the second `TopK` value path to `uint8` and already produced
Kaggle `SubmissionStatus.ERROR` in case347.

Tried a platform-safety repair that keeps the compressed `uint8` table but casts
`mul103` back to `float16` immediately before `TopK`:

```text
path: /tmp/neurogolf_case364_task233_compat/task233_u8_table_cast_before_topk.onnx
cost: 51074
n_fail=0
```

The repair is exact but worse than the current baseline (`49454 -> 51074`), so
`task233` remains deferred.

### solver bank

Ran the newly merged solver bank:

```text
uv --project backend run python -m pipeline.case3 solve \
  --task-dir ../data/lake/neurogolf-2026 \
  --out /tmp/neurogolf_case364_solverbank
```

It solved 52 tasks, but no output was cheaper than the current accepted ONNX
for the corresponding task.

### high-cost single-alias scan

Ran a same-shape/same-dtype single-alias scan over the top current-cost tasks:

```text
/tmp/neurogolf_case364_single_alias_scan
tested: 377 candidates
exact cost-reducing candidates: 0
```

Promising near misses were still not repairable within this cycle:

| task | candidate | cost | gain | n_fail | decision |
|---:|---|---:|---:|---:|---|
| 077 | `b2mn -> b1mn` | `14834 -> 13994` | `0.058293` | 4 | incorrect; sparse color-4 cells require the second propagation |
| 110 | `public_row_table -> row9_public_table` | `13591 -> 13069` | `0.039165` | 1 | incorrect on train example; table selection changes the whole periodic output |
| 187 | `reach_pool_* -> reach_*` | `27533 -> 26908` | `0.022962` | 4 | below medium threshold and incorrect |

Parallel search found:

```text
task002 greedy bit-neighbor prune: 20689 -> 19489
gain ~= 0.059752
n_fail=0
```

This is the same local-positive family that regressed Public Score in case97
(`7180.89 -> 7165.84`), so it is explicitly not submit-worthy.

## Conclusion

Skip the submit window. The only above-threshold exact candidates are known
platform-risk (`task233`) or known Public-regressing (`task002`) families.
Continue searching for a fresh structural current-baseline candidate with
`gain >= 0.030`, or accumulate another exact low/medium-risk batch before the
next submit.
