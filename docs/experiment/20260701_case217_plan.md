# case217 plan: task104 orientation branch trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task104` baseline は `cost=254` (`params=57`, `memory=197`, `n_fail=0`)。
- graph は `GatherND -> MatMul -> Cast -> Gather(pattern_bank) -> Resize -> Gather(pad9_bank) -> Pad -> ConvInteger`。

## 対象

- `data/output/onnx/task104.onnx`

## 仮説

orientation が実データ上で固定、または pattern/pad の片方だけが固定できれば、`GatherND/MatMul/Cast/Gather` の一部を削れる。`cost=254` のため、数十 byte の中間削減でも相対削減率が大きい。

## 検証

- temp ONNX で `orientation -> 0/1/2/3` を probe する。
- `pattern -> pattern_bank[orientation]`、`pads9 -> pad9_bank[orientation]`、両方固定を probe する。
- `src.evaluate.scorer.audit_one` で `task104` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
