# case231 plan: task372 sparse conv weight trim

## 現状
- 採用済み Public Score floor: 7181.21
- `task372` baseline は `cost=710` (`params=710`, `memory=0`, `n_fail=0`)。
- graph は単一 `Conv(input, W, B) -> output`。
- `W` は shape `(10,10,7,1)` の 0/1 float32 で、`B` は 10 要素。

## 対象
- `data/output/onnx/task372.onnx`

## 仮説
memory が 0 なので、中間 tensor を追加する再構成は悪化しやすい。Conv のまま bias 削除、出力 channel の重複/固定化、kernel height の短縮が通れば params を直接削れる。

## 検証
- bias 削除または zero 化を probe する。
- output channel ごとの zero/duplicate 化を probe し、不要 channel があるか確認する。
- kernel height を 7 から短縮できるか確認する。
- `src.evaluate.scorer.audit_one` で `task372` の全ペア完全一致と cost を確認し、`n_fail=0` かつ cost が下がった候補だけ実 ONNX へ反映する。
