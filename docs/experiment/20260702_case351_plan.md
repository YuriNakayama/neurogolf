# 20260702 case351 plan - task209 plus exact micro batch

## Context

- Current accepted Public Score: `7185.47`
- Last accepted change: case350 `task118` conservative positive patch
  keep-first table pruning
- Last submit time: `2026-07-01 18:10:10 UTC`
  (`2026-07-02 03:10:10 JST`)

The next safe standalone candidate is below the preferred `0.030` threshold:
`task209` has a structural alias bundle with gain about `0.0224`. To preserve
submit budget, combine it with two narrow exact micro candidates already
validated by the case351 search.

## Target Batch

| task | change | baseline cost | candidate cost | gain |
|---:|---|---:|---:|---:|
| 209 | `alignment_fits -> key_b` plus `kern_src_*_cl` bypasses | 28683 | 28047 | 0.022423 |
| 090 | 9 area/best-row aliases | 3091 | 3073 | 0.005840 |
| 213 | 5 `h_valid* -> h_valid_and01` aliases | 1887 | 1882 | 0.002653 |

Total expected local gain:

```text
0.0309165
```

## Candidate Files

```text
/tmp/neurogolf_case351_search/task209_alignment_keyb__kern_clip_bypass.onnx
/tmp/neurogolf_case351_search/task090_area_bestrow_9alias.onnx
/tmp/neurogolf_case351_search/task213_hvalid_5alias.onnx
```

## Risk

This batch intentionally avoids known rejected or hidden-sensitive families:

- task118 stronger table-pruning variants;
- task233 uint8 `TopK` scorew family;
- task286 broad bitwise equivalence bundle;
- task101 hidden-unsafe local alias bundle.

`task209` is medium-risk structural alias material, so keep the batch compact
and do not add optional extra micros unless the core batch fails to clear the
local gate.

## Gate

Run `evaluate.audit_one(..., run_correctness=True)` on exactly the three
changed tasks. If all are `n_fail=0` and scorable, run packaging dry-run and
submit at the next 30-minute cadence window (`2026-07-02 03:40 JST` or later).

Adopt only if Kaggle Public Score improves over `7185.47`; otherwise restore
the accepted DVC baseline and record rejection.
