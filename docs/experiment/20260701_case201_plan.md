# case201 plan: task067 tiny threshold mask trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task067` は未着手で、baseline は `cost=47` (`params=8`, `memory=39`, `n_fail=0`)。
- cost は非常に小さいが、1 tensor でも削れれば相対削減率は大きい。

## 対象

- `data/output/onnx/task067.onnx`
- `ReduceSum(input) -> Greater(s, t) -> Pad(m) -> Where(q, input, z)` で、channel/位置ごとの mask を作って入力を残す。

## 仮説

threshold mask の一部が実データ上で固定、または Pad 後 mask `q` を Pad 前 `m` 相当の別形状で代替できれば、`Pad` または `Where` 周辺を削れる。低 cost task なので、固定化候補も含めて短く確認する。

## 検証

- temp ONNX で `q` / `m` / `output` 周辺の alias と固定化を 1 点ずつ probe する。
- `src.evaluate.scorer.audit_one` で `task067` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
