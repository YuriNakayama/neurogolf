# 20260702 case406 plan - task335 cleanup plus banked filler bundle

## Context

Accepted Public Score is `7186.58` after case405 adopted the `task161` plus
`task076` alias bundle.

The next standalone candidate found by current-graph cleanup scan is a
low-risk duplicate-initializer cleanup on `task335`, but its relative gain is
below the practical submit gate:

```text
task335 3002 -> 2952 gain 0.016795826473
```

To keep the 15-minute submit cadence useful without spending a submit on a
likely display-floor no-op, bundle it with current-valid banked micros.

## Candidate Set

```text
task335 3002  -> 2952  gain 0.016795826473
task396 8078  -> 8074  gain 0.000495294710
task305 636   -> 635   gain 0.001573564447
task366 36385 -> 36304 gain 0.002228673762
```

Expected combined gain:

```text
0.021093359392510464
```

Scratch files:

```text
/tmp/neurogolf_case406_cleanup_scan/task335_cleanup.onnx
/tmp/neurogolf_case370_task396/terminal_plus_A1_B1_u8_iota.onnx
/tmp/neurogolf_case404_candidate_row_scan/task305_case404_suffix_prune.onnx
/tmp/neurogolf_nextcycle_task366_bundle/task366_bundle_10.onnx
```

## Method

- `task335`: replace duplicate initializer uses `col_sel -> row_sel` and
  `pool_rows -> pool_cols`, then prune unused duplicate tensors.
- `task396`: reuse terminal uint8 cleanup for label/scalar tables.
- `task305`: prune one unused suffix row from `k_masks_u64`.
- `task366`: apply the already banked 10-alias local-equivalence bundle around
  gap predicates and base color matches.

## Gate

Submit only if all changed tasks pass targeted `audit_one`:

```text
task335: n_fail=0, functions=0, forbidden ops=[]
task396: n_fail=0, functions=0, forbidden ops=[]
task305: n_fail=0, functions=0, forbidden ops=[]
task366: n_fail=0, functions=0, forbidden ops=[]
```

Risk: medium. `task335` and `task396` are low-risk structural cleanups, while
`task305` and `task366` are filler candidates with higher hidden sensitivity.
Adopt only if Public Score improves over `7186.58`.
