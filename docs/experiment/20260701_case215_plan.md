# case215 plan: task391 TopK selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task391` baseline は `cost=159` (`params=24`, `memory=135`, `n_fail=0`)。
- graph は `ReduceSum -> TopK(k=5) -> Cast -> Gather(pick=[2,3,4]) -> Equal -> Pad`。

## 対象

- `data/output/onnx/task391.onnx`

## 仮説

`task391` は TopK の 5 要素から rank 2,3,4 の 3 色を選ぶ。もし上位 3 色だけ、または固定 rank の別表現で正答できるなら、TopK 出力や `pick` の shape を縮めて `cost=159` から相対的に大きく削れる。

## 検証

- temp ONNX で `k=3` と `pick=[0,1,2]`、`k=4` と各 3-rank 選択、`k=5` の pick variants を probe する。
- `ids` の dtype や `Pad` 前 shape 置換で cost が下がるか確認する。
- `src.evaluate.scorer.audit_one` で `task391` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
