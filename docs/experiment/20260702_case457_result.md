# 20260702 case457 result - bounded current-scratch screen

## Summary

No submit.

The bounded screen found many positive cost reductions, but the high-gain
candidates failed local correctness. The only exact positives observed were
low-gain `task064` high-bound aliases that are already represented in the
micro backlog.

## Baselines

```text
task064 cost=12478 n_fail=0 status=ok
task076 cost=18683 n_fail=0 status=ok
task077 cost=14834 n_fail=0 status=ok
task110 cost=13591 n_fail=0 status=ok
task173 cost=15664 n_fail=0 status=ok
task219 cost=14838 n_fail=0 status=ok
```

## Screen

Candidate pool:

```text
targets=064,076,077,110,173,219
candidates=2012
positive_cost_reductions=514
cost_errors=0
zero_cost_skipped=4
```

The full exact audit was stopped after it became too slow, but the top 80
positive reductions were individually re-audited. All top 80 failed local
correctness.

Representative blocked high-gain candidates:

```text
task219 14838 -> 2277   gain=1.874333  n_fail=265
  task219_alias_colour15_TO_cyan_c.onnx

task077 14834 -> 3447   gain=1.459418  n_fail=266
  task077_029_Xor_fillb_TO_x2b.onnx

task173 15664 -> 4404   gain=1.268852  n_fail=266
  task173_alias_out_idx_small_TO_grid.onnx

task076 18683 -> 6395   gain=1.072098  n_fail=266
  task076_hidden_patch4d_TO_color15_h.onnx

task064 12478 -> 9730   gain=0.248753  n_fail=181..193
  task064_fill_mask24_TO_h_range/v_range.onnx

task110 13591 -> 11926  gain=0.130687  n_fail=264
  task110_selected_row_table_TO_public_row_table.onnx
```

This confirms the same pattern as the previous no-submit cycles: the large
relative reductions are semantic branch or value-path removals, not safe
cleanup.

## Exact Positives

Observed exact candidates:

```text
task064 12478 -> 12454  gain=0.001925237  n_fail=0
  /private/tmp/neurogolf_fresh_014_140_case382/task064_bypass_Add_v_high_p1_TO_v_high24.onnx

task064 12478 -> 12454  gain=0.001925237  n_fail=0
  /private/tmp/neurogolf_fresh_014_140_case382/task064_bypass_Add_h_high_p1_TO_h_high24.onnx
```

The same family is already banked in `docs/experiment/micro_backlog.md`:

```text
task064 bundled high-bound aliases, including h_high_p1 -> h_high24 /
v_high_p1 -> v_high24, 12478 -> 12430, gain=0.0039
```

No new submit-worthy candidate was found.

## Decision

Rejected for submission.

The exact `task064` material is far below the standalone threshold and already
tracked as a micro. The high-gain candidates all violate the hard
`n_fail=0` requirement, so no `submission.zip` was produced.

## Next

Move away from old broad scratch aliases for these tasks. The next cycle should
either:

- inspect a fresh current-baseline graph for low-risk duplicate/broadcast
  cleanup not already in `micro_backlog.md`; or
- deliberately hand-repair one small-failure near miss, but only if the failing
  examples identify a narrow exception rather than a full semantic branch.
