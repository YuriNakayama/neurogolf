# 20260703 case566 result - task379/task074 low-fail repair

Accepted Public Score remains: `7187.57`

## Current gates

Historical low-fail candidates still reproduce against the current baseline:

```text
task379
/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Where_color20_TO_color20_c_400.onnx
10753 -> 10353
gain=0.037908
n_fail=2

task379
/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Transpose_color20_t_TO_color20_c_400.onnx
10753 -> 10353
gain=0.037908
n_fail=2

task074
/private/tmp/neurogolf_agent_Y1/candidates/task074_alias_tR_TO_c0.onnx
9050 -> 8150
gain=0.104747
n_fail=15
```

## Local failure diff

### task379

The `task379` near miss fails only examples `1` and `3`, but the failures are
not tiny cell exceptions:

```text
example 1: candidate differs from expected in 214 one-hot cells, 80 argmax cells
example 3: candidate differs from expected in 336 one-hot cells, 134 argmax cells
```

The changed channels are mainly `0`, `2`, and `8`, indicating an orientation or
color20 branch collapse rather than a few missing cells.

The task379 worker confirmed the root cause. The two failing examples are the
vertical-separator cases:

```text
train[1]: 13x18, vertical separators at cols 3 and 14
test[0]: 17x19, vertical separators at cols 4 and 12
```

Best attempted candidates:

```text
task379_bypass_Where_color20_TO_color20_c_400.onnx:      10753 -> 10353, n_fail=2
task379_bypass_Transpose_color20_t_TO_color20_c_400.onnx:10753 -> 10353, n_fail=2
task379_case566_pad_color20_c_pruned.onnx:               10753 ->  9953, n_fail=2
task379_case566_restore_where.onnx:                      10753 -> 10753, n_fail=0, no gain
```

The `color20_c` candidates paint the horizontal/canonical expansion at
transposed coordinates. Restoring `color20 = Where(h_any, color20_c, color20_t)`
fixes correctness but returns to baseline cost.

### task074

The `task074` low-fail candidate fails 15 examples. Each failure drops a compact
colored object to background `0`; examples range from 1 to 22 argmax cells.
This is still a semantic symmetry-branch miss rather than an isolated cleanup.

The task074 worker confirmed no lower-cost exact repair:

```text
task074_alias_tR_TO_c0.onnx:          9050 ->  8150, n_fail=15
task074_alias_tT_TO_tR.onnx:          9050 ->  8150, n_fail=34
exact sparse repair of row failures:  9050 -> 10378, n_fail=0, worse
exact sparse replacement after tR:    9050 -> 10449, n_fail=0, worse
```

All 15 failures are arc-gen examples:

```text
26, 37, 41, 60, 78, 125, 136, 147, 173, 194, 226, 227, 244, 251, 254
```

The candidate removes `tR` from `Max(c0,tT,tR)`, so expected row-mirror cells
become background `0`. A logical sparse repair touches only 83 fixed grid
positions, but ONNX `GatherND`/`ScatterND` indices plus the repaired plane exceed
the saved 900-memory branch. No exact proper subset of the transform set
`I,T,R,C,CT,RC` was sufficient.

## Fallback task076

The fallback worker checked `task076`:

```text
current accepted: 18222, n_fail=0
near miss:        18095, gain_vs_current=0.006994, n_fail=4
slot repair:      18222, n_fail=0, identical to current
new repair:       18230, n_fail=0, worse than current
```

The remaining near-miss savings come from removing the slot-2 out-of-bounds
guard. Restoring correctness requires that guard, so no positive candidate was
found.

## Decision

No submit. All case566 repair lanes either remain locally incorrect or restore
the removed semantic branch and lose the cost advantage.

Next action: move away from task379/task074/task076 families. The next cycle
should inspect either `task364` despite higher fail count, or find a fresh target
outside the exhausted symmetry/orientation/slot-guard repair families.
