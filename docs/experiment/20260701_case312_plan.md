# 20260701 case312 plan - task080 separator mask alias

## Context

- Current Public Score floor: `7183.04`.
- Recent high-ratio candidates failed because they changed boundary behavior.
- This cycle returns to same-task structural aliases, but keeps the larger-ratio
  gate: prefer candidates that remove whole intermediate chains and reach at
  least `old_cost / new_cost >= 1.02`.

## Target

Target: `task080`.

Current local audit:

```text
params=111
memory=16623
cost=16734
n_pass=231
n_fail=0
status=ok
```

## Hypothesis

The current graph computes a separator mask:

```text
mult -> modr -> seprow / sepcolr -> sepmask
Where(sepmask, ..., ...) -> withsep
```

A shape-guided alias probe found that `sepmask` is equivalent to the existing
`lineOrOut` predicate for all local train/test/arc-gen examples. Rewiring only
the condition input keeps the terminal `Where` and output semantics intact while
pruning the separator-construction chain.

This is distinct from the earlier `task080` accepted changes:

- case275 bundled mask aliases;
- case300 removed a redundant corner guard.

## Temp Gate

Temporary ONNX result:

```text
sepmask -> lineOrOut
params=107
memory=15543
cost=15650
n_pass=231
n_fail=0
status=ok
```

Expected local gain:

```text
16734 -> 15650
old_cost / new_cost = 1.0693
ln(old/new) ~= 0.0670 pt
```

## Plan

1. Apply only `sepmask -> lineOrOut` to `data/output/onnx/task080.onnx`.
2. Prune dead nodes, stale value_info, and unused initializers.
3. Run `evaluate.audit_one` for `task080` only.
4. Submit if the individual gate remains `status=ok`, `n_fail=0`, and
   `cost=15650`.
5. Adopt only if Kaggle Public Score exceeds `7183.04`; otherwise restore the
   ONNX via DVC and record rejection.
