# 20260702 case348 plan - task158 tr-ne/fill alias

## Context

- Current accepted Public Score: `7185.37`
- Last accepted change: case346 `task158` diagonal slice alias
- Last submit: case347 `task233` scorew uint8 compression returned
  `SubmissionStatus.ERROR`, so avoid uint8 `TopK` value-path changes.

The scoring metric is logarithmic:

```text
points = max(1, 25 - ln(max(1, cost)))
cost = params + memory_bytes
gain = ln(old_cost / new_cost)
```

Submit cadence is about one submit per 30 minutes. A standalone low-risk
candidate should clear roughly `0.020` local gain; this candidate clears that
threshold and is narrower than the rejected broad/validator-sensitive families.

## Target

`task158`

Current accepted audit:

```text
params=2613, memory=28681, cost=31294
n_fail=0, status=ok
```

## Hypothesis

After case346, additional local equivalences remain in the same diagonal/fill
region. Replacing `tr_ne_bl` with `tl_ne_br` and `fill_u8_6` with `fill_u8_7`
should prune redundant intermediate tensors without changing output behavior.

This is preferred over broader task158 alias bundles because:

- The local gain is meaningful: `ln(31294 / 30187) ~= 0.0360`.
- The edit is limited to two equivalent tensor aliases.
- Parallel scans of current `task187`, `task319`, and selected high-cost tasks
  found no better normal-cadence candidate.

## Planned Method

Use the verified candidate:

```text
/tmp/neurogolf_task158_case348/task158_combo_D_tr_ne_TO_tl_ne__fill6_TO_fill7.onnx
```

Expected local audit:

```text
params=2613, memory=27574, cost=30187
n_fail=0, status=ok
gain = ln(31294 / 30187) ~= 0.0360
```

## Gate

Run only targeted validation:

```text
evaluate.audit_one(data/output/onnx/task158.onnx, task158.json, run_correctness=True)
```

If local correctness holds, submit at the next 30-minute cadence window. Adopt
only if Kaggle Public Score improves over `7185.37`; otherwise restore the DVC
baseline and record rejection.
