# 20260701 case181 plan

## Context

- Current accepted Public Score: 7181.17
- case180 showed `task346` ring/scatter logic has no exact trim.
- `task359`, `task383`, and `task396` were already covered in earlier cases, so
  choose an untried short memory-heavy graph.

## Target

- Task: `task398`
- Baseline audit:
  - params: 190
  - memory: 5417
  - cost: 5607
  - points: 16.367379545409126
  - n_pass: 266
  - n_fail: 0
  - filesize: 1916

## Hypothesis

`task398` builds a compact lookup table from the top row, computes an offset from
one selected cell, creates row/column keys, and gathers the final 30x30 table.

Potential exact reductions:

- The offset `z/offv` may be redundant or constant for public examples.
- `rin/cin` boundary masks may be replaceable by one side or by direct row/column
  coordinates.
- `table` may be replaceable by `top7` if the table-map indirection is unnecessary.

## Method

1. Probe one alias at a time on temporary `task398` ONNX copies.
2. Run `audit_one` only for `task398`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record failed aliases and keep baseline ONNX otherwise.
