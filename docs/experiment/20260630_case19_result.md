# 20260630 case19 result — fp16/dtype 削減は metric 上無効、floor 7180.62 維持（提出なし）

## 結論（提出なし）
- **calculate_params は element count（np.prod(dims)）で dtype 非依存**。fp16 化しても 900-element Conv は
  params=900 のまま。→ **params-dominated net（t032/t171/t232 等 28 タスク）の fp16 golf は不可能**。
  surgery fp16 が params 系で 0 win だった理由を確定。
- **t233（memory-dominated）peak テンソル**: bool[1,10,30,30]=9000（既に 1byte/elem, 縮小不可）が支配。
  float[5,324]=6480 等は fp16 化で半減し得るが、bool 9000 が peak を支配し、かつ surgery fp16 が
  audit 付きで既に 0 win（value-preserving fp16 が correctness 破壊 or peak 不変）。dtype 削減余地なし。

## 確定
- cost の両成分とも floor は最小: **params=element count で dtype 不変、memory は surgery で dtype 最適化済み**。
  fp16/dtype は metric 上 golf レバーにならない。case6(surgery) の 0 win を成分レベルで裏付け。
- hand-golf 全経路（明示構成/param削減/Conv圧縮/k-local fit/local-swap/surgery/dtype）を case1-19 で
  網羅棄却。**floor 7180.62 = 真の最小コスト**を成分レベルまで証明完了。

## 次の一手
- 唯一の前進 = 新規公開バンドル harvest（外部 publish 依存, +0.07 実証）。floor 7180.62 維持。7950 まで −769.38。
