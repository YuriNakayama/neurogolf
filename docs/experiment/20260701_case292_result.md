# case292 result: task396 crop-at-source label Conv

## Summary

- Submission ref: `54224794`
- Previous Public Score: `7182.37`
- New Public Score: `7182.54`
- Decision: adopt

## Local Gate

- Task: `task396`
- Before cost: `9608`
- After cost: `8080`
- Relative reduction: `1.1891`
- Local points: `16.002852848484856`
- Correctness: `n_pass=266`, `n_fail=0`
- Status: `ok`

## Change

Replaced the initial full-size label materialization and crop:

`1x1 Conv -> Cast -> Slice(18x18)`

with direct cropped materialization:

`13x13 sparse Conv -> Cast`

The sparse kernel only uses the top-left tap, so each output location reads the same
input cell that the old full `30x30` label image contributed to the top-left `18x18`
Slice.

## Conclusion

Accepted. This reinforces the current priority rule: crop-at-source rewrites with a
fixed-shape proof should be preferred over small local aliases. The Public gain was
`+0.17`, larger than the recent alias-only cycles.

## Next Step

Use the parallel crop scan to continue with the next proof-grade candidate. `task222`
is the strongest next lead: a `30x30` Conv output has a single `14x14` Slice consumer,
so the same sparse Conv crop technique may produce another high-ratio reduction.
