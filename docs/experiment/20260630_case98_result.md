# 20260630 case98 result — t076 の propagation/candidate 削減は不採用（提出なし）

## 対象

- `task076.onnx`
- baseline: params **340**, memory **19696**, cost **20036**, faithful `n_fail=0`
- Public Score floor: **7180.89**

## 実施内容

t076 は 15x15 board で source component を 3段 MaxPool で広げ、visible/hidden source shape を 7通りの dihedral 候補で target に照合し、hidden cells を `ScatterElements` で追加する graph。過去に TopK/ScatterElements/Mod 混在候補が Kaggle ERROR になった履歴があるため、まず local probe で削減余地を確認した。

## profile

memory 上位:

- final `Pad` output `[1,10,30,30]` bool: 9000
- output concat `[1,5,15,15]` bool: 1125
- board decode Conv/Where `[1,1,15,15]` float: 900 each
- candidate rows/cols `[3,7,7]` float/int32: 588 each
- `ScatterElements` `[226]` float16: 452

## probe

### source propagation 短縮

| 候補 | cost | 結果 |
|---|---:|---|
| `source_component` Cast 入力を `source_step2_mask` に変更 | 19586 | `n_fail=15` |
| Cast 入力を `source_step1_mask` に変更 | 19136 | `n_fail=110` |
| Cast 入力を `source_step0_mask` に変更 | 18685 | `n_fail=266` |

3段目の propagation は faithful 上必要。

### dihedral candidate 削減

| 候補 | cost | 結果 |
|---|---:|---|
| visible rows を第1候補に固定 | 19056 | `n_fail=251` |
| visible cols を第1候補に固定 | 19308 | `n_fail=230` |
| hidden rows を第1候補に固定 | 19966 | `n_fail=258` |
| hidden cols を第1候補に固定 | 19966 | `n_fail=258` |
| visible/hidden rows/cols 全固定 | 15734 | `n_fail=258` |

7通り候補は方向/反転に必要。候補数削減は大きく正答性を壊す。

## 採否

- **不採用、提出なし**。
- `data/output/onnx/task076.onnx` は未変更。

## 結論

t076 の主要 branch は規則上必要で、source propagation と dihedral candidate に余剰は見つからなかった。TopK/ScatterElements を含むため、local-only 微妙候補を無理に submit する価値もない。次は `task219` / `task066` / `task338` の table・branch 削減へ移る。
