# 20260702 case440 result - task191 alias re-gate is no-submit

## Decision

- Task: `task191.onnx`
- Submit: no
- Adoption: none
- Reason: no current-baseline exact candidate reduces cost.

Current baseline:

```text
params=102
memory=14752
cost=14854
status=ok
```

## Graph Evidence

Dominant internal tensors:

```text
valid_u8_q  UINT8 [1,6,23,23]   3174 bytes
yellow_f32  FLOAT [1,1,23,23]   2116 bytes
blue_f32    FLOAT [1,1,23,23]   2116 bytes
color_30    INT8  [1,1,30,30]    900 bytes
yellow_pad  UINT8 [1,1,25,25]    625 bytes
```

`task191` has prior Public-valid evidence from case281, but that accepted change
was already applied to the current baseline.

## Candidate Re-Gate

Re-gated 46 existing current-baseline scratch candidates:

```text
blocked-local-fail: 39
blocked-no-gain:    7
submit-candidate:   0
```

Largest apparent reductions:

```text
task191_63_Max_color_TO_yellow4_i8.onnx
  cost 14854 -> 4103
  gain 1.286551
  n_fail 267
  decision blocked-local-fail

task191_63_Max_color_TO_blue_i8.onnx
  cost 14854 -> 13266
  gain 0.113065
  n_fail 267
  decision blocked-local-fail

task191_31_Mul_motif_TO_rect.onnx
  cost 14854 -> 13950
  gain 0.062790
  n_fail 267
  decision blocked-local-fail

task191_59_Min_blue_u8_TO_paint_u8_q.onnx
  cost 14854 -> 14324
  gain 0.036333
  n_fail 190
  decision blocked-local-fail

task191_drop_59_Min.onnx
  cost 14854 -> 14324
  gain 0.036333
  n_fail 190
  decision blocked-local-fail
```

Exact candidates:

```text
task191_broadcast_compress.onnx  cost 14854 -> 14854, n_fail=0, blocked-no-gain
task191_cleanup.onnx             cost 14854 -> 14854, n_fail=0, blocked-no-gain
task191_conv1x1_to_gather.onnx   cost 14854 -> 14854, n_fail=0, blocked-no-gain
```

Index and dtype probes are invalid under ONNX Runtime because they leave
inferred int32/int64 or float/float16 type mismatches.

## Conclusion

No submit. The remaining large `task191` reductions are semantic value-path
collapses, not safe cleanup. The exact cleanup candidates are stale no-ops
against the current accepted graph.

Do not revisit `task191` through this alias pool. Next cycles should prefer
fresher current-graph targets or build a new formulation rather than repeatedly
submitting local-fail selector/value aliases.
