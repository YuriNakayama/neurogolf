# 20260702 case474 result - adopted five-task micro cadence bundle

## Decision

Adopt.

Kaggle submission:

```text
ref: 54262565
description: case474 reviewed five-task micro cadence bundle gain 0.02421
previous accepted Public Score: 7186.71
new Public Score: 7186.73
delta: +0.02
```

## Submitted Bundle

```text
task009  8310 -> 8306   gain=0.000481464
task080  15650 -> 15590 gain=0.003841234
task096  8818 -> 8798   gain=0.002270664
task213  1851 -> 1834   gain=0.009226660
task255  22731 -> 22541 gain=0.008393758
total gain: 0.024213780
```

All five changed tasks passed the targeted pre-submit gate:

```text
status=ok
n_fail=0
functions=0
forbidden_ops=()
decision=bank-low-gain per task
bundle decision=submit-micro-bundle
```

The bundle was submitted only through the explicit case473 micro lane:

```text
--allow-micro-bundle-gate
--micro-bundle-gain 0.015
--micro-bundle-max-tasks 5
```

## Verification

Dry-run:

```text
uv --project backend run python -m submit submit \
  --onnx-dir /tmp/neurogolf_case474_bundle \
  --output-dir /tmp/neurogolf_case474_submit \
  --gate-baseline-dir data/output/onnx \
  --gate-task-dir data/lake/neurogolf-2026 \
  --allow-micro-bundle-gate \
  --micro-bundle-gain 0.015 \
  --micro-bundle-max-tasks 5 \
  --dry-run \
  -m 'case474 reviewed five-task micro cadence bundle gain 0.02421'
```

Result:

```text
decision: submit-micro-bundle
submission.zip generated
```

Kaggle result:

```text
SubmissionStatus.COMPLETE
publicScore: 7186.73
```

## Conclusion

The explicit micro-bundle lane restored submit cadence without repeating the
case414 regression pattern. The successful bundle excludes known-risk tasks and
uses only current-baseline exact candidates.

Continue maintaining a submit-ready queue:

```text
isolated gain >= 0.020 candidates first
then reviewed micro bundles of exact bank-low-gain tasks with aggregate gain >= 0.015
exclude review-risk/runtime-risk families unless isolated on purpose
```

The band 101-200 subagent found one new backlog-only candidate for a future
bundle:

```text
task110 13591 -> 13475 gain=0.008571692 n_fail=0
/private/tmp/_tmp_neurogolf_task110_alias_idx2d_c_TO_idx_selected_r.onnx
```

The band 301-400 subagent found no submit-grade candidate; its best exact
candidate was `task338`, but it remains `review-known-risk` and should not be
mixed into cadence bundles.
