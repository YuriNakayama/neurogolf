# case240 plan: task117 local mask window trim

## 現状
- 採用済み Public Score floor: 7181.24
- `task117` baseline は `cost=4165` (`params=243`, `memory=3922`, `n_fail=0`)。
- 13x13 の `body_local_mask` と `Pad`/`Xor`/mirror `Or` による local condition があり、params だけでなく中間 tensor memory に効いている。
- 今回の優先基準は絶対 cost ではなく `old_cost / new_cost`。局所 window を 13x13 から縮められれば、中コスト task でも比率改善が見込める。

## 対象
- `data/output/onnx/task117.onnx`

## 仮説
local condition の外周が train/test/arc-gen で不要なら、`body_local_mask` と関連 `Pad` / mirror 処理を 11x11 または 9x9 に縮めても完全一致を保てる。これにより mask params と `fg_local`/`leg_local`/`condition_local` 系の memory をまとめて削減できる可能性がある。

## 検証
- `body_local_mask` の外周値と downstream shape を確認する。
- 13x13 window を 11x11/9x9 に crop し、pad shift 定数と condition pad 定数を同じだけ補正した候補を作る。
- `src.evaluate.scorer.audit_one` で `task117` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost 改善が出た場合のみ実 ONNX に反映し submit する。
