# case224 plan: task292 scatter update trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task292` baseline は `cost=434` (`params=182`, `memory=252`, `n_fail=0`)。
- graph は `Slice(channel4 stripe) -> Mul(update_mask=[0,1]) -> ScatterND`。
- params の大半は `scatter_idx` (`int64`, 168 要素)。

## 対象

- `data/output/onnx/task292.onnx`

## 仮説

`update_mask` は 2 channel update の片側をゼロ化している。もし zero channel scatter が不要、または `updates` を 1 channel に縮めても ScatterND shape を保てるなら、`Mul` と一部 update memory を削れる。`scatter_idx` の片 channel 化まで成立すれば相対削減率が大きい。

## 検証

- temp ONNX で `updates -> pink_source`、`update_mask -> all ones/active only` を probe する。
- `scatter_idx` と `updates` の 1 channel 化を probe する。
- `src.evaluate.scorer.audit_one` で `task292` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
