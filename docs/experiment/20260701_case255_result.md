# 20260701 case255 result — task343 zero_rows Concat→Padで Public 7181.60 採用

## 結果

- 対象: `task343`
- baseline: `params=110`, `memory=1927`, `cost=2037`, `n_fail=0`
- candidate: `params=88`, `memory=1927`, `cost=2015`, `n_fail=0`
- 相対削減率: `2037 / 2015 = 1.0109`
- submit ref: `54212402`
- submitted message: `case255 task343 zero concat pad cost 2037 to 2015`
- Public Score: `7181.59 -> 7181.60`
- 採否: 採用

## 差分

`task343` の終端付近を以下のように変更した。

```text
before:
Concat(zero_rows, output_body_ids, axis=2) -> output_ids

after:
Pad(output_body_ids, pads=[0,0,2,0,0,0,0,0]) -> output_ids
```

`zero_rows (1,1,2,15)` の all-zero uint8 initializer を削除し、8 要素の int64 pads initializer を追加した。memory は同じだが params が `110 -> 88` に下がった。

## 結論

params-only の小削減だが、実 Public Score が `+0.01` 上昇したため採用する。opset 13 以上で zero `Concat` を `Pad` に置換できる候補は、case104 の `task285` では LB 不変だったが、今回のように LB 表示へ反映される場合がある。

## 次の一手

同じ横断検索で残った未採用候補のうち、`task112` の `zero_4x6` prefix `Concat` と `task374` / `task009` の zero col/row を確認する。ただし params-only の極小削減は LB に出ない場合があるため、できれば memory 中間も減る候補を優先する。
