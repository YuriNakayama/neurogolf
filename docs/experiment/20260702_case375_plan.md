# 20260702 case375 plan - task175 base index table probe

## Context

Current accepted Public Score is `7185.86`. Recent dtype probes were blocked by
`Einsum`, `CumSum`, or `GridSample` type constraints. This cycle checks a
different high-ratio surface: a low-cost graph with a large table initializer.

## Target

Target:

```text
data/output/onnx/task175.onnx
```

Current local audit:

```text
params=937
memory=330
cost=1267
n_fail=0
```

Main table:

```text
base_index int32[30,30], values 0..11
onehot_by_code bool[1,10,12]
Gather(onehot_by_code, base_index, axis=2) -> output[1,10,30,30]
```

The table is a large share of the cost by element count. If the index map can be
generated or compressed without materializing a larger runtime tensor, the
relative gain could be meaningful.

## Method

1. Test safe table dtype variants only to confirm ORT constraints.
2. Inspect whether `base_index` has a simple separable/ring formula.
3. Reject formula rewrites that add a full `[30,30]` runtime index tensor unless
   they reduce params enough to beat the current cost.
4. Audit only task175.

## Submit Gate

```text
low risk:    submit if gain >= 0.020, otherwise bank
medium risk: submit if gain >= 0.030, otherwise bank
```

Table/index representation changes are medium risk unless they are exact dtype
or initializer-only changes.
