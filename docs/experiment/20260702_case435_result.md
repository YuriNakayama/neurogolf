# 20260702 case435 result - no-submit task131 ScatterND index dtype recheck

## Outcome

No submit.

Current baseline:

```text
task131 params=675 memory=3302 cost=3977 n_fail=0 status=ok
```

Dominant tensor:

```text
scatter_idx int64 [1,50,4] = 1600B
```

## Candidate

Changed:

```text
scatter_idx8 -> Cast(INT64) -> scatter_idx
```

to:

```text
scatter_idx8 -> Cast(INT32) -> scatter_idx
```

and removed stale `scatter_idx` value info before shape inference. The resulting
candidate inferred `scatter_idx` as INT32.

## Gate Result

```text
/private/tmp/neurogolf_case435_task131_scatter_idx_i32.onnx
  decision=blocked-local-fail
  base=3977
  candidate_cost=None
  status=session_error:
    INVALID_GRAPH: Type 'tensor(int32)' of input parameter of operator
    ScatterND is invalid.
```

## Decision

Reject for submission. This is the same runtime constraint seen in `task284`:
`ScatterND` requires int64 indices in this environment.

## Next

Move to `task162`, where the large tensor is a `20x20` float source slice
rather than a ScatterND index.
