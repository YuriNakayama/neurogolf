# 2026-06-30 case128 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task387.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task387.onnx`
- baseline: params 105, memory 4954, cost 5059, n_pass 266, n_fail 0

## 仮説

`task387` は検出した矩形に水平/垂直 marker と corner decoration を `ScatterElements` で重ねる。corner updates、水平/垂直 indices、または class 選択が冗長なら、`Concat` 前の中間を削って数 % の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `all_indices/all_updates` の一部、`corner_updates`、`h_cols/v_bases`、corner class 選択を簡略化する。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task387.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
