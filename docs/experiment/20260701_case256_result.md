# 20260701 case256 result — task112 zero_4x6 Concat→Padで Public 7181.61 採用

## 結果

- 対象: `task112`
- baseline: `params=158`, `memory=1616`, `cost=1774`, `n_fail=0`
- candidate: `params=138`, `memory=1616`, `cost=1754`, `n_fail=0`
- 相対削減率: `1774 / 1754 = 1.0114`
- submit ref: `54212537`
- submitted message: `case256 task112 zero concat pad cost 1774 to 1754`
- Public Score: `7181.60 -> 7181.61`
- 採否: 採用

## 差分

`task112` の `top_band` 生成を以下のように変更した。

```text
before:
Concat(zero_4x6, motif_rev_rows, axis=1) -> top_band

after:
Pad(motif_rev_rows, pads=[0,6,0,0]) -> top_band
```

`zero_4x6 (4,6)` の all-zero bool initializer を削除し、4 要素の int64 pads initializer を追加した。memory は同じだが params が `158 -> 138` に下がった。

## 結論

params-only の小削減だが、case255 に続いて実 Public Score が `+0.01` 上昇したため採用する。opset 13+ の zero `Concat`→`Pad` は、削減量が小さくても LB 表示に乗る場合がある。

## 次の一手

同じ横断検索で残る `task374` / `task009` / `task396` の zero col/row `Concat` を確認する。ただし既探索済み task は過去結果を優先し、複数 zero initializer をまとめて削れるものを優先する。
