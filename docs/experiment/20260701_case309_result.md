# 20260701 case309 result - reject task374 zero columns to Pad

## Result

No ONNX change and no submit.

`task374` was tested as a follow-up to the successful zero `Concat` -> `Pad`
motif from cases 255 and 256, but the transformation increases cost.

## Probe

Baseline local audit:

- params: 42
- memory: 2262
- cost: 2304
- `n_fail=0`

Candidate:

```text
left_pair_cells  = Pad(h_pairs, pads=[0,0,0,1,0,0,0,0])
right_pair_cells = Pad(h_pairs, pads=[0,0,0,0,0,0,0,1])
```

Candidate audit:

- params: 48
- memory: 2262
- cost: 2310
- `n_fail=0`

## Conclusion

The shared `zero_col` initializer has only 10 bool elements. Replacing both
concats requires two int64 pads initializers with 16 total elements, so params
increase. This motif is only profitable when the removed zero initializer is
larger than the added pads.

`task112` was also rechecked: the remaining zero concats are middle padding, not
prefix/suffix padding. Converting them would add extra intermediates. `task009`
was already rejected in case267 with only `-4` cost.

## Next action

Move back to structural source-crop scans, especially Conv/QLinearConv outputs
that are consumed by one fixed Slice.
