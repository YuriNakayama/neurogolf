# 2026-07-01 case136 plan

## 方針

- floor: Public Score 7181.15
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task250.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task250.onnx`
- baseline: params 142, memory 3076, cost 3218, points 16.92348467244767, n_pass 265, n_fail 0

## 仮説

`task250` は block bbox の上下左右を求め、`Clip(rows, lo/hi)` で row/col map を作り、`OneHot` と `Einsum` で gray layer を bbox 周辺へ射影している。

`rowmap` / `colmap` の Clip が境界保護だけなら削れない可能性が高いが、実データで `lo/hi` が常に範囲内か、片側だけを固定 map にしても一致するなら、OneHot 前の int map 中間を削れる。case134 と同じ Clip 不要化の小型確認として、片側ずつ差し替えて評価する。

## 手順

1. `/tmp` 上の candidate ONNX で `rowmap -> rows`、`colmap -> rows`、片側固定や lo/hi 近似を個別に試す。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task250.onnx` に反映する。
3. submit し、Public Score が 7181.15 を上回れば採用、上がらなければ破棄する。
