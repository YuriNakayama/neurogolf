# 20260703 case552 result - no submit task025 scan

Accepted Public Score:

- `7187.51`

## Summary

No submit. `task025` was scanned as the main structural target, but no exact
cost-reduction candidate was found against the current accepted baseline.

## task025 Results

Current baseline:

```text
task025 cost 11664
```

Best exact candidate from the focused worker:

```text
/private/tmp/neurogolf_agent_case552_A/castto_006_valid_col_10.onnx

old cost 11664
new cost 11664
gain     0.000000
n_fail   0
forbidden ops []
functions []
```

High-gain attempts failed correctness:

```text
feature group prune          cost 6272   gain 0.620412  n_fail 266
alias/concat replacement     cost 10944  gain 0.063716  best n_fail 117
map_terms -> line_color16    cost 11654  gain 0.000857  n_fail 266
dtype-narrow color branch    rejected by ORT type checks
```

Main-process checks of older low-cost artifacts confirmed the same pattern:
large cost-only reductions broke local correctness, commonly with `n_fail=266`.

## Secondary Scan

A parallel worker checked:

```text
task208, task333, task361, task020, task005
```

No standalone candidate cleared the gate.

Best exact micros:

```text
task333 3227 -> 3225  gain 0.000620  n_fail 0
task361 4779 -> 4778  gain 0.000209  n_fail 0
task005 6882 -> 6881  gain 0.000145  n_fail 0
```

These are below the standalone threshold and do not make a viable micro bundle.

## Decision

No submit.

Reasons:

- no `task025` exact cost-reduction candidate;
- no secondary standalone candidate with gain `>=0.020`;
- current exact micros are far below the `0.015` bundle threshold.

## Next Step

Move away from `task025`. The next cycle should choose another non-risk
structural target from the current high-cost list, preferably one with a simpler
output rule than `task025`'s dynamic line remapping.
