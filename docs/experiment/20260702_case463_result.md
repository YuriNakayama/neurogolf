# 20260702 case463 result - task074 symmetry graph re-gate

## Summary

No submit.

`task074` remains exhausted. Current scratch aliases produce large relative
cost reductions, but every positive reduction fails local correctness.

## Baseline

```text
task074
params=50
memory=9000
cost=9050
points=15.8894799633
n_pass=267
n_fail=0
status=ok
```

## Gate Result

```text
candidates=200
positive_cost_reductions=32
errors=0
exact_positive=0
blocked=32
exceptions=0
```

Representative blocked candidates:

```text
9050 -> 6350  gain=0.354310  n_fail=183
  c1 -> c0

9050 -> 7250  gain=0.221763  n_fail=68..183
  c2 -> c1
  c1 -> tT/tR

9050 -> 8150  gain=0.104747  n_fail=15..183
  tR -> c0
  tT -> c0
  tC -> c1
  c2 -> tC
```

The best low-fail member is `tR -> c0` with `n_fail=15`; it is still a semantic
symmetry branch, not an isolated cleanup.

## Decision

Rejected for submission. No candidate met `n_fail=0`.

## Next

Move to `task350`, the other simple cost-9000 graph. If it also produces only
semantic local failures, switch to mid-cost selector tasks rather than repeating
symmetry/propagation branch aliases.
