# 20260702 case495 plan - task005 post-task048 sweep

## Context

Current accepted Public Score:

```text
7187.10
```

Case494 adopted `task048`:

```text
1146 -> 842
gain=0.308253
```

Worker W8 stopped early after finding that candidate and noted that `task005`
still had cost-positive submit-threshold candidates that were not gated.

## Target

```text
task005.onnx
```

Reason:

- W8 identified remaining cost-positive candidates for this task;
- it is a small/medium-cost task where a high relative reduction can move the
  score efficiently;
- it is not in the currently quarantined hidden-regression or Kaggle ERROR
  families.

## Method

1. Gather unique `task005*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Measure current baseline and candidate costs with `evaluate.audit_one`.
3. Rank positive candidates by `ln(old_cost / new_cost)`.
4. Gate positives with `submit.gate.evaluate_candidate_gate`.
5. Submit only if a candidate reaches `submit-candidate`; otherwise record a
   no-submit result and continue.

## Parallel Work

A worker is scanning `task008/task013/task099/task105/task117` for independent
submit candidates or exact micros.
