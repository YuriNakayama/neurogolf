# 20260702 case429 result - no-submit task080 line-mask residual probe

## Outcome

No submit.

Current baseline:

```text
task080 params=107 memory=15543 cost=15650 n_fail=0 status=ok
```

## Inspection

The remaining full-grid separator path is:

```text
islineg   = Equal(cgu, linecu)
outg      = Not(ingrid)
lineOrOut = Or(islineg, outg)
islineu   = Cast(lineOrOut)
rowlineu  = ReduceMin(islineu, axis=3)
...
withsep   = Where(lineOrOut, linecu, exp)
fu        = Where(ingrid, withsep, sentu)
```

`task080` already has accepted structural reductions:

```text
case275: 17136 -> 16836, adopted
case300: 16836 -> 16734, adopted
case312: 16734 -> 15650, adopted
```

## Candidate Results

The exact residual was below standalone gate:

```text
/private/tmp/neurogolf_fresh_014_140_case382/task080_bypass_Floor_bk30flr_TO_bk30f.onnx
  decision=bank-low-gain base=15650 cand=15590 gain=0.0038412339 n_fail=0
```

It was added to `docs/experiment/micro_backlog.md`.

Large-gain terminal/line aliases all failed local correctness:

```text
task080_fu_TO_withsep.onnx
  15650 -> 14749, gain 0.0592956331, n_fail=231

task080_lineOrOut_TO_islineg.onnx
  15650 -> 13850, gain 0.1221856844, n_fail=231

task080_lineOrOut_TO_outg.onnx
  15650 -> 13850, gain 0.1221856844, n_fail=231

task080_outg_TO_ingrid.onnx
  15650 -> 14750, gain 0.0592278342, n_fail=231

task080_withsep_TO_exp.onnx
  15650 -> 14750, gain 0.0592278342, n_fail=231
```

The planned cast-delay rewrite was invalid in ONNXRuntime:

```text
lineOrOut -> ReduceMin(bool) -> Cast(rowline_b) -> rowlineu

session_error:
Type Error: Type 'tensor(bool)' of input parameter of operator ReduceMin is invalid.
```

Other medium candidates around `s1/s2`, `QLinearConv`, and the edge/corner
value masks either failed local audit or stayed below the submit threshold.

## Decision

Reject for submission. The only exact improvement is a `60` cost micro
(`gain=0.003841`), which is below the post-regression submit gate. The full-grid
`islineu` cast cannot be delayed because `ReduceMin(bool)` is not accepted by
the runtime.

## Next

Move to a fresh target instead of forcing `task080`. The next candidates from
the current-graph scan are `task133` and `task243`; prefer `task243` only if
the known `n_fail=1` near miss can be repaired with a small exception, otherwise
inspect `task133` branch-family duplication.
