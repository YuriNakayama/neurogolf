# 20260702 case375 result - skip task175 base index table probe

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.86
```

## Baseline

Target:

```text
data/output/onnx/task175.onnx
```

Current local audit:

```text
params=937
memory=330
cost=1267
n_fail=0
```

The main table is:

```text
base_index int32[30,30]
Gather(onehot_by_code, base_index, axis=2) -> output
```

## Probes

### Gather index dtype

| candidate | result | decision |
|---|---|---|
| `base_index` int64 | exact but cost-neutral | reject |
| `base_index` int16 | ORT rejects Gather index type | reject |
| `base_index` uint8 | ORT rejects Gather index type | reject |
| `base_index` int8 | ORT rejects Gather index type | reject |

Only int32/int64 are valid for this Gather path. Since the scorer counts
initializer parameters by element count, changing int32 to int64 is neutral in
cost and worse in filesize.

### Formula inspection

Simple row/column/ring formulas do not match the table. Error counts against the
900-cell table:

```text
max(row,col):             440
min(row,col):             616
ceil(max(row,col)/2):     419
floor((row+col)/2):       402
distance/ring variants:   403-877
```

The first rows show a nontrivial region table rather than a separable map:

```text
row0: 1 0 1 1 2 2 3 3 4 4 ...
row1: 0 1 0 0 1 1 1 2 2 2 ...
row2: 1 0 1 0 0 0 1 1 1 1 ...
```

Generating this map at runtime would require a full `[30,30]` index tensor or a
multi-stage formula whose intermediates would dominate the current `memory=330`.

### Generic cleanup

One-file case3 surgery:

```text
surgery done: 0/1 tasks improved
```

## Conclusion

Skip submit. `task175`'s table looks large relative to cost, but the valid Gather
index dtypes and parameter accounting leave no initializer-only win. Runtime
formula replacement is unlikely to beat the existing table without a much more
compact closed form.

Next action: continue searching for low-cost high-ratio wins, but prioritize
tables consumed by ops that accept narrower indices or formulas that avoid
materializing a full spatial tensor.
