# 20260702 case388 result - task023 known+fsq3

## Summary

Adopted.

```text
Kaggle ref: 54248681
Public Score: 7186.08 -> 7186.11
task023 local cost: 10348 -> 10060
local gain: ln(10348 / 10060) = 0.028226
```

## Change

Applied the smallest submit-worthy continuation found from the current accepted
`task023` graph:

```text
Sadd4_a -> AS4
b13 -> asg2
fsq3 -> isone3
```

The first two rewires were known exact micros from case387 but below the submit
gate together. Adding one independent `fsq* -> isone*` bypass crossed the
current-relative low-risk threshold while avoiding the broad 19-rewire bundle.

## Local Gate

```text
audit_one task023:
params=70
memory=9990
cost=10060
points=15.78367755634627
n_pass=266
n_fail=0
filesize=7613

forbidden ops: []
functions: 0
nodes: 128
initializers: 15
```

## Candidate Scan Notes

Equivalent `fsq0`, `fsq1`, `fsq2`, and `fsq3` additions all produced the same
local result:

```text
10348 -> 10060
gain 0.028226
```

Smaller compatible additions such as `sqpos* -> nSq*`, `sqpos* -> ONE`, and
`Sacc* -> Ss*` produced:

```text
10348 -> 10132
gain 0.021095
```

These remain useful if `task023` is revisited, but should be staged one narrow
subset at a time because broad local-equivalence bundles are more private-risky.

## Decision

Adopt and keep the new `data/output/onnx.dvc`. Public Score improved by `0.03`,
matching the expected direction from the logarithmic cost reduction.

## Next

Prefer a non-`task023` candidate unless the remaining `task023` exact subsets
can be combined into another narrow `gain >= 0.020` improvement. Current
documented next-candidate pool includes `task319` medium-risk value bundle,
`task002` medium-risk out_bits source bypass, and low-risk batches only when
their accumulated current-relative gain clears the submit-saving threshold.
