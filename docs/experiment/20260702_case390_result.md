# 20260702 case390 result - low-risk micro batch error

## Summary

Rejected.

```text
Kaggle ref: 54249421
description: case390 low-risk micro batch gain 0.03863
status: SubmissionStatus.ERROR
Public Score: none
accepted score remains: 7186.15
```

The submitted batch did not receive a Public Score, so it is not adopted.
`data/output/onnx` was restored to the case389 accepted bundle.

## Submitted Batch

The batch locally passed `audit_one` task-by-task:

```text
task035 2262 -> 2255 gain 0.003099 n_fail=0
task037 4746 -> 4720 gain 0.005493 n_fail=0
task046 2318 -> 2286 gain 0.013901 n_fail=0
task293 1288 -> 1286 gain 0.001554 n_fail=0
task308 1488 -> 1484 gain 0.002692 n_fail=0
task342 1585 -> 1576 gain 0.005694 n_fail=0
task392 1951 -> 1941 gain 0.005139 n_fail=0
task397 2848 -> 2845 gain 0.001054 n_fail=0
```

Combined local gain:

```text
0.038626835551874045
```

All checked candidates had no forbidden ops and no ONNX functions in local
inspection.

## Decision

Reject the batch because Kaggle returned `ERROR`. Do not resubmit the same
bundle. The error source is unknown from the submissions listing, so the next
step should isolate candidates or remove likely compatibility-sensitive files
before spending another submit.

## Next

Treat the individual candidates as locally exact but Kaggle-unproven. The
highest local gain is `task046`, but the safer recovery path is to test a
smaller subset after the next cadence window, not the same 8-task bundle.

Potential isolation order:

```text
1. task046 alone or task046 + tiny already-safe candidates
2. task342/task392/293/308 low-risk subset
3. avoid task037 until checking whether the unused initializer warning is
   related to Kaggle ERROR
```
