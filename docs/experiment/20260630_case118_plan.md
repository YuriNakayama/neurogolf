# 2026-06-30 case118 plan

## 現状

- floor: Public Score 7181.07
- 優先基準: 相対削減率 `old_cost / new_cost`。
- case117 `task350`: 方向 branch 削減は全て `n_fail>0`。

## 対象

- `task359.onnx`
- baseline: params 19, memory 4269, cost 4288, n_pass 266, n_fail 0

Graph は row/col count から mode 色を取り、`gate = colmatch >= rowmatch` で列モードか行モードを選ぶ。

```text
colcnt,rowcnt -> colmode,rowmode
gate -> cm0/rm0 -> selA/selB -> Max -> idxm -> Equal
```

## 仮説

`gate` が hidden/arc-gen で片側に偏る、または `selA/selB` の片側だけで十分なら、row/col どちらかの計算枝を消して大きい相対削減が見込める。

## 手法

1. `idxm` を `selA` / `selB` / `colmode` / `rowmode` に差し替える。
2. `gate` を constant true / false に固定する。
3. `colpres` / `rowpres` の guard を bypass し、`cm0` / `rm0` を直接使う。
4. 各候補を prune し、`src.evaluate.scorer.audit_one` で `task359` の完全一致を確認する。
5. `n_fail=0` かつ cost が下がる候補のみ submit する。

## 足切り

- 変更対象は `task359.onnx` のみ。
- 禁止 op は追加しない。
- 実 Public Score が上がれば採用、上がらなければ破棄する。
