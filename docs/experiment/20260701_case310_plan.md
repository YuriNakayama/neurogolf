# 20260701 case310 plan - large-delta candidate triage

## Context

- Current Public Score floor: `7183.04`.
- Priority rule: correctness is the hard gate, then maximize relative reduction
  `old_cost / new_cost` because the score delta is `ln(old_cost / new_cost)`.
- The user explicitly asked to avoid tiny improvements. This cycle therefore
  rejects micro cleanups unless they are part of a larger same-task structural
  reduction.

## Candidate Search

Large-delta avenues checked before selecting a concrete probe:

1. Crop-at-source scan for `Conv/QLinearConv/MaxPool/Reduce* -> Slice`.
   Shape-only candidates existed, but after accounting for added sparse-kernel
   params or required input slices, no current candidate had positive net saving.
2. `backend/pipeline/case2/golf_wins` harvest.
   All 43 task ONNX files are already reflected in the current output, or are
   more expensive than the current task file.
3. High-cost same-shape alias scan.
   Broad probes on `task233` and `task018` produced no exact win before being
   stopped due poor signal: most candidates caused runtime/index failures.

## Target

Target: `task338`.

Rationale:

- Current audit:
  - `params=689`
  - `memory=17250`
  - `cost=17939`
  - `n_fail=0`
- Earlier case276 found exact single guard aliases, but the best single alias
  only reached `17939 -> 17819` and did not move Public LB.
- The graph has five 24x24 fill branches from a `TopK(k=5)` rectangle detector.
  Removing one complete fill branch would save roughly `1296` cost locally:
  `17939 -> 16643`, a ratio of about `1.078`.

## Planned Probe

Use temporary ONNX copies only.

Probe whether any of the five fill branches is redundant on train/test/arc-gen:

- `fill24 -> fill_0123` to drop branch 4.
- `fill_0123 -> fill_012` to drop branch 3.
- `fill_012 -> fill_01` to drop branch 2.
- `fill_01 -> fill_0` or `fill_01 -> fill_1` to drop branch 1 or 0.

Also test the terminal `valid/fill/out0/zero_b` aliases because they can remove
whole 30x30 boolean tensors if exact.

Submit only if `audit_one(task338)` reports `status=ok`, `n_fail=0`, and a
material cost decrease. Otherwise leave `data/output/onnx/task338.onnx`
unchanged and record a rejection.
