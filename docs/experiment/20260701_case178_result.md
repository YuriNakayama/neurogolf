# 20260701 case178 result

## Target

- Task: `task304`
- Baseline cost: 1441
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task304`.

No exact cost-reducing candidate was found.

Mask/block aliases:

- `mask3 -> grid3`: invalid `Where` condition type
- `mask3 -> pattern`: invalid `Where` condition type
- `mask3 -> true_b/false_b`: invalid `DepthToSpace` rank
- `blocks -> pattern`: invalid `DepthToSpace` rank
- `blocks -> grid3`: invalid `Equal` dimensions
- `blocks -> bg_code`: invalid `DepthToSpace` rank
- `blocks -> major_u8`: invalid `DepthToSpace` rank
- `colors9 -> blocks`: invalid `Equal` dimensions
- `y9 -> colors9`: invalid output type
- `pattern -> grid3`: invalid `Equal` dimensions

Major/color aliases:

- `major_u8 -> grid3`: cost 1396, `n_fail=266`
- `major_u8 -> bg_code`: cost 1396, `n_fail=266`
- `grid3 -> major_u8`: invalid `DepthToSpace` rank
- `grid3 -> bg_code`: invalid `DepthToSpace` rank
- `counts -> x`: cost 1477, `n_fail=266`
- `major -> counts`: cost 1513, `n_fail=234`
- `x -> input`: invalid final `Pad` shape

Dtype probes:

- `label_kernel` float16/uint8/int8: invalid `Conv` type binding
- `color_codes -> uint8`: cost 1441, `n_fail=0`, score-neutral
- `color_codes -> int8/uint16`: invalid `Equal` type binding

Output shape probes:

- graph output set to `input`: unscorable, `n_fail=266`
- graph output set to `y9`: invalid final shape
- graph output set to internal uint8 tensors: invalid declared bool output type

## Submission

- Not submitted.
- Reason: all scored cost reductions failed exact-match auditing or produced invalid ONNX.

## Decision

Rejected. Keep the existing `task304.onnx`.

## Next

The major-color replacement and `DepthToSpace` staging are both essential. Continue
with tasks where first-order simplification produces low fail counts or where an
exact dtype-preserving tensor removal changes scored memory.
