# 20260702 case500 plan - post-case499 breadth scan

## Context

Current accepted Public Score:

```text
7187.12
```

case499 adopted `task031`, confirming that the widened worker search can
recover submit cadence when a current-baseline `n_fail=0` candidate exists.

## Goal

Find the next isolated candidate without forcing a weak submit.

## Method

1. Re-check active worker results from disjoint task bands.
2. Search recent gate JSON files for unadopted `submit-candidate` records.
3. Run a current-baseline unused-initializer cleanup scan over all tasks.
4. Keep exact tiny candidates banked; submit only if a candidate clears
   `n_fail=0` and the submit threshold or has explicit prior Kaggle-safe
   evidence.

## Submit Rule

No forced submit. The 15-minute cadence is a polling cadence: submit only when
there is a gated candidate, otherwise record no-submit and keep workers running.
