# 20260701 case332 result - task348 unused zero cleanup

## Decision

Adopted.

## Local Gate

Only `task348.onnx` was changed and audited.

```text
baseline:  params=194 memory=1774 cost=1968 n_fail=0 status=ok
candidate: params=84  memory=1774 cost=1858 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(1968 / 1858) ~= 0.0575 pt
```

## Submission

```text
ref=54232639
description=case332 task348 unused zero cleanup cost 1968 to 1858
status=COMPLETE
publicScore=7184.88
```

Previous accepted floor:

```text
7184.82
```

The Public Score improved by `+0.06`, so the candidate is accepted.

## Diff Summary

The accepted change removes the dead `zero_b` initializer left behind by the
case325 packed-tail rewrite. No node inputs, node outputs, tensor shapes, or
runtime behavior changed; this is a params-only cleanup on the currently
accepted graph.

## Next Step

The automatic terminal-pack scan found no additional untracked improvements.
Continue with the next validated cleanup candidate, `task274` broadcast
compression (`177 -> 169`), unless a higher-ratio candidate appears first.
