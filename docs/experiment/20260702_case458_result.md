# 20260702 case458 result - current-graph low-risk surgery sweep

## Summary

No submit.

The current-graph low-risk surgery sweep found seven cost-only reductions. The
only exact one was the already-banked `task046` unused-initializer cleanup with
gain `0.000438`, far below the submit threshold.

## Cost-Only Ranking

```text
improved_cost_only=7

task084 2038 -> 1198   gain=0.531315  broadcast_compress
task191 14854 -> 14276 gain=0.039689  identity_elementwise + index_surgery
task023 9340 -> 8980   gain=0.039306  identity_elementwise
task133 33562 -> 32469 gain=0.033109  identity_elementwise + broadcast_compress
task099 1911 -> 1879   gain=0.016887  identity_elementwise
task185 1961 -> 1942   gain=0.009736  identity_elementwise
task046 2286 -> 2285   gain=0.000438  cleanup
```

`task191` and `task133` are recent closed targets, so the first pass focused on
normal-risk tasks: `task084`, `task023`, `task099`, `task185`, and `task046`.

## Individual Audits

### task084

```text
base: cost=2038 n_fail=0 status=ok
cand: cost=1198 n_fail=175 status=INCORRECT
path: /tmp/neurogolf_case458_task084_broadcast_compress.onnx
```

This reproduces the known case357 result. The broadcast-compressed
ScatterElements update no longer matches required dimensions.

### task023

```text
base: cost=9340 n_fail=0 status=ok
cand: cost=8980 n_fail=266 status=INCORRECT
path: /tmp/neurogolf_case458_task023_identity_elementwise.onnx
```

The pass removes a value-path guard that is not semantically neutral for this
already heavily staged task023 graph.

### task099

```text
base: cost=1911 n_fail=0 status=ok
cand: cost=1879 n_fail=265 status=INCORRECT
path: /tmp/neurogolf_case458_task099_identity_elementwise.onnx
```

The apparent identity-elementwise reduction is not valid under actual task
examples.

### task185

```text
base: cost=1961 n_fail=0 status=ok
cand: cost=1942 n_fail=186 status=INCORRECT
path: /tmp/neurogolf_case458_task185_identity_elementwise.onnx
```

The candidate fails local correctness and is not eligible.

### task046

```text
base: cost=2286 n_fail=0 status=ok
cand: cost=2285 n_fail=0 status=ok
path: /tmp/neurogolf_case458_task046_cleanup.onnx
```

This removes one unused initializer and matches the existing micro backlog
entry:

```text
task046 remove unused initializer offset_-2 on current accepted graph,
2286 -> 2285, gain=0.000438, banked case422
```

## Decision

Rejected for submission.

No normal-risk candidate met both `n_fail=0` and `gain >= 0.020`. The only exact
candidate is already banked and too small to submit alone.

## Conclusion

The generic low-risk surgery surface is exhausted for current normal-risk
tasks. Further progress likely requires either:

- a hand repair of a small-failure near miss with a clear exception rule; or
- a fresh semantic simplification on a single task, not a generic pass.
