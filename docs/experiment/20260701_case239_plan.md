# case239 plan: task204 detector kernel trim

## 現状
- 採用済み Public Score floor: 7181.24
- `task204` baseline は `cost=11494` (`params=454`, `memory=11040`, `n_fail=0`)。
- 3..10 サイズの detector kernels (`det_w_3` ... `det_w_10`) と対応する `QLinearConv` / `MaxPool` で red/orange fill を構成している。
- 特に `det_w_10`, `det_w_9`, `det_w_8` は params が大きく、不要なら直接 scorer cost が下がる。

## 対象
- `data/output/onnx/task204.onnx`

## 仮説
public/local examples で最大 detector サイズが不要なら、`fill_core_10` / `fill_core_9` などを小さい detector に alias できる。また kernel 外周や最大 size の branch を削れれば params と memory の両方が下がる可能性がある。

## 検証
- `fill_core_10 -> fill_core_8`, `fill_core_9 -> fill_core_7` など大きい branch alias を probe する。
- `det_w_10/9/8` の crop と `QLinearConv` kernel_shape / `MaxPool` pad 調整を probe する。
- red/orange core の片側 branch 固定を確認する。
- `src.evaluate.scorer.audit_one` で `task204` の全ペア完全一致と cost を確認し、`n_fail=0` かつ十分な cost 改善候補だけ実 ONNX へ反映する。
