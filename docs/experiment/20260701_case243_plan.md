# case243 plan: task042 small conv kernel crop

## 現状
- 採用済み Public Score floor: 7181.26
- `task042` baseline は `cost=3110` (`params=318`, `memory=2792`, `n_fail=0`)。
- 11 node の小型 graph だが、`m1_pair_w_3` `(2,1,4,4)` には下端1行のゼロ外周がある。

## 対象
- `data/output/onnx/task042.onnx`

## 仮説
`m1_pair_w_3` の下端ゼロ行を crop し、必要なら後段 Slice で shape を戻せば、Conv/ConvTranspose 前段の params を削れる可能性がある。小〜中 cost task なので、少量の params 削減でも相対改善の候補になる。

## 検証
- `m1_pair_w_3` の bbox crop を direct / output Slice 付きで probe する。
- 念のため `m2_w_7` / `m3_w_9` の bbox も確認する。
- `src.evaluate.scorer.audit_one` で `task042` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost 改善が出た場合のみ実 ONNX に反映し submit する。
