# 20260702 case440 plan - task191 current-baseline alias re-gate

## Context

Accepted Public Score remains `7186.71`. Cases 438-439 showed that old alias
pools often contain only local failures or stale no-gain candidates after later
accepted changes. This cycle chooses `task191` because it has prior
Kaggle-valid evidence from case281:

```text
case281: task191 14969 -> 14854, gain ~= 0.0077, Public +0.01
```

Current local scorer check:

```text
task191 cost=14854
params=102
memory=14752
status=ok
```

Dominant internal tensors:

```text
valid_u8_q  UINT8 [1,6,23,23]   3174 bytes
yellow_f32  FLOAT [1,1,23,23]   2116 bytes
blue_f32    FLOAT [1,1,23,23]   2116 bytes
color_30    INT8  [1,1,30,30]    900 bytes
yellow_pad  UINT8 [1,1,25,25]    625 bytes
```

## Hypothesis

The known high-gain `task191` candidate failed many local cases, so direct
branch removal is unlikely to submit. However, because `task191` already had a
Public-valid alias, old current-baseline scratch candidates should be re-gated
before the task is deprioritized.

## Method

1. Re-gate current `task191` candidates from the alias, cleanup, dtype, and
   individual-drop scratch directories.
2. Treat value-path and selector aliases as hidden-risky unless they are
   mechanically explainable and locally exact.
3. Submit only if a candidate is:
   - exact on task-local audit;
   - free of forbidden ops/functions;
   - `gain >= 0.020`, or clearly prior-pattern low risk with deliberate review.
4. Otherwise record no-submit and move to a fresher target.
