# 20260702 case510 plan - task173 grid score dtype shrink

Current accepted Public Score:

Pending case509 from `7187.23`.

## Background

During the case509 submit wait, worker AA scanned tasks `161-240` for relative
cost reductions and found a standalone submit-grade exact candidate on
`task173`.

## Target

```text
task173 15664 -> 15020
gain    0.041982439452
n_fail  0
```

Candidate:

```text
/tmp/neurogolf_parallel_AA/dtype_candidates/task173_cast10_to2.onnx
```

Equivalent alternate:

```text
/tmp/neurogolf_parallel_AA/dtype_candidates/task173_cast10_to3.onnx
```

## Hypothesis

The `grid_flat -> grid_score` cast currently uses fp16 (`to=10`). The audited
candidate narrows that path to an integer dtype while preserving all observed
outputs. This is a structural dtype reduction rather than a broad topology
rewrite, and its relative gain clears the standalone submit threshold.

## Gate

Worker AA reported:

```text
decision       submit-candidate
n_fail         0
functions      0
forbidden_ops  []
gain           0.041982439452
```

## Plan

1. Wait for case509 Public Score and adopt/reject `task175` first.
2. Rebase the `task173` candidate onto the accepted post-case509 baseline.
3. Run the submit CLI dry-run with a fresh gate baseline.
4. Submit if the gate remains `submit-bundle` with only `task173` changed.
5. Adopt only if real Public Score improves; otherwise restore the baseline.
