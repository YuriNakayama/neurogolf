# 20260702 case431 plan - task133 branch-family duplication probe

## Context

Case430 reconfirmed that `task243` cannot be repaired below baseline. The next
high-cost candidate is:

```text
task133 params=1278 memory=32284 cost=33562 n_fail=0
```

The current graph has many full-grid branch tensors:

```text
marker_1..4, seed_1..4, anchor_1..4, source_at_anchor,
patch_* and corr_overlay
```

Removing one complete full-grid branch could clear the submit threshold, but
prior docs indicate that naive marker/correction branch drops fail correctness.

## Hypothesis

The only plausible new opportunity is a branch-family rewrite that removes a
duplicate derived tensor while keeping all marker scales and the correction
overlay. Directly dropping marker branches is already known incorrect.

## Method

1. Revalidate current baseline and known candidate families.
2. Inspect whether current accepted graph already includes the small exact
   `sg_gate_2/3/4 -> sg_eqi_2/3/4` cleanup.
3. Gate existing `task133` candidates against the current baseline if present.
4. Submit only an exact candidate with `gain >= 0.020`.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
