# 20260703 case526 plan - task379 repair plus micro replenishment

## Background

Latest accepted Public Score:

```text
7187.38
ref=54272777
case525 task184 review-mid compression
```

Case525 restored submit cadence by adopting the exact `task184` review-mid
candidate. The remaining submit gap cause is still candidate scarcity:

- broad high-gain aliases usually collapse semantics and fail many examples
- exact bank-low candidates are too small for the configured micro-bundle gate
- low-fail near misses need targeted repair rather than wider alias scans

## Hypothesis

The best current standalone repair target is `task379`:

```text
old_cost=10753
candidate_cost=10353
gain=0.037908
n_fail=2
candidate=/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Where_color20_TO_color20_c_400.onnx
```

If the two failures are caused by a narrow color/stamp orientation exception, a
small repair may keep `gain >= 0.020` and become a standalone submit.

In parallel, refresh the exact micro bank against the post-case525 baseline. The
known usable pool includes task066, task161, task219, task349, task260, task054,
and task204, but the total must reach the configured micro-bundle threshold
before spending a submit.

## Method

1. Run a focused `task379` failure comparison and repair search.
2. Re-gate exact micro candidates against current accepted baselines.
3. Search only low-risk non-recent-blocked tasks for additional exact micros.
4. Submit immediately if either:
   - a standalone non-risk candidate has `n_fail=0` and `gain >= 0.020`, or
   - an exact low-risk micro bundle reaches total gain `>= 0.015`.

Do not submit local-fail repairs. Do not include risk-listed tasks unless a
separate isolated plan justifies the risk.

