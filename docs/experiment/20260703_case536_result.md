# 20260703 case536 result - no-submit stale near-miss repair

No Kaggle submit.

Accepted Public Score remains:

- `7187.41`

## Summary

Case536 tested the two high-gain near-miss targets selected after case535:

- `task023`
- `task279`

Three workers ran in parallel: `task023` repair, `task279` repair, and a
low-risk micro ready-queue refresh.

## task023

The documented `Sacc4 -> Sacc3` near miss is stale against the current accepted
graph.

- current accepted `task023`: cost `8764`, `n_fail=0`
- reproduced old near miss after duplicate-init cleanup: cost `9844`,
  `n_fail=2`

Failure pattern:

- `arc-gen[102]`: missing 2x2 block, expected color `8`, candidate emits `2`
- `arc-gen[237]`: same missing 2x2 block pattern

Root cause:

The near miss feeds final `Sg/Lg` from `Sacc3`, dropping the fourth S expansion.
The exact current graph needs the `AS4 -> Sadd4 -> Ss4 -> Sacc4` expansion for
those blocks.

Best exact repair restored the fourth expansion and duplicate-init cleanup:

- cost `11140`
- `n_fail=0`

This is worse than the current accepted `8764`, so no `task023` candidate is
eligible.

## task279

The high-gain flood-extension near miss reproduced:

- baseline: `4827`, `n_fail=0`
- near miss: `4315`, `n_fail=2`, gain `0.1121278321`

Failure cells:

- `train[2]` row `7`, col `4`: predicted `1`, expected `8`
- `test[0]` row `7`, col `7`: predicted `1`, expected `8`

Root cause:

The candidate deletes the final flood-extension pass (`fl_1 -> fl_0`
equivalent). That second 3x3 pass is semantically required for two visible local
cells.

Repairs tried:

- coordinate mask: cost `4827`, `n_fail=262`
- blue-gated coordinate mask: cost `5083`, `n_fail=32`
- full restore of removed flood nodes: cost `4827`, `n_fail=0`

The best exact repair restores baseline cost, so no `task279` candidate is
eligible.

## Micro queue

The refreshed low-risk exact queue remains below submit threshold:

| task | cost | gain |
|---:|---:|---:|
| 117 | `4148 -> 4145` | `0.000723501780` |
| 234 | `6845 -> 6841` | `0.000584538962` |
| 328 | `6904 -> 6902` | `0.000289729105` |
| 206 | `4183 -> 4182` | `0.000239091454` |
| 005 | `6882 -> 6881` | `0.000145317155` |

Aggregate gain: `0.001982178455`, below the `0.015` micro-bundle gate.

## Decision

No submit. Both high-gain targets failed to produce an exact cost-lowering
candidate, and the micro queue is too small.

## Next

The main issue is not submit tooling; it is stale candidate inventory. Next
cycle should build candidate queues from current accepted costs first, then
repair only candidates that are still lower than baseline. Avoid repair work on
old scratch artifacts whose cost is already worse than the accepted graph.
