# 20260630 case26 result — memory は実行時グリッドサイズでトレースと確定（hand-construction 結論は不変）

## 重要な発見: memory は実行時トレースで実グリッドサイズ
- t136 静的 tensor 合計（[1,10,30,30] 前提）= 46179B vs **実 scorer mem=1179**。乖離の原因:
  **scorer は ORT profiling trace で実行時の実際のテンソルサイズを測定**（calculate_memory(model, trace_path)）。
  実グリッドが 10×10 なら [1,10,30,30] 宣言でも実テンソルは [1,10,10,10] 相当で小さい。
- **memory = 実行時の全テンソル実サイズの総和**（静的 30×30 上限ではなく、example の実グリッドで縮む）。

## hand-construction 結論は不変（実測で確認済み）
- ただし私の t002 flood(cost 3,056,510) / t171 frame(41039) は **audit_one の実 trace で測定済み**＝
  実グリッドサイズでもこの値。floor の bool/小tensor economy が実行時でも勝る。
- → memory が実行時トレースでも、explicit float 構成は tensor 数×サイズで floor に負ける（実測確定）。
  case25 の tensor-economy 結論は実行時ベースでも成立。

## 確定
- memory metric の正確な定義（実行時 全tensor実サイズ総和）を確定。スコア式の理解が完全になった。
- この正確な理解の下でも、floor は全タスクで tensor-economy 最小に到達済み（実測の hand-construction
  コストが floor を上回ることで確認）。in-container hand-golf 不可は不変。

## 次の一手
- スコア式を完全に理解（実行時 tensor 総和）。floor 7180.62 は全成分最小。前進は外部 publish のみ。7950 まで −769.38。
