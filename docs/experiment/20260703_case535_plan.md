# 20260703 case535 plan - higher parallelism submit cadence recovery

Accepted Public Score:

- `7187.41`

## Background

The last actual accepted submit was case530 (`task064`, Public `7187.40 ->
7187.41`). After that:

- case531 `task378`: isolated review-risk submit, Public unchanged.
- case532 `task044`: local exact micro regressed Public to `7171.75`; rejected
  and promoted to hard risk.
- case533/case534: no-submit. The available exact candidates were either
  contaminated same-family review-risk (`task046`) or too small (`task005`,
  `task328`), and the promising `task033` repair still had `n_fail=3`.

So the submit gap was caused by candidate depletion plus stricter post-regression
gating, not by a Kaggle/API blocker.

## Cadence change

The requested target is `72` submits in `13` hours, about one submit decision
every `10.8` minutes. This cycle changes the operating mode:

1. Run more parallel search workers so the next submit slot has material.
2. Continue working during Kaggle validation wait time.
3. Treat tiny exact improvements as bankable inventory, but prefer a low-risk
   micro bundle over an isolated one-byte submit.
4. Keep hard-risk tasks blocked and submit review-risk tasks only as isolated
   explicit probes with a plan.

## Parallel search lanes

Spawned workers:

- tasks `001-100`: mechanical cleanup search.
- tasks `101-200`: mechanical cleanup search.
- tasks `201-300`: mechanical cleanup search.
- tasks `301-400`: mechanical cleanup search.
- semantic backlog worker: near-miss and high-EV backlog repair search.
- submit-stall auditor: timeline and process-change audit.

The main thread will independently scan current accepted ONNX files for
low-risk cleanup candidates and prepare a candidate bundle if the total exact
gain is large enough to justify a submit.

## Gate

For any candidate:

- `n_fail=0` is mandatory.
- no forbidden ops/functions.
- compare against current `data/output/onnx/taskNNN.onnx`.
- prefer non-risk tasks from `backend/src/submit/gate.py`.
- use relative gain `ln(old_cost / new_cost)`, not absolute cost.

Submit policy for this cycle:

- standalone non-risk: submit if gain is clearly strong (`>=0.020`).
- low-risk micro bundle: submit if exact aggregate is meaningful and all
  members are mechanically explainable.
- if no bundle exists by the decision window, record no-submit and keep the
  workers running rather than idling.
