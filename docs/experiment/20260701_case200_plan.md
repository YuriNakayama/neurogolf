# case200 plan: task006 tiny mask selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task006` は未着手で、baseline は `cost=153` (`params=27`, `memory=126`, `n_fail=0`)。
- 低 cost だが、3x3 bool tensor 1 個を削るだけでも相対改善が大きい。

## 対象

- `data/output/onnx/task006.onnx`
- 左右の 3x3 blue mask を `And(left_blue, right_blue)` し、`Where(red_mask, when_red, when_black)` で 3-channel small output を作る。

## 仮説

左右 mask の片方が実データ上で同等なら、`red_mask` を `left_blue` または `right_blue` に alias して `And` を削れる。さらに small output が片側固定なら `Where` も削れる。

## 検証

- temp ONNX で `red_mask -> left_blue/right_blue`、`small_output -> when_red/when_black` を probe する。
- `src.evaluate.scorer.audit_one` で `task006` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
