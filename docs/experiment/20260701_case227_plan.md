# case227 plan: task058 bit table encoding trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task058` baseline は `cost=1033` (`params=781`, `memory=252`, `n_fail=0`)。
- graph は `ReduceL2 -> Cast -> Sub -> Gather(row_bits_table) -> BitwiseAnd(column_bits)`。
- params は `row_bits_table` 480、`column_bits` 300 が支配している。

## 対象

- `data/output/onnx/task058.onnx`

## 仮説

実例では size 5..20 がすべて出るため `row_bits_table` の行削減はできない。一方で `column_bits` は channel 0 だけに bit powers が入り、他 channel は zero なので、broadcast 可能な shape や channel mask 分離で params を減らせる可能性がある。また `row_bits_table` の右側 zero tail を truncate して Pad で戻す構成が cost 上有利か確認する。

## 検証

- temp ONNX で `column_bits` の shape 縮小・broadcast variants を probe する。
- `column_bits = col_bits & channel_mask` の分解表現を probe する。
- `row_bits_table` の width truncate + Pad を probe する。
- `src.evaluate.scorer.audit_one` で `task058` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
