# 20260701 case180 plan

## Context

- Current accepted Public Score: 7181.17
- case179 showed `task301` threshold construction has no exact trim.
- `task350` was already covered in case117, so choose an untried compact task.

## Target

- Task: `task346`
- Baseline audit:
  - params: 30
  - memory: 1224
  - cost: 1254
  - points: 17.865006754990563
  - n_pass: 266
  - n_fail: 0
  - filesize: 2850

## Hypothesis

`task346` detects two present colors, checks whether the first color forms a ring
around the second color, then scatters a two-color boolean answer row into a 10-color
output.

Potential exact reductions:

- The ring test may be replaceable by one branch for public examples.
- `color_pair_bool` or `ScatterElements` may be simplified if the selected order is
  stable.
- The final `Unsqueeze`/`Pad` staging may expose a shape-preserving alias.

## Method

1. Probe one alias at a time on temporary `task346` ONNX copies.
2. Run `audit_one` only for `task346`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record failed aliases and leave baseline ONNX otherwise.
