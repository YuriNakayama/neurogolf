# 20260702 case425 result - no-submit task255 source-window probe

## Outcome

No submit.

The current `task255` baseline is:

```text
params=261 memory=22470 cost=22731 n_fail=0 status=ok
```

The source-window narrowing hypothesis did not produce a submit-grade candidate.

## Inspection

Dominant current tensors:

```text
z          float32 [1,1,30,30] 3600 bytes
G/Q paths  bool/uint8 [1,1,30,30] 900 bytes each
edge z_*   float32 4-wide strips 480 bytes each
MatMul masks/partials 120 bytes each
```

`z` is the channel-0 float slice of the full input and feeds both global
reductions and the four directional MatMul/edge branches. Narrowing it locally
would require replacing many consumers, not just shrinking one slice. Prior docs
also record that the float16 MatMul route was rejected by ONNX type constraints.

## Gate Results

Checked known residual and alias candidates against the current baseline:

```text
/private/tmp/neurogolf_case415_safe_surgery/work/task255.onnx
  decision=blocked-no-gain base=22731 cand=22731 gain=0.0000000000 n_fail=0

/private/tmp/neurogolf_case405_alias_scan/task255_029_Or_top_or2_TO_top_bottom29_m.onnx
  decision=blocked-local-fail base=22731 cand=22611 gain=0.0052931181 n_fail=73

/private/tmp/neurogolf_case405_alias_scan/task255_033_Or_bottom_or2_TO_bottom_normal.onnx
  decision=blocked-local-fail base=22731 cand=22667 gain=0.0028195093 n_fail=3

/private/tmp/neurogolf_case405_alias_scan/task255_023_Or_right_or0_TO_right_normal.onnx
  decision=blocked-local-fail base=22731 cand=22640 gain=0.0040113783 n_fail=1

/private/tmp/neurogolf_case405_alias_scan/task255_028_Or_top_or2_TO_top_normal.onnx
  decision=blocked-local-fail base=22731 cand=22670 gain=0.0026871671 n_fail=1
```

## Decision

Reject for submission. The exact candidate has no current cost gain, and the
cost-reducing candidates fail the touched-task audit. This remains a bank-only
area unless a branch-level rewrite can remove a full 30x30 bool family while
preserving all edge cases.

## Next

Move away from `task255` direct aliasing. Continue with memory-heavy tasks where
the dominant tensor is a duplicated derived mask rather than the primary source
slice, because those offer a better chance of `gain >= 0.020` without changing
semantics.
