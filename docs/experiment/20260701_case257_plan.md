# 20260701 case257 plan - task285 color_pad_flat Pad retry

## 現状

- 直近採用: case256 `task112` zero `Concat` -> `Pad` で Public Score `7181.61`
- 優先基準: `n_fail=0` を絶対条件にし、`old_cost / new_cost` の相対削減率を優先する。

## 対象

- `task285`
- baseline audit: `params=550`, `memory=25080`, `cost=25630`, `n_fail=0`

## 仮説

case104 で `task285` の `Concat(color_idx_flat, bottom_zero_row) -> color_pad_flat` を suffix zero `Pad` に置換し、`cost 25630 -> 25602`、`n_fail=0` まで確認したが、当時の Public Score は不変で不採用だった。

case255/256 では同じ params-only の zero `Concat` -> `Pad` がそれぞれ Public `+0.01` として反映された。現在 floor が `7181.61` まで上がっているため、過去に不変だった `-28` cost が表示丸めの境界を越える可能性がある。

## 手法

1. `Concat(color_idx_flat, bottom_zero_row, axis=0) -> color_pad_flat` を `Pad(color_idx_flat, pads=[0,30])` に置換する。
2. `bottom_zero_row` initializer を削除する。
3. `src.evaluate.scorer.audit_one` で `task285.onnx` のみ検証する。
4. `n_fail=0` かつ cost 減なら submit し、Public Score が上がれば採用、上がらなければ破棄する。
