# 20260702 case461 plan - task204 detector branch re-gate

## Context

Case460 rejected `task025`: 128 positive reductions, zero exact positives.
The next normal-risk memory-heavy task from the current ranking is `task204`:

```text
params=454
memory=11040
cost=11494
n_fail=0
```

Historical case239 found that detector branch aliases are close but incorrect:

```text
fill_core_10 -> fill_core_8  cost=10948  n_fail=2
fill_core_9  -> fill_core_7  cost=10944  n_fail=3
```

Because current scratch pools contain later branch aliases and generic surgery
outputs, re-gate against the accepted baseline before deciding whether a manual
repair is worthwhile.

## Target

`task204.onnx`

## Method

1. Gather `task204*.onnx` from shallow `/private/tmp` and `/tmp` scratch pools.
2. Cost-screen against current `data/output/onnx/task204.onnx`.
3. Audit all positive reductions with task204 examples.
4. Submit only if a candidate is exact and clears the normal submit gate:

```text
n_fail=0
gain >= 0.020
no forbidden ops/functions
```

If all high-gain candidates fail, record the best near misses and move on.
