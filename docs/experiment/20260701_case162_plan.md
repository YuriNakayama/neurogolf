# 20260701 case162 plan

## Context

- Current accepted Public Score: 7181.17
- Recent cases 159-161 found no exact-match reduction in path/bridge/two-rectangle masks.
- Continue targeting relative cost reduction, but submit only after `n_fail=0`.

## Target

- Task: `task268`
- Baseline audit:
  - params: 256
  - memory: 3128
  - cost: 3384
  - points: 16.873186279273888
  - n_pass: 266
  - n_fail: 0
  - filesize: 4450

## Hypothesis

`task268` builds a fill area from a detected bounding box plus outside-axis / diagonal extensions. The expensive part is a chain of 20x20 masks:

- `open_top/open_bottom/open_left/open_right`
- gap span masks from adjusted bbox coordinates
- diagonal masks `diag_diff` and `diag_sum`
- final `fill_area = bbox_span | outside`

If one direction or one diagonal branch is redundant for the public distribution, a single bypass can remove multiple mask tensors. The best candidates are `diag_any`, `outside_axis`, `spans_diag`, and open-side selectors.

## Method

1. Probe one replacement at a time on temporary `task268` ONNX copies.
2. Run `audit_one` only for `task268`.
3. Apply and submit only if `n_fail=0` and cost decreases materially.
4. If no exact-match reduction exists, write result and commit docs only.
