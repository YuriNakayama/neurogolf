# 2026-07-01 case144 result

## 結果

- 対象: `task119.onnx`
- 採否: 不採用
- submit: なし
- floor: Public Score `7181.16`

## baseline

```text
task119.onnx
params 121
memory 1244
cost 1365
points 17.781090292380938
n_pass 266
n_fail 0
filesize 4683
```

## 試行

方向・反転 selector を既存 flag または片側入力へ差し替えた。

- `high_start -> bottom_has_cyan`: cost 1364, n_fail 75
- `high_start -> right_has_cyan`: cost 1364, n_fail 62
- `high_start -> left_has_cyan`: cost 1364, n_fail 205
- `edge_is_vertical -> left_has_cyan`: cost 1364, n_fail 75
- `edge_is_vertical -> right_has_cyan`: cost 1364, n_fail 68
- `edge_is_vertical -> high_start`: cost 1364, n_fail 130
- `tb_pos -> bottom_pos_u8`: cost 1364, n_fail 51
- `tb_pos -> top_pos_u8`: cost 1364, n_fail 55
- `lr_pos -> right_pos_u8`: cost 1364, n_fail 61
- `lr_pos -> left_pos_u8`: cost 1364, n_fail 67
- `edge_pos0_i -> lr_pos`: cost 1364, n_fail 106
- `edge_pos0_i -> tb_pos`: cost 1364, n_fail 128
- `axis_pos -> axis_rev`: cost 1355, n_fail 129
- `axis_pos -> axis_base`: cost 1355, n_fail 137
- `y_side -> y_rev`: cost 1355, n_fail 133
- `y_side -> y`: cost 1355, n_fail 133
- `row_f` / `col_f` の片側固定: n_fail 121 以上

selector の true/false 定数固定も全て n_fail 121 以上だった。

## 結論

`task119` の branch は cyan edge の位置・向き・反転の全ケースを区別しており、相対削減は見込めるが完全一致を保てない。ONNX は変更しない。

次は `task183` / `task046` / `task013` など、短い graph で `Where` が多い未探索 task を優先する。
