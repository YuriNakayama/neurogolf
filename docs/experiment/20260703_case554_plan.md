# 20260703 case554 plan - task174 mismatch-any reduction

Accepted Public Score:

- `7187.51`

## Hypothesis

`task174` contains a mismatch-count pattern:

```text
Cast(bool -> int32) + ReduceSum + Equal(0)
```

For the all-equal check, the exact count is unnecessary. Replacing it with a
mismatch-any pattern should preserve correctness while reducing intermediate
memory:

```text
Cast(bool -> uint8) + ReduceMax + Equal(0u8)
```

## Candidate

- Path: `/private/tmp/neurogolf_agent_case553_D/candidate/task174.onnx`
- Local gate: `8978 -> 8744`
- Relative gain: `0.026409`
- Correctness: `n_fail=0`
- Safety: `functions=0`, forbidden ops `[]`

## Method

1. Build a full 400-task bundle from current accepted `data/output/onnx`.
2. Replace only `task174.onnx` with the candidate.
3. Run pre-submit gate against the current baseline.
4. Submit only if the bundle gate remains `submit-bundle`.
5. Adopt only if Kaggle Public Score improves.
