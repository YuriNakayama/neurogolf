# 20260703 case517 result - adopt task238 v alias reduction

## Result

Adopt.

Kaggle submission:

```text
ref=54271014
status=COMPLETE
Public Score: 7187.29 -> 7187.37
```

## Change

```text
task238.onnx
candidate: /tmp/neurogolf_parent_case517_alias/task238_v_TO_candidate.onnx
old_cost=2355
new_cost=2185
gain=0.0749248989
n_fail=0
decision=submit-candidate
```

Pre-submit bundle gate:

```text
changed=1
total_gain=0.074925
decision=submit-bundle
```

The accepted file is now:

```text
data/output/onnx/task238.onnx
```

## Why Submits Had Stalled

After case514, a locally clean micro bundle produced Kaggle `ERROR`. The loop
therefore stopped spending submits on weak bundles and reverted to standalone
candidates only. That reduced ERROR/regression risk, but candidate supply was
too low: only case516 task354 crossed the standalone threshold before this
cycle.

## Parallel Search

To increase submit supply, six workers were used:

```text
U1 tasks 001-100: still running during adoption
U2 tasks 101-200: no safe standalone candidate
U3 tasks 201-300: independently found the same task238 2355 -> 2185 candidate
U4 tasks 301-400: still running during adoption
U5 prior high-upside alias families: still running during adoption
U6 high-cost dtype/initializer reductions: still running during adoption
```

Parent scan found exact task238 candidates:

```text
task238 col2_count -> col_sum_sq: 2355 -> 2245 gain=0.047835 n_fail=0
task238 col_var_zero -> candidate: 2355 -> 2195 gain=0.070359 n_fail=0
task238 col_var_zero -> valid_side_color: 2355 -> 2195 gain=0.070359 n_fail=0
task238 v -> candidate: 2355 -> 2185 gain=0.074925 n_fail=0
```

An additional scan starting from the accepted `2185` candidate found no further
exact reduction below `2185`.

## Decision

Keep `task238`. It is a standalone non-risk task, passed current-baseline local
gate, and improved real Public Score by `+0.08`.

Next cycle: keep the increased parallel search model, but continue standalone
submission only. Banked micro candidates remain useful as search hints, not as
blind bundles.
