# 20260702 case366 plan - task243 near-miss repair

## Context

Accepted Public Score remains `7185.78`. Case365 confirmed that generic surgery
on the current bundle only rediscovers the tiny task285 cleanup (`gain
0.00094`), so this cycle switches back to targeted structural repair.

The priority rule is still relative gain:

```text
gain = ln(old_cost / new_cost)
```

Correctness is a hard gate. No candidate is submitted unless `n_fail=0` and the
risk-adjusted submit-saving threshold is met.

## Primary Target

Revisit task243:

```text
current cost: 22650
near miss: fill_27 -> fill_26
near miss cost: 22002
gain: 0.029026
n_fail: 1
```

This is close to the medium-risk standalone threshold. If the single failing
case can be repaired without giving back most of the saving, it may become a
submit-worthy isolated candidate.

## Parallel Side Target

Recheck task349:

```text
current cost: 24389
near miss: halo_c4_to_c3
near miss cost: 22576
gain: 0.07724
n_fail: 17
```

This has high nominal gain but prior notes say the detector branch is required.
Use it only as a side investigation; do not spend the critical path on a broad
hidden-sensitive rewrite.

## Procedure

1. Spawn parallel scratch-only probes for task243 and task349.
2. Locally inspect task243 graph/failure if needed, because `n_fail=1` makes it
   the better repair target.
3. Audit only changed candidate ONNX with `evaluate.audit_one`.
4. Submit only if an exact candidate clears:
   - low-risk: `gain >= 0.020`
   - medium-risk: `gain >= 0.030`
   - high-risk: `gain >= 0.080`
5. Otherwise write a result, bank any exact micro, and keep `data/output/onnx`
   unchanged.

## Exclusions

- task002 bit-topology pruning, due prior Public regression.
- task233 `uint8 TopK`, due prior Kaggle `SubmissionStatus.ERROR`.
- broad task286 equivalence bundles, due hidden regression.
- task118 table pruning below high-risk threshold.
