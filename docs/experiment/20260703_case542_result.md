# 20260703 case542 result - adopt task023 alias

Accepted Public Score before submit:

- `7187.41`

## Summary

Adopted.

Kaggle submission:

```text
ref          : 54275421
description  : case542 task023 alias Sadd4 to u4
status       : COMPLETE
Public Score : 7187.43
```

Public Score improved by `+0.02`, so the task023 candidate is adopted into
`data/output/onnx/task023.onnx`.

## Candidate

Path:

```text
/private/tmp/neurogolf_agent_case542_A/candidate_task023_alias_0.onnx
```

Local gate before submit:

```text
task023 8764 -> 8548
gain    0.024955084
n_fail  0
decision submit-candidate
```

Rationale:

- Rewire `Sadd4 -> u4`.
- Dead-code elimination removes three nodes.
- No functions.
- No forbidden ops.
- No sparse initializers.

Post-adoption individual audit:

```text
params  = 70
memory  = 8478
cost    = 8548
points  = 15.946547383560961
n_pass  = 266
n_fail  = 0
status  = ok
```

## Other Search Results

Scorable-only artifact mining found no standalone submit candidates, but it
expanded the low-risk micro bank:

```text
task368 6225 -> 6217 gain=0.001286
task117 4148 -> 4145 gain=0.000724
task333 3227 -> 3225 gain=0.000620
task234 6845 -> 6841 gain=0.000585
task358 4392 -> 4390 gain=0.000455
```

Best max-5 micro bundle gain is now `0.003669`, still below the `0.015` bundle
gate.

Validator-compatible kernel-reduction attempts produced no retained candidate:

- task222/task396 1x1 Conv plus Slice variants were exact but cost more.
- task324 has nonzero QLinearConv kernel taps on all borders, so simple crop is
  not possible.

## Decision

Adopt case542 and keep searching from Public Score:

```text
7187.43
```
