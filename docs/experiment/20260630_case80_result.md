# 20260630 case80 result — ReduceMin(bool) は invalid graph、Cast遅延は不採用（提出なし）

## independent_impl:
t080 の 900B 級 Cast を後段へ遅延する graph surgery を試行:
- before: `lineOrOut(bool [1,1,30,30]) -> Cast(uint8) -> ReduceMin(axis=3)`
- probe: `lineOrOut -> ReduceMin(bool) -> Cast(uint8 [1,1,30,1])`

## faithful 検証
- probe は ONNX Runtime session 作成時に invalid graph:
  - `Type 'tensor(bool)' ... of operator (ReduceMin) is invalid`
- 正答性評価まで到達せず。

## 確定
- `ReduceMin` は bool 入力を受け付けないため、現行の `[1,1,30,30]` uint8 Cast は必要。
- `Not/ReduceMax/Not` 代替も ReduceMax(bool) が同様に型制約リスクを持ち、node/memoryも増えるため不利。
- t080 の dtype 遅延方向は不採用。floor 7180.85 維持、提出なし。

## 次の一手
- t080 は保留。次は t173 の template-stamping graph、または高 cost t233/t286/t018 の trace 局所監査へ移る。
