# 20260702 case483 plan - task243 scratch pool cadence scan

## Context

Accepted Public Score is `7186.73`. Cases 477-482 produced no Kaggle submit
because every material candidate failed local correctness or was only a tiny
banked cleanup. The requested cadence is `72 submits / 13 hours`, which is
roughly one submit decision window every `10.8` minutes. That cadence should be
used for gated submit decisions, not for forced weak submissions.

`task243` current baseline:

```text
task243 cost=22650
```

It is outside the current hard/review/runtime risk sets and has a large existing
scratch pool.

## Target

```text
task243
baseline: data/output/onnx/task243.onnx
task json: data/lake/neurogolf-2026/task243.json
```

## Method

1. Gather `task243*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against current baseline.
3. Gate positive candidates with `submit.gate.evaluate_candidate_gate`.
4. Submit only if a candidate satisfies:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Bank exact low-gain candidates; otherwise write a no-submit result and keep
   the parallel worker queue running.
