# 20260630 case85 result — t118 の MaxPool/hash 補正削減は採用不可（提出なし）

## 対象

- `data/output/onnx/task118.onnx`
- baseline: params **4614**, memory **34623**, cost **39237**, `n_fail=0`

## trace

主な memory 支配テンソル:

- `input_u8`: uint8 `[1,10,30,30]` = 9000B
- `plus16`: float16 `[1,1,25,28]` = 1400B（trace 表示は float だが value_info は float16）
- `sel_chosen`: float16 `[1,1,21,24]` = 1008B
- `updates8`: uint8 `[1,2,25,28]` = 1400B
- `red_pair/nb_pair/full_pair` 周辺: `[1,2,21,24]` = 各 1008B

`input_u8` は序盤の slice/QLinearConv と最終 `ScatterElements` の base を兼ねており、局所 Cast 化すると float slice や float updates が増えて悪化する見込み。

## 検証

### MaxPool kernel

| 変更 | cost | 結果 |
|---|---:|---|
| kernel 7 → 5 | 39237 | `n_fail=1` |
| kernel 7 → 3 | 39237 | `n_fail=12` |
| kernel 7 → 1 | 39237 | `n_fail=46` |
| kernel 7 → 9 | 39237 | `n_fail=0` だが cost 不変 |

kernel は縮小不可。拡大は正答だが cost も変わらず採用価値なし。

### hash 補正

| 変更 | cost | 結果 |
|---|---:|---|
| hash 補正全体 bypass | 30853 | `n_fail=5` |
| negative scatter bypass | 36040 | `n_fail=5` |
| positive scatter bypass | 36431 | `n_fail=1` |

hash 補正は少数例に効いており、削除不可。

### dtype / 機械的削減

- `Einsum` / `ConvTranspose` 系の value_info は既に float16。
- dead output: なし
- 重複 node: なし
- 未使用 initializer: なし
- 重複 initializer: なし

## 採否

- Kaggle submit: **なし**
- 採否: **不採用**
- Public Score floor: **7180.87 維持**

## 結論

t118 は 47 node と小さいが、最大の `input_u8` が複数用途を兼ねるため局所化しにくい。MaxPool と hash 補正はいずれも正答性に必要で、今回の一点検証では採用候補なし。

## 次の一手

次は t187/t366/t158/t133 の memory 支配 graph に移り、case84 と同じく反復鎖の過剰段を探す。
