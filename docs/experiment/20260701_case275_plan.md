# 20260701 case275 plan - task080 bundled mask aliases

## Context

- Current accepted Public Score floor: `7181.70`.
- Continue the case273/case274 alias-bundle strategy because it removes memory-producing intermediates and has moved the real Public Score.
- Avoid standalone micro gains unless bundled within one task.

## Target

- Task: `task080`
- Baseline local cost: `params=112`, `memory=17024`, `cost=17136`

## Hypothesis

Alias probing found three exact local mask simplifications:

```text
isol    -> nonb
ecells0 -> eblk
ccells  -> cblk
```

Each single alias removes one 100-byte bool intermediate and passes all local examples. Bundling all three should remove 300 bytes of memory while preserving exact outputs.

## Precheck

Temporary candidate:

```text
/tmp/task080_bundle_isol_ecells0_ccells.onnx
```

Local audit:

```text
params=112
memory=16724
cost=16836
n_pass=231
n_fail=0
status=ok
```

Relative reduction: `17136 / 16836 = 1.0178`.

## Method

1. Apply all three aliases to `task080.onnx`.
2. Prune dead nodes, unused initializers, and stale value_info.
3. Run `audit_one` only for `task080`.
4. Submit if `n_fail=0` and cost remains reduced.
5. Adopt only if actual Kaggle Public Score increases.
