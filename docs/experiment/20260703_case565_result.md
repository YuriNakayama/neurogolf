# 20260703 case565 result - submit paused by gate, parallel search widened

Accepted Public Score remains: `7187.57`

## Why no submit happened

No Kaggle submit has been made since case561 because the intervening cycles did
not produce a submit-ready candidate:

- case562: no exact positive candidate after excluding task316.
- case563: structural-only search found no exact positive candidate.
- case564: risk-segmented search found no submit candidate and no bank-low
  candidate.
- case565 so far: task118 close-miss repair still has one failing example when
  the meaningful cost reduction is preserved.

This is intentional gate behavior, not an idle submit loop. Submitting candidates
with `n_fail > 0`, `gain <= 0`, or known validator risk would spend submit budget
without producing a usable Public Score signal.

## Parallelism increased

The active search width was expanded to use the submit wait time and avoid a
single-task stall:

- existing task118 repair worker;
- independent task118 repair worker;
- scratch candidate mining worker;
- structural search workers for task ranges `001-140`, `141-280`, `281-400`;
- solver-bank / handcraft replacement worker;
- non-task118 close-miss repair worker after the `141-280` sweep completed.

## Completed checks

### Micro backlog refresh

The low-risk micro refresh re-gated the current 20260703/20260702 backlog
against `data/output/onnx`. It found no aggregate bundle:

- `submit-candidate`: `0`
- `bank-low-gain`: `0`

Most entries were already absorbed into the current baseline (`gain=0`) or had
become stale and worse than the current accepted graph.

### task118 repair

The best high-gain candidate remains:

```text
/private/tmp/neurogolf_case565_task118_worker/candidates/task118_closemiss_posscatter_removed.onnx
35807 -> 35095
gain=0.020085
n_fail=1
```

The failure is localized to train example 2. Removing the positive patch scatter
misses four positive mask cells at flat indices `197, 225, 252, 255` in the
`25x28` gray crop, corresponding to grid cells `(7,1)`, `(8,1)`, `(9,0)`,
`(9,3)`.

The only exact positive candidate found in this lane is a micro:

```text
/private/tmp/neurogolf_case565_task118_worker/candidates/task118_exact_lowgain_split_support_pair.onnx
35807 -> 35803
gain=0.000112
n_fail=0
functions=0
forbidden=[]
decision=bank-low-gain
```

This is banked only and not submitted by itself.

The independent repair lane found an even smaller exact positive:

```text
/private/tmp/neurogolf_case565_task118_worker2/candidates/task118_slice_pos_from_negmatch.onnx
35807 -> 35806
gain=0.000028
n_fail=0
functions=0
forbidden=[]
decision=bank-low-gain
```

Conclusion for task118: the meaningful `35095` candidate depends on removing
the positive patch scatter, but exact repair requires restoring a dynamic
positive mask write and brings back almost all of the memory. Further task118
work should not revisit this same scatter-removal family unless a new way to
encode the four missing positive cells is found without restoring the 700-cell
scatter.

### Structural range 141-280

The `141-280` structural sweep found no exact positive candidate and no exact
candidate with gain `>=0.010`.

The only surviving generated candidate was incorrect:

```text
/private/tmp/neurogolf_case565_struct_B/candidates/task156_alias_17_Or_in0_top_small_a_TO_sep5_top_small.onnx
task156 1744 -> 1744
gain=0.000000
n_fail=112
decision=blocked-local-fail
```

### Structural range 001-140

The `001-140` structural sweep scanned 386 generated candidates:

```text
submit-candidate: 0
bank-low-gain: 0
review-mid-gain: 0
exact positive lower-cost rows: 0
```

Decision summary:

```text
blocked-baseline: 4
blocked-local-fail: 360
blocked-no-gain: 22
```

Logs:

```text
/private/tmp/neurogolf_case565_struct_A/scan.txt
/private/tmp/neurogolf_case565_struct_A/scan_summary.json
```

### Structural range 281-400

The `281-400` structural sweep scanned 188 generated candidates:

```text
submit-candidate: 0
bank-low-gain: 0
```

No exact positive lower-cost candidate survived. Conservative duplicate/unused
initializer cleanup, value-info pruning, and single-consumer bypass probes were
either no-gain, local-fail/unscorable, or worse than the current baseline.

### Scratch mining

Existing recent scratch candidate directories were rescanned against the current
baseline. No submit candidate and no current bank-low candidate survived:

```text
/private/tmp/neurogolf_case563_regate: scanned=7, submit=0, bank=0
/private/tmp/neurogolf_case565_task118_worker/candidates: scanned=14, submit=0
/private/tmp/neurogolf_agent_U2/candidates + U3/U4: scanned=24, submit=0, bank=0
/private/tmp/neurogolf_case563_A/work: scanned=10, submit=0, bank=0
/private/tmp/neurogolf_case564_main_dtype/candidates: scanned=45, submit=0, bank=0
/private/tmp/neurogolf_case561_B/candidates: scanned=125, submit=0, bank=0
```

Named recent bank-low paths for `task205`, `task023`, `task066`, `task349`, and
`task054` are now either equal to the current baseline or worse. The actionable
scratch inventory is therefore exhausted under the current baseline.

### Next target ranking

A fresh target-ranking pass found no obvious non-risk duplicate initializer or
stale value-info win in the current accepted ONNX set. The next best targets are
close-miss repairs and dtype/runtime-memory reductions:

```text
1. task187: known 26396 -> 25146, n_fail=4, expected bucket 0.04-0.06
2. task074: known 9050 -> 8150, n_fail=15, expected bucket 0.08-0.11 if repaired
3. task379: known 10753 -> 10353, n_fail=2, expected bucket 0.02-0.04
4. task364: prior near miss around gain 0.039, n_fail=21
5. task076: prior stack near miss around gain 0.032, n_fail=4
```

Next cycle should start with `task187`, `task074`, and `task379` rather than
continuing generic structural sweeps.

### Safe dtype / initializer search

The safe dtype and initializer lane generated 192 ONNX candidates and fully
reported 87 gated candidates:

```text
submit-candidate: 0
bank-low-gain: 0
exact positive: 0
```

All gated rows were `blocked-local-fail`. The largest apparent wins were not
functionally correct:

```text
task084_broadcast_compress.onnx: 2038 -> 1198, gain=0.531315, n_fail=175
task133_broadcast_compress.onnx: 33562 -> 33370, gain=0.005737, n_fail=267
```

Reports:

```text
/private/tmp/neurogolf_case565_safe_dtype/gate_results.json
/private/tmp/neurogolf_case565_safe_dtype/static_dtype_report.json
/private/tmp/neurogolf_case565_safe_dtype/cost_positive.json
/private/tmp/neurogolf_case565_safe_dtype/dtype_cost_positive.json
```

### Low-fail diagnostic: task187

The low-fail diagnostic lane selected `task187`, but did not find a lower-cost
exact repair:

```text
task187_reach12_repro.onnx: 26396 -> 26283, gain=0.004290, n_fail=4
task187_reach12_plus_final_1x3.onnx: 26396 -> 27533, n_fail=3
task187_reach12_plus_final_3x1.onnx: 26396 -> 27533, n_fail=1
task187_reach13_full_restore.onnx: 26396 -> 27533, n_fail=0, worse than baseline
```

The failures are all final reach-frontier misses: reachable-color cells remain
as enclosed color. Single-axis final propagation fixes only one orientation and
already exceeds baseline cost. Exact repair requires restoring the final
propagation step, which is not a positive candidate.

### Non-task118 close-miss repair

The close-miss worker generated and gated 202 candidates:

```text
blocked-local-fail: 198
blocked-no-gain: 4
winners: 0
```

Best remaining close misses:

```text
task243_alias_fc_17_to_fc_16.onnx:   22650 -> 22002, gain=0.029026494, n_fail=1
task243_fill27_to_fill26.onnx:       22650 -> 22002, gain=0.029026494, n_fail=1
task243_QLinearConv_TO_fill_26.onnx: 22650 -> 22326, gain=0.014407933, n_fail=1
task187_alias_n01.onnx:              26396 -> 18896, gain=0.334262224, n_fail=198
```

No submit-ready candidate was found.

## Decision

No submit yet. Continue the widened parallel search and submit immediately once
a candidate or reviewed bundle satisfies:

```text
n_fail=0
candidate cost < current baseline cost
functions=0
forbidden ops=[]
no known Kaggle ERROR/regression family
```
