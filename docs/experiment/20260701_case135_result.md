# 2026-07-01 case135 result

## 対象

- `task044.onnx`
- baseline: params 68, memory 11390, cost 11458, points 15.653556545031478, n_pass 266, n_fail 0

## 実施内容

case134 で有効だった `Clip(..., 0, 1)` 不要化パターンを `task044` の6箇所に適用できるか確認した。各 Clip output を元の sum 入力へ個別に差し替え、`/tmp` 上の候補 ONNX で `audit_one` した。

## 足切り結果

すべて `n_fail>0`。

- `gray_row_active_all -> gray_row_sum_all`: cost 11438, n_fail 80
- `hole_top_row_active -> hole_top_row_sum`: cost 11438, n_fail 42
- `hole_bottom_row_active -> hole_bottom_row_sum`: cost 11438, n_fail 41
- `hole_bottom_col_active -> hole_bottom_col_sum`: cost 11438, n_fail 27
- `movable_row_active -> movable_row_sum`: cost 11258, n_fail 77
- `movable_col_active -> movable_col_sum`: cost 11258, n_fail 53

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

`task044` の Clip は単なる存在判定ではなく、後段の選択や tie-break に必要な 0/1 正規化だった。sum を直接使うと局所的には cost が下がるが完全一致を壊す。

## 次の一手

Clip 横断探索は続けるが、次は `task250` / `task274` のような小さめの Clip graph を優先する。巨大な `task018` / `task366` は候補数が多く、1 cycle の検証単位を絞ってから扱う。
