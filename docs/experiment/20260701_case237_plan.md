# case237 plan: task323 color stencil crop

## 現状
- 採用済み Public Score floor: 7181.24
- `task323` baseline は `cost=2177` (`params=656`, `memory=1521`, `n_fail=0`)。
- `color_kernel` は shape `(1,1,25,25)` の uint8 stencil で、`QLinearConv(... pads=12)` に使われる。
- case236 と同じく、大きめ kernel の安全な center crop が通れば params を直接削れる。

## 対象
- `data/output/onnx/task323.onnx`

## 仮説
13x13 crop に対する stencil の外周が public/local examples で不要なら、`color_kernel` を 23/21/19 などへ crop し pads を縮められる。`ConvInteger` の output pad も過剰なら縮小余地があるが、まず `color_kernel` に絞る。

## 検証
- `color_kernel` を center 23/21/19/17/15 crop にし、`QLinearConv` pads を対応して縮める。
- `output_kernel` channel や `seed_plus_one` branch の固定化も軽く probe する。
- `src.evaluate.scorer.audit_one` で `task323` の全ペア完全一致と cost を確認し、`n_fail=0` かつ十分な cost 改善候補だけ実 ONNX へ反映する。
