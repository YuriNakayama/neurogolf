# 20260702 case393 result - task285 uint8 TopK error

## Summary

Rejected.

```text
Kaggle ref: 54250561
description: case393 task285 uint8 topk isolated gain 0.07765
status: SubmissionStatus.ERROR
Public Score: none
accepted score remains: 7186.22
```

The candidate was not adopted. `data/output/onnx` was restored to the case392
accepted bundle.

## Local Gate

The isolated candidate passed local audit:

```text
task285 local cost: 24535 -> 22702
local gain: ln(24535 / 22702) = 0.07764764320871903
n_fail=0
nodes=84
initializers=34
forbidden ops=[]
functions=0
```

The changed `TopK` input was:

```text
cell_topk input: color_idx_flat
```

This feeds uint8 directly into `TopK`.

## Decision

Reject and mark this family as Kaggle-runtime incompatible unless a cast-safe
repair is found. This confirms the warning from earlier docs: uint8 `TopK`
input bypasses can be locally exact but fail Kaggle validation.

## Next

Do not submit task285 `color_idx_flat_f -> color_idx_flat` again. Possible next
work:

```text
1. Try to recover part of the task285 gain while keeping TopK input float.
2. Test task118 residual table pruning isolated if no safer candidate appears.
3. Rebuild a no-ERROR low-risk batch only if current gain reaches >= 0.030.
```
