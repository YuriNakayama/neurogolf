# 20260703 case547 result - adopted task076 slot repair

Accepted Public Score:

- before: `7187.46`
- after: `7187.48`
- delta: `+0.02`

## Target

Task:

- `task076`

Near-miss:

```text
/private/tmp/neurogolf_agent_case546_C/task076_near_failed.onnx
```

Fresh gate:

```text
task076 18683 -> 18095
gain    0.031978
n_fail  4
status  INCORRECT
```

## Failure Pattern

The four failing examples were local example indices:

```text
130, 132, 157, 222
```

The candidate shifted a small number of colored cells to wrong coordinates. The
accepted baseline matched all expected outputs.

Graph diff showed that the near-miss removed the candidate column in-bounds
guard:

```text
candidate_col_in
candidate_not_in_bounds
candidate_fit_bad
candidate_bad = Or(candidate_visible_bad, candidate_fit_bad)
```

The candidate instead cast `candidate_visible_bad` directly into
`candidate_bad_i32`, so out-of-bounds clipped gathers could be selected.

## Repair Attempts

A cheaper repair was built by guarding the gathered board code with a sentinel
when `candidate_cols != candidate_cols_clip`:

```text
/private/tmp/neurogolf_case547_task076_guarded_where.onnx
```

Gate:

```text
task076 18683 -> 18536
gain    0.007899
n_fail  0
status  ok
decision bank-low-gain
```

This proves the diagnosis, but the repair adds too much cost to remain
submit-worthy. It is below the standalone `0.020` threshold and below the
micro-bundle threshold by itself.

A stronger slot repair from the parallel worker superseded the guarded-where
candidate:

```text
/private/tmp/neurogolf_agent_case547_A/slot_repairs/task076_repair_slots_2.onnx
```

Gate:

```text
task076 18683 -> 18222
gain    0.024984
n_fail  0
status  ok
functions 0
forbidden ops []
decision submit-candidate
```

## Additional Search

Existing `task076` alias/surgery artifacts were re-gated against the current
baseline. Cost-improving candidates included many high-gain variants, but all
failed local correctness. Examples:

```text
source_step3_mask -> source_step0_mask  18683 -> 17107  n_fail=266
target_red -> valid15                  18683 -> 18233  n_fail=266
source_step2_pool -> source_step1_pool 18683 -> 18233  n_fail=15
source_step1_mask -> source_step0_mask 18683 -> 18233  n_fail=15
hidden_scatter -> zero226_f            18683 -> 18291  n_fail=266
candidate_rows_clip bypass             18683 -> 18449  n_fail=220
candidate_cols_clip bypass             18683 -> 18449  n_fail=107
```

No exact `task076` candidate with gain `>=0.020` was found.

## Submission

Submitted the slot repair as a full 400-task submission.

```text
case547 full task076 slot repair
```

Kaggle result:

```text
SubmissionStatus.COMPLETE  7187.48
```

## Decision

Adopt. The public score improved from `7187.46` to `7187.48`.

The accepted ONNX was copied to:

```text
data/output/onnx/task076.onnx
```

Post-adoption self gate against the updated baseline:

```text
task076 18222 -> 18222
n_fail  0
status  ok
forbidden ops []
```

## Next Step

Continue with parallel exploration while Kaggle submissions are pending. The
next cycle should prefer either another exact near-miss repair with local gain
`>=0.015` or a bundle of exact low-risk micros whose combined local gain is
large enough to justify one submit.
