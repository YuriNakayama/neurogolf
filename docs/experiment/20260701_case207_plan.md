# case207 plan: task229 mode channel selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task229` は未着手で、baseline は `cost=240` (`params=28`, `memory=212`, `n_fail=0`)。
- 3x3 の `mode_channel` / `output_map` と mode index chain を削れれば相対改善が見込める。

## 対象

- `data/output/onnx/task229.onnx`
- 入力の channel counts から `mode` を取り、その channel の 3x3 crop を `mode_mask` として使う。
- `Where(mode_mask, mode_u8, five_u8)` で class map を作り、9-channel `Equal` と Pad で出力する。

## 仮説

実データ上で mode color が固定、または `mode_mask` が固定に近ければ、ArgMax/Slice/Where の一部を削れる。`Where` の片側固定や mode 固定を 1 点ずつ確認する。

## 検証

- temp ONNX で `mode_i32/mode_u8` の固定、`mode_mask` 固定、`output_map` 固定を probe する。
- `src.evaluate.scorer.audit_one` で `task229` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
