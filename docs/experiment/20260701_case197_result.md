# case197 result: task154 edge mask branch trim

## 結果

- 対象: `task154`
- baseline: `cost=2760`, `params=99`, `memory=2661`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

最初に scalar `zero_u8` / `two_u8` への alias を試したが、`Concat` / `Gather` の rank が合わず無効だった。続けて同 shape の uint8 initializer を追加し、各 edge mask を固定した。

| 変更 | cost | n_pass | n_fail |
|---|---:|---:|---:|
| `top_rev -> zero_const` | 2687 | 144 | 122 |
| `top_rev -> two_const` | 2687 | 0 | 266 |
| `bot3 -> zero_const` | 2658 | 144 | 122 |
| `bot3 -> two_const` | 2658 | 0 | 266 |
| `left_rev -> zero_const` | 2687 | 122 | 144 |
| `left_rev -> two_const` | 2687 | 0 | 266 |
| `right3 -> zero_const` | 2658 | 122 | 144 |
| `right3 -> two_const` | 2658 | 0 | 266 |

## 結論

上下左右の edge mask はすべて実データ上必要。固定化すれば cost は下がるが完全一致を失うため、実 ONNX は変更していない。

## 次の一手

branch 固定は失敗が続いている。次は `task077` のように同 shape の `Where(Cast(mask), value, zero)` が複数並ぶ中 cost graph で、まとめて arithmetic 化できるかを確認する。
