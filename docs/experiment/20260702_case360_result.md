# 20260702 case360 result - no current-threshold candidate

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.70
```

## Near-Miss Re-Ranking

A parallel explorer and local doc scan found that the only historical
`n_fail=1` family with original gain above `0.030` was task285, but that family
was already repaired and adopted in case354:

```text
case353 raw near miss: 25630 -> 24323, gain 0.05234, n_fail=1
case354 adopted repair: 25630 -> 24537, gain 0.04358, n_fail=0
```

From the current accepted task285 graph:

```text
24537 -> 24323 would be gain ~= 0.0088
```

That is below every standalone submit threshold, and any generic repair would
need to remove at least about 725 current cost while keeping `n_fail=0` to clear
the medium-risk gate.

Other near misses are either excluded by prior submit risk, locally incorrect
with multiple failures, or dominated by current accepted ONNX.

## Task118 Residual Check

Re-audited the strongest remaining task118 scratch variants against the current
accepted task118 baseline:

```text
current task118: 3029 params, 33547 memory, cost 36576, n_fail=0
```

Best residual exact candidate:

```text
/private/tmp/neurogolf_task118_deep4/task118_pos_keep_first_neg_greedy_pos_any_greedy.onnx
params=2550 memory=33257 cost=35807
n_fail=0 status=ok functions=0 forbidden_ops=[]
gain = ln(36576 / 35807) = 0.021249
```

This is a high-risk table-pruning family and no longer clears a standalone
threshold from the current baseline. The older `36653` variant is now worse
than current baseline, and the keep-first `37989` variants are already
superseded by case352.

## Conclusion

Skip the submit window. The useful banked material is:

```text
task118 residual table pruning: 36576 -> 35807, gain 0.021249, high-risk
task002 residual exact micro:   21089 -> 20689, gain 0.019149, medium-risk
low-risk cleanup micros:        total gain ~= 0.003061
```

Do not mix these into a submit until the combined risk-adjusted threshold is
met. Next search should prefer a fresh non-task118 structural candidate with
current-baseline gain at or above `0.030`, or a low-risk cleanup batch totaling
at least `0.030`.
