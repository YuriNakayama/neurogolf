# 20260702 case367 plan - task187 final reach-chain probe

## Context

Accepted Public Score remains `7185.78`. Case365 and case366 skipped submit
after rechecking generic surgery and two threshold-sized near misses.

For this cycle, avoid broad alias scans. Inspect one graph first, then generate
only targeted candidates.

## Target

Task187 current local cost:

```text
params=1008
memory=26525
cost=27533
```

The graph is a compact 25x25 border-reach flood fill:

```text
label_grid -> free_b/free_u8
reach_0 = Min(edge, free)
for k=1..13:
  reach_pool_k = MaxPool(reach_{k-1})
  reach_k = Min(reach_pool_k, free)
label_enclosed = Where(free, 2, label)
label_final = Where(reach_13, 3, label_enclosed)
Pad to 30x30
Equal with palette -> output
```

Prior logs show full depth is required:

```text
reach_13 -> reach_12: n_fail=4
reach_pool_* -> reach_* family: 27533 -> 26908, gain 0.022962, n_fail=4
```

## Hypothesis

The final reach step may only affect a few edge cases. Test whether a narrower
repair around the final frontier, or a selective use of `reach_pool_13` with a
cheaper guard, can restore `n_fail=0` while staying under baseline cost.

## Gate

Submit only if:

- `audit_one` for task187 reports `n_fail=0`;
- cost is below `27533`;
- relative gain clears the risk-adjusted submit-saving gate:
  - low-risk standalone `>=0.020`, or
  - medium-risk standalone `>=0.030`.

Otherwise record the result and keep `data/output/onnx` unchanged.
