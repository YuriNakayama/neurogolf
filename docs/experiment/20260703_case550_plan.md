# 20260703 case550 plan - task066 direct solver attempt

Accepted Public Score:

- `7187.48`

## Background

Case549 found no submit-ready replacement, but identified `task066` as the best
non-risk structural target:

```text
current cost 16268
196 graph nodes
best exact micro 16268 -> 16248, gain 0.001230
```

Large alias reductions for `task066` failed local correctness, so this cycle
does not continue alias surgery. It attempts a direct solver for the apparent
connector-mask rule.

## Hypothesis

`task066` appears to add color `3` orthogonal connector segments between sparse
marker structures while preserving existing colors. The current graph computes
row/column evidence and orientation normalization with many boolean reductions.
A direct formulation may reproduce the same connector mask with fewer
intermediates.

## Method

1. Analyze all train/test/arc-gen examples for cells where output differs from
   input.
2. Infer the row/column conditions that define the added color-3 segments.
3. Build one direct ONNX candidate under `/private/tmp`.
4. Gate only `task066`.
5. Submit only if `n_fail=0`, forbidden ops/functions are clean, and gain is
   `>=0.020`.

## Submit Rule

Only a full 400-task submission is allowed. If no exact standalone candidate is
found, record no-submit and bank any exact micro.
