# 20260703 case517 plan - parallel standalone submit supply

## Context

Latest accepted Kaggle score is `7187.29` from case516:

```text
ref=54270788
task354 2994 -> 2933
gain=0.020584496
n_fail=0
```

No submission has been made after case516. The immediate reason is candidate
supply: after case514's clean-looking micro bundle produced Kaggle `ERROR`, the
loop reverted to standalone candidates only, and only task354 crossed the
standalone submit threshold.

## Priority Rule

The score delta is:

```text
ln(old_cost / new_cost)
```

Therefore task priority is not absolute cost. The cycle should maximize expected
relative reduction while preserving `n_fail=0`.

## Hypothesis

Submit cadence cannot be improved by forcing weak micro bundles; that increases
Kaggle `ERROR`/regression risk and burns submits. Cadence should be improved by
raising parallel search throughput and promoting only isolated candidates with:

```text
n_fail=0
forbidden ops absent
functions absent
known-risk task absent
gain >= 0.020
```

Micro candidates remain banked, but not bundled unless a later isolated audit
identifies the exact failed case514 member and removes the runtime-risk class.

## Work Split

Run four independent workers in parallel:

```text
U1: tasks 001-100
U2: tasks 101-200
U3: tasks 201-300, focus task238 strengthening
U4: tasks 301-400, focus task354 follow-up and task383 repair
```

The parent process owns integration, gate checks, submit, result logging, DVC,
commit, and push.

## Candidate Targets

1. Strengthen `task238` from the known exact candidate:

```text
baseline=2355
known=2325
gain=0.012821
submit target <= 2308
```

2. Continue `task354` after the accepted baseline:

```text
baseline=2933
submit target <= 2874
```

3. Revisit prior accepted families only when the resulting candidate is
standalone and current-baseline exact.

## Submit Policy

To approach the refreshed submit budget target (`72` submissions over `13`
hours), use a rolling cadence:

```text
submit immediately when a standalone submit-candidate is available
otherwise continue parallel search; do not submit bank-low-only bundles
```

This saves submits by avoiding weak bundles while still allowing high-frequency
submissions if parallel workers produce enough isolated candidates.
