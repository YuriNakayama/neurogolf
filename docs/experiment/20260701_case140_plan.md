# 2026-07-01 case140 plan

## 方針

- floor: Public Score 7181.15
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task382.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task382.onnx`
- baseline: params 120, memory 5652, cost 5772, points 16.339226080106236, n_pass 266, n_fail 0

## 仮説

`task382` は `vflag` で vertical/horizontal source を作るが、後段では `vpat` と `hpat` の両方を生成し、`red`、`row_out`、`col_out` と `Max` 合成している。`vflag` や `x2` 方向によって片側 branch が実データで不要なら、5-bank の Slice/Concat/Gather を大きく削れる。

また `red` と outside sentinel (`row_out` / `col_out`) は `Where(..., zero/out255)` のマスクで作っているため、片側を省略できれば相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `vpat`、`hpat`、`red`、`row_out`、`col_out` の各 branch を個別にゼロ/既存出力へ差し替える。
2. `vsrc/hsrc` や `vbank/hbank` の方向選択も個別確認する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task382.onnx` に反映する。
4. submit し、Public Score が 7181.15 を上回れば採用、上がらなければ破棄する。
