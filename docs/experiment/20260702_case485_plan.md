# 20260702 case485 plan - submit task023 greedy S accumulator cleanup

## Context

Accepted Public Score is `7186.73`. Cases 482-484 found no submit candidate in
the main lane, but two independent workers found the same `task023` candidate.

Main re-gate:

```text
path=/tmp/neurogolf_case415_scan/task023_greedy_S_S.onnx
old_cost=9340
new_cost=8764
gain=0.063653831
n_fail=0
decision=submit-candidate
functions=0
forbidden_ops=()
```

## Target

```text
task023
baseline: data/output/onnx/task023.onnx
candidate: /tmp/neurogolf_case415_scan/task023_greedy_S_S.onnx
```

## Method

1. Copy the candidate over `data/output/onnx/task023.onnx`.
2. Run `submit.gate`/pre-submit packaging against the accepted baseline.
3. Submit the isolated one-task change.
4. Adopt only if Kaggle Public Score improves over `7186.73`; otherwise restore
   the accepted ONNX.
