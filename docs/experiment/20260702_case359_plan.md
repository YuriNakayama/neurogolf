# 20260702 case359 plan - submit-saving repair and micro bank

## Context

Accepted Public Score is `7185.70` after case358 (`b839508`). The next submit
window is open, but the current policy is to spend a submit only when the
candidate clears a risk-adjusted relative-gain gate:

```text
standalone low-risk:    >= 0.020
standalone medium-risk: >= 0.030
low-risk batch:         >= 0.030
mixed-risk batch:       >= 0.050
```

The score gain remains logarithmic:

```text
gain = ln(old_cost / new_cost)
```

so the search should favor exact relative reductions, not raw absolute cost.

## Target Set

Re-check the current low-fail and bankable candidates:

```text
task243 fill-chain shortening
task023 accumulator shortening
task002 flood frontier shortening
task077 bbox-iteration shortening
task319 predicate shortening
low-risk cleanup micros across current accepted ONNX
```

## Hypothesis

If a near-miss candidate can be repaired to `n_fail=0` without reintroducing
the removed tensor chain, it may clear a standalone threshold. Otherwise, exact
micro candidates should be banked and submitted only after the accumulated batch
clears the submit-saving threshold.

## Gate

Do not modify accepted ONNX unless a candidate satisfies all of:

1. `evaluate.audit_one` on the changed task has `status=ok` and `n_fail=0`.
2. No forbidden operators are introduced:

```text
Loop, Scan, NonZero, Unique, Script, Function, Compress
```

3. The expected gain clears the relevant submit-saving threshold.
4. For task002 bit-topology changes, treat as at least medium risk because
   case97 showed local `n_fail=0` can still regress Public Score.
