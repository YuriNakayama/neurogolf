# 20260701 case266 plan - task310 zero-offset crop start probe

## 背景

- Current floor: Public Score `7181.63`
- `task310` baseline: `cost=3051` (`params=45`, `memory=3006`, `n_fail=0`)
- case250 の次候補に `task310` が挙がっていた。小〜中 cost で、shape/crop 周辺に削れそうな中間がある。

## 対象

- `data/output/onnx/task310.onnx`

## 仮説

`task310` は crop 開始位置を以下で作っている。

```text
hext = cnt * c025
side_f = hext + c1
side_i = Cast(side_f)
half_h = hext * c0
mean_r = rsum / cnt
rstart_f = mean_r - half_h
mean_c = csum / cnt
cstart_f = mean_c - half_h
```

`half_h` が実質ゼロオフセットなら、`rstart_f` / `cstart_f` を `mean_r` / `mean_c` へ直結し、
`half_h` と2つの `Sub` を削れる。

## 手順

1. `/tmp` に `task310` の `rstart_f -> mean_r`, `cstart_f -> mean_c` 版を生成する。
2. `src.evaluate.scorer.audit_one` で `task310` の完全一致と cost を確認する。
3. `n_fail=0` かつ cost 減なら本体へ反映して submit する。
