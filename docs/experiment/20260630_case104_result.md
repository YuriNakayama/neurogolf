# 20260630 case104 result - t285 color_pad Pad 化は Public 不変で不採用

## 実装候補
`task285` の序盤で `color_idx_flat` `[900]` に `bottom_zero_row` `[30]` を付けて `color_pad_flat` `[930]` を作る `Concat` を、suffix zero `Pad` に置換した。

```text
before: Concat(color_idx_flat, bottom_zero_row) -> color_pad_flat
after : Pad(color_idx_flat, pads=[0,30], value=0) -> color_pad_flat
```

## faithful 検証
- before: cost 25630, params 550, memory 25080, n_pass 265, n_fail 0
- after: cost 25602, params 522, memory 25080, n_pass 265, n_fail 0
- 削減: cost -28

## submit
- ref: `54193096`
- message: `case104 t285 replace color_pad concat with pad cost 25630 to 25602`
- status: `COMPLETE`
- Public Score: **7180.95**（case100 floor から変化なし）

## 採否
- **不採用**。
- local faithful かつ cost -28 だが、実 Public Score が上がらなかった。
- `task285.onnx` は DVC checkout で baseline に戻した。`task066` の case100 改善は保持されている。

## 結論
suffix zero `Concat` の `Pad` 化は有効だが、memory が減らない params-only 小削減は Public 表示に反映されにくい。case100 のように intermediate memory も消せる構造を優先すべき。

## 次の一手
- `task367` / `task349` / `task338` で、params-only ではなく intermediate memory を削れる候補を探す。
