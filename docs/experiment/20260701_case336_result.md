# 20260701 case336 result - task055 row/column alias bundle

## Decision

Adopted.

## Local Gate

Only `task055.onnx` was changed and audited.

```text
baseline:  params=277 memory=3060 cost=3337 n_fail=0 status=ok
candidate: params=276 memory=2940 cost=3216 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(3337 / 3216) ~= 0.0369 pt
```

## Submission

```text
ref=54233756
description=case336 task055 row column alias cost 3337 to 3216
status=COMPLETE
publicScore=7185.05
```

Previous accepted floor:

```text
7185.01
```

The Public Score improved by `+0.04`, so the candidate is accepted.

## Diff Summary

The accepted change rewires `rbC` to `rb` and `cbC` to `cb`, then prunes the
redundant row/column alias intermediates.

## Next Step

The remaining exact candidates from this scan are micro-level. Continue fresh
equivalence and structure scans for `ln(old/new) > 0.02`, while preserving the
small verified candidates in `micro_backlog.md` for future bundling.
