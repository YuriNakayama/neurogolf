# 20260703 case514 result - reject clean banked micro bundle error

## Result

Reject.

Kaggle submission:

```text
ref=54270098
status=ERROR
Public Score: unchanged at 7187.27
```

## Submitted Bundle

```text
task018 38681 -> 38575 gain 0.002744
task092  6872 ->  6807 gain 0.009504
task096  8798 ->  8758 gain 0.004557
task263   680 ->   679 gain 0.001472
total gain: 0.018276
decision  : submit-micro-bundle
```

All four members were locally exact:

```text
n_fail=0
status=ok
functions=0
forbidden_ops=[]
```

Dry-run packaging and the pre-submit bundle gate both succeeded.

## Rejection

Kaggle returned `SubmissionStatus.ERROR`, so the bundle is rejected regardless
of local exactness. The working ONNX directory was restored from:

```text
/tmp/neurogolf_case514_baseline_onnx
```

Post-restore gate against that baseline:

```text
changed=0
decision=blocked-no-changes
```

## Gate Update

Because this was a bundle ERROR and the offending member cannot be identified
from the public result, all changed members are marked review-risk:

```text
task018
task092
task096
task263
```

Do not resubmit this family as a blind micro-bundle. Any future attempt must
isolate one task at a time with a specific runtime-safety explanation.

## Conclusion

The micro-bundle threshold is necessary but not sufficient. Case514 shows that
locally exact banked micros can still fail Kaggle validation as a bundle. The
next cycle should return to standalone `submit-candidate` search or isolate one
case514 member only if there is a clear reason to spend a submit.
