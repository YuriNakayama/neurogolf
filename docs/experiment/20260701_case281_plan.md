# 20260701 case281 plan - task191 column score alias

## Context

- Current adopted Public Score floor: `7181.93`
- Latest adopted cycle: case280 `task209` bundled aliases (`32052 -> 31187`)
- Priority rule: choose candidates by expected relative reduction, not absolute cost.
- `task191` current local audit:
  - `params=125`
  - `memory=14844`
  - `cost=14969`
  - `n_fail=0`

## Target

- `data/output/onnx/task191.onnx`

## Hypothesis

`task191` contains two same-shape column predicate tensors:

```text
col_score_first -> col_has
```

The existing `col_has` tensor is already sufficient for all valid examples where
`col_score_first` is consumed. Replacing the latter with the former should prune
the now-unused scoring branch while preserving exact outputs.

## Expected Gain

- Baseline: `14969`
- Candidate from temporary audit: `14854`
- Relative reduction: `14969 / 14854 = 1.0077`
- Expected point gain: `ln(14969 / 14854) ~= 0.0077`

This is smaller than the recent adopted bundles, but larger than previously
rejected one-parameter changes and may still move the rounded Public Score.

## Verification Plan

1. Apply only `col_score_first -> col_has`.
2. Dead-prune unused nodes and initializers.
3. Run individual `audit_one` for `task191` with
   `data/lake/neurogolf-2026/task191.json`.
4. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
