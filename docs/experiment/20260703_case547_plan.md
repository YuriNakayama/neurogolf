# 20260703 case547 plan - repair task076 near miss

Accepted Public Score:

- `7187.46`

## Background

Case546 found no submit-ready exact candidate after `task023` adoption. Broad
cost-only branch/guard bypass candidates mostly failed local correctness.

The most promising near-miss from the post-task023 search is:

```text
task076 gain ~= 0.031978
n_fail = 4
```

This is above the standalone submit threshold if it can be repaired to
`n_fail=0`. Unlike several other near-misses, `task076` is not listed in the
current hard/review/runtime risk sets.

## Hypothesis

The `task076` near-miss may be recoverable with a small exception or by using a
slightly narrower alias than the failing candidate. Because only four examples
fail locally, a targeted repair may keep most of the relative gain while
restoring correctness.

## Method

1. Re-gate the current `task076` near-miss against accepted baseline and record
   old/new cost, gain, and failing examples.
2. Compare baseline/candidate outputs on the four failures.
3. Inspect the candidate graph around the bypassed tensors and build a bounded
   repair or narrower variant.
4. Gate only `task076`.
5. If repaired candidate has `n_fail=0` and gain `>=0.020`, build a full
   400-task submission directory from accepted baseline and overwrite only
   `task076`.

## Submit Rule

Submit only if:

```text
task076 n_fail=0
gain >= 0.020
forbidden ops = []
functions = 0
```

Never submit a partial one-file directory.
