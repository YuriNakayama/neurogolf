# 20260702 case378 plan - task310 crop memory probe

## Context

Accepted Public Score is `7185.86` after case369. Cases 370-377 did not submit:
they either found stale micro cleanups or exact gains far below the
submit-saving threshold.

The metric is logarithmic:

```text
points = max(1, 25 - ln(max(1, cost)))
cost   = params + memory_bytes
gain   = ln(old_cost / new_cost)
```

So priority is relative reduction, not absolute cost. The next submit should
spend a Kaggle attempt only if a single-task candidate clears the risk-adjusted
threshold, or if low-risk exact micros have accumulated enough current-baseline
gain.

## Target

`task310`

Current local audit:

```text
params 45
memory 3006
cost 3051
points 16.97677531528333
n_fail 0
```

The largest non-output tensor is:

```text
crop FLOAT [1,10,8,8] = 2560 bytes
```

This dominates the measured memory. A faithful rewrite that removes or narrows
`crop` could clear the standalone low-risk threshold. By contrast, the known
case266 zero-offset crop-start edit is rejected and will not be retried.

## Hypothesis

The only plausible threshold-sized win is around the final `Slice -> Pad` path.
Probe whether `crop` can be narrowed or avoided without adding a larger
temporary tensor:

- Cast `crop` to `BOOL` or `UINT8` before `Pad` and set output dtype
  accordingly.
- Try output dtype narrowing only if ONNX Runtime accepts the path and
  `audit_one` remains exact.
- Inspect whether the `Slice -> Pad` can be expressed with a cheaper allowed
  static-shape operator chain. Do not introduce banned ops:
  `Loop`, `Scan`, `NonZero`, `Unique`, `Script`, `Function`, `Compress`.

## Gate

Submit only if:

```text
n_fail = 0
gain >= 0.020 for low-risk graph surgery
```

If the best exact candidate is below the threshold, bank it in
`micro_backlog.md` and skip Kaggle submit. The 30-minute cadence is a maximum
submission frequency, not a reason to spend a weak submit.
