# 20260701 case315 result - adopt task174 inside-mask alias

## Result

- Decision: adopt
- Kaggle submission ref: `54229401`
- Previous Public Score: `7183.22`
- New Public Score: `7183.26`
- Public delta: `+0.04`

## Local Gate

`task174.onnx` after the change:

```text
params=178
memory=8800
cost=8978
n_pass=266
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=178
memory=9148
cost=9326
n_pass=266
n_fail=0
status=ok
```

Local cost reduction:

```text
9326 -> 8978
old_cost / new_cost = 1.0388
ln(old/new) ~= 0.0380 pt
```

## Change

Rewired the mismatch-count input:

```text
mismatch_inside_43 -> mismatch_42
```

This pruned the dynamic inside-mask chain:

```text
height_15 -> Unsqueeze -> row_inside_37
width_17  -> Unsqueeze -> col_inside_39
And(row_inside_37, col_inside_39) -> inside_40
And(mismatch_42, inside_40) -> mismatch_inside_43
```

`height_15` and `width_17` remain live for later shape selection.

## Verification

Individual `evaluate.audit_one` for `task174` passed with `n_fail=0`.
The Kaggle Public LB improved to `7183.26`, so the local exact alias transferred.

## Conclusion

Adopted. The change is another successful same-task predicate-chain deletion.
The current short-term backlog is now depleted of submit-worthy candidates above
the practical gate, so the next cycle should run a fresh structural scan for
same-shape alias bundles or source-crop rewrites.
