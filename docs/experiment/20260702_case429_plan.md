# 20260702 case429 plan - task080 line-mask residual probe

## Context

Case428 rejected `task202`: high-gain branch aliases failed local correctness,
and exact variants were no-gain. The next current-graph candidate is `task080`,
which has prior accepted reductions and still carries full-grid line/mask
intermediates:

```text
task080 params=107 memory=15543 cost=15650 n_fail=0
```

One full `30x30` tensor is `900` cost, giving a potential gain of about `0.059`
if removed.

## Hypothesis

The current line/separator path is:

```text
islineg   = Equal(cgu, linecu)
outg      = Not(ingrid)
lineOrOut = Or(islineg, outg)
islineu   = Cast(lineOrOut)
rowlineu  = ReduceMin(islineu, axis=3)
...
withsep   = Where(lineOrOut, linecu, exp)
fu        = Where(ingrid, withsep, sentu)
```

If `ReduceMin` can consume bool directly in this runtime, or if a residual
terminal mask can be fused without materializing `islineu` or `withsep`, the
change could clear the submit threshold.

## Method

1. Review prior `task080` accepted/rejected aliases.
2. Test only local residual rewrites around `lineOrOut/islineu/withsep/fu`.
3. Run `submit gate` for any candidate.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
