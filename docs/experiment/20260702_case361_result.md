# 20260702 case361 result - adopt task367 rect kernel crop

## Submission

```text
ref=54244710
description=case361 task367 rect kernel crop cost 21549 to 20699
status=COMPLETE
Public Score=7185.74
```

Previous accepted Public Score:

```text
7185.70
```

Result:

```text
+0.04 Public Score
adopt
```

## Local Gate

Changed task only:

```text
task367.onnx
params=3899
memory=16800
cost=20699
n_pass=266
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=4749
memory=16800
cost=21549
n_fail=0
```

Expected local gain:

```text
ln(21549 / 20699) = 0.040244
```

Ops / validity:

```text
ops: Cast, Concat, QLinearConv, Slice, Sub
functions=0
forbidden_ops=[]
```

`submit validate --onnx-dir data/output/onnx` succeeded before submission.

## Change

Replaced:

```text
rect_w shape: (25, 2, 9, 9) -> (25, 2, 8, 8)
QLinearConv pads: [2, 2, 6, 6] -> [2, 2, 5, 5]
```

This keeps the top-left anchored rectangle detector geometry while removing
850 dense initializer elements from `rect_w`.

Source scratch candidate:

```text
/tmp/neurogolf_case361_task367/rect_tl_8x8.onnx
```

## Conclusion

Adopt case361. This validates parameter-side kernel cropping on the post-case289
task367 graph. Continue looking for current-baseline structural reductions with
`gain >= 0.030`, especially dense kernels where top-left or bottom-right zero
support can be cropped without adding compensating tensors.
