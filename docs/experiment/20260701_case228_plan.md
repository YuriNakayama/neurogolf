# case228 plan: task147 scatter index trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task147` baseline は `cost=532` (`params=100`, `memory=432`, `n_fail=0`)。
- graph は `Slice(channel3 6x6) -> Conv(2 filters) -> ScatterElements`。
- `scatter_indices` は shape `(1,2,6,6)` だが、channel 0 はすべて 3、channel 1 はすべて 8。

## 対象

- `data/output/onnx/task147.onnx`

## 仮説

`scatter_indices` の 72 要素は実質 2 定数なので、broadcast 可能な shape や `Expand` なしの小 initializer に置き換えられれば params を大きく削れる。Conv の 2 output channel が片側不要ならさらに削れるが、まず shape を保った index 圧縮を優先する。

## 検証

- temp ONNX で `scatter_indices` を `(1,2,1,1)`、`(2,1,1)`、scalar variants に縮小して型制約と cost を確認する。
- Conv output channel 0/1 の zero/duplicate/1 channel 化を probe する。
- `src.evaluate.scorer.audit_one` で `task147` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
