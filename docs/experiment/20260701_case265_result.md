# 20260701 case265 result - reject task281 direct uint8 bbox axes

## 結果

- 対象: `data/output/onnx/task281.onnx`
- 採否: reject
- submit: なし（足切りで `n_fail>0`）

## 試行内容

以下の二重 Cast を直接 uint8 化して、30 要素 bool 中間 2 本を削る仮説を検証した。

```text
row_nonbg -> Cast(bool) -> bbox_rows_sparse -> Cast(uint8) -> bbox_rows_u8
col_nonbg -> Cast(bool) -> bbox_cols_sparse -> Cast(uint8) -> bbox_cols_u8
```

候補では `bbox_rows_u8` / `bbox_cols_u8` の Cast 入力を、それぞれ `row_nonbg` / `col_nonbg`
へ直接つなぎ、`bbox_rows_sparse` / `bbox_cols_sparse` の Cast ノードを削除した。

## 足切り

baseline:

```text
status ok
params 65
memory 2321
cost 2386
points 17.22262639734214
n_pass 266
n_fail 0
filesize 4219
```

candidate:

```text
status INCORRECT
params 65
memory 2321
cost 2386
points 0.0
n_pass 0
n_fail 266
filesize 4109
```

`row_nonbg` / `col_nonbg` は単純な 0/1 presence ではなく、bool 化が ArgMax 前の正規化として
必要だった。直接 uint8 cast では全例不一致になった。

## 横展開

同じ二重 Cast パターンを全 task で機械検証した。`n_fail=0` で残ったのは `task377` の
`cost 8373 -> 8372` だけで、相対削減率が小さすぎるためこのサイクルでは採用しない。

## 結論

`task281` は足切り失敗のため変更しない。二重 Cast 除去は、case264 のように元値が既に真偽値に
限定される場合だけ有効で、presence 正規化を兼ねる Cast には適用できない。

## 次の一手

Cast 横展開は一旦止める。次は未使用 table row、Pad/Slice の shape 固定、または 30x30 以上の
中間を丸ごと消せる branch を、`old_cost / new_cost` 見込みが 1.01 以上の task に絞って探す。
