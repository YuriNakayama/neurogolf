# 20260630 case81 plan — t173 の `topk_pixels` を削減できるか検証

## 仮説
t173 は cost 17646（params 112 / memory 17534）で、`TopK(grid_score, topk_pixels=19)` が
大きな可変候補列を作る。もし arc-gen 上 19 未満で足りるなら、memory を約146B/slot 削れる。

## 対象
- task173
- 現行: cost 17646, params 112, memory 17534, n_fail 0

## 手法
1. train/test/arc-gen の nonzero pixel 数と変更 pixel 数を確認。
2. `topk_pixels` を 18,17,... と下げる probe を作成。
3. `audit_one(run_correctness=True)` で faithful と cost を確認する。
