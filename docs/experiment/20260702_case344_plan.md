# 20260702 case344 plan - task187 sentinel label conv

## Context

- Current accepted Public Score floor: `7185.13` after case342.
- Case343 skipped submission because low-risk cleanup batch gain was below
  threshold.
- Submit-saving policy allows immediate use of the next submit window for an
  isolated threshold-clearing candidate.

## Candidate

Target `task187`.

Fold the separate label and occupancy computation into one sentinel-label
`Conv`:

- Current graph computes `label_u8_25`, `grid_b_25`, then
  `label_grid_25 = Where(grid_b_25, label_u8_25, sentinel_u8)`.
- Candidate uses one sparse biased convolution where valid one-hot cells emit
  labels `0..9` and zero-vector padded cells emit sentinel `10`.
- Then derive:
  - `free_b_25 = Equal(label_grid_25, 0)`
  - `not_grid_b_25 = Equal(label_grid_25, 10)`

Expected local change:

```text
task187: params/memory/cost 1367/30900/32267 -> 1008/26525/27533
gain = ln(32267 / 27533) ~= 0.1587
```

This clears the medium-risk standalone threshold by a wide margin.

## Gate

1. Copy `/tmp/neurogolf_task187_deep/task187_sentinel_label_conv.onnx` to
   `data/output/onnx/task187.onnx`.
2. Run `src/evaluate.audit_one` only for `task187`.
3. Submit if `n_fail=0` and cost is lower.
4. Adopt only if Kaggle Public Score improves over `7185.13`; otherwise restore
   the DVC baseline and record rejection.
