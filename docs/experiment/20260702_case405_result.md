# 20260702 case405 result - task161 plus task076 alias bundle

## Submission

- Kaggle ref: `54254601`
- Message: `case405 task161 task076 alias bundle gain 0.02270`
- Public Score: `7186.56 -> 7186.58`
- Decision: adopted

## Local Gate

```text
task161: cost 3729 -> 3657, gain 0.019496962029
         n_pass=266, n_fail=0, functions=0, forbidden=[]
task076: cost 18743 -> 18683, gain 0.003206329899
         n_pass=266, n_fail=0, functions=0, forbidden=[]
```

Combined current-relative gain:

```text
0.02270329192858707
```

## Change

- `task161`: bypassed `tl9 = Add(tc0, lc0)` by feeding `tc0` into the
  downstream `base9 = Add(..., hundred)` consumer.
- `task076`: removed the selected board row/column `Clip` pair before the
  hidden scatter index path.

## Conclusion

Adopted. The bundle moved Public Score by `+0.02`, confirming that small exact
changes can still be accumulated when the combined relative gain clears the
practical submit gate.

Next cycle should continue to avoid standalone micros below the display floor,
but keep banking them as filler for isolated candidates near `gain >= 0.02`.
