# 20260701 case306 plan - task216 crop intermediate reduction

## Target

`task216.onnx`

Current local cost scan:

- params: 87
- memory: 9040
- cost: 9127
- required 2% target: `< 8949`
- preferred 5% target: `< 8693`

## Priority rationale

`task216` has low params but high memory, so the only worthwhile path is to
remove or shrink one or two large intermediates. This matches the high-ratio
policy better than byte-level cleanup.

## Hypothesis

The task appears to select/crop a relevant nonzero rectangle from a larger grid.
The current graph reportedly materializes large crop/cast intermediates. If one
full-plane cast or crop tensor can be removed while preserving exact output
shape and content, the reduction can exceed the 2% gate.

## Method

1. Inspect train/test/arc-gen examples to identify the crop rule.
2. Decompose current graph memory.
3. Only edit `task216.onnx` if a local surgery has a clear expected saving above
   183 cost and avoids dynamic-shape or private-LB risk.
4. Audit only `task216.onnx`; submit only if `n_fail=0` and cost is below 8949.
