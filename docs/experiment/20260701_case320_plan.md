# 20260701 case320 plan - task274 packed channel pad

## Context

- Current accepted Public Score floor: `7183.39` after case319.
- Candidate source: parallel scan for the case318/case319 packed channel
  projection pattern.
- Priority rule: `n_fail=0` first, then relative reduction. This candidate has
  `old_cost / new_cost ~= 1.277`, the strongest current queued ratio.

## Target

- `data/output/onnx/task274.onnx`

## Baseline

Current local audit:

```text
params=80
memory=146
cost=226
n_fail=0
status=ok
```

## Hypothesis

The tail currently builds a 9-channel 3x3 tensor with seven explicit zero
channels, then pads to the 10-channel 30x30 output:

```text
Concat(out0_small, zero_small x7, out8_small) -> small_output
Pad(small_output, pads_final) -> output
```

Only two live channels are needed. Replace this with:

```text
Concat(out0_small, out8_small) -> packed_live
QLinearConv(packed_live, one-hot 2ch->10ch weights, pads=[0,0,27,27]) -> output
```

The projection maps:

```text
output ch0 <- out0_small
output ch8 <- out8_small
all other output channels <- 0
```

and performs the bottom/right zero pad in the same node.

## Expected Gain

Temporary audit:

```text
params=94
memory=83
cost=177
n_fail=0
status=ok
```

Expected point gain:

```text
226 / 177 = 1.2768
ln(old/new) ~= 0.2444 pt
```

## Verification Plan

1. Apply only the tail replacement.
2. Run individual `evaluate.audit_one` for `task274`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
