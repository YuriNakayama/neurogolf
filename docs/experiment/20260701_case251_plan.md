# 20260701 case251 plan — task070 column gap補正 removal probe

## 現状
- floor: Public Score `7181.59`
- 足切り: `task070.onnx` の `audit_one` で `n_fail=0`。
- 優先基準: `old_cost / new_cost`。小 task でも 1 branch を消せれば比率改善があるため確認する。

## 対象
- `task070.onnx`
- baseline: `params=31`, `memory=2977`, `cost=3008`, `n_fail=0`

## 仮説
`task070` は channel 8 の 17x17 crop から row/col mask を作り、隠れた rectangle を green で埋める。唯一の特殊処理として `public_gap2` を col2 に `ScatterElements` して `col_mask` を補正している。

```text
cols13 = Gather(col_mask_raw, [1,3])
gap2 = ReduceMin(cols13)
col_mask = ScatterElements(col_mask_raw, col=2, gap2)
```

この補正が local/arc-gen 上で不要なら、ScatterElements と小さい中間を削れる。

## 手法
1. `/tmp` で `col_mask -> col_mask_raw` に差し替えて補正を削除する。
2. `gap2 -> cols13` の形も参考 probe する。
3. `audit_one` で `n_fail=0` かつ cost 減なら本体に反映して submit する。
4. fail が出る場合は提出しない。
