# case216 result: task379 unused initializer prune

## 結果

- 対象: `task379`
- baseline: `cost=10774` (`params=1274`, `memory=9500`, `n_fail=0`)
- 変更候補: 未使用 initializer `neg_one_f` (`float16[1]`) を削除
- 変更後ローカル: `cost=10773` (`params=1273`, `memory=9500`, `n_fail=0`)
- submit ref: `54210001`
- Public Score: `7181.21`
- 採否: 不採用

## 監査結果

```text
baseline: params=1274 memory=9500 cost=10774 n_pass=266 n_fail=0
pruned:   params=1273 memory=9500 cost=10773 n_pass=266 n_fail=0
```

## 採否

未使用 initializer 削除は意味保存でローカル cost も 1 下がったが、実 Public Score は floor `7181.21` から上がらなかった。微小削減は Public 表示に反映されないため不採用とし、`data/output/onnx/task379.onnx` と `data/output/onnx.dvc` は baseline に戻した。

## 次の一手

1 要素級の params 削減は単独 submit しない。次は `cost < 500` でも 10%以上の相対削減が見込める graph 構造、または中コスト task の dtype/shape 削減を優先する。
