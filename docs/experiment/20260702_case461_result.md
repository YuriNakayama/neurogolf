# 20260702 case461 result - task204 detector branch re-gate

## Summary

No submit.

`task204` still has large cost-reducing detector branch aliases, but they all
fail local correctness. The only exact candidate is a one-byte initializer
cleanup, far below the submit threshold.

## Baseline

```text
task204
params=454
memory=11040
cost=11494
points=15.6504195609
n_pass=268
n_fail=0
status=ok
```

## Gate Result

```text
candidates=146
positive_cost_reductions=41
errors=0
exact_positive=1
blocked=40
exceptions=0
```

Exact low-gain candidate:

```text
11494 -> 11493
gain=0.000087006
n_fail=0
decision=bank-low-gain
path=/private/tmp/neurogolf_case410_alias_scan_200_400/task204_7_QLinearConv_in4_det_w_scale_6.onnx
```

Representative blocked candidates:

```text
11494 -> 9396   gain=0.201541  n_fail=218
  red_core -> fill_core_10

11494 -> 9408   gain=0.200265  n_fail=216
  red_core -> fill_core_8

11494 -> 9480   gain=0.192641  n_fail=78
  red_core -> fill_core_4

11494 -> 9660   gain=0.173832  n_fail=256
  orange_core -> fill_core_9

11494 -> 9680   gain=0.171763  n_fail=253
  orange_core -> fill_core_7

11494 -> 9716   gain=0.168051  n_fail=243
  orange_core -> fill_core_5
```

The current scratch pool mostly replays the case239 conclusion: detector branch
shortcuts can save meaningful cost but remove required red/orange fill
semantics.

## Decision

Rejected for submission.

No candidate met:

```text
n_fail=0
gain >= 0.020
```

The one exact candidate is a low-risk micro but its gain is too small for a
standalone submit and should only be retained as backlog material.

## Next

Move to the next normal-risk target. `task340` has prior adopted low-gain guard
aliases and many current scratch candidates; however, its exact remaining
material may already be exhausted. `task379` has only known micro cleanup, so
prefer a fresh task such as `task074`, `task350`, or a mid-cost selector task if
`task340` re-gate also fails.
