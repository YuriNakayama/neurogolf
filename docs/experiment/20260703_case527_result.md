# 20260703 case527 result - no-submit task379 failure diff

## Result

No submit.

Current accepted score remains:

```text
Public Score: 7187.38
latest accepted ref=54272777
```

## Failure Diff

The task379 near miss was rechecked:

```text
candidate=/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Where_color20_TO_color20_c_400.onnx
old_cost=10753
new_cost=10353
gain=0.037908
n_fail=2
```

Direct baseline-vs-candidate prediction diff showed both failures are vertical
separator cases:

```text
train[1] shape=(13, 18), vertical separators at cols 3 and 14
test[0]  shape=(17, 19), vertical separators at cols 4 and 12
```

The candidate replaces:

```text
color20 = Where(h_any, color20_c, color20_t)
```

with:

```text
color20_c
```

So it works for horizontal-orientation cases but loses the transpose-side output
needed for the two vertical cases.

## Repair Attempts

Using `color20_t` directly was not viable:

```text
candidate=/tmp/neurogolf_case527_task379_pad_color20_t.onnx
cost=10753
n_fail=264
```

Restoring the original `Where(h_any, color20_c, color20_t)` repaired
correctness but removed all savings:

```text
candidate=/tmp/neurogolf_case527_task379_restore_where.onnx
old_cost=10753
new_cost=10753
gain=0
n_fail=0
decision=blocked-no-gain
```

## Decision

Reject the task379 near miss for now. The entire 400-cost gain comes from
removing the required `color20` orientation-select tensor, and the minimal exact
repair returns to baseline cost.

Next cycle should not spend more time on this specific `color20` bypass. Prefer
filtered exact micro replenishment or a different low-fail near miss whose
repair does not restore the deleted high-memory tensor.

