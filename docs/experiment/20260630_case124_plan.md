# 2026-06-30 case124 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task358.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task358.onnx`
- baseline: params 174, memory 4312, cost 4486, n_pass 265, n_fail 0

## 仮説

`task358` は入力面積から width/height を決め、代表 row/col を周期復元して交差線を描く。row/col の片側、period 計算、または final valid mask が冗長なら、中間テンソルを削って数 % の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `inside_ids`、`row_line`、`col_colors_grid`、`valid`、period/phase 計算を簡略化する。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task358.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
