# case202 result: task186 tiny patch threshold trim

## 結果

- 対象: `task186`
- baseline: `cost=103`, `params=23`, `memory=80`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

3x3 patch selector と threshold mask を temp ONNX で固定化した。

| 変更 | cost | n_pass | n_fail | status |
|---|---:|---:|---:|---|
| `patch -> red_const` | 62 | 0 | 267 | `INCORRECT` |
| `patch -> black_const` | 62 | 0 | 267 | `INCORRECT` |
| `red -> true` | 59 | 0 | 267 | `INCORRECT` |
| `red -> false` | 59 | 0 | 267 | `INCORRECT` |
| `black_count -> color_counts` | n/a | 0 | 267 | shape error |

## 結論

`task186` の threshold から作る 3x3 red mask は全例で必要。固定 patch や固定 mask は完全一致せず、実 ONNX は変更していない。

## 次の一手

短い `Where` graph の固定化は続けて失敗している。次は `task231` の gather index 生成のように、small vector branch を削れるか確認する。
