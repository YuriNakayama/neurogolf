# case200 result: task006 tiny mask selector trim

## 結果

- 対象: `task006`
- baseline: `cost=153`, `params=27`, `memory=126`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

左右 3x3 mask の `And` と small output の固定化を temp ONNX で確認した。

| 変更 | cost | n_pass | n_fail |
|---|---:|---:|---:|
| `red_mask -> left_blue` | 93 | 19 | 247 |
| `red_mask -> right_blue` | 93 | 17 | 249 |
| `small_output -> red_const` | 63 | 0 | 266 |
| `small_output -> black_const` | 63 | 27 | 239 |

## 結論

`task006` は短い graph だが、左右 mask の AND と red/black selector が正答性に必要。実 ONNX は変更していない。

## 次の一手

小型 task の branch 固定は不発。次は `task067` / `task186` など、短い `Where` graph で selector が入力 shape と強く結びつくものを同様に確認する。
