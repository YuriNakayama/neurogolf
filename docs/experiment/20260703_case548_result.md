# 20260703 case548 result - no submit after parallel repair search

Accepted Public Score:

- `7187.48`

## Summary

No Kaggle submit was made in this cycle. Parallel workers were increased and
searched several standalone-gain near-misses, but every exact repair either
returned to baseline cost or remained locally incorrect.

This was a candidate-supply failure, not a Kaggle/API issue. Submitting any of
these artifacts would violate the `n_fail=0` cutoff or repeat known hidden-risk
patterns.

## Parallel Search Results

Primary targets:

```text
task364 23013 -> 22133  gain 0.038990  n_fail 21
task324  8598 ->  8368  gain 0.027115  n_fail 61
task154  2760 ->  2693  gain 0.024575  n_fail 63
```

Repair outcomes:

```text
task364 exact repair -> 23013  gain 0.000000  n_fail 0
task324 exact repair ->  8598  gain 0.000000  n_fail 0
task154 exact repair ->  2760  gain 0.000000  n_fail 0
```

The removed conditions were not redundant:

- `task364`: `PB7 = MaxPool(PB6 * Gu)` was needed for arc-gen binary cases.
- `task324`: dropped `row_evidence` / fallback logic was needed.
- `task154`: dropped bottom guide conditional branch was needed.

Additional near-misses:

```text
task343 1895 -> 1847  gain 0.025656  n_fail 13
task343 exact repair -> 1895  gain 0.000000  n_fail 0

task243 22650 -> 22002  gain 0.029026  n_fail 1
task243 exact repairs -> worse than baseline or baseline restore
```

`task243` remains known-blocked and was not considered submit-ready.

## Micro Bank

Low-risk exact micros found by the parallel worker:

```text
task368 gain 0.001286
task117 gain 0.000724
task333 gain 0.000620
task234 gain 0.000585
task358 gain 0.000455
task388 gain 0.000401
task328 gain 0.000290
task361 gain 0.000209
task005 gain 0.000145
```

The best max-5 bundle is about `0.0039`, below the `0.015` micro-bundle submit
threshold, so it was banked but not submitted.

## Decision

No submit.

Reasons:

- no non-risk standalone candidate with `n_fail=0` and gain `>=0.020`;
- no current-baseline micro bundle with total gain `>=0.015`;
- repaired candidates that became exact had no retained cost gain;
- retained-gain candidates still failed local correctness.

## Operational Change

The previous stall was caused by serial candidate supply and pending/result
ambiguity. From this cycle onward:

- main process handles Kaggle status, full-400 packaging, dry-run, submit, and
  adoption/rejection;
- subagents continuously generate and repair candidates in parallel;
- every 10-15 minutes is a submit decision point, not an unconditional submit;
- submit only on a full 400-task package after individual gate/dry-run.

## Next Step

Shift from near-miss repair to new structural solver/cost replacement work.
The repair attempts show many high-gain aliases are deleting essential logic,
so the next cycle should look for a task where the accepted solver is
structurally overbuilt and can be replaced by a simpler exact formulation,
rather than trying to patch an unsafe alias.
