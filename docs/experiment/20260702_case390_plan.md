# 20260702 case390 plan - next low-risk banked batch

## Context

Accepted Public Score is `7186.15` after case389 adopted a four-task
low-risk micro batch. To respect the submit-saving cadence, prepare and audit
the next batch now, but avoid another immediate submit unless the 30-minute
window has arrived or a materially stronger candidate appears.

## Target

Re-audit the next banked low-risk set:

```text
task046  2318 -> 2286  gain 0.013901
task342  1585 -> 1576  gain 0.005694
task037  4746 -> 4720  gain 0.005493
task392  1951 -> 1941  gain 0.005139
```

Expected combined gain:

```text
0.030227
```

This barely clears the low-risk batch gate `>= 0.030`, so any failed re-audit
or stale baseline means no submit.

After the first gate passed, also re-audit small low-risk add-ons while waiting
for the submit cadence:

```text
task035 2262 -> 2255 gain 0.003099
task308 1488 -> 1484 gain 0.002692
task293 1288 -> 1286 gain 0.001554
task397 2848 -> 2845 gain 0.001054
```

If these pass, the combined batch gain becomes about `0.038626`, giving more
cushion without spending another submit.

## Method

Use the prior exact scratch candidates and re-run `audit_one` for only the
changed tasks on the current accepted bundle:

```text
task046: /tmp/neurogolf_case385_producer/task046_bundle_1.onnx
task342: /tmp/neurogolf_case383_task342_bundle_all.onnx
task037: /tmp/neurogolf_case385_producer/task037_bundle_1.onnx
task392: /tmp/neurogolf_case385_371_400/task392_30_Unsqueeze_in0.onnx
task035: /tmp/neurogolf_case385_producer/task035_bundle_2.onnx
task308: /tmp/neurogolf_case382_main/task308_38_And_in1.onnx
task293: /tmp/neurogolf_case382_main/task293_29_Squeeze_in0.onnx
task397: /tmp/neurogolf_case357_low_alias/task397_m4_TO_three_new.onnx
```

## Gate

Submit only if:

- all four tasks have `n_fail=0`
- no forbidden ops and no ONNX functions
- combined current-relative gain is still `>= 0.030`
- submit cadence is acceptable relative to the last Kaggle submit

Adopt only if Public Score increases over `7186.15`.
