# 2026-06-30 case127 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task037.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task037.onnx`
- baseline: params 132, memory 4614, cost 4746, n_pass 266, n_fail 0

## 仮説

`task037` は最大6 slot を `TopK` で抽出し、各 slot の diagonal line を `ScatterElements` で描く。slot 数、orientation branch、または valid offset mask に余剰があれば、slot 関連中間を削れて相対削減率を稼げる。

## 手順

1. `/tmp` 上の candidate ONNX で `TopK k6` 短縮、`main_orient` 固定化、`slot_present` / `valid_offsets_raw` 簡略化を試す。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task037.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
