# 20260702 case351 result - task209 plus exact micro batch adopted

## Result

- Previous accepted Public Score: `7185.47`
- Kaggle submission ref: `54241101`
- Submission status: `SubmissionStatus.COMPLETE`
- Public Score: `7185.50`
- Submission time: `2026-07-01 18:39:36 UTC`
  (`2026-07-02 03:39:36 JST`)

Adopted.

## Submitted Batch

| task | change | baseline cost | submitted cost | gain |
|---:|---|---:|---:|---:|
| 090 | 9 area/best-row aliases | 3091 | 3073 | 0.005840 |
| 209 | `alignment_fits -> key_b` plus `kern_src_*_cl` bypasses | 28683 | 28047 | 0.022423 |
| 213 | 5 `h_valid* -> h_valid_and01` aliases | 1887 | 1882 | 0.002653 |

Total local gain:

```text
0.0309165
```

## Gate

All three changed tasks were individually checked with
`evaluate.audit_one(..., run_correctness=True)`:

```text
task090 n_fail=0 cost=3073
task209 n_fail=0 cost=28047
task213 n_fail=0 cost=1882
```

Packaging dry-run also completed and included all 400 tasks. The seven existing
local-cost-unestimated tasks remained structurally valid, matching previous
accepted submissions.

## Conclusion

The batch moved Public Score by `+0.03`, so the submit-saving bundle is adopted.
This validates combining a medium-risk structural alias (`task209`) with narrow
exact micros when the total local logarithmic gain clears about `0.030`.

## Next Step

Parallel search found one next standalone threshold candidate:

```text
task118: /tmp/neurogolf_case352_search/task118_pos_active4_hash6_remap.onnx
37989 -> 36576
gain ~= 0.037904
n_fail=0
```

This is a stronger task118 lookup-table pruning variant and remains high risk.
If attempted, submit it isolated for attribution. Do not bundle it with micros
or other task118 variants.
