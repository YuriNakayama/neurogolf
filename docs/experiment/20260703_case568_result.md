# 20260703 case568 result - task133/task077 repair

Accepted Public Score remains: `7187.57`

## Current gates

Both planned low-fail candidates reproduce against the current baseline:

```text
task133
/private/tmp/neurogolf_agent_case537_alias/near_selected/task133_task133_bypass_ScatterND_corr_overlay_TO_border_grid.onnx
33562 -> 31742
gain=0.055754
n_fail=5

task077
/private/tmp/neurogolf_case410_lowgain/task077_008_MaxPool_b1mn_p_TO_mn0.onnx
14834 -> 14414
gain=0.028722
n_fail=4

task077
/private/tmp/neurogolf_case410_lowgain/task077_012_MaxPool_b2mn_p_TO_b1mn.onnx
14834 -> 14414
gain=0.028722
n_fail=4
```

## task077 repair

The task077 worker re-gated all 24 `task077_*.onnx` candidates under
`/private/tmp/neurogolf_case410_lowgain`. The best four remain:

```text
14834 -> 14414
gain=0.028721887
n_fail=4
decision=blocked-local-fail
```

All four failures are underfills: expected/baseline output is color `4`, while
the candidate leaves the original color. The removed propagation side is one row
short at the failing cells.

Bounded repairs tried cheap final min/max row relaxations:

```text
task077_008_MaxPool_b1mn_p_TO_mn0__repair_rinv_m1.onnx
task077_012_MaxPool_b2mn_p_TO_b1mn__repair_rinv_m1.onnx
task077_010_MaxPool_b1mx_p_TO_mx0__repair_rval_m1.onnx
task077_014_MaxPool_b2mx_p_TO_b1mx__repair_rval_m1.onnx
```

All repaired variants gate as:

```text
14834 -> 14434
gain=0.027335309
n_fail=266
```

Conclusion: the missing cells need the deleted propagation context, not a simple
one-row threshold relaxation. Exact repair restores the removed `MaxPool` and
loses the cost win.

## task133 repair

The task133 worker re-gated the known candidate:

```text
/private/tmp/neurogolf_agent_case537_alias/near_selected/task133_task133_bypass_ScatterND_corr_overlay_TO_border_grid.onnx
33562 -> 31742
gain=0.055754
n_fail=5
```

Repair attempts under `/private/tmp/neurogolf_case568_task133/candidates`:

```text
task133_case568_sparse_corr_idx_overlay_sanitized_name.onnx
  intended cheaper full overlay via sparse corr_idx
  rejected by scorer shape inference for sparse ScatterND indices

task133_case568_full_dense_restore_control_max.onnx
  n_fail=0
  cost=33570, worse than current 33562

task133_case568_baseline_corr_idx_int32_control.onnx
  invalid; local runtime rejects int32 ScatterND indices
```

The bypass removes the final `ScatterND corr_overlay`. The five failures are the
four train examples plus `test[0]`; the missing cells are exactly the baseline
`corr_overlay` writes over zero `border_grid`. Those examples exercise all five
correction groups and all 232 correction rows, so a cheaper subset overlay was
not found in this family.

Full report:

```text
/private/tmp/neurogolf_case568_task133/logs/final_report.json
```

## Decision

No submit. Both planned case568 families remain locally incorrect unless the
deleted semantic branch is restored, which removes the cost win or exceeds the
current baseline.

Next action: move to `task102` or `task343` from the fresh target ranking, while
also considering a new target-generation pass because many recent low-fail
repair families are exhausted.
