# 20260701 case249 result — task055 final Einsum float16化で Public 7181.59 採用

## 対象
- `task055.onnx`
- 採否: 採用
- submit ref: `54212086`
- submitted message: `case249 task055 final einsum float16 cost 4537 to 3337`

## baseline

```text
params=277
memory=4260
cost=4537
n_pass=263
n_fail=0
```

## 変更
末尾の `Rext16 -> Cast(float32) -> Rext` と `Cext16 -> Cast(float32) -> Cext` を削除し、final `Einsum` を float16 の `Rext16` / `Cext16` へ直結した。

あわせて `M2` initializer を float32 から float16 に変換し、graph output dtype も float16 に更新した。

## 足切り
`src.evaluate.scorer.audit_one`:

```text
params=277
memory=3060
cost=3337
n_pass=263
n_fail=0
```

相対削減率は `4537 / 3337 = 1.36`。memory を 1200 bytes 削減。

## 実 submit
- ref: `54212086`
- status: `COMPLETE`
- Public Score: `7181.28 -> 7181.59`

## 採否
採用。

ローカル完全一致を保ち、実 LB も +0.31 上昇した。`task055.onnx` と `data/output/onnx.dvc` を採用状態として保持する。

## 次の一手
float16 finalization は効果が大きい。次は未言及または過去に dtype 周辺を深掘りしていない中 cost task で、末尾の float32 Cast / float32 initializer / final Einsum・MatMul を同様に確認する。
