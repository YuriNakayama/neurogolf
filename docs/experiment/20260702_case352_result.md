# 20260702 case352 result - task118 active hash remap adopted

## Result

- Previous accepted Public Score: `7185.50`
- Kaggle submission ref: `54241709`
- Submission status: `SubmissionStatus.COMPLETE`
- Public Score: `7185.53`
- Submission time: `2026-07-01 19:09:32 UTC`
  (`2026-07-02 04:09:32 JST`)

Adopted.

## Local Candidate

Submitted candidate:

```text
/tmp/neurogolf_case352_search/task118_pos_active4_hash6_remap.onnx
```

Targeted `audit_one` before submission:

```text
baseline:  params=3782, memory=34207, cost=37989
candidate: params=3029, memory=33547, cost=36576
n_fail=0, status=ok
gain = ln(37989 / 36576) ~= 0.0379
```

Packaging dry-run also completed and included all 400 tasks. The seven existing
local-cost-unestimated tasks remained structurally valid, matching previous
accepted submissions.

## Adoption Rationale

The high-risk isolated task118 variant improved Public Score by `+0.03`, so it
is adopted. The accepted candidate keeps only four locally active positive rows
and remaps the hash table to the locally active hashes plus a dummy entry.

## Risk Notes

This result validates only the submitted `active4_hash6_remap` variant on the
current Public LB. Do not infer that arbitrary stronger task118 table-pruning
variants are safe. Continue treating task118 lookup pruning as a high-risk
family that should be submitted only in isolated attribution cycles.

## Next Step

Parallel safe-candidate search did not find a case353-ready non-task118 batch:

```text
best safe/non-excluded audited material total ~= 0.0097
```

The best individual safe candidates were small micros such as task018
`39583 -> 39419`, task173 `15721 -> 15702`, and task397 `2848 -> 2845`. Do not
spend a submit on these alone. The next useful work should search for a new
structural candidate with relative gain near or above `0.030`, or accumulate a
larger low-risk batch before submitting.
