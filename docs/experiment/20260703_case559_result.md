# 20260703 case559 result - widened parallel search no-submit

## Submission

- Submitted: no
- Accepted Public Score remains: `7187.57`
- Decision: no-submit

## What Changed

No ONNX baseline files were changed.

Case559 increased the active search lanes to avoid the recent no-submit gaps:

1. medium/high-gain standalone search;
2. micro-bank search;
3. repair of high-gain near misses;
4. task118 close-miss repair;
5. main-thread current-baseline re-gating.

## Findings

No lane produced a submit-safe exact positive candidate.

Best near misses:

```text
task118
candidate: /tmp/neurogolf_parallel_S/alias_scan/task118_42_ScatterElements_in0_ng_patch_pos_mask_flat_TO_ng_patch_base_flat.onnx
local cost: 35807 -> 35095
estimated gain: 0.020085
n_fail: 1
functions: 0
forbidden ops: []
reason: removing ng_patch_pos_scatter loses required cells in train example 2.
Exact repair restored cost to 35807.

task260
candidate: /private/tmp/neurogolf_agent_nextcycle_A/candidates/task260_high_tri_f16_TO_high_tri_double_f16.onnx
local cost: 2109 -> 2108
estimated gain: 0.000474
n_fail: 2
functions: 0
forbidden ops: []

task292
local cost: 434 -> 170
estimated gain: 0.937246
n_fail: 28
```

Rejected high-gain families:

```text
task340 10918 -> 10630, gain 0.026733, n_fail 220
task367 20699 -> 5648, gain 1.298784, n_fail 266
task187 26396 -> 25146, gain 0.048514, n_fail 4
task364 23013 -> 22133, gain 0.038990, n_fail 21
```

The micro-bank worker stopped with no current-baseline exact positive candidates.
Main-thread checks also found no duplicate or unused initializer cleanup on
non-risk, non-recently-adopted tasks.

## Conclusion

The lack of submits was not caused by Kaggle automation failure. It was caused by
candidate supply: current accepted graphs are already stripped of simple
initializer cleanup, stale backlog candidates are mostly adopted or worse than
current baseline, and the available high-gain aliases fail correctness.

Do not submit local-fail near misses to satisfy cadence. That would spend the
refreshed submit budget on guaranteed zero-point or regression candidates.

## Next Step

Case560 should improve candidate supply rather than only increasing workers:

1. Build a current-baseline alias-screen tool that records exact positives and
   close misses without hanging on runtime-invalid candidates.
2. Prioritize task118-style `n_fail<=2` close misses for bounded repair, but
   stop when exact repair returns to baseline cost.
3. Keep cadence submits for exact candidates only; if no exact candidate exists,
   write no-submit quickly and immediately start the next scan.
