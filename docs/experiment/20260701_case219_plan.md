# case219 plan: task399 threshold board trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task399` baseline は `cost=91` (`params=20`, `memory=71`, `n_fail=0`)。
- graph は `GlobalAveragePool -> Gather(red) -> Greater(thresholds) -> Equal(channel_truth) -> Pad`。

## 対象

- `data/output/onnx/task399.onnx`

## 仮説

`task399` は red channel の平均値を 3x3 thresholds と比較し、2 channel board を作って 30x30 に pad する。もし red count の範囲が限定され、`blue` または `board` が固定に近ければ、`GlobalAveragePool/Gather/Greater` を削れて `91 -> 50` 前後まで下がる可能性がある。

## 検証

- temp ONNX で `blue` 固定候補、`board` 固定候補、`red_avg` 固定候補を probe する。
- thresholds の一部簡略化や threshold dtype 変更は、型制約と cost を見て採用可否を判断する。
- `src.evaluate.scorer.audit_one` で `task399` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
