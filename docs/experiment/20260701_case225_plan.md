# case225 plan: task305 bit table trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task305` baseline は `cost=636` (`params=340`, `memory=296`, `n_fail=0`)。
- graph は `GlobalMaxPool -> ArgMax -> Gather(k_masks) -> BitwiseAnd(column) -> BitwiseAnd(channel_row)`。
- params は `channel_row_u64` 300、`column_u64` 30、`k_masks_u64` 10 が支配している。

## 対象

- `data/output/onnx/task305.onnx`

## 仮説

入力に存在する最大 channel `k_i64` が限定されていれば、`k_masks_u64` の Gather や一部 table を固定・縮小できる。特に `k` 固定や `selected_column_u64` 固定が成立すれば、params と memory の両方を大きく削れる。

## 検証

- temp ONNX で `k_i64 -> 4..9`、`k_mask_u64 -> k_masks[k]` を probe する。
- `selected_column_u64 -> column_u64 & mask` 固定を probe する。
- `channel_row_u64` の channel subset や zero tail 削除は output shape と cost を確認する。
- `src.evaluate.scorer.audit_one` で `task305` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
