# 2026-07-01 case154 plan

## 方針

- floor: Public Score 7181.17
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task368.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task368.onnx`
- baseline: params 203, memory 6022, cost 6225, points 16.26367127866709, n_pass 265, n_fail 0

## 仮説

`task368` は color 5 の placeholder anchor を最大3つ取り、source block の色を読み出して `ScatterElements` で source と placeholders に配置する。`h_probe_valid_b` / `w_probe_valid_b` による probe guard、`valid_offsets_b`、3個目 placeholder の `has3_b` fallback、`a_mask_b` による color A/B split が主な分岐。

実データで source block のサイズや placeholder 数が限定されているなら、probe guard、3個目 fallback、color split の一部を簡約できる可能性がある。cost は 6225 で、12要素/100要素中間を削れれば相対削減が期待できる。

## 手順

1. `/tmp` 上の candidate ONNX で probe guard と `valid_offsets_b` guard を片側へ差し替える。
2. `has3_b` fallback と `a_mask_b` color split を個別に簡約する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task368.onnx` に反映する。
4. submit し、Public Score が 7181.17 を上回れば採用、上がらなければ破棄する。
