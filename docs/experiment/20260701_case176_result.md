# 20260701 case176 result

## Target

- Task: `task287`
- Baseline cost: 1994
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task287`.

No exact cost-reducing candidate was found.

Dtype probes:

- `code_weights` float16/uint8/int8/int16: invalid `Einsum` type binding
- `sym_selector` float16/uint8/int8/int16: invalid `Einsum` type binding
- `code_base -> uint8`: cost 1994, `n_fail=0`, score-neutral because it is already uint8-sized
- `code_base -> int8/uint16`: invalid `Div` type binding
- `classes -> uint8`: cost 1994, `n_fail=0`, score-neutral because it is already uint8-sized
- `classes -> int8/uint16`: invalid `Equal` type binding

Arithmetic aliases:

- `count8 -> code8`: cost 1929, `n_fail=267`
- `count8 -> code_base`: cost 1930, `n_fail=267`
- `src_code -> code8`: cost 1865, `n_fail=267`
- `src_code -> count8`: cost 1930, `n_fail=267`
- `code8 -> code_float`: invalid `Div` type binding

Shape aliases:

- `src9 -> src_code`: cost 1879, `n_fail=267`
- `rows30 -> src9`: invalid final `Equal` shape
- `full_idx -> rows30`: invalid final `Equal` shape
- `full_idx -> src9`: invalid final `Equal` shape
- `full_idx -> src_code`: invalid final `Equal` shape

## Submission

- Not submitted.
- Reason: all scored cost reductions failed exact-match auditing or produced invalid ONNX.

## Decision

Rejected. Keep the existing `task287.onnx`.

## Next

The symbolic 9x9-to-30x30 expansion is tight. Avoid `Einsum` initializer dtype
changes unless the input side is also changed, which would likely add back more cost.
Continue with medium-cost tasks that expose low-fail guard trims or exact tensor
removal.
