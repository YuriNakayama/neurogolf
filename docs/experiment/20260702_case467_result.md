# 20260702 case467 result - task145 selector/dtype re-gate

## Summary

No submit.

`task145` still has many large relative reductions, but all current positive
material candidates fail local correctness. This re-confirms the case199
conclusion that the area/source/overlay branches are semantically required.

## Baseline

```text
task145
params=111
memory=13104
cost=13215
points=15.5108921730
n_fail=0
status=ok
```

## Cost-Only Scan

```text
candidates=155
positive_cost_reductions=95
errors=0
```

Top cost-only reductions were mostly known semantic branch deletions:

```text
13215 -> 3327   gain=1.379282  color_core -> color0
13215 -> 3346   gain=1.373587  color_core -> directional source
13215 -> 3746   gain=1.260664  color_core -> directional marker
13215 -> 9575   gain=0.322197  area -> neg_width/neg_height
13215 -> 11612  gain=0.129314  color_core -> color2
13215 -> 11995  gain=0.096863  lr/ud sum -> directional marker
13215 -> 12815  gain=0.030736  directional marker -> directional source
```

## Correctness Gate

All `gain >= 0.020` candidates were audited with correctness:

```text
material_candidates=95
exact_positive=0
submit_candidates=0
blocked=95
errors=0
```

Representative blocked candidates:

```text
13215 -> 3327   gain=1.379282  n_fail=267
  color_core -> color0

13215 -> 9575   gain=0.322197  n_fail=267
  area -> neg_width/neg_height

13215 -> 11995  gain=0.096863  n_fail=179..187
  lr/ud sum -> directional marker

13215 -> 12215  gain=0.078688  n_fail=264
  color1 -> color0

13215 -> 12415  gain=0.062447  n_fail=267
  area_for_min -> area

13215 -> 12815  gain=0.030736  n_fail=179..267
  marker/source and min/max-mask aliases
```

## Decision

Rejected for submission. No candidate met:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

The useful information is negative: `task145` should not be revisited through
color-core, area, directional marker/source, or terminal overlay aliases.

## Next

Move to `task157`, which has prior Kaggle-valid shape-compatible savings. Avoid
the known bool `Concat -> Pad` route; inspect only numeric/shape-compatible
cleanup on the current graph.
