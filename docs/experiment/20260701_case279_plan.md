# 20260701 case279 plan - task076 bundled board/candidate aliases

## Context

- Current Public Score floor: `7181.84` after case278.
- case277 and case278 both adopted shape-ranked exact aliases with meaningful
  relative cost reductions.
- Continue selecting by `old_cost / new_cost`, with `n_fail=0` as the gate.

## Candidate

Target: `task076`

Baseline local audit:

```text
params=340
memory=19696
cost=20036
n_pass=266
n_fail=0
```

Exact aliases found:

```text
board_code            -> color15_h
candidate_outside     -> candidate_not_in_bounds
candidate_in_bounds   -> candidate_col_in
source_visible_seed   -> source_component
source_hidden_colors  -> source_hidden_colors_raw
source_visible_colors -> source_visible_colors_raw
```

Bundle audit:

```text
params=339
memory=18404
cost=18743
n_pass=266
n_fail=0
status=ok
```

Relative reduction: `20036 / 18743 = 1.0690`.

Rejected from the bundle:

```text
source_step3_mask -> source_step2_pool
```

It is exact alone, but combined with the larger bundle it fails (`n_fail=247`).

## Plan

Apply the six exact bundled aliases to `data/output/onnx/task076.onnx`, then
prune dead nodes and stale metadata.

Gate with `evaluate.audit_one` for `task076` only. Submit only if:

- `status=ok`
- `n_fail=0`
- cost remains `18743`
