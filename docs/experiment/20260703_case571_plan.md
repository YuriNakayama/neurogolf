# 20260703 case571 plan - task187 repaired reach trim

Accepted Public Score:

- `7187.64`

## Background

Case569 and case570 adopted exact improvements for `task343` and `task279`.
Fresh target ranking after those adoptions found no immediate high-gain exact
candidate, but two low-fail candidates remain plausible:

```text
task243: 22650 -> 22002, gain=0.029026, n_fail=1
task319: 18685 -> 18672, gain=0.000696, n_fail=1
```

The existing micro pool has exact candidates only far below the submit threshold,
so the next worthwhile submit needs a repaired low-fail candidate.

During the parallel wait, the task187 worker repaired another low-fail target:

```text
task187: 26396 -> 25771, gain=0.023963, n_fail=0
path: /private/tmp/neurogolf_case570_task187/candidates/task187_rpool12_greater_label_u8_repair.onnx
```

## Hypothesis

`task187` is now the best case571 target because it is already exact and above
the submit threshold. The repair keeps one final `MaxPool(reach_12)` and fuses
the free-cell gate via `Greater(rpool_12, label_u8)`, preserving a 625-cost
reduction from the baseline.

## Method

1. Re-gate the task187 candidate against current `data/output/onnx`.
2. Build a full 400-task bundle replacing only `task187`.
3. Submit only if single-task gate and full dry-run pass.
4. Keep task243/task319 repair workers running during Kaggle wait for the next
   cycle.
