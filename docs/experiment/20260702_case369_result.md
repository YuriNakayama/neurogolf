# 20260702 case369 result - adopt task205 uint8 patch core

## Decision

Adopt.

```text
previous accepted Public Score: 7185.78
Kaggle submission ref: 54246032
new Public Score: 7185.86
delta: +0.08
```

## Submitted Change

Target:

```text
data/output/onnx/task205.onnx
```

Baseline:

```text
params=280
memory=12214
cost=12494
n_fail=0
```

Submitted graph:

```text
params=279
memory=11271
cost=11550
n_fail=0
local gain = ln(12494 / 11550) = 0.0785630921
```

The graph keeps the case244 `AveragePool` change and does not use the rejected
case311 `GridSample -> Slice` boundary-sensitive rewrite. Instead it compresses
integer-valued post-patch computation from float16 to uint8:

- `rowp`, `colp`, `rowrev`, `colrev`: float16 -> uint8 for the ArgMax row/col
  support path.
- `Mh`, `cph`, `seedf`, `inboxf`, `crossf`, and downstream patch-color tensors:
  float16 -> uint8.
- `lev1c`: float16 initializer -> uint8.
- Removed one unused initializer after the dtype rewrite.

## Local Gate

Individual `audit_one` on the submitted `task205.onnx`:

```text
{'onnx': 'task205.onnx', 'params': 279, 'memory': 11271, 'cost': 11550,
 'points': 15.64555928405006, 'n_pass': 266, 'n_fail': 0, 'status': 'ok',
 'filesize': 5937}
```

The candidate clears the medium-risk standalone submit gate:

```text
required gain >= 0.030
actual gain   = 0.0785630921
```

## Parallel Probe Notes

Task370 produced only bankable exact micros:

| candidate | cost | gain | n_fail | decision |
|---|---:|---:|---:|---|
| direct first-valid selector | `10360` | `0.003180` | 0 | dominated |
| direct selector + d2 `Where` | `10348` | `0.004339` | 0 | exact but tiny |
| direct selector + sign aliases | `10351` | `0.004050` | 0 | banked |

The best subagent-rechecked task370 scratch candidate was recorded in
`micro_backlog.md`. Repeat-branch deletion, row/col mask aliases, repeat source
aliases, and lower-selector drops still fail correctness.

## Conclusion

Accept case369. The task205 dtype compression is isolated, locally exact, and
improved real Public Score by `+0.08`.

Next action: search for the same integer-valued float16 patch-core pattern in
other medium-cost tasks, especially graphs where `GridSample` or terminal patch
rendering is followed by equality, `ReduceMax`, and mask arithmetic.
