# 20260702 case435 plan - task131 ScatterND index dtype recheck

## Context

Case434 closed `task355`. The next fresh-scan candidate is:

```text
task131 params=675 memory=3302 cost=3977 n_fail=0
scatter_idx int64 [1,50,4] = 1600B
```

Prior case164 reported that `ScatterND` rejects int32 indices, but the current
baseline has since adopted small exact aliases. This cycle rechecks only the
current index dtype opportunity.

## Hypothesis

If current ONNXRuntime accepts `scatter_idx` as int32, the candidate saves about
800B and clears the gate. If not, close this target.

## Method

1. Change the final `scatter_idx8 -> Cast(INT64)` to `Cast(INT32)`.
2. Remove stale `scatter_idx` value info and rerun shape inference.
3. Run `submit gate`.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
