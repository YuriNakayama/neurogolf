# case242 plan: task185 grid3 kernel crop

## 現状
- 採用済み Public Score floor: 7181.26
- `task185` baseline は `cost=1961` (`params=310`, `memory=1651`, `n_fail=0`)。
- `grid3_kernel` は `(1,10,4,4)` の float32 Conv kernel だが、非ゼロ位置は各 input channel で局所的。
- 小コスト task なので、params を大きく削れれば相対改善率が高い。

## 対象
- `data/output/onnx/task185.onnx`

## 仮説
`grid3_kernel` を非ゼロ bbox に crop し、Conv 出力が増える分だけ後段で Slice して元の `p3_grid_f32` shape に戻せば、完全一致を保ちながら params を削れる可能性がある。追加 Slice の memory 増より kernel params 減が大きければ cost 改善になる。

## 検証
- `grid3_kernel` の非ゼロ bbox crop 候補を一時 ONNX で probe する。
- Conv の `kernel_shape` を補正し、必要なら `p3_grid_f32` 後に Slice を追加して後段 shape を維持する。
- `src.evaluate.scorer.audit_one` で `task185` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost 改善が出た場合のみ実 ONNX に反映し submit する。
