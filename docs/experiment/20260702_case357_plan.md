# 20260702 case357 plan - submit-saving candidate bank

## Context

Accepted Public Score is `7185.68` after case356 (`ec6e801`). The next submit
window is about `2026-07-02 06:04 JST`, but the policy is to avoid spending a
submit unless the candidate clears a practical relative-gain threshold:

```text
standalone low-risk:    >= 0.020
standalone medium-risk: >= 0.030
low-risk batch:         >= 0.030
mixed-risk batch:       >= 0.050
```

## Search Plan

Run read-only searches only:

1. Re-apply the deterministic case3 surgery pass chain to the current accepted
   bundle in `/tmp`, auditing each changed task.
2. Ask a sidecar explorer to search outside task219 and outside the known
   cleanup backlog for exact local aliases.
3. Submit only if a valid isolated or batch candidate clears the threshold.

No repo ONNX should be modified unless a threshold-clearing candidate is found.

## Gate

All candidate tasks must pass `evaluate.audit_one` with `n_fail=0`. No candidate
may introduce forbidden ops:

```text
Loop, Scan, NonZero, Unique, Script, Function, Compress
```
