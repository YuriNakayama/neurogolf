# 20260702 case421 plan - repaired near-miss reclassification

## Context

Case420 found no remaining dense structural compression candidate. The next
post-regression priority is repaired near misses: candidates with small failure
sets where an existing guard can restore correctness without giving back the
gain.

Docs scan found these relevant families:

- `task343`: already repaired and adopted in case407;
- `task110`: case365 documented that the smallest exact repair is worse than
  baseline;
- `task243`: case366 documented that repairs are neutral or worse;
- `task285`: original connectivity shortcut had `n_fail=1`, and a bridge-guard
  repair was adopted in case354.

## Hypothesis

The remaining `task285` scratch family may contain a residual exact current
baseline improvement distinct from the rejected uint8 `TopK` family. If the
best exact residual is below the new gate, record it as banked/no-submit and
move on.

## Gate

Use `submit gate` against current baseline. Submit only if a candidate is:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
not uint8 TopK
single-task and mechanically explainable
```
