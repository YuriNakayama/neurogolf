# 20260702 case509 result - reject task175 guarded repair

## Result

Rejected.

Kaggle submission:

```text
ref          : 54268643
description  : case509 task175 guarded repair
status       : COMPLETE
Public Score : 7187.23 -> 7169.38
```

## Local Gate

The candidate passed the standalone submit gate:

```text
task175 1267 -> 1216
gain    0.041085
n_fail  0
decision submit-candidate
```

Candidate path:

```text
/tmp/neurogolf_parallel_AE/task175_repair_guarded_where.onnx
```

## Decision

The real Public Score regressed by `-17.85`, so the candidate was restored from:

```text
/tmp/neurogolf_case509_baseline/task175.onnx
```

The task is now marked as a review-risk task in the submit gate. Future task175
work must be isolated with a new repair plan instead of being accepted from
local exactness alone.

## Parallel Search Summary

The expanded subagent pass produced one usable next standalone candidate and
several bank-only micros:

```text
task173 15664 -> 15020 gain 0.041982 submit-candidate
task263   680 ->   679 gain 0.001472 bank-low-gain
task301  1141 ->  1140 gain 0.000877 bank-low-gain
task219 14838 -> 14823 gain 0.001011 bank-low-gain
```

Clean bundle assembly after case508 had only `0.005784` total gain and was
blocked by the `0.015` micro-bundle threshold.

## Conclusion

The reason for the no-improve/regression streak is confirmed again: local
`n_fail=0` is necessary but not sufficient for topology/index repairs. Strong
relative gain alone should not override a real Public regression record.

Next action: proceed to case510 with the independent `task173` dtype shrink,
while keeping task175 out of automatic submit lanes.
