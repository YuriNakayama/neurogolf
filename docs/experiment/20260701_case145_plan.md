# 2026-07-01 case145 plan

## 方針

- floor: Public Score 7181.16
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task183.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task183.onnx`
- baseline: params 222, memory 1008, cost 1230, points 17.885230551633537, n_pass 265, n_fail 0

## 仮説

`task183` は 6x6 の cyan mask と四隅 color code から、2x2 / 4x4 / 6x6 の small grid を作り、最後に 30x30 one-hot へ戻す。`g2/g4/g6` の各 quadrant で `Where(mask, code, black)` を使い、さらに `is2` / `is4` でサイズを選ぶ。

実データでサイズや quadrant mask が限定されているなら、`Where(mask, code, black)` の一部、または `is2` / `is4` の分岐を簡約できる可能性がある。cost は 1230 と小さいため、数 node の削減でも相対効率は高い。

## 手順

1. `/tmp` 上の candidate ONNX で `g2/g4/g6` の quadrant mask を片側入力へ差し替える。
2. `grid46` / `color_grid` を `g2_pad6` / `g4_pad6` / `g6_full` に固定できるか確認する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task183.onnx` に反映する。
4. submit し、Public Score が 7181.16 を上回れば採用、上がらなければ破棄する。
