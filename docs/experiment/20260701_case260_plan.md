# 20260701 case260 plan - task185 marker color arithmetic

## 背景

- Current floor: Public Score `7181.61`
- `task185` baseline: `cost=1961` (`params=310`, `memory=1651`, `n_fail=0`)
- case242 では `grid3_kernel` crop を試したが、cost 不変で不採用だった。
- 今回は cost の絶対値ではなく相対削減率を優先し、9x9 bool mask 1枚を消せる候補を見る。

## 対象

- `data/output/onnx/task185.onnx`

## 仮説

`task185` は

```text
grid_eq_line = Equal(grid_u8, line_u8)
marker_color = Where(grid_eq_line, zero_u8, grid_u8)
```

で基準 line color と同じセルを0にしている。実データ上 `grid_u8` が `0` または `line_u8 + marker` の形なら、`marker_color` を `Sub(grid_u8, line_u8)` または guard 付き算術に置換でき、`grid_eq_line` の9x9 bool中間を削れる可能性がある。

## 手順

1. `/tmp` に `task185` の probe ONNX を作り、`marker_color` を算術表現に差し替える。
2. `src.evaluate.scorer.audit_one` で `task185` の完全一致と cost を確認する。
3. `n_fail=0` かつ cost 減なら本体へ反映し、Kaggle submit で採否を決める。
