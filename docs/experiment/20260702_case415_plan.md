# 20260702 case415 plan - post-regression gated search

## Context

Accepted Public Score remains `7186.71`. Case414 regressed sharply because a
bundle of locally exact but unisolated micros contained at least one
hidden-unsafe member. The new regression control document requires:

- no unisolated bundles;
- no standalone submit for `gain < 0.010`;
- prefer isolated, mechanically explainable candidates with `gain >= 0.020`.

## Search Target

Find a single-task current-baseline candidate that satisfies:

```text
status=ok
n_fail=0
functions=0
forbidden ops=[]
gain >= 0.020 preferred
```

Excluded families:

- known hidden-unsafe: `task017`, `task101`, broad `task286`;
- known Kaggle ERROR: `task233` uint8 TopK, `task285` uint8 TopK;
- contaminated filler: `task305`, `task366`;
- case414 unisolated candidates: `task090`, `task209`, `task370`, `task308`,
  `task293` from their submitted scratch paths;
- recent unchanged standalone micros: `task338`, `task342`, `task392`.

## Method

1. Split exploration across subagents:
   - tasks `001-200`;
   - tasks `201-400`.
2. Main agent runs current-baseline safe surgery only, excluding runtime
   equivalence aliasing.
3. Submit only if a single candidate clears the new gate.
4. Otherwise write a no-submit result and bank exact low-gain candidates.
