# 20260702 case421 result - repaired near-miss reclassification

## Summary

No submit.

Accepted Public Score remains `7186.71`. The current-baseline `task285`
near-miss family is already harvested by the adopted case354 bridge-guard
repair and the later accepted cleanup. No remaining exact candidate clears the
submit gate.

## Current Baseline

```text
task285 cost=24512
```

## Candidate Checks

Rechecked the case354 repair family and selected case382 structural candidates
with the case419 `submit gate`.

Best exact residual:

| candidate | cost | gain | local result | decision |
|---|---:|---:|---|---|
| `/private/tmp/neurogolf_case356_struct/task285_And_pivot_src_same_masked_TO_pivot_src_same.onnx` | `24456` | `0.002287` | `n_fail=0` | bank-low-gain |

Other notable results:

| candidate family | result | reason |
|---|---|---|
| case354 bridge repairs | exact variants are `24766+` | worse than current baseline |
| case354 `reqcol3` variants | `n_fail=1` or invalid cyclic graph | still not exact/current-safe |
| case382 `Or`/`And` aliases | `n_fail=125` or `n_fail=265` | broad structural aliases break correctness |
| case382 dtype/shape variants | session/shape errors | not Kaggle-safe candidates |

The known uint8 `TopK` bypass family remains excluded by case393 and was not
treated as actionable.

## Decision

Do not submit. The best exact residual gain is far below the `0.010` bank-only
boundary and well below the `0.020` submit threshold.

## Next Step

Stop spending cycles on old `task285` connectivity scratch unless a new repair
is derived from the current accepted graph. Continue with fresh same-task
value-path reductions or a new `n_fail=1` candidate that has not already been
repaired/dominated.
