# 20260630 case97 result — t002 bit-neighbor OR prune はローカル偽陽性で Public 退行（不採用）

## 対象

- `task002.onnx`
- baseline: params **209**, memory **20880**, cost **21089**, faithful `n_fail=0`
- Public Score floor: **7180.89**

## 実施内容

t002 は enclosed-fill を bit-manipulation で解く graph。後半の `n_x0..n_x9` 近傍拡張反復で、`n_lr*` / `n_ud*` の片側 OR を削れるか検証した。

### probe

| 候補 | cost | local faithful |
|---|---:|---|
| `out_bits: n_x9 -> n_x8` | 20369 | `n_fail=2` |
| `out_bits: n_x9 -> n_x7` | 19649 | `n_fail=6` |
| final `n_lr9` 片側化 | 20929 | `n_fail=0` |
| final `n_ud9` 片側化 | 20929 | `n_fail=0` |
| greedy combined片側化 | **19489** | `n_fail=0` |

greedy combined:

- `n_lr9 -> n_l9`
- `n_ud9 -> n_u9`
- `n_lr0 -> n_l0`
- `n_ud0 -> n_u0`
- `n_lr1 -> n_l1`
- `n_ud1 -> n_u1`
- `n_lr2 -> n_r2`
- `n_lr4 -> n_r4`
- `n_ud5 -> n_d5`
- `n_ud7 -> n_d7`

## submit

- ref: `54192324`
- description: `case97 t002 prune redundant bit-neighbor OR cost 21089->19489`
- status: `COMPLETE`
- Public Score: **7180.89 → 7165.84**

## 採否

- **不採用**。ローカル faithful は `n_fail=0` だったが、実 LB で大幅退行。
- `task002.onnx` は DVC checkout で採用済み状態へ復元済み。
- 復元後 audit:
  - `task002`: cost **21089**, `n_fail=0`
  - `task319`: case96 改善維持、cost **21723**, `n_fail=0`

## 結論

t002 の bit-neighbor 片側 prune は hidden/public 実採点に対するローカル偽陽性。t002 はローカル arc-gen だけで採否判断できないため、今後は同種の bit-topology 削減を submit する場合、単独 probe として扱い、不採用時は即 revert する。
