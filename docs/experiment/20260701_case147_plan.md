# 2026-07-01 case147 plan

## 方針

- floor: Public Score 7181.16
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task013.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task013.onnx`
- baseline: params 61, memory 1869, cost 1930, points 17.43472471810107, n_pass 267, n_fail 0

## 仮説

`task013` は入力中の水平/垂直ラインを検出し、`hmode` で row/col 側の境界・色を選んで周期パターンを作る。末尾では `valid_rows` / `valid_cols` と mode 条件で sentinel / channel id を合成する。

実データで horizontal / vertical の片側や sentinel guard が限定的なら、`hmode` 分岐または final `Where` の一部を外せる可能性がある。cost は 1930 で、数個の 30 要素級中間を削れれば相対効率が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `hmode` 分岐 (`line_lo/hi/color`) を水平・垂直片側へ差し替える。
2. `line_pattern` と `row_code` / `col_code` / `row_side` / `col_side` の sentinel guard を個別に差し替える。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task013.onnx` に反映する。
4. submit し、Public Score が 7181.16 を上回れば採用、上がらなければ破棄する。
