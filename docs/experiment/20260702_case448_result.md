# 20260702 case448 result - task351 answer patch re-gate

## Result

No submit.

Re-gated 36 existing `task351` candidates from cleanup, surgery, future-alias,
and current scratch pools against the accepted baseline.

Current baseline:

```text
task351.onnx
params=19
memory=1632
cost=1651
```

Gate summary:

```text
blocked-local-fail: 16
blocked-no-gain:    20
submit-candidate:    0
```

Best nominal reductions were not exact:

```text
/private/tmp/neurogolf_future_alias_scan/task351_patch_starts_TO_zero_row_col.onnx
  cost 1651 -> 1636
  gain 0.009126927
  n_fail=265

/private/tmp/neurogolf_future_alias_scan/task351_patch_ends_TO_zero_row_col.onnx
  cost 1651 -> 1638
  gain 0.007905180
  n_fail=265
```

Several structural surgery candidates were runtime-unscorable or shape/type
invalid. Exact candidates were cleanup/broadcast no-ops with unchanged cost:

```text
cost 1651 -> 1651
n_fail=0
decision=blocked-no-gain
```

Index-surgery variants were exact but increased cost:

```text
cost 1651 -> 1659
n_fail=0
decision=blocked-no-gain
```

## Decision

Reject for submission. The dominant `answer_patch` is the final value path into
`Pad`; current ONNX operators do not let us slice the float input directly into
a smaller dtype. Adding a cast after the slice would keep the 900-byte float
patch and add another tensor, so it is not a promising path.

## Next

Continue with another current-baseline structural target. Prefer tasks where
the dominant tensor is not the direct final value patch, or where an already
computed lower-memory equivalent can replace the dominant producer.
