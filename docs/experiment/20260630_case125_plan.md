# 2026-06-30 case125 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task382.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task382.onnx`
- baseline: params 120, memory 5652, cost 5772, n_pass 266, n_fail 0

## 仮説

`task382` は `vpat` / `hpat` の縦横 bank、`red`、`row_out`、`col_out` を `Max` 合成している。縦横の片側 bank、pos/neg bank、外枠 mask のどれかが冗長なら、20x20 級中間を削れて数 % 以上の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `cidx` 合成要素、`vbank/hbank`、`row_cnt/col_cnt`、`row_out/col_out` を簡略化する。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task382.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
