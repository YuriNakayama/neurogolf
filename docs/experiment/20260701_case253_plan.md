# 20260701 case253 plan — task025 TopK slot 4→3 probe

## 現状

- 直近採用: case249 `task055` final `Einsum` float16 化で Public Score `7181.59`
- 直近 probe: case252 `task390` gray scatter row 削減は全 row `n_fail>0`
- 優先基準: `n_fail=0` を絶対条件にし、`old_cost / new_cost` の相対削減率を優先する。

## 対象

- `task025`
- baseline audit: `params=144`, `memory=11520`, `cost=11664`, `n_fail=0`

## 仮説

`task025` は `TopK(k=4)` で line slot を 4 本取り、`[1,4,30]` 系の OneHot/CumSum/Einsum 中間と final `Einsum` の projector `(21,5)` を使って出力する。もし faithful/test/arc-gen 上で 4 本目 slot が不要なら、`k=3` に縮めるだけで複数の中間テンソルが `[1,3,30]` になり、final feature も `21→16`, channel も `5→4` に縮む。

この削減は memory に効くため、成功すれば小〜中 cost task として相対改善が見込める。

## 手法

1. `topk_k` を `4→3` に変更する。
2. `slot_projector` を `(21,5)` から `(16,4)` に縮める。
   - feature block は `valid`, `line`, `left`, `right`, `adj_left`, `adj_right`。
   - slot 列は `background + 3 slot`。
3. 関連 value_info の slot 次元を `4→3`、final feature/channel 次元を更新する。
4. `src.evaluate.scorer.audit_one` で `task025.onnx` のみ検証し、`n_fail=0` かつ cost 減なら submit する。
