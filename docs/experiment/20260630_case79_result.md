# 20260630 case79 result — t338 の TopK=5 は必要、Equal出力化は悪化で不採用（提出なし）

## independent_impl:
t338 を個別精査:
- 規則: 色2の矩形枠を検出し、枠内部を色3、その他 valid 領域を色0として出力。
- arc-gen 全267例で output の色3 connected component / 入力矩形数を集計。
- 現行 graph は `TopK(k=5)` で top-left corner 候補を最大5個処理し、各候補の right/bottom を見つけて fill を合成。

## faithful 検証 / データ確認
- arc-gen に **5矩形が必要な例が5件**あり、TopK=5 は必要。
- trace 最大中間:
  - `Conv` code float `[1,1,24,24]` = 2304B
  - fill/output 系 bool `[1,1,30,30]` = 900B 複数
  - TopK score `[1,5,24]` = 480B
- 出力構築を `Where(fill,3,0) -> Where(valid,color,255) -> Equal(color_values)` に置換:
  - **n_fail=0** だが cost **17939→19752** に悪化。
  - 900B級中間2本を別の 900B級 uint8 中間2本へ置換し、さらに params 13 が増えるため不利。

## 確定
- TopK=5 は hidden/arc-gen 上必要で、候補数削減不可。
- 現行の `Xor/And/Concat` 出力は、Equal型の色コード出力より安い。
- Conv code を単channel入力へ寄せる案も、Slice/Gather の float中間が増えて不利見込み。
- 改善候補なし。floor 7180.85 維持、提出なし。

## 次の一手
- t338 は保留。次は t173/t080 の template-stamping graph、または高 cost t233/t286/t018 の局所 trace 監査に戻る。
