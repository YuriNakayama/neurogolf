# 20260702 case466 plan - task066 score alias follow-up

## Context

Cases 464-465 found no submit-grade candidates, but `task096` added several
bankable low-gain exact candidates. The next target should be a task with prior
Kaggle-valid evidence, not another stale broad pool.

`task066` is the best current follow-up:

- case273 adopted a red-clear guard alias for `task066`;
- case409 adopted another isolated same-task score alias;
- case409 moved Public Score `7186.66 -> 7186.69`;
- the adopted case409 candidate reduced `task066` from `16835 -> 16351`
  (`gain=0.029171`).

## Target

```text
task066
current graph: data/output/onnx/task066.onnx
task data: data/lake/neurogolf-2026/task066.json
known safe neighborhood: score/edge aliases around active row and up/down path
```

## Hypothesis

Because same-task score/edge rewrites have already transferred to Kaggle, the
remaining `task066` alias pool is a better submit-saving target than unrelated
micro bundles. A new exact candidate in the same score/edge family with
`gain >= 0.020` should be submitted in isolation.

## Method

1. Re-audit accepted `task066.onnx`.
2. Gather current `task066*.onnx` candidates from `/private/tmp` and `/tmp`.
3. Prioritize but do not exclusively limit to paths containing:
   `n_u_up`, `n_s_up`, `score`, `edge`, `active_row`, and `red_row`.
4. Run `submit.gate.evaluate_candidate_gate` against the current baseline.
5. Submit only if:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

Otherwise record blocked and bank-only candidates, then move to `task145`.
