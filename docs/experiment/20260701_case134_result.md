# 2026-07-01 case134 result

## 対象

- `task004.onnx`
- baseline: params 94, memory 5284, cost 5378, points 16.409928163171188, n_pass 265, n_fail 0

## 実施内容

`below` / `right` の入力を `any_u8` から `vals` に差し替え、`Clip(vals, zero_u8, one_u8) -> any_u8` を prune した。

`endpoint` と `row_next` は boolean 的な「存在有無」だけを使っているように見えるが、実データでは `vals` の非ゼロ code をそのまま比較・ReduceMax に使っても完全一致した。

## 足切り

採用候補:

```text
params 93, memory 5028, cost 5121, points 16.458894988537452
n_pass 265, n_fail 0, status ok
```

baseline 比:

```text
cost 5378 -> 5121
delta -257
relative old/new 1.0501855106424527
score delta +0.048966825366264
```

主な失敗候補:

- `vals -> code`: cost 5122, n_fail 243
- `endpoint -> row_next`: cost 4626, n_fail 265
- `mc -> endpoint`: cost 5090, n_fail 265
- `mc -> row_next`: cost 4610, n_fail 265
- `move_vals -> vals`: cost 5122, n_fail 239
- `stay_vals -> vals`: cost 5122, n_fail 265
- `scalar_out -> shifted`: cost 4866, n_fail 265
- `scalar_out -> stay_vals`: cost 4594, n_fail 265
- `scalar_valid -> scalar_out`: cost 5122, n_fail 263

## submit

- ref: `54205538`
- message: `case134 task004 remove binary clip`
- Public Score: `7181.15`
- previous floor: `7181.11`

## 採否

採用。

ローカルの `task004` cost 減が実 LB に反映され、Public Score は `7181.11 -> 7181.15` に上がった。`data/output/onnx/task004.onnx` と `data/output/onnx.dvc` を採用状態のまま残す。

## 次の一手

同じ「binary 化 Clip が実は不要」パターンを探す。`Clip(..., 0, 1)` や `Cast(bool)->uint8` の後段が比較/存在判定だけのタスクを優先する。
