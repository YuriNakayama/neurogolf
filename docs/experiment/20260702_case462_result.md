# 20260702 case462 result - task340 guard candidate re-gate

## Summary

No submit.

`task340` has no remaining exact positive candidate in the current scratch
pool. All cost-reducing guard/index aliases fail local correctness.

## Baseline

```text
task340
params=136
memory=10782
cost=10918
points=15.7018319177
n_pass=266
n_fail=0
status=ok
```

## Gate Result

```text
candidates=374
positive_cost_reductions=39
errors=0
exact_positive=0
blocked=39
exceptions=0
```

Representative blocked candidates:

```text
10918 -> 10630  gain=0.026733  n_fail=220
  bottom_scan -> bottom_match

10918 -> 10646  gain=0.025229  n_fail=221
  right_scan2 -> right_scan

10918 -> 10706  gain=0.019608  n_fail=220..221
  And scan aliases

10918 -> 10738  gain=0.016624  n_fail=266
  Equal aliases

10918 -> 10858  gain=0.005511  n_fail=255..266
  row/col inner/prev guard aliases
```

## Decision

Rejected for submission.

No candidate met `n_fail=0`, and the task already has a Kaggle `ERROR` history
for guard/index aliases. There is no safe material to stage.

## Next

Move away from `task340`. The next current-rank tasks are `task379`, `task074`,
and `task350`. Because `task379` has mostly known micro cleanup, prefer a fresh
simple-graph probe on `task074` or `task350` unless a current `task379` screen
finds a new exact positive.
