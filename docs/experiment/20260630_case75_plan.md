# 20260630 case75 plan — t066 の重複 shape initializer 統合で params 6 削減を検証

## 仮説
保守的 `onnxoptimizer` probe により、t066 で duplicate initializer 統合だけで
cost 18386→18380（params 641→635, memory 17745 据え置き）となる候補を発見した。
同一 shape initializer を共有するだけなら意味論は変わらず、faithful を維持できる。

## 対象
- task066
- 現行: cost 18386, params 641, memory 17745, n_fail 0
- 候補: cost 18380, params 635, memory 17745, n_fail 0

## 手法
1. `eliminate_duplicate_initializer` 相当の変換で重複 shape initializer を統合。
2. `audit_one(run_correctness=True)` で n_fail=0 を確認。
3. submit し、Public Score が 7180.85 を上回れば採用。上がらなければ破棄。
