# 20260702 case468 result - task157 current shape-compatible re-gate

## Summary

No submit.

`task157` has no remaining current-baseline positive candidate in the scratch
pool. The case398 shape-compatible bundle appears to have exhausted the
available saved candidates.

## Baseline

```text
task157
params=335
memory=7461
cost=7796
points=16.0386339394
status=ok
```

## Cost-Only Scan

```text
candidates=221
positive_cost_reductions=0
errors=0
```

Most `task157` scratch files are stale pre-case398 variants with names such as
`8163_to_7796`, or old shape-alias probes that are no longer below the current
accepted cost. Since there are no current positives, no correctness gate and no
Kaggle submit were run.

## Decision

Rejected for submission.

No candidate met even the first requirement:

```text
candidate_cost < baseline_cost
```

Do not revisit `task157` through the old case398 bundle pool. Future work would
need a new current-graph rewrite, not another stale re-gate.

## Next

Return to the current cost ranking after removing closed targets:

- skip `task379` unless looking only for banked micros;
- inspect `task324` or `task377` next, using cost-only first to avoid stale
  candidate pools;
- prefer a task with prior shape-compatible or mechanical cleanup evidence over
  broad semantic branch aliases.
