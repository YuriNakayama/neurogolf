# 20260702 case497 result - no-submit task013 low-fail scan

## Decision

No submit.

Accepted Public Score remains:

```text
7187.10
```

## Baseline

```text
task013.onnx
cost=1870
params=61
memory=1809
status=ok
```

## Scan

Existing unique `task013*.onnx` scratch candidates from `/tmp` and
`/private/tmp`:

```text
candidates=270
positive cost-only candidates=166
```

All positive candidates were gated:

```text
blocked-local-fail=166
submit-candidate=0
bank-low-gain=0
```

Best low-fail candidates:

```text
1870 -> 1840  gain=0.016173  n_fail=6   blocked-local-fail
1870 -> 1810  gain=0.032612  n_fail=10  blocked-local-fail
1870 -> 1840  gain=0.016173  n_fail=10  blocked-local-fail
```

Representative broad failures:

```text
1870 -> 1502  gain=0.219141  n_fail=267  blocked-local-fail
1870 -> 1630  gain=0.137358  n_fail=125-142 blocked-local-fail
1870 -> 1657  gain=0.120930  n_fail=267  blocked-local-fail
1870 -> 1797  gain=0.039820  n_fail=266-267 blocked-local-fail
```

## Conclusion

Reject locally. The promising candidates remove row/column validity guards:

```text
task013_031_Or_valid_cols_TO_col_base_valid.onnx
task013_030_Or_valid_rows_TO_row_base_valid.onnx
```

They are close enough to be a future repair target, but not submit material.
The likely repair is to restore the exceptional validity branch for the failing
pairs, which may consume much or all of the current gain. Treat this as a
separate semantic repair cycle rather than forcing a weak submit.
