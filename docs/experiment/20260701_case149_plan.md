# 2026-07-01 case149 plan

## 方針

- floor: Public Score 7181.16
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task105.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task105.onnx`
- baseline: params 106, memory 2854, cost 2960, points 17.007055452681893, n_pass 266, n_fail 0

## 仮説

`task105` は 14x13 crop の foreground から row hash を作り、右端幅と内部 line を推定して枠 shape を構成する。`right_f/right_bit`、`interior_rows_same`、`v_selected`、`full_rows` / `row_in_rect` の guard が複数ある。

実データで幅や内部 line の向きが限定されているなら、幅推定 branch または横/縦 line branch を簡約できる可能性がある。cost は 2960 で、14x13 mask を1個削るだけでも相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `right_f/right_bit` を各候補値へ固定できるか確認する。
2. `line_row_active_f` / `line_col_active_f` と `row_cols_u8` / `shape_u8` の guard を個別に差し替える。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task105.onnx` に反映する。
4. submit し、Public Score が 7181.16 を上回れば採用、上がらなければ破棄する。
