# 20260703 case536 plan - high-gain near-miss repair queue

Accepted Public Score:

- `7187.41`

## Background

Case535 showed that the current low-risk exact micro queue is only about
`0.00198` aggregate gain, below the `0.015` micro-bundle submit gate. To restore
the requested submit cadence, the next cycle needs a larger candidate rather
than more one-byte cleanup.

## Targets

Prioritize current non-risk high-gain near misses:

- `task023`: candidate around `11212 -> 9844`, estimated gain `~0.130`,
  currently `n_fail=2`.
- `task279`: candidate around `4827 -> 4315`, estimated gain `0.112128`,
  currently `n_fail=2`.

Both are far above the standalone submit threshold if repaired to exactness.

## Method

Run parallel repair workers:

1. `task023` worker: reproduce failure diffs, identify the minimal semantic
   exception, and try bounded repairs that keep most of the cost reduction.
2. `task279` worker: reproduce failure diffs and search for a cheaper
   flood-extension repair than the previously cost-restoring fixes.
3. micro worker: continue low-risk current-baseline exact queue generation, but
   avoid slow broad scans and exclude all hard/review/runtime-risk tasks.

Main thread will inspect failure diffs for whichever target has a candidate path
available first.

## Gate

Submit only if:

- `n_fail=0`;
- candidate cost is lower than current baseline;
- no forbidden ops/functions;
- non-risk standalone gain `>=0.020`, or low-risk micro bundle gain `>=0.015`.

If neither high-gain near miss can be repaired, record no-submit and carry
forward exact micros in `micro_backlog.md`.
