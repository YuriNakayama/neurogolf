# 20260701 case330 result - task334 packed uint8 tail

## Decision

Adopted.

## Local Gate

Only `task334.onnx` was changed and audited.

```text
baseline:  params=8  memory=189 cost=197 n_fail=0 status=ok
candidate: params=23 memory=108 cost=131 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(197 / 131) ~= 0.4080 pt
```

## Submission

```text
ref=54231924
description=case330 task334 packed uint8 tail cost 197 to 131
status=COMPLETE
publicScore=7184.74
```

Previous accepted floor:

```text
7184.33
```

The Public Score improved by `+0.41`, so the candidate is accepted.

## Diff Summary

The accepted rewrite removes eight explicit zero channels from the terminal
10-channel patch. It keeps the two live uint8 channels (`black` and `gray`) and
uses a one-hot `QLinearConv` with final spatial padding folded into the
convolution pads. This differs from the earlier rejected local variant that
increased cost.

## Next Step

Continue probing terminal sparse tails on very small tasks. The best gains now
come from high relative reductions, not large absolute task costs.
