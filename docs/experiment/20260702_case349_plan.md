# 20260702 case349 plan - low-risk micro cleanup batch

## Context

- Current accepted Public Score: `7185.40`
- Last accepted change: case348 `task158` tr-ne/fill alias
- Last submit time: `2026-07-01 17:10:22 UTC`
  (`2026-07-02 02:10:22 JST`)

The submit-saving policy says to bank small exact cleanups until a low-risk
batch reaches about `0.030` local logarithmic gain:

```text
gain = ln(old_cost / new_cost)
```

Recent parallel scans found no safer standalone candidate above `0.030` after
excluding known hidden-unsafe or validator-sensitive families. The best use of
the next submit window is therefore a compact micro batch with individually
audited tasks.

## Target Batch

| task | change | baseline cost | candidate cost | gain |
|---:|---|---:|---:|---:|
| 382 | alias `h_s4 -> hsrc`, `v_s4 -> vsrc` | 5772 | 5730 | 0.007303 |
| 334 | duplicate initializer cleanup `probe_wzp -> probe_zp` | 131 | 130 | 0.007663 |
| 052 | duplicate initializer cleanup `tailpack_052_wzp -> tailpack_052_xzp` | 209 | 208 | 0.004796 |
| 003 | duplicate initializer cleanup `probe_wzp -> probe_zp` | 263 | 262 | 0.003810 |
| 363 | broadcast-compress `kt1` from `(1,1,4,4)` to `(1,1,4,1)` | 4360 | 4348 | 0.002756 |
| 117 | compact rewire `condition_left -> col_local_i64` | 4158 | 4149 | 0.002167 |
| 361 | duplicate/unused initializer cleanup | 4787 | 4779 | 0.001673 |

Expected total local gain:

```text
0.03017
```

## Risk

This batch is intentionally limited to low-risk changes:

- duplicate initializer canonicalization;
- static broadcast initializer compression;
- two previously banked local-equivalence aliases.

It excludes:

- `task118` table pruning, marked hidden-sensitive;
- `task233` uint8 `TopK` scorew family, which returned Kaggle
  `SubmissionStatus.ERROR`;
- broad task286/task101 local-equivalence bundles with known Public regressions.

## Gate

Run `evaluate.audit_one(..., run_correctness=True)` for exactly the seven
changed tasks. If all remain `n_fail=0` and scorable, run a packaging dry-run
and submit at the next 30-minute cadence window. Adopt only if Kaggle Public
Score improves over `7185.40`; otherwise restore the DVC baseline and record
rejection.
