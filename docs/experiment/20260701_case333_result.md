# 20260701 case333 result - task274 zero_small broadcast compression

## Decision

Adopted.

## Local Gate

Only `task274.onnx` was changed and audited.

```text
baseline:  params=94 memory=83 cost=177 n_fail=0 status=ok
candidate: params=86 memory=83 cost=169 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(177 / 169) ~= 0.0463 pt
```

## Submission

```text
ref=54232731
description=case333 task274 zero_small broadcast cost 177 to 169
status=COMPLETE
publicScore=7184.93
```

Previous accepted floor:

```text
7184.88
```

The Public Score improved by `+0.05`, so the candidate is accepted.

## Diff Summary

The accepted change compresses the `zero_small` initializer from
`uint8[1, 1, 3, 3]` to `uint8[1, 1, 1, 1]`. It is consumed by `Where`, which
broadcasts the scalar tile back to the required output shape, so runtime values
are unchanged while params decrease.

## Next Step

The known validated cleanup queue from the structural explorer is exhausted.
Resume broader scans for high-relative reductions and record small exact
candidates for later bundling.
