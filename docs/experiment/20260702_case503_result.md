# 20260702 case503 result - no-submit stale value_info sweep

## Outcome

No submit.

Accepted Public Score remains:

7187.17

## Gate Summary

The stale `value_info` sweep generated 257 candidates and began gating the
highest relative reductions. High-gain candidates failed local correctness. The
only exact current-baseline candidate observed before stopping the long scan was
a `task018` micro:

```text
task018
baseline_cost : 38681
candidate_cost: 38575
gain          : 0.002744125
n_fail        : 0
functions     : 0
forbidden_ops : []
decision      : bank-low-gain
path          : /tmp/neurogolf_case503_prune_stale_vi/task018_476ff037150c_prunevi.onnx
```

The scan was stopped after it kept producing only lower-gain `task018`
variants. Continuing it was delaying the submit cadence without improving the
chance of a submit-grade candidate.

## Decision

Reject as a standalone submit and bank the exact `task018` micro. The gain is
below the submit threshold and should be used only as part of a future reviewed
micro bundle.

## Submit Gap Analysis

The recent no-submit gap was not caused by Kaggle waiting or local submit
mechanics. It was caused by candidate supply:

- cases495-498 found no `submit-candidate`;
- case499 submitted and adopted `task031`;
- case501 had an exact `task184` candidate, but bundle gate blocked it;
- case502 repaired, submitted, and adopted `task363`;
- case503 found only bank-low-gain exact material.

To reach the requested 72 submits in 13 hours, the bottleneck is not submit
execution but finding enough exact, current-baseline candidates. The active
response is to keep six parallel workers running, divided by task ranges and
near-miss repair, and replace completed workers with new specialized searches.

## Next

Process the first worker-returned `submit-candidate` immediately. If workers
return only low-gain exact candidates, add them to `micro_backlog.md` and build
a reviewed bundle only after the aggregate gain is submit-worthy and excludes
documented hidden-risk families.
