# 20260702 case362 plan - saved mixed exact batch

## Context

Accepted Public Score is `7185.74` after case361.

The scoring metric is logarithmic:

```text
points = max(1, 25 - ln(max(1, cost)))
cost = params + memory_bytes
gain = ln(old_cost / new_cost)
```

Therefore this cycle prioritizes current-baseline relative reduction, not
absolute cost. Correctness remains the hard gate: any changed task must have
`n_fail=0` under `evaluate.audit_one`.

## Candidate Search

The first target was a larger single-task improvement:

- dense `Conv`/`QLinearConv` kernel crops after case361;
- `task243` fill-chain shortening and jump-kernel repair.

Those probes did not produce an exact candidate. The strongest `task243`
near-miss remains `fill_27 -> fill_26`, `22650 -> 22002`, but it has one
public-suite failure and the cheap row-mask repair breaks many cases.

## Batch Plan

Use banked exact candidates whose combined current-baseline gain clears the
practical submit-saving floor. The batch intentionally keeps each individual
change narrow and auditable.

| task | change | old cost | new cost | ln gain | risk |
|---:|---|---:|---:|---:|---|
| 002 | `out_bits` source `n_x9 -> n_tmp9` | 21089 | 20689 | 0.019149 | medium |
| 382 | alias combo `h_s0/h_s8 -> hsrc`, `v_s0 -> vsrc` | 5730 | 5670 | 0.010526 | low-medium |
| 173 | `p_nz -> v_nz` | 15721 | 15702 | 0.001209 | low |
| 397 | `m4 -> three_new` | 2848 | 2845 | 0.001054 | low |
| 325 | duplicate quantization initializer cleanup | 1932 | 1929 | 0.001554 | low |
| 023 | duplicate/unused initializer cleanup | 11222 | 11212 | 0.000892 | low |
| 280 | `dir_up/down` aliases | 6039 | 6035 | 0.000663 | low |
| 377 | `depth_i32 -> depth_i` | 8373 | 8368 | 0.000597 | low |
| 096 | `length_updates -> length_u8` | 8823 | 8818 | 0.000567 | low |
| 222 | remove unused axis initializer | 7597 | 7595 | 0.000263 | low |
| 396 | `am1 -> A1f` | 8080 | 8078 | 0.000248 | low |
| 066 | unused shape initializer cleanup | 16838 | 16835 | 0.000178 | low |
| 379 | unused scalar initializer cleanup | 10774 | 10773 | 0.000093 | low |
| 133 | `sg_gate_2/3/4 -> sg_eqi_2/3/4` | 33573 | 33570 | 0.000089 | low |
| 285 | unused initializer cleanup | 24537 | 24535 | 0.000082 | low |

Expected combined gain:

```text
0.037164
```

## Gate

Before submitting, copy only these candidates into `data/output/onnx` and run
`evaluate.audit_one(..., run_correctness=True)` for the changed task files.
Submit only if every changed task is exact (`n_fail=0`) and no forbidden ops or
model functions are introduced.
