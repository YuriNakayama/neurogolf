# 2026-07-01 case151 plan

## 方針

- floor: Public Score 7181.17
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task064.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task064.onnx`
- baseline: params 70, memory 12408, cost 12478, points 15.568277627329268, n_pass 267, n_fail 0

## 仮説

`task064` は上位色から rectangle と marker を選び、rectangle の row/col presence と marker の row/col presence から 24x24 の fill mask を作る。`h_active24` / `v_active24` で sentinel `255` を入れ、範囲外を無効化している。

実データで marker が rectangle の row/col と常に対応している、または sentinel が後段の range 比較で吸収されるなら、`h_low_s/h_high_s/v_low_s/v_high_s` の guard を外せる可能性がある。cost は 12478 と大きく、24x24 mask 1個でも submit 価値がある。

## 手順

1. `/tmp` 上の candidate ONNX で `h_active24` / `v_active24` guard を片側へ差し替える。
2. `fill_mask24` の h/v range 合成や final output overlay を個別に差し替える。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task064.onnx` に反映する。
4. submit し、Public Score が 7181.17 を上回れば採用、上がらなければ破棄する。
