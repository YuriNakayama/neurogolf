# 20260701 case341 result - source-crop and terminal projection scan

## Decision

No submit.

## Local Search

No new exact material candidate was found.

Exact candidates were limited to task349 kernel crop variants:

```text
task349 bundle: 24389 -> 24369, gain ~= 0.0008
task349 c0:     24389 -> 24381, gain ~= 0.0003
task349 c1:     24389 -> 24383, gain ~= 0.0002
task349 c2:     24389 -> 24385, gain ~= 0.0002
task349 c3:     24389 -> 24387, gain ~= 0.0001
```

The bundle is the same family as case246, already submitted with no Public move
and rejected. The single-channel variants are weaker and not worth banking.

Near-misses had large apparent gains but failed sample correctness, including:

```text
task364 terminal Add shortcut:   23013 -> 3567, sample_fail
task158 terminal Where shortcut: 32414 -> 5118, sample_fail
task219 terminal Add shortcut:   15964 -> 2578, sample_fail
task133 final Max branch bypass: 33573 -> 8006, sample_fail
task277 terminal Mul shortcut:   3745 -> 920, sample_fail
task192 terminal Where shortcut: 8621 -> 2143, sample_fail
```

## Submission

No Kaggle submission was made. This follows the submit-saving policy.

## Next Step

The broad generic cleanup families are currently depleted. Next effort should
target either a hand-inspected task-specific rewrite with clear semantics or a
new solver replacement candidate, rather than spending submits on known micros.
