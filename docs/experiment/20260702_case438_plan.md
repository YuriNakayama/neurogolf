# 20260702 case438 plan - task205 current-baseline residual probe

## Context

Accepted Public Score remains `7186.71`. The post-regression policy is still in
force: do not submit weak or unisolated changes merely to satisfy cadence.

`task205` is Kaggle-proven from earlier isolated improvements:

- case244 replaced an all-ones depthwise `Conv` with `AveragePool`;
- case369 compressed the patch core from float16 to uint8 and improved Public
  Score.

Current local audit:

```text
task205 cost=11550
params=279
memory=11271
status=ok
```

The largest non-output tensors are:

```text
cgrid      FLOAT [1,1,30,30]   3600 bytes
ohB        BOOL  [1,10,10,10]  1000 bytes
bm         BOOL  [1,1,30,30]    900 bytes
bmh_u8_q   UINT8 [1,1,30,30]    900 bytes
hsum       UINT8 [1,1,30,25]    750 bytes
vsum       UINT8 [1,1,25,30]    750 bytes
c6         FLOAT [1,10,4,4]     640 bytes
```

`cgrid` is the main target, but it is consumed by both `Equal(cgrid, bc1f)` and
`GridSample(cgrid, grid)`. A direct dtype rewrite is risky because `GridSample`
requires floating input and casting the full input to uint8 would likely cost
more than it saves.

## Hypothesis

The remaining current-baseline wins, if any, are likely small graph-equivalence
aliases already present in old scratch directories rather than a direct `cgrid`
dtype rewrite. Because `task205` has prior Kaggle-valid isolated evidence, a
medium candidate can be considered only if it is locally exact and clears the
post-regression gate.

## Method

1. Re-gate existing `task205` alias, guard, and cleanup candidates against the
   current accepted `data/output/onnx/task205.onnx`.
2. Treat selector/guard aliases as medium to high hidden risk unless they are
   both exact and mechanically explainable.
3. Submit only if the best current-baseline candidate is:
   - `n_fail=0`;
   - no forbidden ops/functions;
   - `gain >= 0.020`, or a deliberately reviewed prior Kaggle-safe pattern.
4. Otherwise record no-submit and move to the next task.
