# 20260702 case470 plan - task377 ring/rank current re-gate

## Context

`task377` is the next current-rank target after `task324`. Previous evidence:

- case121 found that `diff_left` guard, `CumSum` rank, packed values,
  outer/sentinel concatenation, and ring index paths are semantically required;
- case357/case362 found only the tiny exact `depth_i32 -> depth_i` cleanup;
- current rank shows `task377 cost=8368`.

The priority rule remains relative gain:

```text
gain = ln(old_cost / new_cost)
```

## Target

```text
task377
baseline: data/output/onnx/task377.onnx
task json: data/lake/neurogolf-2026/task377.json
```

## Hypothesis

Most ring/rank aliases are semantic and will fail, but stale-pool filtering may
surface a current-positive mechanical or dtype cleanup. Only current-baseline
positive candidates should be correctness-gated.

## Method

1. Audit accepted `task377.onnx`.
2. Gather `task377*.onnx` candidates from `/private/tmp` and `/tmp`.
3. Run cost-only scan against the current baseline.
4. Correctness-gate current positives:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

5. Bank only exact low-gain candidates; otherwise close `task377` and move to
   the next mid-cost task.
