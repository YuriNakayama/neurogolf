# 20260701 case314 result - adopt task219 residual alias bundle

## Result

- Decision: adopt
- Kaggle submission ref: `54229294`
- Previous Public Score: `7183.14`
- New Public Score: `7183.22`
- Public delta: `+0.08`

## Local Gate

`task219.onnx` after the change:

```text
params=91
memory=15873
cost=15964
n_pass=265
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=91
memory=17149
cost=17240
n_pass=265
n_fail=0
status=ok
```

Local cost reduction:

```text
17240 -> 15964
old_cost / new_cost = 1.0799
ln(old/new) ~= 0.0770 pt
```

## Change

Applied the residual alias bundle:

```text
mul_174  -> gat_164
gat_468  -> gat_392
red_497  -> red_345
red_501  -> red_425
red_505  -> red_429
pad_521  -> pad_445
sli_540  -> gat_537__h16
```

Dead pruning removed duplicated late block work and reduced the model from 232
nodes to 208 nodes.

## Verification

Individual `evaluate.audit_one` for `task219` passed with `n_fail=0`.
The full submission completed on Kaggle and improved the Public LB to
`7183.22`, so the local residual aliases transferred to the real grader.

## Conclusion

Adopted. This is the best recent single-cycle gain since case285. It validates
the updated workflow: keep small and late-discovered candidates in a backlog,
then promote the strongest exact bundle in the next cycle.

## Next Step

Use the queued `task174` inside-mask alias (`9326 -> 8978`, expected `+0.0380`)
as a case315 candidate unless a fresh scan finds a stronger exact bundle first.
