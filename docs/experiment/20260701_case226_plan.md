# case226 plan: task320 scatter lane trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task320` baseline は `cost=770` (`params=322`, `memory=448`, `n_fail=0`)。
- graph は `Slice -> Conv -> ScatterND`。
- params の大半は `scatter_indices` (`int64`, 288 要素)。

## 対象

- `data/output/onnx/task320.onnx`

## 仮説

2 channel の `updates` を 2 channel 分の scatter index に書き込んでいる。片側 channel、Conv filter の一部、または slice lane の一部が冗長なら `scatter_indices` と `updates` shape を縮められる。`770` からの相対削減率が大きいため一点検証する。

## 検証

- temp ONNX で `scatter_indices` の channel 0/1 片側化を probe する。
- `delta_weights` の filter 片側化、zero/identity 風の固定を probe する。
- `updates` 固定・Conv bypass は shape と正答性を確認する。
- `src.evaluate.scorer.audit_one` で `task320` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
