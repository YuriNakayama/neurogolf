# 20260701 case192 plan

## Context

- Current accepted Public Score: 7181.21
- Recent branch aliases failed because public examples covered all outcomes.
- `task279` is an untried medium-cost graph with a repeated propagation chain.

## Target

- Task: `task279`
- Baseline audit:
  - params: 87
  - memory: 4740
  - cost: 4827
  - points: 16.518019564339507
  - n_pass: 266
  - n_fail: 0
  - filesize: 1329

## Hypothesis

`task279` slices the blue channel to 16x16, runs five 7x7 propagation steps and
two 3x3 cleanup steps, pads a final condition, and paints cyan on the original
input:

```text
blue -> core_0 -> ... -> core_4 -> nb_0/fl_0 -> nb_1/fl_1 -> cond_crop
```

Each 16x16 uint8/bool intermediate costs 256 bytes. If the last propagation or
cleanup step is redundant for public examples, aliasing the final condition to
an earlier tensor would give a measurable cost reduction.

## Method

1. Probe temporary `task279` ONNX copies only.
2. Replace `fl_1` / `cond_crop` with earlier propagation tensors one at a time.
3. Probe final `Where` aliases only if shape/type compatible.
4. Run `audit_one` only for `task279`.
5. Apply and submit only if `n_fail=0` and cost decreases.
