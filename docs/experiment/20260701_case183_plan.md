# 20260701 case183 plan

## Context

- Current accepted Public Score: 7181.17
- case182 showed `task364` propagation depth is necessary.
- Continue prioritizing relative cost reduction with a single local graph surgery.

## Target

- Task: `task367`
- Baseline audit:
  - params: 5127
  - memory: 20000
  - cost: 25127
  - points: 14.868301755816644
  - n_pass: 266
  - n_fail: 0
  - filesize: 6234

## Hypothesis

`task367` fills rectangular regions in a 20x20 crop. The graph computes a uint8
`mask20`, casts it to bool, and uses:

```text
ch0m = Where(Cast(mask20), zero20, c0u)
```

Since `mask20` appears to be a 0/1 mask over background cells, this may be
equivalent to:

```text
ch0m = c0u - mask20
```

If ONNX `Sub` accepts the uint8 tensors and all examples satisfy
`mask20 <= c0u`, this removes the bool cast and Where output, replacing two
20x20 intermediates with one.

## Method

1. Build a temporary `task367` variant replacing `Cast` + `Where` with `Sub`.
2. Run `audit_one` only for `task367`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. If invalid or incorrect, record the probe and keep the baseline ONNX.
