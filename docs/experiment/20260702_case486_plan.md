# 20260702 case486 plan - task023 residual candidate re-gate

## Context

Accepted Public Score is `7186.79` after case485 adopted
`task023_greedy_S_S.onnx`.

The worker reports also listed three weaker `task023` candidates that were
submit-worthy against the old `9340` baseline:

```text
task023_97_Add_Ss3_TO_Ss2.onnx
task023_41_Add_Ss1_TO_Sadd1.onnx
task023_greedy_S.onnx
```

## Method

Re-gate these candidates against the new current baseline
`data/output/onnx/task023.onnx` before considering any further submit.
