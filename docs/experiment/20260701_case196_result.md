# case196 result: task332 parity stripe selector trim

## 結果

- 対象: `task332`
- baseline: `cost=563`, `params=134`, `memory=429`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

偶奇 branch、背景 branch、labels branch を temp ONNX 上で簡略化した。

| 変更 | cost | n_pass | n_fail | status |
|---|---:|---:|---:|---|
| `dot_color -> dot_colors_odd_width` | 538 | 110 | 157 | `INCORRECT` |
| `dot_color -> dot_colors_even_width` | 538 | 157 | 110 | `INCORRECT` |
| `background -> zero_u8` | 523 | 22 | 245 | `INCORRECT` |
| `background -> no_color_u8` | 523 | 0 | 267 | `INCORRECT` |
| `labels -> dot_color` | n/a | 0 | 0 | shape error |
| `labels -> background` | n/a | 0 | 0 | shape error |
| `width_parity -> width` | 559 | 110 | 157 | `INCORRECT` |

## 結論

`task332` は odd/even の両ケースを実データで使っており、幅外背景 branch も必要。短い graph で相対削減候補はあるが、完全一致を保てなかった。実 ONNX は変更していない。

## 次の一手

`Where(Cast)` の branch 固定は失敗が続いているため、次は意味を変えにくい重複 initializer、出力に寄与しない value_info/node、または accepted 例に近い `Cast + Where -> arithmetic` の exact pattern を横断で探す。
