# 20260630 case28 plan — 小グリッド単一Conv/低コストタスクの実測ベース再構築

## 仮説
case27 で小グリッドでも規則複雑と判明。だが個別に未検証のタスク（t394 tiling, t114 mirror候補）は
clean な規則かもしれない。実測ベースで最小 net 構築を試みる（仮定でなく build & measure）。

## 手法
t394（tiling 候補）と t114（mirror/halo 候補, 単一Conv 900）の規則を厳密検定。clean なら最小 net 化して
audit_one で実コスト測定。
