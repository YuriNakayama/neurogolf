# 20260701 case278 plan - task187 enclosed mask alias

## Context

- Current Public Score floor: `7181.80` after case277.
- case277 showed that shape-guided exact alias bundles with high relative
  reduction move the real Public Score.
- Continue prioritizing `old_cost / new_cost` over absolute cost.

## Candidate

Target: `task187`

Baseline local audit:

```text
params=664
memory=36150
cost=36814
n_pass=266
n_fail=0
```

Exact alias found by task-local scan:

```text
enclosed_b -> free_b_25
```

Candidate audit:

```text
params=664
memory=34900
cost=35564
n_pass=266
n_fail=0
status=ok
```

Relative reduction: `36814 / 35564 = 1.0351`.

Other `task187` aliases around the flood-fill/reach chain reduced cost but failed
correctness. No additional exact alias was found for a same-task bundle.

## Plan

Apply only `enclosed_b -> free_b_25` to `data/output/onnx/task187.onnx`, then
prune dead nodes and stale metadata.

Gate with `evaluate.audit_one` for `task187` only. Submit only if:

- `status=ok`
- `n_fail=0`
- cost remains `35564`
