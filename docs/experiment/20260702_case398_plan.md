# 20260702 case398 plan - shape-compatible alias scan

## Context

Accepted Public Score is `7186.24`. Case397's broad unary bypass scan produced
an ONNX-checker-valid candidate that segfaulted local `audit_one`, so future
generic scans need a stricter runtime-safety filter.

The useful search target is still relative reduction:

```text
gain = ln(old_cost / new_cost)
```

Known fresh exact micros are banked but too small to submit:

```text
case396 total gain ~= 0.004344
```

## Hypothesis

Restricting aliases to same dtype and same static shape should reduce runtime
crashes while still finding redundant value-path nodes. Scan tasks `121..260`
for single-output nodes whose candidate input has the same inferred tensor type
and shape as the output.

For each candidate:

1. Rewire consumers of the output to the same-shape input.
2. Prune stale value info and unused initializers.
3. Run task-local `audit_one` with correctness enabled.
4. Keep only exact lower-cost candidates.

## Gate

Submit only if a fresh exact candidate or compatible same-task bundle satisfies:

```text
n_fail=0
no forbidden ops/functions
gain >= 0.020
```

Otherwise, bank exact micros and skip submit.
