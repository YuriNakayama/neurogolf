# 20260701 case183 result

## Summary

- Target: `task367`
- Change: replace `Cast(mask20) + Where(mask, zero20, c0u)` with
  `Sub(c0u, mask20)`
- Baseline audit: params 5127, memory 20000, cost 25127, points 14.868301755816644
- New audit: params 5127, memory 19600, cost 24727, points 14.884348956992826
- Local gain: +0.016047201176181646
- Public Score: 7181.18
- Previous accepted Public Score: 7181.17
- Decision: accepted

## Verification

Individual audit for the changed task:

```text
task367.onnx
status: ok
params: 5127
memory: 19600
cost: 24727
points: 14.884348956992826
n_pass: 266
n_fail: 0
filesize: 6203
```

Kaggle submission:

```text
ref: 54208411
description: case183 task367 sub mask ch0m cost -400
status: COMPLETE
publicScore: 7181.18
```

## Difference

The original graph computed the background channel after fill as:

```text
mb = Cast(mask20 -> bool)
ch0m = Where(mb, zero20, c0u)
```

Because `mask20` is a uint8 0/1 fill mask contained within the background
channel, the same result is:

```text
ch0m = Sub(c0u, mask20)
```

This removes the bool `mb` tensor while preserving the final uint8 channel.
The `Where` output is replaced by the `Sub` output with the same shape, so the
net memory reduction is one 20x20 intermediate:

```text
25127 -> 24727
```

## Conclusion

Adopt. The exact local audit passed and the real Kaggle Public Score improved
from 7181.17 to 7181.18.

## Next

Search for similar one-hot mask patterns where `Where(Cast(mask), zero, base)`
can be replaced by arithmetic on a contained 0/1 mask.
