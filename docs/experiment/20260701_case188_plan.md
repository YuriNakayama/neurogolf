# 20260701 case188 plan

## Context

- Current accepted Public Score: 7181.21
- case187 showed a one-param local cleanup is too small to move the displayed
  Public Score and was reverted.
- The remaining valid uint8 `Sub` mask rewrites are exhausted for untried tasks.
- A duplicate-initializer scan found one exact duplicate group in `task066`.

## Target

- Task: `task066`
- Baseline audit:
  - params: 150
  - memory: 17145
  - cost: 17295
  - points: 15.241827278628731
  - n_pass: 266
  - n_fail: 0
  - filesize: 24048

## Hypothesis

`task066` has three identical int64 shape initializers:

```text
n_red_cyan_gather_i_shape    int64[3] = [1, 20, 1]
n_green_cyan_gather_i_shape  int64[3] = [1, 20, 1]
n_marker_out_gather_i_shape  int64[3] = [1, 20, 1]
```

Each is used only as the shape input of an `Expand`. Rewiring the green and
marker `Expand` nodes to reuse `n_red_cyan_gather_i_shape` and deleting the two
duplicates should preserve behavior and reduce params by six:

```text
cost 17295 -> 17289
```

## Method

1. Rewire only the duplicate shape initializer references in `task066.onnx`.
2. Delete the two now-unused duplicate initializers.
3. Run `audit_one` only for `task066`.
4. Submit if `n_fail=0` and cost decreases.
5. Adopt only if real Kaggle Public Score improves over 7181.21; otherwise
   revert ONNX/DVC and record rejection.
