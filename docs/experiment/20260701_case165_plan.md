# 20260701 case165 plan

## Context

- Current accepted Public Score: 7181.17
- case164 `task131` found a valid local cost -10, but Public Score stayed 7181.17 and the ONNX change was reverted.
- To avoid LB rounding, target a higher-cost task where a valid change can remove larger tensors.

## Target

- Task: `task138`
- Baseline audit:
  - params: 222
  - memory: 13543
  - cost: 13765
  - points: 15.470115582423292
  - n_pass: 266
  - n_fail: 0
  - filesize: 5578

## Hypothesis

`task138` decodes a cropped color grid, pads it with sentinel 255, gathers a dynamic bbox, detects which edge color matches the interior max, then chooses one of four `MaxPool` fills:

- vertical selector: `Where(g_down, v_fwd, v_rev)`
- horizontal selector: `Where(g_right, h_fwd, h_rev)`
- orientation selector: `Where(horiz_flag, hsel, vsel)`
- sentinel/valid guards: `ri_valid`, `ci_valid`, `bint_b`, `bx_in`

The cost is memory-dominated. If one direction, orientation selector, or sentinel guard is redundant for public examples, a single bypass can remove one or more large 31x31 / KxK tensors and may be large enough to move Public LB.

## Method

1. Probe one replacement at a time on temporary `task138` ONNX copies.
2. Run `audit_one` only for `task138`.
3. Apply and submit only if `n_fail=0` and cost decreases materially.
4. If Public Score does not exceed 7181.17, revert the ONNX/DVC change and document rejection.
