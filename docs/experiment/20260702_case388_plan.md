# 20260702 case388 plan - task023 remaining small subset

## Context

Accepted Public Score is `7186.08` after case387 adopted the second narrow
`task023` subset:

```text
task023: 10636 -> 10348
rewires: fl2 -> isone2, fl3 -> isone3
gain 0.027451
```

Remaining known exact task023 reductions from the current baseline:

```text
Sadd4_a -> AS4: 10348 -> 10276, gain 0.006982
Sadd4_a -> AS4 + b13 -> asg2: 10348 -> 10204, gain 0.014013
```

These are below the standalone submit gate. However, adding one more compatible
small rewire may clear `gain >= 0.020` while still staying much narrower than
the 19-rewire bundle.

## Hypothesis

A three- or four-rewire subset using `Sadd4_a`, `b13`, and one local
square/accumulator bypass can pass local correctness and clear the gate from the
current accepted cost `10348`.

The threshold is:

```text
target cost <= floor(10348 / exp(0.020)) = 10143
```

## Method

1. Re-scan current `task023.onnx` for exact single bypasses.
2. Test small combinations that include:
   - `Sadd4_a -> AS4`
   - `b13 -> asg2`
   - one or two additional exact single rewires
3. Keep the smallest exact subset that clears `gain >= 0.020`.
4. Submit only that subset; keep the full 19-rewire bundle deferred.

## Gate

Submit if:

- `evaluate.audit_one` for task023 gives `n_fail=0`
- current-relative gain is `>= 0.020`
- no forbidden ops are present

Adopt only if Kaggle Public Score increases.
