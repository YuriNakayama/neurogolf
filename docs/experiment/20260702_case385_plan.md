# 20260702 case385 plan - finish unary scan tail ranges

## Context

Accepted Public Score is `7186.00` after case384 adopted `task319`:

```text
task319: 19569 -> 19080, gain 0.025306
```

The recent case381-case384 run has been productive because it finds compatible
same-task unary/value-path bundles with good relative gain on medium or small
cost graphs.

Coverage still has gaps:

- case381 stopped early after finding `task013`
- case383 stopped the `320-400` continuation around `task366`

## Hypothesis

The unscanned tail ranges may contain another case381-style same-task bundle.
Individual bypasses may be too small, but compatible groups in one task can
clear the standalone submit gate.

## Method

Run two tracks:

1. Main agent: continue generic unary/value-path bypass scan for tasks `367-400`.
2. Parallel explorer: re-scan early tasks `001-054`, excluding already adopted
   `task013`, for fresh exact candidates and compatible bundles.

For each candidate:

- rewire one single-output node to a semantically weaker input
- prune dead nodes and stale value_info
- run cleanup
- audit only the touched task with `evaluate.audit_one`
- combine multiple exact wins in the same task where possible

## Gate

Submit only if a fresh single-task candidate clears:

```text
gain >= 0.020
```

Otherwise write a no-submit result and bank exact micros.
