# 20260630 case14 result — t077 は方向性2D規則で厳密化不可、nik harvest 0win（提出なし）

## 結論（提出なし、floor 7180.62 維持）

### novel solver: t077（14834, row-sep, 1→4 recolor）
- 1-cell が 2 に関係して 4 になる規則。観測: 2 の右側の 1-run 全体が 4（row1: 2@2→cols3-8 全 4）だが、
  両側 2 に挟まれた 1-run は隣接セルのみ 4（row2: cols0-2 のうち col2 のみ）。**方向性（2 の右側優先）
  と隣接/run の混在**で、単純 row-run 規則(0/266)では不一致。真の規則は 2D/方向依存で厳密化困難。
- t077 floor net(14834) が正しい規則を実装済み。row-sep は観測一貫性のみで真の規則は方向性 2D。

### harvest: nikitagajbhiye30/neurogolf-00（17:03, 新規参入 27votes）
- 全タスク cost 比較 → **0 win**（floor 7180.62 に収束/以上）。

## 教訓
- row-separable タスク（t243/t077）は連続して「観測は行一貫だが真の規則は方向性 2D」と判明。
  これらの floor net は near-minimal。現プリミティブでの安価厳密 novel net 化は困難。
- 新規参入バンドルも floor に収束。公開フロントは 7180.62 で安定。

## 次の一手
- 監視 harvest 継続（新 revision/新規参入毎に cross-bundle cherry-pick）。
- floor 7180.62 維持。7950 まで −769.38。

## cycle追記: t077 は完全に 2D（行に 2 が無くても 1→4）= flood 系、novel 不可確定

- 追加検証で t077 が **row-separable ではない**ことが判明: row11 `000000111110011001`（2 が皆無）でも
  1→4 が発生 → 2 は別行にあり**列方向に伝播**する 2D flood 規則。また 2 の左隣 1 は 4 にならず右側のみ
  = 方向性。row-sep 判定は観測データの偶然一致だった。
- **確定**: t243/t077 とも真の規則は方向性 2D flood で、flood-fill 系（case5 で cost∝node数 で構造的に
  golf 不可と実証済み）と同類。floor net が near-minimal。row/col-separable 経由の novel solver 路線は棄却。

## 本日（20260630）総括 更新（cycle1-14）
- **純増 +0.07**: cycle1(lucifer t233, 7180.58) + cycle12(frank2 t233+bor2 t054, **7180.62**)。
- **novel solver 全棄却**（実 LB or 規則特定で）: flood-fill(case5) / t347 OR-fold(case9,−4.43) /
  7件swap(case10,−16.76) / tiny-output(case11) / shape-recolor t364(case12) / interior-fill t243(case13) /
  directional-flood t077(case14)。**高コスト measurable は全て非局所 2D で floor が near-minimal**。
- **harvest が唯一の前進レバー**。bor3/kok/nik(新規参入) は全て 0win = フロント収束。floor 7180.62。
