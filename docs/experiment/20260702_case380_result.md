# 20260702 case380 result - skip Clip bypass micros

## Summary

No Kaggle submit.

Accepted Public Score remains `7185.88` from submission `54246480`
(`case378 task310 argmin slice axes gain 0.02455`).

## Local check

The Clip bypass scan tested current ONNX graphs by replacing each `Clip` output
with its data input, one node at a time, excluding the known hidden/runtime risk
families `task002`, `task118`, and `task233`.

Result:

- checked `83` Clip nodes
- found `56` exact single-node wins
- best single-node win: `task076`, `18743 -> 18713`, `gain 0.001602`
- best combined exact win: `task076`, two Clip bypasses,
  `18743 -> 18683`, `gain 0.003206`, `n_fail=0`
- combo artifact: `/tmp/neurogolf_case380_combo/task076_combo.onnx`

Other exact micro wins:

- `task018`: best single `38683 -> 38659`, `gain 0.000621`; many smaller
  two-cost wins
- `task366`: `36385 -> 36381`, `gain 0.000110`

A parallel non-Clip bypass scan found no current standalone low-risk candidate
with expected gain `>= 0.010`. The apparent `task382` hit was stale because that
change had already been adopted in case362.

## Decision

Reject for immediate submit. The best exact candidate is only `gain 0.003206`,
well below the low-risk submit gate `gain >= 0.020`.

Bank the `task076` combo and the smaller Clip bypass wins as a micro backlog.
Do not spend a Kaggle submit on Clip-only micros unless they are batched with a
larger independent improvement and pass local per-task audit.

## Next

Move away from generic Clip bypass scans. Prioritize candidates with a larger
relative reduction, especially tasks where a whole value path or intermediate
rank can be removed without changing correctness.
