# 20260701 case264 plan - task092 direct bool cast

## 背景

- Current floor: Public Score `7181.61`
- `task092` baseline: `cost=6992` (`params=75`, `memory=6917`, `n_fail=0`)
- case131 では `cost 6992 -> 6982` の小削減が Public 不変で不採用。
- 今回は `row_any` / `col_any` の `float32 -> float16 -> bool` 二重Castを直接 `float32 -> bool` にし、30要素中間2本を削る。

## 対象

- `data/output/onnx/task092.onnx`

## 仮説

`row_any` / `col_any` は `ReduceMax(input)` の非負 one-hot由来値であり、`Cast(float16 -> bool)` と `Cast(float32 -> bool)` は同じ真偽値になるはず。

```text
row_any -> Cast(float16) -> row_any__h16 -> Cast(bool) -> row_valid
col_any -> Cast(float16) -> col_any__h16 -> Cast(bool) -> col_valid
```

を

```text
row_any -> Cast(bool) -> row_valid
col_any -> Cast(bool) -> col_valid
```

へ置換できれば、`row_any__h16` / `col_any__h16` の2中間を削れる。

## 手順

1. `/tmp` に `task092` の direct bool cast 版 ONNX を生成する。
2. `src.evaluate.scorer.audit_one` で `task092` の完全一致と costを確認する。
3. `n_fail=0` かつ cost減なら本体へ反映して Kaggle submit し、Public Score で採否を決める。
