# 20260701 case155 plan

## Context

- Current accepted Public Score: 7181.17
- Latest accepted improvement: case150 `task379` (+0.01 LB)
- Priority rule: keep `n_fail=0` first, then prefer candidates with high relative cost reduction (`old_cost / new_cost`), not raw cost delta.

## Target

- Task: `task378`
- Baseline audit:
  - params: 116
  - memory: 3612
  - cost: 3728
  - points: 16.776372824194517
  - n_pass: 267
  - n_fail: 0
  - filesize: 7040

## Hypothesis

`task378` is a compact geometric renderer with several terminal masks and label merge stages. The most likely safe reductions are single-output bypasses that remove a boolean or `Where` node without changing solved examples:

- simplify color selection guards (`masked_score`, `payload_selector`)
- bypass payload/ray merge (`payload_or_ray`)
- bypass label merge stages (`lab1`, `lab2`, `lab3`)
- simplify distance or box clamp intermediates if the training/test distribution never needs the clipped branch

The expected delta is small, but the task has low enough cost that even a few removed bytes/nodes can be an efficient relative improvement if the audit remains perfect.

## Method

1. Probe one replacement at a time on a temporary ONNX copy.
2. Run `src.evaluate.scorer.audit_one` only for `task378`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. If the Kaggle Public Score does not improve over 7181.17, revert the ONNX/DVC change and record rejection.
