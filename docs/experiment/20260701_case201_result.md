# case201 result: task067 tiny threshold mask trim

## 結果

- 対象: `task067`
- baseline: `cost=47`, `params=8`, `memory=39`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

threshold mask の固定化と output 周辺の alias を temp ONNX で確認した。

| 変更 | cost | n_pass | n_fail | 備考 |
|---|---:|---:|---:|---|
| `q -> true` | 18001 | 0 | 266 | 大きな bool initializer が必要で逆効果 |
| `q -> false` | 18001 | 0 | 266 | 大きな bool initializer が必要で逆効果 |
| `m -> true` | 43 | 54 | 212 | 固定化で不一致 |
| `m -> false` | 43 | 0 | 266 | 固定化で不一致 |
| `output -> input` | 47 | 266 | 0 | graph output 名は変わらず実質 no-op |

## 結論

`task067` の 5 要素 threshold mask は小さいが正答性に必要。固定化では完全一致せず、実質的な cost 減候補は見つからなかった。実 ONNX は変更していない。

## 次の一手

同じ短い `Where` graph でも、`task186` は `cost=103` で 3x3 patch selector を持つため、threshold/patch branch の固定化または Pad 周辺の削減を確認する。
