# 20260702 case345 plan - task319 p10 predicate alias

## Context

- Current accepted Public Score floor: `7185.28` after case344.
- Submit-saving cadence is about one submit window every 30 minutes.
- `task118` table-pruning candidates were re-audited but rejected as
  hidden/private sensitive: the high-gain variants delete positive lookup-table
  targets rather than only duplicate scatter entries.
- A safer isolated `task319` candidate clears the medium-risk threshold.

## Candidate

Target `task319`.

Apply a compact boolean predicate alias:

```text
p10_pair_match -> p10_top_eq_all
```

Expected local change:

```text
baseline:  params=133, memory=20386, cost=20519
candidate: params=132, memory=19437, cost=19569
n_fail=0, status=ok
gain = ln(20519 / 19569) ~= 0.0474
```

Temp ONNX:

```text
/tmp/neurogolf_case345_alt/task319_p10_pair_match_to_p10_top_eq_all.onnx
```

This clears the medium-risk standalone threshold of `0.030`.

## Gate

1. Wait for the next submit cadence window after case344 where possible.
2. Copy the candidate to `data/output/onnx/task319.onnx`.
3. Run `src/evaluate.audit_one` only for `task319`.
4. Submit if `n_fail=0` and cost is lower.
5. Adopt only if Kaggle Public Score improves over `7185.28`; otherwise restore
   the DVC baseline and record rejection.

## Deferred

Do not submit `task118` patch lookup pruning in this cadence window. Although
rank1 was locally exact (`39237 -> 35807`, gain `0.0915`), risk audit showed
that most positive-table savings come from dropping example-specific targets,
not duplicate-only cleanup.
