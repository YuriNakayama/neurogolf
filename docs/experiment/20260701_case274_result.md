# 20260701 case274 result - adopt task255 bundled interior guard aliases

## Result

- Target: `task255`
- Baseline: `params=261`, `memory=22756`, `cost=23017`, `n_fail=0`
- Candidate: `params=261`, `memory=22470`, `cost=22731`, `n_fail=0`
- Relative reduction: `23017 / 22731 = 1.0126`
- Submit ref: `54221654`
- Public Score: `7181.69 -> 7181.70`
- Adoption: adopted

## Change

Applied six same-task guard aliases:

```text
r_full_interior   -> r_full_center
c_top_interior    -> c_top_center
c_bottom_interior -> c_bottom_center
right_top0_a      -> right_p0
right_bottom29_a  -> right_p29
top_bottom29_a    -> top_p29
```

Dead-node pruning removed the now-unused `And` guard intermediates and stale graph metadata.

## Verification

Individual local audit for the submitted `task255.onnx`:

```text
params=261
memory=22470
cost=22731
n_pass=265
n_fail=0
status=ok
```

DVC was updated and pushed after the Public Score improved.

## Conclusion

Adopted. Single aliases in this task were just below the practical threshold, but bundling exact local aliases in one task moved the real Public Score.

## Next Step

Continue alias-bundle search on memory-heavy tasks. `task338` has exact single aliases but the two-alias bundle failed local correctness, so it should be treated carefully. `task064` has two exact aliases but only `12478 -> 12430`; use it only if no larger bundled candidate is available.
