# 20260702 case471 plan - task396 GridSample current re-gate

## Context

`task396` is the next mid-cost current-rank target after `task377`.
Historical evidence:

- case119 found that recolor, A-color exclusion, second-color selection,
  horizontal/vertical grid masks, and 8-length kernels are semantically
  required;
- case370/case406 found only terminal label/scalar uint8 cleanup
  (`8078 -> 8074`), below standalone threshold and later contaminated in a
  rejected bundle;
- current rank shows `task396 cost=8078`.

## Target

```text
task396
baseline: data/output/onnx/task396.onnx
task json: data/lake/neurogolf-2026/task396.json
```

## Hypothesis

Most GridSample/recolor aliases will fail local correctness, but current
scratch may still contain a safe terminal or dtype cleanup. Because prior exact
material is micro-only, this cycle should be fast and use cost-only filtering
first.

## Method

1. Audit accepted `task396.onnx`.
2. Gather `task396*.onnx` candidates from `/private/tmp` and `/tmp`.
3. Run cost-only scan against the current baseline.
4. Correctness-gate current positives:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

5. Bank exact micro candidates only; do not submit stale terminal cleanup alone.
