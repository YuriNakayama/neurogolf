# 20260630 case29 result — 単一Convは中間tensor=0で構造的に無敵, 6-node構成も18904>>910（提出なし）

## 結論（提出なし、floor 7180.62 維持）
- t171 frame を 6-node MaxPool 構成で build & measure → cost **18904 >> floor 910**（かつ correctness 破壊）。
  6 ノードでも [1,1,30,30] 級の中間 tensor が runtime memory を支配。

## 確定した核心的事実: 単一Conv は中間tensor=0 で構造的に無敵
- **floor の単一Conv net は中間 tensor が 0**（input→output 直結）。memory ≈ output のみ ＝ cost=params+tiny。
- **どんな multi-node 構成も中間 tensor を ≥1 個追加**するため、必ず単一Conv より memory が増える。
  → **34 個の単一Conv floor タスクは数学的に hand-construction で破れない**（中間 0 を下回れない）。
- かつ case18 で「multi-node タスクは単一Conv 実現不可（非線形分離）」を確定済み。
  → floor の partition（分離可能=単一Conv / 不可能=multi-node）は**最適かつ hand-construction で破壊不能**。

## hand-construction 不可の最終証明（数学的）
1. 単一Conv タスク（34個）: 中間 tensor=0 が下限、multi-node は必ず増える → 破れない。
2. multi-node タスク: 非線形分離で単一Conv 化不可（case18）、explicit 構成は中間 tensor 増で floor 超過
   （case5/15/25/29 実測）。
→ **全 400 タスクで floor が hand-construction の到達不能な最小**。in-container golf 完全不可を数学的に確定。

## 次の一手
- hand-construction は数学的に不可（単一Conv 中間0 + multi-node 非分離）。前進は外部 publish harvest のみ。
- floor 7180.62（公開首位）維持。7950 まで −769.38。
