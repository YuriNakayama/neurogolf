# 20260702 case383 plan - finish unary continuation and task319 bundle

## Context

Accepted Public Score is `7185.95` after case382 adopted `task055`:

```text
task055: 3216 -> 3096, gain 0.038027
```

Case382 stopped the main unary/value-path scan around `task319`. It found:

- `task319`: compatible bundle `19569 -> 19232`, `gain 0.017371`
- `task285`: `24535 -> 22702`, `gain 0.077648`, exact locally but high-risk
  because it feeds uint8 directly into `TopK`

The `task319` bundle is close to the standalone gate but not enough by itself.

## Hypothesis

There may be one more compatible `task319` unary/value-path simplification that
pushes the same-task bundle past `gain >= 0.020`. If not, the unfinished task
range `320-400` may still contain another case381/case382-style compatible
bundle.

## Method

Run two tracks in parallel:

1. Targeted `task319` search:
   - start from the case382 exact bundle
   - test additional one-input bypasses and predicate aliases on top of it
   - keep only `n_fail=0` and lower-cost variants
2. Continue the generic unary/value-path scan for tasks `320-400`, excluding
   stale already adopted material.

Keep `task285` uint8 `TopK` input bypass in the backlog only. It is not a
submit candidate unless runtime compatibility is independently proven.

## Gate

Submit only if:

- a fresh low-risk single-task candidate clears `gain >= 0.020`; or
- a low-risk batch clears `gain >= 0.030` without known hidden/runtime risk.

Otherwise write a no-submit result and bank exact micros.
