# 20260701 case280 result - adopt task209 bundled vote/kernel aliases

## Result

- Target: `task209`
- Baseline: `params=185`, `memory=31867`, `cost=32052`, `n_fail=0`
- Candidate: `params=183`, `memory=31004`, `cost=31187`, `n_fail=0`
- Relative reduction: `32052 / 31187 = 1.0277`
- Submit ref: `54222626`
- Public Score: `7181.90 -> 7181.93`
- Adoption: adopted

## Change

Applied eight exact aliases:

```text
total_vote_p1_u8 -> total_vote_u8
fkr_v            -> fkr_ge0
fkc_v            -> fkc_ge0
kj_fit           -> kj_ge
ki_fit           -> ki_ge
row_in           -> r_le_bot
col_in           -> c_le_right
kernel_b         -> kern_eq_anchor_b
```

Dead-node pruning removed now-unused vote, fit, and kernel predicate
intermediates plus stale metadata.

## Verification

Individual local audit for the submitted `task209.onnx`:

```text
params=183
memory=31004
cost=31187
n_pass=266
n_fail=0
status=ok
```

DVC was updated and pushed after the Public Score improved.

## Conclusion

Adopted. The bundle moved the real LB by `+0.03`, consistent with the local
relative reduction. This was stronger than the original single alias
(`32052 -> 31651`) because smaller exact guard aliases were compatible.

## Next Step

Continue shape-ranked scans, but keep task-local bundle validation mandatory.
Avoid large noisy scans that include many out-of-bounds Gather probes; cap each
task to high-memory candidates first, then deepen only when exact aliases appear.
