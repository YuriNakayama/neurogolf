# 20260701 case169 plan

## Context

- Current accepted Public Score: 7181.17
- case168 found exact file-size reductions for `task263`, but scored cost remained
  unchanged because only scalar boolean nodes were removed.
- Continue prioritizing small and medium tasks where an exact rewrite can remove a
  tensor counted in `params + memory`.

## Target

- Task: `task329`
- Baseline audit:
  - params: 30
  - memory: 1029
  - cost: 1059
  - points: 18.034919654398593
  - n_pass: 266
  - n_fail: 0
  - filesize: 1155

## Hypothesis

`task329` computes rows to remove from total cell counts, then restores one column
using a center-count detector:

- `out_by_index_b` marks rows by index.
- `row_out_b` broadcasts that row mask.
- `center_i64` selects a column to keep.
- `col_keep_b` is a `ScatterElements` exception.
- `keep_input_b = row_out_b | col_keep_b`.

If the column exception is redundant or can be approximated by an existing row/index
mask for all public examples, removing `ScatterElements` and the final `Or` would be
a large relative reduction for a 1059-cost task.

## Method

1. Probe one alias at a time on temporary `task329` ONNX copies.
2. Run `audit_one` only for `task329`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. If no exact reduction exists, record low-fail candidates and keep baseline ONNX.
