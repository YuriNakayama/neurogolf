# 20260702 case505 plan - micro bundle risk gate hardening

Current accepted Public Score:

7187.19

## Background

Case504 adopted the isolated `task319` repair. The next available material is
mostly exact micro cleanup. A bundle worker found micro bundles that mechanically
pass `--allow-micro-bundle-gate`, but their gain depends on tasks with prior
hidden/runtime suspicion that were not yet encoded in `submit.gate`.

## Candidate Families

Mechanical best bundle:

```text
task002 20689 -> 20609 gain 0.003874
task035  2262 ->  2255 gain 0.003099
task037  4746 ->  4720 gain 0.005493
task354  3004 ->  2994 gain 0.003334
total gain 0.015801
```

No-task002 alternative:

```text
task018 38681 -> 38575 gain 0.002744
task035  2262 ->  2255 gain 0.003099
task037  4746 ->  4720 gain 0.005493
task354  3004 ->  2994 gain 0.003334
task379 10773 -> 10753 gain 0.001858
total gain 0.016530
```

## Hypothesis

The current gate is too permissive for micro bundles. `task035` and `task037`
belong to the case395 batch-ERROR family, and `task002` has a documented
bit-topology Public-regression warning. Letting these tasks satisfy a micro
bundle threshold would repeat the recent pattern of local-exact but hidden-risk
submissions.

## Method

1. Do not submit the mechanical micro bundle.
2. Add `task002`, `task035`, and `task037` to `REVIEW_RISK_TASKS`.
3. Re-run bundle gate on the mechanical candidates and verify they are blocked
   without explicit review override.
4. Bank any new exact non-risk micro found by workers.

## Submit Policy

No Kaggle submit for this cycle unless a separate standalone `submit-candidate`
arrives. This is a submit-count saving cycle: prevent a weak bundle from
consuming a slot and risking Public regression.
