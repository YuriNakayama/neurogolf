# 2026-06-30 case122 result

## 実 LB

- submit なし（local faithful 確認済み、submit は次サイクルで）
- floor: Public Score 7181.08

## 対象

- `task358.onnx`
- baseline: params 174, memory 4312, cost 4486, n_pass 266, n_fail 0
- updated: params 175, memory 4252, cost 4427, n_pass 266, n_fail 0
- 相対削減率: `4486 / 4427 = 1.01332`
- local point delta: `ln(4486 / 4427) = +0.01324`

## 変更

Cast チェーン冗長 2 件を除去:
- `Cast(row_ids_f → bool) → row_nonzero` と `Cast(row_nonzero → uint8) → row_nonzero_u8` を
  `Cast(row_ids_f → uint8) → row_ids_u8`（既存）＋ `Min(row_ids_u8, one_u8) → row_nonzero_u8` に置換。
- 同様に col 側も置換。
- 新 initializer: `one_u8 = 1` (uint8, 1 byte)。

## 試行

| 変更 | nodes | params | memory | cost | n_pass | n_fail | 結果 |
|---|---:|---:|---:|---:|---:|---:|---|
| row/col_nonzero bool 除去 | 47 | 175 | 4252 | 4427 | 266 | 0 | ok |

## 採否

- 採用
- 理由: n_fail=0、cost 4486→4427（-59）。`row_ids_f` は Einsum で得た色インデックス (0〜9) で非負整数なので Cast(float→uint8)→Min(1) = Cast(float→bool)→Cast(bool→uint8) と等価。

## independent_impl

- 対象: task358
- 手法: 手構築（Cast チェーン冗長分析 → bool 中間テンソル 2 個除去）
- faithful 結果: n_fail=0, cost 4486→4427, +0.013 pts

## 結論

Cast チェーン（float→bool→uint8）の冗長な bool 中間テンソルを除去することで、
59 bytes のコスト削減を達成。同パターンが他タスクにも存在する可能性あり。

## 次の一手

- submit で Public Score が 7181.08 を上回るか確認。
- 類似パターン（bool 中間テンソルが Cast 専用）を task201・task206 等で探索。
