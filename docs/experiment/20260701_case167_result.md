# 20260701 case167 result

## Target

- Task: `task345`
- Baseline cost: 1495
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task345`.

No candidate preserved exact correctness with a lower cost.

Closest reductions:

- `hit_4 -> blocker_4`: cost 1486, `n_fail=35`
- `hit_5 -> blocker_5`: cost 1486, `n_fail=41`
- `hit_6 -> blocker_6`: cost 1486, `n_fail=43`
- `hit_3 -> blocker_3`: cost 1486, `n_fail=46`
- `hit_2 -> blocker_2`: cost 1486, `n_fail=50`
- `shift_4 -> hit_4` or `shift_4 -> active_5`: cost 1486, `n_fail=52`
- `out_4 -> active_5`: cost 1477, `n_fail=52`
- `shift_2 -> hit_2` or `shift_2 -> active_3`: cost 1486, `n_fail=59`
- `out_2 -> active_3`: cost 1477, `n_fail=59`

Other chain-stage bypasses failed on 65 to 82 pairs, and aggressive aliases such as
`hit_k -> active_prev` failed on 263 of 264 pairs. Replacing final mask tensors with
smaller intermediate masks produced invalid `Where` shapes.

## Submission

- Not submitted.
- Reason: no `n_fail=0` cost-reducing candidate.

## Decision

Rejected. Keep the existing `task345.onnx`.

## Next

Avoid broad chain-stage bypasses for this family unless the failed stage can be made
conditional. Prioritize candidates with a larger expected `old_cost / new_cost` ratio,
especially medium-cost tasks that expose dtype-preserving guard trims or redundant
intermediate tensors.
