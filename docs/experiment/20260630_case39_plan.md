# 20260630 case39 plan — 学習fit による独自実装: t004 を小CNNで fit→蒸留→最小ONNX

## 現状把握（git log / 過去ログ）
- floor 7180.78（DVC 6e8d4642）。case35-38 で手構築/合成/sparse を検証し独自実装が floor 下限に
  阻まれることを実証したが、**strategy.md の3手法のうち「学習fit」を未実行**。本サイクルで実行。
- harvest は禁止（指示）。独自実装のみ。

## 仮説
t004（floor cost 5378, 18ノード）は **3×3 局所規則**（lut-decidable, k=3, lut=1301）で厳密 decode 可能。
単一 Conv では非線形で表せない（case37）が、**2〜3層の小 CNN（Conv+ReLU、許可op）**なら局所非線形
規則を厳密に学習でき、層が少なければ cost < 5378 を狙える。

## 対象 / 手法
1. t004 の (input one-hot[10,H,W] → output one-hot[10,H,W]) を 265 例で教師データ化。
2. 小 CNN（例: Conv3×3 [10→C] → ReLU → Conv3×3 [C→10]、C を最小化）を勾配学習で fit。
   全例 argmax 一致(=faithful n_fail=0) になる最小 C を二分探索。
3. 蒸留後、重みを ONNX 化（Conv/Relu のみ、静的形状）。int 量子化で params 削減も試行。
4. cost を audit_one で測定。floor 5378 未満なら submit。

## 採否
- faithful n_fail=0 かつ cost<5378 かつ 実 LB>7180.78 で採用（dvc/commit/push）。退行 revert。
- 失敗時は学習fitの失敗ログを result に記録し、合成/手構築の残余可能性を評価。
