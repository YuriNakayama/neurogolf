# 20260630 case83 plan — t286 の bit propagation 反復を一点検証する

## 背景

- 現 floor は Public best **7180.85**。
- case82 では最高コスト t233 を検証したが、TopK 候補数削減・dtype 置換・機械的削減はいずれも不採用。
- 次点の t286 は cost **47013**（params 741 / memory 46272）で、3036 node の path-tracing / bitset graph。op は `BitwiseAnd` 1067、`BitwiseOr` 965、`BitShift` 926 が中心。

## 仮説

t286 は seed からの 2/3 交互 trail を bitset で展開している。局所的には同じ方向の `BitShift`/`BitwiseAnd`/`BitwiseOr` が繰り返されているため、以下のどれかが可能なら memory を下げられる。

1. dead / duplicate node・initializer の機械的削除。
2. 末尾または一部の propagation 反復を削っても全 train/test/arc-gen で一致するか。
3. 先頭の dtype Cast や Reduce 系の出力をより小さい型へ寄せられるか。

ただし t286 は過去ログで逐次 BFS 相当とされており、反復短縮は失敗する可能性が高い。まず trace と graph consumer を見て、1 点だけを temporary ONNX で検証する。

## 対象

- `data/output/onnx/task286.onnx`

## 足切り

- 変更候補ごとに `src.evaluate.scorer.audit_one` で t286 の train/test/arc-gen 全ペア完全一致を確認する。
- n_fail=0 かつ cost 減少の候補のみ submit する。

## 採否

- 実 submit の Public Score が 7180.85 を上回れば採用。
- 上がらない、または faithful で不一致なら破棄。
