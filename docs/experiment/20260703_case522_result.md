# 20260703 case522 result - task011 structural read blocked

## Result

No submit.

Current accepted Kaggle score remains:

```text
Public Score: 7187.37
latest accepted ref=54271014
```

## Target

case520 V3 near misses:

```text
task011 baseline cost=2083
candidate cost=2037
gain=0.022331
n_fail=21
candidate=/tmp/neurogolf_agent_V3/task011_alias_018_4_noise_sum_4_TO_noise_sum_0_dce.onnx
```

## Findings

task011 computes nine `noise_sum_*` values for 3x3 blocks, chooses the first
clean block with `ArgMax`, then copies the selected block into the output tile.

The near miss aliases one `noise_sum` branch to another. This removes a
`Slice + ReduceSum`, but changes the selected clean block whenever the aliased
slot is the relevant clean/noisy discriminator.

Observed local failures:

```text
noise_sum_4 -> noise_sum_0 candidate:
  n_fail=21
  output differences are whole 3x3 tile placements, not localized pixels

noise_sum_3 -> noise_sum_0 candidate:
  n_fail=23
```

Because the wrong `clean_idx` is selected, repairing the output would require
reconstructing the missing noise branch or adding an equivalent selector. That
returns most of the removed cost and is unlikely to preserve `gain >= 0.020`.

## Decision

Do not submit. The candidate is a semantic selector collapse, not a local output
error. Move on to fresh standalone search.
