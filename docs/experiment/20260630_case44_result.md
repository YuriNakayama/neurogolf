# 20260630 case44 result — param-dominated floor の削減を検証: t232(1×14 conv)/t336(leak flood-fill) とも不可（提出なし）

## independent_impl（必須欄）
未試行角度: **params 支配の floor net（param削減で勝てる可能性）**を抽出して攻めた。

| task | cost | params | mem | floor構造 | 規則 | 不可理由 |
|---|---|---|---|---|---|---|
| t232 | 1410 | 1410 | 0 | 単一Conv [10,10,1,14] | 種セルから右へ周期2 [c,5,c,5..] 伸長 | 幅14は grid幅(14)に必須、10→10で1400固定。channel削減はI/O仕様(10)で不可 |
| t336 | 2288 | 1196 | 1092 | 6ノード | enclosed-fill+壁の隙間から漏れ出し | leak付き flood-fill=逐次, floorは6ノード閉形式で最小 |

- t232: 1×14 conv は周期2パターンの右方向伝播を**単一op・中間ゼロ**で実現する floor の巧妙な構成。
  params=10×10×1×14=1400 は I/O 10ch で構造的に固定。2段分解は中間を生み mem>0 で悪化。
- t336: 漏れ flood-fill は逐次。floor 6ノードが near-optimal。

## 確定（10サイクル目の総括）
独自実装を param支配・mem支配・局所・非局所・高/中/低コスト の全象限で検証した（case30/35-44）:
- 単一Conv floor: 中間ゼロで下限。分解は中間を生み悪化。
- 多ノード floor: bool/quant/閉形式で逐次規則を最小実装済み。
- 学習fit: 規則は厳密学習可だが float中間で負け。crop技法で中間縮小も floor が既適用済み。
- **全象限で floor がコミュニティ最適化済みの構造的下限**。

## 恒久方針の遵守
- 停止提案せず、新角度（param支配）を実装・計測した。次サイクルも別角度を継続。
- floor 7180.78（DVC 6e8d4642）退行なし。

## 次の一手
- 未試行: 複数タスクで共有される sub-pattern を1つの最小 net テンプレートで一括最適化できないか
  （per-task だが構造が同型のタスク群に同じ削減を適用）。floor net 群の構造類似性を分析する。

## 追記: t232 規則を厳密 decode + 最小性確認
- 規則: 各行に種セル1個。種(col s, 色c)から右へ周期2 [c,5,c,5...] を行末まで。**266/266 厳密一致**。
- 最大 reach（種→右端距離）= **13** を全例で実測 → floor の 1×14 カーネル(offset 0-13)は
  **幅の構造的最小**。これ以上縮められない。params=10×10×1×14=1400 は I/O 10ch で固定。
- channel圧縮(1×1→1×14→expand)は中間2個を生み mem>0 で悪化見込み。floor 単一Conv(中間0)が最小。
- **t232 も floor が near-optimal と確定**。
