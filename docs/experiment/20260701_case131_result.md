# 2026-07-01 case131 result

## 対象

- `task092.onnx`
- baseline: params 182, memory 6810, cost 6992, points 16.147478082664627, n_pass 265, n_fail 0

## 実施内容

`h_right_idx_active -> h_right_idx` と `v_bottom_idx_active -> v_bottom_idx` を同時適用し、右端・下端 index の active guard を削った。単独では各 `cost 6987`、同時適用では `cost 6982` まで下がった。

## 足切り

候補 ONNX:

```text
params 182, memory 6800, cost 6982, points 16.148909312335018
n_pass 265, n_fail 0, status ok
```

追加確認した失敗候補:

- `h_left_idx_active -> h_left_idx`: n_fail 7
- `v_top_idx_active -> v_top_idx`: n_fail 257 単独相当、組み合わせでも失敗
- `k_five` 縮小、`h_canvas`/`scalar_color_u8` 片側 branch 省略、span mask 省略はいずれも `n_fail>0`

## submit

- ref: `54205272`
- message: `case131 task092 remove redundant right/bottom active guards`
- Public Score: `7181.11`
- previous floor: `7181.11`

## 採否

不採用。

ローカルでは `cost 6992 -> 6982` の小削減で完全一致を保ったが、実 LB は `7181.11 -> 7181.11` で上がらなかった。ルール通り `data/output/onnx/task092.onnx` と `data/output/onnx.dvc` は baseline に戻した。

復元後確認:

```text
params 182, memory 6810, cost 6992, points 16.147478082664627
n_pass 265, n_fail 0, status ok
```

## 次の一手

`task092` は残る片側 guard/TopK/branch 簡略化がほぼ失敗し、通る削減も LB に反映しない程度だった。次は `task014` / `task182` / `task004` など、中 cost で branch や dtype 由来の相対削減がまだ見込めるタスクを優先する。
