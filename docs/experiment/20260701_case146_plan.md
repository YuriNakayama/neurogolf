# 2026-07-01 case146 plan

## 方針

- floor: Public Score 7181.16
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task046.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task046.onnx`
- baseline: params 97, memory 2221, cost 2318, points 17.251539976100304, n_pass 267, n_fail 0

## 仮説

`task046` は各列の path score から TopK で最大16列を取り、gray cell を可視 segment color で埋め、offset に従って3行へ shift する。`width_present`、`boundary_tail`、`gray_cells`、`offset_*_mask` の `Where` が多い。

実データで TopK 幅が常に十分、または boundary / gray guard が後段で吸収されるなら、末尾 guard や segment fill の一部を外して相対 cost を削れる可能性がある。

## 手順

1. `/tmp` 上の candidate ONNX で `width_present` guard、`gray_cells` fill、`boundary_tail` gating を個別に差し替える。
2. offset shift の片側候補が不要でないか、各 `shifted_row_*` を前段へ差し替えて確認する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task046.onnx` に反映する。
4. submit し、Public Score が 7181.16 を上回れば採用、上がらなければ破棄する。
