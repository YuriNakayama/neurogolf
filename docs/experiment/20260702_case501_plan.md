# 20260702 case501 plan - task184 broadcast constant compression

## Context

Current accepted Public Score:

```text
7187.12
```

Recent worker scans found no `submit-candidate`. The best low-risk current
candidate is an isolated mid-gain broadcast constant compression for
`task184`.

## Target

```text
task184.onnx
```

Candidate:

```text
/tmp/neurogolf_case349_scan/bcast/task184_bcast_ones30__1__30__TO__1__1_.onnx
```

Gate:

```text
2520 -> 2491
gain = 0.011574665243288571
n_fail = 0
functions = 0
forbidden_ops = []
decision = review-mid-gain
```

## Hypothesis

The candidate changes only initializer `ones30` from shape `[1, 30]` to
`[1, 1]`; all nodes, names, inputs, and outputs are unchanged. ONNX broadcasting
preserves behavior while reducing memory by 29 cost units. Similar broadcast
constant compression has prior Kaggle-safe evidence in earlier accepted small
cleanups.

## Method

1. Save current `task184.onnx` as the rollback baseline.
2. Copy the candidate into `data/output/onnx/task184.onnx`.
3. Run single-task gate and dry-run submit gate.
4. Submit isolated if the dry-run accepts exactly one changed task.
5. Adopt only if Public Score improves above `7187.12`; otherwise restore.
