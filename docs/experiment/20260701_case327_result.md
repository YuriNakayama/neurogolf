# 20260701 case327 result - task250 packed bool tail

## Decision

Adopted.

## Local Gate

Only `task250.onnx` was changed and audited.

```text
baseline:  params=142 memory=3076 cost=3218 n_fail=0 status=ok
candidate: params=66  memory=3076 cost=3142 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(3218 / 3142) ~= 0.0239 pt
```

## Submission

```text
ref=54231598
description=case327 task250 packed bool tail cost 3218 to 3142
status=COMPLETE
publicScore=7184.17
```

Previous accepted floor:

```text
7184.15
```

The Public Score improved by `+0.02`, so the candidate is accepted.

## Diff Summary

The old tail built a 6-channel bool tensor containing three explicit zero
channels and then padded it to the final output. The accepted replacement keeps
only the three live channels, casts the packed bool tensor to uint8, and uses a
one-hot `QLinearConv` with final pads to restore output channels 0, 2, and 5.

## Next Step

Continue with the remaining exact packed-tail candidate, `task365`, which was
measured at `4028 -> 3945` locally with `n_fail=0`.
