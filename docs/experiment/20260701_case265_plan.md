# 20260701 case265 plan - task281 direct uint8 bbox axes

## 背景

- Current floor: Public Score `7181.63`
- 優先基準: `n_fail=0` を絶対条件にし、絶対 cost ではなく `old_cost / new_cost` の相対削減率を優先する。
- `task281` baseline: `cost=2386` (`params=65`, `memory=2321`, `n_fail=0`)
- graph scan で 30 要素の二重 Cast を 2 本検出した。

```text
row_nonbg -> Cast(bool) -> bbox_rows_sparse -> Cast(uint8) -> bbox_rows_u8
col_nonbg -> Cast(bool) -> bbox_cols_sparse -> Cast(uint8) -> bbox_cols_u8
```

## 対象

- `data/output/onnx/task281.onnx`

## 仮説

`row_nonbg` / `col_nonbg` は非負の presence 値なので、`Cast(bool) -> Cast(uint8)` は
`Cast(uint8)` と同じ 0/1 になるはず。直接 uint8 化できれば、30 要素 bool 中間 2 本を削れる。

期待削減は約 60 memory bytes。`2386 -> 2326` なら `ln(2386/2326) ~= +0.0255`。

## 手順

1. `/tmp` に `task281` の direct uint8 cast 版 ONNX を生成する。
2. `src.evaluate.scorer.audit_one` で `task281` の完全一致と cost を確認する。
3. `n_fail=0` かつ cost 減なら本体へ反映して Kaggle submit し、Public Score で採否を決める。
