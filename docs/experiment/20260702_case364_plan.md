# 20260702 case364 plan - post-case363 candidate search

## Context

Accepted Public Score is `7185.78` after case362. Case363 was a skip-submit
search cycle.

The scoring rule remains logarithmic:

```text
gain = ln(old_cost / new_cost)
```

This cycle should only spend a Kaggle submit if a candidate clears the
risk-adjusted floor:

```text
single low-risk gain >= 0.020
single medium-risk gain >= 0.030
single high-risk gain >= 0.080
low-risk batch gain >= 0.030
mixed-risk batch gain >= 0.050
```

Correctness remains the hard gate (`n_fail=0`).

## Search Plan

1. Re-check the known `task233` exact candidate and see whether the prior Kaggle
   ERROR can be avoided with a tiny compatibility repair.
2. Run the newly merged `pipeline.case3` solver bank against the current bundle
   and compare solved outputs to the current ONNX costs.
3. Run a current-baseline same-shape single-alias scan over high-cost tasks,
   excluding known rejected or just-adopted families.
4. Inspect promising low-fail near misses only if their current-baseline gain is
   at least `0.030`.

Do not submit known public-regressing families such as broad `task002`
bit-topology pruning or `task286` bitwise rewires.
