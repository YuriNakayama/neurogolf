# 2026-06-30 case121 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task377.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task377.onnx`
- baseline: params 107, memory 8266, cost 8373, n_pass 266, n_fail 0

## 仮説

`task377` は ring 色列を `active_b -> CumSum -> rank_match -> packed` で構成している。`diff_left` guard、`rank`、または外周色 `outer4` の一部が冗長なら、20〜30 要素級の中間を複数削れて数 % の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `active_b`、`cand_left/diff_left`、`rank/depth`、`outer4/colors41` の簡略化を試す。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task377.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
