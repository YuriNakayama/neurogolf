# 20260701 case318 result - adopt task367 packed channel pad

## Result

- Decision: adopt
- Kaggle submission ref: `54230389`
- Previous Public Score: `7183.26`
- New Public Score: `7183.33`
- Public delta: `+0.07`

## Local Gate

`task367.onnx` after the change:

```text
params=4749
memory=16800
cost=21549
n_pass=266
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=5127
memory=18000
cost=23127
n_pass=266
n_fail=0
status=ok
```

Local cost reduction:

```text
23127 -> 21549
old_cost / new_cost = 1.0732
ln(old/new) ~= 0.0707 pt
```

## Change

Replaced the tail:

```text
Concat(ch0m, zero20, zero20, zero20, mask20, c5u) -> out20
Pad(out20, sp=[0,0,0,0,0,4,10,10], value=0) -> output
```

with:

```text
Concat(ch0m, mask20, c5u) -> packed3
QLinearConv(packed3, one-hot 3ch->10ch weights, pads=[0,0,10,10]) -> output
```

The one-hot projection maps `ch0m` to output channel 0, `mask20` to channel 4,
and `c5u` to channel 5. All other channels and the bottom/right spatial pad are
zero.

## Verification

Individual `evaluate.audit_one` for `task367` passed with `n_fail=0`.
The Kaggle Public LB improved to `7183.33`, so the structural tail replacement
transferred.

## Conclusion

Adopted. This is the kind of improvement the relative-ratio priority favors:
not a large task by absolute cost alone, but a clean structural deletion of
unnecessary intermediate tensors with `old/new > 1.07`.
