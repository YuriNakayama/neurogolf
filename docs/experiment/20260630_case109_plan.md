# 20260630 case109 plan - t018 残存 7x7 MaxPool の縮小検証

## 現状
- floor/Public: 7180.97
- 対象: `task018`
- baseline faithful: cost 43183, params 2087, memory 41096, n_fail 0

## 背景
case84 と case108 で `task018` の 5x5 `MaxPool` 鎖をそれぞれ 3段短縮し、どちらも Public 改善として採用した。

現行 graph に残る `MaxPool` は以下:

- 単発 7x7: `safe_name_70 -> MaxPool safe_name_71 -> Greater safe_name_72`
- 第一 5x5 鎖: `safe_name_119/121/123` まで（case84 で上限確認済み）
- 第二 5x5 鎖: `safe_name_619/621/623` まで（case108 で上限確認済み）

## 仮説
単発 7x7 pool は detector 用の dilation/近傍集約に見える。kernel を 5x5/3x3/1x1 へ縮小、または bypass できれば 900B 級中間の演算条件が軽くなる可能性がある。

## 手法
1. `safe_name_71` の `kernel_shape/pads` を 5x5, 3x3, 1x1 に変更した候補を作る。
2. `safe_name_72` の入力を `safe_name_70` に bypass した候補を作る。
3. `src.evaluate.scorer.audit_one` で `task018` の完全一致と cost を確認する。
4. `n_fail=0` かつ cost 減なら実ファイルへ適用して submit する。

## 足切り
- 変更対象は `task018` のみ。
- 全数 `audit_dir` は使わず、`audit_one` のみ行う。
