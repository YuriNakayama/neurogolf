# 20260703 case561 result - reject task316 dtype shrink

## Submission

- Submitted: `case561 full task316 cast dtype shrink`
- Previous accepted Public Score: `7187.57`
- Kaggle status: `SubmissionStatus.ERROR`
- Decision: rejected / not adopted

## Candidate

```text
task316
candidate: /private/tmp/neurogolf_case561_C/candidates/task316_cast4_10to2.onnx
local cost: 426 -> 407
estimated gain: 0.045626
n_fail: 0
functions: 0
forbidden ops: []
local gate: submit-candidate
```

## Result

The candidate passed local correctness and the pre-submit gate, but Kaggle
returned `ERROR` without a Public Score. This is treated as a runtime
compatibility failure, not an adoption candidate.

The ONNX baseline was not changed. `task316` is now marked as a review-risk task
in `backend/src/submit/gate.py` so future dtype-shrink candidates are not
submitted automatically.

## Search Context

Case561 used the new `scan-candidates` command from case560:

```text
range A: scanned 0 generated candidates, no submit candidate
range B: scanned 125 candidates, no submit candidate
range C: scanned 1002 candidates, 1 submit candidate
```

The scanner successfully found the candidate quickly, but the Kaggle ERROR shows
that local exactness is still insufficient for some dtype/runtime changes.

## Next Step

Avoid task316 dtype shrink unless a Kaggle-compatible isolated repair is proven.
Continue case562 with fresh exact candidates, but prefer graph rewrites that do
not alter runtime-sensitive dtype behavior.
