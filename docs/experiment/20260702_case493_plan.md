# 20260702 case493 plan - task051 cadence scan

## Context

Current accepted Public Score:

```text
7186.79
```

Recent scans continue to show cost-positive candidates failing local
correctness. Exact micros are being accumulated in `micro_backlog.md` instead
of spending submit slots.

## Target

```text
task051.onnx
```

Reason:

- moderate scratch pool;
- not currently assigned to active workers;
- no recent main-thread current-baseline scan;
- candidate priority is relative reduction `ln(old_cost / new_cost)`.

## Method

1. Gather unique `task051*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank positives using `evaluate.audit_one`.
3. Gate positives with `submit.gate.evaluate_candidate_gate`.
4. Submit only if a candidate reaches `submit-candidate`.

## Parallel Work

Active workers continue on:

- `task054/task066/task076/task077`;
- `task005/task048/task018`.
