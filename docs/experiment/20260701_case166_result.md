# 20260701 case166 result

## Summary

- Task: `task017`
- Baseline:
  - params: 1804
  - memory: 10922
  - cost: 12726
  - points: 15.548597576207262
  - n_pass: 266
  - n_fail: 0
  - filesize: 13998

## Probes

No valid cost-reducing change was found.

`matches_h` dtype probes:

- Cast bool -> `uint8`: invalid `ReduceSum` input type
- Cast bool -> `int8`: invalid `ReduceSum` input type
- Cast bool -> `uint16`: invalid `ReduceSum` input type
- Cast bool -> `int32`: n_fail 0, but cost increased to 14846
- Cast bool -> `float32`: n_fail 0, but cost increased to 14846

Structural bypasses:

- `matches_h -> matches_b`: invalid `ReduceSum` bool input
- `scores -> matches_h`: invalid `ArgMax` shape
- `selected_labels -> pattern0`: cost unchanged, n_fail 266
- `onehot_raw -> selected_labels`: invalid `Pad` type mismatch

## Submission

No submission. No exact-match cost reduction was found.

## Decision

Rejected. The float16 match-score tensor appears to be the smallest numeric dtype accepted by this ONNX Runtime `ReduceSum` path.

## Next Step

Avoid integer `ReduceSum` dtype probes for this runtime. Continue with exact-preserving guard trims, especially candidates that remove masks or guards without changing operator type constraints.
