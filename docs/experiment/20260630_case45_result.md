# 20260630 case45 result — floor は channel削減を既に網羅適用済み、過剰channelタスクなし（提出なし）

## independent_impl（必須欄）
未試行角度「floor net 構造クラスタリング → 過剰 channel の削減波及」を検証:

- 全 single-Conv floor を weight shape でクラスタリング → **コミュニティは既に in_channels を
  真の使用色数まで削減済み**:
  - t073 `[10,1,4,1]`=40, t082 `[10,1,6,3]`, t122 `[10,5,4,4]`, t258 `[10,5,1,3]`,
    t180 `[10,5,2,2]`, t314 `[10,1,3,3]`=100 等、**in=1 や 5 を多用**。
  - 私が「新技法」と思った channel 削減は **floor に既適用済み**。
- t232 `[10,10,1,14]` の in=10 を検証 → 種色が **8色(1,2,3,4,6,7,8,9)** で全 channel 必要。過剰でない。

## 確定（11サイクル目）
- floor は single-Conv の **in_channels を真の使用色数まで削減**する param 最小化を全タスクで適用済み。
- 「過剰 channel で削減余地のあるタスク」は**存在しない**。
- 同一構造の単純な波及削減先もなし（各 weight shape はほぼ 1 タスクずつ = 既に per-task 最適化）。

## 構造的結論（再々確認）
独自実装の全角度（規則decode/手構築/学習fit/sparse/crop技法/param支配/channel削減/構造クラスタ）を
case30/35-45 で検証。floor はコミュニティが per-task で:
- 単一 scorable op（中間ゼロ）or 最小多ノード（bool/quant/閉形式）
- in_channels/kernel を真の必要量まで削減
を適用済みの**構造的下限**。現 op セット・cost モデルで独自実装の純増余地は見つからない。

## 恒久方針の遵守
- 停止提案せず、新角度（構造クラスタ）を実装・計測した。次サイクルも別角度を継続。
- floor 7180.78（DVC 6e8d4642）退行なし。

## 次の一手
- 未試行: 2タスクを1つの onnx で兼ねることは不可(per-task)だが、ある複雑タスクの floor net が
  別タスクの規則の上位互換なら流用で削減できるか。または ONNX opset の新しい op（cost 未計上の
  挙動）の悪用余地を scorer 実装精読で探す。

## 追記: scorer 実装精読（cost-model 抜け道の探索）
`_calculate_memory`(scorer.py:169-256) を精読:
- memory = Σ tensor_memory（全 node 出力テンソル, shape-inference + ORT trace の max）。
- **"input"/"output" 名のテンソルのみ memory から除外**（line 221）。
- ORT は `ORT_DISABLE_ALL`（line 343）で fusion 無し → 全中間が確実に計上される。
- sparse op-input は shape_inference が拒否（case38 で実証, 採点不能）。
- **抜け道なし**: 唯一の「memory 0」は単一 node が直接 "output" に書く構成（=single-op 原則, 既知）。
  cost を下げる自由度は「単一 scorable op で正答 + params 最小（channel/kernel 削減）」に限定され、
  floor が全タスクで到達済み。

## 11サイクルの最終確定
独自実装(規則decode/手構築/学習fit/sparse/crop/param/channel/構造クラスタ/scorer精読)を網羅。
floor 7180.78 はコミュニティ最適化済みの構造的下限。現ルール下で独自実装の純増余地は技術的に枯渇。
ただし恒久方針に従い停止せず、新しい op 構成・規則表現・コンペ仕様変更があれば即試行を継続する。
