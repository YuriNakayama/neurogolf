# 20260702 case505 result - no-submit risk gate hardening

## Outcome

No submit.

Accepted Public Score remains:

7187.19

## Reason

The bundle worker found micro bundles that mechanically passed the pre-submit
micro-bundle gate, but all submit-threshold bundles depended on documented
risk families:

- `task002`: bit-topology alias with prior Public-regression warning;
- `task035`: case395 batch-ERROR family member;
- `task037`: case395 batch-ERROR family member.

The clean strict pool excluding those tasks had only `0.010394` total gain,
below the `0.015` micro-bundle threshold.

## Gate Change

Updated `backend/src/submit/gate.py`:

```text
REVIEW_RISK_TASKS += {2, 35, 37}
```

This makes the previously mechanical micro bundles fail normal micro-bundle
gating because micro bundles may contain only `bank-low-gain` or
`submit-candidate` decisions, not `review-known-risk`.

## Additional Worker Findings

Banked new exact micro:

```text
task198
cost: 13572 -> 13542
gain: 0.002212880
n_fail: 0
functions: 0
forbidden_ops: []
path: /tmp/neurogolf_parallel_C/fast_bypass_short/task198_045_Div_in0_iota30_TO_rp1.onnx
decision: bank-low-gain
```

Not banked:

- `task279` raw near miss `4827 -> 4315`, gain `0.112128`, `n_fail=2`.
  Functional repairs were exact but increased cost above baseline, so there is
  no positive candidate.
- cleanup worker re-found existing `task018` and `task206` banked micros only.

## Decision

Reject local-only mechanical bundle and keep searching. This preserves submit
budget and reduces the chance of another local-exact Public regression.

## Next

Continue the active parallel searches. Submit only a standalone
`submit-candidate` or a micro bundle that passes the hardened gate without
documented risk families.
