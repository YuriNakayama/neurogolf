# case236 plan: task324 line kernel trim

## 現状
- 採用済み Public Score floor: 7181.21
- `task324` baseline は `cost=8894` (`params=1586`, `memory=7308`, `n_fail=0`)。
- `x_kernel` は shape `(1,1,39,39)` の uint8 2 値 table で、`QLinearConv(... pads=19)` の line detection に使われる。
- params と memory の両方が中程度あり、kernel を縮められれば相対削減が見込める。

## 対象
- `data/output/onnx/task324.onnx`

## 仮説
実データの線検出に 39x39 全域が不要なら、`x_kernel` の外周を削って pads を縮められる。`line_any` や rare color branch が固定なら後段 `Where` も複数削れるが、まず scorer に直接効く kernel shape 削減を優先する。

## 検証
- `x_kernel` を center 37/35/31/21 crop にし、`QLinearConv` pads を対応して縮める。
- `line_any` / `rare_color0_is_bg0` / terminal `Where` の固定化を probe する。
- `src.evaluate.scorer.audit_one` で `task324` の全ペア完全一致と cost を確認し、`n_fail=0` かつ十分な cost 改善候補だけ実 ONNX へ反映する。
