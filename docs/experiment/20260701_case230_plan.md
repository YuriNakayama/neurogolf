# case230 plan: task084 scatter offset table trim

## 現状
- 採用済み Public Score floor: 7181.21
- `task084` baseline は `cost=2038` (`params=241`, `memory=1797`, `n_fail=0`)。
- `row_offsets_full_i32` は shape `(1,5,2,21)` の int32 initializer で、5 channel 分に同じ `(1,1,2,21)` offset table が繰り返されている。
- `ScatterElements(axis=2)` の `indices` と `updates` は shape `(1,5,2,21)`。

## 対象
- `data/output/onnx/task084.onnx`

## 仮説
`row_offsets_full_i32` を `(1,1,2,21)` に縮小して `Add`/`ScatterElements` が broadcast を許せば params を 168 要素削れる。許されない場合でも、`updates` 側を同じ小 shape に落とせるなら大きな相対削減が見込める。

## 検証
- `row_offsets_full_i32` を `(1,1,2,21)` / `(2,21)` / `(21)` に縮小して `audit_one` する。
- `update_channel_mask` の shape 縮小、channel 固定化、zero/one 化を個別 probe する。
- `src.evaluate.scorer.audit_one` で `task084` の全ペア完全一致と cost を確認し、`n_fail=0` かつ cost が下がった候補だけ実 ONNX へ反映する。
