# 20260630 case77 result — t285 の反復削減・主要中間置換は不採用（提出なし）

## independent_impl:
t285（object-assembly / source connected component を marker 側へ scatter）を個別精査:
- 規則: pivot object を見つけ、source 側 connected component を marker 色で対応位置へ複写する。
- floor graph は `TopK` で candidate/pivot/source を抽出し、`MaxPool` 反復で connected source を広げ、
  `ScatterElements` で output grid を構築する。

## faithful 検証
connected-source 反復を短縮する probe:
- iter0: cost 25526 まで下がるが **n_fail=265**
- iter1: n_fail=259
- iter2: n_fail=179
- iter3: n_fail=43
- 現行 iter4: n_fail=0

## trace / 中間分析
- 最大級中間:
  - color decode `Conv` float `[1,1,30,30]` = 3600B
  - candidate index `Add` int32 `[4,33,3]` = 1584B ×2
  - flat/scatter grid `[900]` / `[1,1,30,30]` = 900B 級
- color decode を量子化するには input を uint8 化する必要があり、`[1,10,30,30]` uint8 9000B が増えて悪化。
- index `Add` は Gather/Scatter 用に int32 が必要で、低 dtype 化は Gather index 型制約に合わない。
- TopK の K は `shape_cells/max_cells` と graph 全体に連動しており、単独低減は不可。規則上も source component サイズが可変で反復短縮は失敗。

## 確定
- t285 の主要 memory は規則実装上の必要中間で、case74 型の dead-end / redundant Cast ではない。
- 反復 depth は arc-gen で必要。単純削減は不正答。
- 改善候補なし。floor 7180.85 維持、提出なし。

## 次の一手
- t285 は一旦保留。次は同じ memory-dominant でも出力が小さい t319、または rank/order 系 t338 を精査する。
