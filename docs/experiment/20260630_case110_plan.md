# 20260630 case110 plan - t286 final bitset lane の個別削減

## 現状
- floor/Public: 7180.97
- 対象: `task286`
- baseline faithful: cost 47013, params 741, memory 46272, n_fail 0

## 背景
case83 では末尾 propagation の代表として `Concat n4676` 先頭入力 `v4675` を前段 bitset へ置換し、`n_fail>=215` で不採用だった。

現行の final bitset 復元は以下:

```text
Concat(v4675, v4643, v4611, ..., v2403, v3907) -> v4676
Reshape -> BitwiseAnd -> Equal -> final Where
```

`v4675` 以外の lane が冗長かは未確認。

## 仮説
一部 lane は hidden/arc-gen 上で常に不要、または隣接 lane と重複している可能性がある。該当 lane を zero 化または隣接 lane に置換できれば、前段 bitwise chain の一部を prune できる。

## 手法
1. `Concat n4676` の各 lane を代表的に数本選び、zero lane または隣接 lane に置換する。
2. 未使用 node を prune した一時候補を作る。
3. `src.evaluate.scorer.audit_one` で `task286` の完全一致と cost を確認する。
4. `n_fail=0` かつ cost 減なら実ファイルへ適用して submit する。

## 足切り
- 変更対象は `task286` のみ。
- 全数 `audit_dir` は使わず、`audit_one` のみ行う。
