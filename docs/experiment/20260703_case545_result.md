# 20260703 case545 result - adopt full task023 combined aliases

Accepted Public Score before cycle:

- `7187.43`

## Candidate

Task:

- `task023`

Candidate:

```text
/private/tmp/neurogolf_agent_case545_B/task023_submit_ready_combined_asg3_asg0_hadd1sel.onnx
```

Combined aliases:

```text
asg3 -> asgs3
asg0 -> Hadd0
Hadd1_sel -> isone1
```

Local gate:

```text
baseline cost : 8548
candidate cost: 8332
gain          : 0.025594
status        : ok
n_fail        : 0
functions     : 0
forbidden ops : []
decision      : submit-candidate
```

## Submission

The first submit in this cycle used an isolated one-file ONNX directory:

```text
description  : case545 task023 combined aliases
status       : COMPLETE
Public Score : 15.97
```

This was a packaging mistake, not a candidate-quality result. The competition
submission must contain the accepted 400-task baseline with only the changed
task overwritten. A one-file submission scores only that one task.

Corrected full 400-task submission:

```text
description : case545 full task023 combined aliases
status      : COMPLETE
Public Score: 7187.46
```

The corrected dry-run confirmed 400 ONNX files and the expected one-task diff:

```text
changed: 1
task023 8548 -> 8332
gain: 0.025594
decision: submit-bundle
```

## Parallel Search Summary

No non-task023 standalone candidate with gain `>=0.020` was found.

Best safe max-5 bundle before the task023 combined candidate remained:

```text
task023 8548 -> 8476  gain 0.008459
task368 6225 -> 6217  gain 0.001286
task117 4148 -> 4145  gain 0.000724
task333 3227 -> 3225  gain 0.000620
task234 6845 -> 6841  gain 0.000585
total gain: 0.011673
decision: blocked-bundle-gate
```

Additional worker findings:

- `task206 4183 -> 4182`, gain `0.000239`, exact but too small.
- `task105`, `task381`, `task354`, `task379`, `task383`, `task349`, `task219`, `task301`, `task031` were stale or already absorbed.
- Large cost-only aliases from `/private/tmp/neurogolf_agent_V3` failed full correctness and were not submitted.

## Decision

Adopt.

```text
Accepted Public Score: 7187.43 -> 7187.46
delta: +0.03
```

The accepted local baseline was updated:

```text
data/output/onnx/task023.onnx
```

Post-adoption self-gate confirms the adopted task is exact with cost `8332`:

```text
task023 baseline cost : 8332
task023 candidate cost: 8332
status                : ok
n_fail                : 0
```

## Process Fix

Future isolated-task submits must still build the submission directory from all
accepted baseline ONNX files first, then overwrite only the changed task. Do
not submit a one-file directory unless intentionally measuring a one-task score.
