# 2026-07-01 case139 result

## 対象

- `task333.onnx`
- baseline: params 435, memory 2792, cost 3227, points 16.92069180794804, n_pass 265, n_fail 0

## 実施内容

左右上下4方向の line candidate branch について、candidate 省略、axis mask 省略、code selection mask 省略を `/tmp` 上の候補 ONNX で個別に検証した。

## 足切り結果

`n_fail=0` で cost が下がる候補はなし。

候補 branch 省略:

- `left_candidate -> zero_u8`: cost 3020, n_fail 125
- `right_candidate -> zero_u8`: cost 3018, n_fail 87
- `up_candidate -> zero_u8`: cost 3020, n_fail 120
- `down_candidate -> zero_u8`: cost 3018, n_fail 98

2段 mask の片側省略:

- `left_candidate -> left_axis_color`: cost 3113, n_fail 125
- `right_candidate -> right_axis_color`: cost 3113, n_fail 87
- `up_candidate -> up_axis_color`: cost 3113, n_fail 120
- `down_candidate -> down_axis_color`: cost 3113, n_fail 98

code selection mask 省略:

- `left_code_sel -> row_codes`: cost 3187, n_fail 51
- `right_code_sel -> row_codes`: cost 3187, n_fail 20
- `up_code_sel -> col_codes`: cost 3187, n_fail 49
- `down_code_sel -> col_codes`: cost 3187, n_fail 25

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

`task333` の4方向 branch はすべて実データ上必要だった。`Where(..., zero)` が多いが、候補線の存在判定と位置 mask は完全一致に直結している。

## 次の一手

横断候補から `task382` を見る。`vflag` による vertical/horizontal branch と `x8c4`/`x8r4` source selection があり、片側 branch が固定的なら相対削減率が見込める。
