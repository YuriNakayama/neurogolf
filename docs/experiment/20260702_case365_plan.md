# 20260702 case365 plan - submit-saving recheck of near misses

## Context

Accepted Public Score is `7185.78` after case362. Case363 and case364 skipped
Kaggle submit because available exact candidates were either below threshold,
known Public-regressing, or platform-risk.

The competition metric is still treated as:

```text
points = max(1, 25 - ln(max(1, cost)))
cost = params + memory_bytes
gain = ln(old_cost / new_cost)
```

Kaggle's official overview and discussion snippets confirm that the current
cost is parameter count plus memory footprint, and that ONNX graphs must keep
static shapes. Therefore this cycle prioritizes relative reduction and
submission risk, not absolute cost.

## Submit-saving gate

Use the existing submit-conservation thresholds:

- low-risk standalone: `gain >= 0.020`
- medium-risk standalone: `gain >= 0.030`
- high-risk standalone: `gain >= 0.080`
- low-risk batch: `gain >= 0.030`
- mixed-risk batch: `gain >= 0.050`

The 30-minute cadence is a budget guard, not a reason to submit weak material.
If no exact candidate clears the risk-adjusted gate, skip submit and record the
banked result.

## Targets

1. Recheck the case364 near misses with parallel subagents:
   - task077: `b2mn -> b1mn`, `14834 -> 13994`, gain `0.058293`, `n_fail=4`
   - task110: `public_row_table -> row9_public_table`, `13591 -> 13069`,
     gain `0.039165`, `n_fail=1`
2. Run current-baseline `pipeline.case3 surgery` into scratch to find any
   remaining exact low-risk cleanup after case362.
3. Submit only if a fresh exact candidate clears the gate. Otherwise update the
   experiment log and keep `data/output/onnx` unchanged.

## Risk Notes

- Do not resubmit task002 bit-topology pruning; it is locally exact but has a
  documented Public regression family.
- Do not resubmit task233 `uint8 TopK` compression without a compatibility fix;
  case347 produced Kaggle `SubmissionStatus.ERROR`.
- Tiny cleanup such as task285 surgery should be banked, not submitted alone.
