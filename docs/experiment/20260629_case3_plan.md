# 20260629 case3 plan — k-局所タスクの単一 Conv 化（線形分離による大幅 golf）

## 現状把握

- floor = `data/output/onnx`（DVC, combined_best, 実 LB **7172.43**）。隔離コンテナ（sibling
  worktree 無し → dsl-harvest 不可）。data/lake は空だったため Kaggle から 400 タスク +
  公式 neurogolf_utils.py を取得し faithful 採点基盤を復旧（onnx 1.20.0 / ort 1.24.1, offset +0.11）。
- 全 400 タスク scorable、cost-only=7172.74（floor と整合, 0点タスク無し）。

## 仮説（過去結論への反証）

過去ログ E13/winsweep は「単層 Conv は 43 タスク全滅（lstsq/argmax 不一致）」と結論。しかし
**採点デコードは argmax ではなく `out>0` のチャネル独立閾値**。これに対し:
1. 「out[r,c] = 入力 k×k 窓の決定的関数」を全 example（train/test/arc-gen）で満たす最小 k を
   網羅判定 → 14 タスクが厳密 k-局所（k=3/5/7）かつ高コスト（潜在 gain 合計 +17.4）。
2. 各出力チャネル j を「窓→色j」陽性 vs その他+グリッド外（全オフ）陰性の **max-margin
   LinearSVC** で線形分離。分離可能なら単一 Conv[10,10,k,k]（mem=0, cost=params）で厳密解。

## 本サイクルで回す 1 改善

- 14 候補に分離可能性テスト→Conv 構築→faithful 監査を一括適用。n_fail=0 かつ cost<floor の
  win を floor へ overlay → submit。

## 採否

- faithful（train/test/arc-gen 全 example）で n_fail=0 確認 → submit → 実 LB が上がれば採用。
- リスク: hidden private で線形外挿が真ルールと乖離する可能性 → 実 LB が下回れば revert。
  （結果は result.md 参照: t192 のみ分離可能だったが hidden で 0 点、REJECT）
</content>
