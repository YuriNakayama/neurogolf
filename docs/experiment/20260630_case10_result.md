# 20260630 case10 result — 7件 mirzayasir swap は LB −16.76、REJECT（floor の negative-pad net が最適）

## 実 LB / 採否
- **REJECT**。7 タスク（045/127/135/146/149/240/384）を mirzayasir 版に swap した提出 =
  **Public Score 7163.82**（floor 7180.58 から **−16.76**）。floor 未変更（/tmp で probe, DVC
  `713fc67a` 維持, LB 最良 7180.58 を保持）。

## 結論: floor の negative-pad net は Kaggle で正常採点され、かつ最安

- case9（t347 MaxUnpool）と同根の現象を 7 タスクで再確認: **ローカル ORT の score_error
  （negative-pad Conv/ConvTranspose/MaxPool）は Kaggle scorer では発生せず、正常に満点採点される**。
- mirzayasir の scorable 版（cost 366〜74751）への置換は **−16.76** ＝ floor の 7 版の方が Kaggle 上で
  安い。特に t240(66594)/t384(74751) で mirzayasir が高コストだったため大幅損。
- **確定**: 「local未推定（score_error）」タスクは floor が既に最適。ローカルで測れないだけで
  Kaggle では解けており安い。**触ってはならない**。

## 確定した運用則（case9 を強化）
1. ローカル score_error / crash は **ローカル ORT 固有**。Kaggle scorer は negative-pad や
   MaxUnpool/ConvTranspose を正常処理する。これらを根拠に floor を置換しない。
2. **ローカルで測定不能なタスクこそ floor が最安**である場合が多い（公開グレード済み最適 net が
   ローカル ORT の厳格な shape-inference に引っかかるだけ）。swap は損になる。
3. 加点・golf の判断に **ローカル監査の crash/error を使わない**。実 LB のみが真。

## 次の一手
- ローカル偽陽性（crash/error/INCORRECT）由来の swap 路線は case9/10 で完全棄却。floor 7180.58 維持。
- 残るレバー: 公開バンドル監視 harvest（measurable タスクで floor 未満が出た時のみ, 実証済み）+
  bit-pack novel solver（研究）。7950 まで −769.4。
