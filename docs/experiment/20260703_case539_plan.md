# 20260703 case539 plan - solver-bank replacement refresh

Accepted Public Score:

- `7187.41`

## Background

Cases535-538 depleted broad mechanical/dtype/alias searches and several
low-fail semantic repairs. The remaining exact micro queue is only about
`0.001982`, below submit threshold.

Instead of repairing old scratch candidates, this cycle checks whether existing
solver-bank builders can regenerate any non-risk task cheaper than the current
accepted ONNX.

## Method

1. Inspect `backend/pipeline/case3` solver/build output paths and manifests.
2. Generate or collect case3 solver candidates for current tasks.
3. Compare each candidate against `data/output/onnx/taskNNN.onnx` with
   `submit.gate.evaluate_candidate_gate`.
4. Exclude hard/review/runtime-risk tasks and known blocked repair families.

## Submit rule

Submit only if:

- candidate has `n_fail=0`;
- cost is lower than current baseline;
- no forbidden ops/functions;
- standalone gain `>=0.020`, or a low-risk micro bundle reaches `0.015`.

If no candidate qualifies, record no-submit and keep the exact micro queue
banked.
