# 20260702 case475 plan - second reviewed micro cadence bundle

## Context

Case474 adopted the first explicit micro-lane bundle and moved Public Score:

```text
7186.71 -> 7186.73
```

All four band subagents then reported no `gain >= 0.020` submit-grade candidate.
The best usable new current-baseline exact candidate is `task110`:

```text
task110 13591 -> 13475 gain=0.008571692
```

To keep submit cadence moving without weakening correctness, build another
five-task reviewed micro bundle that excludes hard-risk, review-risk, and
runtime-risk families.

## Target Bundle

```text
task110  /private/tmp/_tmp_neurogolf_task110_alias_idx2d_c_TO_idx_selected_r.onnx
task064  /tmp/neurogolf_case475_task064_hv_high_p1_bypass.onnx
task378  /tmp/neurogolf_case385_task378_all_tail.onnx
task044  /private/tmp/neurogolf_fresh_014_140_case382/task044_bypass_And_bottom_m2_match_bool_TO_bottom_row_match.onnx
task046  /private/tmp/neurogolf_case422_cleanup/task046_remove_unused_offset_m2.onnx
```

Expected aggregate gain:

```text
0.008571692 + 0.003854188 + 0.001610739 + 0.000873134 + 0.000437541
= 0.015347294
```

## Gate

Submit only if the current-baseline bundle gate returns:

```text
decision=submit-micro-bundle
n_fail=0 for every changed task
functions=0
forbidden_ops=()
total_gain >= 0.015
changed_tasks <= 5
```

If any member is stale, known-risk, local-failing, or pushes total gain below
threshold, do not submit.
