# case215 result: task391 TopK selector trim

## 結果

- 対象: `task391`
- baseline: `cost=159` (`params=24`, `memory=135`, `n_fail=0`)
- submit: なし
- 採否: 不採用

## 検証内容

temp ONNX で以下を個別に差し替え、`src.evaluate.scorer.audit_one` で検証した。

- `k=3`, `pick=(0,1,2)`
- `k=4`, 3 要素 pick 全組み合わせ
- `k=5`, 3 要素 pick 全組み合わせ
- `sel` 固定候補

## 監査結果

| 変更 | cost | n_pass | n_fail | 判定 |
| --- | ---: | ---: | ---: | --- |
| baseline | 159 | 267 | 0 | ok |
| `k=3`, `pick=(0,1,2)` | 133 | 0 | 267 | reject |
| `k=4`, any 3-rank pick | 146 | 0 | 267 | reject |
| `k=5`, `pick=(2,3,4)` | 159 | 267 | 0 | no improvement |
| `k=5`, other 3-rank picks | 159 | 0 | 267 | reject |
| `sel` fixed common triples | 50 | 0-1 | 266-267 | reject |

## 結論

TopK の rank 2,3,4 が全ペア正答に必要だった。`k=3` なら `159 -> 133`、固定 `sel` なら `159 -> 50` と相対削減率は大きいが、足切りを満たさない。実 ONNX は変更せず、Kaggle submit もしない。

次は、rank 固定型ではなく dtype/initializer の表現を変えて同じ関数を保てる候補を探す。
