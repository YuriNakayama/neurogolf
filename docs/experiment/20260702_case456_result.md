# 20260702 case456 result - task138 direction/fill candidate re-gate

## Summary

No submit.

`task138` had many positive cost-reduction aliases, but none survived the
correctness gate. This is another no-submit cycle caused by `n_fail > 0`, not
by submission tooling or Kaggle queue delay.

## Baseline

```text
task=task138
params=222
memory=13543
cost=13765
points=15.4701155824
n_fail=0
status=ok
```

## Gate Result

Command output:

```text
base_cost=13765 candidates=418
positive_cost_reductions=203 cost_errors=0
exact_positive=0 blocked=203 exceptions=0
decision_counts Counter({'blocked-local-fail': 203})
```

Top reductions:

```text
13765 -> 8493   gain=0.482886844  n_fail=260
  task138_55_Max_filled_TO_boxcg.onnx

13765 -> 8999   gain=0.425015679  n_fail=260
  task138_bypass_d2_55_Max_0.onnx
  task138_term_55_Max_0.onnx

13765 -> 11723  gain=0.160576415  n_fail=136
  task138_54_Where_fill_back_TO_vsel.onnx

13765 -> 11740  gain=0.159127324  n_fail=126
  task138_54_Where_fill_back_TO_hsel.onnx

13765 -> 12229  gain=0.118335     n_fail=136
  task138_bypass_d3_54_Where_2.onnx

13765 -> 12246  gain=0.116946     n_fail=126
  task138_bypass_d3_54_Where_1.onnx

13765 -> 12687  gain=0.08155      n_fail=266
  task138_32_Where_box_int_TO_boxcg.onnx

13765 -> 12753  gain=0.07636      n_fail=60..77
  hsel/vsel -> h_fwd/h_rev/v_fwd/v_rev aliases
```

Several very small aliases also failed, including:

```text
Where_botrow_i_TO_botrow  gain=0.00167   n_fail=65
Or_g_ud_TO_g_down         gain=0.000508  n_fail=61
```

## Decision

Rejected. No candidate met the submit gate:

```text
n_fail=0
gain >= 0.020
single-task isolated change
no known gate-risk reason
```

Therefore no `submission.zip` was produced for this cycle.

## Why No Submit Has Happened Recently

Recent cycles have found cost improvements, but they were not functionally
correct:

- `task044`: exact candidates existed only around zero or negative gain.
- `task102`: best candidate `2527 -> 2342`, gain `0.0760`, `n_fail=10`.
- `task190`: best ray-branch cut `2527 -> 2091`, gain `0.189`, `n_fail>=236`.
- `task062`: best candidate `2536 -> 1683`, gain `0.410`, `n_fail=267`.
- `task230`: best kernel crop `900 -> 100`, gain `2.197`, `n_fail=266`.
- `task138`: best alias `13765 -> 8493`, gain `0.483`, `n_fail=260`.

The immediate cause is the tightened anti-regression policy after recent
non-improving or regressing turns: do not submit merely to satisfy a cadence
when the isolated changed task fails local correctness. The requested
15-minute submit cadence is being treated as a candidate review cadence, not a
forced-submit cadence.

## Conclusion

`task138` remains a structural target, but the existing alias pool is not
usable. Future work should either build a semantics-preserving rewrite from
the original graph or move to another mid-cost target with a fresher candidate
surface.

Next target: inspect `task349` or `task364`, which the subagent ranked after
`task138`, while keeping the same no-forced-submit rule.
