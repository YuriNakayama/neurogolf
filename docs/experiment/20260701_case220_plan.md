# case220 plan: task262 column reorder trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task262` baseline は `cost=106` (`params=16`, `memory=90`, `n_fail=0`)。
- graph は `Slice -> Cast -> Split -> Concat(col0,col2,col1) -> Concat(repeat 3) -> Pad`。

## 対象

- `data/output/onnx/task262.onnx`

## 仮説

3x3 gray patch の列順を `0,2,1` に並べ替え、同じ 3x3 を 3 channel に複製している。もし列順や repeat channel に余剰があれば、`Split/Concat` または後段 repeat を削れて `106` から相対的に大きく下がる。

## 検証

- temp ONNX で列順 permutations を probe する。
- `local_output -> color_cols` や 1 channel/2 channel repeat の Pad 出力が成立するか確認する。
- `gray_u8` cast 省略や `gray_patch` 直接 Split も型制約と cost を確認する。
- `src.evaluate.scorer.audit_one` で `task262` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
