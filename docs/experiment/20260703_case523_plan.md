# 20260703 case523 plan - fresh standalone split search

## Context

Current accepted Kaggle score:

```text
Public Score: 7187.37
latest accepted ref=54271014
```

Recent blocked repair targets:

```text
task243: exact repair loses savings
task033: near miss cannot be repaired within gain threshold
task011: selector collapse, local repair not viable
```

## Hypothesis

The last repair-focused cycles did not produce submit candidates. The next
cycle should return to fresh standalone search, but keep the submit discipline:
only isolated `n_fail=0`, `gain >= 0.020` candidates should be submitted.

## Targeting

Avoid known risk tasks and recently blocked repair targets. Split search across
non-risk tasks with medium cost or prior weak signals:

```text
A: 001-100 excluding known risk and task011/task033
B: 121-220 excluding known risk
C: 221-320 excluding task238/task243 and known risk
D: 321-400 excluding known risk and task354
```

Search methods:

```text
same-shape alias/prune
single initializer cleanup
small constant/output alias probes
current-baseline re-gate of targeted scratch candidates
```

## Submit Policy

Submit immediately only if:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
forbidden ops absent
functions absent
```

Bank exact micro candidates in result logs only. Do not bundle them blindly.
