# 20260703 case553 plan - non-risk high-cost replacement scan

Accepted Public Score:

- `7187.51`

## Background

Case552 found no viable `task025` candidate. High-gain edits broke local
correctness, and exact repairs had no retained gain.

This cycle moves to a broader non-risk high-cost scan, still one submit
candidate at a time.

## Targets

Initial non-risk candidates:

```text
task118, task187, task349, task319, task216, task174, task138, task198
```

These have meaningful cost or visually simple output rules, and are not in the
current hard/review/runtime risk sets.

## Method

1. Mine existing `/private/tmp` artifacts for current-baseline exact wins.
2. Re-gate promising candidates individually.
3. In parallel, inspect `task174`, `task216`, and `task319` for a direct solver
   or bounded structural simplification.
4. Submit only one full 400-task bundle if a candidate has `n_fail=0`, no
   forbidden ops/functions, and gain `>=0.020`.

## Submit Rule

Do not submit micros unless a current-baseline max-5 bundle totals at least
`0.015`. Every real submit must be full 400-task packaging.
