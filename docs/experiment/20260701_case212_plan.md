# case212 plan: task389 channel map index trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task389` は未着手で、baseline は `cost=140` (`params=12`, `memory=128`, `n_fail=0`)。
- first/last color index から 2 要素 scatter で channel map を作る短い graph。

## 対象

- `data/output/onnx/task389.onnx`
- `present_f` から `first_i64` / `last_i64` を取り、int32 cast 後に `color_vec = first + last - 5` を作る。
- `ScatterElements(base_map, [0, color_vec], [color_vec, 5])` で channel map を作り、`Gather(input, channel_map)` で出力する。

## 仮説

実データ上で `first_i64` と `last_i64` が同値、または `color_vec` が固定/片側 index で代替できれば、cast/add/sub/concat の一部を削れる。小型 graph なので cost 減が小さくても比率はある。

## 検証

- temp ONNX で `last_i32 -> first_i32`、`color_vec -> first_i32/last_i32/fixed`、`scatter_indices/updates` 固定化を probe する。
- `src.evaluate.scorer.audit_one` で `task389` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
