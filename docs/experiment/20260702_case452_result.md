# 20260702 case452 result - task102 fill-core candidate gate

## Outcome

No submit.

Current baseline:

```text
task102.onnx
params=113
memory=2414
cost=2527
points=17.1652118926
status=ok
```

## Candidate Scan

Collected 82 existing `task102*.onnx` scratch candidates from `/private/tmp`
and `/tmp`.

Cost-only screening found 30 positive reductions. Every positive reduction
failed local correctness when re-gated against the current accepted baseline:

```text
positive_cost_reductions=30
exact_positive=0
exceptions=0
```

The largest reductions were all `fill_core` branch shortcuts:

```text
/private/tmp/neurogolf_nextcycle_small_alias/task102_010.onnx
  2527 -> 1856
  gain=0.3086091940
  n_fail=127

/private/tmp/neurogolf_nextcycle_small_alias/task102_001.onnx
  2527 -> 1932
  gain=0.2684770926
  n_fail=146

/private/tmp/neurogolf_nextcycle_small_alias/task102_003.onnx
  2527 -> 1936
  gain=0.2664088396
  n_fail=128

/private/tmp/neurogolf_nextcycle_small_alias/task102_006.onnx
  2527 -> 1944
  gain=0.2622851224
  n_fail=81
```

The closest repair-looking candidates were:

```text
/private/tmp/neurogolf_nextcycle_small_alias/task102_002.onnx
/private/tmp/neurogolf_nextcycle_small_alias/task102_004.onnx
/private/tmp/neurogolf_nextcycle_small_alias/task102_007.onnx
```

All three remove the `frame_W4` branch and replace `fill_core4` with an earlier
fill tensor:

```text
base:
QLinearConv(frame_W4) -> cand_u8_4
MaxPool(cand_u8_4) -> fill_core4
Max(cand_u8_1, fill_core2, fill_core3, fill_core4) -> fill_core

candidate:
Max(cand_u8_1, fill_core2, fill_core3, <earlier tensor>) -> fill_core
```

They have attractive cost and a smaller failure count:

```text
2527 -> 2342
gain=0.0760275632
n_fail=10
```

## Repair Inspection

The 10 failing examples all have the same failure pattern:

- exactly 16 output cells are missing from color channel 2;
- the same 16 cells remain background in channel 0;
- the missing region is a `4x4` fill block.

The removed `frame_W4` initializer is a `6x6` frame detector:

```text
frame_W4 int8 [1,1,6,6]
[[  1   1   1   1   1   1]
 [  1 -64 -64 -64 -64   1]
 [  1 -64 -64 -64 -64   1]
 [  1 -64 -64 -64 -64   1]
 [  1 -64 -64 -64 -64   1]
 [  1   1   1   1   1   1]]
```

Its output path is:

```text
cand_u8_4  uint8 [1,1,7,7]
fill_core4 uint8 [1,1,10,10]
```

This is not a small hidden exception that can be repaired cheaply. The deleted
branch is the semantic detector for 6x6 frames / 4x4 interiors, and adding an
equivalent repair gives back the removed branch.

## Decision

Reject for submission. `task102` has no current-baseline exact cost-reducing
candidate in the inspected pool. The best near miss is structurally required,
not an isolated local exception.

## Next

Move to `task190` or `task062` only with the same bounded re-gate discipline.
Do not submit any `task102` fill-core branch shortcut unless a new mechanism
preserves the 6x6 frame / 4x4 fill behavior below baseline cost.
