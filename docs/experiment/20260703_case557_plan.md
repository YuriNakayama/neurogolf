# 20260703 case557 plan - task349 halo repair

Accepted Public Score:

- `7187.54`

## Hypothesis

Case556 repaired the high-gain `task349` halo simplification. The earlier
`c4_to_c3` collapse failed on 17 `arc-gen` cases because `halo_c4` was the only
missing source of color-3 halo. Restoring that branch while shrinking gathers
preserves correctness and retains meaningful cost reduction.

## Candidate

- Path: `/private/tmp/neurogolf_agent_case556_B/candidate/task349.onnx`
- Baseline cost: `24369`
- Candidate cost: `23612`
- Relative gain: `0.031557`
- Correctness: `n_fail=0`
- Safety: `functions=0`, forbidden ops `[]`
- Ops: `Cast`, `Equal`, `Gather`, `Max`, `MaxPool`, `QLinearConv`,
  `ReduceMax`, `Where`

## Method

1. Re-gate the candidate against the current accepted baseline.
2. Build a full 400-task bundle from `data/output/onnx`.
3. Replace only `task349.onnx`.
4. Submit if pre-submit gate returns `submit-bundle`.
5. Adopt only if Kaggle Public Score improves.
