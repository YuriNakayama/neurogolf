# 20260701 case283 result - adopt task219 Min/Max alias bundle

## Result

- Target: `task219`
- Baseline: `params=92`, `memory=18325`, `cost=18417`, `n_fail=0`
- Candidate: `params=91`, `memory=17149`, `cost=17240`, `n_fail=0`
- Relative reduction: `18417 / 17240 = 1.0683`
- Expected point gain: `ln(18417 / 17240) ~= 0.0660`
- Submit ref: `54223271`
- Public Score: `7181.94 -> 7182.00`
- Adoption: adopted

## Change

Applied five exact aliases:

```text
min_44  -> gat_41
min_170 -> gat_41
max_231 -> gat_227
min_245 -> red_236
min_246 -> red_236
```

Dead-node pruning removed redundant `Min` / `Max` producers and their now-unused
dependencies. The largest cost reduction came from the `max_231 -> gat_227`
alias; the remaining aliases removed smaller duplicate clamp/reduce paths.

## Verification

Individual local audit for the submitted `task219.onnx`:

```text
params=91
memory=17149
cost=17240
n_pass=265
n_fail=0
status=ok
```

## Conclusion

Adopted. The real LB moved by `+0.06`, consistent with the local expected gain.
This confirms that same-task alias bundles on mid/high-cost graphs remain the
best current ROI when the aliases remove whole intermediate tensors.

## Next Step

The remaining parallel-scan candidates are weaker:

- `task173`: `17646 -> 17371`, ratio `1.0158`
- `task064`: `12478 -> 12430`, ratio `1.0039`
- `task286`: `47013 -> 47012`, ratio `1.00002`

Prioritize `task173` next unless a fresh scan finds a stronger exact bundle.
