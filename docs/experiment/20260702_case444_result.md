# 20260702 case444 result - task242 reflected patch crop is no-submit

## Decision

- Task: `task242.onnx`
- Submit: no
- Adoption: none
- Reason: no current-baseline exact candidate reduces cost.

Current baseline:

```text
params=60
memory=372
cost=432
status=ok
```

## Graph Evidence

The graph computes a reflected 9-channel `3x3` patch and pads it to the output:

```text
Einsum(input, channel0, coord_weights) -> row_f
Einsum(input, channel0, coord_weights) -> col_f
Sub(fifteen_margin_f32, col_f) -> src_col_f
Concat(one_f32, row_f, src_col_f) -> patch_starts_f
Cast(patch_starts_f) -> patch_starts
Add(patch_starts, slice_delta) -> patch_ends
Slice(input, patch_starts, patch_ends, slice_axes, patch_steps) -> patch_src_f
Pad(patch_src_f, pads, zero_f32) -> output
```

The main tensor is:

```text
patch_src_f FLOAT [1,9,3,3] 324 bytes
```

As with `task039`, this tensor is the direct output value source, so bypassing it
is a semantic crop change.

## Candidate Re-Gate

Re-gated 21 existing candidates:

```text
blocked-local-fail: 11
blocked-no-gain:   10
submit-candidate:   0
```

Best apparent reductions:

```text
task242_002_Add_patch_ends_TO_patch_starts.onnx
  cost 432 -> 417
  gain 0.035339
  n_fail 266
  decision blocked-local-fail

task242_003_Add_patch_ends_TO_slice_delta.onnx
  cost 432 -> 420
  gain 0.028171
  n_fail 266
  decision blocked-local-fail

task242_000_Sub_src_col_f_TO_fifteen_margin_f32.onnx
  cost 432 -> 424
  gain 0.018692
  n_fail 224
  decision blocked-local-fail
```

Exact candidates:

```text
cleanup / broadcast_compress / identity / index_surgery:
  cost 432 -> 432
  n_fail 0
  decision blocked-no-gain
```

The `coord_weights` dtype candidate is invalid because `Einsum` receives mixed
float and float16 inputs.

## Conclusion

No submit. `task242` is another high-ratio-looking small task where the dominant
tensor is required output content. Existing start/end aliases are not exact.

Move on to a non-crop, non-ScatterND candidate class.
