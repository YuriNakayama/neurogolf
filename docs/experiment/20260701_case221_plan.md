# case221 plan: task393 TopK rank trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task393` baseline は `cost=144` (`params=19`, `memory=125`, `n_fail=0`)。
- graph は `GlobalAveragePool -> TopK(k=4) -> Cast -> Slice(rank 1..3) -> Transpose -> Equal -> Pad`。

## 対象

- `data/output/onnx/task393.onnx`

## 仮説

TopK の上位4色から rank 1..3 を選んでいる。もし rank 0 を含めても正答、または候補数を 3 に縮めても正答なら、TopK 出力や Slice を削れて `144` から相対的に大きく下げられる。固定色 mask が成立する場合はさらに削れる。

## 検証

- temp ONNX で `k=3/4/5` と Slice start/end variants を probe する。
- `s` または `st` の固定色候補を probe する。
- `src.evaluate.scorer.audit_one` で `task393` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
