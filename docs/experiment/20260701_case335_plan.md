# 20260701 case335 plan - task209 uniform-branch MaxPool alias pair

## Context

- Current accepted Public Score floor: `7184.98` after case334.
- Priority rule: `n_fail=0` first, then relative reduction
  `old_cost / new_cost`.
- The best remaining exact candidate from the case334 parallel scan is a task209
  alias pair on uniform-branch MaxPool outputs.

## Target

- `data/output/onnx/task209.onnx`

## Baseline

Current local audit:

```text
params=1379
memory=28104
cost=29483
n_fail=0
status=ok
```

## Hypothesis

The current graph materializes `max_s3` and `max_s4`, but for the audited
examples both tensors are equivalent to `label_f` at their consumers. Rewiring
both outputs to `label_f` should preserve values while pruning the redundant
MaxPool branches.

Exact pair:

```text
max_s3 -> label_f
max_s4 -> label_f
```

Nearby larger aliases around `strict_TL`, `valid_TL`, vote, and key-stamp
signals failed local correctness and are intentionally excluded.

## Expected Gain

Temporary audit:

```text
params=1379
memory=27304
cost=28683
n_fail=0
status=ok
```

Expected point gain:

```text
29483 / 28683 = 1.0279
ln(old/new) ~= 0.0275 pt
```

## Verification Plan

1. Apply only the `task209` MaxPool alias pair.
2. Run individual `evaluate.audit_one` for `task209`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
