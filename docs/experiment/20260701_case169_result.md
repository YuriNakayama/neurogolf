# 20260701 case169 result

## Target

- Task: `task329`
- Baseline cost: 1059
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task329`.

No exact cost-reducing candidate was found.

Large but incorrect reductions:

- `keep_input_b -> row_out_b`: cost 96, `n_fail=266`
- `keep_input_b -> col_keep_b`: cost 128, `n_fail=266`
- `keep_input_b -> out_by_index_b`: cost 65, `n_fail=266`
- `row_out_b -> out_by_index_b`: cost 158, `n_fail=266`
- `col_keep_b -> row_out_b`: cost 126, `n_fail=266`
- `col_keep_b -> out_by_index_b`: cost 996, `n_fail=260`

Center detector simplifications:

- `center5_b -> out9_b`: cost 1053, `n_fail=260`
- `center_idx_i64 -> center_i64`: cost 1050, `n_fail=266`
- `center5_b -> true_b`: invalid `ArgMax` rank
- `center5_u8 -> center5_b`: invalid `ArgMax` bool input
- `center_i64 -> center5_u8`: invalid `ScatterElements` index type

Shape/type failures:

- `out_by_index_b -> out9_b`: invalid final `Where` dimensions
- `row_out_b -> out9_b`: invalid `Or` dimensions
- graph output set to `input`: unscorable, `n_fail=266`
- graph output set to boolean masks: invalid float output type

## Submission

- Not submitted.
- Reason: every scored cost reduction failed exact-match auditing or produced an
  invalid graph.

## Decision

Rejected. Keep the existing `task329.onnx`.

## Next

The column exception is essential for all public examples. Move away from tasks
where a single scatter/broadcast exception encodes the core rule, and continue
looking for dtype-preserving tensor reductions or exact guard removals.
