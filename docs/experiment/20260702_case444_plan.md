# 20260702 case444 plan - task242 reflected patch crop probe

## Context

Accepted Public Score remains `7186.71`. Fresh current-graph ranking found
`task242` as another small task where one patch tensor dominates the score:

```text
params=60
memory=372
cost=432
status=ok
```

Current graph:

```text
Einsum(input, channel0, coord_weights) -> row_f
Einsum(input, channel0, coord_weights) -> col_f
Sub(fifteen_margin_f32, col_f) -> src_col_f
Concat(one_f32, row_f, src_col_f) -> patch_starts_f
Cast(patch_starts_f) -> patch_starts
Add(patch_starts, slice_delta) -> patch_ends
Slice(input, patch_starts, patch_ends, slice_axes, patch_steps) -> patch_src_f
Pad(patch_src_f, pads, zero_f32) -> output
```

The dominant tensor is:

```text
patch_src_f FLOAT [1,9,3,3] 324 bytes
```

## Hypothesis

Like `task039`, this is likely a near-minimal dynamic crop. However, `task242`
has several old dtype/index and start/end alias candidates. Because cost is only
`432`, even small exact reductions would be meaningful by relative score.

## Method

1. Re-gate existing `task242` scratch candidates against the current baseline.
2. Prioritize exact reductions in the index/start/end path.
3. Treat direct `Slice` value bypasses as semantic unless locally exact.
4. Submit only if a current-baseline candidate is exact and clears the
   post-regression gate.
