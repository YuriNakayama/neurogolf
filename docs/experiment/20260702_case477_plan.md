# 20260702 case477 plan - task205 current extension search

## Context

Current accepted Public Score is `7186.73` after case474. Case475 regressed and
was rejected; case476 quarantined its five members.

All broad band subagents found no isolated `gain >= 0.020` submit-grade
candidate. The next cycle should return to isolated structural candidates
instead of weak micro bundles.

`task205` is a mid/high-cost current target:

```text
task205 cost=11550
params=279
memory=11271
```

It is not in the hard/review/runtime risk gate sets, and prior history includes
an adopted task205 patch-core improvement. This makes current-baseline
extension search preferable to another generic bank-low bundle.

## Target

```text
task205
baseline: data/output/onnx/task205.onnx
task json: data/lake/neurogolf-2026/task205.json
```

## Method

1. Gather existing `task205*.onnx` scratch candidates from `/tmp` and
   `/private/tmp`.
2. Cost-rank them against current baseline.
3. Correctness-gate positives with `submit.gate.evaluate_candidate_gate`.
4. Submit only if a candidate is:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. If only low-gain exact candidates appear, bank them and do not submit.
