# 2026-07-01 case137 plan

## 方針

- floor: Public Score 7181.15
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task274.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task274.onnx`
- baseline: params 80, memory 146, cost 226, points 19.579465000727716, n_pass 269, n_fail 0

## 仮説

`task274` は短い graph で、`space_formula` を `Clip(space_formula, one, two)` し、`row5_is_empty` / `row5_is_base` の2段 `Where` で最終 `space` を作る。cost は小さいが、Clip/Where 1つを削れれば相対削減率が大きい。

実データで `space_formula` が既に `[1,2]` に収まる、または empty/base 分岐が固定的なら、`space_row5_zero` や `space_nonbase` を直接差し替えられる可能性がある。

## 手順

1. `/tmp` 上の candidate ONNX で `space_row5_zero -> space_formula`、`space_nonbase -> four/space_row5_zero`、`space -> space_formula/space_nonbase` などを試す。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task274.onnx` に反映する。
3. submit し、Public Score が 7181.15 を上回れば採用、上がらなければ破棄する。
