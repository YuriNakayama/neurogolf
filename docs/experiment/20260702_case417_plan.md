# 20260702 case417 plan - targeted high-cost structural inspection

## Context

Accepted Public Score remains `7186.71`. Cases 415 and 416 correctly skipped
Kaggle submit because no candidate cleared the post-case414 gate.

Broad `/tmp` candidate scans are too noisy and risk reusing hidden-sensitive
local aliases. This cycle uses targeted graph inspection on high-cost,
non-excluded tasks.

## Targets

Primary local target:

```text
task018 current cost = 38681
```

Parallel read-only subagent targets:

```text
task133 current cost = 33562
task187 current cost = 27533
```

These are among the highest current non-excluded costs and have prior adopted
or investigated structural work, but are not in the case414 hidden-unsafe set.

## Hypothesis

The best chance for a submit-worthy candidate is not a broad output alias, but
a mechanically explainable reduction such as:

- unused or duplicate initializer cleanup not yet applied;
- repeated shape/index tensor consolidation;
- a local dense intermediate or dtype path that can be narrowed without changing
  selector coverage;
- a same-task continuation of a previously Kaggle-validated structural pattern.

## Gate

Submit only a single task if all conditions hold:

```text
status=ok
n_fail=0
functions=0
forbidden ops=[]
gain >= 0.020
mechanically explainable
```

If the best candidates are below threshold, write a no-submit result and bank
them. Do not bundle unvalidated micros.
