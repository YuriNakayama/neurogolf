# 20260702 case506 plan - isolated task251 local-cell cleanup

Current accepted Public Score:

7187.19

## Background

Case505 intentionally skipped a mechanically passing micro bundle because it
depended on documented risk families. The next parallel sweep found a
standalone current-baseline `task251` candidate that clears the submit threshold
without forbidden ops or functions.

## Target

`task251` only.

Candidate:

```text
/tmp/neurogolf_parallel_J/task251_task251_local_cell_3_4_submit_candidate.onnx
```

Local gate:

```text
baseline_cost : 4920
candidate_cost: 4818
gain          : 0.020949626
n_fail        : 0
functions     : 0
forbidden_ops : []
decision      : submit-candidate
sha256        : c579ff9f3c6b4e17b26a2cce30fe8b75e9d66f97120887653dd350e8650302d1
```

## Method

1. Preserve the current accepted `task251.onnx` as rollback baseline.
2. Replace only `data/output/onnx/task251.onnx` with the candidate.
3. Run dry-run pre-submit gate against the rollback baseline directory.
4. Submit isolated if the bundle gate passes.
5. Adopt only if Kaggle Public Score improves above `7187.19`; otherwise
   restore the baseline.

## Risk

The graph uses `ScatterND`, but `task251` is not in the current index-runtime
risk list and the isolated gate is exact. Submit as a single-task candidate, not
as part of a bundle.
