# 20260702 case355 plan - task018 and task363 cleanup batch

## Context

Accepted Public Score is `7185.58` after case354. The next submit window is
approximately 30 minutes after the case354 submission.

Case355 searches from the current accepted baseline, not from old scratch
baselines:

```text
task018 current cost = 39583
task363 current cost = 4348
```

## Hypothesis

Two exact local cleanup candidates independently reduce intermediate memory:

| task | candidate | old cost | new cost | ln gain |
|---:|---|---:|---:|---:|
| 018 | `/tmp/neurogolf_case355_struct/task018_sim_cleanup.onnx` | 39583 | 38683 | 0.023000 |
| 363 | `/tmp/neurogolf_case355_struct/t363_dead_cleanup.onnx` | 4348 | 4248 | 0.023268 |

Total expected local gain:

```text
0.046267
```

This clears the low/medium-risk batch threshold of `0.030`.

## Implementation

Replace only:

```text
data/output/onnx/task018.onnx
data/output/onnx/task363.onnx
```

with the two scratch candidates above.

## Gate

Run `src.evaluate.audit_one` only on the changed tasks:

- task018 must have `n_fail=0` and `cost=38683`;
- task363 must have `n_fail=0` and `cost=4248`.

Operator check:

- no `Loop`;
- no `Scan`;
- no `NonZero`;
- no `Unique`;
- no `Script`;
- no `Function`;
- no `Compress`.

## Risk

Medium batch. Both candidates pass full local task-specific audit and do not use
forbidden operators, but task018 is a large graph and local equivalence can still
miss hidden behavior. The batch is kept to two tasks so that a regression can be
bisected with one follow-up submit.
