# 20260702 case372 plan - task222 residual sparse kernel check

## Context

Current accepted Public Score is `7185.86`. Case371 found that task328's packed
template representation is already better than a predecoded table.

This cycle returns to a prior strong lead from case292: crop-at-source rewrites.
Task222 has already been improved by case293, but it still has a large sparse
float32 kernel initializer.

## Target

Target:

```text
data/output/onnx/task222.onnx
```

Current local audit:

```text
params=2919
memory=4676
cost=7595
n_fail=0
```

Graph summary:

```text
Conv(input, cw) -> code32[1,1,14,14] -> Cast(uint8) -> morphology -> output
cw float32[1,10,17,17], nonzero=9
```

## Hypothesis

The previous crop-at-source rewrite may still leave slack if the sparse `cw`
kernel's nonzero taps occupy a smaller bounding box than 17x17. If so, the kernel
can be cropped and the Conv pads/shape adjusted without changing the 14x14 code
image. If the taps span the full 17x17 offset range, the current graph is likely
already at the crop-at-source floor.

## Method

1. Inspect `cw` nonzero tap coordinates.
2. If the tap bbox is smaller than 17x17, build a scratch cropped-kernel Conv and
   audit only task222.
3. Re-run one-file case3 surgery to catch known unused initializer cleanup.
4. Submit only if `n_fail=0` and gain clears the risk gate.

## Submit Gate

```text
low risk:    submit if gain >= 0.020, otherwise bank
medium risk: submit if gain >= 0.030, otherwise bank
```

Kernel crop is medium risk unless it is an exact bbox-preserving transform.
