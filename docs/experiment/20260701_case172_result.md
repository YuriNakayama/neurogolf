# 20260701 case172 result

## Target

- Task: `task256`
- Baseline cost: 2948
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task256`.

No exact cost-reducing candidate was found.

Channel aliases:

- `bg -> indiag`: cost 1757, `n_fail=266`
- `bg -> outdiag`: cost 1926, `n_fail=263`
- `bg -> ingrid`: cost 2610, `n_fail=266`
- `blue -> indiag/outdiag/ingrid`: cost 2766, `n_fail=266`
- `redc -> indiag/outdiag/ingrid`: cost 2766, `n_fail=266`
- `green -> indiag/outdiag/ingrid`: cost 2766, `n_fail=266`

Mask aliases:

- `indiag -> ingrid`: cost 2706, `n_fail=266`
- `outdiag -> indiag`: cost 2779, `n_fail=266`
- `outdiag -> ingrid`: cost 2779, `n_fail=266`
- `outdiag -> false_b`: cost 2780, `n_fail=266`
- `above -> false_b`: cost 2936, `n_fail=266`
- `below -> false_b`: cost 2936, `n_fail=266`
- `same -> false_b`: cost 2936, `n_fail=266`

Geometry aliases:

- `dlim2 -> L`: cost 2944, `n_fail=191`
- `dlim2 -> Rrel`: cost 2940, `n_fail=266`
- `Rrel -> L`: cost 2908, `n_fail=239`
- `L -> Rrel`: cost 2944, `n_fail=239`

Shape/type failures:

- scalar `false_b` inserted directly into `Concat`: invalid rank
- `bg/blue/redc/green` replaced by `above/below/same`: invalid `Concat` shape
- `indiag -> above/below/same`: invalid `Concat` shape
- `ingrid -> black`: invalid `And` float input
- `bound -> Rrel/dlim2`: invalid downstream shape

## Submission

- Not submitted.
- Reason: all scored reductions failed exact-match auditing or produced invalid ONNX.

## Decision

Rejected. Keep the existing `task256.onnx`.

## Next

The four channel masks encode the full rule and are not redundant. Prefer future
targets with low-fail first probes; all meaningful `task256` reductions failed on
nearly every example.
