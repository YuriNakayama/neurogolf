# 20260702 case495 result - no-submit task005 bounded sweep

## Decision

No submit.

Accepted Public Score remains:

```text
7187.10
```

## Baseline

```text
task005.onnx
cost=6882
params=490
memory=6392
status=ok
```

## Scan

Existing unique `task005*.onnx` scratch candidates from `/tmp` and
`/private/tmp`:

```text
candidates=3178
positive cost-only candidates=2134
```

Main-thread bounded gate:

```text
gated=138
submit-candidate=0
bank-low-gain=0
blocked-local-fail=138
```

Representative submit-threshold failures:

```text
6882 -> 6517  gain=0.054495  n_fail=266  blocked-local-fail
6882 -> 6525  gain=0.053268  n_fail=266  blocked-local-fail
6882 -> 6678  gain=0.030091  n_fail=254-266 blocked-local-fail
6882 -> 6686  gain=0.028894  n_fail=239-260 blocked-local-fail
6882 -> 6687  gain=0.028744  n_fail=172-231 blocked-local-fail
6882 -> 6727  gain=0.022780  n_fail=128-266 blocked-local-fail
```

## Repair Triage

Parallel repair triage audited the close families:

```text
best low-fail:
6882 -> 6879
gain=0.000436
n_fail=1

best meaningful near miss:
6882 -> 6780
gain=0.014932
n_fail=7
```

The `n_fail=1` family removes a single small `Max`; restoring correctness would
restore the same operation and erase the whole three-cost-unit gain. The
meaningful `marker_raw_* -> hit_slice_*` family fails on real cases where the
removed alternate slice is required, so a correctness-preserving repair would
reintroduce the selection logic.

## Conclusion

Reject locally. `task005` has many cost-positive aliases, but the useful ratio
families break required template/marker/ray logic. The remaining low-fail
candidates are too small and not repairable with positive gain.
