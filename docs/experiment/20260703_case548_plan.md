# 20260703 case548 plan - repair task364 near miss

Accepted Public Score:

- `7187.48`

## Background

Case547 adopted the repaired `task076` slot candidate and moved Public Score
from `7187.46` to `7187.48`.

Recent no-submit cycles were mostly caused by candidate supply: exact micro
candidates were far below the submit threshold, while larger local reductions
usually had correctness failures or known hidden-LB risk. To keep submit cadence
high without repeating packaging/regression mistakes, this cycle keeps Kaggle
submission management in the main process and runs parallel workers for
candidate supply.

## Target

Primary repair target:

```text
/private/tmp/neurogolf_agent_case543_C/fresh_alias_dce/task364_runtime_equiv_alias_dce.onnx
```

Fresh gate against current accepted baseline:

```text
task364 23013 -> 22133
gain    0.038990
n_fail  21
status  INCORRECT
functions 0
forbidden ops []
```

`task364` is not in the current hard/review/runtime risk sets. If the repair
keeps at least about half of the gain, it remains above the standalone submit
threshold.

## Method

1. Compare baseline and near-miss outputs on the 21 failures.
2. Identify whether the alias removed a guard, dtype width, or shape-dependent
   intermediate.
3. Build bounded repairs that restore only the failing condition instead of
   reverting the whole alias.
4. Gate only `task364`.
5. Submit as a full 400-task bundle only if the candidate has `n_fail=0`,
   no forbidden ops/functions, and local gain `>=0.020`.

## Parallel Work

While `task364` is being repaired, parallel workers search for:

- exact low-risk micro candidates to bank for later bundles;
- other high-gain near-miss repairs;
- mid-cost structural simplifications with large relative reduction.

## Submit Cadence Rule

Every 10-15 minutes, submit only if one of these is true:

- a non-risk standalone candidate has local gain `>=0.020`;
- a current-baseline, non-risk micro bundle of at most 5 tasks has total gain
  `>=0.015`.

Every real submit must be a full 400-task directory.
