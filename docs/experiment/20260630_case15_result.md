# 20260630 case15 result — t171 frame net は正答も cost 41039>>910、REJECT（提出なし）

## 結論（提出なし、floor 7180.62 維持）
- t171（910, 3×3-local table=9）規則を完全特定: **all-0 入力のグリッド外周に 8 を描く frame**
  （内部=0）。erode/shift/min による frame net = **faithful n_fail=0/54 だが cost 41039**。
- floor t171 は **単一 Conv(W=10×10×3×3, params=900, cost=910)**。memory metric は単一ノードの
  出力を安く数えるが、私の 12-node 版は中間 [1,1,30,30] テンソル ~10 個が各 3600B 加算され爆発。

## 確定した核心的教訓（本日の総まとめ）
- **memory metric は単一 Conv/Gather net を、明示的 multi-op 構成より原理的に安くする**。floor は
  全タスクで単一 Conv/Gather/bit-pack の最小表現を使用済み。手動の明示構成（shift/min/flood/frame）は
  正答しても中間テンソルが加算され必ず高コストになる。**これが floor を hand-golf で破れない根本理由**。
- case5(flood-fill)/case15(frame) で同じ構造的限界を別タスクで実証: cost ∝ 中間ノード数。
- floor の単一 Conv（t032 gravity, t171 frame 等）は学習済み重みで複雑規則を 1 ノードに圧縮しており、
  明示アルゴリズムより遥かに安い。**novel hand-construction は memory metric 上 floor に勝てない**。

## 次の一手
- hand-construction novel solver は memory metric により構造的に棄却（最終確定）。
- 唯一の前進レバー = 公開バンドル監視 harvest（実証済み, 本日 +0.07）。新 revision 毎 cherry-pick。
- floor 7180.62 維持。7950 まで −769.38。
