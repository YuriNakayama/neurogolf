# 20260701 case329 result - task003 packed uint8 tail

## Decision

Adopted.

## Local Gate

Only `task003.onnx` was changed and audited.

```text
baseline:  params=21 memory=281 cost=302 n_fail=0 status=ok
candidate: params=36 memory=227 cost=263 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(302 / 263) ~= 0.1383 pt
```

## Submission

```text
ref=54231810
description=case329 task003 packed uint8 tail cost 302 to 263
status=COMPLETE
publicScore=7184.33
```

Previous accepted floor:

```text
7184.19
```

The Public Score improved by `+0.14`, so the candidate is accepted.

## Diff Summary

The accepted rewrite removes the explicit zero channel from the terminal
3-channel uint8 tensor. It keeps the two live channels, then uses a one-hot
`QLinearConv` with final pads to restore output channels 0 and 2 in the
10-channel output.

## Next Step

Revisit `task334`: a new probe reportedly found a materially different exact
variant at `197 -> 131`, despite the earlier logged packed-tail attempt
worsening to `230`. Validate that candidate directly before making it the next
cycle.
