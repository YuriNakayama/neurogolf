# 20260702 case363 result - skip submit and bank tiny surgery

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.78
```

## Findings

Parallel exploration did not find a clean low/medium-risk current-baseline
candidate with `gain >= 0.030`.

The only exact above-threshold single-task candidate found was the known
`task233` score-table compression:

```text
task233: 49454 -> 47804
gain ~= 0.03393
n_fail=0
scratch: /private/tmp/neurogolf_case347_alt/task233_scorew_u8_top255_unique_tail1.onnx
```

This family already produced Kaggle `SubmissionStatus.ERROR` in case347, likely
from platform/runtime compatibility around a `uint8` `TopK` value path. Do not
submit it again unless the compatibility issue is fixed.

## Surgery Re-run

Ran the deterministic case3 surgery pipeline on the current accepted bundle:

```text
uv --project backend run python -m pipeline.case3 surgery \
  --base ../data/output/onnx \
  --task-dir ../data/lake/neurogolf-2026 \
  --out /tmp/neurogolf_case363_surgery
```

It found one exact local improvement:

| task | old cost | new cost | ln gain | n_fail | status |
|---:|---:|---:|---:|---:|---|
| 285 | 24535 | 24512 | 0.000937876 | 0 | ok |

This is far below the submit floor, so it is banked only.

## Rejected / Deferred

- `task023 Sacc4 -> Sacc3` on the current graph: `11212 -> 9844`,
  `gain ~= 0.130`, but `n_fail=2`.
- `task023` single-consumer variants for `Lmask` or `Sg`: still `n_fail=2`
  with no current cost gain.
- `task233` uint8 `TopK` compression: exact locally but platform-risk due prior
  Kaggle ERROR.

## Next Step

Continue searching for a fresh structural candidate with current-baseline
`gain >= 0.030`. If none appears, only spend a submit when accumulated exact
low/medium-risk micros again clear the batch threshold.
