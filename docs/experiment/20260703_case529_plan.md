# 20260703 case529 plan - isolate task110 review-risk candidate

## Current State

Latest accepted score:

```text
Public Score: 7187.39
latest accepted ref: 54273283
case: case528 expanded low-risk micro bundle
```

The clean low-risk micro bank was spent in case528. Parallel range scans found
no standalone low-risk candidate with `gain >= 0.020`, and task074/task148/task196
repair triage indicates their high-gain near misses restore the deleted
intermediate tensor when made exact.

## Hypothesis

Case475 regressed badly with a five-task review-risk micro bundle:

```text
task044, task046, task064, task110, task378
Public Score: 7186.73 -> 7171.09
```

The member-level cause is still unknown because those tasks were submitted
together. The largest current exact member is task110:

```text
candidate: /private/tmp/_tmp_neurogolf_task110_alias_idx2d_c_TO_idx_selected_r.onnx
cost: 13591 -> 13475
gain: 0.008571692
n_fail: 0
decision: review-known-risk
```

Submitting task110 in isolation gives useful information for future review-risk
handling and may recover a small score increase if task110 was not the regressing
member.

## Method

1. Build a candidate ONNX directory with only `task110.onnx` changed.
2. Run the pre-submit gate with `--allow-review-gate`.
3. Submit only this isolated candidate; do not mix with other review-risk or
   micro candidates.
4. Adopt only if Kaggle Public Score improves above `7187.39`; otherwise reject
   and keep the current accepted ONNX.

## Risk Control

This is deliberately an isolated submit. A Public regression will identify
task110 as unsafe and prevent it from being reused in future bundles. It will
not be adopted unless the real Public Score improves.
