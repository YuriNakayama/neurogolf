# 20260701 case254 result — task157 opset13 bool Pad化は cost 悪化で不採用

## 結果

- 対象: `task157`
- baseline: `params=351`, `memory=7983`, `cost=8334`, `n_fail=0`
- submit: なし
- 採否: 不採用

## probe

opset 11 では bool `Pad` が ONNX Runtime で無効なため、opset 13 へ上げて以下を互換変換した。

- `Squeeze` / `Unsqueeze`: `axes` attribute を int64 initializer input 化
- `Split`: `split` attribute を int64 initializer input 化
- `ReduceSum`: `axes` attribute を int64 initializer input 化

### 単独 `ch1_active10` Pad

`Concat(ch1_active_pre, zeros_ch1_rows)` を `Pad(ch1_active_pre, [0,0,4,0], false)` に置換。

```text
params: 351 -> 438
memory: 7983 -> 7983
cost: 8334 -> 8421
n_fail: 0
```

### 2-input zero Concat の一括 Pad

同じ opset13 overhead を使い、2-input の bool zero `Concat` をまとめて `Pad` 化。

```text
params: 351 -> 398
memory: 7983 -> 7983
cost: 8334 -> 8381
n_fail: 0
```

正答性は保てたが、opset13 互換変換で追加される axes/split initializer が zero initializer 削減を上回った。

## 結論

`task157` の bool `Concat`→`Pad` は opset13 変換込みでは cost 悪化。実 ONNX は変更せず、Kaggle submit もしない。

## 次の一手

opset 13 への引き上げが必要な bool Pad 化は、同一 graph 内でかなり大きい zero initializer を削れない限り不利。次は既に opset 13/14 以上の graph、または opset 変換なしで dtype/final 合成を削れる task を優先する。
