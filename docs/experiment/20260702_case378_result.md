# 20260702 case378 result - task310 argmin and slice axes

## Decision

Adopted.

```text
ref: 54246480
description: case378 task310 argmin slice axes gain 0.02455
previous accepted Public Score: 7185.86
Public Score: 7185.88
```

The real Public Score increased by `+0.02`, so keep the submitted
`task310.onnx`.

## Local Gate

Baseline:

```text
params 45
memory 3006
cost 3051
points 16.97677531528333
n_pass 266
n_fail 0
filesize 1718
```

Submitted candidate:

```text
params 43
memory 2934
cost 2977
points 17.001328638984223
n_pass 266
n_fail 0
filesize 1678
```

Local gain:

```text
ln(3051 / 2977) = 0.024553323700893265
```

This clears the standalone low-risk submit gate `0.020`.

## Change

Two equivalent graph rewrites were applied:

1. Replace `Neg(masked) -> ArgMax(negmask)` with `ArgMin(masked)`.
2. Shorten `Slice` range vectors from four axes to the actual spatial axes:
   `Slice(input, [rstart, cstart], [rend, cend], axes=[2,3])`.

The second rewrite removes the explicit batch/channel range constants
`zero2_i` and `one_ten_i`, adds a smaller `axes_hw_i = [2,3]` initializer, and
shrinks `s_start`/`s_end` from `[4]` to `[2]`.

## Result

Adopted. This validates the relative-reduction priority rule on a compact
medium-cost task: removing 74 cost from a 3051-cost graph was enough to move the
real leaderboard, while the preceding stale micro batch was not worth a submit.

Next action: continue searching for exact low-risk graph-equivalence rewrites
that clear `gain >= 0.020`, especially range-vector shortening, selector
duality (`ArgMax(-x)` vs `ArgMin(x)`), and dtype changes that remove rather
than add a large temporary.
