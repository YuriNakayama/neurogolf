# 20260630 case76 plan — 全400 graph の機械的局所冗長を再監査する

## 仮説
case71/74 で t349 の冗長 Cast / dead-end Cast が実 LB 改善につながった。
同様に、ONNX graph 内の機械的な局所冗長（optimizer pass, Slice axes, dead node, duplicate node）
を全400タスクへ横展開すれば、追加の安全 win が見つかる可能性がある。

## 対象
- `data/output/onnx` 全400タスク

## 手法
1. `onnxoptimizer` の各 pass を単独適用し、faithful/cost 減候補を抽出。
2. case70 の `Slice axes 省略` を全 task に適用可能か probe。
3. dead-end node / unused initializer / duplicate node / duplicate initializer を graph 走査。
4. n_fail=0 かつ cost 減、さらに実 LB 上昇が見込める候補だけ採用。
