# 20260630 case104 plan - t285 color_pad_flat の suffix zero Concat を Pad 化

## 現状
- floor/Public: 7180.95
- 対象: `task285`
- baseline faithful: cost 25630, params 550, memory 25080, n_fail 0

## 背景
case77 では connected-source 反復削減と主要中間置換を検証し、現行 iter4 が faithful 上必要と判明した。

今回の対象は別箇所で、序盤に `color_idx_flat` `[900]` へ `bottom_zero_row` `[30]` を `Concat(axis=0)` して `color_pad_flat` `[930]` を作る処理。

```text
Reshape color_idx -> color_idx_flat
Concat color_idx_flat + bottom_zero_row -> color_pad_flat
Gather color_pad_flat, cand_*_indices
```

## 仮説
`bottom_zero_row` は suffix zero padding なので、`Pad(color_idx_flat, pads=[0,30], value=0)` に置換できる可能性がある。case102 の t009 と同様に微小削減になる見込みだが、uint8/opset16 なので実行可能性は高い。

## 手法
1. `Concat color_idx_flat + bottom_zero_row -> color_pad_flat` を `Pad` に置換する。
2. `bottom_zero_row` initializer を削除する。
3. `src.evaluate.scorer.audit_one` で `task285` の完全一致を確認する。
4. `n_fail=0` かつ cost 減なら Kaggle submit し、Public Score で採否を決める。

## 足切り
- 変更対象は `task285` のみ。
- 全数 `audit_dir` は使わず、`audit_one` のみ行う。
