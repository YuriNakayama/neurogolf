# 20260702 case491 result - no-submit task041 cadence scan

## Decision

No submit.

Accepted Public Score remains:

```text
7186.79
```

## Baseline

```text
task041.onnx
cost=1789
params=59
memory=1730
status=ok
```

## Scan

Existing unique `task041*.onnx` scratch candidates from `/tmp` and
`/private/tmp`:

```text
candidates=206
positive cost-only candidates=167
```

All positive candidates were gated:

```text
blocked-local-fail=167
submit-candidate=0
bank-low-gain=0
```

Representative failures:

```text
1789 -> 1459  gain=0.203906  n_fail=266  blocked-local-fail
1789 -> 1779  gain=0.005605  n_fail=44-266 blocked-local-fail
```

## Conclusion

Reject locally. The `task041` scratch pool mostly aliases color/bitwise channels
inside the final packing path. These changes reduce cost but break functional
correctness across many validation pairs.
