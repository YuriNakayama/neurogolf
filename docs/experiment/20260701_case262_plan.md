# 20260701 case262 plan - task099 Gather indices int32

## 背景

- Current floor: Public Score `7181.61`
- `task099` baseline: `cost=1911` (`params=278`, `memory=1633`, `n_fail=0`)
- `ScatterElements` の `render_flat_idx` は既に int32 だが、`Gather` 用の `render_label_idx[63]` や `swap2_idx[2]` は int64 のまま。
- 小〜中 cost task では initializer の dtype 半減でも相対改善が見込める。

## 対象

- `data/output/onnx/task099.onnx`

## 仮説

ONNX `Gather` が int32 indices を許すなら、`render_label_idx` と小さい swap/index initializer を int32 化できる。特に `render_label_idx[63]` は int64 で 504 byte相当の initializerとして効くため、int32化だけで `params` を大きく削れる可能性がある。

## 手順

1. `/tmp` に `task099` の Gather index int32版 ONNX を生成する。
2. `src.evaluate.scorer.audit_one` で `task099` の完全一致と costを確認する。
3. `n_fail=0` かつ cost 減なら本体へ反映し、Kaggle submit で採否を決める。
