# 20260701 case341 plan - source-crop and terminal projection scan

## Context

- Current accepted Public Score floor: `7185.09` after case338.
- Case339 low-risk cleanup scan and case340 compact medium-risk scan found no
  new submit-worthy candidates.
- Submit-saving policy remains active: do not spend a Kaggle submit unless a
  candidate clears its risk-adjusted threshold.

## Target

No single task is selected up front. Search another family:

- source-crop rewrites
- kernel-crop rewrites
- terminal projection rewrites
- sparse-tail or final producer bypasses

## Hypothesis

Some terminal branches may be overbuilt and removable, but aggressive bypasses
are likely to fail correctness. Submit only if a candidate is exact and material;
otherwise record near-misses and skip submit.

## Verification Plan

1. Run limited source-crop / terminal projection scans.
2. Audit changed task only for exact candidates.
3. Exclude already rejected families, especially task349 kernel crop from
   case246.
4. Submit only if a new candidate clears the submit-saving threshold.
