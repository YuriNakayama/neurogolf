# case219 result: task399 threshold board trim

## 結果

- 対象: `task399`
- baseline: `cost=91` (`params=20`, `memory=71`, `n_fail=0`)
- submit: なし
- 採否: 不採用

## 検証内容

temp ONNX で以下を個別に差し替え、`src.evaluate.scorer.audit_one` で検証した。

- `red_avg -> count/900` (`count=0..21`)
- `blue -> thresholds` 比較後の固定 pattern
- `board -> channel_truth == blue` の固定 pattern
- all false / all true の `blue` と `board`

## 監査結果

| 変更 | cost | n_pass | n_fail | 判定 |
| --- | ---: | ---: | ---: | --- |
| baseline | 91 | 273 | 0 | ok |
| `red_avg` fixed | 51 | 0-82 | 191-273 | reject |
| `blue` fixed pattern | 46 | 0-82 | 191-273 | reject |
| `board` fixed pattern | 44 | 0-82 | 191-273 | reject |
| all false / all true | 44-46 | 0 | 273 | reject |

## 結論

red count は複数パターンに分布しており、`GlobalAveragePool -> Greater(thresholds)` は全ペア正答に必要だった。`board` 固定なら `91 -> 44` と相対削減率は大きいが足切りを満たさない。実 ONNX は変更せず、Kaggle submit もしない。

次は固定化以外の同等変形、特に `Pad`/`Slice`/`Gather` の shape 表現を削れる候補を探す。
