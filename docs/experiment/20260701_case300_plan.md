# 20260701 case300 plan - task080 remove redundant corner guard

## Context

- Current Public Score floor: `7183.03`.
- case299 cleanup-only `task023` was locally exact but rejected because Public
  Score did not move.
- A parallel explorer found a larger proof-grade guard removal candidate in
  `task080`.

## Target

- `task080`
- Baseline audit:
  - `params=112`
  - `memory=16724`
  - `cost=16836`
  - `n_pass=231`
  - `n_fail=0`

## Hypothesis

Current graph:

```text
cblk      = Greater(dconv, zero_u8)          # [1,1,10,10]
ccf       = Cast(cblk)
cv        = ccf * macrof
cornerc   = ReduceMax(cv)                    # scalar
hascorner = Greater(cornerc, hf)              # scalar
cblk2     = And(cblk, hascorner)              # [1,1,10,10]
corneru   = Cast(cornerc)
s2        = Where(cblk2, corneru, s1)
```

If `cblk` is nonempty, `cornerc > 0.5` and `hascorner` is true.  If `cblk` is
empty, `cblk` is already all false, so `And(cblk, hascorner)` is still equal to
`cblk`.  Therefore `cblk2 == cblk` at the only consumer.

Rewrite:

```text
s2 = Where(cblk, corneru, s1)
```

Then remove the dead `hascorner` Greater, `cblk2` And, and `hf` initializer.

Expected cost:

- remove `cblk2 [1,1,10,10]` bool memory: `-100`;
- remove `hascorner [1,1,1,1]` bool memory: `-1`;
- remove scalar initializer `hf`: `-1`;
- estimated `16836 -> 16734`.

## Gate

- Edit only `data/output/onnx/task080.onnx`.
- Verify with `src.evaluate.audit_one` for `task080` only.
- Submit only if `status=ok`, `n_fail=0`, and cost decreases.
