# 2026-07-01 case129 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task089.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task089.onnx`
- baseline: params 75, memory 7121, cost 7196, n_pass 267, n_fail 0

## 仮説

`task089` は `is3` branch と `is2` branch を別々に処理し、最後に `a2_final_idx` を出力する。片側 branch、body value、patch flip、または neighbor guard が冗長なら、13x13 級の Conv/Where 中間を削れて数 % 以上の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `a2_final_idx -> a3_final_idx/orig_idx`、`a3_final_idx -> orig_idx`、`a2/a3` の mask と patch branch を簡略化する。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task089.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
