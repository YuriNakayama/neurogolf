# 20260702 case442 result - task371 ScatterND index dtype is no-submit

## Decision

- Task: `task371.onnx`
- Submit: no
- Adoption: none
- Reason: the only high-relative-gain path requires int32 `ScatterND` indices,
  which ONNX Runtime rejects.

Current baseline:

```text
params=91
memory=376
cost=467
status=ok
```

## Graph Evidence

The graph is tiny:

```text
Einsum(input, blue_selector, half_coords) -> row_center_f
Einsum(input, blue_selector, half_coords) -> col_center_f
Cast(row_center_f) -> row_center_i INT64
Cast(col_center_f) -> col_center_i INT64
Concat(zero_index, zero_index, row_center_i, col_center_i) -> base_index
Add(base_index, scatter_offsets) -> indices INT64 [1,1,10,4]
ScatterND(input, indices, updates) -> output
```

Dominant memory:

```text
indices         INT64 [1,1,10,4]  320 bytes
scatter_offsets INT64 [1,1,10,4]  320 bytes initializer
updates         FLOAT [1,1,10]     40 bytes initializer
```

The logarithmic score makes this attractive: replacing the index path with int32
would roughly halve the dominant index memory on a `cost=467` task.

## New Candidate

Built `/tmp/neurogolf_case442_task371_i32_indices.onnx`:

- `zero_index` int64 -> int32;
- `scatter_offsets` int64 -> int32;
- `row_center_i` / `col_center_i` casts changed to int32;
- stale value_info removed and shapes re-inferred.

Gate result:

```text
baseline cost : 467
candidate cost: None
status        : session_error / invalid graph
reason        : ScatterND rejects tensor(int32) indices
decision      : blocked-local-fail
```

This matches the runtime constraint seen in the earlier `task131` and `task284`
ScatterND dtype probes.

## Existing Candidate Re-Gate

Re-gated 17 existing scratch candidates:

```text
task371_5_Add_in1.onnx
  cost 467 -> 50
  gain 2.234306
  n_fail 266
  decision blocked-local-fail

task371_5_Add_in0.onnx
  cost 467 -> 107
  gain 1.473500
  n_fail 266
  decision blocked-local-fail

cleanup / broadcast_compress / conv1x1_to_gather / identity:
  cost 467 -> 467
  n_fail 0
  decision blocked-no-gain

index_surgery / dtype_half_coords / dtype_scatter_offsets:
  invalid graph due to type mismatches or ScatterND index constraints
  decision blocked-local-fail
```

## Conclusion

No submit. `task371` has a compelling relative-gain target, but the runtime
requires int64 indices for `ScatterND`. Any repair cast would recreate the large
int64 `indices` tensor and erase the saving.

Do not revisit `task371` through index dtype surgery unless the runtime/operator
constraint changes.
