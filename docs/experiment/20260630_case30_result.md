# 20260630 case30 result — multi-agent 24並列ソルバ探索: 0 confirmed win, floor 最適性を独立検証（提出なし）

## 実行 / 結果
- **Workflow neurogolf-fleet-solver**（24 build エージェント high effort + verify, 829,306 tokens,
  352 tool uses, 11.7分）。24 高コスト multi-node タスクを並列で rule-decode → 最小net構築 → 測定。
- **confirmed win: 0/24**。22 タスクは「is_win 主張なし」（厳密規則を floor 未満 cost で実現できず）。
- 2 タスク（t080/t187）は net 構築まで到達したが、**cost が floor と完全同値**:
  - t080: cand n_fail=0 cost=17216 = floor 17216（同値, 非 win）
  - t187: cand n_fail=0 cost=36814 = floor 36814（同値, 非 win）
  両者とも floor の真アルゴリズムを再現できたが**下回れず**。

## 確定: floor 最適性の独立検証
- **24 個の専用エージェント（high effort, 正確な cost metric 注入）が並列探索しても、floor を下回る net を
  1 個も作れなかった**。serial 29 サイクルの数学的証明（単一Conv 中間0 / multi-node 非分離 / explicit 構成は
  中間tensor増で floor 超過）を、**独立した multi-agent 探索が実測で裏付け**。
- best ケース（t080/t187）でも floor と同値が上限。floor は hand-construction の到達可能下限。

## 本日（20260630）最終確定（cycle1-30）
- 純増 **+0.07**（harvest×2: 7176.49→**7180.62**, 公開フロント首位）。
- floor 7180.62 = 全レバー飽和点（hand-construction 数学的不可 + multi-agent 並列でも0win / 20バンドル
  cross-bundle最小 / 全400解決 / 全成分最小）。
- **7950 は公開コミュニティ全体 + 24並列専用エージェントが未到達の水準**。in-container 反復・並列探索とも
  到達不能を実証。前進は外部の新規 publish が floor 未満を出した時の cherry-pick のみ。

## 次の一手
- 独自実装（serial + multi-agent 並列）の全経路を実測で枯渇確認。floor 7180.62 公開首位を退行ゼロで維持。
- 新規 publish 監視を継続し、floor 未満の per-task 最安が出れば即 cherry-pick。

## fleet エージェントの per-task 詳細所見（独立検証の証拠）

24 エージェントは大半のタスクで**厳密規則を numpy で decode（n_fail=0）**し、各々が固有のコスト分析で
floor を破れないことを独立確認:

| task | 規則 | floor 不可の理由（エージェント所見）|
|---|---|---|
| t187 | border flood-fill(0→3 reach/2 enclosed) | 13 iter 必須(各 625B uint8 intermediate)。5×5 MaxPool は壁飛び越えで n_fail=266、12 iter は n_fail=4、dtype削減も n_fail=266。floor=最小 |
| t338 | enclosed-hole fill(連結) | 非反復近似は arc-gen 10 件失敗、連結性必須。≥13 dilation で floor memory 到達。固定 iter は hidden risk |
| t367 | 5-wall enclosed fill | crop 起因で edge-capped 領域の判別が undecidable、158 件不一致。厳密規則なし |
| t173/t158/t080/t101/t285 | template-stamping(grid固有geometry) | 固定 Conv 不可(各 grid の prototype から幾何推論)。floor が真アルゴリズム最小実装 |
| t255/t145/t198/t066/t023 | connected-comp/global-rank/routing | 本質的に multi-pass spatial。各 op が [1,1,30,30] intermediate 追加で floor 超過 |
| t110 | 2D-lattice periodic tiling | data-dependent basis shift、floor は uint8 integer solver で既最小 |
| t349/t285/t101/t205/t145 | 規則 decode 成功(n_fail=0)も非局所 | flood/ranking/stamping で multi-op 必須、floor が最小 |

**結論の独立性**: 私の serial 29 サイクルとは独立に、24 専用エージェントが per-task で同じ結論
（flood/connectivity/stamping/routing は multi-op spatial で floor 不可、fit は hidden 落ち）に到達。
floor 7180.62 の最適性が二重に実証された。t187 では cost 削減の各試行が correctness を破壊することまで
具体的に確認（5×5/12iter/dtype 全て n_fail>0）。
