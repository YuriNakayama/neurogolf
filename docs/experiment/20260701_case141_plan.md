# 2026-07-01 case141 plan

## 方針

- floor: Public Score 7181.15
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task037.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task037.onnx`
- baseline: params 132, memory 4614, cost 4746, points 16.534942563004293, n_pass 266, n_fail 0

## 仮説

`task037` は色ごとの bbox から最大6 slotを選び、7 offset の diagonal line を `ScatterElements` で描く。`slot_present` と `valid_offsets_raw` による `Where(..., zero_u8)` が3箇所あり、`linear_idx_u8` と `updates_3d` は同じ mask を共有している。

invalid offset の index と update が同時に0なら scatter の `reduction=max` で無害な可能性がある。どちらかの mask を省略できれば、6x7 中間を削れる。

## 手順

1. `/tmp` 上の candidate ONNX で `slot_color_present`、`linear_idx_u8`、`updates_3d` の各 mask を個別に外す。
2. `k6` や `offsets` 範囲の縮小も、完全一致が保てるかだけ確認する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task037.onnx` に反映する。
4. submit し、Public Score が 7181.15 を上回れば採用、上がらなければ破棄する。
