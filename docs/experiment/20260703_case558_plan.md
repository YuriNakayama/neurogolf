# 20260703 case558 plan - post task349 standalone scan

Accepted Public Score:

- `7187.57`

## Background

Case557 adopted `task349 24369 -> 23612`, improving the Public Score. Case556
also adopted an 11-task micro bundle, but the next cycle should return to
standalone candidates because micro gains are small and submit-budget
inefficient.

## Priority

Use the logarithmic score model:

```text
gain = ln(old_cost / new_cost)
```

Prefer exact candidates with `gain >= 0.020`. Keep `n_fail=0` as the hard gate.

## Targets

Parallel worker scans:

```text
task340, task367, task319, task118, task198, task145, task202, task204,
task219, task255, task222
```

Main thread:

1. Re-screen current `/private/tmp` exact candidates against the updated
   baseline for non-risk tasks.
2. Prioritize tasks with positive gain and low local failure counts for bounded
   repair.
3. Submit a full 400-task bundle only for an exact positive candidate.

## Submit Rule

Submit immediately for a standalone candidate with `n_fail=0`, `functions=0`,
forbidden ops `[]`, and lower cost. Avoid another micro bundle unless the
cadence target would otherwise be missed and all standalone searches are empty.
