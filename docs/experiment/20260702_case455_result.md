# 20260702 case455 result - task230 conv-kernel crop re-gate

## Outcome

No submit.

Current baseline:

```text
task230.onnx
params=900
memory=0
cost=900
points=18.1976052367
n_fail=0
status=ok
```

## Candidate Scan

Collected 59 existing `task230*.onnx` scratch candidates from `/private/tmp`
and `/tmp`.

Cost-only screening found 19 positive reductions, all from Conv kernel cropping.
Every positive reduction failed local correctness:

```text
positive_cost_reductions=19
exact_positive=0
exceptions=0
```

## Gate Results

The strongest candidate kept only the kernel center:

```text
/private/tmp/neurogolf_case362_kernel_crops_v2/task230_0_top_bottom_left_right.onnx
  900 -> 100
  gain=2.1972245773
  n_fail=266
  decision=blocked-local-fail
```

Two-axis crops also failed all examples:

```text
/private/tmp/neurogolf_case362_kernel_crops_v2/task230_0_top_bottom.onnx
  900 -> 300
  gain=1.0986122887
  n_fail=266

/private/tmp/neurogolf_case362_kernel_crops_v2/task230_0_left_right.onnx
  900 -> 300
  gain=1.0986122887
  n_fail=266
```

Single-edge crops had lower but still large theoretical gain and also failed
all examples:

```text
task230_0_top.onnx
task230_0_bottom.onnx
task230_0_left.onnx
task230_0_right.onnx
task230_node0_conv_weight_*_crop1.onnx

900 -> 600
gain=0.4054651081
n_fail=266
```

## Decision

Reject for submission. The task is low cost and the relative gains from kernel
cropping are numerically large, but the full 3x3 Conv kernel is semantically
required. There is no exact current-baseline cost-reducing candidate in the
inspected pool.

## Next

Do not continue the simple Conv-kernel crop family for `task230`. If checking
other low-cost Conv-weight tasks (`task220`, `task151`, `task114`, `task098`,
`task015`), use the same bounded gate and expect kernel crops to be high-risk
unless a task-specific proof shows a border is unused.
