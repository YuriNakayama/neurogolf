# 20260701 case338 result - task266 dead BitwiseXor prune

## Decision

Adopted.

## Local Gate

Only `task266.onnx` was changed and audited.

```text
baseline:  params=79 memory=255 cost=334 n_fail=0 status=ok
candidate: params=79 memory=240 cost=319 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(334 / 319) ~= 0.0460 pt
```

## Submission

```text
ref=54234757
description=case338 task266 dead bitwisexor prune cost 334 to 319
status=COMPLETE
publicScore=7185.09
```

Previous accepted floor:

```text
7185.05
```

The Public Score improved by `+0.04`, so the candidate is accepted.

## Diff Summary

The accepted change removes a dead `BitwiseXor(ch3, ch3) -> zero` node that is
not on the output dependency path. This is a low-risk cleanup: params are
unchanged and profiled memory decreases by 15 bytes.

## Next Step

Continue using the submit-saving policy. The compact medium-risk scan found no
standalone candidate above threshold; those exact candidates are banked in
`micro_backlog.md` for future low-cost batching.
