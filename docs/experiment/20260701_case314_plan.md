# 20260701 case314 plan - task219 residual alias bundle

## Context

- Current accepted Public Score floor: `7183.14` after case313.
- Candidate source: case313 parallel residual scan, recorded in
  `docs/experiment/micro_backlog.md`.
- Priority rule: submit exact local candidates by relative reduction, with
  `n_fail=0` as the hard gate.

## Target

- `data/output/onnx/task219.onnx`

## Baseline

Current local audit:

```text
params=91
memory=17149
cost=17240
n_pass=265
n_fail=0
status=ok
```

## Hypothesis

After the adopted case283 Min/Max bundle, `task219` still contains a later
residual block that recomputes tensors equivalent to earlier block outputs. The
temporary scan found the following exact aliases:

```text
mul_174  -> gat_164
gat_468  -> gat_392
red_497  -> red_345
red_501  -> red_425
red_505  -> red_429
pad_521  -> pad_445
sli_540  -> gat_537__h16
```

Together these should prune duplicated `Mul`, `ReduceSum`, `Pad`, and `Slice`
work while leaving the terminal path unchanged.

## Expected Gain

Temporary audit result from the residual scan:

```text
params=91
memory=15873
cost=15964
n_pass=265
n_fail=0
status=ok
```

Expected point gain:

```text
17240 / 15964 = 1.0799
ln(old/new) ~= 0.0770 pt
```

## Rejected Neighbor

Late `Max` aliases such as:

```text
max_459 -> max_383
max_535 -> max_459 / max_383
```

failed local correctness and are not included.

## Verification Plan

1. Apply only the seven aliases listed above.
2. Dead-prune unused nodes, initializers, and value_info.
3. Run individual `evaluate.audit_one` for `task219`.
4. Submit only if `n_fail=0`, `status=ok`, and cost decreases as expected.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
