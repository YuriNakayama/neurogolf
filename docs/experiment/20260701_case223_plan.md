# case223 plan: task290 TopK spatial table trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task290` baseline は `cost=355` (`params=63`, `memory=292`, `n_fail=0`)。
- graph は `ReduceSum -> TopK(k=3) -> size match -> OneHot(color ranks 2,1) -> spatial table -> Einsum`。

## 対象

- `data/output/onnx/task290.onnx`

## 仮説

`color_cols=[2,1]` で TopK の rank 2 と rank 1 を使い、`outer_col=1` で rank 1 の count から size pattern を選んでいる。もし rank 2 が不要、または rank 2 を rank 0/1 で代用できるなら、`k=2` や `color_cols` 変更で TopK 出力を縮められる。`spatial_table` の行数も size pattern が限定されれば削減できる。

## 検証

- temp ONNX で `topk_k=2/3/4`、`color_cols` variants、`outer_col` variants を probe する。
- `size_match` 固定、`spatial_table` row 固定を probe する。
- `src.evaluate.scorer.audit_one` で `task290` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
