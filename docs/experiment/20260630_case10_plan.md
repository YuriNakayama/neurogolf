# 20260630 case10 plan — 7件 negative-pad タスクの mirzayasir 版 swap を LB 実測で検証

## 仮説
case9 で floor の 7 タスク（045/127/135/146/149/240/384）が **ローカル ORT で score_error
（negative-pad / MaxUnpool）**＝ローカルでコスト測定不能と判明。floor のこれら net の真の Kaggle
コストはローカルでは分からない。mirzayasir（top-score）バンドルは**これら 7 全てローカルで scorable**
かつ faithful n_fail=0（cost: t135=366, t127=900, t146=2256, t045=7782, t149=5139, t240=66594,
t384=74751）。

mirzayasir は cycle2 で測定可能 393 タスクは 0-win（≥floor）。**唯一の優位可能性はこの 7 タスク**。
floor の真コストが mirzayasir より高ければ swap で加点。低ければ損。ローカルで判定不能なため
**実 LB で 1 回だけ probe**する（Kaggle は最良提出を保持＝退行リスクゼロ）。

## 採否
- 実 LB > 7180.58 → mirzayasir の 7 版が floor より安い → 採用。
- 実 LB ≤ 7180.58 → floor の 7 版が同等以上 → revert（floor 維持）。
