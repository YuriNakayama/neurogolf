# 20260702 case418 result - task349/task364 docs-directed recheck

## Summary

No submit.

Accepted Public Score remains `7186.71`. The docs-directed recheck found only
locally incorrect reductions for `task349` and `task364`, including the known
high-relative-gain propagation/halo cuts. None cleared the post-regression
submit gate.

## Current Baseline

```text
task349 cost=24389
task364 cost=23013
```

## Audited Candidates

The following candidates were rechecked from the documented scratch
directories:

| task | candidate | cost | est gain | local result | note |
|---:|---|---:|---:|---|---|
| 364 | `task364_PA7PB7_TO_PA6PB6.onnx` family | `21253` | `0.079561` | `n_fail=21` | both propagation-depth branches are required |
| 349 | `task349_halo_c4_TO_ch9_u8.onnx` / `halo_c4_to_c3` family | `22576` | `0.077245` | `n_fail>=17` | detector/halo branch is required |
| 364 | `task364_PB7_to_PB6.onnx` | `22133` | `0.038990` | `n_fail=21` | one propagation branch still required |
| 364 | `task364_PA7_to_PA6.onnx` | `22133` | `0.038990` | `n_fail=21` | symmetric one-branch cut still fails |
| 349 | `task349_hpos_c4_to_c3.onnx` | `23476` | `0.038154` | `n_fail=57` | horizontal-position branch is not redundant |
| 349 | `task349_hpos_c3_to_c4.onnx` | `23476` | `0.038154` | `n_fail=42` | symmetric branch alias fails |
| 349 | `task349_beam_to_ch9.onnx` | `23489` | `0.037600` | `n_fail=255` | broad beam alias breaks most examples |
| 364 | `task364_f_to_w2.onnx` | `22573` | `0.019305` | `n_fail=266` | below submit threshold and locally incorrect |

All checked files had `functions=0` and no banned ops, so the blocker is
functional correctness rather than ONNX compatibility.

## Decision

Rejected locally. Do not submit.

`task349` and `task364` remain interesting only if a repair preserves the
removed propagation/halo semantics while reducing a downstream representation.
Plain branch-depth aliases should be treated as blocked-local-fail, not as
pending submit candidates.

## Next Step

Continue with isolated, mechanically explainable `gain >= 0.020` candidates.
Avoid another weak cadence submit: the recent regression pattern shows that
locally exact but unisolated aliases are not worth a Public LB attempt unless
they either clear the stronger threshold or have prior Kaggle-valid evidence.
