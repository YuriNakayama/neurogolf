# 20260701 case158 plan

## Context

- Current accepted Public Score: 7181.17
- Recent rejected probes:
  - case155 `task378`: local cost -6, LB unchanged
  - case156 `task284`: ScatterND guards required
  - case157 `task397`: ScatterElements safety mask required
- Priority: find `n_fail=0` changes with at least ~1% task-local cost reduction when possible.

## Target

- Task: `task208`
- Baseline audit:
  - params: 109
  - memory: 5974
  - cost: 6083
  - points: 16.286746725679293
  - n_pass: 266
  - n_fail: 0
  - filesize: 8272

## Hypothesis

`task208` finds a source color, locates a matching empty rectangle with `QLinearConv`, then draws a border via 30x30 row/col masks:

- row mask chain: `row_range`, `row_hborder`, `row_nonoutside`, `row_code`
- col mask chain: `col_range`, `col_vborder`, `col_nonoutside`, `col_code`
- final `border = Less(row_code, col_code)` and `Where(border, box_onehot, input)`

These 30x30 bool/u8 intermediates dominate memory. If any side of the border code is redundant for the public distribution, a single bypass could remove hundreds of cost and be large enough to move Public LB.

## Method

1. Probe one mask/code replacement at a time on temporary ONNX copies.
2. Run `audit_one` only for `task208`.
3. Apply and submit only if `n_fail=0` and cost reduction is meaningful.
4. If Public Score does not exceed 7181.17, revert the ONNX/DVC change and document rejection.
