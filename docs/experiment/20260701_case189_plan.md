# 20260701 case189 plan

## Context

- Current accepted Public Score: 7181.21
- case187 and case188 showed tiny params-only reductions do not move the real
  Public Score.
- Return to full-intermediate removal candidates.

## Target

- Task: `task226`
- Baseline audit:
  - params: 20
  - memory: 1613
  - cost: 1633
  - points: 17.601825907029536
  - n_pass: 133
  - n_fail: 0
  - filesize: 1989

## Hypothesis

`task226` builds the output one-hot channels from 10x10 boolean planes. The
color-4 plane is generated as:

```text
ch4 = Xor(ch1, ch1)
```

This is a full 10x10 false plane costing 100 bytes. Replacing `ch4` with an
already existing full-shape false plane would remove the `Xor` output with no
new params.

## Method

1. Probe only temporary `task226` ONNX copies.
2. Alias `ch4` to each existing full-shape channel-like tensor.
3. Run `audit_one` only for `task226`.
4. Apply and submit only if `n_fail=0` and cost decreases.
