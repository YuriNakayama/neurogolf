# 20260630 case98 plan — t076 の source propagation と dihedral candidate 削減を検証する

## 背景

- 現 Public best は **7180.89**。
- `task076.onnx` は faithful baseline で cost **20036**（params 340, memory 19696）。
- 過去ログでは t076 は TopK/ScatterElements/Mod を含む候補が Kaggle ERROR になった履歴があり、現 floor は安全な提出済み版。
- Graph は 15x15 board で source component を 3段 MaxPool で広げ、visible/hidden source shape を 7通りの dihedral 変換で target へ照合し、hidden cells を ScatterElements で足す。

## 仮説

source propagation 3段、または 7通り dihedral candidate の一部が余剰なら、memory を下げられる。ただし t002 のように local faithful が実 LB と乖離する可能性があるため、候補が出ても TopK/ScatterElements 周辺の変更は慎重に扱う。

## 対象タスク

- `task076`

## 手法

1. ORT profile で memory 上位 tensor を確認する。
2. `/tmp` variant で以下を試す。
   - `source_component -> source_step2_mask`（3段目削除）
   - `source_component -> source_step1_mask`（2段目以降削除）
   - `visible_rows_stack` / `visible_cols_stack` の dihedral 候補を一部重複化して候補削減の必要性を確認
   - hidden 側も同様に候補削減を確認
3. faithful `n_fail=0` かつ cost 減の候補が出た場合のみ、Kaggle submit のリスクを結果に明記して採否判断する。

## 採否

- faithful `audit_one` が `n_fail=0` かつ cost が 20036 未満なら submit 候補。
- Public Score が 7180.89 を上回れば採用し、DVC push と git commit/push を行う。
- 壊れる候補、または local-only リスクが高く submit しない候補は破棄する。
