# 20260701 case284 plan - task173 bounds/slot alias bundle

## Context

- Current adopted Public Score floor: `7182.00`
- Latest adopted cycle: case283 `task219` Min/Max aliases (`18417 -> 17240`)
- Candidate source: parallel explorer scan over mid/high-cost tasks.
- Priority rule: choose exact candidates by relative reduction.

## Target

- `data/output/onnx/task173.onnx`

## Baseline

Current local audit:

```text
params=112
memory=17534
cost=17646
n_fail=0
```

## Hypothesis

Several bounding/slot tensors are equivalent to already available clamp tensors
on all valid examples:

```text
p_max      -> c_u
p_min      -> c_l
x_max      -> c_ul
x_min      -> c_dr
slot_outer -> slot_outer_raw
```

Replacing them should prune redundant `Max` / `Min` / `Where` producers and
their dead dependencies.

## Expected Gain

- Baseline: `17646`
- Candidate from temporary audit: `17371`
- Relative reduction: `17646 / 17371 = 1.0158`
- Expected point gain: `ln(17646 / 17371) ~= 0.0157`

This is smaller than case283 but stronger than the remaining small candidates.

## Verification Plan

1. Apply only the five aliases above.
2. Dead-prune unused nodes, initializers, and value_info.
3. Run individual `audit_one` for `task173` with
   `data/lake/neurogolf-2026/task173.json`.
4. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
