# 2026-07-01 case134 plan

## 方針

- floor: Public Score 7181.11
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task004.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task004.onnx`
- baseline: params 94, memory 5284, cost 5378, points 16.409928163171188, n_pass 265, n_fail 0

## 仮説

`task004` は `Conv` で cell code を作り、`below/right` から endpoint と `row_next` を求め、`mc` で move/stay を分けて `Gather`/`Max` で shift 合成している。

小さい graph だが、`valid -> vals -> any_u8`、`row_next -> mc`、`move_vals/stay_vals` の片側が冗長なら、20x20級の中間を削れて相対削減率が高くなる可能性がある。

## 手順

1. `/tmp` 上の candidate ONNX で、`mc`、`endpoint`、`row_next`、`move_vals`、`stay_vals`、`scalar_valid` 周辺を個別に差し替える。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task004.onnx` に反映する。
3. submit し、Public Score が 7181.11 を上回れば採用、上がらなければ破棄する。
