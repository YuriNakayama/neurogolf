# 20260702 case467 plan - task145 selector/dtype re-gate

## Context

Cases 464-466 produced no submit-grade candidate. `task145` is now the next
high-cost normal-risk target in the queue:

```text
task145 cost=13215 params=111 memory=13104
```

The scoring rule is still relative gain:

```text
gain = ln(old_cost / new_cost)
```

Case199 already showed that `task145`'s area sentinel, directional source
branches, and terminal class overlay are semantically required. Those families
can have large cost reduction but should not be treated as submit candidates
unless a current re-gate proves exactness.

## Target

```text
task145
baseline: data/output/onnx/task145.onnx
task json: data/lake/neurogolf-2026/task145.json
known failed families: area/source/overlay aliases from case199
```

## Hypothesis

The old branch/source aliases will fail again, but there may be a smaller
selector, table, dtype-boundary, or mechanical cleanup candidate on the current
graph. Because cost is memory-heavy, even removing one medium tensor can clear
the submit threshold if exact.

## Method

1. Audit accepted `task145.onnx`.
2. Gather `task145*.onnx` candidates from `/private/tmp` and `/tmp`.
3. Run a cost-only scan first to avoid spending time on stale no-gain files.
4. Correctness-gate only current positive reductions, prioritizing:
   - mechanical cleanup;
   - dtype/broadcast/index surgery candidates;
   - candidates not in the known area/source/overlay families.
5. Submit only if:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

Otherwise record blocked and bank-only candidates, then move to `task157`.
