# 20260630 case88 plan — t158 の stamp scale branch を削れるか検証する

## 背景

- 現 Public best は **7180.87**。
- t158 は cost **35327**（params 2340 / memory 32987）で、template-stamping 系 graph。
- graph は anchor から 3x3 template を取り、`ab_u8` に対して 3 系統の stamp branch (`fill_u81`, `fill_u82`, `fill_u83`) を作り `Max` で合成している。

## 仮説

1. 3 つの stamp scale のうち一部 branch が arc-gen 上不要なら、QLinearConv と weight initializer を削れる。
2. `fill_u81/fill_u82/fill_u83` の Max 合成を 2 branch または 1 branch にしても完全一致するか確認する。
3. 重複 initializer / stale value_info があれば機械的に削る。

## 対象

- `data/output/onnx/task158.onnx`

## 足切り

- `src.evaluate.scorer.audit_one` で t158 の train/test/arc-gen 全ペア完全一致。
- n_fail=0 かつ cost 減少の場合のみ submit。

## 採否

- Public Score が 7180.87 を上回れば採用。
- 上がらない、または faithful で不一致なら破棄。
