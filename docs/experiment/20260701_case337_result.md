# 20260701 case337 result - task286 bitwise equivalence bundle

## Decision

Rejected.

## Local Gate

Only `task286.onnx` was changed and audited.

```text
baseline:  params=741 memory=46272 cost=47013 n_fail=0 status=ok
candidate: params=741 memory=43244 cost=43985 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(47013 / 43985) ~= 0.0666 pt
```

## Submission

```text
ref=54234264
description=case337 task286 bitwise equivalence bundle cost 47013 to 43985
status=COMPLETE
publicScore=7170.81
```

Previous accepted floor:

```text
7185.05
```

The Public Score regressed sharply, so the candidate is rejected.

## Diff Summary

The candidate rewired 395 local-equivalence bitwise-chain intermediates to
earlier tensors and pruned dead nodes. Although it passed all local checks, the
Kaggle Public Score indicates hidden/public validation failures. Treat broad
local-equivalence bundles on task286 as hidden-unsafe unless narrowed and
re-tested with stronger evidence.

## Next Step

Restore the accepted ONNX bundle and keep task286 bitwise-chain equivalence
bundles out of standalone submissions. Prefer candidates with either structural
identity that follows ONNX broadcasting/constant semantics or very small,
auditable rewires; broad local-equality alias bundles should require a higher
expected gain or additional validation before spending a submit.
