# 20260701 case288 result - adopt task069 second uint8 subtraction

## Summary

- Target: `task069`
- Change: replace second removal `Where` with uint8 subtraction
- Baseline: `params=61`, `memory=3748`, `cost=3809`, `n_fail=0`
- Candidate: `params=61`, `memory=3648`, `cost=3709`, `n_fail=0`
- Submit ref: `54224317`
- Public Score: `7182.12 -> 7182.15`
- Decision: adopted

## Change

The current graph after case185 already computes the first removal as uint8
subtraction:

```text
rem1 = Sub(t1_lbl, c8_u8)
```

This cycle changed the second removal from:

```text
t2_nz_b = Cast(t2_lbl -> bool)
rem2 = Where(t2_nz_b, zero_u8, rem1)
```

to:

```text
rem2 = Sub(rem1, t2_lbl)
```

This preserves the task structure: `t2_lbl` is the second 0/1 patch-placement
mask selected from the remaining mask, so subtracting it from `rem1` is
equivalent to zeroing those occupied cells.

## Verification

Individual audit:

```text
task069.onnx
status=ok
params=61
memory=3648
cost=3709
n_pass=264
n_fail=0
```

Kaggle:

```text
ref: 54224317
status: COMPLETE
publicScore: 7182.15
```

## Adoption

Adopt. The real Public Score increased by `+0.03`, consistent with the local
score gain from `3809 -> 3709`.

## Next

Continue structural rewrites and avoid local-only alias bundles. The next
promising candidates are:

- `task367`: move four trailing zero channels from `Concat` into final `Pad`,
  expected `24727 -> 23127`.
- `task209`: crop the label Conv at source with a sparse 11x11 top-left-tap
  kernel, expected `31187 -> 29487`.
