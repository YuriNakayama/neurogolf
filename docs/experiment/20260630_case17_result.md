# 20260630 case17 result — 3×3-local単一Conv化は hidden risk で見送り（提出なし）

## 結論（提出なし、floor 7180.62 維持）
- t293（1288, 42nodes）は厳密 3×3-local（table=2738, 決定的）。規則 = 「8 が 3 領域に隣接/囲まれると
  3 になる」denoise 系。単純規則（any3/maj3/enclosed）は 0-4/267 で不一致＝真の規則は full table。
- 単一 3×3 Conv で table を厳密実現するには線形分離が必要。**case3-cycle1 で『可視データに exact fit した
  単一 Conv は hidden private で 0 点』を実 LB 実証済み**（t192, −15.93）。t293 の table=2738 も学習 fit は
  hidden で真ルールと乖離するリスク。**節約は 1288→910(−378, +0.07pts) と小さく、hidden 0 点リスク
  （−約17pts）に見合わない＝負 EV。見送り。**
- floor の 42-node net は真の denoise アルゴリズムを実装しており hidden 安全。これを学習 Conv fit に
  置換するのは case1 の轍。

## 教訓
- 3×3-local multi-node タスクの「単一 Conv 圧縮」は、table が大きい（学習 fit になる）場合 hidden 落ち。
  floor の multi-node net が真アルゴリズムを実装＝hidden 安全であり、cost 差も小さいため置換は負 EV。
- 安全に単一 Conv 化できるのは table が**真に線形分離な真アルゴリズム**の場合のみ（t015等は既に floor が
  単一 Conv 化済み）。残る multi-node 3×3-local（t293/t004/t192/t222）は table 大で fit リスク。

## 次の一手
- hand-construction / Conv-fit は全棄却（case15/16/17）。harvest が唯一の前進レバー。
- floor 7180.62 維持。7950 まで −769.38。

## cycle追記: t293 線形分離検定 + 全400タスク solved 確認

- **t293 単一Conv 厳密性検定**: 90特徴(3×3×10ch one-hot)→出力色の per-channel 線形分離を lstsq で検定
  → acc 0.993-0.996（**完全分離せず**, color0 のみ 1.0）。単一 Conv は可視データですら t293 を厳密実現
  不可。floor の 42-node net が必要（非線形局所規則）。case17 の見送り判断を裏付け、Conv-fit は誤り。
- **全400タスク solved 確認**: measurable 391 の cost-only=6999.43、real LB=7180.62、offset +181.19
  （per-task +0.45, Kaggle vs local 採点差で一様）。**0点タスクは無く全400が LB で解けている**
  （9件 local-unmeasurable も case10 で LB 正常採点を実証）。未解決タスクによる加点余地はゼロ。

## 最終確定（本日の全体結論）
- floor 7180.62 は **全400タスクを収束公開最適コストで解いた状態**。
  - 加点余地（未解決タスク）: **ゼロ**（全解決済み）。
  - golf 余地（hand-construction）: **ゼロ**（case15/16/17 で memory metric/hidden-fit により根本棄却、
    multi-node net は非線形局所規則で単一 Conv 化不可）。
  - surgery 余地: **ゼロ**（case6, 公開 net は dead-code-free）。
- **唯一の前進 = 新規公開バンドルが per-task 最安を更新した時の harvest cherry-pick**（本日 +0.07 実証）。
  これはコミュニティ publish に依存する外部イベント駆動レバー。
