# 20260702 case481 plan - task216 current scratch re-gate

## Context

Accepted Public Score is `7186.73`. Recent high-cost re-gates found only local
failures, so this cycle checks a medium-cost task where a relative reduction can
still matter.

`task216` current baseline:

```text
task216 cost=9127
params=87
memory=9040
```

It is outside the current hard/review/runtime risk sets.

## Target

```text
task216
baseline: data/output/onnx/task216.onnx
task json: data/lake/neurogolf-2026/task216.json
```

## Method

1. Gather `task216*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against current baseline.
3. Correctness-gate positives with `submit.gate.evaluate_candidate_gate`.
4. Submit only an isolated candidate satisfying:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Otherwise record no-submit and bank exact low-gain candidates.
