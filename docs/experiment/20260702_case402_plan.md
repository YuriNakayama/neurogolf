# 20260702 case402 plan - task009 suffix mask cleanup

## Context

Accepted Public Score is `7186.50` after case401 adopted the isolated
`task335` line-pool selector compression.

Parallel next-cycle searches did not find a new conservative `gain >= 0.02`
candidate before the next 15-minute submit slot. Current-state re-audit of
older banked cleanup candidates shows only one remaining positive low-risk
micro.

## Candidate

```text
task009 8310 -> 8306 gain 0.0004814636587950553
```

Scratch file:

```text
/tmp/neurogolf_case400_nextcycle/task009_suffix_mask_pad_current.onnx
```

## Method

Replace suffix false separator mask construction `Concat(false_col/false_row)`
with equivalent `Pad`. This is a mask-only cleanup and does not touch known
unsafe families.

## Local Gate

Current-state re-audit:

```text
task009: status ok, cost 8306, n_pass 265, n_fail 0,
         functions 0, forbidden []
```

Adopt only if Public Score improves over `7186.50`; otherwise restore case401.
If this isolated micro does not move LB, prefer banking similarly tiny changes
until they can be bundled.
