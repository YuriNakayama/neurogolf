# 20260701 case280 plan - task209 bundled vote/kernel aliases

## Context

- Current Public Score floor: `7181.90` after case279.
- Shape-guided task-local alias bundling has produced repeated real LB gains.
- Continue prioritizing relative reduction with `n_fail=0` as the gate.

## Candidate

Target: `task209`

Baseline local audit:

```text
params=185
memory=31867
cost=32052
n_pass=266
n_fail=0
```

Exact aliases selected for the compatible bundle:

```text
total_vote_p1_u8 -> total_vote_u8
fkr_v            -> fkr_ge0
fkc_v            -> fkc_ge0
kj_fit           -> kj_ge
ki_fit           -> ki_ge
row_in           -> r_le_bot
col_in           -> c_le_right
kernel_b         -> kern_eq_anchor_b
```

Bundle audit:

```text
params=183
memory=31004
cost=31187
n_pass=266
n_fail=0
status=ok
```

Relative reduction: `32052 / 31187 = 1.0277`.

Several other exact scalar aliases were score-neutral after this bundle and are
not included.

## Plan

Apply the eight aliases to `data/output/onnx/task209.onnx`, then prune dead nodes
and stale metadata.

Gate with `evaluate.audit_one` for `task209` only. Submit only if:

- `status=ok`
- `n_fail=0`
- cost remains `31187`
