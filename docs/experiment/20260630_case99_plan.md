# 20260630 case99 plan — t219 の階段 block 合成を一点検証する

## 背景

- 現 Public best は **7180.89**。
- `task219.onnx` は faithful baseline で cost **18417**（params 92, memory 18325）。
- 過去ログでは t219 は staircase / 2D逐次系として手作り golf 済み。現 graph は `mul_48` から候補 pattern を作り、複数 block を `Max` で累積して最終 mask `max_535` を作る。
- Graph 後半には `max_231 -> max_307 -> max_383 -> max_459 -> max_535` の段階的合成があり、後段 block が余剰なら大きめの削減余地がある。

## 仮説

後段 block（`k_308`, `k_384`, `k_460` 由来）の一部は rare-case 対応に見える。arc-gen 全ペアで不要なら、`max_535` を途中の `max_459` / `max_383` / `max_307` / `max_231` へ置換して block 一式を prune できる。

## 対象タスク

- `task219`

## 手法

1. ORT profile で memory 上位 tensor を確認する。
2. `/tmp` variant で `max_535` を以下へ置換して audit:
   - `max_459`
   - `max_383`
   - `max_307`
   - `max_231`
   - `gat_151`
3. 各 block 内の orientation selection（`arg_211/287/363/439/515`）を固定・短縮できるかも一点確認する。
4. faithful `n_fail=0` かつ cost 減なら submit 候補とする。

## 採否

- faithful `audit_one` が `n_fail=0` かつ cost が 18417 未満なら `task219.onnx` を差し替えて submit する。
- Public Score が 7180.89 を上回れば採用し、DVC push と git commit/push を行う。
- 壊れる候補、または cost が変わらない候補は破棄する。
