# 20260701 case185 plan

## Context

- Current accepted Public Score: 7181.18
- case183 succeeded by replacing a uint8 boolean-mask `Where` with arithmetic.
- case184 showed the same rewrite fails when the mask source is float.
- A refined scan found `task069` has two `Where(Cast(uint8), zero, uint8)`
  sites whose bool casts feed only those `Where` nodes.

## Target

- Task: `task069`
- Baseline audit:
  - params: 61
  - memory: 3848
  - cost: 3909
  - points: 16.728963134207046
  - n_pass: 264
  - n_fail: 0
  - filesize: 4354

## Hypothesis

`task069` removes already matched 8-blocks with:

```text
rem1 = Where(Cast(t1_lbl), zero, c8_u8)
rem2 = Where(Cast(t2_lbl), zero, rem1)
```

If each nonzero `t*_lbl` overlaps an 8-valued source cell in the removal mask,
then these may be equivalent to:

```text
rem1 = Sub(c8_u8, t1_lbl)
rem2 = Sub(rem1, t2_lbl)
```

This would remove two 10x10 bool intermediates. The risk is that `t*_lbl`
contains template labels rather than literal 8s, so the subtraction may leave
nonzero residue and fail.

## Method

1. Probe `rem1` and `rem2` arithmetic replacements on a temporary ONNX copy.
2. Run `audit_one` only for `task069`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record rejected variants if the template labels break the equivalence.
