# 20260702 case384 plan - task319 isolated stronger bundle

## Context

Accepted Public Score is `7185.98` after case383 adopted `task354`:

```text
task354: 3077 -> 3004, gain 0.024010
```

During case383, a parallel task319 probe found a stronger exact bundle:

```text
task319: 19569 -> 19080
gain 0.025306
scratch /tmp/neurogolf_task319_case383_probe_pruned/combo_0_1_2_3_4_5.onnx
```

This clears the standalone gate, but task319 predicate/value-path rewrites have
some hidden sensitivity. It should be tested alone so the Kaggle result gives a
clean adoption/rejection signal.

## Hypothesis

The stronger task319 bundle removes narrow single-consumer/value-path
intermediates without changing the task behavior. Because it is exact over all
local examples and avoids forbidden operators, it may improve Public Score if
the hidden cases do not depend on the pruned predicate distinctions.

## Method

Apply only the task319 candidate:

- includes the case382 banked bundle around `p10_top_eq`,
  `p20/p21_*_eq_edge`, and `sig_row`
- plus:
  - `p10_top_eq_u8 -> obj0_local10`
  - `obj0_pack_b -> obj0_pack_a`
  - `obj2_top_row_mask -> obj1_top_row_gt0`
  - `obj2_left_col_mask -> obj1_top_row_gt0`
  - `board_h -> row_tail`
  - `obj0_obj_w -> obj0_col_max_p1`

Then run `evaluate.audit_one` for only `task319.onnx`, inspect forbidden ops,
submit, and adopt only if the real Public Score increases.

## Gate

Submit because the isolated local gain clears:

```text
gain >= 0.020
```

Reject and revert if Kaggle Public Score does not increase.
