# 20260703 case527 plan - task379 failure diff repair

## Background

Latest accepted Public Score remains:

```text
7187.38
ref=54272777
```

Case526 showed that unfiltered full correctness gating of broad scratch pools is
too slow and mostly hits runtime-invalid candidates. The strongest current
standalone near miss remains task379:

```text
old_cost=10753
new_cost=10353
gain=0.037908
n_fail=2
candidate=/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Where_color20_TO_color20_c_400.onnx
```

## Hypothesis

The task379 near miss may be repairable if the two failures are a narrow
orientation/color exception. A direct baseline-vs-candidate prediction diff
should identify whether a small restoration can preserve enough of the 400-cost
saving to keep `gain >= 0.020`.

## Method

1. Run baseline and candidate on all task379 train/test/arc-gen examples.
2. Record only failing examples with input/output/prediction differences.
3. Inspect graph names around `color20`, `color20_c`, and `color20_t`.
4. Try only localized repairs that keep static shapes and avoid forbidden ops.
5. Submit only if an exact `n_fail=0` candidate remains below standalone
   threshold cost.

Do not spend a submit on local-fail repairs or on a micro bundle below `0.015`.

