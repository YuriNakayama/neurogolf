# 20260701 case170 plan

## Context

- Current accepted Public Score: 7181.17
- case169 confirmed that removing a core scatter exception can cut cost heavily
  but fails all examples. Continue with compact medium-cost tasks where one guard
  may be redundant.

## Target

- Task: `task244`
- Baseline audit:
  - params: 103
  - memory: 1076
  - cost: 1179
  - points: 17.92757809946263
  - n_pass: 266
  - n_fail: 0
  - filesize: 3861

## Hypothesis

`task244` builds a `geometry_idx` from several sampled cells and uses it to select
precomputed 4x4 row/column grids for `GridSample`.

The graph contains one potentially redundant branch:

- `probe_11_same_color` checks whether two probe cells share a color.
- `is_height_11` checks `r11 < r8`.
- `h11_is_4` is added into `geometry_float`.

If the 4-by-4 height correction is unnecessary for public examples, removing this
guard should reduce cost without changing operator constraints. Other probes will
test whether duplicated terms or individual ReduceMax samples can be aliased.

## Method

1. Probe one rewrite at a time on temporary `task244` ONNX copies.
2. Run `audit_one` only for `task244`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record low-fail geometry aliases for future exception analysis if no exact win
   is found.
