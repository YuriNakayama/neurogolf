# 20260703 case563 plan - non-dtype-sensitive candidate search

Accepted Public Score:

- `7187.57`

## Background

Case561 showed that a locally exact dtype shrink can still produce a Kaggle
`ERROR` (`task316 426 -> 407`). Case562 found no candidate after excluding
`task316`.

## Hypothesis

Avoid raw dtype narrowing and focus on graph-structure reductions that are less
runtime-sensitive:

- single-consumer alias rewires;
- dead-path pruning after alias rewires;
- duplicate or unused initializer cleanup if newly exposed;
- same-shape broadcast compression only when current-baseline exact.

The score priority remains relative gain:

```text
gain = ln(old_cost / new_cost)
```

## Targets

Parallel fresh scans:

```text
low/mid tasks: task001-task180 excluding known risk/recent tasks
mid/high tasks: task181-task300 excluding known risk/recent tasks
range C retry: task301-task400 excluding task316 and known risk/recent tasks
```

Use `submit scan-candidates` on each candidate directory and submit only a
`submit-candidate` that is not review-risk.

## Submit Rule

No Kaggle submit for local-fail, review-risk, or dtype-only candidates. If an
exact positive structural candidate is found, build a full 400-task bundle and
submit immediately.
