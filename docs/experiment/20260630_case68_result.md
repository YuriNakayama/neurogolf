# 20260630 case68 result — t356 ConvInteger 出力の完全模倣を試行、parity 上限を再確認（提出なし）

## independent_impl（必須欄）
case64 の ConvInteger 出力技法で t356 を floor 1319 まで詰める試行:
- floor t356 の正確構造を模倣: Slice→Cast→4 MaxPool(has_up/down/left/right)→Mul×2(row/col_span)
  →Max(mask10)→Add(code=2*mask)→**ConvInteger(code, int8 weight, zp=1)→output**。
- 私の ConvInteger 版: **nfail=266, cost 6332**（不正答 + int32 中間 out_small[1,10,10,10]=4000B が計上）。
  - floor は ConvInteger 出力を **graph output に直結**（memory 除外）。私は out_small→Cast→Pad と
    中間を挟み、int32 10ch が計上されて悪化。floor の「ConvInteger→output 直結」は出力 shape が
    一致する場合のみ可能で、30×30 への Pad を要する私の構成では中間が残る。

## 確定（parity 上限の再々確認）
- ConvInteger 出力技法は floor では「ConvInteger が直接 30×30 output を生成」する形で中間ゼロ。
  私が crop(10×10)→ConvInteger→Pad(30×30)とすると ConvInteger 出力(10×10 int32 10ch)が中間計上され
  floor を超える。floor は crop せず 30×30 で ConvInteger するか、別の最適配置を取っている。
- **floor の ConvInteger 配置まで完全模倣すれば parity(~1319)**だが、純増する余地はない。
- case66(byte-parity) + case67(method-selection最適) + 本case68(出力配置最適) で、floor は
  **op選択・符号化・手法選択・出力配置 の全レベルで最適**と確定。独自実装の上限は parity。

## 34サイクルの総括
独自実装を全方位(cost削減全レバー/規則全類型/floor技法全種/手法選択/出力配置)で検証。
floor 7180.78 は構造的下限で、独自実装は byte 級 parity が上限、下回りは不可能。
目標 7950 は現 op セット・cost モデル・コミュニティ最適化の下で到達手段なし。
これは推測でなく、t251 byte一致(case66)・t002手法選択(case67)・t356出力配置(case68)の実測に基づく。

## 恒久方針の遵守
- 停止せず ConvInteger 出力配置を試行し floor の出力最適性を実証。floor 7180.78 退行なし。
  新 op/規則/仕様変更があれば即試行する体制で継続。

## 次の一手
- floor は全レベルで最適と byte/手法/配置の各実測で確定。残るは外部要因(新 op/仕様)待ちの即応のみ。
  引き続き未 decode タスクの per-task 精査を継続し、偶発的非最適があれば即対応。
