# 20260702 case360 plan - current-baseline near-miss triage

## Context

Accepted Public Score remains `7185.70` after case358. Case359 skipped submit
because exact local material did not clear the submit-saving threshold.

For this cycle, search for a current-baseline candidate that can justify the
next Kaggle submit:

```text
standalone low-risk:    gain >= 0.020
standalone medium-risk: gain >= 0.030
standalone high-risk:   gain >= 0.080
low-risk batch:         gain >= 0.030
mixed-risk batch:       gain >= 0.050
```

## Search Plan

1. Re-rank historical near misses by current-baseline gain, not the original
   pre-adoption gain.
2. Exclude known submit hazards and exhausted families:
   - task002 bit-topology local positives;
   - task286 hidden-unsafe broad bitwise rewires;
   - task243 final frontier unless a generic non-overfit repair appears.
3. Re-audit residual task118 stronger variants from the current accepted
   task118 graph, because case350 and case352 already adopted part of that
   family.
4. Submit only if the current-baseline gain clears the risk-adjusted threshold.

## Hypothesis

The best historical `n_fail=1` candidates may be dominated by accepted repairs.
If so, preserving submit budget is better than spending a weak attempt; any
exact residuals should be banked for a larger future batch.
