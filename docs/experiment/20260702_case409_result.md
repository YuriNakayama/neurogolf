# 20260702 case409 result - task066 u_up score alias

## Submission

- Kaggle ref: `54255846`
- Message: `case409 task066 u_up score alias gain 0.02917`
- Public Score: `7186.66 -> 7186.69`
- Decision: adopted

## Local Gate

```text
task066: cost 16835 -> 16351
         params=142, memory=16209
         n_pass=266, n_fail=0
         forbidden ops=[]
```

Current-relative gain:

```text
0.029170995060588376
```

## Change

Extended the earlier `active_row_any -> red_row_any` alias with an `u_up`
branch score rewire:

```text
n_u_up_score = Where(n_u_up_valid_4, n_u_up_gated_score, zero_f)
          ->  Where(n_u_up_valid_4, n_s_up_edge, zero_f)
```

This pruned the `n_u_up_height_ok_1` / gated-score chain and two initializers.

## Conclusion

Adopted. The isolated submit improved Public Score by `+0.03`, validating the
task-local extension. Continue searching for same-task compatible aliases that
clear the relative-gain threshold before falling back to banked micros.
