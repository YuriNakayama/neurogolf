# 20260702 case355 result - adopt task018 and task363 cleanup batch

## Submission

```text
ref=54243456
description=case355 t018 t363 cleanup batch gain 0.04627
status=COMPLETE
Public Score=7185.62
```

Previous accepted Public Score:

```text
7185.58
```

Result:

```text
+0.04 Public Score
adopt
```

## Local Gate

Changed tasks only:

| task | params | memory | cost | n_pass | n_fail | status |
|---:|---:|---:|---:|---:|---:|---|
| 018 | 2087 | 36596 | 38683 | 266 | 0 | ok |
| 363 | 309 | 3939 | 4248 | 265 | 0 | ok |

Baseline before this cycle:

| task | old cost | new cost | ln gain |
|---:|---:|---:|---:|
| 018 | 39583 | 38683 | 0.023000 |
| 363 | 4348 | 4248 | 0.023268 |

Total expected local gain:

```text
0.046267
```

## Change

Applied two cleanup candidates:

```text
/tmp/neurogolf_case355_struct/task018_sim_cleanup.onnx
/tmp/neurogolf_case355_struct/t363_dead_cleanup.onnx
```

to:

```text
data/output/onnx/task018.onnx
data/output/onnx/task363.onnx
```

Both candidates avoid forbidden operators and have no model functions.

## Conclusion

Adopt case355. The two-task batch moved Public Score while staying small enough
to bisect if needed. This confirms that accumulated exact cleanup candidates can
be worth a submit once their combined relative gain clears the batch threshold.

Next action: continue non-task118 structural search from Public `7185.62`; keep
task118 residual pruning banked because its current-baseline gain is only about
`0.02125`, below the high-risk standalone threshold.
