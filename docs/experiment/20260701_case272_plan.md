# 20260701 case272 plan - task023 duplicate kernel initializer alias

## Context

- Current accepted Public Score floor: `7181.66`.
- Priority remains `n_fail=0` first, then relative reduction `old_cost / new_cost`.
- Large structural candidates in case271 were blocked by bool `Pad` type constraints, so this cycle uses an exact params-only cleanup with no runtime dtype risk.

## Target

- Task: `task023`
- Baseline local cost: `params=157`, `memory=12843`, `cost=13000`

## Hypothesis

`task023` has three pairs of byte-identical uint8 kernel initializers:

- `Ksb` and `Ksc`: shape `(1,1,2,2)`
- `Khb` and `Khc`: shape `(1,1,1,3)`
- `Kvb` and `Kvc`: shape `(1,1,3,1)`

Aliasing the second initializer in each pair to the first and removing the duplicate initializer preserves graph behavior exactly while reducing params by `10`.

## Precheck

Temporary ONNX `/tmp/task023_alias_dup_inits.onnx`:

- checker: ok
- cost: `13000 -> 12990`
- correctness: `n_pass=266`, `n_fail=0`

## Method

1. Rewrite node inputs:
   - `Ksc -> Ksb`
   - `Khc -> Khb`
   - `Kvc -> Kvb`
2. Remove duplicate initializers `Ksc`, `Khc`, `Kvc`.
3. Run `audit_one` only for `task023`.
4. Submit and accept only if actual Public Score increases.
