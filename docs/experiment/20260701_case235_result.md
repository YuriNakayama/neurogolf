# case235 result: task213 phase selector branch trim

## 結果
- 対象: `data/output/onnx/task213.onnx`
- baseline: `cost=1887` (`params=49`, `memory=1838`, `n_pass=265`, `n_fail=0`)
- 採否: 不採用
- Kaggle submit: なし

## 検証内容
h/v の 3-phase selector と `Where` cascade について、branch alias と dtype 変更を probe した。

| 候補 | cost | n_pass | n_fail | 判定 |
|---|---:|---:|---:|---|
| baseline | 1887 | 265 | 0 | ok |
| `h_pos_selected -> h_pos_selected_raw` | 1887 | 146 | 119 | reject |
| `v_pos_selected -> v_pos_selected_raw` | 1887 | 119 | 146 | reject |
| `not_h_valid -> h_valid_no_pair` | 1887 | 0 | 265 | reject |
| row/col factor one-sided aliases | 1887 | 119-146 | 119-146 | reject |
| `h_ids0 -> h_ids_p0` | 1887 | 265 | 0 | reject: cost unchanged |
| `v_ids0 -> v_ids_p0` | 1887 | 265 | 0 | reject: cost unchanged |
| `h_ids0` + `v_ids0` prune | 1887 | 265 | 0 | reject: cost unchanged |
| other h/v ids cascade fixed | 1887 | 155-254 | 11-110 | reject |
| grouped h/v where fixed | 1887 | 155-166 | 99-110 | reject |
| bool position Cast -> int32/float32 | invalid | 0 | 0 | reject: type mismatch |

## 結論
`h_ids0` と `v_ids0` の最初の fallback `Where` は実データ上は true branch で足り、node/file size は減らせる。しかし scorer の memory/params には影響せず、cost は `1887` のままだった。その他の phase selector や h/v gate は完全一致に必要だった。

`n_fail=0` かつ cost 改善の候補がないため、ONNX は変更せず submit しない。

## 次の一手
node 数だけ減る prune は採点上無意味なので、memory tensor の shape/dtype を実際に減らせる候補を優先する。次は low-fail ではなく、完全一致で数十以上の memory を削れる同一 task 内 multi-prune を探す。
