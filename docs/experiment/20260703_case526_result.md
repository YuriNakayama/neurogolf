# 20260703 case526 result - no-submit task379 repair and micro search

## Result

No submit.

Current accepted score remains:

```text
Public Score: 7187.38
latest accepted ref=54272777
```

## Task379 Repair

Target near misses:

```text
/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Where_color20_TO_color20_c_400.onnx
/private/tmp/neurogolf_parallel_AP/scan_candidates/task379_bypass_Transpose_color20_t_TO_color20_c_400.onnx

old_cost=10753
new_cost=10353
gain=0.037908
n_fail=2
```

A focused repair worker was started, but it did not return a repaired exact
candidate within the useful cycle time and was shut down to preserve cadence.
Keep task379 as a next-cycle target, but start from failure diff tooling rather
than broad scratch gating.

## Micro Bank Re-gate

Current exact low-risk bank after case525:

| task | cost | gain | status |
|---|---:|---:|---|
| 066 | `16351 -> 16268` | `0.005089` | exact |
| 161 | `3657 -> 3651` | `0.001642` | exact |
| 219 | `14838 -> 14823` | `0.001011` | exact |
| 349 | `24389 -> 24369` | `0.000820` | exact |
| 260 | `2110 -> 2109` | `0.000474` | exact |
| 054 | `27089 -> 27081` | `0.000295` | exact |
| 204 | `11494 -> 11493` | `0.000087` | exact |

Total across all seven is only `0.009419`; top five total is `0.009037`.
This does not clear the configured `0.015` micro-bundle gate.

The additional scratch search found exact candidates for task383 and task301,
but the best independent exact total remained `0.008082`, still below the
bundle threshold and weaker than the current bank.

## Parent Scratch Gate

A parent process screened recent scratch dirs and found 531 current-baseline
cost-positive candidates. Full correctness gating was stopped after it became
dominated by runtime-invalid candidates:

- ScatterElements/GatherElements out-of-range failures
- modulo-by-zero runtime failures
- output shape mismatches

This was useful as a diagnosis: unfiltered full correctness gating of broad
scratch pools is too expensive for the desired submit cadence. Next cycle should
pre-filter by op family, shape stability, and known runtime-risk signatures
before running `evaluate_candidate_gate`.

## Decision

Skip submit. No standalone `n_fail=0`, `gain >= 0.020` candidate was found, and
the exact low-risk micro pool is below the bundle threshold.

Next action:

1. build a faster failure-diff path for task379, or
2. run a filtered low-runtime-risk micro search until total exact gain reaches
   `0.015`.

