# 20260630 case107 result - t173 後段 slot/center/pair 削減は不可

## 実装候補
`task173` で case81 の `topk_pixels=19` 以外に残る候補幅・final scatter branch を検証した。

対象:

- `topk_slots=3`
- `topk_centers=7`
- final scatter branch: `pair_h_color`, `pair_v_color`, `pair_dr_color`

## faithful 検証
baseline:

- cost 17646, params 112, memory 17534, n_pass 266, n_fail 0

候補:

| 変更 | cost | 結果 |
|---|---:|---|
| `topk_slots: 3 -> 2` | 17623 | `n_fail=1` |
| `topk_centers: 7 -> 6` | 17560 | `n_fail=266` |
| `topk_centers: 7 -> 5` | 17474 | `n_fail=266` |
| drop `pair_h_color` | 17665 | `n_fail=99` |
| drop `pair_v_color` | 17665 | `n_fail=39` |
| drop `pair_dr_color` | 17665 | `n_fail=48` |

pair branch drop は false-vector initializer を追加した soft probe のため cost は増えたが、正答性確認が目的。いずれも faithful 不一致。

## submit
- なし

## 採否
- **不採用**。
- slot/center 幅と final pair branch は全て arc-gen 上必要。

## 結論
t173 は case81 の pixel 候補数だけでなく、後段の slot/center/pair 更新も削れない。TopK+Scatter 構造は小さく見える候補幅まで正答性に効いている。

## 次の一手
- `task286` / `task018` の高 cost trace へ戻り、反復鎖・bitwise chain の局所削減を探す。
