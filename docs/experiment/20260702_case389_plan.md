# 20260702 case389 plan - low-risk micro batch

## Context

Accepted Public Score is `7186.11` after case388.

The submit-saving rule is now more important than single-micro immediacy:
small exact improvements should be banked until the combined relative gain is
large enough to justify a Kaggle submit. Because score gain is logarithmic,
the relevant quantity is still:

```text
gain = ln(old_cost / new_cost)
```

## Target

Re-audit a low-risk batch of previously documented exact micro reductions:

```text
task009  8410 -> 8310   gain 0.011962
task374  2304 -> 2284   gain 0.008718
task382  5670 -> 5627   gain 0.007613
task340 10994 -> 10918  gain 0.006937
```

Expected combined gain:

```text
0.035230
```

This clears the low-risk batch gate `>= 0.030` and gives enough cushion above
the three-task `task009 + task374 + task382` batch, which is only about
`0.028293`.

## Method

Use existing exact scratch candidates from prior cycles, but do not trust them
blindly. Copy each candidate onto the current accepted bundle and run
`evaluate.audit_one` only for the changed tasks:

```text
task009: /tmp/neurogolf_case381_bundle/task009_content_unsq.onnx
task374: /tmp/neurogolf_case385_task374_row_col_base_color.onnx
task382: /tmp/neurogolf_case385_task382_all_tail.onnx
task340: /tmp/neurogolf_case383_task340_bundle_all.onnx
```

## Gate

Submit only if all changed tasks have:

- `n_fail=0`
- no forbidden ops
- no ONNX functions
- combined current-relative gain `>= 0.030`

Adopt only if Kaggle Public Score increases over `7186.11`.
