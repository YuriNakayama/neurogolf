# 20260702 case485 result - adopt task023 greedy S accumulator cleanup

## Decision

Adopt.

Kaggle submission:

```text
ref=54263476
status=COMPLETE
Public Score: 7186.73 -> 7186.79
```

## Change

```text
task023.onnx
candidate: /tmp/neurogolf_case415_scan/task023_greedy_S_S.onnx
old_cost=9340
new_cost=8764
gain=0.063653831
n_fail=0
decision=submit-candidate
```

The accepted file is now:

```text
data/output/onnx/task023.onnx
params=70
memory=8694
cost=8764
points=15.921592299
```

## Verification

Pre-submit gate:

```text
changed=1
total_gain=0.063654
decision=submit-bundle
task023 n_fail=0
functions=0
forbidden_ops=[]
```

Dry-run packaging succeeded and produced:

```text
data/output/submit/submission.zip
```

DVC remote update:

```text
dvc push data/output/onnx.dvc
2 files pushed
```

## Conclusion

Keep the `task023` greedy S accumulator cleanup. It is isolated, locally exact,
and improved the real Public Score by `+0.06`.

The next cycle should continue the parallel lane approach: use workers to search
wide scratch pools while the main lane immediately submits any isolated
`submit-candidate` that clears current-baseline gating.
