# case216 plan: task379 unused initializer prune

## 現状

- 採用済み Public Score floor: 7181.21
- `task379` baseline は `cost=10774` (`params=1274`, `memory=9500`, `n_fail=0`)。
- 静的スキャンで未使用 initializer `neg_one_f` (`float16[1]`) を検出した。

## 対象

- `data/output/onnx/task379.onnx`

## 仮説

`neg_one_f` はどの node input からも参照されておらず、onnxruntime も未使用 initializer として警告している。削除すれば正答性を変えずに params を 1 減らせる。

改善幅は `10774 -> 10773` と極小で、Public 表示に出ない可能性が高い。ただし完全な意味保存候補なので、単独で足切りと submit を確認する。

## 検証

- `neg_one_f` を削除した `task379.onnx` を作る。
- `src.evaluate.scorer.audit_one` で `task379` の完全一致と cost を確認する。
- `n_fail=0` かつ cost 減なら DVC に反映し、Kaggle submit する。
- 実 Public Score が上がれば採用、上がらなければ revert する。
