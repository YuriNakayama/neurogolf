# 20260703 case545 plan - safe micro bundle completion

Accepted Public Score:

- `7187.43`

## Background

Case544 showed that local-exact score-weight repairs can be hidden-unsafe:

```text
task192 8621 -> 6820
local n_fail=0
Public 7187.43 -> 7171.49
```

Task192 is now hard-risk. The next submit should avoid hidden-sensitive
semantic rewrites and prioritize exact low-risk material that is already
mechanical or alias-only.

Current best safe bundle from case544:

```text
task023 8548 -> 8476  gain 0.008459
task368 6225 -> 6217  gain 0.001286
task117 4148 -> 4145  gain 0.000724
task333 3227 -> 3225  gain 0.000620
task234 6845 -> 6841  gain 0.000585
total gain: 0.011673
```

This is below the `0.015` micro-bundle gate by about `0.00333`.

## Hypothesis

Completing a low-risk exact micro bundle is safer than another local-equivalence
semantic rewrite. The most useful next work is to find one or two additional
scorable non-risk exact micros that improve the max-5 bundle above `0.015`, or
to find a standalone scorable non-risk candidate that clears `0.020` without
changing learned-looking weights/threshold semantics.

## Method

1. Re-gate the current best max-5 bundle and candidate paths.
2. Search recent artifacts for exact non-risk scorable candidates with gain
   above the current fifth bundle member (`0.000585`), because only those can
   improve the top-5 total.
3. Search task023 for another same-family exact alias that can combine with
   `asg3 -> asgs3`, but require a fresh same-task combined gate.
4. Exclude hard/review/runtime-risk tasks, unscorable baselines, and known
   hidden-unsafe families.

## Submit Rule

Use normal pre-submit gate only.

Submit only if:

- standalone gain `>=0.020`, or
- micro bundle has at most five changed tasks and total gain `>=0.015`.

Do not use `--skip-pre-submit-gate`.
