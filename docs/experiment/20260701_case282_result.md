# 20260701 case282 result - reject task340 row/col guard aliases

## Result

- Target: `task340`
- Baseline: `params=136`, `memory=10858`, `cost=10994`, `n_fail=0`
- Candidate: `params=136`, `memory=10798`, `cost=10934`, `n_fail=0`
- Relative reduction: `10994 / 10934 = 1.0055`
- Submit ref: `54223200`
- Public Score: none
- Kaggle status: `SubmissionStatus.ERROR`
- Adoption: rejected

## Change Tested

Applied two exact local aliases:

```text
row_valid_a -> row_next
col_valid_a -> col_next
```

Local dead-pruning removed the corresponding guard branches.

## Verification

Individual local audit before submit:

```text
params=136
memory=10798
cost=10934
n_pass=266
n_fail=0
status=ok
```

## Decision

Rejected. The candidate passed local correctness and cost checks but Kaggle
returned `SubmissionStatus.ERROR`, so it is not a valid adoption under the real
submit gate. `data/output/onnx` was restored with DVC checkout to the case281
adopted state.

## Next Step

Do not retry this `task340` guard bundle without identifying the Kaggle runtime
failure. Continue with candidates from the remaining parallel scans, preferring
changes that do not alter guard predicates feeding indexing or shape-sensitive
paths.
