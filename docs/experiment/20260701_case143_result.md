# 2026-07-01 case143 result

## 結果

- 対象: `task093.onnx`
- 採否: 不採用
- submit: なし
- floor: Public Score `7181.16`

## baseline

```text
task093.onnx
params 103
memory 3456
cost 3559
points 16.822765114489805
n_pass 265
n_fail 0
filesize 3382
```

## 試行

`ReduceMax(w/x)` から作る存在フラグ `D/E` と、左右上下候補選択を単独で差し替えた。

- `L -> I`: n_fail 119
- `M -> J`: n_fail 114
- `N -> I`: n_fail 131
- `O -> J`: n_fail 131
- `ab -> j`: n_fail 133
- `ab -> g`: n_fail 132
- `ac -> j`: n_fail 132
- `ac -> g`: n_fail 133
- `ah -> X`: n_fail 132
- `ah -> Y`: n_fail 127
- `ap -> Z`: n_fail 133
- `ap -> aa`: n_fail 129

scalar default への置換は一部 ONNX Runtime の shape inference が崩れたため除外した。

## 結論

`task093` の `D/E` guard と `QLinearMatMul` 片側選択は、内部矩形の境界検出に必要だった。完全一致を保つ cost 減候補がないため、ONNX は変更しない。

次は `task119` / `task183` / `task046` など、同じく短い graph で `Where(..., zero)` が多い未探索 task を見る。
