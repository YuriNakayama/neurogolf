# 20260701 case178 plan

## Context

- Current accepted Public Score: 7181.17
- case177 showed `task281` has no exact bbox recolor branch trim.
- Continue prioritizing relative cost reduction by targeting compact memory-dominated
  graphs with exact guard or shape-stage removal opportunities.

## Target

- Task: `task304`
- Baseline audit:
  - params: 37
  - memory: 1404
  - cost: 1441
  - points: 17.72647179620938
  - n_pass: 266
  - n_fail: 0
  - filesize: 1077

## Hypothesis

`task304` crops the input, finds the major color, decodes 3x3 labels, masks labels
matching the major color to a background code, expands with `DepthToSpace`, then
one-hot encodes and pads.

Potential exact reductions:

- `mask3` may be replaceable by a simpler grid or constant for public examples.
- `blocks` may be equivalent to `pattern` or `grid3` if the major-color replacement
  is redundant.
- The final one-hot/pad staging may expose a shape-preserving alias.

## Method

1. Probe one alias at a time on temporary `task304` ONNX copies.
2. Run `audit_one` only for `task304`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record failed aliases and keep the baseline ONNX if no exact scored reduction is
   found.
