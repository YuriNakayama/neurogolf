# 20260703 case567 result - task148/task364 plus fresh target pivot

Accepted Public Score remains: `7187.57`

## Summary

Case567 started from the case566 conclusion: avoid exhausted task118/task187/
task379/task074/task076 repair families, then test task148 and do a short
task364 confirmation.

## task364 confirmation

The task364 short pass re-gated 115 scratch candidates and case548 repair paths.
No lower-cost exact candidate survived.

Best lower-cost rows still fail:

```text
task364_PA7_PB7_to_6.onnx                                  23013 -> 21253, gain=0.079561, n_fail=21
task364_runtime_equiv_alias_dce.onnx                        23013 -> 22133, gain=0.038990, n_fail=21
task364_PA7_to_PA6.onnx                                     23013 -> 22133, gain=0.038990, n_fail=21
task364_PB7_to_PB6.onnx                                     23013 -> 22133, gain=0.038990, n_fail=21
0245_task364_cost19493_gain0.166004_task364_PA7PB7_TO_PA5PB5.onnx 23013 -> 19493, gain=0.166004, n_fail=67
```

The only exact repair is:

```text
/private/tmp/neurogolf_agent_case548_C/task364_repaired_exact.onnx
23013 -> 23013
gain=0
n_fail=0
```

Conclusion: this family is exhausted because exactness requires restoring the
seventh propagation path, especially `PB7 = MaxPool(PB6 * Gu)`.

Details are in:

```text
docs/experiment/20260703_case567_task364_confirmation.md
```

## task148 re-gate and repair

The task148 worker staged 90 candidates from
`/private/tmp/neurogolf_agent_V3/` and re-gated them against current
`data/output/onnx`.

No lower-cost exact candidate survived:

```text
scanned: 90
submit-candidate: 0
bank-low-gain: 0
```

Best rows ranked by low `n_fail`, then gain:

```text
task148_alias_049_0_fill_top_pre_TO_source_fill_top_dce.onnx 6007 -> 5877 gain=0.021879 n_fail=17
task148_alias_048_0_source_fill_top_TO_source_right_fill_top_dce.onnx 6007 -> 5647 gain=0.061801 n_fail=125
task148_alias_027_0_source_left_fill_top_TO_c8_top_b_dce.onnx 6007 -> 5767 gain=0.040773 n_fail=125
task148_alias_025_1_left_of_marker_TO_c8_top_b_dce.onnx 6007 -> 5887 gain=0.020179 n_fail=125
```

The bounded repair targeted the `n_fail=17` row. The candidate had deleted the
`dest_top` term from:

```text
fill_top_pre = source_fill_top OR dest_top
fill_top = fill_top_pre AND col_valid
```

All 17 failures were missing color `8` on rows 7..9. Two repair candidates were
written under `/private/tmp/neurogolf_case567_task148/candidates`:

```text
task148_case567_full_restore_control.onnx 6007 -> 6007 gain=0.000000 n_fail=0
task148_case567_bottom3_restore.onnx      6007 -> 6019 gain=-0.001996 n_fail=0
```

The exact full repair restores baseline cost, while the narrower bottom-row
repair is exact but costs more than baseline because it needs extra slice/concat
guarding. Existing task148 cleanup/broadcast-compress artifacts also re-gated at
baseline cost only.

## Fresh target ranking

The fresh ranking worker found that task148 is not a top-five low-fail target:
current re-gate evidence has high-gain task148 entries mostly at
`n_fail=125..266`, with the lone `n_fail=17` row requiring baseline-cost repair.

Recommended next targets:

```text
1. task279: 4827 -> 4315, gain=0.112128, n_fail=2
2. task133: 33562 -> 31742, gain=0.055754, n_fail=5
3. task077: 14834 -> 14414, gain=0.028722, n_fail=4
4. task102: 2527 -> 2342, gain=0.076028, n_fail=10
5. task343: 1895 -> 1847, gain=0.025656, n_fail=13
```

Case567 therefore pivoted to a task279 repair worker.

## task279 repair

The task279 worker reproduced the high-gain near miss but did not find an exact
lower-cost repair:

```text
baseline task279.onnx:                                      4827, n_fail=0
reproduced_task279_bypass_Min_fl_1_TO_fl_0.onnx:             4315, gain=0.112128, n_fail=2
valid_k53_down2.onnx:                                       4321, gain=0.110738, n_fail=10
k53down_filter_Min_fl_0_blue.onnx:                          4577, gain=0.053181, n_fail=10
full restore reference:                                     4827, n_fail=0
```

The `4315` candidate deletes the second flood-extension pass and misses exactly
two cells:

```text
train[2] row=7 col=4: expected 8, candidate leaves 1
test[0]  row=7 col=7: expected 8, candidate leaves 1
```

Single-node directional repair without the blue clip overpaints broadly. A
larger one-pass kernel with the blue clip remains cheap but introduces false
positives in 10 arc-gen examples. One extra existing-tensor filter did not
remove those failures.

Detailed report:

```text
/private/tmp/neurogolf_case567_task279/candidates/final_report.json
```

## Decision

No submit. `task148`, `task364`, and `task279` all preserve cost only while
locally incorrect; their exact repairs restore baseline cost or worse.

Next action: start case568 with the remaining fresh-ranked targets:

```text
task133 correction-overlay bypass repair
task077 propagation-depth trim repair
```
