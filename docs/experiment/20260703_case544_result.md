# 20260703 case544 result - reject task192 score-weight repair

Accepted Public Score before submit:

- `7187.43`

## Summary

Rejected.

Kaggle submission:

```text
ref          : 54275901
description  : case544 task192 linsep score weights
status       : COMPLETE
Public Score : 7171.49
```

The isolated task192 repair regressed Public Score by `-15.94`, so it is not
adopted.

## Candidate

Path:

```text
/private/tmp/neurogolf_agent_case544_A/task192_linsep_score_weights_only.onnx
```

Local gate:

```text
task192 8621 -> 6820
gain    0.234342
n_fail  0
status  ok
decision submit-candidate
```

Individual local audit:

```text
params  = 105
memory  = 6715
cost    = 6820
points  = 16.17238524916249
n_pass  = 265
n_fail  = 0
status  = ok
```

## Repair Rationale

The case543 near-miss removed the `score == 12` vertical-gap guard:

```text
vgap_b    = Equal(score, twelve_f)
target_u8 = Where(vgap_b, bg_or_out_u8, pre_target_u8)
```

It wired the final output directly from `pre_target_u8`, causing `19` arc-gen
failures. Each failure was a single extra bridge/noise cell: one background
cell was replaced by a colored cell at the same coordinate.

The repair kept the low-cost 14-node graph and changed only `score_w` to a
channel-aware 3x3 float32 stencil scaled to the existing `9.5` threshold. This
fixed all local train/test/arc-gen examples without adding nodes.

## Decision

Reject. The repair is hidden-unsafe despite local exactness.

Task192 has been added to `HARD_RISK_TASKS`:

```text
case544 task192 score-weight repair passed local but regressed Public validation
```

Keep accepted Public Score at:

```text
7187.43
```

Do not adopt the task192 ONNX into `data/output/onnx`.

## Other Work

Task219/task255 repair triage produced no submit-ready candidates:

```text
task219 exact repair: 14823 -> 14823, no gain
task255 best bounded repair: 22541 -> 22511, n_fail=18, gain=0.001332
```

Banked exact bundle with task023 follow-up:

```text
task023 8548 -> 8476  gain 0.008459
task368 6225 -> 6217  gain 0.001286
task117 4148 -> 4145  gain 0.000724
task333 3227 -> 3225  gain 0.000620
task234 6845 -> 6841  gain 0.000585
total gain: 0.011673
```

This is still below the `0.015` micro-bundle gate, so no bundle submit was
attempted.
