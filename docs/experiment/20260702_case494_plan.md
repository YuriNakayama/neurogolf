# 20260702 case494 plan - submit task048 exp_7 cleanup

## Context

Current accepted Public Score:

```text
7186.79
```

Expanded worker W8 found a current-baseline `submit-candidate`:

```text
task048
/private/tmp/neurogolf_case406_scan/task048_093_BitwiseAnd_exp_7_TO_h1_4.onnx
1146 -> 842
gain=0.308252883
n_fail=0
```

## Target

```text
task048.onnx
```

## Hypothesis

The candidate removes an expensive redundant bitwise expansion path by aliasing
`exp_7` to `h1_4`. Worker gating reports no forbidden ops, no functions, and
`n_fail=0`. The relative gain is large enough to spend a submit immediately.

## Method

1. Re-gate the candidate in the main thread.
2. Replace only `data/output/onnx/task048.onnx`.
3. Run the changed-task gate and dry-run submit bundle check.
4. Submit to Kaggle and wait for Public Score.
5. Adopt only if Public Score improves above `7186.79`; otherwise restore the
   accepted baseline and record rejection.
