# 20260702 case488 plan - task014 current scratch cadence scan

## Context

Accepted Public Score is `7186.79` after case485. Case487 rejected the current
`task319` high-gain scratch pool because the top 220 positive candidates all
failed local correctness.

The next submit window should avoid saturated high-gain alias families and
look for a medium-cost task with a large scratch pool and possible relative
gain.

Current baseline:

```text
task014 cost=7985
```

Scratch pool:

```text
task014 candidates ~= 330
```

## Target

```text
task014
baseline: data/output/onnx/task014.onnx
task json: data/lake/neurogolf-2026/task014.json
```

## Method

1. Gather unique `task014*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against the current baseline.
3. Gate cost-reducing candidates with `submit.gate.evaluate_candidate_gate`.
4. Submit only if a candidate satisfies:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Bank exact low-gain candidates; otherwise record no-submit.
