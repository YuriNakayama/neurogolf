# 20260702 case502 result - adopt task363 corr merge repair

## Summary

- Target: `task363.onnx`
- Kaggle ref: `54265782`
- Decision: adopted

## Starting Near Miss

Worker scan found:

```text
task363_bypass_Add_corr_TO_corr1.onnx
4248 -> 3899
gain=0.085728174
n_fail=1
```

The failing pair was `train[0]`, with four extra color-2 cells. The sibling
`corr0` bypass failed only `train[1]`.

## Repair

Baseline used:

```text
corr0 = n2 * rm0
corr1 = f1 * rm1
corr  = corr0 + corr1
paint2 = paint - corr
```

The first merge attempt:

```text
corr = (n2 + f1) * (rm0 + rm1)
```

reduced cost but failed two local pairs because the inactive mask also removed
cells outside the current paint region.

Final repair:

```text
corr = paint * (n2 + f1) * (rm0 + rm1)
```

Then stale `value_info` entries for deleted `corr0` and `corr1` were removed.
Without this pruning the graph was exact but cost-neutral/worse; after pruning
the scorer stopped counting the deleted tensors.

Candidate:

```text
/tmp/neurogolf_case502_task363_corr_merge_paintgate_pruned.onnx
```

## Gate

```text
baseline cost : 4248
candidate cost: 4049
gain          : 0.04797834688961361
status        : ok
n_fail        : 0
functions     : 0
forbidden ops : []
decision      : submit-candidate
```

Dry-run pre-submit gate:

```text
changed: 1
total gain: 0.047978
decision: submit-bundle
```

## Public Score

```text
7187.12 -> 7187.17
```

## Conclusion

The repaired `task363` graph is Public-safe and improves the leaderboard. Keep
the updated `task363.onnx`.

## Follow-Up

The `task279` near misses were investigated in parallel. Their only exact
repair restores the removed flood extension and returns to baseline cost, so
they are not a useful next cycle unless a new cheaper extension mechanism is
found.
