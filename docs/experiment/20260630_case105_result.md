# 20260630 case105 result - t338 終端 zero_b / fill_4 削減は不採用

## 実装候補
`task338` の終端出力構築を 2 点検証した。

1. `zero_b = fill & out0` を static all-false initializer に置換し、dynamic `And` を削除する。
2. 最終候補 branch `fill_4` を落として `fill_0123` だけで出力する。

## faithful 検証
baseline:

- cost 17939, params 689, memory 17250, n_pass 267, n_fail 0

候補:

| 変更 | params | memory | cost | 結果 |
|---|---:|---:|---:|---|
| `zero_b` static all-false 化 | 1589 | 18150 | 19739 | `n_fail=0` だが悪化 |
| `fill_4` drop | 689 | 17250 | 17939 | `n_fail=75` |

## submit
- なし

## 採否
- **不採用**。
- `zero_b` は動的生成の 900B 中間だが、static initializer 化すると params が 900 増え、memory も scorer 上は下がらず cost 悪化。
- `fill_4` は arc-gen 上必要で、TopK=5 が必要という case79 の結論と整合する。

## 結論
t338 の現行終端 `Xor/And/Concat` は、見た目より score 上かなり詰まっている。ゼロ channel を initializer に逃がす方針は不可。

## 次の一手
- params-only / static-zero 置換ではなく、case100 のように node と intermediate を同時に消せる候補を探す。
- 次は `task173` / `task080` の template-stamping graph、または `task286` / `task018` の高 cost trace を再確認する。
