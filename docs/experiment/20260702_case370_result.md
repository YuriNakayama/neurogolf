# 20260702 case370 result - skip after small-grid probes

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.86
```

## task244 Primary Probe

Baseline:

```text
params=103
memory=1076
cost=1179
n_fail=0
```

The target was selected because small absolute savings can produce useful
relative gains on a low-cost task, and the graph contains a compact
`GridSample` renderer with float16 row/column grid intermediates.

Tested candidates:

| candidate | result | decision |
|---|---|---|
| geometry accumulation as int32 casts + int32 `Sum` | ORT rejects int32 `Sum` | reject |
| remove terminal `Pad` and output 4x4 sample | `n_fail=266` | reject |
| float16 pad value for float output | ORT type mismatch | reject |
| Slice start/end/axes int16/int8/uint8 | ORT rejects Slice index types | reject |
| all Slice indices int64 | exact but cost-neutral | reject |
| `GatherND`-style cell probe alternatives | not cheaper than current 1x1 `Slice` probes | reject |
| case3 surgery cleanup | `0/1` improved | reject |

No exact cost-reducing task244 candidate was found.

## task022 Side Probe

`task022` has a large `gray_idx` initializer:

```text
gray_idx int64[1,81,3]
```

It is regular: channel 5 over the 9x9 interior. Replacing
`GatherND(input, gray_idx)` with:

```text
Slice(input[:, 5:6, 1:10, 1:10]) -> Flatten
```

was locally exact but worse:

```text
2302 -> 2392
n_fail=0
```

The removed index table is outweighed by the extra 9x9 Slice/Flatten
intermediates, so this pattern is not useful here.

## task396 Parallel Probe

Baseline:

```text
params=1795
memory=6283
cost=8078
n_fail=0
```

Best exact scratch candidate:

```text
/tmp/neurogolf_case370_task396/terminal_plus_A1_B1_u8_iota.onnx
8078 -> 8074
gain = ln(8078 / 8074) = 0.000495
n_fail=0
```

This keeps the `GridSample` data path float/float16, then moves only the terminal
label/recolor path to uint8. Broader dtype rewrites are blocked:

- ORT rejects uint8 `GridSample`.
- ORT rejects uint8 `ReduceSum`.
- Tail-only uint8 without the scalar/table cleanup worsens cost.

The exact candidate is recorded in `micro_backlog.md` and not submitted.

## Conclusion

Skip the submit window. The only exact gain found in this cycle is far below the
low-risk submit threshold, and the stronger-looking task244/task022
representation rewrites either fail ORT constraints or increase memory.

Next action: search for another case369-style integer-valued patch core, but
exclude small `GridSample` graphs where the sampled tensor itself must remain
float and the tail is already near-minimal.
