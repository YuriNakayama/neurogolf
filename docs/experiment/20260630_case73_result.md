# 20260630 case73 result — bool復帰 Cast dtype 拡張候補は全て不採用（提出なし）

## independent_impl:
case72 で uint8 復帰 Cast を検証したため、残る dtype 拡張候補を probe:
- t148 `row_has_8_i`: `Cast(bool)->Cast(int32)` を元 `row_c8_f` へ直結。
- t280 `contig2_f16`: `Cast(bool)->Cast(float16)` を元 `in2_f` へ直結。
- t316 `scores`: `Cast(bool)->Cast(float16)` を元 `color_id` へ直結。

## faithful 検証
- t148: `Mul` が int32/float 型不一致で session_error。
- t280: `Sum` が float16/float 型不一致で session_error。
- t316: cost 426→397（-29）までは下がるが **n_fail=266**。

## 確定
- dtype 復帰 Cast は、後段 op の型制約または bool 正規化（0/1化）に必要。
- case71 の t349 以外に同系統の安全な除去候補は見つからず。
- floor 7180.81 維持。提出なし。

## 次の一手
- 型変換の横展開は一旦完了。次は高 cost task の trace から、単純な `Cast` ではなく
  `Where` の値入力・`Equal` 出力・`MaxPool` 出力の中間を graph output 直結または既存中間再利用で消せる箇所を探す。
