# case204 plan: task236 top-bottom equality selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task236` は未着手で、baseline は `cost=237` (`params=29`, `memory=208`, `n_fail=0`)。
- 4x4 の bool/uint8 patch を 1 つ削れれば、小型 task として相対改善が見込める。

## 対象

- `data/output/onnx/task236.onnx`
- top と bottom の 4x4 slice を `Equal(top, bottom)` し、`Where(same, black_vec, green_vec)` で 4-channel patch を作る。
- 最後に Pad して 30x30 出力へ戻す。

## 仮説

比較結果 `same` が片側固定、または `top` / `bottom` のどちらかだけで同等の判定ができれば、`Equal` や片側 Slice を削れる。短い graph なので固定化と片側 alias を 1 点ずつ確認する。

## 検証

- temp ONNX で `same -> true/false`、`same -> top/bottom` 系、`patch4 -> fixed patch` を probe する。
- `src.evaluate.scorer.audit_one` で `task236` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
