# 20260701 case323 result - task023 packed qlinear tail

## Submission

- Kaggle ref: `54231046`
- Public Score before: `7183.79`
- Public Score after: `7183.86`
- Decision: adopted

## Local Gate

```text
task023 baseline:  params=157 memory=11835 cost=11992 n_fail=0 status=ok
task023 candidate: params=80  memory=11142 cost=11222 n_fail=0 status=ok
```

Relative reduction:

```text
11992 / 11222 = 1.0686
ln(old/new) ~= 0.0664 pt
```

## Change

Replaced a sparse 10-channel small output with seven explicit zero-channel
positions:

```text
Pad(Sacc4, spad) -> Sg
Pad(Lmask, spad) -> Lg
Concat(black, zc, Lg, zc, zc, zc, zc, zc, Sg, zc) -> small
Pad(small, pads) -> output
```

with a three-channel packed tensor and one-hot `QLinearConv` projection:

```text
Concat(black, Lg, Sg) -> packed3
QLinearConv(packed3, one-hot 3ch->10ch weights, pads=final output pad) -> output
```

## Conclusion

The packed-channel tail pattern again produced a Public LB increase. The
relative-reduction ordering remains useful: this candidate had lower absolute
cost than many tasks but a strong `old/new` ratio.

## Next Step

Try the smaller exact packed-tail candidate for `task266`:

```text
cost 350 -> 334
old/new = 1.0479
n_fail=0
```
