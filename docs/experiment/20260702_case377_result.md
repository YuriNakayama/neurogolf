# 20260702 case377 result - skip stale cleanup batch

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.86
```

## Recheck

Selected subset:

```text
003, 023, 052, 066, 206, 222, 285, 325, 334, 363, 379
```

Command:

```text
uv run python -m pipeline.case3 surgery \
  --base /tmp/neurogolf_case377_one \
  --task-dir ../data/lake/neurogolf-2026 \
  --out /tmp/neurogolf_case377_surgery
```

Result:

```text
surgery done: 1/11 tasks improved
```

Only current exact improvement:

| task | current cost | candidate cost | gain | n_fail | path |
|---:|---:|---:|---:|---:|---|
| 285 | `24535` | `24512` | `0.000938` | 0 | `/tmp/neurogolf_case377_surgery/task285.onnx` |

The older task334/task052/task003 duplicate-initializer entries are stale for the
current accepted graph; the selected-subset surgery did not reproduce them.

## Gate Check

```text
current rechecked low-risk gain = 0.000938
submit threshold              = 0.030
```

## Conclusion

Skip submit. The actionable current cleanup batch is far below the low-risk
submit threshold. Keep task285 in the backlog, but do not spend a Kaggle submit
on it alone.

Next action: continue searching for fresh standalone candidates rather than
submitting stale micro backlog entries.
