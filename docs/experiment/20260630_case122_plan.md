# 2026-06-30 case122 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task201.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task201.onnx`
- baseline: params 138, memory 4476, cost 4614, n_pass 266, n_fail 0

## 仮説

`task201` は黄色矩形を除去して pattern を crop/orient し、左右色・角色・valid mask を合成している。`valid_spatial`、枠線色、反転 branch、または `pattern_full` 合成の一部が冗長なら、数百 byte 級の中間を削れて数 % の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で final valid mask、corner/side branch、orientation branch、rect mask を簡略化する。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task201.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
