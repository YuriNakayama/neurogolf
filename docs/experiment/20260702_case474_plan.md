# 20260702 case474 plan - reviewed five-task micro cadence bundle

## Context

Case473 added an explicit `--allow-micro-bundle-gate` path so exact
`bank-low-gain` changes can be submitted as a reviewed aggregate bundle without
weakening local correctness or known-risk gates.

The micro-bundle subagent identified a five-task Tier A/B bundle that avoids
case414 members, hard-risk tasks, and TopK/index runtime-risk families.

## Target Bundle

Use one current-baseline candidate per task:

```text
task213  /private/tmp/neurogolf_case410_alias_scan_200_400/task213_27_And_in0_h_valid_p2.onnx
task255  /private/tmp/neurogolf_case410_alias_scan_200_400/task255_29_Or_in0_c_top_center.onnx
task080  /private/tmp/neurogolf_fresh_014_140_case382/task080_bypass_Floor_bk30flr_TO_bk30f.onnx
task096  /private/tmp/neurogolf_fresh_014_140_case382/task096_bypass_Add_row_bottom_plus_one_TO_row_bottom.onnx
task009  /tmp/neurogolf_case400_nextcycle/task009_suffix_mask_pad_current.onnx
```

Expected aggregate relative gain:

```text
0.009227 + 0.008394 + 0.003841 + 0.002271 + 0.000481 ~= 0.0242
```

## Hypothesis

The bundle should pass only with the explicit micro lane:

```text
--allow-micro-bundle-gate
--micro-bundle-gain 0.015
--micro-bundle-max-tasks 5
decision=submit-micro-bundle
```

If any member is stale, local-failing, known-risk, or not a current-baseline
gain, reject locally and do not submit.

## Method

1. Copy the current accepted ONNX directory to a scratch candidate directory.
2. Replace only the five target task files.
3. Run bundle gate against `data/output/onnx`.
4. If the gate returns `submit-micro-bundle`, run submit CLI dry-run.
5. Submit to Kaggle and adopt only if Public Score improves; otherwise restore
   the accepted baseline task files.
