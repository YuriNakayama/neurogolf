# 20260703 case550 result - adopted task066 broadcast pad

Accepted Public Score:

- before: `7187.48`
- after: `7187.51`
- delta: `+0.03`

## Target

Task:

- `task066`

Candidate:

```text
/private/tmp/neurogolf_agent_case550_A/task066_broadcast_pad.onnx
```

Local gate:

```text
task066 16268 -> 15866
gain    0.025022
n_fail  0
status  ok
functions 0
forbidden ops []
decision submit-candidate
```

## Change

The candidate removes the final `Unsqueeze` before `Pad`. The downstream
`Where` broadcasts the `[1,30,30]` hidden mask over the 10 color channels,
saving the `20x20` boolean intermediate and two pad parameters while preserving
exact behavior.

This is a structural graph simplification, not a broad alias that drops task
logic.

## Submission

Full 400-task dry-run:

```text
changed: 1
task066 16268 -> 15866
total gain 0.025022
decision submit-bundle
```

Kaggle submit:

```text
case550 full task066 broadcast pad
SubmissionStatus.COMPLETE  7187.51
```

## Decision

Adopt. Public Score improved from `7187.48` to `7187.51`.

The accepted ONNX was copied to:

```text
data/output/onnx/task066.onnx
```

Post-adoption self gate:

```text
task066 15866 -> 15866
n_fail  0
status  ok
functions 0
forbidden ops []
```

## Next Step

Continue the same submit cadence. While this submission was pending, the next
parallel worker started looking at non-risk structural targets:

```text
task025, task005, task208, task333, task361, task020
```

Use its result for the next cycle, but re-gate against the updated baseline
because `task066` is now adopted.
