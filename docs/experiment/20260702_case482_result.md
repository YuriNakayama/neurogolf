# 20260702 case482 result - no-submit task089 current scratch re-gate

## Decision

No submit.

Accepted Public Score remains:

```text
7186.73
```

## Baseline

```text
task089.onnx
cost=7027
params=75
memory=6952
status=ok
```

## Scan

Existing `task089*.onnx` scratch candidates from `/tmp` and `/private/tmp`:

```text
candidates=218
positive cost-only candidates=143
```

All positive candidates were gated; none were exact:

```text
submit-candidate=0
bank-low-gain=0
```

Representative failures:

```text
7027 -> 5011  gain=0.338124  n_fail=182  blocked-local-fail
7027 -> 5176  gain=0.305727  n_fail=174  blocked-local-fail
7027 -> 6191  gain=0.126663  n_fail=174  blocked-local-fail
7027 -> 6689  gain=0.049295  n_fail=174-206  blocked-local-fail
7027 -> 6858  gain=0.024344  n_fail=14-267   blocked-local-fail
7027 -> 6994  gain=0.004707  n_fail=174     blocked-local-fail
7027 -> 6998  gain=0.004135  n_fail=173     blocked-local-fail
```

## Conclusion

Reject locally. The current `task089` scratch pool has large cost-only deltas,
but every cost-reducing candidate breaks correctness. This explains the lack of
a Kaggle submit in this cycle: there was no `n_fail=0` candidate to spend a
submit on.

The next cadence should keep parallel exploration running while only submitting
when a candidate clears the pre-submit gate. For the user's requested
`72 submits / 13 hours` rate, the operational target is a submit decision window
of roughly `10.8` minutes, but a window should still be recorded as no-submit
when the gated candidate queue is empty.
