# 20260702 case420 result - dense structural compression search

## Summary

No submit.

Accepted Public Score remains `7186.71`. The post-regression search focused on
Public-backed structural families, but no single-task candidate cleared the new
gate.

## Checks

### task222 crop-at-source recheck

`task222` has a sparse `17x17` Conv kernel, but the scorer already treats this
graph as a crop-at-source design:

```text
baseline: params=2919 memory=4676 cost=7595 n_fail=0
```

Two exact candidates were built and gated:

| candidate | cost | gain | local result | decision |
|---|---:|---:|---|---|
| crop Conv to `1x1`, then Slice output window | `8331` | `-0.092493` | `n_fail=0` | blocked-no-gain |
| Slice input window, then crop Conv to `1x1` | `12571` | `-0.503902` | `n_fail=0` | blocked-no-gain |

Both reduce parameters but add float intermediate memory, so total cost gets
worse. This confirms the earlier case372 conclusion that `task222` is already
at the crop-at-source floor.

### task367 dense kernel follow-up

Current graph:

```text
task367: params=3899 memory=16800 cost=20699 n_fail=0
```

Inspection:

- `rect_w` is already cropped to `(25, 2, 8, 8)`;
- `fill_w` is `(1, 25, 5, 5)`;
- both kernels have full spatial support;
- no duplicate `(rect_w channel, fill_w channel, rect_b)` groups remain.

No safe follow-up crop or duplicate-channel compression was found.

### task335 selector follow-up

Current graph:

```text
task335: params=112 memory=2840 cost=2952 n_fail=0
```

The accepted selector rewrite and duplicate cleanup have already harvested the
obvious surface. Remaining initializers are small (`row_sel=20`,
`pool_cols=30`, coordinates/padding/scalars), and no `gain >= 0.020`
single-task change was identified.

### targeted current-baseline surgery

Ran surgery on only the current shortlist:

```text
task367 task335 task319 task285 task158
```

Command:

```text
uv run python -m pipeline.case3 surgery \
  --base /private/tmp/neurogolf_case420_surgery_in \
  --out /private/tmp/neurogolf_case420_surgery \
  --task-dir ../data/lake/neurogolf-2026
```

Result:

```text
surgery done: 0/5 tasks improved, local points delta +0.00
```

### task110 near-miss

Rechecked the prior repaired-near-miss trail from case365. It remains a poor
target for this cycle:

- the documented high-gain alias is `n_fail=1`;
- the smallest exact repair was already documented as worse than baseline;
- old scratch candidates generated many runtime failures and did not provide a
  clean gate-clearing candidate.

## Decision

Do not submit. This cycle produced no exact `submit-candidate`.

## Next Step

Shift away from already-harvested dense crops. The next cycle should target a
fresh same-task value-path or repaired-near-miss candidate with small failure
set and a cheap existing guard, rather than repeating generic surgery or stale
scratch scans.
