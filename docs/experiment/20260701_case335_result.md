# 20260701 case335 result - task209 uniform-branch MaxPool alias pair

## Decision

Adopted.

## Local Gate

Only `task209.onnx` was changed and audited.

```text
baseline:  params=1379 memory=28104 cost=29483 n_fail=0 status=ok
candidate: params=1379 memory=27304 cost=28683 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(29483 / 28683) ~= 0.0275 pt
```

## Submission

```text
ref=54233396
description=case335 task209 maxpool label alias cost 29483 to 28683
status=COMPLETE
publicScore=7185.01
```

Previous accepted floor:

```text
7184.98
```

The Public Score improved by `+0.03`, so the candidate is accepted.

## Diff Summary

The accepted change rewires `max_s3` and `max_s4` consumers to `label_f`, then
prunes the redundant uniform-branch MaxPool outputs. Larger nearby aliases were
left untouched because they failed local correctness.

## Next Step

The remaining exact candidates in `micro_backlog.md` are below the recent
standalone threshold. Start a fresh structural scan for new high-relative
reducers, while keeping one-byte and small initializer cleanups available for
future bundles.
