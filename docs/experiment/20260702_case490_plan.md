# 20260702 case490 plan - task187 cadence scan

## Context

Current accepted Public Score:

```text
7186.79
```

Recent no-submit cycles show many high-ratio aliases failing the mandatory
`n_fail=0` gate. The next window should keep the 15-minute cadence as a
submit-opportunity cadence, not a forced-submit rule.

## Target

```text
task187.onnx
```

Reason:

- it has a sizeable existing scratch pool;
- it is not assigned to the current replacement workers;
- it is outside the recently exhausted `task014/task138/task198/task243/task319`
  scans;
- relative-cost reductions are the ranking criterion.

## Method

1. Gather unique `task187*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Measure baseline and candidate costs with `evaluate.audit_one`.
3. Rank cost-positive candidates by `ln(old_cost / new_cost)`.
4. Gate ranked positives with `submit.gate.evaluate_candidate_gate`.
5. Submit only if a candidate is `submit-candidate`; otherwise record
   no-submit and continue.

## Parallel Work

Workers continue searching:

- task138 independent sweep;
- task054/task066/task076/task077;
- task101-200 fresh ratio targets;
- task005/task048/task018;
- task008/task013/task029/task041/task051/task099.
