# 20260702 case436 plan - task162 source-slice probe

## Context

Case435 closed `task131` because `ScatterND` rejects int32 indices. The next
fresh-scan candidate is:

```text
task162 params=44 memory=4024 cost=4068 n_fail=0
```

Dominant tensors:

```text
c0_20  float [1,1,20,20] = 1600B
c0_u8  uint8 [1,1,20,20] = 400B
```

Older progress notes say this graph is already near-optimal, with the 1600B
float input slice as the lower-bound entry surface.

## Hypothesis

If `QLinearConv` can consume a narrower or already-uint8 tensor without
materializing `c0_20`, cost can drop sharply. If the float slice is required by
runtime type conversion from the official one-hot float input, no exact
candidate is expected.

## Method

1. Confirm current graph structure and baseline.
2. Test only narrow variants:
   - bypass `c0_20` directly into `QLinearConv` if accepted;
   - change source slice to channel 0 only only if shapes remain valid.
3. Run `submit gate`.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
