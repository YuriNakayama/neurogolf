# 20260702 case465 result - task096 selector path re-gate

## Summary

No submit.

`task096` produced four exact positive candidates, but all are below the
standalone submit threshold. The larger selector/color/radius bypasses failed
local correctness.

## Baseline

```text
task096
params=805
memory=8013
cost=8818
n_fail=0
status=ok
```

## Gate Result

```text
candidates=93
positive_cost_reductions=72
errors=0
exact_positive=4
blocked=68
exceptions=0
```

Best blocked reductions:

```text
8818 -> 8043  gain=0.091993  n_fail=266
  task096_bypass_d4_107_Where_1

8818 -> 8697  gain=0.013817  n_fail=186..266
  color_grid -> color_grid_in_box / color_grid_fg

8818 -> 8723  gain=0.010832  n_fail=266
  row_in_span guard aliases

8818 -> 8773  gain=0.005116  n_fail=266
  zero_color_by_radius broadcast aliases
```

Exact bank-only candidates:

```text
8818 -> 8798  gain=0.002271  decision=bank-low-gain
  Add_row_bottom_plus_one -> row_bottom
  Sub_row_radius_num -> row_radius_num_plus_one
  Sub_row_top_minus_one -> row_top

8818 -> 8813  gain=0.000567  decision=bank-low-gain
  Where_color_updates -> color_ids_u8
```

These exact candidates were added to `docs/experiment/micro_backlog.md`.

## Decision

Rejected for submission. No candidate met:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

The exact row-scalar aliases are useful backlog material, but submitting them
standalone would repeat the recent no-move micro pattern.

## Next

Use the subagent queue instead of continuing broad stale pools:

1. `task066`: prior isolated same-task aliases moved Public and the current
   graph may still have compatible score/edge rewrites.
2. `task145`: high cost, but avoid old branch/source aliases; inspect only
   selector/table or dtype-boundary reductions.
3. `task157`: proven shape-compatible savings history; avoid bool `Pad`.
