# 20260701 case189 result

## Summary

- Target: `task226`
- Baseline: params 20, memory 1613, cost 1633, points 17.601825907029536
- Baseline correctness: n_pass 133, n_fail 0
- Decision: reject, no submit

## Probes

All probes were run on temporary `task226` ONNX copies and audited only with
`src.evaluate.scorer.audit_one`.

`ch4` alias probes:

- `ch4 -> ch0`: cost 1533, n_fail 133
- `ch4 -> ch1`: cost 1533, n_fail 133
- `ch4 -> ch2`: cost 1533, n_fail 133
- `ch4 -> ch3`: cost 1533, n_fail 133
- `ch4 -> ch5`: cost 1533, n_fail 133
- `ch4 -> ch12`: cost 1533, n_fail 133
- `ch4 -> ch35`: cost 1533, n_fail 133

## Analysis

`ch4 = Xor(ch1, ch1)` is a true full false plane, but no existing full-shape
boolean plane is also false for all public examples. A scalar false tensor `z`
exists, but it cannot be used directly in the channel `Concat` because its
height/width dimensions are `1x1`, not `10x10`. Replacing `ch4` with a new
10x10 false initializer would trade 100 bytes of memory for 100 params, which
is score-neutral and not worth submitting.

## Conclusion

No exact cost-reducing variant was found. The baseline ONNX was unchanged and no
Kaggle submission was made.

## Next

Continue looking for removable full-shape intermediates where an existing tensor
is provably equivalent, or where graph restructuring avoids creating the tensor
without adding matching params.
