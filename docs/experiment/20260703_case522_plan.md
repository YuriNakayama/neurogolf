# 20260703 case522 plan - task011 structural repair/read

## Context

Recent repair attempts:

```text
case519 task243: high-upside but exact repair loses savings
case521 task033: gain>0.020 near miss, but threshold leaves room for only one op
```

case520 V3 found task011 near misses:

```text
task011 2083 -> 2037
gain=0.022331
n_fail=21
candidate=/tmp/neurogolf_agent_V3/task011_alias_018_4_noise_sum_4_TO_noise_sum_0_dce.onnx
candidate=/tmp/neurogolf_agent_V3/task011_alias_018_3_noise_sum_3_TO_noise_sum_0_dce.onnx
```

## Hypothesis

task011 is small enough to inspect manually. The near miss aliases one
`noise_sum` branch. If the branch is redundant only for common cases, a slightly
different branch alias or local simplification may retain `gain >= 0.020` while
fixing correctness.

## Plan

1. Inspect task011 ONNX graph and examples.
2. Compare baseline and near-miss failures.
3. Try local repairs or alternative aliases under `/tmp`.
4. Gate against current baseline.
5. Submit only if standalone `submit-candidate`.
