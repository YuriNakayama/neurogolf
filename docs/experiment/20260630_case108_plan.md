# 20260630 case108 plan - t018 第二 MaxPool 鎖の短縮再確認

## 現状
- floor/Public: 7180.95
- 対象: `task018`
- baseline faithful: cost 44197, params 2087, memory 42110, n_fail 0

## 背景
case84 で第一 5x5 `MaxPool` 鎖の末端 `safe_name_130` を `safe_name_124` にバイパスし、cost 45211→44197、Public 7180.87 を採用した。

現行 graph にはもう一つ 5x5 `MaxPool` 鎖が残る。

```text
safe_name_617 -> 619 -> 620 -> 621 -> 622 -> 623 -> 624 -> 625 -> 626 -> 627 -> 628 -> 629
```

case84 には第二 crop 側は不採用とあるが、詳細が薄いため、現行 graph 上で末端短縮を再確認する。

## 仮説
第二鎖の後段に余剰が残っていれば、case84 と同様に 5x5 pool+後続中間を削れて memory が落ちる。

## 手法
1. 第二鎖の最終利用先を特定する。
2. その入力を `safe_name_627` / `safe_name_625` / `safe_name_623` / `safe_name_621` / `safe_name_619` へ順にバイパスする一時候補を作る。
3. `src.evaluate.scorer.audit_one` で `task018` の完全一致と cost を確認する。
4. `n_fail=0` かつ cost 減なら実ファイルに適用して submit する。

## 足切り
- 変更対象は `task018` のみ。
- 全数 `audit_dir` は使わず、`audit_one` のみ行う。
