# 2026-07-01 case132 result

## 対象

- `task014.onnx`
- baseline: cost 7985, score 16.015171972880673

## 実施内容

`task014` の rarest-color bbox crop graph について、重複に見える終端側 `ArgMax`、crop index の clip、rect mask、inside/class/pad sentinel を `/tmp` の候補 ONNX で個別に差し替えた。

## 足切り結果

baseline:

```text
cost 7985
```

主な候補:

- `bad_b -> absent_b`: cost 7965, n_fail 4
- `adj -> counts`: cost 7913, n_fail 266
- `r1i -> r0i`: cost 7973, n_fail 266
- `c1i -> c0i`: cost 7973, n_fail 266
- `Hlen -> one_i`: cost 7965, n_fail 266
- `Wlen -> one_i`: cost 7965, n_fail 266
- `ridx -> rraw`: cost 7917, n_fail 34
- `cidx -> craw`: cost 7913, n_fail 47
- `rect -> shifted`: cost 7568, n_fail 262
- `class18 -> inside`: cost 7262, n_fail 266
- `inside -> Bu8`: cost 5638, n_fail 262
- `Pad` の埋め値 `out_u8 -> zero_u8` / `Bu8`: n_fail 266

固定 crop サイズも確認したが、266 pair の期待出力最大が `17x18` で、現行の `idxr=17` / `idxc=18` は削れない。

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

`task014` は現行実装が実データの最大 crop と一致しており、周辺の guard / sentinel / clip は小さく見えても完全一致に必要だった。

## 次の一手

`task014` は near-minimal と判断し、次は `task182` を見る。`task182` は Conv/AveragePool/QLinearConv/MaxPool の中間が大きく、kernel 生成や dilation 閾値まわりに相対削減余地があるかを確認する。
