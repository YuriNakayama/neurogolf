# 2026-06-30 case112 result

## 実 LB

- submit ref: `54193878`
- Public Score: **7180.97**
- floor: **7180.97**
- 採否: **不採用**（実 LB 上昇なし）

## 対象

- `task090.onnx`
- baseline: params 82, memory 3009, cost 3091, n_pass 267, n_fail 0

## 試行

全 ONNX を単純な値保存候補で走査した。

- `task090`: `Where(cond, zero_u8, zero_u8)` は削減可能。
- `task165`: `Add(seed_row, zero_col)` は broadcast rank を作る用途で、単純 bypass は invalid。
- `task387`: `Add(top_i64, top_pair_zeros)` は rank/shape 拡張用途で、単純 bypass は invalid。

`task090` の node 334:

```text
Where i1_best_row_start ['i1_area_better', 'zero_u8', 'zero_u8'] -> ['i1_best_row_start']
```

後続入力を `zero_u8` に差し替えて prune。

| 変更 | nodes | params | memory | cost | n_pass | n_fail |
|---|---:|---:|---:|---:|---:|---:|
| same-value `Where` 削除 | 447 | 82 | 3008 | 3090 | 267 | 0 |

## 提出と採否

- message: `case112 t090 remove same-value Where cost 3091 to 3090`
- Public Score は **7180.97** で floor から変化なし。
- 目標ルール通り不採用。
- `data/output/onnx/task090.onnx` と `data/output/onnx.dvc` は baseline に復元済み。

## 結論

機械的な値保存 surgery の取り残しはほぼなく、今回見つかった安全候補も cost 1 のみで Public に反映されなかった。`+0` 系でも shape/rank broadcast 用の Add は削除できない。

## 次の一手

- 単純 identity sweep は枯渇。
- 次は高 cost タスクの構造依存候補に戻り、100B 以上の memory 中間を消せる可能性がある箇所を探す。
