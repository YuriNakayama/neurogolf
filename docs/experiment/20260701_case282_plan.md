# 20260701 case282 plan - task340 row/col guard aliases

## Context

- Current adopted Public Score floor: `7181.94`
- Latest adopted cycle: case281 `task191` column score alias (`14969 -> 14854`)
- Candidate source: parallel explorer scan over small/mid-cost tasks.
- Priority rule: choose exact candidates by relative reduction.

## Target

- `data/output/onnx/task340.onnx`

## Baseline

Current local audit:

```text
params=136
memory=10858
cost=10994
n_fail=0
```

## Hypothesis

`task340` has row/column guard tensors that are equivalent to already available
next-index predicates on all valid examples:

```text
row_valid_a -> row_next
col_valid_a -> col_next
```

Replacing both should remove two `Or` guard branches while keeping the final
output exact.

## Expected Gain

- Baseline: `10994`
- Candidate from temporary audit: `10934`
- Relative reduction: `10994 / 10934 = 1.0055`
- Expected point gain: `ln(10994 / 10934) ~= 0.0055`

This is smaller than case281 but is the strongest exact candidate currently
returned by the parallel scans.

## Verification Plan

1. Apply only the two aliases above.
2. Dead-prune unused nodes and initializers.
3. Run individual `audit_one` for `task340` with
   `data/lake/neurogolf-2026/task340.json`.
4. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
