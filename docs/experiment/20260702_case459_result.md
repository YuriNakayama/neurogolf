# 20260702 case459 result - near-miss repair triage

## Summary

No submit and no ONNX change.

The three reviewed near misses are not good next-cycle targets. In each case,
the local failures correspond to exactly the branch or propagation layer being
deleted, and known repairs either fail or erase the cost saving.

## task077

Current baseline:

```text
cost=14834
n_fail=0
```

Known current-baseline near misses:

```text
b2mn -> b1mn              14834 -> 13994  gain=0.058293  n_fail=4
b2mx -> b1mx              14834 -> 13994  gain=0.058293  n_fail=4
b2mn/b2mx -> b1mn/b1mx    14834 -> 13154  gain=0.120300  n_fail=8
```

Case365 already concluded that the second propagation is required for sparse
color-4 fills. Cheap widening or threshold-bias repairs either underfill or
overfill. This is not a narrow exception repair.

Decision: reject as next target.

## task110

Subagent baseline audit:

```text
params=632
memory=12959
cost=13591
n_pass=266
n_fail=0
status=ok
```

Best current near miss:

```text
public_row_table -> row9_public_table
13591 -> 13069
gain=0.039165
n_fail=1
```

The single failing train example needs the full row7 public table. The smallest
known exact repair restores the needed selector/table path and costs `13592`,
which is one byte worse than baseline. Larger stale aliases also fail, including
`task110_selected_row_table_TO_public_row_table.onnx` at `11926` with
`n_fail=264`.

Decision: reject as next target.

## task187

Subagent baseline audit:

```text
params=1008
memory=26525
cost=27533
n_pass=266
n_fail=0
status=ok
```

Best near miss:

```text
/private/tmp/neurogolf_case356_struct/task187_reach12.onnx
27533 -> 26283
gain=0.046463
n_fail=4
```

The failures are exactly final exterior-frontier cells left as color `2` where
the baseline makes them color `3` (`train[1]`, `arc-gen[42]`, `arc-gen[95]`,
`arc-gen[108]`). The known exact substitute restores the final frontier logic
but remains `cost=27533`, so it is cost-neutral.

Decision: reject as next target.

## Conclusion

Do not spend another cycle on `task077`, `task110`, or `task187` unless a new
repair mechanism is found. The next search should move to a fresh single task
where the deleted component is redundant across all examples, not merely absent
from most examples.

Practical next direction:

- rank normal-risk tasks not recently closed by current cost and fanout;
- inspect one graph manually for duplicated selector/table families;
- avoid old broad alias pools unless a candidate already has `n_fail=0`.
