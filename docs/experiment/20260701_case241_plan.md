# case241 plan: task023 sparse QLinearConv kernel crop

## 現状
- 採用済み Public Score floor: 7181.25
- `task023` baseline は `cost=13130` (`params=287`, `memory=12843`, `n_fail=0`)。
- `Ksb/Ksc/Khb/Khc/Kvb/Kvc` はすべて 5x5 の sparse `uint8` kernel で、外周ゼロが多い。
- 同じ kernel が多数の `QLinearConv` で再利用されるため、initializer crop だけで params をまとめて削れる可能性がある。

## 対象
- `data/output/onnx/task023.onnx`

## 仮説
各 kernel の非ゼロ bbox に合わせて crop し、対応する `QLinearConv` の `kernel_shape` と `pads` を `old_pad - cropped_border` で補正すれば、出力 shape と畳み込み位置を保ったまま完全一致できる。memory はほぼ据え置きでも params が約100以上減る見込み。

## 検証
- 6個の kernel を個別/一括で crop した一時 ONNX を作る。
- 対応する全 `QLinearConv` の `kernel_shape` / `pads` を更新する。
- `src.evaluate.scorer.audit_one` で `task023` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost 改善が出た場合のみ実 ONNX へ反映し submit する。
