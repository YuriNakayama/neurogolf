# 20260630 case48 plan — flood-fill テンプレートを正答化し shallow タスク(t251)へ転用、cost最小化

## 仮説
case47 で t002 flood-fill の seed バグ(ext pad=1 が必要)を特定。完全正答テンプレートを作り、
depth の浅い t251(depth7, floor4920)に転用。crop技法+単channel処理+op/iter削減で floor 未満を狙う。

## 手法
- ext を pad=1 で dilation → 正しい border seed。MaxPool(8conn=4connと一致)で伝播。
- crop[S=12]、単channel [1,1,12,12] 処理、channel-pad で 10ch 復元。op/iter を MaxPool+Mul の2opへ。

## 採否
- faithful n_fail=0 かつ cost<4920 かつ 実LB>7180.78 で採用。退行 revert。
