# 20260701 case302 plan - task056 bool terminal Pad probe

## Context

- Current Public Score floor: `7183.04`.
- case301 found no static cleanup or source-crop candidate with
  `old_cost / new_cost >= 1.02`.
- Because the metric is logarithmic, a low-cost task can still be valuable if
  its relative reduction is large.

## Target

- `task056`
- Baseline audit:
  - `params=0`
  - `memory=34`
  - `cost=34`
  - `n_pass=46`
  - `n_fail=0`

## Hypothesis

The graph builds six active bool channels, casts them to float16, then pads to
the full canvas:

```text
active_bool     = Concat(h1, h2, h3, false_bool, false_bool, h6)
active_channels = Cast(active_bool -> float16)
output          = Pad(active_channels)
```

Since the official correctness check thresholds `output > 0`, a bool output
would be semantically equivalent.  If `Pad(active_bool)` is valid, we can remove
the float16 Cast and its `active_channels` intermediate.

Expected best case:

- remove `active_channels [1,6,1,1]` float16 memory: `-12`;
- keep output uncounted;
- possible `cost 34 -> 22` if no new params are needed.

## Probe

Build temporary candidates only:

1. opset 9 `Pad(active_bool)` using the existing attribute-form Pad;
2. opset 10 `Pad(active_bool)` using the existing attribute-form Pad;
3. opset 11 `Pad(active_bool, pads)` with the required input-form Pad.

Submit only if `audit_one` returns `status=ok`, `n_fail=0`, and net cost is
below baseline.
