# 20260702 case443 plan - task039 dynamic crop probe

## Context

Accepted Public Score remains `7186.71`. The fresh current-graph ranking found
`task039` as a very high relative-gain target because one 3x3 float crop
dominates the small cost:

```text
params=56
memory=424
cost=480
status=ok
```

Current graph:

```text
GatherND(input, feature_indices) -> features
MatMul(features, pattern_weights) -> pattern_f
Cast(pattern_f) -> pattern
Gather(start_lookup, pattern) -> starts
Add(starts, three) -> ends
Slice(input, starts, ends, grid_axes) -> crop_f FLOAT [1,10,3,3]
Pad(crop_f, pad_pads) -> output
```

The `crop_f` tensor is 360 bytes, so any removal would be a large relative
reduction. The risk is that it is the direct value tensor for the final output,
so most apparent removals will be semantic failures.

## Hypothesis

`task039` is likely already near-minimal, but existing scratch candidates include
small dynamic-start aliases and cleanup variants. Re-gating them against the
current baseline is cheap and may find a bankable or submit-worthy exact
candidate.

## Method

1. Re-gate existing `task039` candidates from the case385/case406/case407/case415
   and cleanup scratch directories.
2. Treat any direct `Slice`/`Pad` bypass as high semantic risk unless it is
   locally exact.
3. Submit only if a current-baseline candidate is exact and clears the
   post-regression gate.
