# 20260702 case487 plan - task319 current scratch cadence scan

## Context

Accepted Public Score is `7186.79` after case485. The old `task023` residuals
were closed in case486 as no-gain against the new baseline.

`task319` is a mid/high-cost task with prior Kaggle-valid isolated evidence and
a large scratch pool.

Current baseline:

```text
task319 cost=19080
```

## Target

```text
task319
baseline: data/output/onnx/task319.onnx
task json: data/lake/neurogolf-2026/task319.json
```

## Method

1. Gather unique `task319*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against the current baseline.
3. Gate cost-reducing candidates with `submit.gate.evaluate_candidate_gate`.
4. Submit only if a candidate satisfies:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Otherwise record no-submit and continue parallel exploration.
