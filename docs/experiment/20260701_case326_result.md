# 20260701 case326 result - task325 packed bool tail

## Submission

- Kaggle ref: `54231491`
- Public Score before: `7184.05`
- Public Score after: `7184.15`
- Decision: adopted

## Local Gate

```text
task325 baseline:  params=123 memory=2010 cost=2133 n_fail=0 status=ok
task325 candidate: params=102 memory=1830 cost=1932 n_fail=0 status=ok
```

Relative reduction:

```text
2133 / 1932 = 1.1040
ln(old/new) ~= 0.0989 pt
```

## Change

Replaced the sparse bool tail:

```text
Concat(bg, zero6, zero6, zero6, zero6, zero6, zero6, zero6, diag) -> compact
Pad(compact, pads) -> output
```

with a packed live-channel projection:

```text
Concat(bg, diag) -> compact_packed
Cast(compact_packed) -> compact_packed_u8
QLinearConv(compact_packed_u8, one-hot 2ch->10ch weights, pads=final output pad)
  -> output
```

The projection maps `bg` to output channel 0 and `diag` to output channel 8.

## Conclusion

The bool packed-tail pattern adopted in case325 generalized cleanly to this
task and moved Public LB again. Keep using measured local exact candidates; do
not use rough estimates alone because projection params can erase gains on
small graphs.

## Next Step

Try the remaining lower-ratio validated sparse-tail candidates:

1. `task250`: `3218 -> 3142`
2. `task365`: `4028 -> 3945`
