# 2026-07-01 case150 plan

## 方針

- floor: Public Score 7181.16
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task379.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task379.onnx`
- baseline: params 1274, memory 9540, cost 10814, points 15.71140313005805, n_pass 266, n_fail 0

## 仮説

`task379` は row/col の line を検出し、`h_any` で水平向きへ正規化して 20x20 の base/stamp/channel2 mask を作る。最後に `h_any` で transpose を戻して 30x30 one-hot にする。

実データで向きや stamp / channel2 branch が限定的なら、`h_any` 周辺または final color overlay を簡約できる可能性がある。cost は 10814 で、20x20 mask や Conv branch を削れれば LB に反映する可能性がある。

## 手順

1. `/tmp` 上の candidate ONNX で `h_any` 正規化 branch と transpose 戻しを片側へ差し替える。
2. `stamp_c` / `channel2_c` / `active_rows_c` / `active_cols` の color overlay を個別に差し替える。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task379.onnx` に反映する。
4. submit し、Public Score が 7181.16 を上回れば採用、上がらなければ破棄する。
