# 20260702 case358 plan - task358 area formula compression

## Context

Accepted Public Score is `7185.68` after case356. case357 was skipped because
the accumulated exact micro backlog was below the submit-saving threshold.

Franklin found a standalone low-risk task358 candidate:

```text
task358 baseline:  params=174 memory=4312 cost=4486 n_fail=0
task358 candidate: params=150 memory=4242 cost=4392 n_fail=0
gain:              ln(4486 / 4392) = 0.021177
```

This clears the standalone low-risk gate `>= 0.020`, so do not bundle the
case357 micro backlog into this submit.

## Hypothesis

The current task358 graph uses an area lookup/dim-case branch for simple
coordinate bounds. Replacing that lookup with compact `Sqrt` plus `Floor/Ceil`
formula logic and fp16 coordinate compares preserves all local examples while
removing small lookup/table and dtype overhead.

## Implementation

Replace only:

```text
data/output/onnx/task358.onnx
```

with:

```text
/tmp/neurogolf_case358_explorer_b/task358_area_sqrt_fp16_floorceil_coords.onnx
```

## Gate

Run `evaluate.audit_one` for task358 only:

```text
n_pass=265
n_fail=0
cost=4392
```

Candidate ops:

```text
Add, And, ArgMax, Cast, Ceil, Einsum, Equal, Floor, GatherElements, Less,
Mod, Pad, ReduceMax, ReduceSum, Sqrt, Sub, Unsqueeze, Where
```

No forbidden op is introduced and `functions=0`.

## Risk

Low-medium. This is a compact formula/dtype replacement with local exactness and
extra randomized sanity checks, but it introduces `Sqrt`, `Floor`, and `Ceil`.
Submit as an isolated single-task change for clean attribution.
