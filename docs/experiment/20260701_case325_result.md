# 20260701 case325 result - task348 packed bool tail

## Submission

- Kaggle ref: `54231343`
- Public Score before: `7183.91`
- Public Score after: `7184.05`
- Decision: adopted

## Local Gate

```text
task348 baseline:  params=169 memory=2104 cost=2273 n_fail=0 status=ok
task348 candidate: params=194 memory=1774 cost=1968 n_fail=0 status=ok
```

Relative reduction:

```text
2273 / 1968 = 1.1550
ln(old/new) ~= 0.1440 pt
```

## Change

Replaced the sparse bool tail:

```text
Concat(ch0_b, zero_b, zero_b, zero_b, zero_b, zero_b, zero_b, ch7_b, ch8_b)
  -> output_9x11
Pad(output_9x11, pad_to_30) -> output
```

with a packed live-channel projection:

```text
Concat(ch0_b, ch7_b, ch8_b) -> packed_live
Cast(packed_live) -> packed_live_u8
QLinearConv(packed_live_u8, one-hot 3ch->10ch weights, pads=final output pad)
  -> output
```

The graph output type is now uint8, which passed local exactness and Public LB.

## Conclusion

The bool packed-tail pattern is viable when the memory saved by removing zero
channels is larger than the cast and projection overhead. Continue with the same
measured-candidate rule rather than relying on rough estimates.

## Next Step

Try `task325`, the next validated packed-tail candidate:

```text
cost 2133 -> about 1932
old/new ~= 1.104
n_fail=0 in probe
```
