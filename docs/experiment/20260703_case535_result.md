# 20260703 case535 result - submit stall audit and parallel search expansion

No Kaggle submit.

Accepted Public Score remains:

- `7187.41`

## Why submits stopped

The recent gap was not a Kaggle/API failure. It was caused by candidate supply
and risk gating:

- case530 was the last accepted improvement: ref `54273472`, Public
  `7187.40 -> 7187.41`.
- case531 submitted isolated `task378`, but Public stayed `7187.41`.
- case532 submitted isolated `task044`; local exact cost reduction regressed
  Public to `7171.75`, so it was rejected and `task044` was promoted to
  hard-risk.
- case533 and case534 then found only tiny or contaminated candidates:
  `task046` review-risk one-byte cleanup, `task328` `6904 -> 6902`,
  `task005` `6882 -> 6881`, and `task033` `n_fail=3`.

So the loop stopped forcing cadence submits after a hidden/Public regression.
That protected the accepted score, but left the requested submit cadence without
enough ready candidates.

## Parallel work

In response, this cycle increased the search fan-out to six concurrent workers:

- mechanical cleanup tasks `001-100`
- mechanical cleanup tasks `101-200`
- mechanical cleanup tasks `201-300`
- mechanical cleanup tasks `301-400`
- semantic backlog / near-miss repair
- submit-stall audit

The workers were constrained to `/private/tmp` scratch paths and did not edit
repo files or submit to Kaggle.

## Confirmed exact candidates

Low-risk current-baseline exact candidates:

| task | path | cost | gain | decision |
|---:|---|---:|---:|---|
| 117 | `/private/tmp/neurogolf_agent_mech_B/task117_dup_init/task117.onnx` | `4148 -> 4145` | `0.000724` | bank-low-gain |
| 206 | `/private/tmp/neurogolf_agent_mech_C/task206_onnxsim.onnx` | `4183 -> 4182` | `0.000239` | bank-low-gain |
| 234 | `/private/tmp/neurogolf_agent_mech_C/task234_onnxsim.onnx` | `6845 -> 6841` | `0.000585` | bank-low-gain |
| 328 | `/private/tmp/neurogolf_case533_candidate_onnx/task328.onnx` | `6904 -> 6902` | `0.000290` | bank-low-gain |
| 005 | `/private/tmp/neurogolf_case533_candidate_onnx/task005.onnx` | `6882 -> 6881` | `0.000145` | bank-low-gain |

Aggregate low-risk gain is about `0.00198`, far below the default micro-bundle
submit gate of `0.015`.

Review-risk exact candidate:

| task | path | cost | gain | decision |
|---:|---|---:|---:|---|
| 066 | `/private/tmp/neurogolf_agent_mech_A/candidates/task066_onnxslim_16268_to_16248.onnx` | `16268 -> 16248` | `0.001230` | bank-low-gain, review-risk |

`task066` was not bundled because review-risk fillers are exactly the pattern
that caused recent hidden/Public damage.

## Stale backlog check

Several previously banked candidates are now stale against the accepted graph:

- `task105` candidate now compares `2945 -> 2945`.
- `task381` candidate now compares `2188 -> 2188`.
- `task354` old path now worsens current baseline, `2933 -> 2994`.

This confirms that submit windows should consume a current-baseline, pre-gated
queue rather than raw historical scratch paths.

## Decision

No submit. The confirmed low-risk exact bundle is too small to justify spending
a Kaggle attempt, and mixing in review-risk `task066` would repeat the weak
cadence-submit failure mode.

## Next

Use 10-11 minute decision windows, but separate them from discovery:

1. Keep at least six search workers active while a submit is pending.
2. Maintain a current-baseline pre-gated queue with stale-path checks.
3. Prefer one high-gain repair target next: `task023` (`n_fail=2`, possible
   `~0.130` gain) or `task279` (`n_fail=2`, possible `0.112` gain).
4. Submit immediately if a non-risk standalone reaches `gain >= 0.020`, or if
   a low-risk micro bundle reaches `gain >= 0.015`.
