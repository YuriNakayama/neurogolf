# 20260702 case489 plan - task138 cadence scan with expanded workers

## Context

Current accepted Public Score:

```text
7186.79
```

Recent cycles stalled because high-ratio scratch candidates were failing the
local functional gate before submit. This is a candidate-supply problem rather
than a Kaggle submit command failure.

The current metric is:

```text
points = max(1, 25 - ln(max(1, cost)))
cost = params + memory_bytes
improvement = ln(old_cost / new_cost)
```

So task selection should prioritize relative reduction, not absolute cost.
Large tasks are useful only when the expected ratio is also large.

## Target

Primary local target:

```text
task138.onnx
```

Reason:

- current cost is medium/high enough for useful movement if a structural ratio
  cut exists;
- prior worker scans found many scratch candidates but no fully current
  main-thread re-gate in the latest accepted baseline;
- it is a suitable cadence slot while expanded subagents search other bands.

## Method

1. Gather unique `task138*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Measure cost-only via `evaluate.audit_one(run_correctness=False)`.
3. Rank positive candidates by `ln(old_cost / new_cost)`.
4. Gate ranked candidates with `submit.gate.evaluate_candidate_gate`.
5. Submit only if the decision is `submit-candidate`.

## Parallel Work

Expanded workers are assigned to:

- `task138` independent sweep;
- `task054/task066/task076/task077` medium-ratio banks;
- `task243/task198` low-fail repair;
- `task014/task319` failure-family triage;
- micro-bundle cadence policy;
- fresh ratio-target discovery.

## Acceptance

Adopt only if Kaggle Public Score improves above `7186.79`. If no candidate
passes local gate, write a no-submit result, commit, push, and continue to the
next candidate window.
