# 20260702 case404 plan - task238 horizontal guard alias

## Context

Accepted Public Score is `7186.50` after case401. Case402 did not move Public,
and case403 regressed because local-usage-only candidate-row pruning was
hidden-unsafe.

The next candidate should avoid local-only table pruning and use a single
shape-compatible structural alias. Parallel next-cycle search found a current
`task238` candidate.

## Candidate

```text
task238 2495 -> 2355 gain 0.057748001735100904
```

Scratch file:

```text
/tmp/neurogolf_nextcycle_alias/task238_31_And_h_TO_candidate.onnx
```

## Method

Bypass the horizontal guard:

```text
h = And(candidate, row_var_zero)
Where(h, ...) -> Where(candidate, ...)
```

The sibling vertical path is unchanged. The change prunes the `And` result and
associated memory while preserving local behavior.

## Gate

Current-state targeted audit:

```text
baseline:  params 444, memory 2051, cost 2495, n_fail 0
candidate: params 444, memory 1911, cost 2355, n_fail 0
functions=0
forbidden ops=[]
```

Risk: low-medium. This is a structural guard-removal alias, not table pruning,
but it assumes `row_var_zero` is redundant whenever `candidate` is true. Submit
isolated and adopt only if Public Score improves over `7186.50`.
