# 20260702 case412 result - task338 isolated tr-pair alias

## Decision

Rejected.

```text
ref: 54256450
description: case412 task338 isolated tr pair alias gain 0.00671
previous accepted Public Score: 7186.71
Public Score: 7186.71
```

The real Public Score did not improve, so `task338.onnx` was restored to the
case410 accepted DVC state.

## Local Gate

Baseline:

```text
params 689
memory 17250
cost 17939
points 15.20526760730091
n_pass 267
n_fail 0
```

Submitted candidate:

```text
params 689
memory 17130
cost 17819
points 15.211979417272586
n_pass 267
n_fail 0
```

Local gain:

```text
ln(17939 / 17819) = 0.006711809971675796
```

Forbidden ops:

```text
[]
```

## Change Tested

```text
tr_pair -> tr_pair_0
```

Scratch:

```text
/tmp/neurogolf_future_task338/task338_tr_pair_TO_tr_pair_0.onnx
```

## Parallel Findings

The `201-400` subagent found no clean current candidate with `gain>=0.02`.
Best exact small candidates were:

- `task338`: `17939 -> 17819`, `gain=0.006712`, rejected here as Public
  unchanged.
- `task379`: `10773 -> 10753`, `gain=0.001858`.
- `task383`: `7245 -> 7237`, `gain=0.001105`.

Higher-gain exact candidates in that range are mostly prior `ERROR`-family
members and need isolation before reuse.

## Conclusion

Reject. The candidate is locally exact but still below the effective Public
movement threshold. Do not submit standalone `task338` pair aliases again. The
next cycle should prioritize either a new structural candidate above `0.02` or a
carefully isolated `ERROR`-family member with enough gain to justify the submit.
