# 20260702 case415 result - no-submit gated search

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7186.71
```

## Reason

No candidate cleared the post-regression submit gate. After case414, submitting
sub-threshold or unisolated micros is explicitly disallowed unless there is
prior Kaggle-valid evidence for the same task/pattern.

## Checked Candidates

### Safe current-baseline surgery

The main scan excluded runtime equivalence aliasing and tried only safer
mechanical passes:

```text
cleanup
identity_elementwise
index_surgery
broadcast_compress
conv1x1_to_gather
fp16_surgery
```

The scan was stopped after it produced only one tiny exact improvement and then
spent most time in ORT failures:

| task | current cost | candidate cost | gain | decision |
|---:|---:|---:|---:|---|
| 046 | 2286 | 2285 | 0.0004375410 | bank only |

Candidate:

```text
/tmp/neurogolf_case415_safe_surgery/task046.onnx
```

Subagent rechecked the same cleanup independently at:

```text
/tmp/neurogolf_case415_opt/task046_optimizer_cleanup.onnx
```

It removes one unused initializer (`offset_-2`), has `n_fail=0`, no forbidden
ops, and `functions=0`, but the gain is far below the standalone threshold.

### 001-200 subagent

No `gain >= 0.020` exact candidate was found.

Best exact candidate:

```text
task046: 2286 -> 2285
gain = ln(2286 / 2285) = 0.00043754102645092923
n_fail=0, status=ok, functions=0, forbidden=[]
```

### 201-400 subagent

No `gain >= 0.020` exact candidate was found.

Best exact candidate:

```text
task213
path=/private/tmp/neurogolf_case410_alias_scan_200_400/task213_27_And_in0_h_valid_p2.onnx
1851 -> 1834
gain = ln(1851 / 1834) = 0.009226659757087448
n_fail=0, status=ok, functions=0, forbidden=[]
```

Runner-up:

```text
task255: 22731 -> 22541
gain = 0.0083937584
```

## Conclusion

Skip submit. The best exact candidates are below the new gate:

- `task213` is the best banked micro at `gain=0.00923`;
- `task255` is another banked micro at `gain=0.00839`;
- `task046` cleanup is safe but negligible at `gain=0.00044`.

Next action: continue searching for a fresh isolated structural candidate with
current-baseline `gain >= 0.020`. Do not spend a Kaggle submission on the
banked candidates above unless they are later combined with already
Kaggle-validated members or strengthened within the same task.
