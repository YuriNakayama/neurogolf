# 20260702 case367 result - task187 final reach step required

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.78
```

## Checks

Baseline:

```text
task187
params=1008
memory=26525
cost=27533
n_fail=0
```

Recreated the strongest final-depth near misses:

| candidate | cost | gain | n_fail | decision |
|---|---:|---:|---:|---|
| `reach_13 -> reach_12`, pruned | `26283` | `0.046463` | 4 | reject |
| `reach_pool_13 -> reach_12` style | `26908` | `0.022962` | 4 | reject |
| `reach_13 -> reach_11`, pruned | `25033` | `0.095190` | 13 | reject |
| `reach_13 -> reach_10`, pruned | `23783` | `0.146414` | 41 | reject |

The `reach_12` candidate fails only frontier cells that should become color 3
but remain color 2:

```text
train[1]    row 8,  cols 11..15
arc-gen[42] row 11, cols 13..17
arc-gen[95] row 11, cols 10..11
arc-gen[108] col 10, rows 7..11
```

Tried an exact replacement for the final `Min(reach_pool_13, free_u8_25)`:

| candidate | cost | n_fail | decision |
|---|---:|---:|---|
| Cast `reach_pool_13` to bool, `And` with `free_b_25` | `27533` | 0 | exact but neutral |

This confirms that the final dilation plus free-cell mask is required. Removing
it saves memory only when the frontier cells are lost; restoring correctness
materializes the same amount of intermediate state as the baseline.

## Conclusion

Skip submit. Task187 has an attractive local gain if the last reach step is
removed, but the final step is needed for train and arc-gen correctness. No
`n_fail=0` candidate below the current `27533` baseline was found.

Next action: move away from flood-fill depth trimming. Prefer tasks where a
selector/table branch can be eliminated entirely, not reach-chain tasks where
the remaining errors are exactly the removed frontier.
