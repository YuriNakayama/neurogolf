# 20260702 case500 result - no-submit breadth scan

## Summary

No submit.

Accepted Public Score remains:

```text
7187.12
```

## Completed Checks

### Gate JSON Sweep

Recent `/tmp` and `/private/tmp` gate logs contained no unadopted
`submit-candidate`.

```text
matches=4
submit-candidate: task048 only, already adopted in case494
bank-low-gain: task054 only, already banked
```

### All-Task Unused Initializer Scan

Current-baseline mechanical cleanup across all tasks found one exact tiny
candidate:

```text
task046: 2286 -> 2285
gain=0.000437541
n_fail=0
decision=review-known-risk
path=/tmp/neurogolf_case500_unused_init_scan/task046.onnx
```

This candidate is already represented in the micro backlog and is far below the
standalone submit threshold.

### Worker Results

Fresh generation pass over `task158/task255/task002/task080/task145`:

```text
submit-candidate=0
bank-low=0
```

Best low-fail near misses:

```text
task002: 20689 -> 20529, gain=0.007763637, n_fail=5
task002: 20689 -> 20349, gain=0.016570387, n_fail=6
task255: 22541 -> 21761, gain=0.035216496, n_fail=28
```

Fresh generation pass over `task096/task110/task118/task133/task145`:

```text
submit-candidate=0
bank-low=0
```

Best near miss:

```text
task118: 35807 -> 9000, gain=1.380919, n_fail=267
```

The previous `081-160` existing-candidate scan also found no submit-candidate.
Its best near misses were `task138` local failures with `n_fail>=59` and a
`task084` broadcast-compress failure with `n_fail=175`.

## Decision

Rejected locally. No Kaggle submit was made because no new candidate reached
`n_fail=0` with a submit-worthy gain.

## Next Action

Keep active workers running on remaining bands and start the next cycle from the
first reported `submit-candidate`. Avoid revisiting documented closed repair
families such as `task077`, `task110`, `task187`, and `task243` unless a new
repair mechanism appears.
