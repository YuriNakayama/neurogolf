# 20260701 case317 result - reject task135 fixed crop

## Result

- Decision: reject
- Kaggle submission ref: `54230244`
- Previous Public Score: `7183.26`
- New Public Score: `7182.65`
- Public delta: `-0.61`

## Local Gate

Candidate `task135.onnx`:

```text
params=6
memory=360
cost=366
n_pass=266
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=None
memory=None
cost=None
n_pass=266
n_fail=0
status=score_error:[ShapeInferenceError] Conv pads contains negative values
```

## Change Tested

Replaced the original negative-pad `Conv` with:

```text
Slice(input, rows 0:3, cols 6:9) -> Pad(bottom=27, right=27)
```

All local train/test/arc-gen examples satisfy the fixed crop rule, but the real
Kaggle Public LB regressed.

## Conclusion

Rejected and restored the previous DVC-tracked ONNX. This is another example
where local exactness over the available examples is insufficient: the private
validation distribution likely requires the original negative-pad `Conv`
semantics or a broader rule than the visible fixed crop.

Do not retry `task135` fixed crop unless a rule is proven against hidden-style
augmentation, not just the visible examples.
