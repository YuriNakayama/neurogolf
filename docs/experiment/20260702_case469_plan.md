# 20260702 case469 plan - task324 line/color current re-gate

## Context

`task324` is the next current-rank target after closing `task145` and
`task157`. It has prior adopted kernel-trim work, and the current rank shows:

```text
task324 cost=8598 params=1290 memory=7308
```

The graph is line/color selector heavy, with many old aliases around rare
colors, background row/column predicates, and terminal color maps. These may be
semantic, so the first pass must be current-baseline cost-only filtering.

## Target

```text
task324
baseline: data/output/onnx/task324.onnx
task json: data/lake/neurogolf-2026/task324.json
```

## Hypothesis

Old source/selector aliases will mostly fail local correctness, but there may
be a mechanical cleanup or same-shape value alias left after the accepted kernel
trim. Because the current cost is mid-high, even a moderate exact reduction can
clear the submit threshold.

## Method

1. Audit accepted `task324.onnx`.
2. Gather `task324*.onnx` candidates from `/private/tmp` and `/tmp`.
3. Cost-only scan all candidates against the current baseline.
4. Correctness-gate current positive candidates, with submit only if:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

Bank low-gain exact candidates; do not force a submit for local-fail semantic
aliases.
