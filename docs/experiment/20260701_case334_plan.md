# 20260701 case334 plan - task319 obj1 row/view alias bundle

## Context

- Current accepted Public Score floor: `7184.93` after case333.
- Priority rule: `n_fail=0` first, then relative reduction
  `old_cost / new_cost`.
- Parallel structural scans found no broad dtype-narrowing or terminal-pack
  candidate above this one. The strongest exact candidate is a task319 alias
  bundle that removes redundant `obj1_*` row/view intermediates.

## Target

- `data/output/onnx/task319.onnx`

## Baseline

Current local audit:

```text
params=133
memory=21590
cost=21723
n_fail=0
status=ok
```

## Hypothesis

The current graph materializes several `obj1_*` row and view tensors that are
value-equivalent to earlier padded/local tensors for all public checks. Rewiring
their consumers to the earlier tensors should preserve outputs while pruning the
dead aliases.

Exact bundle:

```text
obj1_top_rows -> obj1_local5_pad
obj1_top_view -> obj1_top_rows
obj1_bottom_rows -> obj1_bottom_pad
obj1_bottom_view -> obj1_bottom_rows
obj1_left_rows -> obj1_local5_pad
obj1_left_view -> obj1_left_rows
obj1_right_rows -> obj1_right_pad
obj1_right_view -> obj1_right_rows
```

Do not mirror this to `obj2_*`: analogous aliases failed local correctness in
the structural scan.

## Expected Gain

Temporary audit:

```text
params=133
memory=20386
cost=20519
n_fail=0
status=ok
```

Expected point gain:

```text
21723 / 20519 = 1.0587
ln(old/new) ~= 0.0570 pt
```

## Verification Plan

1. Apply only the `task319` alias bundle.
2. Run individual `evaluate.audit_one` for `task319`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
