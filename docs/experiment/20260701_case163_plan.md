# 20260701 case163 plan

## Context

- Current accepted Public Score: 7181.17
- case162 rejected `task268`; all open-side/diagonal branches were required.
- Shift to a simpler frame-drawing task where exact simplification of inner/outer rectangles may be possible.

## Target

- Task: `task224`
- Baseline audit:
  - params: 371
  - memory: 2460
  - cost: 2831
  - points: 17.0516147148881
  - n_pass: 266
  - n_fail: 0
  - filesize: 3484

## Hypothesis

`task224` detects a bounding box, constructs two nested rectangles, and paints their XOR as a frame:

- `rect1 = row_span1 & col_span1`
- `rect2 = row_span2 & col_span2`
- `frame = rect1 XOR rect2`
- `Where(frame_full, paint_color, input)`

If public examples never require one side of the nested frame, or if one of the one-cell shrink operations is redundant, replacing `frame`, `rect1`, `rect2`, or span inputs can remove 30x30/inner mask tensors. Because the baseline cost is 2831, a successful removal of multiple masks would be meaningful.

## Method

1. Probe one rectangle/frame replacement at a time on temporary `task224` ONNX copies.
2. Run `audit_one` only for `task224`.
3. Apply and submit only if `n_fail=0` and cost decreases materially.
4. If no exact-match reduction exists, write result and commit docs only.
