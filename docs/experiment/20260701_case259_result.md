# 20260701 case259 result - task367 label terminal one-hot

## 結果

- 対象: `data/output/onnx/task367.onnx`
- baseline: `cost=24727` (`params=5127`, `memory=19600`, `n_pass=266`, `n_fail=0`)
- 採否: 不採用
- Kaggle submit: なし

## 試行

終端 `Concat(ch0m, zero20..., mask20, c5u...) -> out20 -> Pad(output)` を、20x20 label 画像に畳んで最後に `Equal` で one-hot 化する案を probe した。

| 候補 | params | memory | cost | n_pass | n_fail | 判定 |
|---|---:|---:|---:|---:|---:|---|
| baseline | 5127 | 19600 | 24727 | 266 | 0 | ok |
| `label20 = mask20*4 + c5u*5` | 4740 | 17300 | 22040 | 3 | 263 | reject |
| `mask20` 優先 label | 4741 | 17700 | 22441 | 3 | 263 | reject |
| `c5u` 優先 label | 4741 | 17700 | 22441 | 3 | 263 | reject |

## 結論

local cost は大きく下がるが、完全一致しない。`task367` の終端は単一 label ではなく、`mask20` と `c5u` などが同時に立ち得る multi-hot 表現を必要としている。`Equal(label)` 化は faithful 条件を満たさないため、ONNX は変更せず submit しない。

## 次の一手

terminal one-hot を単一 label に落とす候補は、元 graph が厳密な one-hot であることを先に確認してから試す。次は `Concat` zero block ではなく、`Where(Cast(mask), zero, base)` や `Pad` 化のように multi-hot を保つ局所変換を優先する。
