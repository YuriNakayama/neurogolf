# case235 plan: task213 phase selector branch trim

## 現状
- 採用済み Public Score floor: 7181.21
- `task213` baseline は `cost=1887` (`params=49`, `memory=1838`, `n_fail=0`)。
- h/v 方向それぞれで 3 phase (`p0/p1/p2`) を Slice し、valid 判定と `Where` cascade で compact 9x7 mask を作る。
- Scatter はなく、`Where`/`Cast`/`Slice` の中間 memory が主な削減余地。

## 対象
- `data/output/onnx/task213.onnx`

## 仮説
public/local examples で h/v の片側や phase valid pattern が固定なら、h/v 対称 branch や `Where` cascade をまとめて削れる。単発数 bytesではなく、複数 node の exact prune を束ねられる可能性がある。

## 検証
- `h_valid_no_pair` / `not_h_valid` / `h_pos_selected` / `v_pos_selected` の alias を probe する。
- h/v の phase `Where` cascade (`h_ids0/h_ids01/h_ids_selected`, `v_*`) を固定化する。
- `Cast(bool)->float16` の dtype 変更や直接 ReduceSum が可能か確認する。
- `src.evaluate.scorer.audit_one` で `task213` の全ペア完全一致と cost を確認し、`n_fail=0` かつ十分な cost 改善候補だけ実 ONNX へ反映する。
