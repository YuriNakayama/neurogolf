# 20260701 case281 result - adopt task191 column score alias

## Result

- Target: `task191`
- Baseline: `params=125`, `memory=14844`, `cost=14969`, `n_fail=0`
- Candidate: `params=102`, `memory=14752`, `cost=14854`, `n_fail=0`
- Relative reduction: `14969 / 14854 = 1.0077`
- Expected point gain: `ln(14969 / 14854) ~= 0.0077`
- Submit ref: `54222995`
- Public Score: `7181.93 -> 7181.94`
- Adoption: adopted

## Change

Applied one exact alias:

```text
col_score_first -> col_has
```

Dead-node pruning removed the now-unused column scoring branch and stale
metadata.

## Verification

Individual local audit for the submitted `task191.onnx`:

```text
params=102
memory=14752
cost=14854
n_pass=267
n_fail=0
status=ok
```

## Conclusion

Adopted. The local gain was small but enough to move the real Public Score by
`+0.01`, confirming that exact mid-cost single aliases are still worth
submitting when the expected gain is near one displayed basis point.

## Next Step

Continue parallel task-local scans for exact alias bundles. Prioritize candidates
with relative reduction above `1.01`, but keep `~1.007` candidates if they are
low-risk and no stronger candidate is available.
