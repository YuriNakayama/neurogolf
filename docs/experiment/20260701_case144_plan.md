# 2026-07-01 case144 plan

## 方針

- floor: Public Score 7181.16
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task119.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task119.onnx`
- baseline: params 121, memory 1244, cost 1365, points 17.781090292380938, n_pass 266, n_fail 0

## 仮説

`task119` は cyan edge から開始位置と向きを推定し、red count 由来の depth で scatter 更新を作る。`high_start`、`edge_is_vertical`、`side_rev`、`side_transpose` の branch が row/col と反転方向を決めている。

実データの edge 配置が限定的なら、これらの branch selector の一部を片側に固定、または元の edge flag へ簡約できる可能性がある。cost は小さいが branch 1 個あたりの削減が相対的に効きやすい。

## 手順

1. `/tmp` 上の candidate ONNX で `high_start` / `edge_is_vertical` / `side_rev` / `side_transpose` 周辺の `Where` を片側入力へ差し替える。
2. `axis_pos`、`y_side`、`row_f`、`col_f` の中間を個別に簡約し、`n_fail=0` の候補だけ組み合わせる。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task119.onnx` に反映する。
4. submit し、Public Score が 7181.16 を上回れば採用、上がらなければ破棄する。
