# 20260702 case464 plan - task350 span graph re-gate

## Context

Accepted Public Score floor remains `7186.71`. Cases 456-463 produced no
submit because the cost-reducing candidates failed local correctness, or were
exact but too small to spend a standalone submit.

The priority rule remains relative cost reduction:

```text
gain = ln(old_cost / new_cost)
```

`task350` is the next normal-risk cost-9000 target after `task074`. It is a
simple span graph whose cost is dominated by intermediate tensors, so deleting
one faithful branch would give material relative gain. However, case117 already
showed that the four directional spans were semantically required on the then
current graph.

## Target

```text
task350
baseline: params=24 memory=9012 cost=9036 n_fail=0
```

Known graph from case117:

```text
m = cast(slice(input color1))
hl/hr = horizontal MaxPool directions
hs = Min(hl, hr)
vu/vd = vertical MaxPool directions
vs = Min(vu, vd)
span = Max(hs, vs)
fc = span > m
Pad(fc) -> Where(fill8, input)
```

## Hypothesis

If any current scratch candidate preserves the full span semantics while
removing a redundant branch, it should clear the submit threshold. If all
positive candidates still fail local correctness, close `task350` for now and
move to mid-cost selector tasks instead of repeatedly testing directional span
aliases.

## Method

1. Re-audit the accepted `task350.onnx`.
2. Gather current `task350` candidates from `/private/tmp` and `/tmp`.
3. Evaluate candidates against `data/lake/neurogolf-2026/task350.json`.
4. Submit only if a candidate reaches:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

5. Otherwise write a no-submit result with the best blocked reductions and move
   to the next target.
