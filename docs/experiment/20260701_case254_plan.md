# 20260701 case254 plan — task157 opset13 bool Padで終端 Concat削減

## 現状

- 直近採用: case249 `task055` final `Einsum` float16 化で Public Score `7181.59`
- 直近 probe: case253 `task025` TopK slot 4→3 は `n_fail=35`
- 優先基準: `n_fail=0` を絶対条件にし、`old_cost / new_cost` の相対削減率を優先する。

## 対象

- `task157`
- baseline audit: `params=351`, `memory=7983`, `cost=8334`, `n_fail=0`

## 仮説

case101 で `ch1_active_pre + zeros_ch1_rows -> ch1_active10` の suffix-zero `Concat` を `Pad` 化しようとしたが、opset 11 の bool `Pad` が ONNX Runtime で無効だった。opset 13 へ上げ、既存 `Squeeze` / `Unsqueeze` の axes attribute を axes input に変換すれば、bool `Pad` が使える可能性がある。

`zeros_ch1_rows (4,15)` の initializer と `Concat` 出力 memory を削れるため、単発では小さいが `n_fail=0` なら採用候補になる。opset 13 化自体で挙動が変わらないことも同時に確認する。

## 手法

1. `task157` の opset を 13 に上げる。
2. `Squeeze` / `Unsqueeze` の `axes` attribute を int64 initializer input に変換する。
3. `Concat ch1_active_pre + zeros_ch1_rows -> ch1_active10` を `Pad(ch1_active_pre, pads=[0,0,4,0], constant=false)` に置換する。
4. `src.evaluate.scorer.audit_one` で `task157.onnx` のみ検証し、`n_fail=0` かつ cost 減なら submit する。
