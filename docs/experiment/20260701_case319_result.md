# 20260701 case319 result - adopt task363 packed channel pad

## Result

- Decision: adopt
- Kaggle submission ref: `54230512`
- Previous Public Score: `7183.33`
- New Public Score: `7183.39`
- Public delta: `+0.06`

## Local Gate

`task363.onnx` after the change:

```text
params=321
memory=4039
cost=4360
n_pass=265
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=297
memory=4339
cost=4636
n_pass=265
n_fail=0
status=ok
```

Local cost reduction:

```text
4636 -> 4360
old_cost / new_cost = 1.0633
ln(old/new) ~= 0.0614 pt
```

## Change

Replaced the tail:

```text
Concat(out0, zero_plane, out2, zero_plane, zero_plane, gray_u8) -> active
Pad(active, outpad, value=0) -> output
```

with:

```text
Concat(out0, out2, gray_u8) -> packed_tail
QLinearConv(packed_tail, one-hot 3ch->10ch weights, pads=[0,0,20,20]) -> output
```

The one-hot projection maps `out0` to channel 0, `out2` to channel 2, and
`gray_u8` to channel 5. Other output channels and the bottom/right pad are zero.

## Verification

Individual `evaluate.audit_one` for `task363` passed with `n_fail=0`.
The Kaggle Public LB improved to `7183.39`, so the packed channel projection
transferred.

## Conclusion

Adopted. The case318 pattern generalized: explicit zero channel materialization
near the output is worth replacing with a packed live-channel representation and
one-hot projection when the saved intermediate memory exceeds the added weights.

Next candidates from the same scan: `task274`, `task271`, `task030`, and
`task023`, in that order by relative reduction.
