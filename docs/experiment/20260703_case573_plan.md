# 20260703 case573 plan - task325 density repair

Accepted Public Score:

- `7187.67`

## Background

After case572 adopted task243, the task325 repair worker produced an exact
candidate from the previous three-fail `Sum -> n0` line.

Candidate:

```text
path:    /private/tmp/neurogolf_case573_task325/candidates/task325_density_repair.onnx
task325: 1929 -> 1900
gain:    0.015148
n_fail:  0
```

## Hypothesis

The original cheap task325 candidate undercounted three density examples. The
repair adds bounded scalar density corrections and avoids restoring the heavier
patch-convolution correction branch, retaining a useful relative reduction.

The candidate is below the normal standalone threshold but close to it, exact,
and outside known risk sets. Given the requested cadence and the successful
recent review-mid submissions, it is suitable for isolated review submit.

## Method

1. Re-gate task325 against the current accepted baseline.
2. Build a full 400-task bundle replacing only `task325`.
3. Run full dry-run with review gate enabled.
4. Submit if dry-run passes; adopt only if Public Score improves over `7187.67`.
