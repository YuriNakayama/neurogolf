# 2026-07-01 case135 plan

## 方針

- floor: Public Score 7181.15
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task044.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task044.onnx`
- baseline: params 68, memory 11390, cost 11458, points 15.653556545031478, n_pass 266, n_fail 0

## 仮説

case134 では `Clip(vals, 0, 1)` を外しても後段の存在判定が完全一致し、相対約 4.8% の cost 削減になった。

`task044` には `Clip(row/col sums, 0, 1)` 系の active mask が6箇所ある。後段が `ArgMax`、`Cast(bool)`、`And` などで「非ゼロかどうか」だけを使っている箇所なら、Clip を外して sum を直接使える可能性がある。特に複数 Clip をまとめて prune できれば、20x20級中間の削減で LB に反映する見込みがある。

## 手順

1. `/tmp` 上の candidate ONNX で、各 Clip output を元入力 sum に個別差し替えする。
2. 個別に `n_fail=0` の箇所だけを組み合わせる。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task044.onnx` に反映する。
4. submit し、Public Score が 7181.15 を上回れば採用、上がらなければ破棄する。
