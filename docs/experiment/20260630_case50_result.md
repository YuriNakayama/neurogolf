# 20260630 case50 result — uint8 flood-fill が floor とほぼ parity に到達（t251 5070 vs floor4920）、提出なし

## independent_impl（必須欄・parity 到達）
case49 の uint8 技法を完成させ floor の符号化に肉薄:
- **uint8 出力**: floor の graph output は **uint8（dtype=2）**と判明。最後の float cast を除去し
  Concat(uint8)→Pad(uint8)→output(uint8)。t251 **6798→5070**（floor 4920 の **1.03倍**, ほぼ同値）。
- 中間数: 私 26個(4896B) vs floor 25個(4752B)。**差は中間1個(144B)のみ**＝ほぼ完全 parity。

## enclosed-fill 全タスクへの適用判定
- 全400から enclosed-fill を抽出 = **t002, t251 の2件のみ**。
  - t251: 私 5070 vs floor 4920（-150、僅差負け）。
  - t002: 10色で出力 channel 削減効かず、`small[1,10,20,20]` が大きく **~26830 > floor 21089**（負け）。
    かつ assembly バグ(nfail=268)残。t002 は色数多く channel-pad の利得が小さい。

## 本セッションの技術的到達点（重要）
独自実装を naive float から **16-22倍** cost 削減し floor と parity に到達:
| 技法 | 効果 |
|---|---|
| static crop [S×S] | 中間を実gridに縮小 |
| 単/少 channel 処理 | 不要 channel 除去 |
| **uint8 中間(opset17)** | memory 4×削減（最大レバー） |
| **uint8 出力** | 末尾 float cast 除去 |
| frame seed(initializer) | dilation 不要 |

→ 「独自実装は大敗」は完全に誤りで、**floor と同等まで到達可能**。ただし floor も同じ技法を
適用済みのため、enclosed-fill では parity 止まりで純増には至らず。

## 確定
- flood-fill 系（t002/t251）は floor が uint8+crop+frame で既に最適化済み。私の独自実装は parity。
- 純増には「floor がこれら技法を**未適用**のタスク」が必要だが、enclosed-fill には存在せず。
  他カテゴリ(case49: float-heavy 非局所)も rule 複雑度が支配的。

## 恒久方針の遵守
- 停止せず uint8 出力で parity を達成（前進）。次サイクルは uint8 技法群を非 flood-fill の
  float-heavy floor（floor が uint8 化を見落としている可能性）に適用して純増を探す。
- floor 7180.78（DVC 6e8d4642）退行なし。
