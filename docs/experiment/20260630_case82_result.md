# 20260630 case82 result — t233 の候補数/dtype/機械的削減は採用不可（提出なし）

## 対象

- `data/output/onnx/task233.onnx`
- baseline: params **556**, memory **50512**, cost **51068**, `n_fail=0`

## 検証

### trace

主な memory 支配テンソル:

- `output`: bool `[1,10,30,30]` = 9000B
- `safe_name_119`: float `[5,324]` = 6480B
- `safe_name_46`: float `[1,1,30,30]` = 3600B
- `safe_name_293/291/290/288/287/285/118`: `[5,324]` 系 = 各 1620B

### TopK 候補数削減

| 変更 | cost | 結果 |
|---|---:|---|
| `k5=4` | 51042 | `n_fail=266` |
| `k5=3` | 51016 | `n_fail=266` |
| `k5=2` | 50990 | `n_fail=266` |
| `k5=1` | 50964 | `n_fail=266` |
| `k3=2` | 50423 | `n_fail=3` |
| `k3=1` | 49778 | `n_fail=5` |

`k5` は後続が固定 `[5,*]` shape を仮定しており、単純縮小で reshape 系が破綻する。`k3` は cost を大きく下げるが arc-gen で失敗するため不採用。

### dtype / Cast 周辺

- `equ4 -> cas5` と `gre7 -> cas8` は直後の `QLinearConv` 入力で uint8 が必要。
- `equ88_4d_bool -> res92` は `Conv` が bool 入力を受けられず、`pow2w` が 1..256 の float16 bit pattern なので単純 uint8 置換も不可。

### `ReduceMin` 二段畳み込み

`ReduceMin(axis=1)` → `ReduceMin(axis=0)` を `ReduceMin(axes=[0,1])` に畳む候補を確認したが、中間の行方向最小値が後続 `Equal` にも分岐して使われているため削除不可。

### 機械的削減

- dead output: なし
- 同一入力・同一属性の重複 node: なし
- 未使用 initializer: なし
- 重複 initializer: なし
- onnxoptimizer 単独 pass: `split_init` / `split_predict` 以外に構造変化なし。両者は unscorable。

## 採否

- Kaggle submit: **なし**
- 採否: **不採用**
- Public Score floor: **7180.85 維持**

## 結論

t233 は現行 frank3 系 graph で、候補数・主要 dtype・機械的 dead/duplicate 削減がほぼ飽和している。`k3` は cost を 645B 下げるが 3 ペア失敗するため、LB ゲート以前に採用不可。

## 次の一手

t233 は保留。次は高コスト帯の別タスク（t286/t018/t118/t187 など）に移り、trace 上の候補数削減または不要 Cast の一点検証を行う。
