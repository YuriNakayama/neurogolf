# 20260702 case413 plan - isolate task392 col-distance squeeze

## Context

- Accepted Public Score is `7186.71`.
- case411 `task342` and case412 `task338` were both locally exact but Public
  unchanged, so standalone micros below roughly `0.006` are weak.
- Fresh `001-200` and `201-400` searches have not yet returned a clean
  `gain>=0.02` exact candidate.
- The remaining case390/case395 `ERROR`-family candidates need isolation before
  they can be reused in any bundle.

## Target

Target: `task392`.

Candidate:

- scratch: `/tmp/neurogolf_case385_371_400/task392_30_Unsqueeze_in0.onnx`
- rewrite: `col_dist_4d -> col_dist`
- current baseline: `cost=1951`, `n_fail=0`
- candidate: `cost=1941`, `n_fail=0`
- expected gain: `ln(1951 / 1941) = 0.005139`

## Rationale

The gain is small, but the change is mechanically simple: it removes an
unnecessary `Unsqueeze` value path and keeps dtype/opset unchanged. `task392`
was included in the earlier ERROR batches, but has not been isolated. A single
task submit gives clear attribution and, if Kaggle accepts it, adds another
safe micro to the accepted bundle.

## Gate

1. Replace only `data/output/onnx/task392.onnx`.
2. Run `evaluate.audit_one` for `task392` only.
3. Confirm no forbidden ops and no ONNX functions are introduced.
4. Submit if `n_fail=0` and cost remains lower.
5. Adopt only if Kaggle Public Score improves over `7186.71`; otherwise restore
   the accepted DVC state and record rejection.
