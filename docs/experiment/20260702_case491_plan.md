# 20260702 case491 plan - task041 cadence scan

## Context

Current accepted Public Score:

```text
7186.79
```

The expanded-worker pass has confirmed the current bottleneck is candidate
supply: many scratch ONNX files reduce cost but fail the mandatory local
correctness gate. Micro candidates are being banked, not force-submitted, unless
their reviewed aggregate is large enough.

## Target

```text
task041.onnx
```

Reason:

- it has a moderate scratch pool and is not assigned to active workers;
- it avoids the highest-risk known regression/ERROR families;
- relative gain `ln(old_cost / new_cost)` remains the ranking criterion.

## Method

1. Gather unique `task041*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Measure cost-only with `evaluate.audit_one(run_correctness=False)`.
3. Rank positives by relative gain.
4. Gate positives with `submit.gate.evaluate_candidate_gate`.
5. Submit only if a candidate reaches `submit-candidate`.

## Parallel Work

Active workers continue on:

- `task054/task066/task076/task077`;
- `task005/task048/task018`;
- `task133` repair search.
