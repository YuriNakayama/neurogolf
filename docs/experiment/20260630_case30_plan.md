# 20260630 case30 plan — multi-agent 並列ソルバ探索（ユーザー承認の token-intensive sweep）

## 背景
case1-29 で serial hand-construction が floor を破れないことを数学的に証明（単一Conv=中間tensor0、
multi-node=非分離）。ユーザーが multi-agent 並列ソルバ探索を選択（明示的 opt-in）。

## 仮説
24 個の高コスト multi-node measurable タスク（t187/t366/t158/t133/t209/t349/t285/t367/t255/t319/
t076/t219/t066/t338/t101/t173/t080/t191/t110/t138/t198/t145/t023/t205）を並列エージェントで同時に
rule-decode → 最小tensor net 構築 → faithful 検証 → cost 測定。serial より遥かに広く探索する。

## 手法（Workflow: neurogolf-fleet-solver）
- pipeline: 各タスク 1 build エージェント（high effort）→ is_win 主張のみ verify エージェント（独立再検証）。
- 各エージェントへ memory metric の正確な理解（=全tensor総和、単一Conv 中間0 が最安）を注入。
- 厳密規則のみ（fit 禁止, hidden 落ち回避）。faithful n_fail=0 かつ cost<floor のみ win。
- 二重検証（build agent 測定 + verify agent 独立再測定）で偽陽性を排除。

## 採否
- confirmed_win があれば該当 onnx を floor へ overlay → faithful 再監査 → combined submit → 実 LB で採否。
- 退行ゼロ最優先（floor 7180.62 公開首位を維持）。win なければ floor 維持。
