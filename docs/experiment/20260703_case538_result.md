# 20260703 case538 result - no-submit low-fail semantic repair

No Kaggle submit.

Accepted Public Score remains:

- `7187.41`

## Summary

Case538 refreshed two low-fail, non-risk semantic families against the current
accepted graph:

- `task077` propagation depth trim
- `task102` fill-core branch shortcut

Both still have positive current-baseline gain, but neither could be repaired to
`n_fail=0` while preserving a submit-worthy cost reduction.

## task077

Re-gated `62` discovered `task077*.onnx` candidates.

Best current near misses:

- baseline cost: `14834`
- candidate cost: `14414`
- gain: `0.028721886812117015`
- `n_fail=4`

Representative paths:

- `/private/tmp/neurogolf_case410_lowgain/task077_008_MaxPool_b1mn_p_TO_mn0.onnx`
- `/private/tmp/neurogolf_case410_lowgain/task077_010_MaxPool_b1mx_p_TO_mx0.onnx`
- `/private/tmp/neurogolf_case410_lowgain/task077_012_MaxPool_b2mn_p_TO_b1mn.onnx`
- `/private/tmp/neurogolf_case410_lowgain/task077_014_MaxPool_b2mx_p_TO_b1mx.onnx`

Failure pattern:

- min-side variants miss four cells that should become color `4`
- max-side variants similarly miss four color-`4` cells

Root cause:

Each near miss removes an early `MaxPool` dilation stage. That under-approximates
the final fill mask, leaving a few cells at their original color.

Repair attempts:

- global shifts did not fix the failures
- comparison-only and row-limited threshold repairs caused broad false positives
  (`108`, `135`, or `266` failures)
- exact structural repair restores the deleted `MaxPool`, returning to baseline
  cost `14834`

To preserve `gain >= 0.020`, repaired cost must be `<=14540`; no such exact
repair was found.

## task102

Best current near-miss family:

- baseline cost: `2527`
- candidate cost: `2342`
- gain: `0.076027563`
- `n_fail=10`

Representative paths:

- `/private/tmp/neurogolf_nextcycle_small_alias/task102_002.onnx`
- `/private/tmp/neurogolf_nextcycle_small_alias/task102_004.onnx`
- `/private/tmp/neurogolf_nextcycle_small_alias/task102_007.onnx`

Failure pattern:

The candidates remove the `frame_W4` / `fill_core4` 6x6-frame branch. The misses
are exactly the 4x4 interiors of those 6x6 frames.

Bounded repair:

- attempted cheap MaxPool-derived substitute branches
- best repaired cost: `2442`
- gain: `0.034215453`
- still `n_fail=10`

The exact repair restores the missing 6x6 branch, returning to baseline behavior
and losing the gain.

## Decision

No submit. Neither target produced an exact lower-cost candidate. The standing
low-risk micro queue is still below the `0.015` bundle threshold.

## Next

Avoid revisiting branch-removal near misses where the only exact repair restores
the deleted branch. Prefer candidates where the failure cells can be described
by an already-present tensor or a genuinely cheap static mask.
