# 20260703 case554 result - task174 mismatch-any reduction

## Summary

- Submit: `case554 full task174 mismatch any`
- Public Score: `7187.51 -> 7187.53`
- Decision: adopt

## Candidate

- Task: `task174`
- Candidate: `/private/tmp/neurogolf_agent_case553_D/candidate/task174.onnx`
- Baseline cost: `8978`
- Candidate cost: `8744`
- Relative gain: `0.026409`
- Gate: `n_fail=0`, `functions=0`, forbidden ops `[]`

## Change

Replaced an exact mismatch-count check:

```text
Cast(bool -> int32) + ReduceSum + Equal(0)
```

with an equivalent mismatch-any check:

```text
Cast(bool -> uint8) + ReduceMax + Equal(0u8)
```

This preserves the all-equal predicate while reducing intermediate memory.

## Verification

Full 400-task dry-run passed with one changed task:

```text
task174 8978 -> 8744 gain 0.026409 n_fail=0 decision=submit-candidate
bundle decision: submit-bundle
```

## Adoption

The Public Score improved to `7187.53`, so the candidate was copied to:

```text
data/output/onnx/task174.onnx
```

## Next Step

Continue standalone candidate search. Keep the newly discovered `task205`
micro candidate in the bank, but prioritize gain `>=0.020` changes before
another micro bundle.
