# 20260702 case510 result - reject task173 grid-score dtype shrink

## Result

Rejected.

Kaggle submission:

```text
ref          : 54268729
description  : case510 task173 grid-score dtype shrink
status       : ERROR
Public Score : none
```

## Local Gate

The candidate passed local correctness and the standalone submit gate:

```text
task173 15664 -> 15020
gain    0.041982
n_fail  0
decision submit-candidate
```

Candidate path:

```text
/tmp/neurogolf_parallel_AA/dtype_candidates/task173_cast10_to2.onnx
```

The equivalent alternate was:

```text
/tmp/neurogolf_parallel_AA/dtype_candidates/task173_cast10_to3.onnx
```

## Decision

Because Kaggle returned `ERROR`, the candidate was restored from:

```text
/tmp/neurogolf_case510_baseline/task173.onnx
```

Task173 is now marked as review-risk for this dtype-shrink family. Do not submit
the `cast10_to2` or `cast10_to3` variants again without isolating the Kaggle
runtime error.

## Conclusion

This was not a scoring regression but a runtime/submission compatibility failure
that local ORT did not catch. Dtype shrink candidates that change a value path
feeding score/ranking logic need an additional runtime-risk review even when
`n_fail=0` locally.
