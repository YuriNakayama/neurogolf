# case222 plan: task235 code mask trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task235` baseline は `cost=170` (`params=59`, `memory=111`, `n_fail=0`)。
- graph は `GatherND(sample_indices) -> MatMul(code_w) -> Equal(code_values) -> Pad`。

## 対象

- `data/output/onnx/task235.onnx`

## 仮説

3 点の sampled bits から 3-cell code を作り、7 channel の `top_left` mask を生成している。もし code pattern の一部が固定、または 7 channel 候補を縮められるなら、`GatherND/MatMul/Equal` や `code_values` を削れて `170` から相対的に大きく下げられる。

## 検証

- temp ONNX で `codes_f` 固定候補を probe する。
- `top_left` 固定候補と channel subset を probe する。
- `code_values` の channel 数削減や dtype 変更は型制約と cost を確認する。
- `src.evaluate.scorer.audit_one` で `task235` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
