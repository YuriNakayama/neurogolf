# case194 plan: task121 small patch selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task121` は未着手で、baseline は `cost=341` (`params=66`, `memory=275`, `n_fail=0`)。
- 低 cost タスクだが、90-byte の `small` tensor または周辺 scalar/1x1 tensor を削れれば相対削減率が大きい。

## 対象

- `data/output/onnx/task121.onnx`
- 3x3 の `bg_patch` を bool 化し、top row の背景 pattern から `top_col_offset` を決める。
- その offset で取得した `color_selector` と `zero_selector1` を `Where(patch_bg, zero_selector1, color_selector)` で合成し、30x30 に Pad する。

## 仮説

1. `top_col_offset` の `And(top_b0, top_b1)` 側が実データ上冗長なら、1x1 scalar chain を削れる。
2. `small` の合成が単純化できれば 90-byte tensor 周辺を削れる可能性がある。

## 検証

- temp ONNX で selector/offset の alias を 1 点ずつ probe する。
- `src.evaluate.scorer.audit_one` で `task121` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
