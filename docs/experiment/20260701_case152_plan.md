# 2026-07-01 case152 plan

## 方針

- floor: Public Score 7181.17
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task365.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task365.onnx`
- baseline: params 120, memory 3908, cost 4028, points 16.698974746161547, n_pass 266, n_fail 0

## 仮説

`task365` は 10x10 の color0 から top-left 候補を3個選び、row/col 方向の連続セルから width/height を推定し、選んだ bbox を 6x6 に pack する。`width_cols_valid` / `height_rows_valid`、`width_e*` / `height_e*`、`tl_present` の guard が多い。

実データで候補 bbox が常に範囲内、または top-left 候補が常に有効なら、boundary guard や score gate を外せる可能性がある。cost は 4028 で、3候補 x 10/6 の中間を削れれば相対効率がある。

## 手順

1. `/tmp` 上の candidate ONNX で width/height boundary guard を raw 値または sentinel 側へ差し替える。
2. `tl_present` score gate、pack mask、color1/color2 packed mask を個別に簡約する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task365.onnx` に反映する。
4. submit し、Public Score が 7181.17 を上回れば採用、上がらなければ破棄する。
