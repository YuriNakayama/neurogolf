# 20260702 case391 result - no-submit recovery check

## Summary

No Kaggle submit.

Accepted Public Score remains:

```text
7186.15
```

## Findings

Case390 returned Kaggle `ERROR`. The first recovery idea was to exclude
`task037`, because the earlier combined audit showed an ORT warning:

```text
Removing initializer 'safe_name_18'. It is not used by any node
```

Separate per-task audit showed the warning actually comes from `task046`:

```text
task046 2318 -> 2286 gain 0.013901 n_fail=0
```

The warning is not directly visible as a `safe_name_18` string in the saved
ONNX file, so it appears to be emitted by ORT graph cleanup during evaluation.
It may or may not be related to Kaggle `ERROR`, but resubmitting it inside
another batch would not isolate the issue.

## Rechecked Batch Without task046/task037

A no-`task046`, no-`task037` recovery batch was rechecked:

```text
task035 2262 -> 2255 gain 0.003099
task209 28047 -> 27927 gain 0.004288
task293 1288 -> 1286 gain 0.001554
task308 1488 -> 1484 gain 0.002692
task342 1585 -> 1576 gain 0.005694
task370 10393 -> 10351 gain 0.004049
task392 1951 -> 1941 gain 0.005139
task397 2848 -> 2845 gain 0.001054
```

Current-relative combined gain:

```text
0.027569
```

This is below the low-risk batch gate `0.030`.

Several backlog entries that looked useful are already present in the current
accepted bundle and therefore add no current gain:

```text
task173 current cost already 15702
task280 current cost already 6035
task325 current cost already 1929
task096 current cost already 8818
task377 current cost already 8368
task396 current cost already 8078
```

## Decision

Do not submit. Restore `data/output/onnx` to the case389 accepted bundle.

## Next

Do not spend another submit on the same case390 recovery family until one of
these is true:

```text
1. task046 warning is understood or isolated in a deliberately small submit
2. enough no-warning low-risk residuals are found to clear gain >= 0.030
3. a stronger isolated medium/high-upside candidate is rebuilt and audited
```

The next useful work is likely rebuilding `task023` residual pruning as an
isolated candidate, or re-evaluating the `task319` medium-risk bundle with
current baselines.
