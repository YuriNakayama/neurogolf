# 20260630 case100 result - t066 final zero padding Pad 化は採用

## 実装
`task066` の終端 mask 拡張を 2 段 `Concat` から 1 個の `Pad` に置換した。

- before: `hidden4 + hidden_right -> hidden_wide`, `hidden_wide + hidden_bottom -> hidden30`
- after: `Pad(hidden4, pads=[0,0,0,0,0,0,10,10], value=false) -> hidden30_pad`
- all-zero bool initializer `hidden_right` `(1,1,20,10)` と `hidden_bottom` `(1,1,10,30)` を削除

## faithful 検証
- before: cost 18386, params 641, memory 17745, n_pass 266, n_fail 0
- after: cost 17295, params 150, memory 17145, n_pass 266, n_fail 0
- 削減: cost -1091, params -491, memory -600

## submit
- ref: `54192622`
- message: `case100 t066 replace final zero concat with pad cost 18386 to 17295`
- status: `COMPLETE`
- Public Score: **7180.95**（前 floor 7180.89 から +0.06）

## 採否
- **採用**。
- ローカル faithful 完全一致で、実 Public Score も上昇した。

## 結論
zero initializer を伴う `Concat` padding は、同形の `Pad` へ置換できる場合に params と intermediate memory を同時に削れる。t066 は case75 の duplicate initializer 微小削減では LB 変化なしだったが、今回は memory-dominant な終端構造を削ったため Public に反映された。

## 次の一手
- `task338` / `task101` / `task080` など、終端や中盤で all-zero tensor を `Concat` している graph を同様に探索する。
