# 20260702 case451 result - task044 guarded candidate re-gate

## Result

No submit.

Current baseline:

```text
task044.onnx
params=68
memory=11390
cost=11458
points=15.6535565450
status=ok
```

## Gate Scope

Re-gated existing `task044` candidates from:

```text
/private/tmp/neurogolf_cleanup_probe/candidates/task044*.onnx
/private/tmp/neurogolf_fresh_014_140_case382/task044*.onnx
/private/tmp/neurogolf_case415_safe_surgery/work/task044.onnx
/private/tmp/neurogolf_case413_task044_bundle.onnx
/private/tmp/neurogolf_case356_struct/_opt_task044.onnx
/private/tmp/neurogolf_surgery_probe_1782911678/task044.onnx
```

The scan covered 110 candidates. To avoid another stale broad-candidate submit,
cost-only screening was followed by correctness audit for every positive cost
reduction.

## Findings

Positive cost reductions: 94.

Exact reductions: 9.

No exact candidate cleared the post-regression submit gate. The best exact
reductions were only:

```text
/private/tmp/neurogolf_fresh_014_140_case382/task044_bypass_And_bottom_m2_match_bool_TO_bottom_row_match.onnx
  11458 -> 11448
  gain=0.000873134
  n_fail=0
  decision=bank-low-gain

/private/tmp/neurogolf_fresh_014_140_case382/task044_bypass_And_bottom_inner_row_bool_TO_bottom_inner_row_ge.onnx
  11458 -> 11448
  gain=0.000873134
  n_fail=0
  decision=bank-low-gain
```

Seven additional scalar/bounds bypasses were exact at `11458 -> 11456`,
`gain=0.000174566` each:

```text
task044_bypass_Sub_top_inner_row_start_TO_half_scalar.onnx
task044_bypass_Sub_top_inner_row_end_TO_top_inner_row_end_raw.onnx
task044_bypass_Sub_top_inner_col_end_TO_top_inner_col_end_raw.onnx
task044_bypass_Sub_top_box_h_full_TO_gap_row.onnx
task044_bypass_Sub_bottom_inner_row_end_TO_bottom_inner_row_end_raw.onnx
task044_bypass_Sub_bottom_inner_h_TO_seven_scalar.onnx
task044_bypass_Sub_bottom_inner_col_end_TO_bottom_inner_col_end_raw.onnx
```

All larger task044 reductions failed local correctness. The highest-gain
near-miss family was:

```text
11458 -> 11258
gain=0.017609189
```

but every member had `n_fail > 0`, including output/value-path bypasses such as
`Sum_grid_out`, `Sub_grid_without_sources`, `Mul_*_fill`, and
`Clip_movable_*_active`.

## Decision

Reject for submission. The task044 current candidate pool does not contain an
isolated `submit-candidate`.

The exact low-gain candidates were added to
`docs/experiment/micro_backlog.md`, but they should not be submitted alone.
Even combining all exact task044 micros would still be far below the strict
`gain >= 0.020` submit threshold and would introduce unnecessary hidden-risk
surface.

## Regression Countermeasure

This cycle applied the post-regression policy correctly:

- broad stale candidates were re-gated against the accepted baseline;
- local failures were stopped before staging;
- low-gain exact candidates were banked instead of submitted;
- no Kaggle submit was spent on a candidate unlikely to move Public Score.

## Next

Move away from task044 broad alias candidates. Search for a fresh structural
target with `gain >= 0.020`, or a repaired near-miss where the failing examples
identify a small exception rather than a value-path rewrite.
