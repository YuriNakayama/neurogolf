# 20260702 case407 result - task343 repaired guard alias

## Submission

- Kaggle ref: `54255071`
- Message: `case407 task343 repaired guard alias gain 0.06140`
- Public Score: `7186.58 -> 7186.65`
- Decision: adopted

## Local Gate

```text
task343: cost 2015 -> 1895
         params=85, memory=1810
         n_pass=266, n_fail=0
         forbidden ops=[]
```

Current-relative gain:

```text
0.06140035686425664
```

## Change

Kept the near-miss removal of the `col8 == col0` path and added a small guard
on existing `p4_col0` to reject the single local failing signature before using
`p4_eq4` as `p4_valid`.

## Conclusion

Adopted. The isolated submit moved Public Score by `+0.07`, and isolating the
task avoided the attribution problem seen in case406.

Next cycle should continue this pattern: submit strong single-task candidates
alone, and only bundle micros when every member is low-risk cleanup.
