# 20260702 case456 plan - task138 direction/fill candidate re-gate

## Context

Case455 closed `task230`: all Conv-kernel crop candidates failed every local
example. The next target should come from a fresher mid-cost task, not another
low-cost Conv crop.

The subagent and case438 both identify `task138` as an unclosed mid-cost target
after `task174`, `task216`, and `task191` were closed.

## Target

`task138.onnx`

Current baseline:

```text
params=222
memory=13543
cost=13765
points=15.4701155824
n_fail=0
status=ok
```

Older case165 concluded that direct direction selector and sentinel-padding
shortcuts were not exact, but there is no current-baseline result after the
later accepted graph changes.

## Hypothesis

Most stale aliases around `hsel`, `vsel`, `fill_back`, and directional
forward/reverse fills are semantic, but the task still has enough cost that a
current-baseline exact candidate with `gain >= 0.020` may exist among the
bounded scratch pool.

## Method

1. Gather existing `task138*.onnx` candidates from known scratch pools.
2. Run cost-only screening against the current accepted baseline.
3. Run correctness gate only for positive cost reductions.
4. Submit only if a candidate reaches `submit-candidate`; otherwise record the
   no-submit result and move on.

## Gate

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
no forbidden ops/functions
single task only
```
