# 20260703 case549 result - no submit structural replacement scan

Accepted Public Score:

- `7187.48`

## Summary

No Kaggle submit was made. This cycle shifted from near-miss alias repair to
structural replacement search. Parallel workers and the main process scanned
case3 solver replacements, case2 golf wins, recent generated banks, and mined
current-baseline candidates.

No standalone non-risk candidate satisfied:

```text
n_fail = 0
gain >= 0.020
forbidden ops = []
functions = 0
```

No micro bundle reached the `0.015` total-gain threshold.

## Rechecked Sources

Main process:

- regenerated case3 solver output for non-risk tasks;
- re-gated `backend/pipeline/case2/golf_wins` against current accepted
  `data/output/onnx`;
- inspected current cost rankings for non-risk tasks;
- inspected `task066` as the best structural-replacement target.

Parallel workers:

- case3/case2/fixed-subgrid replacement scan;
- current-baseline artifact mining;
- visual/JSON shortlist of overbuilt tasks.

Results:

```text
case3 generated replacements: 51 solved, 0 current-baseline wins
case2 golf_wins: 36 checked, 0 current-baseline wins
recent artifact mining: no candidate with gain >= 0.010
```

## Banked Micro Queue

The best current low-risk exact queue is:

```text
task368 6225  -> 6217   gain 0.001286
task066 16268 -> 16248  gain 0.001230
task117 4148  -> 4145   gain 0.000724
task205 11547 -> 11539  gain 0.000693
task333 3227  -> 3225   gain 0.000620
```

Best five total:

```text
0.004553
```

This is below the `0.015` micro-bundle submit threshold, so the queue was
banked but not submitted.

`task066` exact micro was re-gated:

```text
/private/tmp/neurogolf_agent_mech_A/candidates/task066_onnxslim_16268_to_16248.onnx

task066 16268 -> 16248
gain    0.001230
n_fail  0
status  ok
functions 0
forbidden ops []
decision bank-low-gain
```

## Structural Target Review

The best non-risk hand-golf target identified is `task066`:

```text
current cost 16268
196 graph nodes
rule appears to fill color-3 orthogonal connector segments between sparse
markers/obstacles
```

Several older `task066` low-cost alias artifacts were re-gated. The large
reductions were locally incorrect:

```text
2927  gain 1.715222  n_fail 266
3728  gain 1.473328  n_fail 266
5622  gain 1.062513  n_fail 266
12975 gain 0.226176  n_fail 266
14709 gain 0.100740  n_fail 129
14751 gain 0.097889  n_fail 129
```

So `task066` remains a candidate for a new direct solver, not for alias
surgery.

## Decision

No submit.

Reasons:

- no standalone `n_fail=0` candidate with gain `>=0.020`;
- best micro queue totals only `0.004553`;
- existing case2/case3 banks are already absorbed into the accepted baseline;
- aggressive `task066` aliases fail local correctness.

## Next Step

Continue with a direct `task066` replacement attempt. The useful direction is
to derive the connector mask directly from red/green/cyan row-column evidence,
rather than removing subgraphs from the existing solver.
