# 20260630 case95 result — t243 の 28段 flood 反復は必要（提出なし）

## 対象

- `task243.onnx`
- baseline: params **42**, memory **22608**, cost **22650**, faithful `n_fail=0`
- Public Score floor: **7180.87**

## 実施内容

t243 は `ch1` seed から 18x18 内部を `QLinearConv(CROSS_W) -> Where(is_zero)` で反復 flood し、`fill_27` を pad して出力する graph。各反復は `QLinearConv` と `Where` の 18x18 uint8 tensor 2個で、1段短縮できれば約648 cost 下がる。

### final 反復短縮 probe

| 候補 | cost | 結果 |
|---|---:|---|
| final `fill_27 -> fill_26` | 22002 | `n_fail=1` |
| final `fill_27 -> fill_25` | 21354 | `n_fail=2` |
| final `fill_27 -> fill_24` | 20706 | `n_fail=2` |
| final `fill_27 -> fill_23` | 20058 | `n_fail=2` |
| final `fill_27 -> fill_22` | 19410 | `n_fail=2` |
| final `fill_27 -> fill_20` | 18114 | `n_fail=2` |
| final `fill_27 -> fc_26`（maskなし raw conv） | 22326 | `n_fail=265` |
| start `fill0 -> ch1_u8` | 22002 | `n_fail=1` |

1段短縮でも arc-gen の最長距離例で不一致。raw conv を使うと zero mask が外れて全滅する。

## 採否

- **不採用、提出なし**。
- `data/output/onnx/task243.onnx` は未変更。

## 結論

t243 は 28段反復が faithful 上必要。現行 floor は単純な反復展開だが、反復数に余剰はなく、局所短縮では Public Score を上げられない。次は `task319` / `task002` / `task076` へ移る。
