# 20260702 case393 plan - isolated task285 uint8 TopK bypass

## Context

Accepted Public Score is `7186.22` after case392 adopted the isolated task023
broad residual. Submit cadence is now about 15 minutes per user instruction.

Current candidate scan from the accepted bundle:

```text
task285 24535 -> 22702 gain 0.07764764320871903
task118 36576 -> 35807 gain 0.02124888259312957
task046  2318 ->  2286 gain 0.013901179544940979
```

Most low-risk micros are either adopted already or below threshold. The
task285 candidate has the largest current-relative gain.

## Candidate

Use the banked exact local candidate:

```text
/tmp/neurogolf_case382_main/task285_8_Cast_in0.onnx
```

This bypasses `color_idx_flat_f -> color_idx_flat` before `TopK`, feeding uint8
directly into `TopK`.

## Risk

Runtime/platform risk is high: docs compare this family to the earlier task233
uint8 `TopK` candidate that returned Kaggle `ERROR`. Local ORT accepts it and
`audit_one` passes, but Kaggle validator compatibility is not proven.

Because the gain is large and submit count has refreshed, test it as an
isolated candidate only. Do not batch with low-risk micros.

## Gate

Submit if:

- `task285` audit gives `n_fail=0`
- cost is `22702`
- current-relative gain is `0.0776476`
- no forbidden ops and no ONNX functions
- no other ONNX files are changed

Adopt only if Kaggle Public Score improves over `7186.22`. If Kaggle returns
`ERROR` or the score does not improve, restore the case392 accepted bundle.
