# 20260702 case443 result - task039 dynamic crop is no-submit

## Decision

- Task: `task039.onnx`
- Submit: no
- Adoption: none
- Reason: no current-baseline exact candidate reduces cost.

Current baseline:

```text
params=56
memory=424
cost=480
status=ok
```

## Graph Evidence

The graph is already a direct dynamic crop plus pad:

```text
GatherND(input, feature_indices) -> features
MatMul(features, pattern_weights) -> pattern_f
Cast(pattern_f) -> pattern
Gather(start_lookup, pattern) -> starts
Add(starts, three) -> ends
Slice(input, starts, ends, grid_axes) -> crop_f FLOAT [1,10,3,3]
Pad(crop_f, pad_pads) -> output
```

`crop_f` is 360 bytes and dominates the small `cost=480`, but it is the direct
value tensor for the output. Removing it is a semantic change, not cleanup.

## Candidate Re-Gate

Re-gated 46 existing candidates:

```text
blocked-local-fail: 27
blocked-no-gain:   19
submit-candidate:   0
```

Largest apparent reductions:

```text
task039_003_Gather_pattern_TO_grid_axes.onnx
  cost 480 -> 396
  gain 0.192372
  n_fail 242
  decision blocked-local-fail

task039_3_Gather_starts_TO_pattern.onnx
  cost 480 -> 463
  gain 0.036059
  n_fail 220
  decision blocked-local-fail

task039_4_Add_ends_TO_starts.onnx
  cost 480 -> 471
  gain 0.018928
  n_fail 264
  decision blocked-local-fail
```

Exact candidates:

```text
cleanup / broadcast_compress / identity / index_surgery:
  cost 480 -> 480
  n_fail 0
  decision blocked-no-gain
```

Several direct `Slice` input rewires are invalid or unscorable because the final
`Pad` receives the wrong shape or because the dynamic Slice inputs no longer
match the axes rank.

## Conclusion

No submit. `task039` is a high relative-gain-looking target only because it is
small; the large tensor is the necessary 3x3 output crop. Old scratch candidates
do not contain an exact reduction.

Move to the next fresh current-graph target rather than forcing dynamic crop
aliases.
