# 20260702 case493 result - no-submit task051 cadence scan

## Decision

No submit.

Accepted Public Score remains:

```text
7186.79
```

## Baseline

```text
task051.onnx
cost=1803
params=101
memory=1702
status=ok
```

## Scan

Existing unique `task051*.onnx` scratch candidates from `/tmp` and
`/private/tmp`:

```text
candidates=157
positive cost-only candidates=131
```

All positive candidates were gated:

```text
blocked-local-fail=131
submit-candidate=0
bank-low-gain=0
```

Representative failures:

```text
1803 -> 551   gain=1.185472  n_fail=265  blocked-local-fail
1803 -> 1487  gain=0.192691  n_fail=131-134 blocked-local-fail
1803 -> 1623  gain=0.105176  n_fail=66-69 blocked-local-fail
1803 -> 1773  gain=0.016779  n_fail=66-265 blocked-local-fail
1803 -> 1801  gain=0.001110  n_fail=68-265 blocked-local-fail
```

## Conclusion

Reject locally. The `task051` scratch pool cuts beam/row/column direction logic
and moment-based selector paths. Even tiny reductions fail local correctness, so
there is no submit or backlog candidate.
