# 20260701 case168 result

## Target

- Task: `task263`
- Baseline cost: 680
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task263`.

No candidate reduced scored cost while preserving exact correctness.

Orientation simplifications:

- `selected_counts -> h_vec`: cost 679, `n_fail=97`
- `selected_counts -> v_vec`: cost 680, `n_fail=116`
- `selected_starts_u8 -> h_starts_u8`: cost 680, `n_fail=97`
- `selected_starts_u8 -> v_starts_u8`: cost 680, `n_fail=116`
- `selected_ends_u8 -> h_ends_u8`: cost 680, `n_fail=97`
- `selected_ends_u8 -> v_ends_u8`: cost 680, `n_fail=116`
- all-horizontal selection: cost 678, `n_fail=97`
- all-vertical selection: cost 678, `n_fail=116`
- `h_valid -> h_valid_raw`: invalid `Where` shape inference

Exact but score-neutral odd-branch aliases:

- `odd1 -> ne01`: cost 680, `n_fail=0`
- `odd2a -> ne02`: cost 680, `n_fail=0`
- `odd3 -> odd3a`: cost 680, `n_fail=0`
- `odd3a -> ne03`: cost 680, `n_fail=0`
- `odd4 -> odd4a`: cost 680, `n_fail=0`
- `odd4 -> p4`: cost 680, `n_fail=0`
- `odd4a -> e01` or `odd4a -> ne04`: cost 680, `n_fail=0`

Combining the exact aliases removed up to ten graph nodes and reduced file size
from 4756 to 4455 bytes, but scored `params + memory` remained 680.

Other failures:

- `odd0 -> ne01/ne02`: `n_fail=75/74`
- `odd1 -> e02`: `n_fail=64`
- `odd2a -> e01`: `n_fail=64`
- `odd3 -> p3` and `odd3a -> e01`: `n_fail=16`
- `odd_map -> odd_u8`: invalid cyclic graph
- `idx -> idxv`: invalid rank for table `Gather`
- `selected_starts/ends -> selected_starts_u8/selected_ends_u8`: invalid `Slice` index type
- `color_count_w` float16/uint8/int8: invalid `Conv` type constraints

## Submission

- Not submitted.
- Reason: no `n_fail=0` candidate with lower scored cost.

## Decision

Rejected. Keep the existing `task263.onnx`.

## Next

Do not spend more time on file-size-only simplifications. Prefer candidates where
an exact rewrite removes a tensor counted in `params + memory`, not only scalar
boolean nodes.
