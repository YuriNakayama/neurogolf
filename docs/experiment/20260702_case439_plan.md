# 20260702 case439 plan - task174 current-baseline slice probe

## Context

Accepted Public Score remains `7186.71`. Case438 found no submit candidate in
the saturated `task205` alias pool. This cycle checks `task174`, which has prior
Kaggle-valid evidence from case315 but still has one large source slice.

Current local audit from the scorer:

```text
task174 cost=8978
params=178
memory=8800
status=ok
```

Dominant internal tensors:

```text
colors4_1      FLOAT [1,9,10,10]  3600 bytes
colors4_u8_174 UINT8 [1,9,10,10]   900 bytes
colors_pad_19  UINT8 [1,3,13,13]   507 bytes
colors_flat_20 UINT8 [1,3,169]     507 bytes
```

The large `colors4_1` slice is consumed by both:

```text
Cast(colors4_1) -> colors4_u8_174
ReduceSum(colors4_1) -> present9_2
```

## Hypothesis

The large slice is probably structural because the graph needs all nonzero
color channels for both present-color selection and later crop extraction.
However, old cleanup/dtype candidates exist and should be re-gated against the
current baseline before deprioritizing the task.

## Method

1. Re-gate existing `task174` cleanup, dtype, and material-bypass candidates.
2. Check whether any candidate removes or narrows `colors4_1` while preserving
   local exactness.
3. Submit only if a current-baseline candidate is exact and clears the
   post-regression gate. Otherwise record no-submit and move on.
