# 20260702 case426 result - no-submit task054 index-family narrowing

## Outcome

No submit.

Current baseline:

```text
task054 params=238 memory=26851 cost=27089 n_fail=0 status=ok
```

To clear the post-regression submit gate, `task054` needed about `537` cost
reduction (`gain >= 0.020`). The inspected index-family candidates did not meet
that bar.

## Inspection

Dominant current tensors:

```text
label_f32          float32 [1,1,30,30] 3600B
fill_indices_4_8_4 int64   [4,8,4]     1024B
v_indices_1_1_30_4 int64   [1,1,30,4]   960B
```

`label_f32` is a primary `Conv(input, label_conv_w)` value path and was not
touched. The intended safer target was the derived index family:

```text
fill_indices_4_8_4 -> GatherND(cleared_label, ...) and ScatterND(cleared_label, ...)
v_indices_1_1_30_4 -> ScatterElements(h_line_u8, ..., axis=3)
```

## Gate Results

Known and scratch candidates against the current baseline:

```text
/private/tmp/neurogolf_nextcycle_task054/task054_i32_scatter_indices.onnx
  unscorable: Type Error, output expected int32 but producer still int64

/private/tmp/neurogolf_nextcycle_task054/task054_i32_scatter_indices_v2.onnx
  unscorable: GatherND rejects int32 indices in this opset/runtime

/private/tmp/neurogolf_case406_output_alias/task054_197_Where_seed_2_safe_col_i64_TO_seed_2_col_i64.onnx
  decision=bank-low-gain base=27089 cand=27081 gain=0.0002953664 n_fail=0

/private/tmp/neurogolf_case415_safe_surgery/work/task054.onnx
  decision=blocked-no-gain base=27089 cand=43269 gain=-0.4683087018 n_fail=0
```

## Decision

Reject for submission. Full index-family int32 narrowing is blocked by
`GatherND`/runtime type constraints, and the only exact alias is an 8-cost
bank-only cleanup.

## Next

Do not keep forcing `task054` unless a larger representation rewrite appears.
The parallel current-graph scan found better local relative-gain candidates:

```text
task192 cost=8621: one full 30x30 derived tensor is about 10.4%
task202 cost=11277: one full 30x30 derived tensor is about 8.0%
```

Start with `task192` because its duplicated `Where`/mask family is smaller and
more local than the repeated flood-fill or high-cost branch families.
