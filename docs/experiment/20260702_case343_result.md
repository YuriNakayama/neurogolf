# 20260702 case343 result - submit-saving low-risk batch check

## Result

No Kaggle submission.

The current verified low-risk cleanup batch is still below the submit-saving
threshold:

```text
low-risk batch gain ~= 0.0214 to 0.0220
required low-risk batch gain >= 0.030
```

Submitting this batch now would spend a Kaggle submit on changes that prior
history suggests are often below displayed Public Score movement. The exact
micro candidates remain banked in `micro_backlog.md`.

## Verified Low-Risk Candidates

The scan found only known or already banked cleanup material:

| task | transformation | local cost | gain | risk |
|---|---|---:|---:|---|
| 334 | duplicate initializer `probe_wzp -> probe_zp` | `131 -> 130` | `0.007663` | low |
| 052 | duplicate initializer `tailpack_052_wzp -> tailpack_052_xzp` | `209 -> 208` | `0.004796` | low |
| 003 | duplicate initializer `probe_wzp -> probe_zp` | `263 -> 262` | `0.003810` | low |
| 361 | duplicate/unused initializer cleanup | `4787 -> 4779` | `0.001673` | low |
| 325 | duplicate initializer bundle | `1932 -> 1929` | `0.001554` | low |
| 023 | duplicate initializer bundle | `11222 -> 11212` | `0.000892` | low-medium |
| 222 | unused initializer prune | `7597 -> 7595` | `0.000263` | low |
| 117 | duplicate initializer cleanup | `4158 -> 4157` | `0.000241` | low |
| 206 | constant/broadcast params cleanup | `4184 -> 4183` | `0.000239` | low |
| 066 | duplicate initializer cleanup | `16838 -> 16835` | `0.000178` | low |
| 379 | unused initializer prune | `10774 -> 10773` | `0.000093` | low |

All listed candidates were verified with `audit_one` and `n_fail=0` by the
parallel low-risk cleanup scan.

## Rejected Local Direction

`task118` has a large final `channel_indices` initializer:

```text
channel_indices int64[1,2,25,28] = 11200 bytes
```

A temp candidate replaced the final `ScatterElements(input_u8, channel_indices,
updates8, axis=1)` with channel `Slice`, spatial `Pad`, and final `Concat`.
It remained correct locally but worsened cost:

```text
baseline:  params=4614, memory=34623, cost=39237
candidate: params=3229, memory=43623, cost=46852
n_fail=0
```

The initializer saving is outweighed by the extra intermediate memory, so this
direction is rejected locally and should not be submitted.

## Next Step

Continue searching for one isolated candidate above threshold, preferably a
medium-cost structural cleanup with gain `>= 0.030`, or accumulate enough
low-risk micro cleanups to clear the `0.030` batch threshold.
