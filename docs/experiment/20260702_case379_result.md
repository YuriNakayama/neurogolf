# 20260702 case379 result - skip after equivalence scan

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.88
```

## Scans

Post-case378 graph pattern scan:

- `ArgMax(Neg(x)) -> ArgMin(x)`: no remaining current-bundle hit outside the
  already adopted `task310`.
- `Slice` axes/range shortening: only tiny candidates were found; no credible
  single-task `gain >= 0.020`.
- dtype narrowing: large candidates either add a wider source temporary or are
  known runtime risks.
- kernel crop: apparent zero-border kernels generally need shape/coordinate
  compensation; no proof-grade single-task candidate cleared the gate.

Full current-bundle deterministic surgery was re-run:

```text
uv run python -m pipeline.case3 surgery \
  --base /tmp/neurogolf_case379_base \
  --task-dir ../data/lake/neurogolf-2026 \
  --out /tmp/neurogolf_case379_surgery
```

Result:

```text
surgery done: 1/400 tasks improved
```

Only changed task:

| task | current cost | candidate cost | gain | n_fail | path |
|---:|---:|---:|---:|---:|---|
| 285 | `24535` | `24512` | `0.0009378759836859475` | 0 | `/tmp/neurogolf_case379_surgery/task285.onnx` |

This is the same low-risk micro cleanup class already seen in case377, still
far below the low-risk standalone submit threshold.

## Deferred Candidate

The only non-adopted exact candidate above the numeric threshold remains
`task233` uint8 `TopK` value-path compression:

```text
49454 -> 47804
gain ~= 0.03393
```

It is not low-risk because the same family previously produced Kaggle
`SubmissionStatus.ERROR`; the cast-back repair was exact but worse than
baseline. Keep it deferred until the runtime compatibility issue is solved.

## Conclusion

Skip submit. Current low-risk exact material is only:

```text
task285: gain 0.000938
```

Continue with single-task structural rewrites, but do not spend a Kaggle submit
until a fresh exact low-risk candidate clears `gain >= 0.020` or a re-audited
low-risk batch clears the batch threshold.
