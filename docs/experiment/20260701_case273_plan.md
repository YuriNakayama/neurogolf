# 20260701 case273 plan - task066 red-clear guard alias

## Context

- Current accepted Public Score floor: `7181.66`.
- Scoring priority is still `n_fail=0` first, then relative reduction `old_cost / new_cost`.
- Recent rejected cycles show that params-only micro reductions do not move the Public Score, so this cycle targets memory-producing intermediate tensors.

## Target

- Task: `task066`
- Baseline local cost: `params=150`, `memory=17145`, `cost=17295`
- Prior task066 attempt: case188 only deduplicated shape initializers (`-6` params) and was rejected as too small.

## Hypothesis

The south/down candidate guard computes:

```text
n_s_down_red_bad   = n_red_bad_pref_m - n_red_bad_rrmax
n_s_down_red_clear = Equal(n_s_down_red_bad, zero_f)
n_s_down_no_bad_1  = And(n_h_clear, n_s_down_red_clear)
n_s_down_no_bad_2  = And(n_s_down_no_bad_1, n_s_down_green_clear)
```

An alias probe over all local examples found that replacing `n_s_down_no_bad_1` with `n_h_clear` preserves correctness. This removes the red-clear intermediate chain feeding only that guard.

## Precheck

Temporary candidate `/tmp/task066_alias_115_n_h_clear.onnx`:

- checker/session: ok
- local audit: `params=147`, `memory=16691`, `cost=16838`
- correctness: `n_pass=266`, `n_fail=0`
- relative reduction: `17295 / 16838 = 1.0271`

## Method

1. Replace consumer input `n_s_down_no_bad_1` with `n_h_clear`.
2. Prune newly dead nodes and unused initializers.
3. Run `audit_one` only for `task066`.
4. Submit if `n_fail=0` and cost remains reduced.
5. Adopt only if actual Kaggle Public Score increases.
