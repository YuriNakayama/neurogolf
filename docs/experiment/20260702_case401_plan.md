# 20260702 case401 plan - task335 line-pool selector compression

## Context

Accepted Public Score is `7186.35` after case400.

During the case400 wait window, a targeted next-cycle probe found a strong
current-baseline candidate for `task335`. This is a single-task change with
large relative reduction, so keep it isolated for attribution.

## Candidate

```text
task335 3501 -> 3002 gain 0.1537699087612584
```

Scratch file:

```text
/tmp/neurogolf_case400_nextcycle/task335_einsum_line_pool.onnx
```

## Method

Replace sparse row/column pooling `Conv` weights with compact float `Einsum`
selectors for the same line-pooling result. The change removes 499 parameters
while keeping the measured memory unchanged.

## Local Gate

Current-state re-audit before applying to `data/output/onnx`:

```text
baseline:  params 661, memory 2840, cost 3501, n_pass 266, n_fail 0
candidate: params 162, memory 2840, cost 3002, n_pass 266, n_fail 0
functions=0
forbidden ops=[]
```

Submit only `task335` in this cycle. Do not bundle the tiny `task009`
`8310 -> 8306` cleanup; keep it banked for a later filler batch.

Adopt only if Public Score improves over `7186.35`; otherwise restore case400.
