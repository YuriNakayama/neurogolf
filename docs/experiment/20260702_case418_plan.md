# 20260702 case418 plan - task349/task364 docs-directed recheck

## Context

Accepted Public Score remains `7186.71`. Case417 found no submit-worthy
candidate in `task018`, `task133`, or `task187`.

The next high-cost non-excluded tasks include:

```text
task158 cost=30187
task349 cost=24389
task364 cost=23013
```

`task158` was already exhausted in case368: branch deletions had large gains
but failed correctness, and generic surgery had no gain. Therefore this cycle
uses `task158` only as a reference and focuses on docs-directed checks for
`task349` and `task364`.

## Hypothesis

`task349` and `task364` have high-gain near misses from earlier scans. Most
failed because a propagation/halo branch was structurally required. A current
recheck may reveal a narrow exact residual after later adopted changes, but
only docs-listed candidates should be tested to avoid noisy broad scans.

## Candidate Sources

Targeted directories:

```text
/private/tmp/neurogolf_case353
/private/tmp/neurogolf_case357_alias
/private/tmp/neurogolf_case358_t349
/private/tmp/neurogolf_case358_itercut
/private/tmp/neurogolf_case349_scan
```

## Gate

Submit only a single-task candidate if:

```text
status=ok
n_fail=0
functions=0
forbidden ops=[]
gain >= 0.020
mechanically explainable
```

Otherwise record a no-submit result and keep any exact low-gain candidate
banked.
