# 20260702 case394 plan - isolated task118 residual table pruning

## Context

Accepted Public Score is `7186.22`. Case393 confirmed that the task285 uint8
`TopK` input bypass is Kaggle-runtime incompatible (`SubmissionStatus.ERROR`).

The next strongest current-baseline candidate is task118:

```text
task118 36576 -> 35807
gain ln(36576 / 35807) = 0.02124888259312957
```

Scratch:

```text
/private/tmp/neurogolf_task118_deep4/task118_pos_keep_first_neg_greedy_pos_any_greedy.onnx
```

## Risk

This is table pruning and therefore hidden-sensitive. It is below the high-risk
comfort threshold used earlier, but current low-risk material is below submit
gate and submit count has refreshed. Testing it isolated gives clean
attribution and avoids the case390 ERROR family.

## Gate

Submit only if:

- `task118` audit gives `n_fail=0`
- cost is `35807`
- no forbidden ops and no ONNX functions
- no other ONNX files are changed

Adopt only if Kaggle Public Score improves over `7186.22`; otherwise restore
the accepted bundle and record rejection.
