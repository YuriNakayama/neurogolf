# 20260630 case72 result — bool→uint8 復帰 Cast 横展開は13候補すべて不採用（提出なし）

## independent_impl:
case71 の t349 冗長 Cast 除去を全400タスクへ横展開:
- graph 走査で `Cast(to=BOOL) -> Cast(to=UINT8)` の復帰 Cast を **13候補**検出。
- 各候補について、後段の入力を復帰 Cast 出力から元テンソルへ差し替える独自 probe ONNX を生成。
- `audit_one(run_correctness=True)` で faithful 検証。

## 結果
候補:
- t013: `row_presence_u8`, `col_presence_u8` → n_fail 125/142
- t105: `row_any_u8` → n_fail 259
- t137: `row_any_u8` → n_fail 266
- t148: `label_base_top` → type mismatch（Where の値型不一致）
- t281: `bbox_rows_u8`, `bbox_cols_u8` → n_fail 266/266
- t328: `tl/tr/bl/br_active_u8` → n_fail 180/186/187/176
- t358: `row/col_nonzero_u8` → n_fail 186/184

## 確定
- t349 の `beam_u8` は 0/1 uint8 として値入力に使われる特殊ケースだった。
- 他候補は ArgMax/Gather/Where の意味論上、bool 化後の 0/1 正規化が必要、または元テンソルの値域・型が一致しない。
- 横展開による追加採用なし。floor 7180.81 は維持。

## submit
- 改善候補なしのため提出なし。

## 採否
- 不採用。実装 probe は全て n_fail>0 または型不一致。

## 次の一手
- t349 と同じ「値入力として戻す Cast」ではなく、`Cast(bool)` 自体を不要化できる `Where` / `Equal` / `ArgMax`
  周辺の局所 graph surgery を高 cost task から探索する。
