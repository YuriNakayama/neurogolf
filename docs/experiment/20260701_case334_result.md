# 20260701 case334 result - task319 obj1 row/view alias bundle

## Decision

Adopted.

## Local Gate

Only `task319.onnx` was changed and audited.

```text
baseline:  params=133 memory=21590 cost=21723 n_fail=0 status=ok
candidate: params=133 memory=20386 cost=20519 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(21723 / 20519) ~= 0.0570 pt
```

## Submission

```text
ref=54233294
description=case334 task319 obj1 alias bundle cost 21723 to 20519
status=COMPLETE
publicScore=7184.98
```

Previous accepted floor:

```text
7184.93
```

The Public Score improved by `+0.05`, so the candidate is accepted.

## Diff Summary

The accepted change rewires eight `obj1_*` row/view intermediates to equivalent
earlier tensors and prunes the now-dead alias nodes. The analogous `obj2_*`
aliases were not applied because local structural probes failed correctness.

## Next Step

The next strongest exact candidate from the same scan is `task209`, aliasing
`max_s3` and `max_s4` to `label_f`, with local cost `29483 -> 28683` and
estimated gain `0.0275`. Keep the smaller exact cleanups in
`micro_backlog.md` for future bundles.
