# 20260701 case278 result - adopt task187 enclosed mask alias

## Result

- Target: `task187`
- Baseline: `params=664`, `memory=36150`, `cost=36814`, `n_fail=0`
- Candidate: `params=664`, `memory=34900`, `cost=35564`, `n_fail=0`
- Relative reduction: `36814 / 35564 = 1.0351`
- Submit ref: `54222368`
- Public Score: `7181.80 -> 7181.84`
- Adoption: adopted

## Change

Applied one exact same-task alias:

```text
enclosed_b -> free_b_25
```

Dead-node pruning removed the now-unused enclosed-mask path and stale metadata,
reducing scored memory by 1250 bytes.

## Verification

Individual local audit for the submitted `task187.onnx`:

```text
params=664
memory=34900
cost=35564
n_pass=266
n_fail=0
status=ok
```

DVC was updated and pushed after the Public Score improved.

## Conclusion

Adopted. The real LB moved by `+0.04`, matching the local point gain scale from
`ln(36814 / 35564)`.

## Next Step

Continue the shape-ranked exact alias workflow. Known remaining exact singles
from the case278 scan:

```text
task076: board_code -> color15_h, cost 20036 -> 19586, ratio 1.0230
task209: total_vote_p1_u8 -> total_vote_u8, cost 32052 -> 31651, ratio 1.0127
```

Prefer finding same-task bundles before submitting these singles, but `task076`
is a reasonable fallback if no larger candidate appears.
