# 20260702 case424 plan - task198 label-path probe

## Context

Accepted Public Score remains `7186.71`. Case423 identified `task198` as a
fresh memory-heavy value-path target:

```text
task198 cost=13572
```

The dominant path is expected to be a float label map:

```text
G = Conv(input, W)  float32 [1,1,30,30]
G_u8 = Cast(G)
```

This is similar to other Conv-label tasks, but `task198` has not been recently
exhausted in cases417-423.

## Hypothesis

If the label map can be represented as uint8/bool or replaced by an existing
downstream value path without adding an input-wide cast or a larger int64/ArgMax
chain, memory may fall by enough to clear the `gain >= 0.020` threshold.

## Method

1. Inspect current `task198.onnx` graph, dominant tensors, and consumers of
   `G`/`G_u8`.
2. Try only narrow transformations that keep static shapes and avoid forbidden
   ops.
3. Run `submit gate` on any candidate.

## Gate

Submit only if:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden ops=[]
```

Otherwise record no-submit and the reason.
