# 20260630 case99 result — t219 の階段 block 合成は後段まで必要（提出なし）

## 対象

- `task219.onnx`
- baseline: params **92**, memory **18325**, cost **18417**, faithful `n_fail=0`
- Public Score floor: **7180.89**

## 実施内容

t219 は staircase / 2D逐次系の graph。候補 pattern を作った後、`max_231 -> max_307 -> max_383 -> max_459 -> max_535` で複数 block を累積し、最終 mask から出力を作る。後段 block が rare-case 対応なら削減できる可能性があるため、各段を bypass して確認した。

## profile

memory 上位:

- final `Pad` output `[1,10,30,30]` bool: 9000
- final `Equal` `[1,10,15,10]` bool: 1500
- 各 block の `Pad [21,10]` float: 840
- block input/gather/mul `[15,10]` float: 600
- 各 cumulative `Max [15,10]` float16: 300

## block削減 probe

| 候補 | cost | 結果 |
|---|---:|---|
| final `max_535 -> max_459` | 16389 | `n_fail=7` |
| final `max_535 -> max_383` | 14361 | `n_fail=43` |
| final `max_535 -> max_307` | 12333 | `n_fail=77` |
| final `max_535 -> max_231` | 10055 | `n_fail=179` |
| final `max_535 -> gat_151` | 7086 | `n_fail=265` |
| `max_459 -> max_383`（block384削除相当） | 16389 | `n_fail=43` |
| `max_383 -> max_307`（block308削除相当） | 16389 | `n_fail=77` |
| `max_307 -> max_231`（block126削除相当） | 16139 | `n_fail=179` |
| orientation first 固定 | 17933 | `n_fail=158` |

最終 block 1つだけでも arc-gen に必要。block の累積は全段が正答性に寄与している。

## 採否

- **不採用、提出なし**。
- `data/output/onnx/task219.onnx` は未変更。

## 結論

t219 の後段 block は rare-case ではなく faithful 上必要。現 floor は staircase pattern の段階的合成としてかなり詰まっており、単純な block prune や orientation 固定では改善できなかった。次は `task066` / `task338` / `task101` の params/table 側を狙う。
