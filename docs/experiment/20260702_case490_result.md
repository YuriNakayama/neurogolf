# 20260702 case490 result - no-submit task187 cadence scan

## Decision

No submit.

Accepted Public Score remains:

```text
7186.79
```

## Baseline

```text
task187.onnx
cost=27533
params=1008
memory=26525
status=ok
```

## Scan

Existing unique `task187*.onnx` scratch candidates from `/tmp` and
`/private/tmp`:

```text
candidates=938
positive cost-only candidates=460
```

All positive candidates were gated:

```text
blocked-local-fail=460
submit-candidate=0
bank-low-gain=0
```

Representative failures:

```text
27533 -> 6908   gain=1.382705  n_fail=266  blocked-local-fail
27533 -> 7530   gain=1.296490  n_fail=266  blocked-local-fail
27533 -> 11016  gain=0.916037  n_fail=266  blocked-local-fail
27533 -> 18158  gain=0.416274  n_fail=257-266 blocked-local-fail
27533 -> 23158  gain=0.173045  n_fail=41-266  blocked-local-fail
27533 -> 26283  gain=0.046463  n_fail=4    blocked-local-fail
27533 -> 26908  gain=0.022962  n_fail=4    blocked-local-fail
```

## Parallel Notes

W9 found a tiny exact micro:

```text
task099: 1911 -> 1910, gain=0.000523, n_fail=0
```

This was added to `docs/experiment/micro_backlog.md` rather than submitted,
because it is far below the standalone submit threshold and the current safe
micro pool is too small to justify a cadence bundle.

W10 confirmed the `task243` `n_fail=1` near miss cannot currently be repaired
while preserving a positive cost delta. The cheapest exact repair found was
`22999`, above the accepted baseline `22650`.

## Conclusion

Reject locally. The `task187` scratch pool contains many high relative
reductions, but they remove required reach-chain state. The closest candidates
still fail four validation pairs, so they are repair targets only, not submit
material.
