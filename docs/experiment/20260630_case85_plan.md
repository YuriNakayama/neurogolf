# 20260630 case85 plan — t118 の少数大テンソルと hash 補正を一点検証する

## 背景

- 現 Public best は case84 採用後の **7180.87**。
- t118 は cost **39237**（params 4614 / memory 34623）で高コストだが、node 数は 47 と少ない。
- ルールは色2/5マーカーから色8を生成する非局所補完で、既存 graph は `QLinearConv` / `MaxPool` / `ConvTranspose` / `ScatterElements` / hash 補正を使う。

## 仮説

1. `input_u8` の全域 Cast や `red8`/`gray8` スライスが memory 支配なら、局所 Cast へ寄せる余地があるか確認する。
2. `MaxPool(kernel=7,pads=3)` が過剰なら kernel/pad を小さくできるか確認する。
3. hash 補正 `GatherND`/`MatMul`/`ScatterElements` が一部不要なら削れるか確認する。

まず trace で memory 上位を確認し、temporary ONNX で `audit_one` の full train/test/arc-gen 完全一致を行う。

## 対象

- `data/output/onnx/task118.onnx`

## 足切り

- `src.evaluate.scorer.audit_one` で t118 の train/test/arc-gen 全ペア完全一致。
- n_fail=0 かつ cost 減少の場合のみ submit。

## 採否

- Public Score が 7180.87 を上回れば採用。
- 上がらない、または faithful で不一致なら破棄。
