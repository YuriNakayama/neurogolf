# 20260702 case389 result - low-risk micro batch

## Summary

Adopted.

```text
Kaggle ref: 54248778
Public Score: 7186.11 -> 7186.15
combined local gain: 0.035230
```

## Changes

Submitted four previously banked exact micros as one low-risk batch:

```text
task009   8410 ->  8310  gain 0.011962
task340  10994 -> 10918  gain 0.006937
task374   2304 ->  2284  gain 0.008718
task382   5670 ->  5627  gain 0.007613
```

Combined:

```text
gain = 0.03522985952480256
```

This cleared the low-risk batch gate and avoided spending separate submits on
sub-threshold micros.

## Local Gate

All changed tasks passed individual `audit_one`:

```text
task009 n_fail=0 cost=8310 forbidden=[] functions=0
task340 n_fail=0 cost=10918 forbidden=[] functions=0
task374 n_fail=0 cost=2284 forbidden=[] functions=0
task382 n_fail=0 cost=5627 forbidden=[] functions=0
```

## Decision

Adopt. Public Score increased by `+0.04`, confirming that low-risk micros can
be accumulated and submitted as a batch when the combined logarithmic gain is
large enough.

## Next

Continue submit-saving mode. The next low-risk batch pool should start with
documented exact candidates that were not included here:

```text
task046 2318 -> 2286 gain 0.013901
task342 1585 -> 1576 gain 0.005694
task037 4746 -> 4720 gain 0.005493
task392 1951 -> 1941 gain 0.005139
```

Together these are about `0.030227`, just over the low-risk batch gate, but
must be re-audited on the current accepted bundle before any submit.
