# 20260702 case386 plan - task023 broad bundle decomposition

## Context

Accepted Public Score is `7186.03` after case385 adopted the single
`task023` rewire:

```text
task023: 11212 -> 10924, gain 0.026022
```

The same producer scan found a much larger local-exact `task023` bundle:

```text
pre-case385 baseline: 11212 -> 9340
gain 0.182678
scratch /tmp/neurogolf_case385_producer/task023_bundle_19.onnx
```

This is too broad to submit blindly. Broad local-equivalence bundles have caused
hidden regressions before.

## Hypothesis

Some of the remaining `task023` rewires inside the 19-rewire bundle are
compatible and lower-risk after the accepted `asgs0 -> a10` change. A smaller
subset may still clear the standalone gate from the current baseline:

```text
current task023: cost 10924
target for gain >= 0.020: cost <= floor(10924 / exp(0.020)) = 10707
```

## Method

1. Treat current `data/output/onnx/task023.onnx` as baseline.
2. Re-run unary/value-path bypass scans only on `task023`.
3. Test compatible bundles greedily from exact single rewires, but avoid
   automatically applying the whole 19-rewire set.
4. Compare with `/tmp/neurogolf_case385_producer/task023_bundle_19.onnx` only
   to understand available reductions.
5. Submit only a smaller isolated subset if it clears `gain >= 0.020`.

## Gate

Submit if the candidate is exact and either:

- a narrow subset clears `gain >= 0.020`, or
- the only submit-worthy candidate is the broad bundle and its risk is justified
  by the much larger isolated gain.

Otherwise bank the exact subset candidates and skip submit.
