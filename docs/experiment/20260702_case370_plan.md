# 20260702 case370 plan - task244 small-grid relative gain probe

## Context

Current accepted Public Score is `7185.86` after case369. The accepted task205
change showed that integer-valued patch computation left in float16 can still
produce a material relative gain:

```text
task205: 12494 -> 11550
gain = ln(12494 / 11550) = 0.078563
```

The scoring rule is logarithmic, so this cycle again prioritizes relative
reduction over absolute cost. Functional correctness remains a hard gate.

## Target

Primary target: `task244.onnx`.

Current local score:

```text
cost=1179
```

Why this task:

- It contains a compact `GridSample` renderer driven by small row/column tables.
- The graph has float16 row/column grid intermediates and a float sampled patch,
  but only a 4x4 output window. A small absolute saving can still be a useful
  relative gain on this task.
- Case170 already rejected broad geometry simplifications, so this cycle avoids
  semantic branch deletion and only checks representation-level compression:
  dtype, table shape, unused initializer/node cleanup, and safe direct grid
  construction.

Fallback/parallel target: `task396.onnx`.

Task396 has a `GridSample` recolor graph, but prior cases already rejected
several semantic shortcuts. A parallel read-only probe will only look for the
case369-style integer-valued dtype opportunity.

## Method

1. Keep `data/output/onnx` unchanged while probing.
2. Build temporary task244 candidates under `/tmp`.
3. Test one representation change at a time:
   - remove unnecessary `Expand` if broadcasting or table reshape can feed
     `Concat`/`GridSample` directly;
   - compress row/column tables or grid construction without changing sampled
     coordinates;
   - replace float geometry accumulation with smaller integer/uint8 equivalent
     only if ONNX type constraints allow it;
   - avoid `GridSample -> Slice` unless all boundary semantics are provably
     unchanged.
4. Run `evaluate.audit_one` only for `task244`.

## Submit Gate

Use the submit-saving policy:

```text
low risk:    submit if gain >= 0.020, otherwise bank
medium risk: submit if gain >= 0.030, otherwise bank
high risk:   submit only if gain >= 0.080 and isolated
```

Because task244 is small, a low-risk representation cleanup may clear the
standalone gate with only a few dozen bytes saved. If the best exact candidate
is below threshold, record it in `micro_backlog.md` and skip submit.
