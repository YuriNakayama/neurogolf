# case213 plan: task376 modulus index trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task376` は未着手で、baseline は `cost=186` (`params=42`, `memory=144`, `n_fail=0`)。
- `height_offset` で `moduli=[7,8,9,11]` から modulus を選び、`row_idx = packed % modulus` で output row を gather する。

## 対象

- `data/output/onnx/task376.onnx`
- `Slice -> ReduceSum -> Cast -> Gather(moduli) -> Mod(packed, modulus) -> Gather(input, row_idx)` の短い index graph。

## 仮説

実データ上で `height_offset` または `modulus` が固定に近ければ、Slice/ReduceSum/Gather の一部を削れる。`row_idx` 全体を固定すると params が増える可能性があるため、cost も合わせて確認する。

## 検証

- temp ONNX で `modulus -> fixed 7/8/9/11`、`height_offset -> fixed 0..3`、`row_idx -> fixed` を probe する。
- `src.evaluate.scorer.audit_one` で `task376` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
