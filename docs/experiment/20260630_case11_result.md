# 20260630 case11 result — tiny-output タスクは全て複雑選択規則、単純集約では解けず（提出なし）

## 結論（提出なし、floor 7180.58 維持）

tiny-output(≤3×3) 高コストタスクに単純集約規則 novel solver を試行 → 全て不一致:

| task | output | cost | 最良単純規則 | 一致率 |
|---|---|---|---|---|
| t355 | 1×1 | 2755 | noise-neighbor-majority | 233/267（不一致）|
| t048 | 1×1 | 1146 | max_color | 186/270 |
| t346 | 1×1 | 1254 | max_color | 144/267 |

- t355 は「4象限ブロック上に散布された noise 色マーカーを含む領域の色を返す」複雑な空間規則。
  noise-neighbor-majority で 233/267 まで近づくが厳密化には領域選択ロジックが必要で、ONNX 化には
  動的位置参照（NonZero=禁止）or 多ノードの集約が要り、floor の 2755 を下回る保証なし。
- t048/t346 も mode/max/min 等の単純集約では厳密一致せず、複雑な選択規則。

## 確定（本セッションの一貫した知見）
- **floor net は分析した全タスクで正答かつ真の規則複雑度に見合う near-minimal コスト**。
  tiny-output でも規則自体が非自明（領域選択・パターン照合）で、単純集約 net では厳密解けない。
- 公開コミュニティが収束・最適化済みのため、measurable タスクでも floor を下回る novel 構成は
  本セッションの探索範囲（colormap/幾何/tiling/mirror/flood-fill/k-local/gravity/projection/crop/
  tiny-output 集約）では発見できず。

## 次の一手
- novel solver は「単純規則 × 過剰 net」の前提が成り立つタスクが floor に残っていないため、
  現プリミティブ探索では到達困難（cycle13 の旧ログ「145構成/0win」と整合）。
- 唯一の前進: 新規公開バンドル監視 harvest（measurable で floor 未満が出た時のみ採用）。
- floor 7180.58 維持。7950 まで −769.4。
