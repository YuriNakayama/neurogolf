# 20260702 case431 result - no-submit task133 branch-family probe

## Outcome

No submit.

Current baseline:

```text
task133 params=1278 memory=32284 cost=33562 n_fail=0 status=ok
```

## Inspection

Dominant current tensors include:

```text
corr_idx_c                int64  [232,4]          7424B initializer
border_grid_f             float  [1,1,30,30]      3600B
source_at_anchor          uint8  [1,1,30,30]       900B
seed_1..4                 uint8  [1,1,30,30]       900B each
marker_1..4               uint8  [1,1,30,30]       900B each
anchor_1..4               bool   [1,1,30,30]       900B each
corr_overlay/out_grid     uint8  [1,1,30,30]       900B each
```

Prior docs already showed direct marker branch drops are not exact:

```text
drop marker_1: n_fail=180
drop marker_2: n_fail=126
drop marker_3: n_fail=73
drop marker_4: n_fail=26
```

The current accepted graph already includes the small `sg_gate_2/3/4 ->
sg_eqi_2/3/4` cleanup and the later case400 micro bundle.

## Gate Results

Known exact/micro candidates against the current baseline are now no-gain:

```text
task133_bundle_final_33570_to_33562.onnx
  decision=blocked-no-gain base=33562 cand=33562 n_fail=0

task133_bundle_7_shape_plus_sg234.onnx
  decision=blocked-no-gain base=33562 cand=33563 n_fail=0

task133_sg_gate_234_TO_sg_eqi.onnx
  decision=blocked-no-gain base=33562 cand=33570 n_fail=0
```

Branch-removal candidates still fail local correctness:

```text
task133_bypass_Max_out_grid_TO_marker_4.onnx
  33562 -> 32670, gain 0.0269372490, n_fail=267

task133_bypass_Max_out_grid_TO_marker_3.onnx
  33562 -> 32670, gain 0.0269372490, n_fail=267

task133_bypass_Max_out_grid_TO_corr_overlay.onnx
  33562 -> 32670, gain 0.0269372490, n_fail=263

task133_bypass_QLinearConv_marker_4_TO_seed_4.onnx
  33562 -> 32670, gain 0.0269372490, n_fail=26

task133_bypass_Where_seed_4_TO_source_at_anchor.onnx
  33562 -> 32670, gain 0.0269372490, n_fail=263

task133_bypass_ScatterND_corr_overlay_TO_border_grid.onnx
  33562 -> 31742, gain 0.0557537495, n_fail=5
```

## Decision

Reject for submission. The only exact candidates are already adopted or worse
than the current baseline. The branch-family shortcuts still break local
correctness, including the closest marker-4 reduction.

## Next

Do not keep forcing `task133` branch drops. Move to another candidate class:
either `task187` reach-mask near miss, or a fresh current-graph scan for
medium-cost tasks where a removable tensor is not a semantically required
branch.
