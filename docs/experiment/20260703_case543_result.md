# 20260703 case543 result - no-submit task023 follow-up

Accepted Public Score:

- `7187.43`

## Summary

No submit.

After case542 adopted task023 `Sadd4 -> u4`, this cycle refreshed task023
follow-up candidates, the current low-risk micro bank, and scorable non-risk
standalone search.

## Task023 Follow-Up

Current task023 baseline:

```text
cost   : 8548
n_fail : 0
```

Worker A tested `552` rebased old/fresh task023 candidates.

Exact scorable candidates found: `10`.

Positive exact candidates found: `1`.

Best candidate:

```text
path   : /private/tmp/neurogolf_agent_case543_A/fresh_alias/task023_equiv_alias_000_asg3_TO_asgs3.onnx
cost   : 8548 -> 8476
gain   : 0.008459
n_fail : 0
gate   : bank-low-gain
```

This is below the standalone submit threshold `0.020`, so it was not submitted.

## Micro Refresh

Best current max-5 low-risk micro bundle:

```text
task368 6225 -> 6217 gain=0.001285967064
task117 4148 -> 4145 gain=0.000723501780
task333 3227 -> 3225 gain=0.000619962822
task234 6845 -> 6841 gain=0.000584538962
task358 4392 -> 4390 gain=0.000455477120
```

Total gain:

```text
0.003669447747
```

This remains below the `0.015` micro-bundle gate.

## Scorable Standalone Search

Worker C searched the top current-scorable non-risk tasks outside task023.
No exact submit-ready candidate was found.

High-gain local-fail candidates:

```text
task255 22541 -> 16037 gain=0.340437 n_fail=138
task192  8621 ->  6820 gain=0.234342 n_fail=19
task219 14823 -> 11787 gain=0.229183 n_fail=94
```

These are not submit candidates without repair.

## Decision

No submit. Keep accepted Public Score at:

```text
7187.43
```

Carry forward the task023 `asg3 -> asgs3` exact candidate as banked material,
but do not spend a standalone submit on it.
