# 20260702 case462 plan - task340 guard candidate re-gate

## Context

Case461 rejected `task204`: only a one-byte exact cleanup was found. The next
ranked task is `task340`:

```text
params=136
memory=10782
cost=10918
n_fail=0
```

`task340` has mixed history:

- case282 local-exact guard aliases returned Kaggle `ERROR`;
- case389 later adopted a low-risk `task340` micro bundle as part of a
  successful low-risk batch.

Therefore any remaining `task340` candidate must be treated as review-risk if
it touches guard/index paths.

## Method

1. Gather `task340*.onnx` from shallow `/private/tmp` and `/tmp` scratch pools.
2. Cost-screen against current accepted `task340.onnx`.
3. Audit positive reductions for task340 correctness.
4. Do not submit automatically unless the candidate is exact, material, and
   not in the known Kaggle-ERROR guard/index family.

## Gate

```text
n_fail=0
gain >= 0.020
no forbidden ops/functions
no guard/index runtime-risk family
```
