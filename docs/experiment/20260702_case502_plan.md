# 20260702 case502 plan - task363 corr near-miss repair

## Context

Current accepted Public Score:

```text
7187.12
```

The post-case499 breadth scans found no immediate `submit-candidate`. The best
new repair target is `task363`, where two current-baseline candidates have only
one local failure while preserving meaningful relative gain.

## Target

```text
task363.onnx
```

Candidate paths:

```text
/tmp/neurogolf_case501_medium_small/task363_bypass_Add_corr_TO_corr1.onnx
/tmp/neurogolf_case501_medium_small/task363_bypass_Add_corr_TO_corr0.onnx
```

Signals:

```text
4248 -> 3899, gain=0.085728174, n_fail=1
4248 -> 3904, gain=0.084446615, n_fail=1
```

## Hypothesis

The deleted `corr` addition is redundant for almost all examples but handles a
single exception. If the exception is localized to a small mask or one branch,
we may be able to restore only that branch while keeping most of the cost
reduction.

## Method

1. Identify the exact failing pair for both near misses.
2. Inspect graph tensors around `corr`, `corr0`, and `corr1`.
3. Try cheap repairs that reintroduce only the failing correction path.
4. Gate any repaired candidate with `evaluate_candidate_gate`.
5. Submit only if it reaches `n_fail=0` and clears the submit gate.
