# 20260701 case160 plan

## Context

- Current accepted Public Score: 7181.17
- case159 showed `task335` path branches are core rule logic.
- Continue one-task probes, prioritizing relative cost reduction and exact correctness.

## Target

- Task: `task341`
- Baseline audit:
  - params: 35
  - memory: 1394
  - cost: 1429
  - points: 17.735269822070133
  - n_pass: 266
  - n_fail: 0
  - filesize: 3500

## Hypothesis

`task341` computes whether an 8x8 inner bridge should be vertical or horizontal:

- `vertical = any_row_empty & row_edges_clear`
- `horizontal = Not(vertical)`
- vertical and horizontal select different row/column bridge masks before padding and painting cyan.

If public examples only need one orientation or if one of the vertical guards is redundant, replacing the orientation selector can remove several small tensors. The absolute cost is small, so a valid reduction must be judged mainly by relative reduction.

## Method

1. Probe orientation and bridge-mask replacements on temporary `task341` ONNX copies.
2. Run `audit_one` only for `task341`.
3. Apply and submit only if `n_fail=0` and cost decreases materially for this task.
4. If no exact-match reduction exists, write result and commit docs only.
