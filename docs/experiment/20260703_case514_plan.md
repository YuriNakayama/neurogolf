# 20260703 case514 plan - clean micro-bundle from banked exact wins

Current accepted Public Score:

```text
7187.27
```

## Background

Case513 adopted the standalone `task187` reduction and also produced new
bank-low-gain exact candidates:

```text
task092 6872 -> 6807 gain 0.009504
task096 8798 -> 8758 gain 0.004557
task260 2110 -> 2109 gain 0.000474
```

Case512 and earlier sweeps already banked small clean exact reductions such as
`task018`, `task263`, and `task383`. Individually these are too small, but the
metric is logarithmic and micro changes can be submitted as a bundle if the
combined relative/log gain clears the hardened gate.

## Hypothesis

A clean non-risk micro-bundle can be assembled with at most five tasks:

```text
task092
task096
task018
task263
task383
```

Expected combined gain is approximately:

```text
0.009504 + 0.004557 + 0.002744 + 0.001472 + 0.001105 = 0.019382
```

This is above the `0.015` micro-bundle threshold and avoids known hard/review
risk tasks.

## Gate

Submit only if:

```text
decision = submit-micro-bundle
n_fail = 0 for every changed task
changed tasks <= 5
no hard/review/runtime risk members
```

If any member fails current-baseline re-gating, drop it and continue banking
rather than forcing the submit.
