# 20260703 case538 plan - low-fail semantic repair refresh

Accepted Public Score:

- `7187.41`

## Background

Case537 depleted broad current-baseline mechanical/dtype searches. The only
meaningful remaining path is semantic repair of low-fail candidates whose gain
is still large enough after repair.

## Targets

Refresh and repair these non-risk low-fail families:

- `task077`: propagation depth trim (`b2mn/b2mx -> b1mn/b1mx` family). Prior
  candidates had gain `0.058` to `0.120` with `n_fail=4..8`.
- `task102`: fill-core branch shortcut. Prior best was about `2527 -> 2342`,
  gain `0.076`, `n_fail=10`.

These are old candidates, so the first step is current-baseline re-gating. If a
candidate is stale or still fails with costly repairs, reject without submit.

## Method

1. Re-gate existing scratch candidates for `task077` and `task102` against
   current `data/output/onnx`.
2. For the best still-positive low-fail candidate, inspect failure diffs.
3. Try one bounded repair that preserves `gain >= 0.020`.
4. Submit only if the repaired candidate is `n_fail=0`, lower cost than current
   baseline, and non-risk.

No review-risk or hard-risk task is included in this cycle.
