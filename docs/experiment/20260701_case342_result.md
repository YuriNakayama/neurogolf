# 20260701 case342 result - task233 post-TopK bounds gate

## Result

- Previous accepted Public Score: `7185.09`
- New Public Score: `7185.13`
- Kaggle submission ref: `54235832`
- Submission time: `2026-07-01 14:57:47 UTC` (`2026-07-01 23:57:47 JST`)
- Previous accepted submission time: `2026-07-01 14:15:48 UTC`

Accepted.

The submit interval from the previous accepted submission was about 42 minutes,
which fits the new submit-saving cadence of roughly one submit window every 30
minutes when a threshold-clearing candidate is available.

## Local Gate

`task233` only:

```text
baseline:  params=556, memory=50512, cost=51068
candidate: params=520, memory=48934, cost=49454
n_fail=0, status=ok
gain = ln(51068 / 49454) ~= 0.0321
```

The candidate cleared the medium-risk standalone threshold of `0.030`.

## Change

Replaced the full `[1,324]` `scorew_valid` boundary mask with a lighter
post-`TopK` selected-candidate bounds check, then folded compatible exact micro
rewires for strict-valid and absolute-difference intermediates.

This is a small semantic refactor rather than a broad local-equivalence bundle,
and the real Public Score moved by `+0.04`, so the ONNX and DVC state are
adopted.

## Follow-Up

Continue using the submit-saving cadence:

- Submit immediately only for isolated threshold-clearing candidates.
- Otherwise bank exact local micro candidates.
- Low-risk batches need total expected gain `>= 0.030`; mixed-risk batches need
  total expected gain `>= 0.050` and must avoid known hidden-unsafe families.

The parallel `task018` deep inspection returned a stronger local candidate
(`39583 -> 39213`, gain `0.0094`) but remains below standalone threshold and is
banked only.
