# 20260701 case299 result - task023 duplicate initializer cleanup rejected

## Result

- Decision: reject
- Kaggle submission ref: `54226064`
- Public Score: `7183.03 -> 7183.03`

## Local Gate

- Baseline: `params=157`, `memory=11835`, `cost=11992`, `n_fail=0`
- Candidate: `params=147`, `memory=11835`, `cost=11982`, `n_fail=0`
- Cost delta: `-10`
- Point delta estimate: `+0.000834`

## Change Tested

The candidate rewired duplicate initializer pairs:

```text
Ksc -> Ksb
Khc -> Khb
Kvc -> Kvb
```

This was semantically exact locally, but the improvement was too small to move
the Public LB.

## Conclusion

Rejected because the actual Kaggle Public Score did not improve.  The ONNX
bundle was restored with DVC checkout.  Do not spend additional cycles on
single-digit params cleanup unless bundled with a larger same-task structural
change.

## Next Step

Return to structural searches: source-crop, input-crop before pooling/reduction,
or task-specific solver replacement.  Require a materially larger expected
point delta before submitting cleanup-only changes.
