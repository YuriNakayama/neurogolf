# 20260702 case433 result - no-submit task284 ScatterND index dtype recheck

## Outcome

No submit.

Current baseline:

```text
task284 params=653 memory=3082 cost=3735 n_fail=0 status=ok
```

The fresh current-graph scan ranked `task284` highest by relative single-tensor
opportunity:

```text
indices int64 [56,4] = 1792B
```

## Candidate

Changed only the final index cast:

```text
indices_i8 -> Cast(INT64) -> indices
```

to:

```text
indices_i8 -> Cast(INT32) -> indices
```

The first candidate still had stale `value_info` for `indices INT64`, so it
failed type validation before testing `ScatterND`. A second candidate removed
the stale value info and re-ran shape inference, confirming `indices INT32`.

## Gate Result

```text
/private/tmp/neurogolf_case433_task284_indices_i32_no_vi.onnx
  decision=blocked-local-fail
  base=3735
  candidate_cost=None
  status=session_error:
    INVALID_GRAPH: Type 'tensor(int32)' of input parameter of operator
    ScatterND is invalid.
```

## Decision

Reject for submission. `ScatterND` requires int64 indices in this runtime/opset,
so the dominant `indices` tensor cannot be narrowed by dtype.

## Next

Continue the fresh-scan list. The next candidates are:

```text
task355 cost=2715: row_mx/col_mx float [1,10,30], each 1200B
task131 cost=3977: scatter_idx int64 [1,50,4], 1600B
task162 cost=4068: c0_20 float [1,1,20,20], 1600B
```

Prefer `task355` first because one row/col reduction tensor is a very large
relative share and may have a symmetric alias or dtype alternative.
