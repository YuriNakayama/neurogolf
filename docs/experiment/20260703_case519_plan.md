# 20260703 case519 plan - task243 near-miss repair

## Context

case518 found no safe standalone submit candidate, but it exposed high-upside
task243 near misses:

```text
task243 baseline cost=22650
task243 fill27_to_fill26_pruned: 22650 -> 22002 gain=0.029026 n_fail=1
task243 final_fill_17: 22650 -> 16170 gain=0.337002 n_fail=2
task243 final_fill_16: 22650 -> 15522 gain=0.377901 n_fail=3
```

The score function is logarithmic, so repairing one of these candidates would
produce much larger expected gain than another micro cleanup.

## Hypothesis

`task243` is an unrolled flood-fill graph. The near misses prune late
`QLinearConv` + `Where` iterations. Because the best candidate fails only one
public example, the missing behavior may be limited to a rare maximum-distance
case. A small static repair may restore correctness while retaining
`gain >= 0.020`.

## Plan

1. Compare baseline and near-miss outputs for all task243 examples.
2. Identify the failing example geometry and whether the error is only underfill
   from insufficient propagation.
3. Try bounded repairs in `/tmp` only:

```text
start from fill26 candidate and add one cheaper targeted step if possible
or start from fill17/fill16 candidate and add enough late steps only where needed
```

4. Gate any repaired candidate against the current baseline.
5. Submit only if standalone gate returns `submit-candidate`.
