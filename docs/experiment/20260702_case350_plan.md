# 20260702 case350 plan - task118 conservative patch table pruning

## Context

- Current accepted Public Score: `7185.43`
- Last accepted change: case349 low-risk micro cleanup batch
- Last submit time: `2026-07-01 17:40:31 UTC`
  (`2026-07-02 02:40:31 JST`)

The scoring metric is logarithmic:

```text
gain = ln(old_cost / new_cost)
```

Recent scans did not find a safer standalone structural candidate above
`0.030`. The strongest available candidate is `task118`, but it is table
pruning and therefore hidden-sensitive. To keep attribution clean, this cycle
will submit only `task118`.

## Target

`task118`

Current accepted audit:

```text
params=4614, memory=34623, cost=39237
n_fail=0, status=ok
```

Candidate:

```text
/tmp/neurogolf_case350_search/task118_pos_keepfirst.onnx
```

Candidate audit:

```text
params=3782, memory=34207, cost=37989
n_fail=0, status=ok
gain = ln(39237 / 37989) ~= 0.0323
```

## Hypothesis

The positive patch lookup/scatter table contains duplicate scatter targets.
Keeping only the first positive patch entry per target prunes the table while
preserving observed train/test/arc-gen behavior.

This is not treated as proof-grade semantics for hidden cases. It is an
isolated attack candidate because duplicate table pruning can overfit observed
examples.

## Risk Controls

- Do not bundle with `task209` or any micro cleanup.
- Avoid the stronger task118 variants (`36653`, `35807`) because they are
  higher risk.
- Adopt only if Kaggle Public Score improves over `7185.43`.
- If Public Score does not improve, restore the accepted DVC baseline and keep
  task118 table pruning marked hidden-sensitive.

## Gate

Run targeted validation only:

```text
evaluate.audit_one(data/output/onnx/task118.onnx, task118.json, run_correctness=True)
```

Then run packaging dry-run and submit at the next 30-minute cadence window
(`2026-07-02 03:10 JST` or later).
