# 20260702 case479 plan - task202 current scratch re-gate

## Context

Accepted Public Score is `7186.73`. Recent cycles show that weak mixed micro
bundles are unsafe after case475, so continue searching for isolated
submit-grade candidates.

`task202` is a current mid/high-cost task:

```text
task202 cost=11277
params=24
memory=11253
```

It is not currently in the hard/review/runtime risk sets and has a small params
surface with memory-heavy tensors, making current scratch re-gate cheap.

## Target

```text
task202
baseline: data/output/onnx/task202.onnx
task json: data/lake/neurogolf-2026/task202.json
```

## Method

1. Gather `task202*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against current baseline.
3. Correctness-gate positives with `submit.gate.evaluate_candidate_gate`.
4. Submit only an isolated candidate satisfying:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Otherwise record no-submit and bank exact low-gain candidates.
