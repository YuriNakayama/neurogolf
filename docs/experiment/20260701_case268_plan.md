# 20260701 case268 plan - task355 terminal uint8 Pad

## 背景

- Current floor: Public Score `7181.63`
- 優先基準: `n_fail=0` を絶対条件にし、`old_cost / new_cost` の相対削減率を優先する。
- `task355` baseline: `cost=2755` (`params=27`, `memory=2728`, `n_fail=0`)
- graph 末尾に `sel_u8 -> Cast(float) -> Pad(output)` がある。

```text
sel_b -> Cast(uint8) -> sel1d
sel1d -> Unsqueeze -> sel_u8
sel_u8 -> Cast(float) -> sel
sel -> Pad(..., zero) -> output
```

## 対象

- `data/output/onnx/task355.onnx`

## 仮説

`output` は scorer 側で `out > 0` によって判定されるため、float output でなく uint8 output でも
同じ正答になる可能性がある。`Pad` が uint8 入力を受けられるなら、`Cast(sel_u8 -> float)` を削り、
`zero` も uint8 に置き換えられる。

削れる中間は小さいが、`cost=2755` の task では 10 要素 float tensor 1 個でも相対削減率が
約 1% に近い。

## 手順

1. `/tmp` に `task355` の terminal uint8 Pad 版を生成する。
2. `src.evaluate.scorer.audit_one` で `task355` の完全一致と cost を確認する。
3. `n_fail=0` かつ `old_cost / new_cost >= 1.01` なら本体へ反映して submit する。閾値未満なら submit しない。
