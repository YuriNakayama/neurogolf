# 20260702 case434 plan - task355 row/col reduction alias probe

## Context

Case433 closed `task284` because `ScatterND` rejects int32 indices. The next
fresh-scan target is:

```text
task355 params=27 memory=2688 cost=2715 n_fail=0
```

Dominant tensors:

```text
row_mx float [1,10,30] = 1200B
col_mx float [1,10,30] = 1200B
```

These are very large relative to total cost. Removing one would exceed the
submit threshold if exact.

## Hypothesis

`task355` chooses the color whose bounding-box area has the largest missing
count:

```text
row_cnt = ReduceSum(ReduceMax(input, width))
col_cnt = ReduceSum(ReduceMax(input, height))
bbox    = row_cnt * col_cnt
missing = bbox - counts
```

If local examples have symmetric row/col occupancy for the selected object, one
of `row_mx`/`col_mx` may alias the other. If not, the graph is already tight.

## Method

1. Test `row_cnt` from `col_mx`.
2. Test `col_cnt` from `row_mx`.
3. Gate each candidate.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
