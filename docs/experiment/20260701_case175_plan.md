# 20260701 case175 plan

## Context

- Current accepted Public Score: 7181.17
- case174 showed `task278` has no redundant halo/composite stage.
- Continue with short memory-heavy graphs where one branch of a multi-direction
  composition might be exactly removable.

## Target

- Task: `task190`
- Baseline audit:
  - params: 247
  - memory: 2280
  - cost: 2527
  - points: 17.165552939370103
  - n_pass: 266
  - n_fail: 0
  - filesize: 1806

## Hypothesis

`task190` detects corner markers, expands four diagonal rays, combines them with
`Max`, pads the ray mask, and uses it to paint colors from a pooled color mask.

Potential reductions:

- One diagonal ray branch may be redundant for public examples.
- Opposite ray branches may be equivalent after marker splitting.
- `ray30` or `color10_f` may be replaceable by an existing intermediate while
  preserving output shape.

## Method

1. Probe one alias at a time on temporary `task190` ONNX copies.
2. Run `audit_one` only for `task190`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record failed ray and color-mask aliases before moving on.
