# case193 result: task196 propagation tail shortener

## 結果

- 対象: `task196`
- baseline: `cost=4538`, `params=38`, `memory=4500`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

`Less(poolL, blue)` の `poolL` を前段 tensor に差し替え、未使用 tail node を prune した。

| 変更 | cost | n_pass | n_fail |
|---|---:|---:|---:|
| `poolL -> bad4` | 4313 | 260 | 6 |
| `poolL -> pool3` | 4088 | 260 | 6 |
| `poolL -> bad3` | 3863 | 184 | 82 |
| `poolL -> pool2` | 3638 | 184 | 82 |
| `poolL -> bad2` | 3413 | 134 | 132 |
| `poolL -> pool1` | 3188 | 134 | 132 |
| `poolL -> bad1` | 2963 | 64 | 202 |
| `poolL -> pool0` | 2738 | 64 | 202 |
| `poolL -> bad` | 2513 | 11 | 255 |
| `poolL -> blue` | 2288 | 0 | 266 |

## 結論

末尾 1 段だけでも完全一致を失うため、`task196` の 5 段伝播はこの検証集合では必要。実 ONNX は変更していない。

## 次の一手

同じ伝播系でも、単純な段数削減ではなく、`MaxPool`/`Min` の dtype・重複 initializer・Pad/Where 周辺の alias を優先して確認する。
