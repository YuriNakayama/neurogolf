# 20260701 case257 result - task285 color_pad_flat Pad retry は Public 不変で不採用

## 結果

- 対象: `task285`
- baseline: `params=550`, `memory=25080`, `cost=25630`, `n_fail=0`
- candidate: `params=522`, `memory=25080`, `cost=25602`, `n_fail=0`
- 相対削減率: `25630 / 25602 = 1.0011`
- submit ref: `54217287`
- submitted message: `case257 task285 retry zero concat pad cost 25630 to 25602`
- Public Score: `7181.61 -> 7181.61`
- 採否: 不採用

## 差分

case104 と同じく、以下の suffix zero `Concat` を `Pad` に置換した。

```text
before:
Concat(color_idx_flat, bottom_zero_row, axis=0) -> color_pad_flat

after:
Pad(color_idx_flat, pads=[0,30]) -> color_pad_flat
```

`bottom_zero_row (30,)` を削除し、2 要素の int64 pads initializer を追加したため、params は `-28`。

## 結論

現在 floor `7181.61` でも Public Score は上がらなかったため不採用。実 ONNX は baseline に戻す。params-only の `-28` は case255/256 より大きいが、この task では Kaggle 表示に反映されなかった。

## 次の一手

params-only micro retry は一旦止める。次は `old_cost / new_cost` が 1.01 以上見込める memory 中間削減、または同一 task 内で複数中間をまとめて削れる候補に戻る。
