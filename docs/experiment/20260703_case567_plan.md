# 20260703 case567 plan - task148/fresh target after exhausted repairs

Accepted Public Score:

- `7187.57`

## Background

Cases 565 and 566 widened parallel search but produced no submit-ready
candidate. The following families are now low priority because exact repair
restores the removed semantic branch and loses the cost advantage:

```text
task118 positive-scatter removal
task187 final reach-frontier shortening
task379 color20 orientation branch
task074 row-mirror symmetry branch
task076 slot-2 guard removal
```

`task364` remains a known medium-gain near miss:

```text
task364 23013 -> 22133, gain ~= 0.038990, n_fail=21
```

But case548 already found that a guarded exact repair returned to baseline
cost. Therefore it should get only a short confirmation pass.

`task148` still has many scratch candidates under
`/private/tmp/neurogolf_agent_V3/` and is not in the current gate risk sets. The
target ranking from case565 placed it in the medium bucket because it has a
`30x30` label/selector structure and prior `6007 -> 5877` style near misses.

## Hypothesis

The recent submit drought is caused by exhausted repair families and stale micro
inventory. A fresh non-risk selector/label task such as `task148` has a better
chance of producing either:

- an exact medium-gain rewrite, or
- a low-fail candidate whose repair does not restore a full 30x30 branch.

## Method

1. Re-gate existing task148 scratch candidates against current baseline.
2. Rank by `n_fail` first, then relative gain.
3. Attempt one bounded repair on the best task148 low-fail candidate.
4. In parallel, run a short task364 confirmation to avoid redoing an exhausted
   branch.
5. In parallel, run fresh target ranking for non-risk candidates not covered in
   cases 565-566.

Submit immediately only if a candidate satisfies:

```text
n_fail=0
candidate cost < current baseline cost
functions=0
forbidden ops=[]
not a known Kaggle ERROR/regression family
```

