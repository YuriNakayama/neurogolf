# 20260702 case372 result - skip task222 residual kernel check

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.86
```

## Baseline

Target:

```text
data/output/onnx/task222.onnx
```

Current local audit:

```text
params=2919
memory=4676
cost=7595
n_fail=0
```

## Kernel Inspection

The remaining large initializer is:

```text
cw float32[1,10,17,17]
nonzero=9
```

Nonzero taps:

```text
channels 1..9 at kernel position (1,1), values 1..9
```

The 17x17 kernel is not carrying a large dense rule. It is being used as a
crop-at-source Conv: with a 30x30 input and no padding, the 17x17 kernel produces
the required 14x14 code image without materializing an input Slice.

A smaller 1x1/2x2 kernel would require an explicit input crop such as
`input[:, :, 1:15, 1:15]`, adding a `[1,10,14,14]` intermediate. That memory cost
would dominate the saved sparse kernel params.

## Checks

One-file case3 surgery:

```text
surgery done: 0/1 tasks improved
```

No scratch ONNX beats the current task222 graph.

## Conclusion

Skip submit. The current task222 graph is already at the crop-at-source floor for
this representation. Do not retry small-kernel replacement unless a method avoids
the explicit 10-channel Slice intermediate.
