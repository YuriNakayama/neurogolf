# 20260703 case537 result - no-submit current-baseline search

No Kaggle submit.

Accepted Public Score remains:

- `7187.41`

## Summary

This cycle switched from stale scratch repair to direct current-baseline
candidate generation. Four workers plus a main-thread focused surgery scan ran
against current `data/output/onnx`.

## Searches

### Dtype shrink

The dtype worker generated `88` single-initializer shrink candidates and gated
all of them. None were exact cost-lowering candidates.

Top failures were type-binding errors:

- `task029` `oh_vals` `float32 -> float16`: `Einsum` mixed `float/float16`
- `task398` `R` `int32 -> uint8`: `Less` mixed `uint8/int32`
- `task198` `rp1` `int32 -> uint8`: `Mod` mixed `uint8/int32`
- `task219` `k_143` `int64 -> int32`: `Sub` mixed `int32/int64`
- `task280` `zero2_col_i64` `int64 -> int32`: `Concat` mixed `int32/int64`

### Mechanical cleanup

The mechanical worker evaluated `546` retained candidates through
`submit.gate.evaluate_candidate_gate`.

Only known low-gain micros survived:

| task | cost | gain |
|---:|---:|---:|
| 117 | `4148 -> 4145` | `0.0007235018` |
| 234 | `6845 -> 6841` | `0.0005845390` |
| 206 | `4183 -> 4182` | `0.0002390915` |

### Small/medium task search

The small-task worker confirmed the full current micro queue:

| task | cost | gain |
|---:|---:|---:|
| 117 | `4148 -> 4145` | `0.000724` |
| 234 | `6845 -> 6841` | `0.000585` |
| 328 | `6904 -> 6902` | `0.000290` |
| 206 | `4183 -> 4182` | `0.000239` |
| 005 | `6882 -> 6881` | `0.000145` |

Aggregate gain: `0.001982`, below the `0.015` micro-bundle gate.

### Main-thread surgery scan

The main thread ran `pipeline.case3` surgery passes on `137` non-risk tasks with
current cost between `1000` and `6000`. It found only:

- `task117`: `4148 -> 4145`
- `task206`: `4183 -> 4182`

### Alias search and task133 repair

Alias search found no exact lower-cost candidates. The best near miss was:

- `task133`: `33562 -> 31742`, gain `0.055754`, `n_fail=5`

Repair diagnostics:

- Candidate bypasses `ScatterND(corr_idx_c, corr_upd) -> corr_overlay` and feeds
  `border_grid` directly into final `Max`.
- All `262` arc-gen examples pass, but train/test correction-overlay cases fail.
- Best exact restore costs `33570`, worse than current baseline `33562`.

So `task133` is not submit-eligible.

## Decision

No submit. There is no standalone `gain >= 0.020` exact candidate, and the
low-risk micro bundle remains far below `0.015`.

## Next

The broad current-baseline mechanical/dtype passes are depleted. The next cycle
should focus on semantic near misses with small `n_fail` and enough residual
gain after repair, especially candidates whose repair can be expressed as a
small static mask rather than restoring the original expensive subgraph.
