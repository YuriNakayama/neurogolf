# 20260702 case398 result - task157 shape-compatible bundle

## Summary

Adopted.

```text
Kaggle ref: 54252263
description: case398 task157 shape-compatible bundle gain 0.04600
Public Score: 7186.24 -> 7186.29
```

## Submitted Candidate

Applied only the isolated `task157` shape-compatible alias bundle:

```text
task157 local cost: 8163 -> 7796
local gain: ln(8163 / 7796) = 0.046000966823904574
```

Local gate:

```text
status=ok
params=335
memory=7461
cost=7796
n_pass=265
n_fail=0
functions=0
forbidden ops=[]
```

## Deferred Exact Bundles

Keep the next safest batch separate for attribution:

```text
task188 1187 -> 1166 gain 0.017850
task213 1882 -> 1851 gain 0.016609
task219 15060 -> 14838 gain 0.014851
combined gain ~= 0.049310
```

Do not include `task209` in that batch. Its shape-compatible candidate is the
same `28047 -> 27927` change that was included in the case395 batch returning
Kaggle `ERROR`.

## Decision

Adopt. The isolated `task157` bundle moved Public by `+0.05`, consistent with
the local gain.

## Next

Submit the `task188` + `task213` + `task219` shape-compatible batch next, unless
a stronger isolated candidate appears before the cadence window.
