# case293 result: task222 offset crop-at-source Conv

## Summary

- Submission ref: `54224897`
- Previous Public Score: `7182.54`
- New Public Score: `7182.63`
- Decision: adopt

## Local Gate

- Task: `task222`
- Before cost: `8321`
- After cost: `7597`
- Relative reduction: `1.0953`
- Local points: `16.06449128849678`
- Correctness: `n_pass=266`, `n_fail=0`
- Status: `ok`

## Change

Replaced:

`1x1 Conv -> Slice([1:15, 1:15]) -> Cast`

with:

`17x17 sparse Conv -> Cast`

The sparse Conv has the old label weights at kernel offset `(1,1)`, so output coordinate
`(r,c)` reads the old full Conv value at `(r+1,c+1)`. This directly materializes the
`14x14` crop and removes the dead full `30x30` float tensor.

## Conclusion

Accepted. This confirms that offset crop-at-source rewrites are also safe when the
kernel tap is moved to the Slice start coordinate.

## Next Step

Continue through the crop scan list. The next candidates are smaller but proof-grade:

- `task048`: `30x1 -> 8x1` packed row crop,
- `task237`: `30x1 -> 9x1` packed grid crop,
- `task268`: row/col ReduceSum vectors cropped from 30 to 10.
