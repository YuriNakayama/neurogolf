# case206 plan: task083 mirrored scatter index trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task083` は未着手で、baseline は `cost=447` (`params=71`, `memory=376`, `n_fail=0`)。
- 6x8 の bool/int32 tensor を削れれば、小型 task として相対改善が見込める。

## 対象

- `data/output/onnx/task083.onnx`
- 3x4 の background mask を row/col mirror gather で 6x8 に拡張し、dominant color index と `Where(bg_mirrored, 0, color_idx)` で scatter channel を作る。
- `ScatterElements(input, scatter_idx, one_updates)` で mirrored pattern を追加する。

## 仮説

実データ上で mirrored background mask または dominant color が固定に近ければ、`Where`、mirror gather、または color index chain を削れる。`scatter_idx` の固定化は params が増えるため、cost が下がる候補だけ採用対象にする。

## 検証

- temp ONNX で `scatter_idx -> color_idx/zero/fixed`、`bg_mirrored -> true/false`、`color_idx -> fixed color` を probe する。
- `src.evaluate.scorer.audit_one` で `task083` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
