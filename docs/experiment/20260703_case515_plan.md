# 20260703 case515 plan - post-error standalone sweep

Current accepted Public Score:

```text
7187.27
```

## Background

Case514 proved that locally exact micro-bundles can still produce Kaggle
`ERROR`. The next cycle should avoid another blind bundle and return to
standalone search.

## Hypothesis

Independent workers may still find isolated non-risk candidates if the search is
split by task ranges and gated by current accepted baseline.

Targets:

```text
n_fail = 0
not hard/review/runtime risk
standalone gain >= 0.020 preferred
gain >= 0.010 review-only at most
```

## Search

Workers:

```text
S1 tasks 301-400
S2 tasks 001-120
```

Both write only under `/tmp/neurogolf_agent_S*` and do not commit, push, submit,
or modify tracked repo files.

## Submit Rule

Submit only `submit-candidate`. Bank low-gain exact outputs but do not assemble
a new micro-bundle immediately after the case514 bundle ERROR.
