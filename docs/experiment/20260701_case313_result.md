# 20260701 case313 result - adopt task158 mask-a alias

## Result

- Decision: adopt
- Kaggle submission ref: `54229157`
- Previous Public Score: `7183.10`
- New Public Score: `7183.14`
- Public delta: `+0.04`

## Local Gate

`task158.onnx` after the change:

```text
params=2629
memory=29785
cost=32414
n_pass=266
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=2630
memory=31087
cost=33717
n_pass=266
n_fail=0
status=ok
```

Local cost reduction:

```text
33717 -> 32414
old_cost / new_cost = 1.0402
ln(old/new) ~= 0.0399 pt
```

## Change

Rewired the first pair-mask channel:

```text
mask_a_u8 -> nonbg_u8
```

This preserves the downstream two-channel `Concat` while pruning the
`mask_a_bool` Equal/Cast chain and an unused initializer.

## Rejected Probe

The symmetric single alias also passed locally:

```text
mask_b_u8 -> nonbg_u8: cost=32415, n_fail=0
```

but it is mutually exclusive with the adopted change. Applying both channels:

```text
mask_a_u8 -> nonbg_u8
mask_b_u8 -> nonbg_u8
```

collapses the pair input and fails with `n_fail=152`.

## Additional Queued Candidates

Parallel exploration found two submit-worthy candidates after the case313
submission was already sent:

- `task219` residual alias bundle: `17240 -> 15964`, expected `+0.0770`.
- `task174` inside-mask alias: `9326 -> 8978`, expected `+0.0380`.

Both are recorded in `docs/experiment/micro_backlog.md` for the next cycle.

## Conclusion

Adopted. The Public LB movement matches the local logarithmic gain scale. This
confirms that the new gate should allow same-task structural aliases around
`old/new >= 1.02`, and that exact alternatives below or near the gate should be
kept in a backlog instead of discarded.

## Next Step

Use the stronger queued `task219` residual alias bundle as the first case314
candidate, then fall back to `task174` if it does not transfer.
