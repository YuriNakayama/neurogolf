# case202 plan: task186 tiny patch threshold trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task186` は未着手で、baseline は `cost=103` (`params=23`, `memory=80`, `n_fail=0`)。
- 3x3 patch selector を固定または簡略化できれば相対削減率が大きい。

## 対象

- `data/output/onnx/task186.onnx`
- `GlobalLpPool(input)` から `black_count` を取り、3x3 の `thresholds` と比較して `red` mask を作る。
- `Where(red, red_color, black_color)` で 3-channel patch を作り、Pad して出力する。

## 仮説

実データ上で `red` mask の一部または patch branch が固定なら、`Less` / `Where` / `Pad` 周辺を削れる。低 cost なので、fixed patch と red mask alias を中心に確認する。

## 検証

- temp ONNX で `patch -> red/black const`、`red -> fixed mask`、`black_count` / thresholds 周辺 alias を probe する。
- `src.evaluate.scorer.audit_one` で `task186` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
