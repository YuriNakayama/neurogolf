# 20260701 case253 result — task025 TopK slot 4→3 は fail 35 で不採用

## 結果

- 対象: `task025`
- baseline: `params=144`, `memory=11520`, `cost=11664`, `n_fail=0`
- submit: なし
- 採否: 不採用

## probe

`TopK(k=4)` を `k=3` に縮め、slot 由来の tensor を `[1,3,30]` へ落とした。final `slot_projector` は `(21,5)` から `(16,4)` に変更した。

```text
params: 144 -> 103
memory: 11520 -> 8978
cost: 11664 -> 9081
n_pass: 231
n_fail: 35
```

相対削減率は `11664 / 9081 = 1.2844` と大きいが、4 本目 slot が faithful/test/arc-gen の完全一致に必要だった。

## 結論

`task025` の slot 数削減は採用不可。実 ONNX は変更せず、Kaggle submit もしない。

## 次の一手

slot 数削減のような大きい比率候補は引き続き有効だが、TopK rank は必要なケースが多い。次は同じく中 cost で、rank ではなく dtype/final 合成や guard 由来の中間を削れる task を優先する。
