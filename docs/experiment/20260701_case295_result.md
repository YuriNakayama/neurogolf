# case295 result: task173 top-left crop-at-source label Conv

## Summary

- Submission ref: `54225090`
- Previous Public Score: `7182.86`
- New Public Score: `7182.96`
- Decision: adopt

## Local Gate

- Task: `task173`
- Before cost: `17371`
- After cost: `15721`
- Relative reduction: `1.1050`
- Local points: `15.337247322808485`
- Correctness: `n_pass=266`, `n_fail=0`
- Status: `ok`

## Change

Replaced:

`1x1 Conv -> Cast -> Slice([0:25, 0:25])`

with:

`6x6 sparse Conv -> Cast`

The sparse Conv keeps the old label weights at top-left tap `(0,0)`, so it directly
emits the same `25x25` grid used by the solver and removes the dead full `30x30` float
and uint8 label intermediates.

## Conclusion

Accepted. This is another proof that crop-at-source remains the strongest current
optimization family when the deleted tensor is spatially large enough to offset sparse
kernel parameter growth.

## Next Step

Continue with net-cost-aware scans rather than raw crop-size scans. Candidates with tiny
vector crops should be rejected unless they avoid parameter growth.
