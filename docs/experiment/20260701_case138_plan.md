# 2026-07-01 case138 plan

## 方針

- floor: Public Score 7181.15
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task361.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task361.onnx`
- baseline: params 367, memory 4472, cost 4839, points 16.51553663320668, n_pass 266, n_fail 0

## 仮説

`task361` は空きセルから中心を選び、top colors を回転対称位置へ `ScatterElements` する。末尾に `rot1/rot2/rot3_idx_i32` を `Clip(..., 0, 99)` する3箇所がある。

index safety の Clip なので必要な可能性は高いが、実データで回転後 index が常に範囲内なら、3つの Clip と中間をまとめて削れる。1つでも外せれば 16-element index 中間を削れるため、相対削減候補として確認する。

## 手順

1. `/tmp` 上の candidate ONNX で `rot1_safe_i32 -> rot1_idx_i32`、`rot2_safe_i32 -> rot2_idx_i32`、`rot3_safe_i32 -> rot3_idx_i32` を個別に試す。
2. 通るものがあれば組み合わせる。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task361.onnx` に反映する。
4. submit し、Public Score が 7181.15 を上回れば採用、上がらなければ破棄する。
