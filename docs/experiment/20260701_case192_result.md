# 20260701 case192 result

## Summary

- Target: `task279`
- Baseline: params 87, memory 4740, cost 4827, points 16.518019564339507
- Baseline correctness: n_pass 266, n_fail 0
- Decision: reject, no submit

## Probes

All probes were run on temporary `task279` ONNX copies and audited only with
`src.evaluate.scorer.audit_one`.

Propagation tail aliases:

- `fl_1 -> nb_1`: cost 4571, n_fail 266
- `fl_1 -> fl_0`: cost 4315, n_fail 2
- `fl_1 -> nb_0`: cost 4059, n_fail 266
- `fl_1 -> core_4`: cost 3803, n_fail 234
- `fl_1 -> core_3`: cost 3547, n_fail 234
- `fl_1 -> core_2`: cost 3291, n_fail 239
- `fl_1 -> core_1`: cost 3035, n_fail 261
- `fl_1 -> core_0`: cost 2779, n_fail 266
- `fl_1 -> blue`: cost 2523, n_fail 266

## Analysis

The final cleanup step is nearly but not fully redundant: `fl_1 -> fl_0` passes
264/266 examples and would reduce cost by 512, but the task scoring is
all-or-nothing and the two failures make it unusable. Earlier propagation cuts
fail many examples.

## Conclusion

No exact cost-reducing variant was found. The baseline ONNX was unchanged and no
Kaggle submission was made.

## Next

Look for propagation chains where the final step is provably redundant on all
examples, not just nearly redundant. The `fl_1 -> fl_0` near miss is useful as a
pattern but cannot be adopted.
