# 20260630 case91 result — t054 の seed branch は全て必要（提出なし）

## 対象

- `task054.onnx`
- baseline: params **238**, memory **26851**, cost **27089**, faithful `n_fail=0`
- Public Score floor: **7180.87**

## 実施内容

`task054` は背景/host 色を抽出し、参照 motif を選んで最大4 seed の水平・垂直 line update と fill を適用する graph。memory 上位は final one-hot `Equal`（9000）、初期 label `Conv`（3600）、fill index `Concat`（1024）、vertical scatter index `Expand`（960）。

### seed branch 削減 probe

| 候補 | 結果 |
|---|---|
| seed 3 の horizontal/vertical update を 0 固定 | `n_fail=62` |
| seed 2/3 の horizontal/vertical update を 0 固定 | `n_fail=193` |
| horizontal update 全削除 | `n_fail=180` |
| vertical update 全削除 | `n_fail=167` |
| fill の seed 2/3 を inactive 固定 | `n_fail=164` |

seed 2/3 は optional に見えるが、arc-gen を含む faithful では全て必要。水平・垂直 line も片側削除不可。

### final output probe

- final `Equal(output_label, color_values)` を省いて label grid を直接 output:
  - `n_fail=266` / `unscorable`
  - t209 と同様、採点側は `[1,10,30,30]` one-hot bool 出力を要求する。

### metadata prune

- `value_info` は 285 個あり、削除で filesize **39701→27359**。
- ただし faithful cost は **27089 のまま**で、Public Score 改善には寄与しない。
- ONNX 差し替えは行わない。

## 採否

- **不採用、提出なし**。
- 正答性を保って cost を下げる候補は見つからなかった。
- `data/output/onnx/task054.onnx` は未変更。

## 結論

t054 は公開 bundle で byte 級まで golf 済みで、seed 展開と final one-hot は score 上必要。次は `task285` / `task367` / `task349` など、未再検証の高 cost graph に移る。
