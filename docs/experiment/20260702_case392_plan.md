# 20260702 case392 plan - isolated task023 broad residual

## Context

Accepted Public Score is `7186.15` after case389. Case390 returned Kaggle
`ERROR`, and case391 found that the no-warning recovery subset no longer clears
the submit-saving gate on current baselines.

Current accepted `task023`:

```text
cost 10060
n_fail 0
```

The previously banked broad producer bundle from case385 is still locally exact
when audited directly against the current examples:

```text
scratch: /tmp/neurogolf_case385_producer/task023_bundle_19.onnx
cost 9340
n_fail 0
gain ln(10060 / 9340) = 0.07426091243084182
```

No forbidden ops or ONNX functions were present in the scratch model.

## Hypothesis

Although broad task023 value-path simplification is hidden-risky, it is now the
strongest available current-relative candidate. Submitting it alone gives clean
attribution and avoids mixing it with unrelated micros or the case390 ERROR
family.

## Method

1. Copy only `/tmp/neurogolf_case385_producer/task023_bundle_19.onnx` to
   `data/output/onnx/task023.onnx`.
2. Re-run `evaluate.audit_one` for `task023` only.
3. Verify no forbidden ops and no ONNX functions.
4. Submit only after the 30-minute cadence from the previous Kaggle submit is
   reached.

## Gate

Submit if:

- `task023` remains `n_fail=0`
- cost is `9340`
- current-relative gain remains `0.0742609`
- no forbidden ops and no ONNX functions
- no other ONNX files are changed

Adopt only if Kaggle Public Score improves over `7186.15`; otherwise restore
the case389 accepted bundle and record rejection.
