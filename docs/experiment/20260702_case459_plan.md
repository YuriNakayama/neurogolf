# 20260702 case459 plan - near-miss repair triage

## Context

Cases452-458 show the same failure pattern: high-gain candidates are mostly
semantic removals with `n_fail>0`; generic low-risk surgery is exhausted or
too small. The next useful search shape is repaired near misses, but only when
the failing examples imply a cheap exception rather than reintroducing the
deleted branch.

## Scope

Triage three known near-miss families:

```text
task077 b2 propagation depth trim
task110 public row-table selector trim
task187 reach-depth trim
```

## Method

1. Re-read prior experiment logs and current baseline costs.
2. Use subagents for `task110` and `task187` so the repair assessment runs in
   parallel.
3. Keep the main thread on `task077`, using the documented current-baseline
   failure table.
4. Choose a next implementation target only if a repair can plausibly keep
   cost below baseline and reach `n_fail=0`.

## Gate

No submit from triage alone. A candidate must be implemented and individually
audited before staging.
