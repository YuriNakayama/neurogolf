# 20260630 case101 plan - t157 ch1_active10 のゼロ行 Concat を Pad 化

## 現状
- floor/Public: 7180.95
- 対象: `task157`
- baseline faithful: cost 8334, params 351, memory 7983, n_fail 0

## 仮説
`task157` の終端付近では、配置済み active mask `ch1_active_pre` `(6,15)` に all-zero bool initializer `zeros_ch1_rows` `(4,15)` を `Concat(axis=0)` し、`ch1_active10` `(10,15)` を作っている。

これは suffix 方向のゼロ padding なので、`Pad(ch1_active_pre, pads=[0,0,4,0], false)` に置換できる可能性がある。

## 手法
1. `Concat ch1_active_pre + zeros_ch1_rows -> ch1_active10` を 1 個の `Pad` に置換する。
2. `zeros_ch1_rows` initializer を削除する。
3. `src.evaluate.scorer.audit_one` で `task157` の完全一致を確認する。
4. `n_fail=0` かつ cost 減なら Kaggle submit し、Public Score で採否を決める。

## 足切り
- 変更対象は `task157` のみ。
- 全数 `audit_dir` は使わず、`audit_one` のみ行う。
