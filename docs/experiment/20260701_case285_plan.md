# 20260701 case285 plan - task023 square branch alias bundle

## Context

- Current adopted Public Score floor: `7182.02`
- Latest adopted cycle: case284 `task173` bounds/slot aliases (`17646 -> 17371`)
- Candidate source: parallel explorer scan over low/mid-cost older tasks.
- Priority rule: choose exact candidates by relative reduction.

## Target

- `data/output/onnx/task023.onnx`

## Baseline

Current local audit:

```text
params=157
memory=12843
cost=13000
n_fail=0
```

## Hypothesis

The fourth square branch contains several predicates that are equivalent to the
existing `u4` tensor, and its selected addend is equivalent to `sb4` on all valid
examples:

```text
nSq4      -> u4
nt14      -> u4
nt4       -> u4
isone4    -> u4
sqpos4    -> u4
fsq4      -> u4
Sadd4_sel -> sb4
```

Rewiring these should prune the branch-local horizontal/vertical checks and
selector chain, including the `Sadd4_a` input that consumed `Sadd4_sel`.

## Expected Gain

- Baseline: `13000`
- Candidate from temporary audit: `11992`
- Relative reduction: `13000 / 11992 = 1.0841`
- Expected point gain: `ln(13000 / 11992) ~= 0.0807`

This is currently the strongest available exact candidate after case284.

## Verification Plan

1. Apply only the seven aliases above.
2. Dead-prune unused nodes, initializers, and value_info.
3. Run individual `audit_one` for `task023` with
   `data/lake/neurogolf-2026/task023.json`.
4. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
