# 20260702 case436 result - no-submit task162 source-slice probe

## Outcome

No submit.

Current baseline:

```text
task162 params=44 memory=4024 cost=4068 n_fail=0 status=ok
```

Current graph:

```text
Slice(input, [0,0,0,0] -> [1,1,20,20]) -> c0_20 float [1,1,20,20]
Cast(c0_20) -> c0_u8
QLinearConv(c0_u8, det_weight) -> score18
MaxPool(score18) -> block20_score
Greater -> Pad -> Where(block30, blue_pixel, input)
```

The source slice is already channel 0 only:

```text
slice_starts = [0,0,0,0]
slice_ends   = [1,1,20,20]
```

## Candidate

Tried bypassing the cast:

```text
QLinearConv(c0_20, ...)
```

Gate result:

```text
/private/tmp/neurogolf_case436_task162_qconv_from_c0_20.onnx
  decision=blocked-local-fail
  status=session_error:
    INVALID_GRAPH: Type 'tensor(float)' of input parameter of operator
    QLinearConv is invalid.
```

## Decision

Reject for submission. `QLinearConv` requires quantized input, and the 20x20
float slice is already the minimal channel/spatial slice needed before casting
to uint8. This confirms the older progress note that `task162` is already at
the entry-surface lower bound.

## Next

Continue the fresh-scan list with non-Scatter, non-source-slice candidates.
The next practical choices are small `Pad`-dominated tasks such as `task070`,
`task102`, `task190`, or `task062`, where a full 30x30 output mask may be
terminal and probably not removable, but a micro or exact Pad simplification may
exist.
