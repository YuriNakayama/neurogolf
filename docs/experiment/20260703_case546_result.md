# 20260703 case546 result - no submit after post-task023 search

Accepted Public Score:

- `7187.46`

## Summary

No submit.

After case545 adopted the `task023` combined alias, the remaining low-risk
current-baseline micro backlog is too small for the micro-bundle gate:

```text
task368 6225 -> 6217  gain 0.001286
task117 4148 -> 4145  gain 0.000724
task333 3227 -> 3225  gain 0.000620
task234 6845 -> 6841  gain 0.000585
task358 4392 -> 4390  gain 0.000455
total gain: 0.003669
gate target: 0.015000
```

## Local Work

Current-baseline duplicate-initializer coalescing found only two exact
non-risk candidates:

```text
task117 4148 -> 4145  gain 0.000724
task206 4183 -> 4182  gain 0.000239
```

The standalone worker's guarded surgery/optimizer sweep added one more useful
exact micro:

```text
task205 11547 -> 11539  gain 0.000693
```

Its artifact is:

```text
/private/tmp/neurogolf_agent_case546_B/candidates/task205_surgery_11547_to_11539.onnx
```

Current-baseline metadata/value_info/unused-initializer pruning changed `365`
candidate files, but none passed as a cost-reducing exact candidate.

Cost-only artifact mining produced many apparent high-gain candidates, but the
top full-gated candidates failed local correctness. Representative failures:

```text
task324 8598 -> 2959  n_fail=266
task215 750  -> 332   n_fail=115
task129 80   -> 40    n_fail=265
task349 24369 -> 12620 n_fail=267
task084 2038 -> 1198  n_fail=175
task202 11276 -> 6716 n_fail=230
task358 4392 -> 3135  n_fail=265
task206 4183 -> 3026  n_fail=252
```

Same-task stacking also did not find a submit-ready candidate. Best clean stack:

```text
task117 4148 -> 4145  gain=0.000724
```

Near-threshold/high-gain stack attempts failed correctness:

```text
task234 gain=0.021709 n_fail=154
task198 gain=0.020365 n_fail=160
task154 gain=0.024575 n_fail=63
task324 gain=0.027115 n_fail=61
task005 gain=0.164864 n_fail=257
task382 gain=0.047494 n_fail=70
task364 gain=0.038990 n_fail=21
task076 gain=0.031978 n_fail=4
```

## Decision

Do not submit. No candidate met either submit rule:

- standalone gain `>=0.020` with `n_fail=0`
- at most five low-risk changed tasks with total gain `>=0.015`

Keep accepted Public Score at:

```text
7187.46
```

## Next Step

The remaining exact micro backlog needs a new larger exact candidate to become
submit-worthy. The next cycle should target a repaired near-miss with small
`n_fail` rather than broad cost-only branch bypasses, because the latter are
mostly correctness false positives.
