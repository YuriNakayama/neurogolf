# 20260702 case466 result - task066 score alias follow-up

## Summary

No submit.

`task066` was the best follow-up because same-task score aliases previously
transferred to Kaggle. On the current accepted graph, however, the remaining
cost-reducing material and mid-gain candidates all fail local correctness.

## Baseline

```text
task066
params=142
memory=16209
cost=16351
points=15.2979556635
n_pass=266
n_fail=0
status=ok
```

## Search

The first full correctness pass over all matching candidates was too slow, so
the gate was split into:

1. cost-only scan over all `task066*.onnx` scratch candidates;
2. correctness audit for all `gain >= 0.020` material reductions;
3. correctness audit for all `0.010 <= gain < 0.020` mid reductions.

Cost-only scan:

```text
candidates=1519
positive_cost_reductions=83
errors=0
```

Material correctness gate:

```text
material_candidates=44
exact_positive=0
submit_candidates=0
blocked=44
errors=0
```

Mid-gain correctness gate:

```text
mid_candidates=32
exact_mid=0
blocked_mid=32
```

## Representative Blocked Candidates

```text
16351 -> 2927   gain=1.720311  n_fail=266
  hidden4 -> cyan_b4

16351 -> 3728   gain=1.478417  n_fail=266
  hidden_any -> cyan

16351 -> 8423   gain=0.663323  n_fail=266
  broad hidden/selection alias

16351 -> 14753  gain=0.102843  n_fail=266
  n_selected_2 -> n_green_path

16351 -> 15635  gain=0.044777  n_fail=129..266
  hidden_any/cyan_norm yes aliases

16351 -> 15874  gain=0.029607  n_fail=266
  n_selected_1 -> n_red_path

16351 -> 16035  gain=0.019515  n_fail=129..266
  hidden_any/cyan_norm no aliases
```

## Decision

Rejected for submission. No candidate met:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

The case409 score/edge gain appears exhausted on the current graph. The
remaining large reductions are broad selection/hidden-path deletions that
destroy task semantics.

## Next

Move to `task145` as the next high-cost normal-risk task, but do not repeat the
old source/overlay branch aliases from case199. Inspect only selector/table or
dtype-boundary reductions.
