# 20260702 case465 plan - task096 selector path re-gate

## Context

Cases 460-464 closed the current high-cost normal-risk targets
`task025`, `task204`, `task340`, `task074`, and `task350` as no-submit. The
common failure was semantic branch deletion: large relative reductions existed,
but every material candidate failed local correctness.

The next target should change failure surface while still following the
relative-gain rule:

```text
gain = ln(old_cost / new_cost)
```

## Target

```text
task096
current rank cost=8818
ops: ArgMax/Where/Add/Sub/ScatterElements selector and radius paths
known exact micro: length_updates -> length_u8, gain about 0.000567, bank-only
```

`task096` is not in the current known-risk gate lists, and a fresh case382
scratch pool contains many selector, length, radius, and color bypass
candidates. This is a better next probe than repeating span/symmetry aliases.

## Hypothesis

Most large bypasses will be semantic, but a selector or dtype cleanup may
preserve correctness while removing an over-wide intermediate. A material exact
candidate should clear the submit threshold; an exact micro should be banked,
not submitted alone.

## Method

1. Audit accepted `data/output/onnx/task096.onnx`.
2. Gather current `task096*.onnx` candidates from `/private/tmp` and `/tmp`.
3. Run `submit.gate.evaluate_candidate_gate` against
   `data/lake/neurogolf-2026/task096.json`.
4. Submit only if a candidate is:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

5. If only bank-low-gain exact candidates appear, record them in the result and
   keep accumulating; do not force a standalone submit.
