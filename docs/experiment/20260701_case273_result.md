# 20260701 case273 result - adopt task066 red-clear guard alias

## Result

- Target: `task066`
- Baseline: `params=150`, `memory=17145`, `cost=17295`, `n_fail=0`
- Candidate: `params=147`, `memory=16691`, `cost=16838`, `n_fail=0`
- Relative reduction: `17295 / 16838 = 1.0271`
- Submit ref: `54221536`
- Public Score: `7181.66 -> 7181.69`
- Adoption: adopted

## Change

The candidate rewired:

```text
n_s_down_no_bad_2 = And(n_s_down_no_bad_1, n_s_down_green_clear)
```

to:

```text
n_s_down_no_bad_2 = And(n_h_clear, n_s_down_green_clear)
```

This made the red-clear branch dead:

```text
n_s_down_red_bad   = n_red_bad_pref_m - n_red_bad_rrmax
n_s_down_red_clear = Equal(n_s_down_red_bad, zero_f)
n_s_down_no_bad_1  = And(n_h_clear, n_s_down_red_clear)
```

Dead-node pruning removed the unused chain and its unused shape initializer references.

## Verification

Individual local audit for the submitted `task066.onnx`:

```text
params=147
memory=16691
cost=16838
n_pass=266
n_fail=0
status=ok
```

DVC was updated and pushed after adoption.

## Conclusion

Adopted. This is the kind of memory-producing guard alias that survives the real Public LB: it removes several intermediate tensors while preserving all local train/test/arc-gen examples.

## Next Step

Continue the same alias-probe strategy on memory-heavy tasks, but require either:

- local `n_fail=0` and `old_cost / new_cost >= 1.01`; or
- multiple same-task exact aliases bundled into a comparable reduction.

Promising follow-ups from the same scan:

- `task338`: `tr_pair -> tr_pair_0` or `bl_pair -> bl_pair_0`, `cost 17939 -> 17819`, local exact but only `1.0067`.
- `task255`: `r_full_interior -> r_full_center`, `cost 23017 -> 22797`, local exact but `1.0097`.
- `task064`: `h_high_p1 -> h_high24` or `v_high_p1 -> v_high24`, `cost 12478 -> 12454`, local exact but small.
