# 20260701 case246 plan — task349 horizontal detector kernel crop

## 現状
- floor: Public Score `7181.28`
- scoring: `points = max(1, 25 - ln(max(1, cost)))`
- 優先基準: 絶対 cost ではなく `old_cost / new_cost`。ただし `n_fail=0` が絶対条件。
- 足切り: `task349.onnx` の `audit_one` で全ペア完全一致。

## 対象
- `task349.onnx`
- baseline: `params=89`, `memory=24300`, `cost=24389`, `n_fail=0`

## 仮説
`task349` は case71 / case74 で冗長 Cast を削除済みだが、水平 detector の QLinearConv kernel `h_kernel_i8_c0..c3` には右端側のゼロ列が残っている。

各 Conv は `pads=[0,1,0,10]`, `kernel_shape=[1,12]` で出力幅を保っている。非ゼロ範囲に合わせて kernel 幅を `4/6/8/10` に縮め、右 padding を `2/4/6/8` に調整すれば、出力形状と値を保ったまま params を 20 削れる見込み。

## 手法
1. `/tmp` で `h_kernel_i8_c0..c3` の右端ゼロ列を crop し、各 QLinearConv の `kernel_shape` と `pads` を更新する。
2. `audit_one` で `n_fail=0` と cost 減を確認する。
3. 成功した場合のみ `data/output/onnx/task349.onnx` に反映し、submit する。
4. Public Score が `7181.28` を上回れば採用、上がらなければ破棄する。
