# 20260630 case91 plan — t054 の seed branch と final output を一点検証する

## 背景

- 現 Public best は **7180.87**。
- `task054.onnx` は faithful baseline で cost **27089**（params 238, memory 26851）。
- 過去ログでは bundle harvest により 27102→27094→27089 と byte 級で採用済み。独自の構造削減は未検証。
- Graph は背景/host 色を抽出し、参照 motif を選び、最大4 seed について水平・垂直 line update を展開し、最後に fill と one-hot output を作る。

## 仮説

seed 2/3 は optional branch として `seed_2_present` で guard されている。全ペアで 2 seed までに収まる、または seed 3 片側更新が不要なら、seed 2/3 branch を削除して cost を下げられる。final one-hot `Equal` は大きいが、採点側が label grid を許さない可能性が高いため、まず faithful で確認する。

## 対象タスク

- `task054`

## 手法

1. ORT profile で memory 上位 tensor を確認する。
2. `/tmp` variant で以下を一点ずつ試す。
   - seed 3 update を 0 に固定
   - seed 2/3 update を 0 に固定
   - horizontal または vertical update の片側削除
   - fill の seed 2/3 を inactive に固定
   - final one-hot を label grid 出力へ変更
3. 正答性を保った候補だけ cost を比較する。

## 採否

- faithful `audit_one` が `n_fail=0` かつ cost が 27089 未満なら `task054.onnx` を差し替えて submit する。
- Public Score が 7180.87 を上回れば採用し、DVC push と git commit/push を行う。
- 壊れる候補、または cost 悪化候補は破棄し、結果のみ記録する。
