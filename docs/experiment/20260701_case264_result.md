# 20260701 case264 result - task092 direct bool cast

## 結果

- 対象: `data/output/onnx/task092.onnx`
- 採否: 採用
- submit ref: `54218083`
- Public Score: `7181.61 -> 7181.63`

## 差分

`row_any` / `col_any` の二重Castを直接 bool cast にした。

```text
row_any -> Cast(float16) -> row_any__h16 -> Cast(bool) -> row_valid
col_any -> Cast(float16) -> col_any__h16 -> Cast(bool) -> col_valid
```

を以下に置換。

```text
row_any -> Cast(bool) -> row_valid
col_any -> Cast(bool) -> col_valid
```

削除した中間:

- `row_any__h16` `[1,1,30,1]`
- `col_any__h16` `[1,1,1,30]`

## 足切り

個別 audit:

```text
task092.onnx
params 182
memory 6690
cost 6872
points 16.16478953633591
n_pass 265
n_fail 0
filesize 6641
```

baseline:

```text
params 182
memory 6810
cost 6992
points 16.147478082664627
n_pass 265
n_fail 0
filesize 6767
```

cost は `6992 -> 6872` で 120 削減。local points は約 `+0.01731`。

## 結論

実 LB が `7181.61` から `7181.63` へ上昇したため採用する。case131 の `-10` 削減は Public 表示に出なかったが、今回は `-120` の相対削減が表示スコアに反映した。

## 次の一手

同じ「片用途の `float -> small dtype -> bool/u8` 二重Cast」を、消える中間が少なくとも数十 byte 以上ある task に限定して横展開する。
