# 20260702 case482 plan - task089 current scratch re-gate

## Context

Accepted Public Score is `7186.73`. Cases 477-481 found no isolated
submit-grade candidate in several high/mid-cost tasks. Case471 explicitly named
`task089` as a next candidate for cost-only filtering before correctness gates.

`task089` current baseline:

```text
task089 cost=7027
params=75
memory=6952
```

It is outside the current hard/review/runtime risk sets and has many existing
scratch candidates.

## Target

```text
task089
baseline: data/output/onnx/task089.onnx
task json: data/lake/neurogolf-2026/task089.json
```

## Method

1. Gather `task089*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against current baseline.
3. Correctness-gate positives with `submit.gate.evaluate_candidate_gate`.
4. Submit only an isolated candidate satisfying:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Otherwise record no-submit and bank exact low-gain candidates.
