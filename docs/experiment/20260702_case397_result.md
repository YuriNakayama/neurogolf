# 20260702 case397 result - bounded unary scan no submit

## Summary

No Kaggle submit.

Accepted Public Score remains:

```text
7186.24
```

## Scan

Ran a bounded current-baseline unary/value-path bypass scan over tasks
`001..120`, excluding recent high-risk/error families.

The scan itself ended with a local ORT/Python segfault while auditing generated
candidates. The only surviving candidate artifact was:

```text
/tmp/neurogolf_case397_unary_scan/task044_10_Squeeze_in0.onnx
```

Structural inspection:

```text
onnx.checker: ok
functions: 0
forbidden ops: none
ops: Add, And, ArgMax, Cast, Clip, Conv, Equal, Gather, Greater, Less,
     MatMul, Mul, Or, Pad, ReduceMax, ReduceSum, Slice, Squeeze, Sub, Sum,
     Unsqueeze
```

However, task-local `audit_one` on the candidate reproducibly exits with
segfault (`code 139`). The accepted `task044.onnx` remains locally scorable:

```text
params=68
memory=11390
cost=11458
status=ok
```

## Decision

Reject the candidate and do not submit. A graph that segfaults local auditing is
not eligible for the submit gate even if ONNX checker passes.

## Next

Avoid broad unary scans that generate many invalid runtime shapes; they consume
the cadence window and can crash ORT. Prefer targeted scans with shape-compatible
candidate filters, or task-specific structural rewrites where the expected
relative gain is at least `0.020`.

The parallel near-miss review also found no quick exact repair for:

```text
task077 propagation aliases
task110 public row-table alias
task243 fill_16/fill_26 flood-fill cuts
```
