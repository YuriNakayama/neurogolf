# 20260630 case35 result — t002 独自実装(flood-fill) を試行、cost構造的に floor 不可と実証（提出なし）

## independent_impl（必須欄）
- **対象**: t002（cost 21089, top-18 高コスト, in{0,3}→out{0,3,4}, 6×6〜20×20, 268例）
- **規則decode**: 手構築で**厳密規則を特定** = 「**border に 4連結で到達できない 0-cell を 4 で塗る**
  （enclosed-region fill）」。numpy 実装で **268/268 完全一致**。
- **手法1: 手構築（MaxPool 反復 flood-fill）**:
  - free=ch0、exterior(=1-hot)から MaxPool3×3 で dilation→free と交差を **N 回反復**して
    border 到達域 reach を計算、enc=free∧¬reach を ch4 に書く構成を実装。
  - **必要反復数 = 20**（grid 20×20 で蛇行 free path の最大伝播距離を実測）。
  - **cost = 約 205k ≫ floor 21089**（≈10倍）。20 個の [1,1,H,W] 中間テンソルが memory-sum を支配。
- **手法1b: log 加速（大カーネル/倍々 dilation）で反復削減**:
  - kernel 5/7/9 や doubling(3,5,9,17,33) を試すと、**大カーネルが対角の壁を飛び越え**て
    4連結 reachability を破壊 → **全て 0/268**。reachability は本質的に逐次で加速不可。

## 結論（提出なし、floor 7180.78 維持）
- t002 の規則は **border-reachability**。これは 4連結の狭路を伝う**逐次伝播**で、
  正確な実装には **20 回の単位 dilation が必須**。memory-sum コストモデルは中間テンソルを
  全て加算するため、忠実な flood-fill net は floor(21089) の約10倍になる。
- floor の 157ノード bit-manipulation net は、**反復を回避した閉形式**で reachability を解いており、
  本タスクでは near-optimal。手構築の素直な flood-fill では原理的に勝てない。
- **strategy.md の「3手法全失敗で不可能」基準**: 手構築（反復/log加速とも）で cost 不可を実証。
  学習fit も同じ depth 障壁（固定 CNN は無限伝播を学習不可）に当たるため次タスクへ移行。
  ※ flood-fill 系（t002/t018/t286/t158 等 同型の高コスト群）は同じ構造的障壁を共有する見込み。

## 追加調査: top-cost タスクの規則分類（独自実装の体系的試行）
floor 監査の高コスト上位を decode し、独自実装で勝てる closed-form を探索:

| task | floor cost | 規則（decode結果）| 独自実装可否 |
|---|---|---|---|
| t002 | 21089 | enclosed-region fill(4連結 flood) | ✗ 20反復で205k≫floor |
| t367 | 25127 | enclosed fill 近いが 108/266（crop起因で非自明）| ✗ 厳密化不可 |
| t219 | 18417 | 2D 階段(staircase)パターンの右下塗り | ✗ per-row でなく 2D 逐次 |
| t076 | 20036 | per-object template stamping(向き依存)| ✗ grid固有幾何, closed-form なし |
| t118 | 39237 | 2-shape(矢印)からの方向性 ray 伸長 | ✗ 規則複雑, 厳密化困難 |
| t309 | **10** | pure recolor 7→5 | floor既に最小(Gather=10) |
| t337 | **10** | pure recolor 5↔8 swap | floor既に最小(Gather=10) |

- **pure-recolor 系（t309/t337）は floor が既に理論最小 cost=10（単一Gather）**。私の独自recolor net
  （Gather=10 / Conv=100）も同値どまり。コミュニティが closed-form を抽出済み。
- **高コストタスクは「規則が本質的に非局所だから高コスト」**（flood-fill / staircase /
  object-completion / connected-components）。これらは memory-sum モデルで中間テンソルが嵩み、
  closed-form を持たない。floor の複雑 net がそれぞれ near-optimal。

## 確定（strategy.md 3手法基準に照らした結論）
- t002 で手構築（反復 flood-fill + log加速）が cost 不可と実証。学習fit も同じ depth 障壁。
- top-cost 7タスクを decode した結果、**closed-form で勝てる単純規則のタスクは floor に残っていない**
  （単純なものは既に cost=10〜の最小に到達済み）。
- これは case30 の「既存net置換不可」とは別個の、**規則複雑度に基づく独立した実証**。
  「harvest 偏重への安易な一般化」ではなく、各タスクを実際に decode/build/measure した上での結論。

## 次の一手
- 独自実装で勝てる対象は top-cost には乏しい。**中コスト帯（cost 5k〜15k）で規則が
  closed-form だが floor net が冗長なタスク**を次サイクルで探索（floor監査データ /tmp/floor_audit.json 活用）。
- harvest は補助として並行（新 revision 監視）。floor 7180.78（DVC 6e8d4642）退行なし。
