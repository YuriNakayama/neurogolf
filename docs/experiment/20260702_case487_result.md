# 20260702 case487 result - no-submit task319 current scratch cadence scan

## Decision

No submit.

Accepted Public Score remains:

```text
7186.79
```

## Baseline

```text
task319.onnx
cost=19080
params=132
memory=18948
status=ok
```

## Scan

Existing unique `task319*.onnx` scratch candidates from `/tmp` and
`/private/tmp`:

```text
candidates=1049
positive cost-only candidates=429
```

The first 220 highest-gain positive candidates were gated. None were exact:

```text
blocked-local-fail=220
submit-candidate=0
bank-low-gain=0
```

Representative failures:

```text
19080 -> 9000   gain=0.751416  n_fail=267  blocked-local-fail
19080 -> 11194  gain=0.533263  n_fail=267  blocked-local-fail
19080 -> 11244  gain=0.528806  n_fail=267  blocked-local-fail
19080 -> 13153  gain=0.371991  n_fail=249-267 blocked-local-fail
19080 -> 13162  gain=0.371307  n_fail=103-267 blocked-local-fail
19080 -> 14599  gain=0.267688  n_fail=101-264 blocked-local-fail
```

The dominant failed family also produced repeated Gather out-of-bounds runtime
errors during audit, so it should not be submitted or bundled.

## Parallel Findings

Worker E scanned `task222,319,324,340,349,350,364,377,379,383,396`.
It found no submit-candidate or review-mid-gain candidate. The only exact
current-baseline material was bank-low:

```text
task379 10773 -> 10753 gain=0.001858219
task383  7245 ->  7237 gain=0.001104820
task349 24389 -> 24369 gain=0.000820378
task396  8078 ->  8074 gain=0.000495295
```

One-candidate-per-task aggregate gain is about `0.004279`, below the micro
bundle threshold. These are retained only as backlog material.

## Conclusion

Reject locally. `task319` still has many large cost-only reductions, but they
collapse source/object selection state and fail local correctness broadly. Move
to another current-baseline target rather than spending a submit on this family.
