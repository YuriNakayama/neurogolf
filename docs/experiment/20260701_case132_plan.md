# 2026-07-01 case132 plan

## 方針

- floor: Public Score 7181.11
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task014.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task014.onnx`
- baseline: cost 7985, score 16.015171972880673

## 仮説

`task014` は rarest-color plane の bbox crop を 30x30 one-hot に戻す構成。過去ログでは near-minimal だが、現行 graph には `ArgMax(row_has)` / `ArgMax(col_has)` の重複、`Where(rect, inside, out_u8)`、`Pad(..., out_u8)`、`Equal(chidx, class30)` まわりに小さい冗長性が残っている可能性がある。

特に bbox の終端側が始端側と同じ実装になっているため、出力の完全一致を保ったまま片側 index / mask / pad sentinel を整理できれば、メモリ支配 cost を数十から数百削れる見込み。

## 手順

1. `/tmp` 上の candidate ONNX で、重複 `ArgMax`、inside/rect mask、Pad sentinel、crop gather の各簡略化を個別に試す。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task014.onnx` に反映する。
3. submit し、Public Score が 7181.11 を上回れば採用、上がらなければ破棄する。
