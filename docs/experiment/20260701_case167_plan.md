# 20260701 case167 plan

## Context

- Current accepted Public Score: 7181.17
- case166 showed `task017` cannot reduce match-score dtype because `ReduceSum` rejects smaller integer inputs.
- Continue with exact-match probes, preferring branch chains where one guard may be redundant.

## Target

- Task: `task345`
- Baseline audit:
  - params: 64
  - memory: 1431
  - cost: 1495
  - points: 17.690118514175214
  - n_pass: 264
  - n_fail: 0
  - filesize: 2472

## Hypothesis

`task345` propagates a red mask through five blocker rows:

- `hit_k = active_k & blocker_k`
- `shift_k = Gather(hit_k, shift_idx)`
- `out_k = active_k | shift_k`
- `active_{k-1} = out_k XOR hit_k`

If any blocker stage is redundant for public examples, replacing `out_k` or `active_k` with its predecessor can remove a repeated `And/Gather/Or/Xor` block. Because the task is compact, only exact reductions with multiple removed nodes are worth considering.

## Method

1. Probe one chain replacement at a time on temporary `task345` ONNX copies.
2. Run `audit_one` only for `task345`.
3. Apply and submit only if `n_fail=0` and cost reduction is meaningful.
4. If no exact-match reduction exists, write result and commit docs only.
