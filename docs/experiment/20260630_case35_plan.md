# 20260630 case35 plan — 独自実装サイクル: floor全監査→高コストタスクを decode→独自net構築

## 仮説（strategy.md 準拠）
floor 全400監査で高コスト・未解答タスクを特定し、規則を decode して独自 net で floor を下回る。
harvest ではなく独自実装が主軸。

## 手法
1. floor 全監査（audit_one run_correctness=True）→ 未解答(0点)・高コスト top-20 特定。
2. 高コストタスクの規則を numpy で decode。
3. closed-form/少数op で独自 net 構築（手構築→学習fit→合成の順）。faithful 検証(n_fail=0,cost<floor)。
4. floor未満かつ全例一致なら submit。

## 採否
- 実 LB > 7180.78 で accept、退行 revert。各タスクは3手法の失敗を記録して初めて諦める。
