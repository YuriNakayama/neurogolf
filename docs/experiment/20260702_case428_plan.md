# 20260702 case428 plan - task202 branch/Where fusion probe

## Context

Case427 rejected `task192`: its high-gain terminal shortcuts all fail local
correctness, and the exact terminal predicate requires two full-grid conditions.

The next graph-scan target is:

```text
task202 params=24 memory=11253 cost=11277 n_fail=0
```

One full `30x30` derived tensor is `900` cost, about `8.0%` of the task.

## Hypothesis

`task202` has a scalar orientation branch and several full-grid color-code
intermediates:

```text
bandc = Where(isvert, colcolor, rowcolor)
hcell = Where(iszero, bandc, 0)
hb    = Where(isvert, ReduceMax(hcell, width), ReduceMax(hcell, height))
cg0   = Where(hb == bandc, 0, bandc)
cg    = Where(ingrid, cg0, sent)
```

If `isvert` is fixed on all examples, or if `bandc`/`hcell`/`hb` can be safely
aliased under one orientation, deleting one full-grid tensor should clear the
submit threshold.

## Method

1. Inspect runtime `isvert` distribution and tensor equalities across all valid
   examples.
2. Test local alias/branch candidates only if supported by those equalities.
3. Run `submit gate`.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
