# 20260702 case366 result - task243 repair not cost-effective

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.78
```

## task243

Recreated the historical near miss:

| candidate | cost | gain | n_fail | decision |
|---|---:|---:|---:|---|
| baseline | `22650` | - | 0 | keep |
| `fill_27 -> fill_26` | `22002` | `0.029026` | 1 | reject |

The only failure is `arc-gen[239]`, one decoded cell at row `0`, col `10`.
The candidate leaves it as background while expected output paints color 1.

Tried exact repairs:

| repair | cost | n_fail | decision |
|---|---:|---:|---|
| final `MaxPool(1x3)` plus zero mask | `22650` | 0 | exact but neutral |
| top-row slice/pool/pad repair | `23088` | 0 | worse |
| right-neighbor zero-mask repair | `23286` | 0 | worse |

The last removed propagation step is genuinely needed. Reintroducing it as a
small-looking local patch still materializes enough mask/pad intermediates that
the cost equals or exceeds the baseline.

## task349 side probe

Parallel side probe reproduced the high-gain near miss but found no exact
reduction:

| candidate | cost | n_fail | decision |
|---|---:|---:|---|
| baseline | `24389` | 0 | keep |
| `halo_c4 -> halo_c3` | `22576` | 17 | reject |
| drop `halo_c4` | `22576` | 17 | reject |
| bypass valid guard | `18988` | 212 | reject |

The failing cases are arc-gen examples where the color-4 halo branch supplies a
large halo around 10-wide horizontal blocks. The detector branch is structurally
required unless a new cheap predicate can replace it.

## Medium Alias Scan

Started a same-shape/same-dtype alias scan across selected medium-cost tasks.
It produced many structurally invalid candidates and no printed exact or
low-fail useful hit before being interrupted. This scan shape is too noisy
without per-task pruning.

## Conclusion

Skip submit. The primary task243 candidate is close to the standalone threshold
but cannot be made exact below baseline. The side task349 candidate is a
high-gain local near miss but remains far from correctness.

Next action: avoid generic alias scans over many tasks. Pick one graph at a
time and inspect branch fanout before generating candidates, so invalid
rewires do not dominate the cycle.
