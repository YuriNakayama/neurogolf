# 20260702 case348 result - task158 tr-ne/fill alias adopted

## Result

- Previous accepted Public Score: `7185.37`
- Kaggle submission ref: `54239288`
- Submission status: `SubmissionStatus.COMPLETE`
- Public Score: `7185.40`
- Submission time: `2026-07-01 17:10:22 UTC`
  (`2026-07-02 02:10:22 JST`)

Adopted.

## Local Candidate

Submitted candidate:

```text
/tmp/neurogolf_task158_case348/task158_combo_D_tr_ne_TO_tl_ne__fill6_TO_fill7.onnx
```

Targeted `audit_one` before submission:

```text
baseline:  params=2613, memory=28681, cost=31294
candidate: params=2613, memory=27574, cost=30187
n_fail=0, status=ok
gain = ln(31294 / 30187) ~= 0.0360
```

Dry-run packaging validation also completed and included all 400 tasks. Seven
existing tasks remained local-cost-unestimated but structurally valid, matching
the prior submission behavior.

## Adoption Rationale

The Public Score improved by `+0.03`, so the Kaggle result confirms the local
task158 alias is submit-safe. This is a narrow continuation of the case346
diagonal-slice family:

- `tr_ne_bl -> tl_ne_br`
- `fill_u8_6 -> fill_u8_7`

The rejected case347 `task233` uint8 `TopK` value-path family remains excluded
from normal cadence because Kaggle returned `SubmissionStatus.ERROR`.

## Next Step

Use the next 30-minute window for a submit-saving batch rather than another
single tiny change. The best current direction is to regenerate and individually
audit a low-risk micro batch from the backlog, especially:

```text
task382 + task090 + task334 + task052 + task363 + task213 + task117
```

Estimated combined local gain from the backlog is about `0.0333`. Avoid mixing
in task118 table pruning unless deliberately choosing a higher-risk isolated
attempt.
