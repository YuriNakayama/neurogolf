# 20260701 case156 plan

## Context

- Current accepted Public Score: 7181.17
- Latest cycle: case155 `task378` local cost -6 was rejected because Public Score stayed 7181.17.
- Priority update: avoid tiny trims that are hidden by LB rounding; target at least roughly 1% task-local cost reduction when possible, while preserving `n_fail=0`.

## Target

- Task: `task284`
- Baseline audit:
  - params: 653
  - memory: 3082
  - cost: 3735
  - points: 16.774496902433082
  - n_pass: 266
  - n_fail: 0
  - filesize: 11983

## Hypothesis

`task284` detects two colored endpoints, derives an arrow-like shape, and writes 56 points with `ScatterND`. The graph has several compact but potentially over-guarded pieces:

- `side_valid = Concat(line_valid_i8, true6_i8)` gates only part of the generated indices.
- color expansion and `Mul(..., side_valid_col_i8)` may be redundant if invalid positions are always paired with zero-channel updates or unused coordinates.
- symmetric side1/side2 row/col construction may contain removable cap/stem branches for this dataset.

Unlike case155, the target has enough tail logic that a successful replacement can remove tens or hundreds of bytes of memory, making the relative cost reduction large enough to matter on Public LB.

## Method

1. Probe one structural bypass at a time on temporary ONNX copies.
2. Run `audit_one` only for `task284`.
3. Apply and submit only if `n_fail=0` and cost decreases meaningfully.
4. If Public Score does not exceed 7181.17, revert the ONNX/DVC change and record rejection.
