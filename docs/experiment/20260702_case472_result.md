# 20260702 case472 result - no-submit task014 bbox crop re-gate

## Decision

No submit.

Accepted Public Score remains unchanged from the last accepted Kaggle result.

## Local baseline

```text
task014.onnx
params=107
memory=7878
cost=7985
points=16.014679939350888
n_fail=0
status=ok
```

## Scan

The current-baseline scratch scan found many cost-only reductions:

```text
candidates=346
positive=213
errors=0
```

Top cost-only candidates were large by the logarithmic metric:

```text
7985 -> 5674  gain=0.341670  Where_inside_TO_zero_u8
7985 -> 5675  gain=0.341494  Where_inside_TO_Bu8
7985 -> 5945  gain=0.295014  Where_shifted_TO_rect
7985 -> 6780  gain=0.163588  Add_in1
7985 -> 6789  gain=0.162261  Min_ridx_TO_clipmax
```

## Gate

Material gate:

```text
material_candidates=33
exact_positive=0
submit_candidates=0
blocked=33
errors=0
```

Mid-gain gate:

```text
mid_candidates=3
exact_positive=0
review_mid_gain=0
blocked=3
errors=0
```

Representative failures:

```text
7985 -> 5674  gain=0.341670  n_fail=266  blocked-local-fail
7985 -> 5945  gain=0.295014  n_fail=262  blocked-local-fail
7985 -> 6780  gain=0.163588  n_fail=262  blocked-local-fail
7985 -> 7262  gain=0.094910  n_fail=266  blocked-local-fail
7985 -> 7837  gain=0.018709  n_fail=124  blocked-local-fail
```

## Conclusion

Reject locally. The attractive `task014` reductions all remove semantic bbox,
inside/class, or crop-index machinery and fail ARC examples before Kaggle. This
explains part of the recent no-submit pattern: cost-only scans are producing
large apparent gains, but the current submit gate correctly blocks them because
they are not functionally correct.

No exact low-gain candidate was found to bank from this task.

## Next

Increase parallel candidate discovery instead of weakening the correctness
gate. To support the requested submit cadence, keep multiple independent
subagents searching disjoint task bands for `n_fail=0`, no forbidden op, and
`gain>=0.020` candidates while the main loop integrates only submit-grade or
carefully tiered micro-bundle results.
