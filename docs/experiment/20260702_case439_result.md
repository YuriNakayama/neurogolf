# 20260702 case439 result - task174 slice probe is no-submit

## Decision

- Task: `task174.onnx`
- Submit: no
- Adoption: none
- Reason: no current-baseline exact candidate reduces cost.

Current baseline:

```text
params=178
memory=8800
cost=8978
status=ok
```

## Graph Evidence

Dominant internal tensors:

```text
colors4_1      FLOAT [1,9,10,10]  3600 bytes
colors4_u8_174 UINT8 [1,9,10,10]   900 bytes
colors_pad_19  UINT8 [1,3,13,13]   507 bytes
colors_flat_20 UINT8 [1,3,169]     507 bytes
```

The large `colors4_1` source slice is live for two different purposes:

```text
Slice(input) -> colors4_1
Cast(colors4_1) -> colors4_u8_174
ReduceSum(colors4_1) -> present9_2
```

Because the same slice feeds both present-color selection and the later uint8
crop path, the obvious dtype-only rewrite does not remove the 3600-byte tensor.

## Candidate Re-Gate

Re-gated 34 existing candidates against the current accepted baseline:

```text
blocked-local-fail: 14
blocked-no-gain:   20
submit-candidate:   0
```

Best apparent reductions:

```text
task174_term_55_And_1.onnx
  cost 8978 -> 8719
  gain 0.029273
  n_fail 266
  decision blocked-local-fail

task174_bypass_d4_52_And_1.onnx
  cost 8978 -> 8810
  gain 0.018890
  n_fail 254
  decision blocked-local-fail

task174_bypass_d4_52_And_0.onnx
  cost 8978 -> 8912
  gain 0.007378
  n_fail 231
  decision blocked-local-fail
```

Exact candidates:

```text
cleanup / broadcast_compress / conv1x1_to_gather / identity_elementwise:
  cost 8978 -> 8978
  n_fail 0
  decision blocked-no-gain

fp16_surgery / safe_surgery:
  cost 8978 -> 10754
  n_fail 0
  decision blocked-no-gain
```

Several index/dtype candidates are invalid in ONNX Runtime because they leave
`Equal`, `Less`, or index outputs with mismatched int32/int64 types.

## Conclusion

No submit. `task174` has already received its transferable inside-mask deletion
in case315. The remaining large slice is structural under the current
formulation, and the known material/terminal aliases fail local correctness.

Move to another current-graph target instead of forcing old `task174` scratch
candidates.
