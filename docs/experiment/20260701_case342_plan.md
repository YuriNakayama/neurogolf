# 20260701 case342 plan - task233 post-TopK bounds gate

## Context

- Current accepted Public Score floor: `7185.09` after case338.
- Submit-saving policy now uses about one submit window per 30 minutes, but an
  isolated threshold-clearing candidate can be submitted in the next window.
- Competition scoring is logarithmic:

```text
points = max(1, 25 - ln(max(1, cost)))
cost = params + memory_bytes
gain = ln(old_cost / new_cost)
```

## Candidate

Target `task233`.

Subagent deep inspection found a medium-risk semantic refactor:

- Replace the full `[1,324]` `scorew_valid` boundary mask with a lighter
  post-`TopK` selected-candidate bounds check using `row < red57` and
  `col < sub77`.
- Add compatible exact micro rewires for strict-valid and absolute-difference
  intermediates:
  `whe118_strict_valid`, `whe198_strict_valid`, `whe237_strict_valid`,
  `whe194`, `abs134`, `abs174`, `abs213`, `abs252`.

Expected local change:

```text
task233: params/memory/cost 556/50512/51068 -> 520/48934/49454
gain = ln(51068 / 49454) ~= 0.0321
```

This clears the medium-risk standalone submit threshold of `0.030`.

## Gate

1. Copy `/tmp/neurogolf_task233_deep/task233_final_abs134_abs213.onnx` to
   `data/output/onnx/task233.onnx`.
2. Run `src/evaluate.audit_one` only for `task233`.
3. Submit if `n_fail=0` and cost is lower.
4. Adopt only if Kaggle Public Score improves over `7185.09`; otherwise restore
   the DVC baseline and record rejection.
