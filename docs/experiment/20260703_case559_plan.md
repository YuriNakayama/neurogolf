# 20260703 case559 plan - widened parallel search and cadence bundle policy

Accepted Public Score:

- `7187.57`

## Background

Case558 submitted a locally exact `task319 18685 -> 18669` candidate, but the
Public Score stayed flat at `7187.57`. The candidate was rejected because it did
not improve the real LB score.

The recent submit gaps were caused by waiting for larger standalone repairs while
not using the exact micro backlog when the cadence window arrived. To hit the
requested 13-hour / 72-submit exploration pace, the workflow now needs separate
lanes for high-value search and cadence-safe bundles.

## Priority Model

The score improvement is logarithmic:

```text
gain = ln(old_cost / new_cost)
```

So relative reduction is the ranking key, not absolute cost. The hard gate
remains `n_fail=0`, `functions=0`, and forbidden ops `[]`.

## Parallel Lanes

1. Medium/high-gain standalone search:
   `task340, task367, task118, task198, task202, task204, task219, task255, task222`.
2. Micro-bank search:
   exact small reductions on non-risk tasks, excluding recently accepted tasks.
3. Repair lane:
   diagnose and repair high-gain near misses (`task340`, `task367`, `task187`,
   `task364`) without submitting failures.
4. Main thread:
   poll Kaggle, write adoption/rejection docs, build full 400-task bundles, and
   submit the best available exact candidate near the cadence window.

## Submit Policy

Submit immediately for a standalone exact candidate with meaningful relative
gain. If no such candidate is ready near the cadence window, submit a reviewed
micro bundle only when the bundle is exact, low-risk, and has a clear positive
total gain. Keep all non-adopted candidates out of `data/output/onnx`.
