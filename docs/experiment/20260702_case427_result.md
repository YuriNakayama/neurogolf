# 20260702 case427 result - no-submit task192 terminal Where collapse

## Outcome

No submit.

Current baseline:

```text
task192 params=106 memory=8515 cost=8621 n_fail=0 status=ok
```

## Inspection

Terminal graph:

```text
score          = Conv(input, score_w)
keep_or_vgap_b = Greater(score, 9.5)
vgap_b         = Equal(score, 12)
bg_or_out_u8   = row/col outside/background grid
main_u8        = dominant non-background color
pre_target_u8  = Where(keep_or_vgap_b, main_u8, bg_or_out_u8)
target_u8      = Where(vgap_b, bg_or_out_u8, pre_target_u8)
output         = Equal(channel_codes_u8, target_u8)
```

Runtime score values across all valid examples:

```text
score values: 0..14
target != bg_or_out exactly matches: score > 9.5 and score != 12
```

This means `score == 12` is a gap inside the kept range. A single threshold
cannot express the exact condition.

## Candidates

Checked known terminal shortcuts and one new local rewrite:

```text
task192_bypass_d1_14_Where_1.onnx
  8621 -> 2143, gain 1.3919943707, n_fail=265

task192_bypass_d2_13_Where_2.onnx
  8621 -> 7634, gain 0.1215891329, n_fail=265

task192_bypass_d1_14_Where_2.onnx
  8621 -> 7720, gain 0.1103867232, n_fail=19

task192_013_Where_pre_target_u8_TO_bg_or_out_u8.onnx
  8621 -> 6734, gain 0.2470317665, n_fail=265

task192_014_Where_target_u8_TO_bg_or_out_u8.onnx
  8621 -> 1243, gain 1.9366732747, n_fail=265

task192_014_Where_target_u8_TO_pre_target_u8.onnx
  8621 -> 6820, gain 0.2343416154, n_fail=19

task192_where_vgap_main.onnx
  8621 -> 8621, n_fail=265

task192_optimizer_cleanup.onnx
  8621 -> 8621, n_fail=0
```

## Decision

Reject for submission. The two terminal predicates are both semantically
required. Replacing the chain with a correct one-condition expression would
require extra full-grid bool intermediates, which is not a cost reduction.

## Next

Move to `task202`, the next current-graph candidate where one full 30x30
derived tensor is about 8% of task cost. Prefer local branch/Where fusion over
terminal shortcuts that remove required predicates.
