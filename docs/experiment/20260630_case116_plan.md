# 2026-06-30 case116 plan

## 現状

- floor: Public Score 7180.97
- 優先基準: 相対削減率 `old_cost / new_cost`。
- case115 `task265`: 7.4% 削減候補は `n_fail=1` で不採用。

## 対象

- `task277.onnx`
- baseline: params 44, memory 4101, cost 4145, n_pass 266, n_fail 0

Graph は color8 channel `P` 上で seed `ABSEED` を 6 段 propagate し、左右 slice `A/B` の和から wide 判定を作る。

```text
ABSEED*P -> (MaxPool -> *P) x6 -> AB6
AB6 -> Slice A/B -> Add -> ReduceMax -> Equal -> label
```

## 仮説

1 段 propagation を削れる、または `A/B` の片側判定で十分なら、30x30 級中間を複数削れて 5〜15% 程度の相対削減が見込める。

## 手法

1. `AB6` の利用を `AB5` / `mp4` / `AB4` に差し替えて後段を prune。
2. `wsum = A+B` を `A` または `B` に差し替えて片側判定を試す。
3. `wu` を constant 0 / 1 に固定して wide 判定の必要性を確認する。
4. `src.evaluate.scorer.audit_one` で `task277` の完全一致を確認する。
5. `n_fail=0` かつ cost が下がる候補のみ submit する。

## 足切り

- 変更対象は `task277.onnx` のみ。
- 禁止 op は追加しない。
- 実 Public Score が上がれば採用、上がらなければ破棄する。
