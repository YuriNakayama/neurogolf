# 20260701 case191 plan

## Context

- Current accepted Public Score: 7181.21
- Recent tiny params-only changes did not improve LB. Need task-local memory
  reduction, preferably by deleting branch tensors.
- `task374` is an untried compact graph with several `Where` branches over
  10x10 row/column line-classification tensors.

## Target

- Task: `task374`
- Baseline audit:
  - params: 42
  - memory: 2262
  - cost: 2304
  - points: 17.25759797818422
  - n_pass: 267
  - n_fail: 0
  - filesize: 3069

## Hypothesis

The graph computes row and column line lengths, then assigns colors:

```text
base = 4 for active rows/cols
max  = 1
min  = 2
```

If one of the max/min branches is redundant on the public examples, aliasing
`row_long_color`, `row_color`, `col_long_color`, or `col_color` to an earlier
tensor could remove a 10-element or 10x10 intermediate.

## Method

1. Probe temporary `task374` ONNX aliases only.
2. Test one branch alias at a time:
   - row max/min branch aliases
   - col max/min branch aliases
   - final `cell_color`/`color_grid` aliases
3. Run `audit_one` only for `task374`.
4. Apply and submit only if `n_fail=0` and cost decreases.
