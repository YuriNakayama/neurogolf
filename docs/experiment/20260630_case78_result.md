# 20260630 case78 result — t319 の局所 plane Cast 化は正答だが cost 悪化で不採用（提出なし）

## independent_impl:
t319 の `input_u8` 全体 Cast を削れるか検証:
- `ReduceMax(input_u8)` の row/col occupancy は `input` float から直接計算。
- `Gather(input_u8, color0/1/2)` は `Gather(input, colorX)` で float plane を取り、各 plane を `Cast(uint8)`。
- 既存 downstream は uint8/boolean 前提のため、object plane の Cast は残した。

## faithful 検証
- before: cost 22097, params 269, memory 21828, n_fail 0
- probe: **cost 24257, params 269, memory 23988, n_pass 267, n_fail 0**
- 正答性は維持したが **+2160 cost 悪化**。

## 確定
- 全体 `input_u8` Cast は一見 9000B と大きいが、3つの dynamic color plane を float で Gather すると
  各 `[1,30,30]` float 中間が 3600B になり、さらに uint8 Cast plane が必要。
- 現行の「先に全10chを uint8 化してから dynamic Gather」の方が memory が安い。
- t319 の dtype 局所化は不採用。floor 7180.85 維持、提出なし。

## 次の一手
- t319 は dtype 方向では改善なし。次は rank/order 系 t338、または t173/t080 の template-stamping graph を精査する。
