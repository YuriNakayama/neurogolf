# 20260703 case556 result - micro fallback and repair search

## Summary

- Submit: `case556 full micro bank 11 tasks`
- Public Score: `7187.53 -> 7187.54`
- Decision: adopt

## Submitted Bundle

The submitted full bundle changed 11 exact low-risk micro candidates:

```text
task005 6882 -> 6881 gain 0.000145
task117 4148 -> 4145 gain 0.000724
task205 11547 -> 11539 gain 0.000693
task206 4183 -> 4182 gain 0.000239
task234 6845 -> 6841 gain 0.000585
task328 6904 -> 6902 gain 0.000290
task333 3227 -> 3225 gain 0.000620
task358 4392 -> 4390 gain 0.000455
task361 4779 -> 4778 gain 0.000209
task368 6225 -> 6217 gain 0.001286
task388 2496 -> 2495 gain 0.000401
```

Total local relative gain: `0.005647`.

The bundle passed with:

```text
--allow-micro-bundle-gate --micro-bundle-gain 0.005 --micro-bundle-max-tasks 11
```

Every changed task had `n_fail=0`, `functions=0`, and forbidden ops `[]`.

## Adoption

The Public Score improved to `7187.54`, so all 11 candidates were copied into
`data/output/onnx`.

## Repair Search

Main thread:

- `task187 reach12` remained `n_fail=4`.
- Restoring one reach iteration made it exact but increased cost:

```text
task187 26396 -> 27533 n_fail=0
```

So `task187` repair was rejected.

Parallel worker:

- `task349` repair succeeded:

```text
/private/tmp/neurogolf_agent_case556_B/candidate/task349.onnx
task349 24369 -> 23612
gain 0.031557
n_fail=0
functions=0
forbidden ops=[]
```

## Next Step

Submit the repaired `task349` candidate as case557. It is standalone
submit-ready and should take priority over additional micro banking.
