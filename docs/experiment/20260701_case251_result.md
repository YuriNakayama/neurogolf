# 20260701 case251 result — task070 column gap補正 removal は fail 1 で不採用

## 対象
- `task070.onnx`
- 採否: 不採用
- submit: なし
- floor: Public Score `7181.59`

## baseline

```text
params=31
memory=2977
cost=3008
n_pass=266
n_fail=0
```

## 試行
`/tmp` の候補 ONNX で column gap 補正を外した。

```text
col_mask -> col_mask_raw:
  params=28
  memory=2957
  cost=2985
  n_pass=265
  n_fail=1

gap2 -> cols13:
  params=31
  memory=2976
  cost=3007
  n_pass=0
  n_fail=266
```

## 結論
不採用。

`col_mask -> col_mask_raw` は fail 1 まで近いが、完全一致条件を満たさない。削減幅は 23 cost しかなく、例外処理を足すと savings を超える見込みが高い。ONNX 本体は変更しない。

## 次の一手
fail 1 の小削減は追いすぎない。次は未言及 mid-cost から、1箇所で数百 byte 以上の memory を消せる terminal dtype / final output / large mask 系へ戻る。
