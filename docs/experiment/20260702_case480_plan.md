# 20260702 case480 plan - task191 current scratch re-gate

## Context

Accepted Public Score is `7186.73`. Cases 477-479 found no submit-grade
isolated candidate in `task205`, `task367`, or `task202`.

`task191` is a current mid/high-cost task outside the hard/review/runtime risk
sets:

```text
task191 cost=14854
params=102
memory=14752
```

## Target

```text
task191
baseline: data/output/onnx/task191.onnx
task json: data/lake/neurogolf-2026/task191.json
```

## Method

1. Gather `task191*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against current baseline.
3. Correctness-gate positives with `submit.gate.evaluate_candidate_gate`.
4. Submit only an isolated candidate satisfying:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Otherwise record no-submit and bank exact low-gain candidates.
