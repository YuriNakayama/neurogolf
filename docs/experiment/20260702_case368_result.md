# 20260702 case368 result - task158 exhausted, task319 micro banked

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7185.78
```

## task158

Current baseline:

```text
params=2613
memory=27574
cost=30187
n_fail=0
```

Re-audited the existing case348 scratch set against the current accepted graph:

```text
scanned: 173 task158 scratch ONNX files
exact cost-reducing candidates: 0
near candidates: 0
```

Targeted branch probes around the remaining three pair/fill branches:

| candidate | cost | gain | n_fail | decision |
|---|---:|---:|---:|---|
| keep fill branches 1+2 | `26365` | `0.135374` | 54 | reject |
| keep fill branches 1+3 | `26905` | `0.115099` | 111 | reject |
| keep fill branches 2+3 | `27013` | `0.111093` | 134 | reject |
| keep fill branch 1 only | `23083` | `0.268315` | 158 | reject |
| keep fill branch 2 only | `23191` | `0.263647` | 182 | reject |
| keep fill branch 3 only | `23731` | `0.240629` | 224 | reject |

Each branch is still required. Generic single-task surgery also produced no
gain:

```text
cleanup / dedup / slice-index / broadcast-compress: cost 30187, n_fail=0
index_surgery: session_error due int64/int32 Mul type mismatch
```

## task319 side probe

The documented high-gain near miss was reproduced:

| candidate | cost | gain | n_fail | decision |
|---|---:|---:|---:|---|
| `src2_possible -> p20_pair_match` | `18173` | `0.074010` | 14 | reject |
| partial repair with `p21` rest terms | `19364` | `0.010531` | 2 | reject |
| full repair through all `p21_*_eq_edge` terms | `19569` | 0 | 0 | neutral |

One exact micro was found and banked:

```text
task319 p20_rest_plus_top_eq_all
19569 -> 19567
gain = 0.0001022077
n_fail=0
path=/tmp/task319_probe4/p20_rest_plus_top_eq_all.onnx
```

This is far below every submit threshold, so it is not applied or submitted.

## Conclusion

Skip submit. Task158 has no remaining branch-level exact reduction in the
tested surface, and the task319 high-gain predicate shortcut cannot be repaired
without giving back nearly all of the saving.

Next action: keep the task319 two-byte cleanup in the micro backlog. Continue
with a different selector/table task rather than further task158 branch
deletion.
