# case196 plan: task332 parity stripe selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task332` は未着手で、baseline は `cost=563` (`params=134`, `memory=429`, `n_fail=0`)。
- graph は 10 node と短いが、`dot_color` 20 bytes、`background` 20 bytes、`labels` 60 bytes を削れれば相対改善が見込める。

## 対象

- `data/output/onnx/task332.onnx`
- channel 5 の 3x20 slice から `width` を取り、偶奇で `dot_color` を選ぶ。
- `col_active` で幅外を `no_color_u8=6` にし、`ch5b` で dot/background を合成して `QLinearConv` に渡す。

## 仮説

実データ上で幅の偶奇または背景 branch が固定に近ければ、`dot_color`/`background`/`labels` 周辺を削れる。特に `dot_color` を odd/even の片側に固定できれば scalar parity chain を prune できる。

## 検証

- temp ONNX で parity branch、background branch、labels branch の alias を 1 点ずつ probe する。
- `src.evaluate.scorer.audit_one` で `task332` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
