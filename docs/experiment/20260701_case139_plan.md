# 2026-07-01 case139 plan

## 方針

- floor: Public Score 7181.15
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task333.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task333.onnx`
- baseline: params 435, memory 2792, cost 3227, points 16.92069180794804, n_pass 265, n_fail 0

## 仮説

`task333` は green 2x2 box を基準に、左右上下4方向の外側 code を拾い、各方向の line candidate を `Max(idx_u8, left_candidate, right_candidate, up_candidate, down_candidate)` で合成している。

4方向 branch は対称で `Where(..., zero)` が多い。実データで特定方向の branch が不要、または `axis_color` と `candidate` の2段 mask の片側が冗長なら、10x10 candidate 中間を削れて相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `left/right/up/down_candidate` を `zero_u8` または `axis_color` に差し替える。
2. `code_sel` や `toward_box` mask を省略する候補も個別確認する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task333.onnx` に反映する。
4. submit し、Public Score が 7181.15 を上回れば採用、上がらなければ破棄する。
