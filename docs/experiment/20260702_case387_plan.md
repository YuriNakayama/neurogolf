# 20260702 case387 plan - task023 fl2/fl3 continuation

## Context

Accepted Public Score is `7186.06` after case386 adopted the narrow `task023`
subset:

```text
task023: 10924 -> 10636
rewires: fl0 -> isone0, fl1 -> isone1
gain 0.026718
```

The next exact subsets from case386 were:

```text
fl0/fl1/fl2/fl3: 10924 -> 10348
fl0/fl1/fl2/fl3 + Sadd4_a: 10924 -> 10276
fl0/fl1/fl2/fl3 + Sadd4_a + b13: 10924 -> 10204
```

Now that `fl0/fl1` is accepted, the incremental `fl2/fl3` subset should be
tested alone from the current baseline.

## Hypothesis

Adding only:

- `fl2 -> isone2`
- `fl3 -> isone3`

keeps the same narrow pattern as case386 and should reduce current cost from
`10636` to `10348`, for:

```text
ln(10636 / 10348) ~= 0.02745
```

This clears the standalone gate while avoiding broader `Sadd4_a`, `b13`, and
19-rewire changes.

## Method

Use the already generated exact candidate
`/tmp/neurogolf_case386_task023_fl0123.onnx`, re-audit it against current
`task023` baseline, inspect forbidden ops, and submit only this isolated next
subset.

## Gate

Submit if:

- `evaluate.audit_one` for task023 gives `n_fail=0`
- cost decreases from current `10636`
- forbidden ops remain absent
- current-relative gain is `>= 0.020`

Adopt only if the real Kaggle Public Score increases.
