# 20260703 case537 plan - current-baseline candidate generation

Accepted Public Score:

- `7187.41`

## Background

Cases535-536 showed two problems:

- The low-risk exact micro queue is only `0.001982` aggregate gain.
- Several old scratch candidates are stale against the accepted graph. For
  example, the old `task023` near miss costs `9844`, while current accepted
  `task023` is already `8764`.

So this cycle must generate candidates from current `data/output/onnx` directly,
not repair historical artifacts first.

## Strategy

Run current-baseline searches in parallel:

1. Dtype shrink search on non-risk tasks with medium/high cost.
2. Single-consumer alias search on non-risk tasks, but immediately gate every
   candidate and ignore cost-only positives with `n_fail>0`.
3. Mechanical cleanup search for stale `value_info`, duplicate initializers,
   unused constants, and safe simplifier output.
4. Focused high-ratio small-task search where a few bytes can still produce
   visible relative gain.

Risk exclusions:

- all hard/review/runtime-risk tasks from `backend/src/submit/gate.py`;
- known blocked tasks `011,033,074,148,157,196,243,279,379`;
- old stale `task023` S expansion artifacts.

## Submit rule

Submit only if:

- standalone non-risk candidate has `n_fail=0` and gain `>=0.020`; or
- low-risk micro bundle has all members `n_fail=0`, changed current-baseline
  files, and aggregate gain `>=0.015`.

If no such candidate appears, write a no-submit result and carry exact micros
forward.
