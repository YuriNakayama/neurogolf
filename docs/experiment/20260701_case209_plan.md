# case209 plan: task056 false channel trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task056` は未着手で、baseline は `cost=34` (`params=0`, `memory=34`, `n_fail=0`)。
- 純演算の極小 graph だが、`false_bool = And(h1, h3)` は `h1=(b2>b0)` と `h3=(b0>b2)` の AND なので論理的に常に false。

## 対象

- `data/output/onnx/task056.onnx`
- `h1`, `h2`, `h3`, `false_bool`, `false_bool`, `h6` を concat して 6-channel output を作る。

## 仮説

`false_bool` を定数 false または既存の同値 false branch に置き換えれば `And(h1,h3)` を削れる。ただし params が増えると scorer cost が悪化する可能性があるため、temp ONNX で cost を確認する。

## 検証

- temp ONNX で `false_bool -> false_const`、`false_bool -> h1/h3/equal_bits` などを probe する。
- `src.evaluate.scorer.audit_one` で `task056` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
