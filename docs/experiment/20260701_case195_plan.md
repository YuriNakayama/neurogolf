# case195 plan: task207 background block selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task207` は未着手で、baseline は `cost=236` (`params=44`, `memory=192`, `n_fail=0`)。
- 低 cost のため、4-byte から 40-byte の中間削減でも相対改善が大きい。

## 対象

- `data/output/onnx/task207.onnx`
- 4 隅の 2x2 背景 channel を `Sum` し、`Mod(..., 3)` で `background_block` を作る。
- `Where(background_block, background_channel, color_channel)` で 2x2 block を作り、30x30 に Pad する。

## 仮説

実データ上、4 隅の背景 count または `Mod(..., 3)` が過剰な可能性がある。`background_u8` を `bg_counts_u8` に alias できれば `Mod` を削れる。さらに `bg_counts` を単一 corner に alias できれば複数 Slice/Sum を prune でき、相対効果が大きい。

## 検証

- temp ONNX で `background_u8 -> bg_counts_u8` と `bg_counts -> corner` を 1 点ずつ probe する。
- `src.evaluate.scorer.audit_one` で `task207` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
