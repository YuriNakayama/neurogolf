# 20260630 case13 plan — row/col-separable タスクの novel solver + 新 revision harvest

## 仮説
1. row/col-separable（出力行が入力行のみの関数）の measurable タスクは 1D 演算の broadcast で
   floor より安く解ける可能性。最高コスト t243(22650, row-sep) の規則を特定し最小 net 化。
2. 並行して新 revision（kokinnwakashuu 16:59 等）を harvest。

## 対象 / 手法
- t243 の per-row/2D 規則を numpy 特定 → 許可 op（ray-cast 累積 + Where）で実装。
- 新 revision cross-bundle cherry-pick。

## 採否
- novel net: faithful n_fail=0 かつ cost<22650 で submit。
- harvest: floor 未満を faithful 検証 → submit、実 LB 上昇で採用。
