# 20260701 case247 plan — task370 repeat candidate branch prune

## 現状
- floor: Public Score `7181.28`
- 足切り: `task370.onnx` の `audit_one` で `n_fail=0`。
- 優先基準: `old_cost / new_cost`。memory 中間を消せる候補を優先する。

## 対象
- `task370.onnx`
- baseline: `params=1267`, `memory=9126`, `cost=10393`, `n_fail=0`

## 仮説
`task370` は d5/d4/d3/d2 の repeat 候補を作り、`valid_d5`, `valid_d4`, `valid_d3` から最初に有効な候補を選ぶ。もし上位候補のどれかが faithful/arc-gen 上で不要なら、QLinearConv repeat 中間を削れて相対削減が見込める。

case154 後の次手として挙がっていた未検証 task であり、`repeat_d5` などは 20x20 級の memory を持つため、params-only の微小削減より採用価値が高い。

## 手法
1. `/tmp` で `valid_d5`, `valid_d4`, `valid_d3` を個別に false 定数へ差し替える。
2. 差し替えた候補の score/repeat producer を削除できる範囲で消し、`audit_one` する。
3. `n_fail=0` かつ cost 減なら本体 ONNX に反映して submit する。
4. fail が出た候補は提出しない。
