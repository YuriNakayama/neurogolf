# 20260701 case328 result - task365 packed bool tail

## Decision

Adopted.

## Local Gate

Only `task365.onnx` was changed and audited.

```text
baseline:  params=120 memory=3908 cost=4028 n_fail=0 status=ok
candidate: params=109 memory=3836 cost=3945 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(4028 / 3945) ~= 0.0208 pt
```

## Submission

```text
ref=54231697
description=case328 task365 packed bool tail cost 4028 to 3945
status=COMPLETE
publicScore=7184.19
```

Previous accepted floor:

```text
7184.17
```

The Public Score improved by `+0.02`, so the candidate is accepted.

## Diff Summary

The accepted rewrite removes explicit zero channels from the final sparse bool
tail. It keeps only the live channels, casts the packed bool tensor to uint8,
and uses a one-hot `QLinearConv` with the final spatial padding folded into the
convolution pads.

## Next Step

The remaining ready packed-tail queue is exhausted. Continue searching for
same-task bundles or structural rewrites with larger relative reductions than
the historical alias-only micro candidates.
