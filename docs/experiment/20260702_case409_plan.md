# 20260702 case409 plan - task066 active row alias

## Context

Accepted Public Score is `7186.66` after case408 adopted the isolated
`task335` duplicate-initializer cleanup.

Fresh searches after case408 initially found a weaker `task066` semantic alias,
then the task-local subagent extended it to a stronger same-task rewire:

```text
task066 16835 -> 16351 gain 0.029170995100
```

This clears the preferred standalone `gain >= 0.02` threshold. Submit isolated
so any Public movement can be attributed to `task066` alone.

## Candidate

```text
task066 16835 -> 16351 gain 0.029170995100
```

Scratch file:

```text
/tmp/neurogolf_case407_rewire_scan/cand_228_n_u_up_gated_score_TO_n_s_up_edge.onnx
```

## Method

Extend the local-equivalent row predicate alias with an `u_up` branch score
rewire:

```text
active_row_any -> red_row_any
n_u_up_score = Where(n_u_up_valid_4, n_u_up_gated_score, zero_f)
          ->  Where(n_u_up_valid_4, n_s_up_edge, zero_f)
```

The rewrite prunes the `n_u_up_height_ok_1` / gated-score chain and two
initializers, reducing memory cost.

## Gate

Submit only if targeted `audit_one` passes:

```text
task066: n_fail=0, functions=0, forbidden ops=[]
```

Risk: moderate-low. This is not table pruning, TopK, or broad bitwise surgery,
and it stays within one task. It is still a semantic scoring alias, so it relies
on generated coverage being representative for the `u_up` branch. Adopt only if
Public Score improves over `7186.66`.
