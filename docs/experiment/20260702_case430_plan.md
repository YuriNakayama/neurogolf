# 20260702 case430 plan - task243 near-miss revalidation

## Context

Case429 banked only a `task080` micro. The next possible high-gain repair is
the documented `task243` near miss:

```text
baseline: 22650
candidate: fill_27 -> fill_26
candidate cost: 22002
gain: 0.029026
n_fail: 1
```

Case366 previously found that exact repairs were neutral or worse. This cycle
does not repeat a broad search; it revalidates the known candidate against the
current baseline and only proceeds if there is a new narrow repair opening.

## Hypothesis

If the failing arc-gen cell can be repaired with a smaller-than-648-cost top-row
or frontier exception, the candidate may clear the submit gate. If repairs
still reintroduce the removed frontier/mask cost, reject and move on.

## Method

1. Gate the known `fill_27 -> fill_26` candidate against current baseline.
2. Gate the known repaired variants from case366 if present.
3. Do not generate broad fill-chain aliases; previous scans already found the
   same failure family.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
