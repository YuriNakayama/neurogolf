# 20260702 case451 plan - task044 guarded candidate re-gate

## Context

Recent no-move and regression cycles show that local exactness is only a gate,
not proof of hidden safety. The updated scoring priority is relative cost
reduction:

```text
points = max(1, 25 - ln(cost))
gain = ln(old_cost / new_cost)
```

After the regressions, submit only isolated, mechanically explainable candidates
with current-baseline `n_fail=0` and preferably `gain >= 0.020`.

## Target

`task044.onnx`

Current baseline:

```text
params=68
memory=11390
cost=11458
points=15.6535565450
status=ok
```

The graph has a known expensive cropped input path:

```text
Slice(input) -> crop_f32  FLOAT  [1,10,10,10]
Cast(crop_f32) -> crop    FLOAT16 [1,10,10,10]
```

Prior logs show that direct `Clip` bypasses are unsafe locally or semantically
fragile, and broad alias scans are not suitable for direct submit after recent
regressions.

## Hypothesis

Previously generated cleanup/surgery candidates may contain a narrow current
baseline reduction, but stale broad alias candidates must be re-gated against
the accepted baseline before any consideration.

## Method

1. Re-gate only existing `task044` candidates from small cleanup/surgery pools
   and the case382 task044 pool.
2. Compare each candidate against `data/output/onnx/task044.onnx` with
   `submit.gate.evaluate_candidate_gate`.
3. Accept only `submit-candidate` decisions. Bank or reject everything else.
4. Do not submit broad case406 alias-scan output unless a result is exact,
   reduces cost materially, and has a simple mechanical explanation.

## Submit Rule

Submit only if the candidate is isolated, uses no forbidden ONNX ops/functions,
has `n_fail=0`, and clears the strict gate. Otherwise write a no-submit result
and move to the next structural target.
