# 20260701 case339 plan - low-risk cleanup search with submit-saving gate

## Context

- Current accepted Public Score floor: `7185.09` after case338.
- Submit-saving policy:
  - low-risk standalone submit threshold: `ln(old/new) >= 0.020`
  - bank exact micro candidates below threshold
  - do not submit rediscovered backlog micros individually
- Case338 adopted the only new low-risk cleanup above threshold from the prior
  scan.

## Target

No single task is selected up front. Search for low-risk exact candidates only:

- duplicate/dead initializers
- unused nodes
- mathematically guaranteed broadcast constant shrink
- tiny dtype/index initializer cleanup

## Hypothesis

After case338, remaining low-risk exact cleanups are likely to be known micro
items already listed in `micro_backlog.md`. If no new or stronger candidate
clears the `0.020` standalone threshold, skip Kaggle submit to preserve submit
budget.

## Verification Plan

1. Run low-risk cleanup scans on the accepted bundle.
2. Individually audit any candidate that improves cost.
3. Submit only if a new or strictly stronger candidate clears the low-risk
   standalone threshold.
4. Otherwise write a no-submit result and continue searching other families.
