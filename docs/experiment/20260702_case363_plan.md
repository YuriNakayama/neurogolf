# 20260702 case363 plan - post-adoption structural search

## Context

Accepted Public Score is `7185.78` after case362.

The submit-saving rule remains:

```text
single low-risk gain >= 0.020
single medium-risk gain >= 0.030
single high-risk gain >= 0.080
low-risk batch gain >= 0.030
mixed-risk batch gain >= 0.050
```

Correctness is still the hard gate (`n_fail=0`).

## Search Targets

1. Re-check non-overlapping structural candidates after the case362 batch.
2. Re-run the deterministic case3 surgery chain on the current accepted ONNX
   bundle to catch any new cleanup introduced by the accepted batch.
3. Do not submit known platform-risk candidates unless their compatibility is
   specifically fixed.

Excluded known families:

- accepted or just-submitted micro aliases from case362;
- `task118` table pruning below the high-risk isolated floor;
- `task243` fill-chain near misses;
- `task233` uint8 `TopK` score-table compression unless validator
  compatibility is repaired.

## Decision Rule

Submit only if the current-baseline candidate clears a risk-adjusted gain floor.
Otherwise, write the result and bank exact micros for a future batch.
