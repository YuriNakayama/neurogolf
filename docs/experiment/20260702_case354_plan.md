# 20260702 case354 plan - task285 connectivity shortcut with bridge guard

## Context

Case353 skipped submission because the best exact low/medium batch was only
`0.014890`, below the `0.030` submit-saving threshold.

For case354, the strongest non-task118 candidate is a repaired task285
connectivity shortcut:

```text
baseline:  params=550 memory=25080 cost=25630 n_fail=0
candidate: params=573 memory=23964 cost=24537 n_fail=0
gain:      ln(25630 / 24537) = 0.043581
```

## Hypothesis

The naive task285 shortcut replaced the final 5x5 connectivity expansion with
the source-pivot grid and saved more cost, but it produced one arc-gen failure by
creating a disconnected side branch. Adding a small bridge guard preserves the
rare boundary case while still pruning most of the expansion chain.

This is preferred over another task118 table-pruning attempt because:

- it is non-task118;
- it clears the medium-risk standalone threshold;
- local audit is exact on all task285 train/test/arc-gen pairs;
- the expected gain is larger than any accumulated low-risk bank.

## Implementation

Replace only:

```text
data/output/onnx/task285.onnx
```

with:

```text
/tmp/neurogolf_case354_struct/task285_conn_shortcut_bridge_guard.onnx
```

No other tasks are changed.

## Risk

Medium-high. The candidate is a semantic simplification of the connectivity
expansion, not a pure duplicate-initializer cleanup. It is isolated to one task
so that Public LB movement can be attributed cleanly.

Known unsafe families remain excluded:

- task101 broad local-equivalence;
- task118 table pruning;
- task233 uint8 `TopK`;
- task286 broad bitwise equivalence.

## Gate

Before submit:

1. Run `src.evaluate.audit_one` for task285 only.
2. Confirm `n_fail=0` and cost decrease.
3. Package and submit as a single-task-attributable change.
