# case244 plan: task205 all-ones Conv to AveragePool

## 現状
- 採用済み Public Score floor: 7181.26
- `task205` baseline は `cost=12854` (`params=640`, `memory=12214`, `n_fail=0`)。
- 先頭の `Conv(input, k6)` は `group=10`, `kernel_shape=6x6`, `strides=8x8` で、`k6` は全要素 1 の `(10,1,6,6)` float32。
- 後段は `ReduceMax` と `ArgMax` で最大 channel を選ぶだけなので、全 channel に同じ正スケールを掛けても順位は不変。

## 対象
- `data/output/onnx/task205.onnx`

## 仮説
全1 depthwise Conv の sum を `AveragePool(kernel=6,stride=8)` の average に置換しても、channel ranking は同じため完全一致を保てる。これにより `k6` initializer 360 params を削除でき、memory を増やさず cost を下げられる可能性が高い。

## 検証
- `Conv code` を `AveragePool` に置換し、`k6` initializer を削除する。
- `src.evaluate.scorer.audit_one` で `task205` の全ペア完全一致と cost を確認する。
- `n_fail=0` かつ cost 改善が出た場合のみ実 ONNX に反映し submit する。
