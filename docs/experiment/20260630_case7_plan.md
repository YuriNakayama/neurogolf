# 20260630 case7 plan — bit-pack/k-local per-task 設計の探索 + 公開フロント再確認

## 仮説
surgery/harvest 飽和（cycle2/4/6）後、残る hand-golf 勝ち筋を探る:
1. 純幾何変換タスク（flip/rot/transpose）→ 極小 Gather net で floor を破れるか。
2. 厳密 3×3-local タスク（output cell = 3×3 窓の決定的関数）→ 完全 lookup net。
3. 新 boristown revision（16:21 再走）の harvest。

## 対象 / 手法
- 上記 3 クラスを全 400 で網羅判定し、高コスト × 単純構造のタスクを 1 つ選んで設計 or harvest。

## 採否
- faithful n_fail=0 かつ cost<floor を submit。hidden 汎化が疑わしい fit（k-local 巨大 table）は
  cycle1 の教訓により提出しない。
