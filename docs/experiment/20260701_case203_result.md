# case203 result: task231 gather index branch trim

## 結果

- 対象: `task231`
- baseline: `cost=219`, `params=39`, `memory=180`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

middle gather index branch を固定化した。

| 変更 | cost | n_pass | n_fail |
|---|---:|---:|---:|
| `mid_idx -> phase_mid` | 150 | 53 | 213 |
| `mid_idx -> zero_mid` | 182 | 63 | 203 |
| `gather_idx -> all_phase` | 150 | 53 | 213 |
| `gather_idx -> all_zero_mid` | 150 | 63 | 203 |

## 結論

`task231` の `active_mid` branch は幅に応じて必要。固定化では完全一致せず、実 ONNX は変更していない。

## 次の一手

次は `task236` / `task072` の上下比較 `Where(Equal(...), classA, classB)` 系を確認する。比較結果が片側固定でなければ早く切る。
