# 20260701 case261 plan - task288 ScatterND indices int32

## 背景

- Current floor: Public Score `7181.61`
- `task288` baseline: `cost=958` (`params=189`, `memory=769`, `n_fail=0`)
- 小 cost task では固定量の削減でも `ln(old_cost/new_cost)` の効率が高い。

## 対象

- `data/output/onnx/task288.onnx`

## 仮説

`task288` は `ScatterND` 用の `scatter_indices` を int64 で作っている。

```text
zero_i64_12x1, channel_col, rowcol12 -> Concat -> scatter_indices [12,4]
```

ONNX `ScatterND` が int32 indices を許すなら、`rowcol12_bank` / `zero` / `channel_mask` を int32 にし、`antenna_index_3d` だけ int32へ cast して indices 全体を int32化できる。`scatter_indices` と `rowcol12` 系の memory/params が半分になり、cost 958 の task では相対削減率が見込める。

## 手順

1. `/tmp` に `task288` の int32 indices 版 ONNX を生成する。
2. `src.evaluate.scorer.audit_one` で `task288` の完全一致と cost を確認する。
3. `n_fail=0` かつ cost 減なら本体へ反映し、Kaggle submit で採否を決める。
