# 20260703 case563 result - structural-only search no-submit

## Submission

- Submitted: no
- Accepted Public Score remains: `7187.57`
- Decision: no-submit

## Search

Case563 avoided raw dtype narrowing after the `task316` Kaggle ERROR and focused
on structural reductions:

- single-consumer alias rewires;
- dead-path pruning;
- same-shape/broadcast-safe cleanup;
- duplicate/unused initializer cleanup.

Parallel worker results:

```text
range A task001-task180: scanned 24 candidates, submit-candidate 0, bank-low 0
range B task181-task300: scanned 0 candidates, submit-candidate 0, bank-low 0
range C task301-task400: scanned 81 candidates, submit-candidate 0, bank-low 0
```

Main-thread stale structural re-gates also found no current positive candidates:

```text
task090, task209, task381, task397: exact but already current baseline
task161, task213, task238: worse than current baseline
task055/task011/task013 stale pool: local failures only
```

## Conclusion

No Kaggle submission was made because there was no exact positive candidate. The
structural-only pass reduces Kaggle ERROR risk, but candidate supply is currently
too sparse under this restriction.

## Next Step

Case564 should broaden the search again, but with explicit risk segmentation:

1. allow low-risk dtype or initializer rewrites only if they are not in known
   runtime-sensitive task families;
2. keep `task316` review-risk blocked;
3. use `scan-candidates` to separate exact submit candidates from local-fail
   near misses quickly.
