# 20260630 case6 plan — 新規採用 net（lucifer t233 等）への graph-surgery 再走

## 現状把握
- floor 7180.58（cycle1 で lucifer t233 を採用）。cycle2 で surgery 飽和と記録したが、それは
  **旧 floor に対して**。cycle1 で t233 を lucifer net に差し替えたため、この新 net は未 surgery。
- t233 cost 56707 の内訳: 多数の [5,324] bool/uint8/float 中間（各 1620B）の総和。runtime trace で
  output[1,10,30,30] bool=9000B + float[5,324]=6480B 等。意味保存縮約の余地あり。

## 仮説
`pipeline/case3/surgery.py`（意味保存・audit_one 検証付き: int64→int32 narrow, cleanup,
index_surgery, broadcast_compress, conv1x1→gather, fp16）を**現 floor 全体に再走**すると、
cycle1 で新規採用した lucifer/harish 由来 net（過去 surgery 未適用）で cost 減が出る可能性。
意味保存パスは hidden 安全（grader-faithfulness）。

## 対象 / 手法
- 全 floor に surgery を走らせ、n_fail=0 かつ cost 減のタスクのみ採用 → overlay → submit。

## 採否
- 各 net は audit_one で n_fail=0 確認済みのもののみ surgery 出力に含まれる。
- combined を submit、実 LB が 7180.58 を上回れば採用、否なら revert。
