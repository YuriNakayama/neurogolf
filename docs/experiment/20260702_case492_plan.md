# 20260702 case492 plan - task029 cadence scan

## Context

Current accepted Public Score:

```text
7186.79
```

Expanded workers are still running, but recent completed scans have produced no
meaningful submit candidate. Tiny exact micros are banked instead of spending a
submit slot.

## Target

```text
task029.onnx
```

Reason:

- moderate scratch pool;
- not exhausted by the main thread in the latest accepted baseline;
- avoids the most clearly known hidden-regression and Kaggle ERROR families;
- ranking is by relative cost improvement `ln(old_cost / new_cost)`.

## Method

1. Gather unique `task029*.onnx` from `/tmp` and `/private/tmp`.
2. Measure cost-only with `evaluate.audit_one`.
3. Gate cost-positive candidates using `submit.gate.evaluate_candidate_gate`.
4. Submit only if the decision is `submit-candidate`.

## Parallel Work

Workers continue on:

- `task054/task066/task076/task077`;
- `task005/task048/task018`;
- low-risk initializer/params cleanup discovery.
