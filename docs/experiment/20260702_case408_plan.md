# 20260702 case408 plan - task335 isolated duplicate cleanup

## Context

Accepted Public Score is `7186.65` after case407 adopted the isolated
`task343` repaired guard alias.

Case406 bundled four candidates and regressed, making attribution ambiguous.
The low-risk member was `task335`, a pure duplicate-initializer cleanup. It is
below the preferred `gain >= 0.02` submit gate, but it is the best current
low-risk candidate and isolating it can determine whether case406's regression
came from the filler tasks.

## Candidate

```text
task335 3002 -> 2952 gain 0.016795826473
```

Scratch file:

```text
/tmp/neurogolf_case406_cleanup_scan/task335_cleanup.onnx
```

## Method

Replace duplicate initializer uses and prune the duplicate tensors:

```text
col_sel   -> row_sel
pool_rows -> pool_cols
```

This reduces params by `50` while leaving runtime tensor memory unchanged.

## Gate

Submit only if targeted `audit_one` passes:

```text
task335: n_fail=0, functions=0, forbidden ops=[]
```

Risk: low. This is a structural duplicate-initializer cleanup, not a
data-dependent guard, table pruning, dtype conversion, or TopK/runtime-sensitive
operator change. Submit isolated and adopt only if Public Score improves over
`7186.65`.
