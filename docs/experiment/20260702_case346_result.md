# 20260702 case346 result - task158 diagonal slice alias

## Result

- Previous accepted Public Score: `7185.33`
- New Public Score: `7185.37`
- Kaggle submission ref: `54237941`
- Submission time: `2026-07-01 16:10:07 UTC` (`2026-07-02 01:10:07 JST`)

Accepted.

The submit interval from case345 was about 26 minutes. This is close to the
30-minute cadence and used a single isolated threshold-clearing candidate.

## Local Gate

`task158` only:

```text
baseline:  params=2629, memory=29785, cost=32414
candidate: params=2613, memory=28681, cost=31294
n_fail=0, status=ok
gain = ln(32414 / 31294) ~= 0.0352
```

## Change

Applied the diagonal slice alias bundle:

```text
tr28 -> tl28
br28 -> bl28
```

The symmetric alternative `bl28 -> tl28`, `br28 -> tr28` had the same local
cost and audit result, so only one alternative was submitted for attribution.

## Deferred Candidates

`task233 scorew uint8` remains a viable banked candidate:

```text
49454 -> 47804
gain ~= 0.0339
n_fail=0
```

It was not mixed into this submit to preserve one-task attribution.

`task018` and `task366` deep inspection did not find a submit-worthy candidate.
The best `task018` cleanup was `39583 -> 39581`, far below threshold; task366
cleanup/crop probes had no gain or failed correctness.
