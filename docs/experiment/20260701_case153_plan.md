# 2026-07-01 case153 plan

## 方針

- floor: Public Score 7181.17
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task354.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task354.onnx`
- baseline: params 79, memory 2998, cost 3077, points 16.968289624677958, n_pass 266, n_fail 0

## 仮説

`task354` は top row の left/mid/right color と gray row の gap から segment id を作り、left/mid seed segment をそれぞれ塗る。`gap` による sentinel、`left_light` / `mid_light` による seed segment、`left_same_seg` / `mid_same_seg` の fill branch が主な cost 要因。

実データで seed が常に存在する、または left/mid の片側が不要なら、segment seed guard や fill branch を削れる可能性がある。cost は 3077 で、10x10 mask 1個の削減でも相対効率がある。

## 手順

1. `/tmp` 上の candidate ONNX で `seg_gray` / `right_fill` / `left_seed_seg` / `mid_seed_seg` を片側へ差し替える。
2. `left_rect` / `rect_label` / `label10` の fill branch を個別に簡約する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task354.onnx` に反映する。
4. submit し、Public Score が 7181.17 を上回れば採用、上がらなければ破棄する。
