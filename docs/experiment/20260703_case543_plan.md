# 20260703 case543 plan - task023 follow-up and micro refresh

Accepted Public Score:

- `7187.43`

## Background

Case542 adopted task023:

```text
task023 8764 -> 8548
gain    0.024955
Public  7187.41 -> 7187.43
```

The successful change was a local alias/DCE rewrite:

```text
Sadd4 -> u4
```

Because the current accepted task023 graph changed, previously stale or blocked
task023 artifacts need a fresh current-baseline recheck. Artifact mining also
expanded the low-risk micro bank, but the best max-5 bundle is still only
`0.003669`, below the `0.015` micro-bundle gate.

## Hypothesis

The best next submit candidate is likely another scorable task023 alias on top
of the new accepted graph, because the same local-equivalence family already
proved Kaggle-safe in case542. If no task023 follow-up exists, search should
continue over scorable non-risk candidates only.

## Method

1. Rebase task023 alias candidates on the accepted case542 task023 graph.
2. Gate each task023 candidate individually; submit only if gain `>= 0.020`.
3. Recompute the low-risk micro bank from current baseline, including new
   task368/task333/task358/task388/task361 material.
4. Keep excluding unscorable-baseline replacements and hard/review/runtime-risk
   tasks.

## Submit Rule

Use normal pre-submit gate only. Do not use `--skip-pre-submit-gate`.
