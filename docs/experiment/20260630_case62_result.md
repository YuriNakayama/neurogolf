# 20260630 case62 result — tiny-output タスクは複雑な選択規則で安価集約不可（提出なし）

## independent_impl（必須欄）
未試行角度「tiny-output（≤3×3）の高コストタスク = 単純集約で勝てる可能性」を全 floor から探索:
- tiny-output(≤9 cells): **51 件**。高コスト: t079(3143), t394(2800), t355(2755,1×1), t184(2520)。
- **t079 精査**（floor 3143, 出力 3×3）: 入力に散在する複数の 3×3 shape から、**ユニークな
  shape を1つ抽出**する規則（pattern-counting + selection）。shape 照合が本質で複雑。
- 1×1 出力(t355/t346/t048)は case11 で**複雑選択規則**（noise-neighbor-majority 等）と既判明、
  単純集約(max/mode/min)では解けず。

## 確定
- **tiny-output ≠ 単純規則**: 出力が小さくても、その1色/小パターンを選ぶ**選択ロジックが複雑**
  （ユニーク shape 検出 / 領域選択 / pattern 照合）。floor のコストは選択複雑度に見合う。
- 安価集約（単一 ReduceMax/mode）で解ける tiny-output タスクは floor に残っていない。

## 28サイクルの総括
独自実装の全角度（cost削減25レバー + flood-fill/gravity/denoise/classification/tiny-output の
規則類型）を網羅。floor は per-task で規則複雑度に見合う最小 op 構成に到達済み。
独自実装は正答構成可能だが parity が上限、純増余地は本探索(28サイクル, case30/35-62)で未発見。

## 恒久方針の遵守
- 停止せず tiny-output を網羅探索し複雑選択規則を実証。floor 7180.78 退行なし。
- 各タスクの実 decode に基づく結論。次も新類型を継続 decode。

## 次の一手
- 未 decode の規則類型（mirror/symmetry完成, periodic修復, object-count）を引き続き精査。
  floor が過剰 op のタスクを per-task で探し続ける。
