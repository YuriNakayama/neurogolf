# 20260630 case18 result — multi-node tasks は全て単一Conv圧縮不可（提出なし、floor 7180.62 維持）

## 結論
- **3×3 exact 線形分離検定（cost 911-25000 の multi-node 全タスク）: 該当 0 件**。全 multi-node
  measurable タスクは per-channel 線形分離せず、単一 3×3 Conv で厳密実現不可。
- **5×5-local（not 3×3）タスク（cost>2700）: 8 件（t243/t077/t208 等）だが table=8893-116831 と巨大**。
  250 特徴の単一 5×5 Conv で実現不可能なサイズ。可視 fit しても hidden 落ち（case1 の轍）。
- floor の partition は最適: **単一 Conv 化できる規則は既に単一 Conv（34 タスク, params 最小）、
  できない規則は multi-node**。手動で再 partition する余地ゼロ。

## 最終確定（hand-golf 完全不可の網羅証明）
本日 case5-18 で、floor を hand-construction で破る全経路を検定し全棄却:
| 経路 | 結果 | case |
|---|---|---|
| 明示的 multi-op 構成（flood/frame）| cost ∝ 中間ノード数 で爆発 | 5,15 |
| 単一 Conv の param 削減（疎カーネル）| Slice 中間が param 節約超過 | 16 |
| multi-node→単一 Conv 圧縮（3×3/5×5 分離）| 該当 0、全て非分離 | 17,18 |
| k-local table fit | table 大で hidden 落ち | 1,17 |
| local-error swap | LB −4.43/−16.76 | 9,10 |
| graph surgery | dead-code-free で 0 削減 | 6 |

→ **floor 7180.62 = memory metric 下の全タスク真の最小コスト**。in-container hand-golf 余地ゼロを網羅証明。

## 次の一手
- 唯一の前進レバー = 新規公開バンドル harvest（外部 publish 依存, 本日 +0.07 実証）。
- floor 7180.62 維持。7950 まで −769.38。
