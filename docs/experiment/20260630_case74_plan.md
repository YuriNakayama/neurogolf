# 20260630 case74 plan — t349 case71後に残った未使用 `beam_b` Cast を削除

## 仮説
case71 で `Where(halo_b, three_u8, beam_u8)` に変更した結果、旧経路
`beam_u8 -> Cast(bool) -> beam_b` は後段から参照されなくなった。
この dead-end Cast は `[1,1,30,30]` bool 中間 900B を計上しているため、削除すれば
faithful を保ったまま cost を 900 下げられる。

## 対象
- task349
- 現行: cost 25289（params=89, memory=25200）

## 手法
1. graph から `Cast ['beam_u8'] -> ['beam_b']` を削除。
2. `audit_one(run_correctness=True)` で n_fail=0 と cost 24389 を確認。
3. submit し、Public Score が 7180.81 を上回れば採用。
