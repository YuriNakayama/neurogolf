# 20260702 case343 plan - submit-saving low-risk batch check

## Context

- Current accepted Public Score floor: `7185.13` after case342.
- Submit cadence: about once every 30 minutes only when a threshold-clearing
  candidate or batch is available.
- Low-risk micro batch threshold: total expected gain `>= 0.030`.
- Mixed-risk batch threshold: total expected gain `>= 0.050`, excluding known
  hidden-unsafe families.

## Targets

This cycle checks whether the current banked micro candidates justify spending
the next submit window.

1. Re-scan low-risk duplicate/unused initializer and constant broadcast cleanup.
2. Revisit `task118` final `ScatterElements` with the large `channel_indices`
   initializer, but only as a temp ONNX candidate.
3. Submit only if a verified candidate or same-risk batch clears the
   submit-saving threshold.

## Gate

- Every candidate must pass `audit_one` with `n_fail=0`.
- Do not submit if the low-risk batch total stays below `0.030`.
- Do not mix medium/high-risk material into a low-risk batch just to clear the
  threshold.
