# 20260630 case88 result — t158 の stamp scale branch は全て必要（提出なし）

## 対象

- `data/output/onnx/task158.onnx`
- baseline: params **2340**, memory **32987**, cost **35327**, `n_fail=0`

## trace

主な memory 支配テンソル:

- `color_f`: float `[1,1,30,30]` = 3600B
- `pair_u83`: uint8 `[1,4,24,23]` = 2208B
- `pair_u82`: uint8 `[1,4,21,20]` = 1680B
- `ab_u8`: uint8 `[1,2,26,25]` = 1300B
- `pair_u81`: uint8 `[1,4,18,17]` = 1224B
- `fill_u81/fill_u82/fill_u83/fill_any`: uint8 `[1,1,26,25]` = 650B each

## 検証

### stamp branch 単独化

`fill_any_u8` の利用先を各 branch に直接差し替え、未使用 branch を剪定。

| 変更 | params | memory | cost | 結果 |
|---|---:|---:|---:|---|
| only `fill_u81` | 376 | 27197 | 27573 | `n_fail=158` |
| only `fill_u82` | 796 | 26885 | 27681 | `n_fail=182` |
| only `fill_u83` | 1432 | 26789 | 28221 | `n_fail=224` |

### 2 branch 化

`Max(fill_u81, fill_u82, fill_u83)` から 1 branch を削除。

| 変更 | params | memory | cost | 結果 |
|---|---:|---:|---:|---|
| drop `fill_u81` | 2096 | 30057 | 32153 | `n_fail=134` |
| drop `fill_u82` | 1676 | 30369 | 32045 | `n_fail=111` |
| drop `fill_u83` | 1040 | 30465 | 31505 | `n_fail=54` |

全 branch が arc-gen 正答性に必要。`fill_u83` 削除が最も近いが 54 failure で不採用。

### 機械的削減

- unused initializer: なし
- duplicate initializer: なし

## 採否

- Kaggle submit: **なし**
- 採否: **不採用**
- Public Score floor: **7180.87 維持**

## 結論

t158 は 3 scale の stamp branch がそれぞれ異なる arc-gen 例に効いており、branch 削減では正答性を維持できない。params 大の branch を削る余地はあるが、現行 faithful を保つには全 branch が必要。

## 次の一手

次は t133/t209 の高コスト memory 支配 graph を検証する。
