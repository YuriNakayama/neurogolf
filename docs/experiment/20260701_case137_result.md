# 2026-07-01 case137 result

## 対象

- `task274.onnx`
- baseline: params 80, memory 146, cost 226, points 19.579465000727716, n_pass 269, n_fail 0

## 実施内容

`Clip(space_formula, one, two)` と、その後段の `space_nonbase` / `space` / output channel branch を `/tmp` 上の候補 ONNX で差し替えた。

## 足切り結果

cost 減で完全一致する候補はなし。

- `space_row5_zero -> space_formula`: cost 224, n_fail 2
- `space_row5_zero -> one`: cost 224, n_fail 5
- `space_row5_zero -> two`: cost 224, n_fail 9
- `space_nonbase -> space_row5_zero`: cost 222, n_fail 37
- `space_nonbase -> four`: cost 221, n_fail 14
- `space -> space_formula`: cost 218, n_fail 28
- `space -> space_nonbase`: cost 224, n_fail 102
- `out0_small -> zero_small`: cost 216, n_fail 269

完全一致だが cost が変わらなかった候補:

- `Clip` の上限を `two -> four`: cost 226, n_fail 0
- `Clip(space_formula, one, two)` を `Max(space_formula, one)` に置換: cost 226, n_fail 0

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

上限 clamp は実データ上不要だが、scorer の cost は `Clip` と `Max` で同じだった。下限 clamp は2 pair で必要なため、Clip 全体を削ることはできない。

## 次の一手

Clip 系では `task361` の3つの index Clip を確認する。ただし index 安全化の可能性が高いため、失敗なら Clip 横断を一旦止め、`Cast(bool)->uint8` や redundant `Where` の相対削減候補へ戻る。
