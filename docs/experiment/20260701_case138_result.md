# 2026-07-01 case138 result

## 対象

- `task361.onnx`
- baseline: params 367, memory 4472, cost 4839, points 16.51553663320668, n_pass 266, n_fail 0

## 実施内容

末尾の回転 scatter index safety Clip 3箇所を `/tmp` 上の候補 ONNX で個別に外した。

## 足切り結果

すべて `n_fail>0`。Clip を外すと `ScatterElements` が範囲外 index を受ける pair が多数発生した。

- `rot1_safe_i32 -> rot1_idx_i32`: cost 4779, n_fail 62
- `rot2_safe_i32 -> rot2_idx_i32`: cost 4779, n_fail 184
- `rot3_safe_i32 -> rot3_idx_i32`: cost 4779, n_fail 138

代表的な runtime error:

```text
ScatterElements: indices element out of data bounds, idx=107 must be within the inclusive range [-100,99]
```

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

`task361` の Clip は実データ上でも範囲外回転 index の安全化に必須だった。Clip 横断のうち、小型候補 `task250` / `task274` / `task361` はいずれも有効な cost 減なし。

## 次の一手

Clip 横断を一旦止める。次は `Cast(bool)->uint8`、`Where(cond, x, zero)`、`Equal` one-hot 直前の冗長 branch を持つ中 cost タスクを探し、相対削減率が高い候補を選ぶ。
