# 20260702 case478 plan - task367 current kernel/rect re-gate

## Context

Accepted Public Score is `7186.73` after case474. Case475 regressed and its
members are quarantined, so this cycle avoids weak mixed micro bundles.

`task367` is a current high-cost non-risk task:

```text
task367 cost=20699
params=3899
memory=16800
```

Historical evidence:

- case361 adopted a `task367` rect/kernel crop and moved Public Score;
- it is not in the current hard/review/runtime risk gate sets.

## Target

```text
task367
baseline: data/output/onnx/task367.onnx
task json: data/lake/neurogolf-2026/task367.json
```

## Hypothesis

The accepted kernel crop may not have exhausted every same-family current
baseline simplification. Existing scratch candidates should be re-ranked against
the current accepted graph before any new surgery.

## Method

1. Gather `task367*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against current baseline.
3. Correctness-gate positives with `submit.gate.evaluate_candidate_gate`.
4. Submit only an isolated candidate satisfying:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Otherwise record no-submit and bank only exact low-gain candidates.
