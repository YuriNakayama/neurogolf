# 20260703 case541 result - reject task135 unscorable replacement

Accepted Public Score before submit:

- `7187.41`

## Summary

Rejected.

Kaggle submission:

```text
ref          : 54275008
description  : case541 isolated task135 unscorable replacement
status       : COMPLETE
Public Score : 7186.80
```

The isolated task135 replacement regressed Public Score by `-0.61`, so it is
not adopted.

## Submitted Candidate

Candidate:

```text
/private/tmp/neurogolf_agent_case540_C/custom_candidates/task135_crop_0_6.onnx
```

Local candidate audit:

```text
task135 candidate cost: 368
n_fail                : 0
```

The accepted baseline task135 is locally unscorable:

```text
status: score_error:[ShapeInferenceError]
cost  : None
n_fail: 0
```

The failure mode is strict ONNX shape inference rejecting negative pads in the
accepted baseline `Conv`. Because the baseline cost is unknown, the normal
pre-submit gate blocks the replacement as `blocked-baseline`. This cycle used
`--skip-pre-submit-gate` deliberately for one isolated information-gathering
submit, because the candidate was non-risk, packageable, exact locally, and much
smaller than the accepted baseline file.

Actual Public Score proved that this replacement is unsafe. Task135 has been
added to `HARD_RISK_TASKS`:

```text
case541 unscorable-baseline crop replacement regressed Public validation
```

## Task173 Risk Rehabilitation

The task173 standalone-size candidate from case510/case540 was re-audited:

```text
baseline : 15664
candidate: 15020
gain     : 0.041982
n_fail   : 0
```

The current baseline and rejected candidate differ by exactly one ONNX
attribute:

```text
Cast node "grid_score"
baseline  to=10 FLOAT16
candidate to=2  UINT8
```

An equivalent `INT8` variant has the same local gain. The first `TopK` consumes
this integer tensor. Direct repairs that avoid 8-bit `TopK` were exact but did
not keep positive gain:

```text
INT16   cost=15664  gain=0
INT32   cost=16952  gain=-0.079021
FLOAT   cost=16952  gain=-0.079021
INT64   cost=19528  gain=-0.220484
DOUBLE  cost=19528  gain=-0.220484
```

Conclusion: the task173 gain depends on 8-bit `TopK`, the same family that
already produced Kaggle ERROR. Do not resubmit task173 dtype-shrink variants
without a new implementation that avoids 8-bit `TopK`.

## Parallel Search Results

Five workers were used:

- task173 ERROR analysis: no repaired submit candidate.
- non-risk structural search: no scorable exact lower-cost candidate.
- overlooked mid-gain mining: no submit queue; task184/task238/task354 were
  already absorbed, task243 still failed.
- task173 non-8bit alternative: no viable candidate.
- skipped/unscorable audit: task135 and task127 candidates were exact, but
  baseline costs were unknown; task135 submit proved unsafe.

Structural sparse-kernel probes for task222, task396, and task324 were locally
correct but unscorable under the validator, so they are not submit candidates.

## Decision

Reject case541. Keep accepted Public Score at:

```text
7187.41
```

Do not adopt the task135 ONNX into `data/output/onnx`.

## Next Step

Avoid unscorable-baseline replacement submits unless there is task-specific
evidence that the private benchmark behavior is preserved. Continue searching
for non-risk structural candidates that are both locally exact and scorable, or
for isolated review-risk repairs with a direct explanation of the prior Kaggle
failure.
