# 20260701 case179 result

## Target

- Task: `task301`
- Baseline cost: 1141
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task301`.

No exact cost-reducing candidate was found.

Threshold aliases:

- `b_thresholds -> bcolor_thresholds`: cost 811, `n_fail=266`
- `b_thresholds -> b0_thresholds`: cost 789, `n_fail=266`
- `a_compact -> a0`: cost 524, `n_fail=266`
- `a0 -> a0_uncapped`: cost 1128, `n_fail=124`
- `target_plus1_u8 -> counts_nonbg`: cost 1131, `n_fail=200`
- `target_plus1_u8 -> gap_u8`: cost 1036, `n_fail=266`
- `height_u8 -> width_u8`: cost 1135, `n_fail=200`
- `gap_u8 -> height_u8`: cost 1140, `n_fail=266`
- `gap_u8 -> width_u8`: cost 1140, `n_fail=256`

Constant aliases:

- `gap_u8 -> zero_u8/one_u8`: cost 1141, `n_fail=200`
- `gap_u8 -> high_u8`: cost 1141, `n_fail=266`
- `height_u8 -> zero_u8/one_u8/high_u8`: cost 1136, `n_fail=266`
- `width_u8 -> one_u8/high_u8`: cost 1141, `n_fail=266`
- `width_u8 -> zero_u8`: invalid integer division by zero

Shape/type failures:

- `a_thresholds -> a_compact` or `a0`: invalid final `Greater` shape
- `bcolor_thresholds -> bcolor`: invalid repeated threshold shape
- `bcolor -> width_u8/height_u8/gap_u8/zero_u8`: invalid `Pad` or `Concat` shape
- `a_compact -> acolor`: invalid `Greater` dimensions
- `acolor -> counts_nonbg/zero_u8`: invalid `Concat` shape
- `a0 -> width_limit/height_u8/width_u8`: invalid `Concat` shape
- `row_eq -> counts_nonbg`: invalid `Where` condition type
- `width_u8 -> height_u8`: cyclic graph
- `counts_nonbg -> color_counts_u8`: invalid `Greater` dimensions
- `total_area_u8 -> color_counts_u8`: invalid `Add` dimensions
- `color_counts_u8 -> color_counts_f`: invalid `Div` type binding

## Submission

- Not submitted.
- Reason: all scored cost reductions failed exact-match auditing or produced invalid
  ONNX.

## Decision

Rejected. Keep the existing `task301.onnx`.

## Next

The capped `a0` path is the closest simplification but still fails 124 examples.
Continue toward tasks where the first-order simplification fails on only a few
examples or where a dtype-preserving rewrite changes scored memory exactly.
