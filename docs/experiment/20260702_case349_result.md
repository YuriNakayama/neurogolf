# 20260702 case349 result - low-risk micro cleanup batch adopted

## Result

- Previous accepted Public Score: `7185.40`
- Kaggle submission ref: `54239852`
- Submission status: `SubmissionStatus.COMPLETE`
- Public Score: `7185.43`
- Submission time: `2026-07-01 17:40:31 UTC`
  (`2026-07-02 02:40:31 JST`)

Adopted.

## Submitted Batch

| task | change | baseline cost | submitted cost | gain |
|---:|---|---:|---:|---:|
| 003 | duplicate initializer cleanup `probe_wzp -> probe_zp` | 263 | 262 | 0.003810 |
| 052 | duplicate initializer cleanup `tailpack_052_wzp -> tailpack_052_xzp` | 209 | 208 | 0.004796 |
| 117 | compact rewire `condition_left -> col_local_i64` | 4158 | 4149 | 0.002167 |
| 334 | duplicate initializer cleanup `probe_wzp -> probe_zp` | 131 | 130 | 0.007663 |
| 361 | duplicate/unused initializer cleanup | 4787 | 4779 | 0.001673 |
| 363 | broadcast-compress `kt1` from `(1,1,4,4)` to `(1,1,4,1)` | 4360 | 4348 | 0.002756 |
| 382 | alias `h_s4 -> hsrc`, `v_s4 -> vsrc` | 5772 | 5730 | 0.007303 |

Total local gain:

```text
0.030167
```

## Gate

All seven changed tasks were individually checked with
`evaluate.audit_one(..., run_correctness=True)`:

```text
task003 n_fail=0 cost=262
task052 n_fail=0 cost=208
task117 n_fail=0 cost=4149
task334 n_fail=0 cost=130
task361 n_fail=0 cost=4779
task363 n_fail=0 cost=4348
task382 n_fail=0 cost=5730
```

Packaging dry-run also completed and included all 400 tasks. The seven existing
local-cost-unestimated tasks remained structurally valid, matching previous
accepted submissions.

## Conclusion

The low-risk batch moved Public Score by `+0.03`, validating the submit-saving
policy: small exact cleanups can be banked and submitted together once combined
relative gain clears the batch threshold.

## Next Step

The strongest currently known standalone candidate is `task118`
`/tmp/neurogolf_case350_search/task118_pos_keepfirst.onnx`:

```text
39237 -> 37989
gain ~= 0.0323
n_fail=0
```

However this is table pruning and is marked hidden-sensitive in the backlog.
If attempted, submit it isolated for attribution. The safer fallback is the
banked `task209` structural alias bundle (`28683 -> 28047`, gain `0.0224`) plus
additional low-risk micro material once enough exists to clear a batch threshold.
