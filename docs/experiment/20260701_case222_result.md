# case222 result: task235 code mask trim

## 結果

- 対象: `task235`
- baseline: `cost=170` (`params=59`, `memory=111`, `n_fail=0`)
- submit: なし
- 採否: 不採用

## 検証内容

temp ONNX で以下を個別に差し替え、`src.evaluate.scorer.audit_one` で検証した。

- `codes_f` 固定候補
- `top_left` 固定候補
- 1 channel 固定 mask
- `code_values` の channel 数削減

## 監査結果

| 変更 | cost | n_pass | n_fail | 判定 |
| --- | ---: | ---: | ---: | --- |
| baseline | 170 | 69 | 0 | ok |
| `codes_f` fixed triples | 107 | 0-1 | 68-69 | reject |
| `top_left` fixed triples | 134 | 0-1 | 68-69 | reject |
| single channel fixed mask | 134 | 0-1 | 68-69 | reject |
| `code_values` first 3-6 channels | invalid | 0 | 69 | reject |
| `code_values` first 7 channels | 170 | 69 | 0 | no improvement |

## 結論

3 点 code と 7 channel `top_left` mask は入力依存で、固定化は全ペア正答に必要な情報を失う。channel 数削減は output channel shape が 10 に戻らず invalid。実 ONNX は変更せず、Kaggle submit もしない。

次は固定 mask ではなく、scatter/gather の index dtype や table 圧縮で同等関数を保てる候補を探す。
