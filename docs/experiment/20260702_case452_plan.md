# 20260702 case452 plan - task102 fill-core candidate gate

## Context

Post-regression work must avoid revisiting tasks already closed by a
current-baseline result. Recent logs closed `task243`, `task133`, `task187`,
`task355`, `task131`, `task162`, `task070`, `task205`, `task174`, `task191`,
`task216`, `task371`, `task039`, `task242`, `task341`, `task319`, and `task044`.

Case438 mentioned three small Pad/output-mask tasks whose existing large
reductions failed locally:

```text
task102 cost=2527
task190 cost=2527
task062 cost=2536
```

`task102` has no dedicated current-baseline result yet and has a bounded scratch
pool, so it is the next single-task target.

## Target

`task102.onnx`

Current baseline:

```text
params=113
memory=2414
cost=2527
points=17.1652118926
status=ok
```

## Hypothesis

The known `fill_core` reductions are likely semantic and may fail locally, but
there may be a small current-baseline exact cleanup or a mechanically simple
candidate hidden among the existing surgery/alias pools.

Because the task cost is only `2527`, even a few hundred bytes saved gives a
large relative gain. However, after recent regressions, only an isolated
`n_fail=0` candidate with `gain >= 0.020` should be considered for submit.

## Method

1. Gather existing `task102*.onnx` scratch candidates.
2. Run cost-only screening against the current accepted baseline.
3. Run correctness gate only for positive cost reductions.
4. Submit only if a candidate reaches `submit-candidate`; otherwise bank exact
   micros and write a no-submit result.

## Gate

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
no forbidden ops/functions
single task only
```
