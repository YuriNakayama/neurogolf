# 20260701 case179 plan

## Context

- Current accepted Public Score: 7181.17
- case178 showed `task304` has no exact block-expansion trim.
- Continue with compact medium-cost tasks where threshold construction may contain
  redundant branches.

## Target

- Task: `task301`
- Baseline audit:
  - params: 64
  - memory: 1077
  - cost: 1141
  - points: 17.961787590757317
  - n_pass: 266
  - n_fail: 0
  - filesize: 2061

## Hypothesis

`task301` computes non-background color counts, derives width/height/gap, builds a
set of horizontal `a_thresholds` and vertical `b_thresholds`, then compares them.

Potential exact reductions:

- `a0` capping or `acolor` row-equality logic may be redundant.
- `bcolor_thresholds` repeated across columns may be replaceable by a simpler
  compact vector.
- The final comparison may tolerate a simplified side on all public examples.

## Method

1. Probe one alias at a time on temporary `task301` ONNX copies.
2. Run `audit_one` only for `task301`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record failed threshold aliases and keep baseline ONNX otherwise.
