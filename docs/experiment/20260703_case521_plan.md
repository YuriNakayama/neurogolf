# 20260703 case521 plan - task033 near-miss repair

## Context

case520 found no submit candidate, but task033 produced the closest near miss:

```text
task033 baseline cost=1639
candidate cost=1594
gain=0.027840
n_fail=3
candidate=/tmp/neurogolf_agent_V3/task033_alias_018_1_line_cell_1_0_TO_line_cell_0_1_dce.onnx
```

The gain is above the standalone submit threshold. If the three failing examples
can be repaired cheaply, task033 can become a submit candidate.

## Hypothesis

The candidate aliases one `line_cell` tensor to a neighbor. The low fail count
suggests a rare orientation or endpoint case. A narrow guard or alternative
alias may restore those examples without giving back the full cost saving.

## Plan

1. Compare baseline, candidate, and target outputs for all task033 examples.
2. Identify failing examples and whether the mismatch is localized.
3. Try minimal static repairs in `/tmp`:

```text
single alternative alias
small selector/guard if already present
partial restore of the affected line_cell only
```

4. Gate candidates against current accepted baseline.
5. Submit only if `n_fail=0` and `gain >= 0.020`.
