# 20260701 case267 plan - task009 suffix false mask Pad

## 背景

- Current floor: Public Score `7181.63`
- 優先基準: `n_fail=0` を絶対条件にし、`old_cost / new_cost` の相対削減率を優先する。
- `task009` baseline: `cost=8410` (`params=1081`, `memory=7329`, `n_fail=0`)
- zero edge Concat 横断検索で、suffix false mask 2 箇所を検出した。

```text
Concat(v_mask_tail, false_col, axis=3) -> v_sep_mask
Concat(h_mask_tail, false_row, axis=2) -> h_sep_mask
```

## 対象

- `data/output/onnx/task009.onnx`

## 仮説

`false_col` / `false_row` は端に 1 列/行を足すだけなので、opset 17 の `Pad` で置換できる。
正答は保てるはずだが、削減は params 数個に留まる見込み。

## 手順

1. `/tmp` に `task009` の `Concat` -> `Pad` 版を生成する。
2. `src.evaluate.scorer.audit_one` で `task009` の完全一致と cost を確認する。
3. `n_fail=0` かつ相対削減率が十分なら本体へ反映して submit する。削減が小さすぎる場合は submit しない。
