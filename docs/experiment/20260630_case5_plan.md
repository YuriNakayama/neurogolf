# 20260630 case5 plan — flood-fill タスク t002 の hand-golf（4連結 enclosed-fill）

## 現状把握
- floor 7180.58（cycle1 で確立, DVC `713fc67a`）。公開バンドル harvest 飽和（cycle2/4）。
- 簡単規則タスクは全 golf 済み（cycle4）。残る高コストは flood-fill/shape-fill 系。

## 仮説
高コスト同形状・少色タスクを enclosed-region-fill 規則で網羅判定 → **t002（cost 21089, 2色,
fill=4）が 4連結 enclosed-fill 規則と全 268 example 完全一致**。これを静的 ONNX
（unrolled 4-conn dilation: 各ステップ = 4方向 shift の Max ∩ free）で再構築し、
bit-pack floor net より安く解けるか検証する。

## 対象 / 手法
- t002 のみ。free=非wall, seed=外周ring, reach=K回 dilation∩free, enclosed=color0∧¬reach→4。
- 許可 op（Pad/Slice/Max/Mul/Sub/Concat）のみ、静的形状 [1,*,30,30]。

## 採否
- faithful n_fail=0 かつ cost<21089 なら submit。コスト超過なら floor 維持（提出せず）。
