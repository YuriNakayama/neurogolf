# 20260701 case320 result - adopt task274 packed channel pad

## Result

- Decision: adopt
- Kaggle submission ref: `54230606`
- Previous Public Score: `7183.39`
- New Public Score: `7183.64`
- Public delta: `+0.25`

## Local Gate

`task274.onnx` after the change:

```text
params=94
memory=83
cost=177
n_pass=269
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=80
memory=146
cost=226
n_pass=269
n_fail=0
status=ok
```

Local cost reduction:

```text
226 -> 177
old_cost / new_cost = 1.2768
ln(old/new) ~= 0.2444 pt
```

## Change

Replaced the tail:

```text
Concat(out0_small, zero_small x7, out8_small) -> small_output
Pad(small_output, pads_final) -> output
```

with:

```text
Concat(out0_small, out8_small) -> packed_live
QLinearConv(packed_live, one-hot 2ch->10ch weights, pads=[0,0,27,27]) -> output
```

The one-hot projection maps `out0_small` to channel 0 and `out8_small` to
channel 8. Other output channels and the bottom/right pad are zero.

## Verification

Individual `evaluate.audit_one` for `task274` passed with `n_fail=0`.
The Kaggle Public LB improved to `7183.64`, so the packed projection
transferred.

## Conclusion

Adopted. This validates the relative-ratio priority especially strongly: even
though the absolute cost is small, deleting a 9-channel intermediate gives a
large score gain because `old/new` is high.

Next queued candidates from the same scan: `task271`, `task030`, `task023`, and
the smaller exact `task266`.
