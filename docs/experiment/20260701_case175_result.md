# 20260701 case175 result

## Target

- Task: `task190`
- Baseline cost: 2527
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task190`.

No exact cost-reducing candidate was found.

Combined ray aliases:

- `ray_u -> nw_ray_u`: cost 2091, `n_fail=248`
- `ray_u -> ne_ray_u`: cost 2091, `n_fail=250`
- `ray_u -> sw_ray_u`: cost 2091, `n_fail=241`
- `ray_u -> se_ray_u`: cost 2091, `n_fail=236`

Single-branch removal or cross-alias:

- drop or alias `nw_ray_u`: cost 2427-2428, `n_fail=127`
- drop or alias `ne_ray_u`: cost 2427-2428, `n_fail=119`
- drop or alias `sw_ray_u`: cost 2427-2428, `n_fail=133`
- drop or alias `se_ray_u`: cost 2427-2428, `n_fail=145`

Marker/color aliases:

- `nw5_u -> ne5_u` or `ne5_u -> nw5_u`: cost 2527, `n_fail=144`
- `sw5_u -> se5_u` or `se5_u -> sw5_u`: cost 2527, `n_fail=166`
- `color10_f -> pooled_color`: cost 2477, `n_fail=266`
- `color10_f -> input`: cost 2437, `n_fail=266`

Shape/type failures:

- `ray_u -> nw5_u/ne5_u/sw5_u/se5_u`: invalid final `Where` dimensions
- `ray_u -> zero_u8`: invalid `Pad` rank
- `marker_u -> bg10_u`: invalid `Split` shape
- `ray10 -> ray_u`: invalid `Where` condition type
- `ray30 -> ray10`: invalid final `Where` dimensions

## Submission

- Not submitted.
- Reason: all scored cost reductions failed exact-match auditing or produced invalid ONNX.

## Decision

Rejected. Keep the existing `task190.onnx`.

## Next

The four directional rays are all essential. Continue with other short graphs, but
avoid assuming symmetric ray branches are redundant without a very low-fail first
probe.
