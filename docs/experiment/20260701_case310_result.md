# 20260701 case310 result - reject task338 fill-branch trim

## Result

- Decision: reject
- Submission: none
- Current Public Score floor: `7183.04`
- Files changed in ONNX output: none

## Local Baseline

`task338.onnx`:

```text
params=689
memory=17250
cost=17939
n_pass=267
n_fail=0
status=ok
```

## Probes

All probes were run on temporary ONNX copies, then audited only for `task338`.

| Probe | Cost | Result |
|---|---:|---|
| `fill24 -> fill_0123` | 16643 | `n_fail=75` |
| `fill24 -> fill_012`, `fill_0123 -> fill_012` | 15347 | `n_fail=239` |
| `fill_01 -> fill_1` | 16643 | `n_fail=5` |
| `fill_01 -> fill_0` | 16643 | `n_fail=23` |
| `fill_012 -> fill_01` | 16643 | `n_fail=154` |
| `fill_0123 -> fill_012` | 16643 | `n_fail=237` |
| `out0 -> valid` | 17039 | `n_fail=244` |
| `out0 -> fill` | 15934 | `n_fail=267` |
| `zero_b -> fill` | 17039 | `n_fail=244` |

## Other Search Results

- Crop-at-source scan found no net-positive current candidate after accounting
  for new sparse-kernel params or required input slices.
- `backend/pipeline/case2/golf_wins` contained no currently cheaper exact task
  file compared with `data/output/onnx`.
- Broad same-shape alias probing on `task233` and `task018` did not find an exact
  win before the run was stopped due low signal from invalid/runtime-failing
  candidates.

## Conclusion

Rejected. The large-ratio `task338` reductions are not exact; every complete
fill-branch removal changes at least five local examples, and terminal boolean
aliases are also incorrect.

This confirms the updated priority rule but also rules out this specific
high-ratio branch-trim path. Continue with structural candidates only: complete
intermediate removal, final dtype reductions, or task-local bundles above the
ratio gate. Do not submit single `task338` guard aliases unless bundled into a
larger exact change, because case276 already showed the single-alias gain is too
small for the current Public LB floor.
