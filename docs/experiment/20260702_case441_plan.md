# 20260702 case441 plan - task216 mechanical cleanup probe

## Context

Accepted Public Score remains `7186.71`. Case440 found no current-baseline
`task191` candidate. The side scan recommended `task216` only for mechanical
Slice/index cleanup, not semantic crop pruning.

Current local scorer check:

```text
task216 cost=9127
params=87
memory=9040
status=ok
```

Dominant internal tensors:

```text
c12_f32     FLOAT [1,2,20,20]  3200 bytes
c12         UINT8 [1,2,20,20]   800 bytes
crop        UINT8 [1,2,18,18]   648 bytes
tl_flat_mid UINT8 [1,400]       400 bytes
tl_flat     UINT8 [1,400]       400 bytes
tl          UINT8 [1,1,20,20]   400 bytes
red_3d      UINT8 [1,20,20]     400 bytes
```

The `c12_f32 -> c12` path is central to corner detection, row/column checks,
rectangle scoring, and final crop. Case306 already rejected broad crop/cast
rewrites.

## Hypothesis

No semantic crop-path pruning should be submitted. The only acceptable probe is
current-baseline mechanical cleanup:

- old cleanup/index candidates;
- `tl_flat_mid -> tl_flat` if the zeroed edge winners are provably irrelevant;
- tiny int/index or Slice metadata candidates if they are exact.

## Method

1. Re-gate existing `task216` cleanup, index, guard, and alias candidates.
2. Classify any exact low-gain result as banked unless it clears the current
   threshold.
3. Submit nothing unless a candidate is locally exact, mechanically explainable,
   and `gain >= 0.020`.
