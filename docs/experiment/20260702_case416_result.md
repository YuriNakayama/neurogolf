# 20260702 case416 result - no-submit same-task strengthening

## Decision

No Kaggle submit.

Accepted Public Score remains:

```text
7186.71
```

## Checks

### Current high-cost map

Cost-only scan confirmed the strongest non-excluded current tasks are:

```text
task018 38681
task133 33562
task158 30187
task187 27533
task054 27089
task349 24389
task364 23013
task255 22731
task243 22650
task367 20699
task002 20689
task319 19080
```

Known exclusions from the regression controls were kept out of the submit pool.

### Broad scratch scan avoided

A direct `/tmp` recursive scan found about `45006` candidate ONNX files for the
target set, which is too broad and noisy for the 15-minute cadence. It was
stopped and replaced with docs-directed checks.

### Same-task greedy probe

Tried to combine exact same-task candidate families for `task213` and `task255`
by extracting input-edge rewrites from candidate graphs and greedily applying
compatible reductions on the current baseline.

Result:

```text
task213 FINAL 1851 -> 1851, applied=0
task255 FINAL 22731 -> 22731, applied=0
```

The exact candidates are not simple one-edge deltas:

```text
task213_27_And_in0_h_valid_p2.onnx: 3 graph input diffs, cost 1834
task255_29_Or_in0_c_top_center.onnx: 23 graph input diffs, cost 22541
task255_29_Or_in1_c_bottom_center.onnx: 19 graph input diffs, cost 22541
```

This means they include downstream pruning or optimizer side effects. They
should not be mechanically merged without a task-specific derivation.

## Conclusion

Skip submit. The best available exact candidates remain below the post-case414
gate:

```text
task213: 1851 -> 1834, gain 0.0092266598
task255: 22731 -> 22541, gain 0.0083937584
task046: 2286 -> 2285, gain 0.0004375410
```

Next action: stop broad scratch scanning. Use targeted graph inspection on
high-cost non-excluded tasks, especially `task018`, `task133`, `task158`,
`task187`, `task054`, `task349`, `task364`, `task243`, and `task367`, looking
for a fresh isolated structural reduction with `gain >= 0.020`.
