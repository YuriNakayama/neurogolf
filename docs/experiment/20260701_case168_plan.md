# 20260701 case168 plan

## Context

- Current accepted Public Score: 7181.17
- Recent large and medium branch-deletion probes failed exact-match auditing.
- Per the corrected scoring analysis, prioritize candidates by expected relative cost
  reduction (`old_cost / new_cost`) rather than absolute cost alone.

## Target

- Task: `task263`
- Baseline audit:
  - params: 117
  - memory: 563
  - cost: 680
  - points: 18.477907201829847
  - n_pass: 267
  - n_fail: 0
  - filesize: 4756

## Hypothesis

`task263` selects either horizontal or vertical 5-color count vectors, finds the odd
count, then slices a 3-cell region. The graph has compact but explicit orientation
and color-position guards:

- `h_valid` selects `h_vec` versus `v_vec`.
- `odd_map` is built from five equality branches.
- `offset_table` and `end_table` convert the odd index into Slice bounds.

Because the baseline cost is low, removing even one intermediate tensor can produce
a useful relative reduction. The most plausible exact-preserving probes are
orientation simplifications and branch aliases that keep operator types unchanged.

## Method

1. Probe one ONNX rewrite at a time on temporary `task263` copies.
2. Run `audit_one` only for `task263`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record all failed aliases, especially low-fail candidates, before moving on.
