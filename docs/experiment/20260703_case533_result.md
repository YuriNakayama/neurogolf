# 20260703 case533 result - no-submit after task044 regression

## Result

No submit.

Accepted score remains:

```text
Public Score: 7187.41
latest accepted ref: 54273472
latest adopted case: case530 task064
```

## Why No Submit

Case532 showed that task044 is hidden/Public unsafe even when submitted in
isolation:

```text
task044 Public Score: 7187.41 -> 7171.75
```

After that regression, do not submit the remaining case475 member task046 just
for cadence. Its gain is only `0.000438`, and it shares the same contaminated
bundle history.

## Candidate Search

Current confirmed exact low-risk candidates:

| task | cost | gain | status |
|---|---:|---:|---|
| 005 | `6882 -> 6881` | `0.000145` | exact |

Rejected or stale leads:

| task | finding |
|---|---|
| 328 | recent candidate path was not reproducibly scorable/exact against the current accepted ONNX |
| 213 | stale; current baseline is already `1834`, so candidate is `1834 -> 1834` no-gain |
| 033 | meaningful near miss remains `n_fail>0`, and recent repairs indicate structural blockage |

The parent dry-run proved a tiny task005/task328 bundle can be forced through a
lowered micro gate, but its total gain is only about `0.000435` and task328 is
not stable enough to trust. Do not submit it.

## Decision

Skip submit for this cycle. Keep task005 banked. Continue search for either:

1. a non-risk exact candidate with materially larger current-baseline gain, or
2. a repair target whose exact fix does not restore the deleted high-memory
   tensor.
