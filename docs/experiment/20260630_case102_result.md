# 20260630 case102 result - t009 false Pad 化は Public 不変で不採用

## 実装候補
`task009` の separator mask 生成で、末尾 false を付ける 2 つの `Concat` を `Pad` に置換した。

- `v_mask_tail + false_col -> v_sep_mask` を `Pad(v_mask_tail, right=1, false)` へ置換
- `h_mask_tail + false_row -> h_sep_mask` を `Pad(h_mask_tail, bottom=1, false)` へ置換

## faithful 検証
- before: cost 8410, params 1081, memory 7329, n_pass 265, n_fail 0
- after: cost 8407, params 1078, memory 7329, n_pass 265, n_fail 0
- 削減: cost -3

## submit
- ref: `54192749`
- message: `case102 t009 replace false concat sep masks with pad cost 8410 to 8407`
- status: `COMPLETE`
- Public Score: **7180.95**（case100 floor から変化なし）

## 採否
- **不採用**。
- local faithful かつ cost -3 だが、実 Public Score が上がらなかった。
- `task009.onnx` は DVC checkout で baseline に戻した。`task066` の case100 改善は保持されている。

## 結論
微小な params-only 近傍の削減は Public 表示に反映されにくい。`Pad` 置換は有効でも、memory が減らない候補は優先度を下げる。

## 次の一手
- case100 と同様に、zero initializer 削除だけでなく intermediate memory も落ちる候補を探す。
