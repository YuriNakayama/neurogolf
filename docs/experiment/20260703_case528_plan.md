# 20260703 case528 plan - expanded parallel candidate supply

## Current State

Latest accepted Kaggle result:

```text
ref=54272777
Public Score=7187.38
submitted=2026-07-03 02:33:50 JST
description=case525 task184 review-mid compression
```

No submit has happened after case525 because case526/case527 did not produce a
safe candidate:

- the exact low-risk micro bank stayed below the `0.015` bundle threshold
- task379 had a high-gain near miss, but the `n_fail=2` repair restored the full
  baseline cost

## Hypothesis

The bottleneck is candidate supply, not Kaggle availability. To support the
requested higher cadence, run more parallel search lanes and treat each cycle as
a queue-building pass:

1. rank by relative reduction `ln(old_cost / new_cost)`, not absolute cost
2. require `n_fail=0` before any submit
3. submit immediately if a standalone low-risk candidate clears `0.020`
4. otherwise submit a low-risk micro bundle once independent candidates reach
   `>=0.015`
5. keep risk-listed candidates isolated; do not mix them into low-risk bundles

## Work Split

Parallel lanes:

- current micro-bank re-gate from `docs/experiment/micro_backlog.md`
- larger existing near-miss search from `/tmp` and `/private/tmp`
- operational bottleneck review for 72 submits / 13 hours
- task range `001-100` scratch scan
- task range `101-250` scratch scan
- task range `251-400` scratch scan

The parent process will keep the critical path local: collect returned exact
candidates, build the strongest independent bundle, run dry-run submit gate, and
submit only if the bundle is gate-clean or if one isolated candidate is
standalone-grade.

## Acceptance

Adopt only if real Kaggle Public Score improves above `7187.38`. If no bundle or
standalone candidate clears the gate, record no-submit with the updated bank and
continue searching without touching accepted ONNX files.
