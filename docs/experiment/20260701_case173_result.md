# 20260701 case173 result

## Target

- Task: `task222`
- Baseline cost: 8321
- Baseline status: `n_fail=0`

## Probes

Temporary ONNX rewrites were audited only with `audit_one` for `task222`.

No exact cost-reducing candidate was found.

Coverage branch aliases:

- `cov_u8 -> sp_a`: cost 7149, `n_fail=86`
- `cov_u8 -> sp_b`: cost 7149, `n_fail=73`
- `sp_a -> sp_b`: cost 7345, `n_fail=73`
- `sp_b -> sp_a`: cost 7345, `n_fail=86`
- `sp_a -> zero_u8`: cost 7346, `n_fail=73`
- `sp_b -> zero_u8`: cost 7346, `n_fail=86`
- `cov_u8 -> code`: cost 5976, `n_fail=266`
- `cov_u8 -> one_u8/zero_u8`: cost 5977, `n_fail=266`

Code/pad aliases:

- `cgu -> code`: cost 5780, `n_fail=266`
- `cgu -> cov_u8`: cost 8125, `n_fail=263`
- `comp -> code`: cost 8125, `n_fail=266`
- `cg16 -> cgu`: invalid final `Equal` shape
- `cg30 -> cg16`: invalid final `Equal` shape
- `code -> code32`: invalid `Sub` type binding
- `code32 -> codef`: invalid final shape

Shape/type failures:

- `cov_u8 -> monou_a/monou_b`: incompatible `Mul` dimensions
- `sp_a/sp_b -> monou_a/monou_b`: incompatible `Max` dimensions
- `mono_a/mono_b` and `monou_a/monou_b` cross-aliases: incompatible dimensions
- scalar constants used before `Pad`: invalid pad rank
- graph output set to internal uint8 tensors: invalid declared bool output type

## Submission

- Not submitted.
- Reason: all scored cost reductions failed exact-match auditing or produced invalid
  ONNX.

## Decision

Rejected. Keep the existing `task222.onnx`.

## Next

The two coverage branches are both needed. The best one-branch deletion still fails
73 public examples, so exception logic would likely erase the cost savings. Move to
another memory-heavy short graph with a clearer exact dtype or tensor-shape trim.
