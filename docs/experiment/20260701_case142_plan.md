# 2026-07-01 case142 plan

## 方針

- floor: Public Score 7181.15
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task096.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task096.onnx`
- baseline: params 805, memory 8108, cost 8913, points 15.904733835869283, n_pass 266, n_fail 0

## 仮説

`task096` は上位5色の row/col span から code を作り、radius ごとの color/length table へ scatter して 11x11 pattern を生成する。`present_bool`、`row_gap_exists`、`row_span_eq_*`、`stroke_active/in_box` の `Where` が多く、候補色や radius が実データで限定的なら一部 mask を省略できる可能性がある。

特に `present_bool` による `code_i32` / `radius_idx` / `radius_used` / `color_updates` / `length_updates` は同じ5要素 mask を共有しているため、無効色が実データで無害なら相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `present_bool` mask と `row_gap_exists` / ambiguous code branch を個別に外す。
2. `stroke_active` / `in_box` の final mask 省略も確認する。
3. `n_fail=0` かつ cost 減なら `data/output/onnx/task096.onnx` に反映する。
4. submit し、Public Score が 7181.15 を上回れば採用、上がらなければ破棄する。
