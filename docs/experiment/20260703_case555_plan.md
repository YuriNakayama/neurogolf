# 20260703 case555 plan - post task174 standalone search

Accepted Public Score:

- `7187.53`

## Background

Case554 adopted `task174 8978 -> 8744`, improving the Public Score. Case553
also showed that exact micro bundles can restore submit cadence, but a
ten-task local gain of `0.004954` did not improve the Public Score.

## Priority

Prefer standalone candidates with relative gain `>=0.020`. Keep exact micro
candidates banked, but do not spend the next submit on a micro bundle unless no
standalone candidate is found near the cadence target.

## Targets

Parallel worker:

```text
task118, task187, task198, task216, task319, task349, task364, task367,
task396, task340, task145, task202, task204, task205
```

Main thread:

1. Re-gate the new `task205 11547 -> 11539` micro candidate against the updated
   baseline.
2. Scan nearby count/equality patterns in non-risk medium-cost tasks for the
   same `ReduceSum Equal(0)` to `ReduceMax Equal(0)` reduction that worked on
   `task174`.

## Submit Rule

Submit a full 400-task bundle immediately for a standalone `submit-candidate`.
If only micro candidates are available near the cadence target, submit a
full-bundle micro experiment with explicit gate flags and document the elevated
submit-cadence rationale.
