# 20260702 case457 plan - bounded current-scratch screen

## Context

Case456 rejected `task138`: 203 positive cost-reduction candidates all failed
local correctness. Follow-up subagent checks also rejected `task349` and
`task364` as next targets because their high-gain cuts remove required halo or
propagation semantics.

The next cycle should avoid another single stale high-cost branch cut. Instead,
screen a bounded set of current-baseline scratch candidates from still-relevant
mid/high-cost non-hard-risk tasks and only proceed if an isolated candidate is
both exact and submit-worthy.

## Scope

Screen existing candidates for:

```text
task064
task076
task077
task110
task173
task219
```

These tasks are not in the hard-risk gate set and still have current costs high
enough that a one-tensor exact reduction could matter. They also have known
near-miss scratch pools, so this is a cheap way to determine whether a
current-baseline exact candidate exists before investing in a hand repair.

Current baselines:

```text
task064 cost=12478 n_fail=0
task076 cost=18683 n_fail=0
task077 cost=14834 n_fail=0
task110 cost=13591 n_fail=0
task173 cost=15664 n_fail=0
task219 cost=14838 n_fail=0
```

## Method

1. Gather `task064`, `task076`, `task077`, `task110`, `task173`, and `task219`
   ONNX candidates from `/private/tmp` and `/tmp` shallow scratch pools.
2. Run cost-only screening against the current accepted baseline.
3. Audit the highest-gain positive reductions for correctness.
4. Submit only if a candidate reaches:

```text
n_fail=0
gain >= 0.020
single-task isolated change
no forbidden ops/functions
not a known-risk family
```

5. If only micro exact candidates appear, bank them and skip submit.

## Risk Control

This cycle is candidate discovery and re-gating only. It must not stage a broad
bundle and must not submit a local-failing candidate to satisfy cadence.
