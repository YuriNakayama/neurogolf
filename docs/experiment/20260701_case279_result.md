# 20260701 case279 result - adopt task076 bundled board/candidate aliases

## Result

- Target: `task076`
- Baseline: `params=340`, `memory=19696`, `cost=20036`, `n_fail=0`
- Candidate: `params=339`, `memory=18404`, `cost=18743`, `n_fail=0`
- Relative reduction: `20036 / 18743 = 1.0690`
- Submit ref: `54222482`
- Public Score: `7181.84 -> 7181.90`
- Adoption: adopted

## Change

Applied six exact aliases:

```text
board_code            -> color15_h
candidate_outside     -> candidate_not_in_bounds
candidate_in_bounds   -> candidate_col_in
source_visible_seed   -> source_component
source_hidden_colors  -> source_hidden_colors_raw
source_visible_colors -> source_visible_colors_raw
```

Dead-node pruning removed now-unused candidate/visibility intermediates and stale
metadata.

## Verification

Individual local audit for the submitted `task076.onnx`:

```text
params=339
memory=18404
cost=18743
n_pass=266
n_fail=0
status=ok
```

DVC was updated and pushed after the Public Score improved.

## Conclusion

Adopted. The shape-guided scan found several individually exact aliases; bundling
compatible ones produced a meaningful `6.9%` relative cost reduction and moved
the real LB by `+0.06`.

## Next Step

Continue with the same workflow. Known remaining exact single from the case278
scan:

```text
task209: total_vote_p1_u8 -> total_vote_u8, cost 32052 -> 31651, ratio 1.0127
```

Before submitting it, run a task-local scan for additional compatible `task209`
aliases so the submitted bundle has a better chance of clearing LB rounding.
