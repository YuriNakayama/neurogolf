# 20260702 case498 plan - task284 cadence scan

## Context

Current accepted Public Score:

```text
7187.10
```

Recent cycles:

- case494 adopted `task048` with a high relative gain.
- cases495-497 found no submit-ready candidates; exact micros were banked.
- A remote auto commit added case3 solver code but did not change the accepted
  ONNX DVC pointer.

## Target

```text
task284.onnx
```

Reason:

- moderate scratch pool remains;
- current cost is enough for meaningful movement if a ratio cut exists;
- not listed as a hard hidden-regression family in the current regression
  analysis, though index/dtype changes require caution.

## Method

1. Gather unique `task284*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Measure current baseline and candidate costs with `evaluate.audit_one`.
3. Rank positive candidates by `ln(old_cost / new_cost)`.
4. Gate positives with `submit.gate.evaluate_candidate_gate`.
5. Submit only if a candidate reaches `submit-candidate`; otherwise record
   no-submit and continue.

## Parallel Work

A worker is scanning `task031/task069/task117/task136/task148`.
