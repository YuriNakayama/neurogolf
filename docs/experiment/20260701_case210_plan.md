# case210 plan: task386 compact AND selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task386` は未着手で、baseline は `cost=211` (`params=31`, `memory=180`, `n_fail=0`)。
- `task006` と似た左右 mask の AND selector だが、4x3 の 4-channel patch で、1 tensor 削減の相対効果がある。

## 対象

- `data/output/onnx/task386.onnx`
- 左右の 4x3 slice を bool 化し、`green = And(left_b, right_b)` で green/black patch を選ぶ。

## 仮説

左右 mask の片方が実データ上で十分、または `green` / `small4` が固定に近ければ `And` / `Where` 周辺を削れる。`task006` では失敗したが、shape と task が違うため短く確認する。

## 検証

- temp ONNX で `green -> left_b/right_b/const`、`small4 -> fixed patch` を probe する。
- `src.evaluate.scorer.audit_one` で `task386` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
