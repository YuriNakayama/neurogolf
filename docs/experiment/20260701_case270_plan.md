# 20260701 case270 plan - task316 TopK score uint8

## 背景

- Current floor: Public Score `7181.66`
- 優先基準: `n_fail=0` を絶対条件にし、絶対 cost ではなく `old_cost / new_cost` の相対削減率を優先する。
- `task316` baseline: `cost=426` (`params=38`, `memory=388`, `n_fail=0`)

`task316` は先頭 10 columns の occupancy を作り、TopK で使う score に float16 Cast している。

```text
color_id -> Cast(bool) -> occupied_b
occupied_b -> Cast(float16) -> scores
TopK(scores, k=9) -> topidx
```

`topvals` は下流で使われず、必要なのは `topidx` だけ。

## 対象

- `data/output/onnx/task316.onnx`

## 仮説

`scores` は 0/1 の ranking にだけ使われるため、float16 ではなく uint8 でも TopK の index が同じになる可能性がある。
`scores` の shape は `[10]` なので、float16 20 bytes から uint8 10 bytes へ下がる。

baseline cost が 426 と小さいため、10 bytes 削減でも:

```text
426 / 416 = 1.0240
```

となり、相対削減率は 1% を超える。

## 手順

1. `/tmp` に `task316` の `score_columns` Cast を `FLOAT16 -> UINT8` に変えた候補を生成する。
2. `src.evaluate.scorer.audit_one` で `task316` の完全一致と cost を確認する。
3. `n_fail=0` かつ `old_cost / new_cost >= 1.01` なら本体へ反映して submit する。閾値未満または session error なら submit しない。
