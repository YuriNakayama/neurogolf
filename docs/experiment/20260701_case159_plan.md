# 20260701 case159 plan

## Context

- Current accepted Public Score: 7181.17
- Recent probes show tiny local changes are insufficient and Scatter safety guards are usually required.
- Continue with middle-cost tasks where a successful mask simplification could remove image-shaped tensors.

## Target

- Task: `task335`
- Baseline audit:
  - params: 661
  - memory: 2840
  - cost: 3501
  - points: 16.839196079045337
  - n_pass: 266
  - n_fail: 0
  - filesize: 4158

## Hypothesis

`task335` extracts two endpoint rows/cols, builds a 20x20 path mask, pads it to 30x30, and paints the path yellow:

- `vert = row_open & col_is_cyan`
- `horiz = row_is_red & (col_open | col_is_cyan)`
- `path = vert | horiz`

The graph is compact, but the path construction has independent vertical and horizontal branches. If one branch or one open-interval guard is redundant for the public distribution, the replacement can remove several 20x20 bool tensors and produce a meaningful relative cost reduction.

## Method

1. Probe one mask replacement at a time on temporary `task335` ONNX copies.
2. Run `audit_one` only for `task335`.
3. Apply and submit only if `n_fail=0` and cost decreases enough to matter.
4. If Public Score does not exceed 7181.17, revert the ONNX/DVC change and document rejection.
