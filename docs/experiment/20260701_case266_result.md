# 20260701 case266 result - reject task310 zero-offset crop start

## 結果

- 対象: `data/output/onnx/task310.onnx`
- 採否: reject
- submit: なし（足切りで `n_fail>0`）

## 試行内容

`task310` の crop start 計算で、`half_h` を使う subtract を外した。

```text
rstart_f = mean_r - half_h  -> mean_r
cstart_f = mean_c - half_h  -> mean_c
```

削除候補:

- `half_h`
- `rstart_f`
- `cstart_f`

## 足切り

baseline:

```text
status ok
params 45
memory 3006
cost 3051
points 16.97677531528333
n_pass 266
n_fail 0
filesize 1718
```

candidate:

```text
status INCORRECT
params 45
memory 2994
cost 3039
points 0.0
n_pass 0
n_fail 266
filesize 1623
```

削減見込みは 12 cost だったが、crop 位置/shape が崩れ、全例不一致になった。

## 追加スキャン

- scalar no-op (`+0`, `-0`, `*1`, `/1`) の bypass を全 task で検証: wins 0。
- shape op (`Reshape` / `Flatten` / `Squeeze` / `Unsqueeze`) の無差別 bypass も試したが、
  runtime shape error が多く、1サイクルの一点改善としては粗すぎるため中断。

## 結論

`task310` は変更しない。名前上 no-op に見える crop offset でも、実際には出力サイズ復元に効く。

## 次の一手

無差別 bypass ではなく、trace で大きい中間を先に特定してから、その producer 周辺だけを
task 固有に probe する。`old_cost / new_cost >= 1.01` 見込みを事前条件に戻す。
