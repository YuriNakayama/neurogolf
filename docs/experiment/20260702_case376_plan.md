# 20260702 case376 plan - micro backlog submit-gate audit

## Context

Current accepted Public Score is `7185.86`. Cases 370-375 found only exact
micros or blocked dtype/table rewrites. The user instruction is to preserve small
changes while reducing submit count, so this cycle checks whether the current
micro backlog is large enough for a controlled batch submit.

## Candidate Pool

Use only exact local candidates already recorded in `micro_backlog.md`.

Exclude:

- adopted entries;
- locally rejected entries;
- hidden/public unsafe families such as task002 bit topology and task118 table
  pruning;
- medium/high-risk entries unless the mixed-risk submit threshold is met.

## Submit Gate

From the submit-saving policy:

```text
low-risk batch:   total expected gain >= 0.030
mixed-risk batch: total expected gain >= 0.050 and no hidden-unsafe family
```

The 30-minute cadence is a maximum frequency, not a reason to submit weak
material.

## Method

1. Sum actionable low-risk exact micros.
2. Sum the same pool plus the task370 medium-risk selector/sign micro.
3. Submit only if the relevant threshold is reached.
4. Otherwise skip submit and continue searching for a larger standalone
   candidate.
