# 20260630 case93 result — t255 の端補正 branch は全て必要（提出なし）

## 対象

- `task255.onnx`
- baseline: params **261**, memory **22756**, cost **23017**, faithful `n_fail=0`
- Public Score floor: **7180.87**

## 実施内容

t255 は color 0 channel から縦横の rectangle/line geometry を検出し、channel 3 で paint する graph。memory 上位は最終 `Where` 出力、30x30 の bool geometry 合成、入力 channel slice。右端・上端・下端の特殊補正 branch に削減余地があるかを確認した。

### branch 削減 probe

| 候補 | cost | 結果 |
|---|---:|---|
| `right_or2 -> right_normal` | 22861 | `n_fail=3` |
| `top_or2 -> top_normal` | 22954 | `n_fail=1` |
| `bottom_or2 -> bottom_normal` | 22954 | `n_fail=3` |
| right/top/bottom 全 normal 化 | 22703 | `n_fail=7` |
| right top0 補正だけ削除 | 22924 | `n_fail=1` |
| right bottom29 補正だけ削除 | 22954 | `n_fail=2` |
| left/right/hbase/top/bottom の広域 branch 削除 | 20666-21637 | `n_fail=62-137` |

端補正は一見特殊 case だが、arc-gen に境界例が存在し、各補正が faithful 上必要。

### metadata prune

- `value_info` 全削除で filesize **16630→11428**。
- faithful cost は **23017 のまま**で Public Score 改善には寄与しない。
- ONNX 差し替えは行わない。

## 採否

- **不採用、提出なし**。
- `data/output/onnx/task255.onnx` は未変更。

## 結論

t255 の近い削減候補は `n_fail=1` まで迫るが、境界補正が全ペア完全一致に必要。最終出力と 30x30 geometry 合成が支配的で、現 graph に安全な局所削減は見つからなかった。次は `task364` / `task243` / `task319` の未再検証領域へ移る。
