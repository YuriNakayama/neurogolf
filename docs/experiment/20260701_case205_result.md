# case205 result: task072 equality selector trim

## 結果

- 対象: `task072`
- baseline: `cost=421`, `params=31`, `memory=390`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

6x5 equality selector を temp ONNX で固定化・片側化した。

| 変更 | cost | n_pass | n_fail | status |
|---|---:|---:|---:|---|
| `k -> true` | 196 | 0 | 268 | `INCORRECT` |
| `k -> false` | 196 | 0 | 268 | `INCORRECT` |
| `k -> t` | n/a | 0 | 0 | type error |
| `k -> b` | n/a | 0 | 0 | type error |
| `p -> black_const` | 248 | 0 | 268 | `INCORRECT` |
| `p -> green_const` | 248 | 0 | 268 | `INCORRECT` |

## 結論

`task072` も `task236` と同様、top/bottom equality branch が正答性に必要。固定化では全例不一致になり、実 ONNX は変更していない。

## 次の一手

小型 equality selector の固定化は打ち止めにする。次は `task083` の mirrored scatter index または `task229` の mode selector など、別構造の短い graph を確認する。
