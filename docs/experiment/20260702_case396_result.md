# 20260702 case396 result - fresh surgery micros banked

## Summary

No Kaggle submit.

Accepted Public Score remains:

```text
7186.24
```

## Scan Result

Ran the current-bundle surgery scan with these passes:

```text
cleanup
dedup_tiny_shape_index_initializers
remove_redundant_slice_index_inputs
default_slice_axes_hole_punch
index_surgery
broadcast_compress
```

Exact current-baseline wins:

```text
task011 2090 -> 2083 gain 0.003355
task285 24535 -> 24512 gain 0.000938
task018 38683 -> 38681 gain 0.000052
```

Combined fresh gain:

```text
0.004344483316347677
```

## Decision

Do not submit. The candidates are exact and low-risk, but the combined gain is
well below the current submit gate and unlikely to move a two-decimal Public
Score by itself.

Bank these micros for a future batch. `task285` here is a broadcast cleanup on
the accepted graph, not the rejected uint8 `TopK` candidate from case393.

## Next

The quick repair probes for `task077` and `task110` both indicate the known
near-misses need real representation changes, not local patches. Continue with
fresh current-baseline scans, but narrow them by task/risk to avoid spending
long wall-clock time on all 400 tasks during the submit cadence window.
