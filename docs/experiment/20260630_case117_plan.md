# 2026-06-30 case117 plan

## 現状

- floor: Public Score 7181.07 (case116)
- 優先基準: 相対削減率 `old_cost / new_cost`。
- case116 では `task277` の propagation を 1 段短縮し、cost 4145 -> 3745、Public +0.10 で採用。

## 対象

- `task350.onnx`
- baseline: params 24, memory 9012, cost 9036, n_pass 267, n_fail 0
- graph:

```text
m = cast(slice(input color1))
hl = MaxPool(m)  # horizontal left
hr = MaxPool(m)  # horizontal right
hs = Min(hl, hr)
vu = MaxPool(m)  # vertical up
vd = MaxPool(m)  # vertical down
vs = Min(vu, vd)
span = Max(hs, vs)
fc = span > m
Pad(fc) -> Where(fill8, input)
```

## 仮説

`task350` は cost 9036 の中 cost memory 支配タスク。水平/垂直 span の片側、または左右/上下片方向 MaxPool が冗長なら、26x26 級中間を 1〜2 個削れて数十 % の相対削減が見込める。

## 手法

1. `span = Max(hs, vs)` を `hs` / `vs` / `hl` / `hr` / `vu` / `vd` に差し替える。
2. `hs = Min(hl, hr)` を `hl` / `hr` に差し替える。
3. `vs = Min(vu, vd)` を `vu` / `vd` に差し替える。
4. 各候補を prune し、`src.evaluate.scorer.audit_one` で `task350` の完全一致を確認する。
5. `n_fail=0` かつ cost が下がる候補のみ submit する。

## 足切り

- 変更対象は `task350.onnx` のみ。
- 禁止 op は追加しない。
- 実 Public Score が上がれば採用、上がらなければ破棄する。
