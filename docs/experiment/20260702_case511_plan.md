# 20260702 case511 plan - post-rejection parallel sweep

Current accepted Public Score:

7187.23

## Background

Case509 and case510 showed that local exactness alone is still insufficient for
index and dtype value-path rewrites:

```text
case509 task175 guarded repair: Public 7187.23 -> 7169.38
case510 task173 dtype shrink   : Kaggle ERROR
```

Both families were added to the review-risk gate. The next step is to consume
the active AF-AK subagent sweep and submit only if a clean candidate clears the
hardened gate.

## Plan

1. Wait for the six range workers covering `001-400`.
2. Accept only standalone `submit-candidate` items that are not review-risk.
3. Bank low-gain exact candidates but do not force a submit.
4. Write a no-submit result if the sweep produces no hardened-gate candidate.
