# 20260702 case453 plan - task190 ray-branch candidate gate

## Context

Case452 closed `task102`: the attractive fill-core branch removals all failed
local correctness, and the closest `n_fail=10` candidates deleted a required
6x6 frame detector.

The remaining small Pad/output-mask targets from case436/case438 are:

```text
task190 cost=2527
task062 cost=2536
```

`task190` has no dedicated current-baseline result yet and has a bounded
scratch pool.

## Target

`task190.onnx`

Current baseline:

```text
params=247
memory=2280
cost=2527
points=17.1652118926
status=ok
```

## Hypothesis

Existing ray-branch and value-path aliases probably fail because each ray branch
is semantically required, but a current-baseline cleanup may exist. Since the
task is low cost, any real full-tensor removal would have a high relative gain;
therefore the strict gate is still worthwhile.

## Method

1. Gather existing `task190*.onnx` scratch candidates.
2. Run cost-only screening against the accepted baseline.
3. Run correctness gate only for positive cost reductions.
4. Submit only if a candidate reaches `submit-candidate`; otherwise record the
   closed result and move to `task062`.

## Gate

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
no forbidden ops/functions
single task only
```
