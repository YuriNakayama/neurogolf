# 20260701 case324 result - task266 packed qlinear tail

## Submission

- Kaggle ref: `54231169`
- Public Score before: `7183.86`
- Public Score after: `7183.91`
- Decision: adopted

## Local Gate

```text
task266 baseline:  params=35 memory=315 cost=350 n_fail=0 status=ok
task266 candidate: params=79 memory=255 cost=334 n_fail=0 status=ok
```

Relative reduction:

```text
350 / 334 = 1.0479
ln(old/new) ~= 0.0468 pt
```

## Change

Replaced a 9-channel sparse tail with four explicit zero-channel positions and
a separate final pad:

```text
BitwiseXor(ch3, ch3) -> zero
Concat(bg, zero, zero, ch3, zero, zero, ch6, ch7, ch8) -> small
Pad(small, out_pads) -> output
```

with a five-channel packed tensor and one-hot `QLinearConv` projection:

```text
Concat(bg, ch3, ch6, ch7, ch8) -> packed_tail
QLinearConv(packed_tail, one-hot 5ch->10ch weights, pads=final output pad) -> output
```

## Conclusion

Even this small exact candidate moved the Public LB display. Keep recording and
testing small ratio-positive candidates when they are locally exact and isolated
to one task.

## Next Step

The known packed-channel queue from the previous scan is exhausted. Run a new
scan for sparse-tail or alias candidates, ranking by `old_cost / new_cost` and
filtering out known hidden-unsafe patterns.
