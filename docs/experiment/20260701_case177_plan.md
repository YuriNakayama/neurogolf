# 20260701 case177 plan

## Context

- Current accepted Public Score: 7181.17
- case176 showed `task287` has no exact dtype or expansion trim.
- Continue with medium-cost tasks where a two-stage branch may contain a redundant
  guard.

## Target

- Task: `task281`
- Baseline audit:
  - params: 65
  - memory: 2321
  - cost: 2386
  - points: 17.223826961557957
  - n_pass: 266
  - n_fail: 0
  - filesize: 4219

## Hypothesis

`task281` detects a non-background bbox, identifies two top colors, builds outer
and inner color grids, pads them, then one-hot encodes.

Potential exact reductions:

- The inner color branch may be redundant for public examples.
- The outer bbox guard may be replaceable by row/column-only masks.
- Active row/column background handling may be over-specified.

## Method

1. Probe one alias at a time on temporary `task281` ONNX copies.
2. Run `audit_one` only for `task281`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. Record low-fail candidates and leave baseline ONNX unchanged if no exact win is
   found.
