# 20260702 case405 plan - task161 plus task076 alias bundle

## Context

Accepted Public Score is `7186.56` after case404 adopted the isolated
`task238` horizontal guard alias.

Next-cycle guard/alias search did not find another single `gain >= 0.02`
candidate. The best current exact candidate is `task161` at `0.019497`, just
below the submit threshold. Bundle it with the already banked `task076` exact
micro to clear the threshold while avoiding local-usage-only table pruning.

## Candidate Set

```text
task161 3729 -> 3657 gain 0.019496962029
task076 18743 -> 18683 gain 0.003206329899
```

Expected combined gain:

```text
0.02270329192858707
```

Scratch files:

```text
/tmp/neurogolf_case405_alias_scan/task161_006_Add_tl9_TO_tc0.onnx
/tmp/neurogolf_case380_combo/task076_combo.onnx
```

## Method

- `task161`: bypass `tl9 = Add(tc0, lc0)` by feeding `tc0` into the downstream
  `base9 = Add(..., hundred)` consumer.
- `task076`: remove the selected board row/column `Clip` pair before the hidden
  scatter index path.

## Gate

Submit only if both changed tasks pass targeted `audit_one`:

```text
task161: n_fail=0, functions=0, forbidden ops=[]
task076: n_fail=0, functions=0, forbidden ops=[]
```

Risk: medium. Both candidates are local-exact structural aliases, but neither
is pure dead-code removal. Submit as a small isolated bundle and adopt only if
Public Score improves over `7186.56`.
