# 20260630 case25 result — t136 doubling再構築も tensor-economy で floor に負ける（提出なし）

## 結論（提出なし、floor 7180.62 維持）
- **t136 規則を厳密確認**: color1 ブロック→up-left 対角 ray、color2 ブロック→down-right 対角 ray
  （**266/266 exact**）。
- **log-depth doubling 構成**（6 shift-max × 2 方向 ≈ 36-45 nodes）でノード数は floor(49) と同等まで
  短縮可能。だが各中間が **[1,1,30,30] float=3600B**。memory=tensor総和なので ~45×3600≈162000 >>
  **floor mem=1179**。
- **floor の 49-node net は bool/小shape の極小 tensor を使い、tensor 総和を 1179 に抑えている**。私の
  full-grid float tensor 構成は tensor-economy で全く敵わない。

## 確定した核心（memory=tensor総和の正確な含意）
- hand-construction が負ける理由は **tensor 数だけでなく tensor サイズ**: floor は bool/小shape の
  極小中間を使い tensor 総和を最小化。explicit な full-grid float 構成は各 tensor が 3600B+ で総和が爆発。
- floor は「最小ノード数 × 最小 tensor サイズ（bool/小shape/学習圧縮）」の二重最適。hand-construction で
  この tensor-economy を再現するのは事実上不可能（学習済み圧縮表現に対抗できない）。
- **完全デコード 3 タスク（t002/t171/t136）全てで、正しい net を最小ノードで作っても tensor-economy で
  floor に負ける**ことを確定。

## 次の一手
- memory=tensor総和の正確な理解の下でも hand-construction 不可を確定。前進は外部 publish harvest のみ。
- floor 7180.62 維持。7950 まで −769.38。
