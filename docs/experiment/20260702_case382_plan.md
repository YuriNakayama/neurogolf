# 20260702 case382 plan - split unary bundle continuation

## Context

Accepted Public Score is `7185.92` after case381 adopted `task013`:

```text
task013: 1930 -> 1870, gain 0.031582
```

The case381 broad unary-bypass scan found a real same-task bundle before being
stopped, so the pattern is still worth continuing. Submit budget should still be
protected: micros are banked unless a fresh candidate clears the gate.

## Hypothesis

The remaining task range may contain more compatible unary/value-path bypasses.
Individual rewrites are often sub-threshold, but two or more in the same task
can combine past the logarithmic submit gate.

## Method

Split the search:

- subagent A: tasks `014-140`
- subagent B: tasks `141-280`
- main agent: tasks `281-400` and compatibility checks for known fresh micros

For each candidate:

1. Rewire a single-output node output to one of its semantically weaker inputs.
2. Prune dead nodes and stale value_info, run cleanup.
3. Audit only the touched task with `evaluate.audit_one`.
4. If multiple exact wins occur in one task, test compatible bundles.

Exclude known unsafe/stale families:

- `task002` bit-topology local positives
- `task118` table pruning
- `task233` uint8 `TopK` value-path compression
- broad `task286` local-equivalence bundles
- already adopted/banked candidates unless a new compatible same-task bundle is
  proven exact

## Gate

Submit only if a fresh low-risk candidate clears:

```text
gain >= 0.020
```

Otherwise write a no-submit result and add exact micros to the backlog.
