# 20260701 case328 plan - task365 packed bool tail

## Context

- Current accepted Public Score floor: `7184.17` after case327.
- Priority rule: `n_fail=0` first, then relative reduction
  `old_cost / new_cost`.
- The remaining validated packed-tail candidate is small, but exact. Per the
  current strategy, small exact changes are retained and submitted when they
  have no competing larger ready candidate.

## Target

- `data/output/onnx/task365.onnx`

## Baseline

Current local audit:

```text
params=120
memory=3908
cost=4028
n_fail=0
status=ok
```

## Hypothesis

The tail currently builds a sparse bool output with live color channels and
explicit zero channels before padding. Replace the sparse tail with only the
live channels, cast to uint8, then use a one-hot `QLinearConv` with the final
spatial pad folded into the convolution pads.

The projection restores the live channels into the final 10-channel output.

## Expected Gain

Temporary audit:

```text
params=109
memory=3836
cost=3945
n_fail=0
status=ok
```

Expected point gain:

```text
4028 / 3945 = 1.0210
ln(old/new) ~= 0.0208 pt
```

## Verification Plan

1. Apply only the `task365` tail replacement.
2. Run individual `evaluate.audit_one` for `task365`.
3. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
4. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
