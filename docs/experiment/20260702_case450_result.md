# 20260702 case450 result - task319 same-task continuation re-gate

## Result

No submit.

Current baseline:

```text
task319.onnx
params=132
memory=18948
cost=19080
status=ok
```

## Re-gate

The broad old case382 scratch pool contains many stale candidates with invalid
runtime shapes or pre-case384 node numbering. A full gate attempt was stopped
after it spent several minutes in repeated load/runtime failures, because that
does not produce a useful submit candidate faster than the post-regression
workflow allows.

The meaningful current-baseline continuation candidates are the case384 bundle
and the same bundle plus the remaining `6` rewire from the case383 probe:

```text
/private/tmp/neurogolf_task319_case383_probe_pruned/combo_0_1_2_3_4_5.onnx
  current baseline 19080 -> candidate 19080
  n_fail=0
  decision=blocked-no-gain

/private/tmp/neurogolf_task319_case383_probe_pruned/combo_0_1_2_3_4_5_6.onnx
  current baseline 19080 -> candidate 19080
  n_fail=0
  decision=blocked-no-gain
```

The standalone residual `6` is exact but worse than the accepted current graph:

```text
/private/tmp/neurogolf_task319_case383_probe_pruned/combo_6.onnx
  current baseline 19080 -> candidate 19224
  n_fail=0
  decision=blocked-no-gain
```

The older case368 micro is also worse than the accepted current graph:

```text
/tmp/task319_probe4/p20_rest_plus_top_eq_all.onnx
  current baseline 19080 -> candidate 19567
  n_fail=0
  decision=blocked-no-gain
```

## Decision

Reject for submission. The previously Kaggle-valid `task319` same-task value
bundle is already fully represented in the accepted baseline. The remaining
nearby candidates do not reduce current cost.

## Next

Deprioritize residual `task319` alias continuations. Move to a different
structural target rather than spending more time on stale pre-case384 scratch
candidates.
