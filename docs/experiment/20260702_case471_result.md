# 20260702 case471 result - task396 GridSample current re-gate

## Summary

No submit.

`task396` has current positive reductions, but all material and mid-gain
candidates fail local correctness. The remaining GridSample/recolor shortcuts
are semantic.

## Baseline

```text
task396
params=1795
memory=6283
cost=8078
points=16.0031004039
status=ok
```

## Cost-Only Scan

```text
candidates=140
positive_cost_reductions=46
errors=0
```

## Correctness Gate

Material candidates:

```text
gain >= 0.020
material_candidates=11
exact_positive=0
submit_candidates=0
blocked=11
errors=0
```

Mid-gain candidates:

```text
0.010 <= gain < 0.020
mid_candidates=6
exact_mid=0
blocked_mid=6
```

Representative blocked candidates:

```text
8078 -> 5748  gain=0.340292  n_fail=266
  Equal path input alias

8078 -> 7276  gain=0.104563  n_fail=266
  terminal Where branch aliases

8078 -> 7422  gain=0.084696  n_fail=266
  Conv H/V -> amf

8078 -> 7753  gain=0.041064  n_fail=245
  Equal path alternative

8078 -> 7833  gain=0.030799  n_fail=266
  s2 -> smp

8078 -> 7984  gain=0.011705  n_fail=266
  x/y source Sub aliases
```

## Decision

Rejected for submission. No candidate met:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

The only known exact `task396` material remains terminal micro cleanup below
standalone threshold.

## Next

Move to another current-rank mid-cost target such as `task014`, `task383`, or
`task089`, using cost-only filtering before correctness gates.
