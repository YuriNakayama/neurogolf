# 20260702 case347 plan - task233 scorew uint8 compression

## Context

- Current accepted Public Score floor: `7185.37` after case346.
- Submit-saving cadence is about one submit window every 30 minutes.
- The current low-risk micro batch is still below threshold.
- Known hidden-unsafe families remain excluded from normal cadence:
  `task286`, `task101`, and `task349` tiny variants.

## Candidate

Target `task233`.

Compress the second `TopK` `scorew` value path from a float16 table to a uint8
rank/value table. The changed path feeds the second TopK scoring logic around
`mul103`, `top104_valid`, and `gre107`.

Expected local change:

```text
baseline:  params=520, memory=48934, cost=49454
candidate: params=520, memory=47284, cost=47804
n_fail=0, status=ok
gain = ln(49454 / 47804) ~= 0.0339
```

Temp ONNX:

```text
/tmp/neurogolf_case347_alt/task233_scorew_u8_top255_unique_tail1.onnx
```

This clears the medium-risk standalone threshold of `0.030`.

## Risk

The candidate is a structural table simplification, not a broad equivalence
bundle. Hidden risk remains because compressing 324 float16 ranks into 255 uint8
values may introduce score ties that do not appear in local examples.

Risk audit preferred this `top255_unique_tail1` mapping over the original
`floor255` mapping because it has fewer local TopK tie hazards at the same cost
and gain:

```text
top255_unique_tail1: tie_inside_top3=9,  tie_3rd4th=9
floor255:            tie_inside_top3=45, tie_3rd4th=21
```

## Gate

1. Re-audit the temp ONNX for `task233` only.
2. Run parallel risk/alternative scans until the next submit cadence window.
3. If no stronger or safer candidate appears, copy the candidate to
   `data/output/onnx/task233.onnx`.
4. Run `src/evaluate.audit_one` only for `task233`.
5. Submit if `n_fail=0` and cost is lower.
6. Adopt only if Kaggle Public Score improves over `7185.37`; otherwise restore
   the DVC baseline and record rejection.
