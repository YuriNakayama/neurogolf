# 20260702 case361 plan - task367 rect kernel crop

## Context

Accepted Public Score is `7185.70`. Cases 359 and 360 skipped submits because
the available exact material did not clear the risk-adjusted submit thresholds.

The current task367 graph is small but parameter-heavy:

```text
params=4749
memory=16800
cost=21549
n_fail=0
```

Most parameters are in `rect_w`, a dense `QLinearConv` kernel with shape
`(25, 2, 9, 9)`.

## Hypothesis

The `rect_w` support is anchored at the top-left. Cropping the bottom/right edge
from `9x9` to `8x8`, while updating the convolution pads from
`[2,2,6,6]` to `[2,2,5,5]`, may preserve the detector geometry but remove
`25 * 2 * (81 - 64) = 850` initializer elements.

Expected current-baseline gain:

```text
21549 -> 20699
gain = ln(21549 / 20699) = 0.040244
```

This clears the medium-risk standalone threshold `0.030`.

## Candidate

```text
/tmp/neurogolf_case361_task367/rect_tl_8x8.onnx
```

## Gate

Submit only if the changed `task367.onnx` passes:

1. `evaluate.audit_one` with `status=ok` and `n_fail=0`.
2. No forbidden ops:

```text
Loop, Scan, NonZero, Unique, Script, Function, Compress
```

3. `functions=0` and static shape inference remains valid.
4. Submission package validation succeeds.
