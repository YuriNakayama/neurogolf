# 20260630 case87 plan — t366 の TopK/component 候補数削減を検証する

## 背景

- 現 Public best は **7180.87**。
- t366 は cost **36385**（params 488 / memory 35897）で、634 node の multi-component matching graph。
- 過去 harvest で 10B 程度の削減は採用済みだが、現在も memory 支配。
- graph には `TopK` が 6 個あり、non-background pixel の上位候補、anchor 候補、component 順序を固定 shape で持っている。

## 仮説

1. `TopK` 候補数（topk_eight/topk_pos/topk_anchor/topk_three）を下げても full train/test/arc-gen に影響しない場合、memory を削れる。
2. component 3 本（c0/c1/c2）や rank 2/3 の一部が不要なら、後段を bypass できる。
3. ただし shape 固定の gather/reshape が多いため、単純な k 変更は shape error になる可能性が高い。

## 対象

- `data/output/onnx/task366.onnx`

## 足切り

- `src.evaluate.scorer.audit_one` で t366 の train/test/arc-gen 全ペア完全一致。
- n_fail=0 かつ cost 減少の場合のみ submit。

## 採否

- Public Score が 7180.87 を上回れば採用。
- 上がらない、または faithful で不一致なら破棄。
