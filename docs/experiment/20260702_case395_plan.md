# 20260702 case395 plan - recovery batch without task046

## Context

Accepted Public Score is `7186.24` after case394. Case390's 8-task micro batch
returned Kaggle `ERROR`; case391 isolated the local ORT warning to `task046`.

The strict residual set excluding both `task046` and `task037` is below the
batch gate:

```text
gain ~= 0.027569
```

Adding `task037` clears the gate while still excluding the warning-emitting
`task046`:

```text
gain ~= 0.033063
```

## Candidate Set

Apply these current-baseline exact candidates:

```text
task342 1585 -> 1576 gain 0.005694
task392 1951 -> 1941 gain 0.005139
task209 28047 -> 27927 gain 0.004288
task370 10393 -> 10351 gain 0.004049
task035 2262 -> 2255 gain 0.003099
task308 1488 -> 1484 gain 0.002692
task293 1288 -> 1286 gain 0.001554
task397 2848 -> 2845 gain 0.001054
task037 4746 -> 4720 gain 0.005493
```

Do not include:

```text
task046: emits ORT unused-initializer warning and was in case390 ERROR
task285: isolated uint8 TopK candidate returned Kaggle ERROR in case393
```

## Gate

Submit only if each changed task has:

- `n_fail=0`
- no forbidden ops and no ONNX functions
- combined current-relative gain >= `0.030`

Adopt only if Kaggle Public Score improves over `7186.24`; otherwise restore
the accepted bundle and record rejection.
