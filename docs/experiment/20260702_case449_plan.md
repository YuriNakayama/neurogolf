# 20260702 case449 plan - task029 selector re-gate

## Context

Accepted Public Score floor remains `7186.71`.

Current `task029` audit:

```text
params=38
memory=10728
cost=10766
status=ok
```

Dominant intermediates from case423:

```text
row_selector FLOAT [30,30] = 3600 bytes
col_selector FLOAT [30,30] = 3600 bytes
```

They feed:

```text
Einsum(input, row_selector, col_selector) -> output
```

Saving one selector would give a large relative reduction:

```text
ln(10766 / (10766 - 3600)) ~= 0.407
```

Even smaller exact rewires above about 214 cost clear the standalone
`gain >= 0.020` gate.

## Hypothesis

Direct `Gather` materialization is risky because it may create a full
`[1,10,30,30]` float tensor. However, there are existing scratch aliases around
row/column source, OneHot, and the final Einsum. Re-gating them against the
current accepted baseline may reveal an exact selector simplification or confirm
that the two selectors are both required.

## Method

1. Re-gate existing `task029` cleanup, surgery, and alias candidates.
2. Submit only if a candidate is exact and classified `submit-candidate`.
3. Otherwise record no-submit and move to another structural target.
