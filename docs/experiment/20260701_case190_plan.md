# 20260701 case190 plan

## Context

- Current accepted Public Score: 7181.21
- case187 and case188 showed tiny params-only gains do not move the real LB.
- Need candidates that remove full intermediates. `task085` is an untried
  three-node graph with a large full-grid Conv score tensor.

## Target

- Task: `task085`
- Baseline audit:
  - params: 881
  - memory: 4500
  - cost: 5381
  - points: 16.409370490510582
  - n_pass: 265
  - n_fail: 0
  - filesize: 3853

## Hypothesis

`task085` computes:

```text
score = Conv(input, score_weights, score_bias)  # float[1,1,30,30]
keep = Cast(score -> bool)                      # bool[1,1,30,30]
output = Where(keep, input, bg)
```

The rule blanks alternating interior cells of horizontal 3-row bars. If the
`keep` mask or final `Where` can be replaced by an existing tensor for all
public examples, removing one full-grid intermediate would reduce cost by
hundreds to thousands.

## Method

1. Probe only temporary `task085` ONNX copies.
2. Try final aliases (`output -> input`, `output -> bg`) and condition aliases.
3. Try removing `Cast` only if ONNX type constraints allow it.
4. Apply and submit only if `n_fail=0` and cost decreases.
