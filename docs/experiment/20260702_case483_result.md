# 20260702 case483 result - no-submit task243 scratch pool cadence scan

## Decision

No submit.

Accepted Public Score remains:

```text
7186.73
```

## Baseline

```text
task243.onnx
cost=22650
params=42
memory=22608
status=ok
```

## Scan

Existing unique `task243*.onnx` scratch candidates from `/tmp` and
`/private/tmp`:

```text
candidates=329
positive cost-only candidates=260
```

All positive candidates were gated:

```text
blocked-local-fail=260
submit-candidate=0
bank-low-gain=0
```

Representative failures:

```text
22650 -> 16170  gain=0.337002  n_fail=2    blocked-local-fail
22650 -> 16179  gain=0.336446  n_fail=2    blocked-local-fail
22650 -> 16818  gain=0.297710  n_fail=2    blocked-local-fail
22650 -> 17466  gain=0.259904  n_fail=2    blocked-local-fail
22650 -> 22002  gain=0.029026  n_fail=1    blocked-local-fail
22650 -> 22326  gain=0.014408  n_fail=1-265 blocked-local-fail
```

## Conclusion

Reject locally. The `task243` scratch pool contains many high-ratio candidates,
but the family is a final-fill / branch-fill alias surface where even the
smallest cost-reducing moves break correctness on at least one pair.

This reinforces the current submit cadence rule: a `10.8` minute window should
trigger a gate decision, not a forced Kaggle submit. This window is recorded as
no-submit because the gated queue is empty.
