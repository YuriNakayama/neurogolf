# 20260702 case441 result - task216 mechanical cleanup is no-submit

## Decision

- Task: `task216.onnx`
- Submit: no
- Adoption: none
- Reason: no current-baseline exact candidate reduces cost.

Current baseline:

```text
params=87
memory=9040
cost=9127
status=ok
```

## Graph Evidence

Dominant internal tensors:

```text
c12_f32     FLOAT [1,2,20,20]  3200 bytes
c12         UINT8 [1,2,20,20]   800 bytes
crop        UINT8 [1,2,18,18]   648 bytes
tl_flat_mid UINT8 [1,400]       400 bytes
tl_flat     UINT8 [1,400]       400 bytes
tl          UINT8 [1,1,20,20]   400 bytes
red_3d      UINT8 [1,20,20]     400 bytes
```

`Slice(input) -> c12_f32 -> Cast -> c12` is already limited to channels 1 and 2
over the 20x20 active grid. The resulting `c12` is shared by corner detection,
row/column checks, rectangle scoring, and final dynamic crop.

## Candidate Re-Gate

Re-gated 100 current-baseline candidates:

```text
blocked-local-fail: 87
blocked-no-gain:   13
submit-candidate:   0
```

Best apparent reductions:

```text
task216_tl_flat_mid_TO_tl_flat.onnx
  cost 9127 -> 8709
  gain 0.046880
  n_fail 109
  decision blocked-local-fail

task216_000_And_row_zero_after_TO_after_col.onnx
  cost 9127 -> 8727
  gain 0.044815
  n_fail 266
  decision blocked-local-fail

task216_005_And_col_zero_after_TO_after_row.onnx
  cost 9127 -> 8727
  gain 0.044815
  n_fail 266
  decision blocked-local-fail

task216_010_And_col_in_TO_after_col.onnx
  cost 9127 -> 8951
  gain 0.019472
  n_fail 84
  decision blocked-local-fail

task216_012_And_row_in_TO_after_row.onnx
  cost 9127 -> 8951
  gain 0.019472
  n_fail 97
  decision blocked-local-fail
```

Exact candidates:

```text
task216_broadcast_compress.onnx  cost 9127 -> 9127, n_fail=0, blocked-no-gain
task216_cleanup.onnx             cost 9127 -> 9127, n_fail=0, blocked-no-gain
task216_identity_elementwise.onnx cost 9127 -> 9127, n_fail=0, blocked-no-gain
```

Several crop-end and pad-tail aliases also produced output shape mismatches
during runtime, confirming they are semantic crop changes rather than mechanical
metadata cleanup.

## Conclusion

No submit. `task216` is structurally compact under the current formulation. The
only high-gain cleanup-looking candidate, `tl_flat_mid -> tl_flat`, still fails
109 local cases, so the edge-winner zeroing step is semantically required.

Do not revisit `task216` through the current alias pool. Future work would need
a new crop-search formulation, not local alias deletion.
