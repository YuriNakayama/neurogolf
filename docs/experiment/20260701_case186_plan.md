# 20260701 case186 plan

## Context

- Current accepted Public Score: 7181.21
- case185 accepted a single uint8 arithmetic replacement on `task069`.
- Continue the refined mask-arithmetic scan with one task at a time.

## Target

- Task: `task161`
- Baseline audit:
  - params: 34
  - memory: 3695
  - cost: 3729
  - points: 16.77610461982118
  - n_pass: 266
  - n_fail: 0
  - filesize: 3701

## Hypothesis

`task161` builds row and column masks as uint8 tensors, casts them to bool, then
selects either the detected special color or zero:

```text
row_code_u8 = Where(Cast(row_mask_u8), sp_u8, zero_u8)
col_code_u8 = Where(Cast(col_mask_u8), sp_u8, zero_u8)
```

If the masks are 0/1 uint8, these are equivalent to:

```text
row_code_u8 = Mul(row_mask_u8, sp_u8)
col_code_u8 = Mul(col_mask_u8, sp_u8)
```

Each replacement can remove one 30-element bool tensor. Testing row, column,
and both variants independently is required.

## Method

1. Build temporary `task161` variants replacing row, column, and both code
   branches with `Mul`.
2. Run `audit_one` only for `task161`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. If both is valid, apply both as a single task-local optimization; otherwise
   apply the best single valid replacement.
