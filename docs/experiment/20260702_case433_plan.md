# 20260702 case433 plan - task284 ScatterND index dtype recheck

## Context

After case432, a fresh current-graph scan ranked `task284` highest by relative
single-tensor opportunity:

```text
task284 params=653 memory=3082 cost=3735 n_fail=0
indices int64 [56,4] = 1792B
```

The dominant tensor is the `ScatterND` index cast:

```text
indices_i8 -> Cast(INT64) -> indices -> ScatterND(input, indices, desired_updates)
```

One successful int64-to-int32 narrowing would cut about `896` cost units, a
large relative gain. However, case156 reported that this op/runtime rejected
int32 indices.

## Hypothesis

If the current runtime accepts `ScatterND` int32 indices, the candidate will
clear the submit gate. If it still rejects int32, close `task284` and move to
the next fresh-scan target.

## Method

1. Reconfirm the current baseline.
2. Build a temporary candidate changing only the final `indices` Cast target
   from INT64 to INT32.
3. Run `submit gate`.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
