# 20260701 case283 plan - task219 Min/Max alias bundle

## Context

- Current adopted Public Score floor: `7181.94`
- case282 `task340` guard aliases were rejected because Kaggle returned
  `SubmissionStatus.ERROR`.
- Candidate source: parallel explorer scan over mid/high-cost tasks.
- Priority rule: choose exact candidates by relative reduction.

## Target

- `data/output/onnx/task219.onnx`

## Baseline

Current local audit:

```text
params=92
memory=18325
cost=18417
n_fail=0
```

## Hypothesis

Several `Min` / `Max` outputs in `task219` are equivalent to earlier tensors on
all valid examples:

```text
min_44  -> gat_41
min_170 -> gat_41
max_231 -> gat_227
min_245 -> red_236
min_246 -> red_236
```

Replacing these tensors should prune the redundant producers and their dead
dependencies. The largest expected gain comes from `max_231 -> gat_227`; the
remaining aliases remove smaller duplicate clamp/reduce paths.

## Expected Gain

- Baseline: `18417`
- Candidate from temporary audit: `17240`
- Relative reduction: `18417 / 17240 = 1.0683`
- Expected point gain: `ln(18417 / 17240) ~= 0.0660`

This is substantially stronger than case281/case282 and is large enough to be
visible on the Public Score if Kaggle accepts the graph.

## Verification Plan

1. Apply only the five aliases above.
2. Dead-prune unused nodes, initializers, and value_info.
3. Run individual `audit_one` for `task219` with
   `data/lake/neurogolf-2026/task219.json`.
4. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
