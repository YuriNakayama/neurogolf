# 20260701 case322 result - task030 packed qlinear tail

## Submission

- Kaggle ref: `54230944`
- Public Score before: `7183.72`
- Public Score after: `7183.79`
- Decision: adopted

## Local Gate

```text
task030 baseline:  params=148 memory=2248 cost=2396 n_fail=0 status=ok
task030 candidate: params=82  memory=2148 cost=2230 n_fail=0 status=ok
```

Relative reduction:

```text
2396 / 2230 = 1.0744
ln(old/new) ~= 0.0718 pt
```

## Change

Replaced the explicit zero-channel crop and separate final pad:

```text
Concat(bg, x1, x2_shift, zero, x4_shift) -> crop_output
Pad(crop_output, out_pad) -> output
```

with a packed live-channel tensor and one-hot `QLinearConv` projection:

```text
Concat(bg, x1, x2_shift, x4_shift) -> packed_live
QLinearConv(packed_live, one-hot 4ch->10ch weights, pads=final output pad) -> output
```

## Conclusion

The packed-channel tail replacement again translated cleanly to a Public LB
increase. This strengthens the priority for sparse output tails where explicit
zero channels can be removed and reconstructed by a one-hot projection.

## Next Step

Try `task023` packed-channel `QLinearConv` tail next:

```text
cost 11992 -> 11222
old/new = 1.0686
n_fail=0
```
