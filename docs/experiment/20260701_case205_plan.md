# case205 plan: task072 equality selector trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task072` は未着手で、baseline は `cost=421` (`params=31`, `memory=390`, `n_fail=0`)。
- `task236` と同型の上下比較 graph だが、6x5 patch で 1 tensor 削減の相対効果がある。

## 対象

- `data/output/onnx/task072.onnx`
- top/bottom の 6x5 slice を `Equal(t, b)` し、`Where(k, tv, fv)` で 4-channel patch を作る。
- Pad して出力に戻す。

## 仮説

`task236` では固定化が全滅したが、`task072` は shape とデータ分布が違う。`k` の固定化、patch 固定化、または片側 slice だけでの判定が通れば 30-byte 級以上の削減になる。

## 検証

- temp ONNX で `k -> true/false`、`p -> fixed patch`、片側 alias を probe する。
- `src.evaluate.scorer.audit_one` で `task072` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
