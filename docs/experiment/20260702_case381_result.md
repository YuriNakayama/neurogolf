# 20260702 case381 result - task013 Unsqueeze bypass bundle

## Decision

Adopted.

```text
ref: 54247409
description: case381 task013 unsqueeze bypass bundle gain 0.03158
previous accepted Public Score: 7185.88
Public Score: 7185.92
```

The real Public Score increased by `+0.04`, so keep the submitted
`task013.onnx`.

## Local Gate

Baseline:

```text
params 61
memory 1869
cost 1930
points 17.43472471810107
n_pass 267
n_fail 0
filesize 6309
```

Submitted candidate:

```text
params 61
memory 1809
cost 1870
points 17.466306290151365
n_pass 267
n_fail 0
filesize 4308
```

Local gain:

```text
ln(1930 / 1870) = 0.03158157205029884
```

This clears the standalone low-risk submit gate `0.020`.

## Change

Two compatible `Unsqueeze` outputs were bypassed:

- `col_presence_4d -> col_presence`
- `col_pattern -> line_pattern`

Each individual bypass was exact at `1930 -> 1900`, `gain 0.015666`. Applying
both together remained exact and removed `60` memory cost.

No forbidden operators were introduced.

## Parallel Findings

The same cycle also found smaller exact candidates that were not submitted:

- `task009`: `content_u8 -> content_3d_u8`, `8410 -> 8310`,
  `gain 0.011962`
- `task209`: `kc_for_min -> COL`, `28047 -> 27927`,
  `gain 0.004288`
- `task370`: already banked direct selector/sign bundle,
  `10393 -> 10351`, `gain 0.004049`

These remain backlog material only.

## Conclusion

Adopted. This is a good example of the ratio-based priority rule: a small
absolute reduction on a low-cost task produced enough relative gain to move the
leaderboard.

Next action: continue looking for same-task compatible bundles where two or more
sub-threshold exact rewrites combine past `gain >= 0.020`.
