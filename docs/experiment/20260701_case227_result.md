# case227 result: task058 bit table encoding trim

## 結果

- 対象: `task058`
- baseline: `cost=1033` (`params=781`, `memory=252`, `n_fail=0`)
- submit: なし
- 採否: 不採用

## 検証内容

temp ONNX で以下を個別に差し替え、`src.evaluate.scorer.audit_one` で検証した。

- `column_bits` の broadcast shape 縮小
- `column_bits = col_powers & channel_mask` への分解
- `row_bits_table` の width truncate + `Pad`

## 監査結果

| 変更 | cost | n_pass | n_fail | 判定 |
| --- | ---: | ---: | ---: | --- |
| baseline | 1033 | 22 | 0 | ok |
| `column_bits` broadcast shape | invalid | 0 | 22 | reject |
| factor `column_bits` | 7973 | 0 | 22 | reject |
| `row_bits_table` width 16 + Pad | 943 | 17 | 5 | reject |
| `row_bits_table` width 20 + Pad | 1039 | 22 | 0 | reject: cost worsens |
| `row_bits_table` width 24 + Pad | 1135 | 22 | 0 | reject: cost worsens |
| `row_bits_table` width 28 + Pad | 1231 | 22 | 0 | reject: cost worsens |
| `row_bits_table` width 29 + Pad | 1255 | 22 | 0 | reject: cost worsens |

## 結論

実例の size は 5..20 をすべて含むため、`row_bits_table` の行削減は不可。width 20 以上の truncate は正答だが、`Pad` 中間 memory が増えて cost が悪化する。`column_bits` の単純 broadcast 縮小は output shape と合わず invalid、mask 分解は中間が大きすぎて cost が大幅悪化した。実 ONNX は変更せず、Kaggle submit もしない。

次は params 圧縮が memory 悪化で相殺されない、より小さい shape の table task を狙う。
