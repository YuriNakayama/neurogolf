# 2026-06-30 case111 result

## 実 LB

- submit ref: `54193759`
- Public Score: **7180.97**
- floor: **7180.97**
- 採否: **不採用**（実 LB 上昇なし）

## 対象

- `task118.onnx`
- baseline: params 4614, memory 34623, cost 39237, n_pass 267, n_fail 0

## 試行

### branch 固定

`gates` を constant に固定し、`support` 比較・gate 生成を prune。

| 変更 | nodes | params | memory | cost | n_pass | n_fail |
|---|---:|---:|---:|---:|---:|---:|
| gate2 only `[1,0]` | 34 | 4615 | 32574 | 37189 | 138 | 129 |
| gate3 only `[0,1]` | 34 | 4615 | 32574 | 37189 | 141 | 126 |

両 branch とも必要で、固定は不可。

### support score 補正削除

`three16` を 2/1/0/4 に変えても全て n_fail=0 だったため、`support_ge3` を
`support3_score >= support2_score` から `support3 >= support2` に直接変更し、
`ReduceMax/Split/Mul/Add` 補正サブグラフを prune。

| 変更 | nodes | params | memory | cost | n_pass | n_fail |
|---|---:|---:|---:|---:|---:|---:|
| direct `support3 >= support2` | 41 | 4613 | 34607 | 39220 | 267 | 0 |

個別監査では正答、cost は 17 減少。

## 提出と採否

- message: `case111 t118 remove redundant support max cost 39237 to 39220`
- Public Score は **7180.97** で前回 floor から変化なし。
- 目標ルール「実 LB スコアが上がれば採用、上がらなければ破棄」に従い不採用。
- `data/output/onnx/task118.onnx` と `data/output/onnx.dvc` は baseline に復元済み。

## 結論

`task118` の support score 補正は faithful 監査上は冗長だが、cost 削減が 17 と小さく Public 表示に反映されなかった。単独採用せず、より大きい memory 削減候補を探す。

## 次の一手

- `task187` / `task133` / `task285` / `task349` など、100B 以上の memory 中間を消せる可能性があるタスクへ移る。
- `task118` では branch 固定は不可。補正削除は将来の複数微小差分バッチ候補としてのみ記録する。
