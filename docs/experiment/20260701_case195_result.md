# case195 result: task207 background block selector trim

## 結果

- 対象: `task207`
- baseline: `cost=236`, `params=44`, `memory=192`, `n_fail=0`
- submit: なし
- 採否: 不採用

## 試行

4 隅の背景 count と `Mod(..., 3)` を temp ONNX 上で簡略化した。

| 変更 | cost | n_pass | n_fail |
|---|---:|---:|---:|
| `background_u8 -> bg_counts_u8` | 232 | 43 | 222 |
| `bg_counts -> tl_bg` | 172 | 74 | 191 |
| `bg_counts -> tr_bg` | 172 | 75 | 190 |
| `bg_counts -> bl_bg` | 172 | 58 | 207 |
| `bg_counts -> br_bg` | 172 | 58 | 207 |
| `bg_counts -> tl_bg`, no mod | 168 | 74 | 191 |
| `bg_counts -> tr_bg`, no mod | 168 | 75 | 190 |
| `bg_counts -> bl_bg`, no mod | 168 | 58 | 207 |
| `bg_counts -> br_bg`, no mod | 168 | 58 | 207 |

## 結論

`task207` は低 cost だが、4 隅 count と mod 3 判定が正答性に直結している。1 corner alias も `Mod` 削除も完全一致しないため、実 ONNX は変更していない。

## 次の一手

低 cost 候補では、単純 selector alias よりも dtype 変換や unused initializer のような意味を変えない削減を優先する。次は `task332` または `task153` の短い graph を確認する。
