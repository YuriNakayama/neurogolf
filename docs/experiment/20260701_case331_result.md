# 20260701 case331 result - task052 packed bool strip tail

## Decision

Adopted.

## Local Gate

Only `task052.onnx` was changed and audited.

```text
baseline:  params=14 memory=213 cost=227 n_fail=0 status=ok
candidate: params=29 memory=180 cost=209 n_fail=0 status=ok
```

Local point gain estimate:

```text
ln(227 / 209) ~= 0.0826 pt
```

## Submission

```text
ref=54232306
description=case331 task052 packed bool strip tail cost 227 to 209
status=COMPLETE
publicScore=7184.82
```

Previous accepted floor:

```text
7184.74
```

The Public Score improved by `+0.08`, so the candidate is accepted.

## Diff Summary

The accepted rewrite removes four explicit zero channels from the terminal
6-channel bool strip. It repeats only the two live channels, casts the packed
strip to uint8, and restores output channels 0 and 5 through one
`QLinearConv` with final padding folded into the convolution pads.

## Next Step

Continue sparse-tail probing, but also wait for the medium/high-cost structural
explorer to identify a larger same-task candidate.
