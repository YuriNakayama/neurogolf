# 20260703 case565 plan - task118 close-miss repair

Accepted Public Score:

- `7187.57`

## Background

Broad candidate generation in cases 563 and 564 produced no submit-ready
candidates. The best known close miss with meaningful relative gain is:

```text
task118
candidate: /tmp/neurogolf_parallel_S/alias_scan/task118_42_ScatterElements_in0_ng_patch_pos_mask_flat_TO_ng_patch_base_flat.onnx
local cost: 35807 -> 35095
gain: 0.020085
n_fail: 1
```

Prior repair attempts restored correctness only by returning to baseline cost or
left one failing example with mask artifacts.

## Hypothesis

The single failing train example may be repairable with a small targeted guard or
patch that preserves most of the scatter removal. The repair must avoid adding
back the full deleted scatter path.

## Method

1. Re-gate the close miss against current baseline.
2. Inspect the failing task118 example and mismatch cells.
3. Try one bounded repair at a time in `/private/tmp/neurogolf_case565_task118`.
4. Submit only if `n_fail=0`, cost `<35807`, functions `0`, forbidden ops `[]`.

## Submit Rule

Do not submit `n_fail=1` or an exact repair with no positive gain.
