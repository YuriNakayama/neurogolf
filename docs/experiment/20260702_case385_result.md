# 20260702 case385 result - task023 single asgs0 bypass

## Decision

Adopted.

```text
ref: 54248254
description: case385 task023 single asgs0 bypass gain 0.02602
previous accepted Public Score: 7186.00
Public Score: 7186.03
```

The real Public Score increased by `+0.03`, so keep the submitted
`task023.onnx`.

## Local Gate

Baseline:

```text
params 70
memory 11142
cost 11212
points 15.675260087715321
n_pass 266
n_fail 0
filesize 8187
```

Submitted candidate:

```text
params 70
memory 10854
cost 10924
points 15.701282517406405
n_pass 266
n_fail 0
filesize 7923
```

Local gain:

```text
ln(11212 / 10924) = 0.026022429691083263
```

This clears the standalone submit gate `0.020`.

## Change

Applied only the single rewire:

- `asgs0 -> a10`

The broader producer scan also found a 19-rewire `task023` bundle, but it was
not submitted because it is much broader and more hidden-sensitive.

Verification:

```text
nodes 144 -> 140
forbidden ops []
functions 0
n_fail 0
```

## Deferred Findings

Exact candidates banked from this cycle:

- `task023` broad 19-rewire bundle: `11212 -> 9340`, `gain 0.182678`,
  high hidden-risk, scratch
  `/tmp/neurogolf_case385_producer/task023_bundle_19.onnx`
- `task046`: `2318 -> 2286`, `gain 0.013901`, scratch
  `/tmp/neurogolf_case385_producer/task046_bundle_1.onnx`
- `task009`: `8410 -> 8310`, `gain 0.011962`, already known from case381
- `task037`: `4746 -> 4720`, `gain 0.005493`
- `task035`: `2262 -> 2255`, `gain 0.003099`
- `task374`: `2304 -> 2284`, `gain 0.008718`
- `task382`: `5670 -> 5627`, `gain 0.007613`
- `task392`: `1951 -> 1941`, `gain 0.005139`

## Conclusion

Adopted. Single-task, single-rewire reductions with modest relative gain
continue to move the leaderboard. Keep broad local-equivalence bundles separate
from low-risk submits unless they are isolated and worth the risk.
