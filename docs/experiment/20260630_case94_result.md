# 20260630 case94 result — t364 の 7段 propagation は必要（提出なし）

## 対象

- `task364.onnx`
- baseline: params **113**, memory **22900**, cost **23013**, faithful `n_fail=0`
- Public Score floor: **7180.87**

## 実施内容

t364 は `gA` / `gB` から `MaxPool -> Mul(Gu)` を 7段ずつ展開し、`PA7 * PB7` の値で recolor 種別を決める graph。memory 上位は 20x22 の propagation tensor 群で、1段短縮できれば 440 cost 単位の削減が見込める。

### propagation 短縮 probe

| 候補 | cost | 結果 |
|---|---:|---|
| `PA7 -> SA6` | 22573 | `n_fail=21` |
| `PB7 -> SB6` | 22573 | `n_fail=21` |
| `PA7 -> SA6`, `PB7 -> SB6` | 22133 | `n_fail=21` |
| `PA7 -> SA5` | 21693 | `n_fail=67` |
| `PB7 -> SB5` | 21693 | `n_fail=66` |
| `PA7 -> SA5`, `PB7 -> SB5` | 20373 | `n_fail=67` |

A/B 片側だけでも最終 7段目が必要。両側を同時に短縮しても同じ境界例で壊れる。

### metadata prune

- `value_info` 全削除で filesize **3687→2394**。
- faithful cost は **23013 のまま**で Public Score 改善には寄与しない。
- ONNX 差し替えは行わない。

## 採否

- **不採用、提出なし**。
- `data/output/onnx/task364.onnx` は未変更。

## 結論

t364 の 14本 MaxPool は冗長ではなく、arc-gen の連結成分形状分類で 7段到達が必要。現 graph に安全な局所短縮は見つからなかった。次は `task243` / `task319` / `task002` へ移る。
