# 2026-06-30 case120 plan

## 方針

- floor: Public Score 7181.07
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task383.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task383.onnx`
- baseline: params 41, memory 7264, cost 7305, n_pass 266, n_fail 0

## 仮説

`task383` は bounding box の上下左右を検出し、`vcol4` と `hrow4` の 2 branch で縦横方向を補色している。もし片側 branch、または `F4/G4` の二段色選択が一部データで冗長なら、30x30 級の `Where` / `Gather` / `Equal` 中間を削れて数 % の相対削減が見込める。

## 手順

1. `/tmp` 上の candidate ONNX で `Cout -> afterV`、`Cout -> Cidx`、`vcol4/hrow4` 固定化、`vColor/hColor` 簡略化を試す。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task383.onnx` に反映する。
3. submit し、Public Score が 7181.07 を上回れば採用、上がらなければ破棄する。
