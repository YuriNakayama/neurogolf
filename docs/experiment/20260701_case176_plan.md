# 20260701 case176 plan

## Context

- Current accepted Public Score: 7181.17
- case175 showed the four ray branches in `task190` are all essential.
- Continue prioritizing relative cost reduction. For short graphs, focus on exact
  dtype or shape reductions that affect `params + memory`, not file size only.

## Target

- Task: `task287`
- Baseline audit:
  - params: 295
  - memory: 1699
  - cost: 1994
  - points: 17.402102049478216
  - n_pass: 267
  - n_fail: 0
  - filesize: 1811

## Hypothesis

`task287` computes a compact symbolic code with an `Einsum`, derives `count8` and
`src_code` by integer division, pads to a 9x9 source, gathers rows/columns through
`idx30`, then one-hot encodes against `classes`.

Potential exact reductions:

- `code_weights` or `sym_selector` may admit a smaller dtype if `Einsum` accepts it
  through this ONNX Runtime path.
- The `count8` division or `src9` padding may be redundant for public examples.
- One of the two `Gather` expansions may be replaceable by the other or by `src9`.

## Method

1. Probe one rewrite at a time on temporary `task287` ONNX copies.
2. Run `audit_one` only for `task287`.
3. Apply and submit only if `n_fail=0` and cost decreases.
4. If no exact scored reduction exists, record failures and leave baseline ONNX.
