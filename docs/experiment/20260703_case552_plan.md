# 20260703 case552 plan - task025 structural reduction

Accepted Public Score:

- `7187.51`

## Background

Case550 adopted `task066` and improved Public Score to `7187.51`. Case551 found
only low-gain exact micros, so this cycle returns to a single non-risk
structural target.

`task025` was identified in the case549 shortlist as a high-upside non-risk
task:

```text
current cost 11664
shapes vary around 15x14 to 15x26
same-shape transformation
```

## Hypothesis

The visible examples look like sparse point or marker relocation across
separator bands. The current ONNX may contain structural overhead that can be
reduced without changing the solver's logic.

## Method

1. Inspect all train/test/arc-gen examples and summarize changed cells.
2. Inspect current graph op types and high-memory intermediates.
3. Re-gate any existing `task025` scratch candidates against the current
   accepted baseline.
4. Try one bounded structural simplification if a clearly redundant
   intermediate is found.

## Submit Rule

Submit only if the changed `task025.onnx` has:

```text
n_fail = 0
gain >= 0.020
functions = 0
forbidden ops = []
```

Otherwise record no-submit and bank exact micros only.
