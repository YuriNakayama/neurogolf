# 20260702 case470 result - task377 ring/rank current re-gate

## Summary

No submit.

`task377` has current positive reductions, but all material and mid-gain
candidates fail local correctness. This matches the case121 conclusion that the
ring/rank paths are semantically required.

## Baseline

```text
task377
params=107
memory=8261
cost=8368
points=15.9678298137
status=ok
```

## Cost-Only Scan

```text
candidates=119
positive_cost_reductions=39
errors=0
```

## Correctness Gate

Material candidates:

```text
gain >= 0.020
material_candidates=15
exact_positive=0
submit_candidates=0
blocked=15
errors=0
```

Mid-gain candidates:

```text
0.010 <= gain < 0.020
mid_candidates=6
exact_mid=0
blocked_mid=6
```

Representative blocked candidates:

```text
8368 -> 6962  gain=0.183948  n_fail=266
  rd -> curr

8368 -> 7666  gain=0.087620  n_fail=266
  rd -> prev

8368 -> 8046  gain=0.039240  n_fail=144
  Mul rank path input alias

8368 -> 8159  gain=0.025293  n_fail=255
  active_b -> row_any

8368 -> 8186  gain=0.021990  n_fail=253
  picked -> rank_match_u8

8368 -> 8263  gain=0.012627  n_fail=144..266
  rank -> active_i / left_idx -> row_idx
```

## Decision

Rejected for submission. No candidate met:

```text
n_fail=0
gain >= 0.020
decision=submit-candidate
```

Do not revisit `task377` through `rd`, `rank`, `active_b`, `picked`, or ring
index aliases without a repair strategy.

## Next

Move to the next current-rank mid-cost target. Avoid repeating closed stale
pools; use cost-only first and focus on candidates with either prior hidden
transfer evidence or mechanical cleanup semantics.
