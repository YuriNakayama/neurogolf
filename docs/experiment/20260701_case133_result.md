# 2026-07-01 case133 result

## 対象

- `task182.onnx`
- baseline: params 98, memory 6695, cost 6793, points 16.176352050808696, n_pass 267, n_fail 0

## 実施内容

`Conv -> AveragePool -> QLinearConv -> MaxPool -> Where` の中間を削れるか、kernel/threshold/dilation/recolor guard 周辺を `/tmp` の候補 ONNX で個別に差し替えた。

## 足切り結果

主な失敗候補:

- `score_dil -> score`: cost 6393, n_fail 267
- `dil_b -> c1_b`: cost 4975, n_fail 266
- `recolor_b -> dil_b`: cost 6393, n_fail 267
- `recolor_b -> c1_b`: cost 4575, n_fail 266
- `thr_u8 -> yzp`: cost 6682, n_fail 256
- `thr_i32 -> nshape_i32`: cost 6788, n_fail 266
- `kflat_u8 -> tmask_u8`: cost 6768, n_fail 267
- `out_id -> cid`: cost 2959, n_fail 267
- `enc -> one_u8` / `enc -> z_u8`: cost 6791, n_fail 267
- `Pad` の埋め値 `pad255 -> z_u8`: cost 6792, n_fail 267

完全一致だが cost が変わらなかった候補:

- `Cast(tmask_i32)` の入力を `tmask_u8 -> tmask_b`: cost 6793, n_fail 0
- `Greater(tv, z_u8)` を `Greater(tv, one_u8)`: cost 6793, n_fail 0

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

`task182` の大きな中間は `QLinearConv` と `MaxPool` が正答性に直結しており、threshold/kernel/recolor guard の省略はすべて完全一致を壊した。意味を保つ小変更は scorer 上の cost に影響しなかった。

## 次の一手

`task004` を見る。`task004` は `Conv` 後の move/stay 分岐と `Gather` が小さい graph にまとまっているが、`move_vals`/`stay_vals` の片側や `row_next` guard を削れれば相対削減率は比較的大きい。
