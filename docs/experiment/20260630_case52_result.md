# 20260630 case52 result — uint8-redo 候補20件を特定（float MaxPool floor）、t187/t278 を精査（提出なし）

## independent_impl（必須欄）
case49-51 で確立した「uint8 が MaxPool/論理ベースに効く」を活かし、**floor が float MaxPool を
使う高コストタスク**を網羅抽出（uint8 で 4× 削減できる純増候補）:
- **20件発見**: t110(13787,9MP,9756Bfloat), t187(36814,14MP,7200B), t349(26189,6MP,7200B),
  t364(23013,14MP), t278(6131,1MP), t204, t350 等。floor がこれらで float 中間を使用。
- **t187 精査**: 8-box の内部を 2 で fill + 8-wall 接続 + 3-bg の**多段規則**（add 2 と 3 両方）。
  単純 enclosed-fill テンプレート非適用。14 MaxPool は多段 flood/dilation。
- **t278 精査**: ペアになった 2-cell に 3×3 の 3-ring を stamp する**局所規則**（k=5 local,
  lut=8893）。uint8 Conv-stamp 化の候補だが lut 大（hidden 過学習リスク, case1 教訓）で要慎重。

## 前進（純増候補パイプライン確立）
- uint8 レバーで勝てる可能性のある具体的タスク **20件**を特定（float MaxPool floor）。
  これらは floor が uint8 化を**未適用**の可能性があり、uint8 redo で cost 純減を狙える本命群。
- ただし各タスクの規則は多段(t187)や large-lut(t278)で、単純テンプレート転用は不可。
  per-task で規則を decode し uint8 構築する必要（次サイクル以降で1つずつ）。

## 恒久方針の遵守
- 停止せず、uint8 純増候補20件を体系的に抽出（探索を前進）。次サイクルで候補の1つを
  per-task decode + uint8 構築して floor 越えを狙う。stop提案なし。
- floor 7180.78（DVC 6e8d4642）退行なし。

## 次の一手
- 候補20件から「規則が単純（少 lut or 明確な多段）かつ float MaxPool 主体」のタスクを選び、
  uint8 で per-task 構築。t204(11494,7MP,25node)/t350(9036,4MP,12node) など低ノードを優先精査。
