# 20260701 case276 result - reject task338 single pair alias

## Result

- Target: `task338`
- Baseline: `params=689`, `memory=17250`, `cost=17939`, `n_fail=0`
- Candidate: `params=689`, `memory=17130`, `cost=17819`, `n_fail=0`
- Relative reduction: `17939 / 17819 = 1.0067`
- Submit ref: `54222073`
- Public Score: `7181.72 -> 7181.72`
- Adoption: rejected

## Change Tested

Applied one local-exact alias:

```text
tr_pair -> tr_pair_0
```

The alternative single alias `bl_pair -> bl_pair_0` has the same local cost.
Bundling both was not submitted because local audit fails:

```text
cost=17689
n_pass=258
n_fail=9
status=INCORRECT
```

## Verification

Submitted candidate local audit:

```text
params=689
memory=17130
cost=17819
n_pass=267
n_fail=0
status=ok
```

## Decision

Rejected. The local gain was real but too small to move the displayed Public
Score from the current floor. `data/output/onnx/task338.onnx` was restored from
DVC and no ONNX/DVC change was adopted.

## Next Step

Keep the updated priority rule: require `n_fail=0`, then favor candidates with
larger `old_cost / new_cost`. Single aliases around `0.7%` can be skipped unless
they can be bundled within the same task. Continue searching for same-task
bundles near or above `1%`, especially boolean mask or guard aliases that remove
multiple memory-counted intermediates.
