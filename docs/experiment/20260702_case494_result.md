# 20260702 case494 result - adopt task048 exp_7 cleanup

## Decision

Adopt.

Public Score:

```text
7186.79 -> 7187.10
```

Kaggle submission:

```text
ref=54264410
description=case494 task048 exp_7 cleanup
status=SubmissionStatus.COMPLETE
```

## Change

Replaced:

```text
data/output/onnx/task048.onnx
```

Candidate:

```text
/private/tmp/neurogolf_case406_scan/task048_093_BitwiseAnd_exp_7_TO_h1_4.onnx
```

Local gate:

```text
baseline cost : 1146
candidate cost: 842
gain          : 0.308253
n_fail        : 0
functions     : 0
forbidden ops : []
decision      : submit-candidate
```

Dry-run pre-submit gate:

```text
changed=1
total gain=0.308253
decision=submit-bundle
```

## Conclusion

The worker-found `task048` alias was hidden-safe on Public LB and produced a
large visible improvement. Keep prioritizing relative-gain candidates from
small and medium tasks when they pass `n_fail=0`; this result is a concrete
example of a small absolute-cost task with a large ratio improvement.

## Next Step

Continue expanded scratch sweeps. W8 stopped after finding `task048`; its cost
scan noted `task005` still has submit-threshold cost-positive candidates that
were not gated after the early stop, so `task005` is the next high-priority
candidate.
