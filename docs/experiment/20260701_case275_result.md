# 20260701 case275 result - adopt task080 bundled mask aliases

## Result

- Target: `task080`
- Baseline: `params=112`, `memory=17024`, `cost=17136`, `n_fail=0`
- Candidate: `params=112`, `memory=16724`, `cost=16836`, `n_fail=0`
- Relative reduction: `17136 / 16836 = 1.0178`
- Submit ref: `54221866`
- Public Score: `7181.70 -> 7181.72`
- Adoption: adopted

## Change

Applied three same-task mask aliases:

```text
isol    -> nonb
ecells0 -> eblk
ccells  -> cblk
```

Each alias removes one 100-byte intermediate; dead-node pruning removed the now-unused `And` nodes and stale metadata.

## Verification

Individual local audit for the submitted `task080.onnx`:

```text
params=112
memory=16724
cost=16836
n_pass=231
n_fail=0
status=ok
```

DVC was updated and pushed after the Public Score improved.

## Conclusion

Adopted. This confirms the current priority rule: bundled exact mask aliases with relative reduction above roughly 1% can move the real Public Score.

## Next Step

Continue alias-bundle search. The interrupted scan found many tiny `task101` aliases, but most were below 0.2%; it may only be worthwhile if several independent aliases can be bundled without introducing local failures.
