# 20260702 case427 plan - task192 terminal Where collapse

## Context

Case426 closed `task054` because the large index-family rewrite is blocked by
`GatherND`/runtime type constraints. The current graph scan ranked `task192` as
the best next relative-gain target:

```text
task192 params=106 memory=8515 cost=8621 n_fail=0
```

One full `30x30` derived tensor is `900` cost, or about `10.4%` of this task.

## Hypothesis

The terminal path is:

```text
keep_or_vgap_b = score > 9.5
vgap_b         = score == 12
pre_target_u8 = Where(keep_or_vgap_b, main_u8, bg_or_out_u8)
target_u8     = Where(vgap_b, bg_or_out_u8, pre_target_u8)
```

Because `score == 12` is a subset of `score > 9.5`, the chain may collapse to a
single condition for the non-gap line class. If the valid score values make that
condition exact, deleting one or more full-grid intermediates should clear the
submit threshold.

## Method

1. Inspect runtime `score` values on all task examples.
2. Build at most one local terminal candidate if the value set exposes an exact
   single-condition rewrite.
3. Run `submit gate`.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
