# case233 plan: task234 terminal grid where trim

## 現状
- 採用済み Public Score floor: 7181.21
- `task234` baseline は `cost=6853` (`params=45`, `memory=6808`, `n_fail=0`)。
- 終盤は `base_grid = Where(valid_mask, zero_u8, outside_u8)`、`pure_grid = Where(pure_mask, pure_color_u8, base_grid)`、`color_grid = Where(arm_mask, arm_color_u8, pure_grid)`、`Pad(..., outside_u8)`。
- params は小さく、20x20 mask/grid の中間 memory 削減が主な改善余地。

## 対象
- `data/output/onnx/task234.onnx`

## 仮説
20x20 crop 内が常に valid なら `base_grid` を scalar `zero_u8` に alias して、`valid_mask` と `base_grid` の中間を削れる。さらに `pure_mask` / `arm_mask` の重なりが固定なら終端 `Where` を削れる可能性がある。

## 検証
- `base_grid` を `zero_u8` / `outside_u8` に alias して `audit_one` する。
- `pure_grid` を `base_grid` / `pure_color_u8` に固定する。
- `color_grid` を `pure_grid` / `arm_color_u8` に固定する。
- `vertical` と arm direction branch の固定化を最小限 probe する。
- `src.evaluate.scorer.audit_one` で `task234` の全ペア完全一致と cost を確認し、`n_fail=0` かつ cost が下がった候補だけ実 ONNX へ反映する。
