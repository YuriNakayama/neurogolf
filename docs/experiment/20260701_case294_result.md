# case294 result: task201 width crop-at-source Conv

## Summary

- Submission ref: `54224983`
- Previous Public Score: `7182.63`
- New Public Score: `7182.86`
- Decision: adopt

## Local Gate

- Task: `task201`
- Before cost: `4614`
- After cost: `3676`
- Relative reduction: `1.2552`
- Local points: `16.790419516524423`
- Correctness: `n_pass=266`, `n_fail=0`
- Status: `ok`

## Change

Replaced:

`Conv(13x30) -> Cast -> Slice([0:13, 0:13])`

with:

`Conv(13x13) -> Cast`

The original Conv already had tight height geometry via a `(2,1)` kernel with height
dilation `17`. The rewrite expands only the kernel width to `18`, places the old weights
at width offset `0`, and directly emits the first 13 columns used by the solver.

## Conclusion

Accepted. The Public gain was `+0.23`, confirming that crop-at-source is currently the
highest-yield optimization family when the deleted tensor is large enough to beat sparse
kernel parameter growth.

## Next Step

Continue the crop-after-full scan, but reject candidates where sparse-kernel parameter
growth exceeds memory saved. `task366` is the next high-value candidate to inspect
carefully because it has a large `30x30` label tensor with multiple cropped consumers.
