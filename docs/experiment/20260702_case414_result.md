# 20260702 case414 result - reject submit-saving exact micro bundle

## Submission

```text
ref: 54256964
description: case414 submit-saving exact micro bundle gain 0.02629
status: COMPLETE
previous Public Score: 7186.71
new Public Score: 7169.75
decision: reject
```

## Local Gate

The five-task bundle passed targeted local audit before submit:

| task | local cost | n_fail | forbidden ops | functions |
|---:|---:|---:|---|---:|
| 090 | 3030 | 0 | [] | 0 |
| 209 | 27927 | 0 | [] | 0 |
| 370 | 10355 | 0 | [] | 0 |
| 308 | 1484 | 0 | [] | 0 |
| 293 | 1286 | 0 | [] | 0 |

Combined local gain:

```text
0.026288176493078214
```

## Outcome

Reject. The real Public Score regressed by `-16.96`, so at least one member of
the bundle is hidden-unsafe despite `n_fail=0` locally.

The accepted DVC baseline was restored immediately after the result:

```text
git restore data/output/onnx.dvc
dvc checkout --force data/output/onnx
```

## Conclusion

The submit-saving idea remains valid, but the risk model was too optimistic:
locally exact micros from old alias scans cannot be treated as automatically
low-risk just because they are small. For future submit-saving bundles:

- do not include `task090`, `task209`, `task370`, `task308`, or `task293` from
  this candidate set without isolated re-validation;
- avoid mixing multiple previously unisolated local-equivalence aliases in one
  submit unless each task is already Kaggle-validated or the total expected
  gain is large enough to justify bisection cost;
- prefer one structural candidate with `gain >= 0.02` over a bundle of
  unisolated hidden-sensitive micros;
- keep `task035` from the subagent result banked only; its gain is `0.00310`
  and it is not worth a standalone submit after this regression.

Next action: return to isolated candidates with stronger relative gain, or
explicitly bisect case414 only if no better candidate appears and submit budget
allows.
