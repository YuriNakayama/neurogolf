# 20260630 case74 result — t349 case71後の未使用 `beam_b` Cast を削除、Public 7180.85 採用

## independent_impl:
case71 後の t349 graph を再点検し、`beam_u8 -> Cast(bool) -> beam_b` が未使用 dead-end として残っていることを確認。
- case71 で `Where(halo_b, three_u8, beam_u8)` に変更したため、`beam_b` は後段入力から消滅。
- それでも `Cast ['beam_u8'] -> ['beam_b']` node が残り、`[1,1,30,30]` bool 中間 900B を計上していた。
- graph から当該 Cast を削除。

## faithful 検証
- before: cost 25289, params 89, memory 25200, n_fail 0
- after: **cost 24389, params 89, memory 24300, n_pass 267, n_fail 0**
- 削減: **900 cost**（未使用 bool 中間）

## submit
- ref: `54190546`
- message: `case74 t349 remove dead beam_b cast cost 25289 to 24389`
- status: `COMPLETE`
- Public Score: **7180.85**（7180.81 から **+0.04**）

## 採否
- **採用**。ローカル faithful n_fail=0 かつ実 LB が 7180.81→7180.85 に上昇。

## 次の一手
- 同じ dead-end 残存を全タスクで機械探索したところ、未使用 initializer / Constant は0件。
  次は保守的 `onnxoptimizer` probe で見つかった t066 の小改善（cost 18386→18380, -6）を精査する。
