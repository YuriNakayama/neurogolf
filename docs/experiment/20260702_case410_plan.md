# 20260702 case410 plan - isolate task046 row alias

## Context

- Accepted Public Score is `7186.69` after case409 adopted the isolated
  `task066` score alias.
- Scoring priority is correctness first, then relative cost reduction:
  `gain = ln(old_cost / new_cost)`.
- `task338` was rechecked locally and by a subagent. The only exact candidates
  remain the two known single pair aliases, each `17939 -> 17819`
  (`gain=0.006712`), and the two-alias bundle still fails local correctness.
  This is too small and already failed to move Public as a standalone case.

## Target

Target: `task046`.

Candidate:

- scratch: `/tmp/neurogolf_case385_producer/task046_bundle_1.onnx`
- rewrite: `shifted_row_2_off_-2 -> src_row_0`
- current baseline: `cost=2318`, `n_fail=0`
- candidate: `cost=2286`, `n_fail=0`
- expected gain: `ln(2318 / 2286) = 0.013901`

## Rationale

This candidate is smaller than the preferred `0.02` standalone gate, but it is
the best current isolated candidate above the purely micro range. It was part
of the case390 ERROR batch, but case395 removed `task046` and still returned
Kaggle `ERROR`, so `task046` is not the leading ERROR suspect. Isolating it
keeps attribution clear and avoids mixing it with known problematic family
members.

## Gate

1. Replace only `data/output/onnx/task046.onnx`.
2. Run `evaluate.audit_one` for `task046` only.
3. Confirm no forbidden ops are introduced.
4. Submit if `n_fail=0` and cost remains lower.
5. Adopt only if Kaggle Public Score improves over `7186.69`; otherwise restore
   the accepted DVC state and record rejection.
