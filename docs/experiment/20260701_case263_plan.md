# 20260701 case263 plan - task108 Conv kernel crop

## 背景

- Current floor: Public Score `7181.61`
- `task108` baseline: `cost=971` (`params=107`, `memory=864`, `n_fail=0`)
- `labels` Conv の `w` は shape `[1,10,3,3]` だが、非ゼロは中央1x1だけで外周がゼロ。
- 小 cost task では kernel element 削減の相対効率が高い。

## 対象

- `data/output/onnx/task108.onnx`

## 仮説

`Conv` の kernel を 3x3 から 1x1 にcropし、pads `[17,17,0,0]` を crop offset に合わせて調整できれば、Conv output shapeと値を保ったまま params を削れる可能性がある。

## 手順

1. `/tmp` に `task108` の cropped kernel版 ONNX を生成する。
2. `src.evaluate.scorer.audit_one` で `task108` の完全一致と costを確認する。
3. `n_fail=0` かつ cost 減なら本体へ反映し、Kaggle submit で採否を決める。
