# 20260701 case321 result - task271 packed float16 channel pad

## Submission

- Kaggle ref: `54230858`
- Public Score before: `7183.64`
- Public Score after: `7183.72`
- Decision: adopted

## Local Gate

```text
task271 baseline:  params=46 memory=1141 cost=1187 n_fail=0 status=ok
task271 candidate: params=57 memory=1033 cost=1090 n_fail=0 status=ok
```

Relative reduction:

```text
1187 / 1090 = 1.0890
ln(old/new) ~= 0.0853 pt
```

## Change

Replaced the final sparse channel materialization:

```text
Concat(blue_patch, zero_patch x6, cyan_patch) -> output_3x3_c1_to_c8
Pad(output_3x3_c1_to_c8, pads=[0,1,0,0,0,1,27,27]) -> output
```

with a two-channel packed tensor and a one-hot float16 projection:

```text
Concat(blue_patch, cyan_patch) -> packed_live
Conv(packed_live, one-hot 2ch->10ch weights, pads=[0,0,27,27]) -> output
```

The projection maps output channel 1 from `blue_patch` and channel 8 from
`cyan_patch`; all other output channels are zero.

## Conclusion

The local exact cost reduction produced a real LB improvement, consistent with
the recent packed-channel tail replacements in cases 318-320. Keep prioritizing
relative reductions in this pattern, but record even smaller exact candidates in
`micro_backlog.md` so they are not lost.

## Next Step

Try the next packed-channel candidates in descending relative reduction:

1. `task030`: `2396 -> 2230`
2. `task023`: `11992 -> 11222`
3. `task266`: `350 -> 334`
