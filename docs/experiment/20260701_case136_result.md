# 2026-07-01 case136 result

## 対象

- `task250.onnx`
- baseline: params 142, memory 3076, cost 3218, points 16.92348467244767, n_pass 265, n_fail 0

## 実施内容

`rowmap` / `colmap` の `Clip(rows, lo/hi)` を中心に、OneHot 前の index map と出力 occupancy branch を `/tmp` 上の候補 ONNX で差し替えた。

## 足切り結果

すべて `n_fail>0` または session error。

- `rowmap -> rows`: cost 3120, n_fail 265
- `colmap -> rows`: cost 3120, n_fail 262
- `rowmap -> lo_r` / `hi_r`: OneHot indices rank error
- `colmap -> lo_c` / `hi_c`: OneHot indices rank error
- `Rmat -> Cmat`: cost 2880, n_fail 221
- `Cmat -> Rmat`: cost 2880, n_fail 219
- `og_f -> gray__h16`: cost 2326, n_fail 265
- `occ_b -> block_b`: cost 3118, n_fail 265
- `occ_b -> og_b`: cost 3118, n_fail 265
- `bg_b -> zero_b`: cost 3018, n_fail 265
- `rowmap_i -> rowmap` / `colmap_i -> colmap`: OneHot type unsupported

## submit

なし。

`n_fail=0` で cost が下がる候補がなかったため、Kaggle submit は行わない。

## 採否

不採用。

`task250` の Clip は bbox 周辺への gray layer 射影で index を正しく折り返すために必要だった。case134 のような単純な binary 化不要パターンではない。

## 次の一手

`task274` を見る。cost は小さいが graph が短く、`Clip(space_formula, one, two)` と `Where` 2段の閾値生成が整理できれば相対削減率は大きい。
