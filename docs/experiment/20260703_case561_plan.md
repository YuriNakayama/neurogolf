# 20260703 case561 plan - scanner-driven fresh range search

Accepted Public Score:

- `7187.57`

## Background

Case560 added `submit scan-candidates`, allowing broad candidate directories to
be screened without stopping on runtime-invalid ONNX files.

## Hypothesis

Fresh current-baseline alias/dtype scans on lower-risk task ranges, followed by
`scan-candidates`, should find exact positives faster than manual re-gating stale
scratch candidates.

## Targets

Parallel workers should write candidates only to private directories:

```text
range A: task001-task140 excluding risk/recent tasks
range B: task141-task280 excluding risk/recent tasks
range C: task281-task400 excluding risk/recent tasks
```

Main thread will run:

```bash
cd backend
uv run python -m submit scan-candidates <worker-dir> \
  --baseline-dir ../data/output/onnx \
  --task-dir ../data/lake/neurogolf-2026
```

## Submit Rule

Submit immediately for any `submit-candidate` result. If only `bank-low-gain`
results exist, bundle only exact, non-risk candidates and avoid already rejected
families such as `task319`.
