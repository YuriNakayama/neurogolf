# 20260701 case286 plan - task157 top/merge alias bundle

## Context

- Current adopted Public Score floor: `7182.10`
- Latest adopted cycle: case285 `task023` square branch aliases (`13000 -> 11992`)
- Candidate source: interrupted high-cost parallel explorer scan.
- Priority rule: choose exact candidates by relative reduction.

## Target

- `data/output/onnx/task157.onnx`

## Baseline

Current local audit:

```text
params=351
memory=7983
cost=8334
n_fail=0
```

## Hypothesis

Several top/merge decision tensors are equivalent to earlier tensors on all
valid examples:

```text
case34_perm3_obj3       -> top_present_1
merge23_rows           -> single2_rows
single5_case_match_vec -> top_keep_vec_5
merge_span_4_5         -> merge_delta_4_5
not_top5               -> top_present_1
same4                  -> top_present_1
```

Replacing them should prune redundant decision and merge helper tensors while
preserving exact output.

## Expected Gain

- Baseline: `8334`
- Candidate from temporary audit: `8163`
- Relative reduction: `8334 / 8163 = 1.0209`
- Expected point gain: `ln(8334 / 8163) ~= 0.0207`

This is the strongest remaining verified candidate after case285.

## Verification Plan

1. Apply only the six aliases above.
2. Dead-prune unused nodes, initializers, and value_info.
3. Run individual `audit_one` for `task157` with
   `data/lake/neurogolf-2026/task157.json`.
4. Submit only if `n_fail=0`, `status=ok`, and cost decreases.
5. Adopt only if Kaggle Public Score improves; otherwise restore ONNX and record
   rejection.
