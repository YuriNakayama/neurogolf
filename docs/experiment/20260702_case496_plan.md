# 20260702 case496 plan - task008 cadence scan

## Context

Current accepted Public Score:

```text
7187.10
```

Case495 rejected `task005` after bounded main-thread gating and repair triage.
The useful ratio families failed broadly, while low-fail candidates had gains
too small to repair profitably.

## Target

```text
task008.onnx
```

Reason:

- moderate scratch pool;
- not in the hard reject list from the regression analysis;
- small/medium-cost tasks can still produce large relative improvements, as
  case494 `task048` showed.

## Method

1. Gather unique `task008*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Measure baseline and candidate costs with `evaluate.audit_one`.
3. Rank positives by `ln(old_cost / new_cost)`.
4. Gate positives with `submit.gate.evaluate_candidate_gate`.
5. Submit only if a candidate reaches `submit-candidate`; otherwise record
   no-submit and continue.

## Parallel Work

The active worker continues scanning `task008/task013/task099/task105/task117`
for an independent signal or exact micro backlog candidates.
