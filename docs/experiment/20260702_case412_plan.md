# 20260702 case412 plan - retry isolated task338 single pair alias

## Context

- Accepted Public Score remains `7186.71` after case411 was rejected as
  unchanged.
- A fresh `001-200` scan found no exact new `gain>=0.02` single-task candidate.
- The `task110` near miss has `gain=0.039`, but the single failing example
  corrupts the whole output pattern, making a cheap hidden-safe repair unlikely.
- The `201-400` background search is still running; this cycle keeps the submit
  cadence with a known local-exact candidate.

## Target

Target: `task338`.

Candidate:

- scratch: `/tmp/neurogolf_future_task338/task338_tr_pair_TO_tr_pair_0.onnx`
- rewrite: `tr_pair -> tr_pair_0`
- current baseline: `cost=17939`, `n_fail=0`
- candidate: `cost=17819`, `n_fail=0`
- expected gain: `ln(17939 / 17819) = 0.006712`

## Rationale

This exact alias did not move Public when tried earlier, but the accepted floor
has since changed and the gain is larger than the rejected case411 `task342`
micro. It is also outside the current ERROR-family suspects. Apply only one of
the two symmetric aliases because the two-alias bundle fails local correctness
with `n_fail=9`.

## Gate

1. Replace only `data/output/onnx/task338.onnx`.
2. Run `evaluate.audit_one` for `task338` only.
3. Confirm no forbidden ops are introduced.
4. Submit if `n_fail=0` and cost remains lower.
5. Adopt only if Kaggle Public Score improves over `7186.71`; otherwise restore
   the accepted DVC state and record rejection.
