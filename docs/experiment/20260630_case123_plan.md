# 2026-06-30 case123 plan

## 方針

- floor: Public Score 7181.08
- 優先基準: 相対削減率 `old_cost / new_cost`。
- 足切り: `task206.onnx` の `audit_one` で `n_fail=0`。

## 対象

- `task206.onnx`
- baseline: params 79, memory 4105, cost 4184, n_pass 266, n_fail 0

## 仮説

`task206` は 3x3 patch から `ScatterND` の indices/updates を作り、hole と non-hole を同時更新している。hole 側、background 側、または `ingrid_b` / `nbf` の guard が冗長なら、patch 由来の中間や Concat を削れて相対削減率を稼げる。

## 手順

1. `/tmp` 上の candidate ONNX で `updates`、`indices`、`tmpl_b`、`ishole/nothole` を簡略化する。
2. `n_fail=0` かつ cost 減なら `data/output/onnx/task206.onnx` に反映する。
3. submit し、Public Score が 7181.08 を上回れば採用、上がらなければ破棄する。
