# 20260702 case411 plan - isolate task342 row/column aliases

## Context

- Accepted Public Score is `7186.71` after case410 adopted isolated `task046`.
- Current backlog re-audit found 17 still-cost-reducing scratch candidates.
- Exclusions:
  - `task285` uint8 `TopK` candidate: already isolated as Kaggle `ERROR`.
  - `task017` candidate-row pruning: already isolated and hidden-unsafe.
  - `task305` and `task366`: contaminated the case406 hidden regression.
  - `task037`: still a stronger suspect in the case390/case395 `ERROR`
    family.

## Target

Target: `task342`.

Candidate:

- scratch: `/tmp/neurogolf_case383_task342_bundle_all.onnx`
- rewrites: `row_minus2 -> cyan_row_sum`, `col_minus2 -> cyan_col_sum`
- current baseline: `cost=1585`, `n_fail=0`
- candidate: `cost=1576`, `n_fail=0`
- expected gain: `ln(1585 / 1576) = 0.005694`

## Rationale

The gain is below the normal standalone threshold, but case410 showed that
isolated sub-`0.02` candidates can still move the rounded Public score. This
also continues the ERROR-family isolation work without mixing multiple suspects.

## Gate

1. Replace only `data/output/onnx/task342.onnx`.
2. Run `evaluate.audit_one` for `task342` only.
3. Confirm no forbidden ops are introduced.
4. Submit if `n_fail=0` and cost remains lower.
5. Adopt only if Kaggle Public Score improves over `7186.71`; otherwise restore
   the accepted DVC state and record rejection.
