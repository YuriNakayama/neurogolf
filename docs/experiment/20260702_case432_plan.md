# 20260702 case432 plan - task187 reach-chain revalidation

## Context

Case431 rejected `task133` branch-family reductions. The next documented
candidate is `task187`:

```text
task187 current cost = 27533
```

Known near misses:

```text
reach_13 -> reach_12: 27533 -> 26283, gain 0.046463, n_fail=4
reach_pool_* -> reach_*: 27533 -> 26908, gain 0.022962, n_fail=4
```

Prior case367 concluded the final reach step is required. This cycle is a
current-baseline revalidation, not a broad reach-chain search.

## Hypothesis

If any existing repair candidate is exact on the current accepted graph and
keeps cost below baseline, it can be submitted. Otherwise, `task187` should stay
closed until a non-reach-chain representation appears.

## Method

1. Re-audit the current `task187` baseline.
2. Gate known reach-chain and repair artifacts from cases356/359/367.
3. Submit only if `decision=submit-candidate`.

## Gate

Submit only:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
```
