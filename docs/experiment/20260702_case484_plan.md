# 20260702 case484 plan - task198 current scratch cadence scan

## Context

Accepted Public Score is `7186.73`. Cases 482-483 were no-submit windows
because all positive cost candidates failed local correctness. The active
cadence is a submit decision window of roughly `10.8` minutes, with no forced
Kaggle submit when the gated queue is empty.

`task198` current baseline:

```text
task198 cost=13572
params=138
memory=13434
```

It is outside the current hard/review/runtime risk sets and has a large scratch
pool.

## Target

```text
task198
baseline: data/output/onnx/task198.onnx
task json: data/lake/neurogolf-2026/task198.json
```

## Method

1. Gather unique `task198*.onnx` candidates from `/tmp` and `/private/tmp`.
2. Cost-rank against the current baseline.
3. Gate every cost-reducing candidate with `submit.gate.evaluate_candidate_gate`.
4. Submit only an isolated candidate satisfying:

```text
decision=submit-candidate
n_fail=0
gain >= 0.020
functions=0
forbidden_ops=()
```

5. Bank exact low-gain candidates; otherwise record no-submit and continue the
   parallel search queue.
