# 20260703 case555 result - standalone search after task174

## Summary

- Submit: none
- Decision: no-submit
- Accepted Public Score remains: `7187.53`

## Local Work

The main thread re-gated the newly found `task205` micro candidate:

```text
task205 11547 -> 11539
gain 0.000693
n_fail=0
functions=0
forbidden ops=[]
decision=bank-low-gain
```

The main thread also scanned non-risk `ReduceSum` patterns similar to the
case554 `task174` improvement. Simple `ReduceSum -> ReduceMax` trials either
kept the same cost or failed correctness:

```text
task019 2798 -> 2798 n_fail=0 no gain
task025 11664 -> 11664 n_fail=0 no gain
task067 47 -> 47 n_fail=186 failed
```

## Parallel Worker Findings

The worker searched `task118`, `task187`, `task198`, `task216`, `task319`,
`task349`, `task364`, `task367`, `task396`, `task340`, `task145`, `task202`,
`task204`, and `task205`.

No standalone `submit-candidate` was found.

Best failed high-gain candidates:

```text
task349 24369 -> 22576 gain 0.076424 n_fail=17
task364 23013 -> 22133 gain 0.038990 n_fail=21
task340 10918 -> 10662 gain 0.023727 n_fail=220
task367 20699 -> 5648 gain 1.298784 n_fail=266
task187 26396 -> 26283 gain 0.004290 n_fail=4
```

Exact bank-low addition:

```text
task205 11547 -> 11539 gain 0.000693 n_fail=0
```

## Decision

No submit was made because the only exact improvement was a very small micro.
The previous micro-bundle submit with larger total local gain did not improve
Public Score, so spending another submit on `task205` alone is not justified.

## Next Step

Prioritize repair of low-fail high-gain candidates:

1. `task187`: small local fail count (`n_fail=4`) makes targeted repair
   plausible even though gain is modest.
2. `task349`: large gain (`0.076424`) with `n_fail=17`; inspect failed pairs
   to isolate the required halo branch.
3. `task364`: large gain (`0.038990`) with `n_fail=21`; inspect PB7 propagation
   guard before trying another alias.
