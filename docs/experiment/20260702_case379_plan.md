# 20260702 case379 plan - post-case378 equivalence scan

## Context

Accepted Public Score is `7185.88` after case378. The last adopted change was a
low-risk graph-equivalence rewrite on `task310`:

```text
task310: 3051 -> 2977
gain: ln(3051 / 2977) = 0.024553323700893265
```

The scoring rule is logarithmic, so continue prioritizing relative cost
reduction:

```text
points = max(1, 25 - ln(max(1, cost)))
cost   = params + memory_bytes
gain   = ln(old_cost / new_cost)
```

## Search Targets

Look for another single-task, low-risk equivalence with expected
`gain >= 0.020`:

- selector duality such as `ArgMax(Neg(x)) -> ArgMin(x)`;
- `Slice` range shortening when omitted axes are full-range constants;
- deterministic cleanup from `pipeline.case3 surgery`;
- dtype changes only when they remove a large temporary rather than add one;
- kernel/source-crop candidates only when output coordinates and shapes remain
  equivalent.

Exclude known risk families:

- `task002` bit-topology local positives;
- `task118` table pruning;
- `task233` uint8 `TopK` value path until the Kaggle runtime error is fixed.

## Gate

Submit only if:

```text
n_fail = 0
gain >= 0.020 for low-risk graph equivalence
```

If only micro material is found, skip submit and record it for a future
submit-saving batch.
