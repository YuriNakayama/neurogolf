# 20260702 case353 plan - submit-saving candidate audit

## Context

Accepted Public Score is `7185.53` after case352. The next submit window is
roughly 30 minutes after the case352 submission, but the submit-saving policy is
explicit that the clock is a budget guard, not a reason to submit weak material.

The score improvement for a correct task is:

```text
gain = ln(old_cost / new_cost)
cost = params + memory_bytes
```

So this cycle prioritizes relative cost reduction and treats correctness as a
hard gate.

## Hypothesis

After two accepted task118 table-pruning submissions, the safer next move is to
avoid another hidden-sensitive task118 variant and search for either:

- one non-task118 structural candidate with `n_fail=0` and `gain >= 0.030`;
- or a low/medium-risk micro batch with total `gain >= 0.030`.

Known unsafe families remain excluded:

- task101 broad local-equivalence aliases;
- task118 stronger table-pruning variants;
- task233 uint8 `TopK` scorew;
- task286 broad bitwise equivalence.

## Work Plan

1. Run parallel exploration for:
   - non-task118 structural candidates with material relative gain;
   - low/medium-risk micro candidates suitable for batching.
2. Re-audit candidate task ONNXs only with `src/evaluate.audit_one`.
3. Submit only if a threshold-clearing candidate or batch appears.
4. If all exact candidates remain below threshold, skip the Kaggle submit and
   bank the exact candidates in `micro_backlog.md`.

## Submit Gate

```text
single low-risk:       gain >= 0.020
single medium-risk:    gain >= 0.030
single high-risk:      gain >= 0.080 isolated
low-risk batch:        total gain >= 0.030
mixed-risk batch:      total gain >= 0.050
```
