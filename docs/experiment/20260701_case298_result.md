# 20260701 case298 result - task361 pre-crop MaxPool inputs adopted

## Result

- Decision: adopt
- Kaggle submission ref: `54225854`
- Public Score: `7183.02 -> 7183.03`

## Local Gate

- Baseline: `params=367`, `memory=4472`, `cost=4839`, `n_fail=0`
- Candidate: `params=375`, `memory=4412`, `cost=4787`, `n_fail=0`
- Cost delta: `-52`
- Point delta estimate: `+0.010804`

## Change

`task361` previously pooled a `10x10` empty mask and sliced the interior
candidate windows:

```text
MaxPool(empty10_u8, kernel=3) -> empty3_u8 [1,1,8,8]
Slice(empty3_u8, rows 2:7, cols 2:7) -> int_empty_u8 [1,1,5,5]

MaxPool(empty10_u8, kernel=2) -> empty2_u8 [1,1,9,9]
Slice(empty2_u8, rows 3:8, cols 2:7) -> half_empty_u8 [1,1,5,5]
```

The adopted graph slices the MaxPool inputs first:

- integer branch: `empty10_u8[2:9,2:9] -> 7x7`, then `MaxPool(kernel=3)`;
- half branch: `empty10_u8[3:9,2:8] -> 6x6`, then `MaxPool(kernel=2)`.

This preserves the exact pooled windows while removing the wider `8x8` and
`9x9` MaxPool intermediates.

## Conclusion

The improvement is small, but it passed local exact audit and increased Public
LB.  The useful pattern is non-first MaxPool crop localization by slicing the
input region before pooling, where the added input slice is smaller than the
removed wide pool output.

## Next Step

Continue searching for source-crop candidates, but raise priority for patterns
where the moved crop eliminates several wide intermediates at once.  Single
small MaxPool localizations are acceptable only when no larger proof-grade
candidate is available.
