# case238 plan: task063 row/col kernel trim

## 現状
- 採用済み Public Score floor: 7181.24
- `task063` baseline は `cost=2011` (`params=283`, `memory=1728`, `n_fail=0`)。
- `row_kernel (1,10,1,13)` / `col_kernel (1,10,13,1)` は channel 0 の 12 個の 1 と端の 0 で、空 row/col 判定に使われる。
- `Slice(sum_start=1, sum_end=13)` で Conv 出力を 12 幅へ切っている。

## 対象
- `data/output/onnx/task063.onnx`

## 仮説
kernel 先頭または末尾の 0 が padding 目的だけなら、kernel 長を 12 にし、Slice/Pads を合わせることで params と memory を削れる可能性がある。

## 検証
- row/col kernel を 12 長に crop し、Slice start/end と Pad を調整して `audit_one` する。
- 11 長以下も低 fail 境界として確認する。
- `mark30` / `green_vector` / active mask の固定化を軽く probe する。
- `src.evaluate.scorer.audit_one` で `task063` の全ペア完全一致と cost を確認し、`n_fail=0` かつ十分な cost 改善候補だけ実 ONNX へ反映する。
