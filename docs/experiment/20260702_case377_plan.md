# 20260702 case377 plan - recheck stale low-risk micro backlog

## Context

Case376 found the 20260702 backlog alone below the micro-batch submit gate.
However, older `micro_backlog.md` entries include low-risk duplicate-initializer
cleanups for very small tasks such as task334, task052, and task003. If those are
still present in the current accepted graph, the low-risk batch could exceed the
`0.030` submit threshold.

## Target Pool

Recheck only candidate tasks from the low-risk cleanup family:

```text
003, 023, 052, 066, 206, 222, 285, 325, 334, 363, 379
```

No hidden-sensitive task002/task118 candidates are included.

## Method

1. Copy the selected ONNX files to `/tmp`.
2. Run `pipeline.case3 surgery` on that selected subset only.
3. Compare each surgered candidate with current `data/output/onnx`.
4. Submit only if exact current improvements sum to at least `0.030`.

## Submit Gate

```text
low-risk micro batch threshold: gain >= 0.030
```
