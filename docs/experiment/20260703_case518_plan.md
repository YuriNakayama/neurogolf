# 20260703 case518 plan - post task238 standalone supply

## Context

case517 adopted task238:

```text
Public Score: 7187.29 -> 7187.37
task238 2355 -> 2185
gain=0.0749248989
```

The submit stall was fixed by increasing parallel exploration. Remaining worker
coverage:

```text
U1 tasks 001-100
U5 prior high-upside alias families
U6 high-cost dtype/initializer reductions
```

Completed worker findings:

```text
U2 tasks 101-200: no safe standalone candidate
U3 tasks 201-300: task238 accepted, no task260 candidate
U4 tasks 301-400: no submit-grade task354/task383 follow-up
```

## Hypothesis

The next submit should again be a standalone current-baseline candidate. Forced
micro bundles remain banned after case514. The best chance is to mine stale
scratch pools and prior high-upside families against the current accepted
baseline, because task238 was found by the same shape-alias pattern.

## Target Selection

Prioritize tasks that are:

```text
not in known risk lists
n_fail=0 required
gain >= 0.020 preferred
mid-gain candidates are banked only
```

Parent re-gate targets:

```text
task023, task055, task076, task077, task080, task118, task133, task138,
task145, task158, task191, task198, task219, task243, task319, task328,
task349, task364, task367, task377, task379, task396
```

## Submit Policy

Submit immediately if a standalone `submit-candidate` is found and passes
dry-run bundle gate. Otherwise keep parallel workers running and record a
no-submit result rather than spending a submit on weak banked changes.
