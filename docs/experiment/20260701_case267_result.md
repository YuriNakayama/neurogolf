# 20260701 case267 result - reject task009 suffix false mask Pad

## 結果

- 対象: `data/output/onnx/task009.onnx`
- 採否: reject
- submit: なし（削減幅が小さすぎる）

## 試行内容

suffix false mask の `Concat` 2 箇所を `Pad` へ置換した。

```text
Concat(v_mask_tail, false_col, axis=3) -> v_sep_mask
Concat(h_mask_tail, false_row, axis=2) -> h_sep_mask
```

を以下へ変更。

```text
Pad(v_mask_tail, pads=[0,0,0,0,0,0,0,1]) -> v_sep_mask
Pad(h_mask_tail, pads=[0,0,0,0,0,0,1,0]) -> h_sep_mask
```

`false_col` / `false_row` initializer を削除し、2 つの pads initializer を追加した。

## 足切り

baseline:

```text
status ok
params 1081
memory 7329
cost 8410
points 15.962823247033006
n_pass 265
n_fail 0
filesize 5741
```

candidate:

```text
status ok
params 1077
memory 7329
cost 8406
points 15.963298984434937
n_pass 265
n_fail 0
filesize 5907
```

## 結論

正答は保てたが、削減は 4 cost の params-only。相対削減率は `8410 / 8406 = 1.00048` で、
今回の優先基準 `old_cost / new_cost >= 1.01` を大きく下回る。Public 反映は期待できないため
submit せず、ONNX 本体は変更しない。

## 次の一手

zero edge Concat の単発 Pad 化は小さすぎる。複数箇所をまとめても memory が減らない限り優先度を下げる。
次は trace 上 1200B 以上の中間を持つ task から、dtype 境界や branch を丸ごと消せる候補だけを選ぶ。
