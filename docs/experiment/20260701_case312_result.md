# 20260701 case312 result - adopt task080 separator mask alias

## Result

- Decision: adopt
- Kaggle submission ref: `54228862`
- Previous Public Score: `7183.04`
- New Public Score: `7183.10`
- Public delta: `+0.06`

## Local Gate

`task080.onnx`:

```text
params=107
memory=15543
cost=15650
n_pass=231
n_fail=0
status=ok
```

Baseline before this cycle:

```text
params=111
memory=16623
cost=16734
n_pass=231
n_fail=0
status=ok
```

Local cost reduction:

```text
16734 -> 15650
old_cost / new_cost = 1.0693
ln(old/new) ~= 0.0670 pt
```

## Change

Rewired the separator mask condition:

```text
sepmask -> lineOrOut
```

The terminal `Where` is preserved:

```text
Where(lineOrOut, linecu, exp) -> withsep
```

Dead-node pruning removed the separator-construction chain:

```text
mult -> modr -> seprow / sepcolr -> sepmask
```

and its now-unused initializer.

## Verification

Individual `evaluate.audit_one` for `task080` passed with `n_fail=0`.
The full submission completed on Kaggle and improved the Public LB to `7183.10`,
so the local exact alias transferred to the real grader.

## Conclusion

Adopted. This is the strongest recent cycle because it removed a whole
memory-producing predicate chain rather than a micro cleanup. The result supports
the current priority rule: same-task structural aliases remain high ROI when
they delete complete intermediate chains and exceed the ratio gate.

## Next Step

Continue shape-guided same-task bundle search. Prefer candidates similar to this
one:

- same dtype and static shape;
- replacement tensor already computed for semantic reasons;
- pruned chain removes at least one 30x30 or comparable tensor;
- local ratio above `1.02` where possible.

Avoid local-only hidden-unsafe aliases on known hazard tasks (`task101`,
`task340`) and avoid boundary-sensitive GridSample/Slice rewrites unless the
sampled window is provably in bounds.
