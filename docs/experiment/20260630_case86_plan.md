# 20260630 case86 plan — t187 の flood-fill 反復上限を再検証する

## 背景

- 現 Public best は case84 採用後の **7180.87**。
- t187 は cost **36814**（params 664 / memory 36150）で、25x25 crop 上の border-reach flood-fill graph。
- graph は `MaxPool`/`Min` の 13 段反復で `reach_13` を作り、未到達 free cell を色2、到達 free cell を色3へ変換する。
- 過去ログでは 12 iter が `n_fail=4` とされているが、case84 で同種の反復鎖に余剰が見つかったため、現在 floor で再確認する。

## 仮説

1. `reach_13` を `reach_12` 以前へバイパスできれば、625B 単位で memory を削れる。
2. `MaxPool` kernel を大きくして反復数を減らす案は壁を飛び越える可能性が高いが、代表候補を再確認する。
3. stale `value_info` / dead node / duplicate initializer が残っていれば機械的に削れる。

## 対象

- `data/output/onnx/task187.onnx`

## 足切り

- `src.evaluate.scorer.audit_one` で t187 の train/test/arc-gen 全ペア完全一致。
- n_fail=0 かつ cost 減少の候補のみ submit。

## 採否

- Public Score が 7180.87 を上回れば採用。
- 上がらない、または faithful で不一致なら破棄。
