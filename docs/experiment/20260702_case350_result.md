# 20260702 case350 result - task118 conservative patch table pruning adopted

## Result

- Previous accepted Public Score: `7185.43`
- Kaggle submission ref: `54240441`
- Submission status: `SubmissionStatus.COMPLETE`
- Public Score: `7185.47`
- Submission time: `2026-07-01 18:10:10 UTC`
  (`2026-07-02 03:10:10 JST`)

Adopted.

## Local Candidate

Submitted candidate:

```text
/tmp/neurogolf_case350_search/task118_pos_keepfirst.onnx
```

Targeted `audit_one` before submission:

```text
baseline:  params=4614, memory=34623, cost=39237
candidate: params=3782, memory=34207, cost=37989
n_fail=0, status=ok
gain = ln(39237 / 37989) ~= 0.0323
```

Packaging dry-run also completed and included all 400 tasks. The seven existing
local-cost-unestimated tasks remained structurally valid, matching previous
accepted submissions.

## Adoption Rationale

Despite the hidden-sensitive risk noted in the plan, the isolated task118 table
pruning improved Public Score by `+0.04`. The accepted change keeps only the
conservative positive-patch keep-first variant and does not include the stronger
task118 table-pruning variants.

## Risk Notes

The broader task118 table-pruning family is still risky. Do not assume the
stronger variants are safe from this result alone:

```text
36653 cost variant: higher risk
35807 cost variant: higher risk
```

The safe conclusion from this cycle is only that
`task118_pos_keepfirst.onnx` is accepted by the current Public LB.

## Next Step

The best next submit-saving candidate is the case351 batch found in parallel
search:

```text
task209: 28683 -> 28047, gain ~= 0.022423
task090:  3091 ->  3073, gain ~= 0.005840
task213:  1887 ->  1882, gain ~= 0.002653
total gain ~= 0.030917
```

Candidate paths:

```text
/tmp/neurogolf_case351_search/task209_alignment_keyb__kern_clip_bypass.onnx
/tmp/neurogolf_case351_search/task090_area_bestrow_9alias.onnx
/tmp/neurogolf_case351_search/task213_hvalid_5alias.onnx
```

This batch avoids the task118, task233, task286, and task101 rejected or
hidden-unsafe families, and should be re-audited on the post-case350 baseline
before submission.
