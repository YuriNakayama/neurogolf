# 20260702 case509 plan - expanded parallel submit queue

Current accepted Public Score:

7187.23

## Background

Case508 adopted a hardened five-task micro bundle and moved Public Score from
`7187.21` to `7187.23`. The latest user instruction asks for enough parallel
exploration to sustain roughly `72` submissions over `13` hours. The practical
constraint is that recent regressions came from forcing locally exact but
hidden-unsafe or too-weak candidates into Kaggle, so cadence must be a queue
discipline rather than a forced-submit rule.

## Current Parallelism

The active session is at the subagent limit. Six workers are running:

```text
X  tasks 001-200 broad standalone scan
Y  tasks 001-080 relative-gain scan
Z  tasks 081-160 relative-gain scan
AA tasks 161-240 relative-gain scan
AB tasks 241-320 relative-gain scan
AC tasks 321-400 relative-gain scan
```

An attempted seventh worker for bundle assembly was rejected by the orchestration
limit, so bundle assembly remains in the main agent while workers search.

## Immediate Gate State

Known unadopted backlog was re-gated against the post-case508 current baseline.
The clean `bank-low-gain` material is still below the micro-bundle threshold:

```text
task018 38681 -> 38575 gain 0.002744
task054 27089 -> 27073 gain 0.000591
task383  7245 ->  7237 gain 0.001105
task349 24389 -> 24369 gain 0.000820
task396  8078 ->  8074 gain 0.000495
task099  1911 ->  1910 gain 0.000523
task117  4149 ->  4148 gain 0.000241
task008  5626 ->  5625 gain 0.000178
task260  2110 ->  2109 gain 0.000474
task204 11494 -> 11493 gain 0.000087
task219 14838 -> 14837 gain 0.000067
task206  4184 ->  4183 gain 0.000239
```

Best exact mid-gain items remain review-only:

```text
task184  2520 ->  2491 gain 0.011575 decision review-mid-gain
task243 22650 -> 22343 gain 0.013647 decision review-mid-gain
```

## Plan

1. Keep all six subagents running until they return a candidate or a near-miss
   frontier.
2. Main agent owns submit queue assembly:
   - standalone candidate if `gain >= 0.020`, `n_fail=0`, and no risk block;
   - micro bundle if `<=5` changed clean tasks, all `bank-low-gain` or
     `submit-candidate`, and total gain `>=0.015`;
   - no forced submit for `review-mid-gain` unless a separate isolated plan
     explains why hidden risk is acceptable.
3. While waiting, attempt local repair work on the two review-mid items:
   `task243` first because its repaired candidate is near the standalone
   threshold, then `task184`.
4. If a worker returns a clean candidate, immediately run the per-task gate,
   write the result file, and submit if the hardened gate clears.

## Expected Cadence

The target cadence is a submit readiness check every 10-15 minutes, not an
unconditional Kaggle submit. This keeps the 72-submit budget usable while
avoiding the documented cause of the recent no-improve/regression streak:
below-threshold or contaminated local positives.
