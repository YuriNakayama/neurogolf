# case197 plan: task154 edge mask branch trim

## 現状

- 採用済み Public Score floor: 7181.21
- `task154` は未着手で、baseline は `cost=2760` (`params=99`, `memory=2661`, `n_fail=0`)。
- `Where(mask, two_u8, zero_u8)` の edge mask が 4 箇所あり、1 箇所でも固定できれば 16-24 byte 級の中間を削れる。

## 対象

- `data/output/onnx/task154.onnx`
- `top_rev`, `bot3`, `left_rev`, `right3` は入力 channel 5 の edge slice を `2/0` の class code に変換する。
- 後段で bottom/right の line selector と合成し、赤 channel と `Max` して 30x30 one-hot に戻す。

## 仮説

実データ上、いずれかの edge slice が常に空または常に class 2 なら、対応する `Where` と bool Cast を削れる。edge mask ごとの片側固定を temp ONNX で確認する。

## 検証

- temp ONNX で `top_rev`, `bot3`, `left_rev`, `right3` を `zero_u8` または `two_u8` に alias する。
- `src.evaluate.scorer.audit_one` で `task154` の全ペア完全一致を確認する。
- `n_fail=0` かつ cost が下がった候補だけを実 ONNX に反映し、submit する。
