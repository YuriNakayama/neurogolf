# 20260702 case454 result - task062 ref/object candidate gate

## Outcome

No submit.

Current baseline:

```text
task062.onnx
params=71
memory=2465
cost=2536
points=17.1616566844
status=ok
```

## Candidate Scan

Collected 160 existing `task062*.onnx` scratch candidates from `/private/tmp`
and `/tmp`.

Cost-only screening found many positive reductions. Correctness gating of the
positive reductions produced:

```text
exact_positive=0
blocked=99
exceptions=0
```

Several candidates produced repeated runtime Gather bound errors during local
correctness evaluation, for example:

```text
indices element out of data bounds, idx=13 must be within [-10,9]
indices element out of data bounds, idx=11 must be within [-10,9]
```

These are still local gate failures and are not submit candidates.

## Best Reductions

Largest ref/object simplifications all failed broad local coverage:

```text
/private/tmp/neurogolf_nextcycle_small_alias/task062_007.onnx
  2536 -> 1683
  gain=0.4100101214
  n_fail=267

/private/tmp/neurogolf_nextcycle_small_alias/task062_011.onnx
  2536 -> 1745
  gain=0.3738334809
  n_fail=267

/private/tmp/neurogolf_nextcycle_small_alias/task062_010.onnx
  2536 -> 1809
  gain=0.3378138302
  n_fail=267
```

The best non-total-failure ref branch cuts were still not repair-like:

```text
/private/tmp/neurogolf_nextcycle_small_alias/task062_004.onnx
  2536 -> 2183
  gain=0.1498879590
  n_fail=130

/private/tmp/neurogolf_nextcycle_small_alias/task062_001.onnx
  2536 -> 2240
  gain=0.1241121707
  n_fail=137
```

Index/guard shortcuts also failed heavily:

```text
task062_030_Add_idx_v_raw_TO_offset_minus_id_i32.onnx
  2536 -> 2431
  gain=0.0422853412
  n_fail=130

task062_029_Add_idx_h_raw_TO_offset_minus_id_i32.onnx
  2536 -> 2488
  gain=0.0191088617
  n_fail=137

task062_031/032_Where_idx_*_pad_TO_idx_*_raw.onnx
  2536 -> 2504
  gain=0.0126985833
  n_fail=123..128
```

The smallest-failure exact-looking family was still too broken:

```text
task062_025/026_Where_delta_* aliases
  2536 -> 2515 or 2532
  gain=0.0015785323..0.0083152330
  n_fail=60..70
```

## Decision

Reject for submission. No current-baseline exact cost-reducing candidate was
found. The attractive candidates remove required object/reference or index
guard logic and fail too broadly to be a small repair target.

## Next

The small Pad/output-mask trio from cases436/438 is now closed:

```text
task102: no exact positive candidate
task190: no exact positive candidate
task062: no exact positive candidate
```

Move to a different candidate class rather than continuing stale small-task
alias pools. Prefer a fresh structural target where the high-ratio tensor is a
duplicated derived mask, not a required value/index guard.
