# 20260702 case438 result - task205 residual probe is no-submit

## Decision

- Task: `task205.onnx`
- Submit: no
- Adoption: none
- Reason: no current-baseline exact candidate reduces cost.

Current baseline:

```text
params=279
memory=11271
cost=11550
status=ok
```

## Graph Evidence

The remaining large internal tensors are:

```text
cgrid      FLOAT [1,1,30,30]   3600 bytes
ohB        BOOL  [1,10,10,10]  1000 bytes
bm         BOOL  [1,1,30,30]    900 bytes
bmh_u8_q   UINT8 [1,1,30,30]    900 bytes
hsum       UINT8 [1,1,30,25]    750 bytes
vsum       UINT8 [1,1,25,30]    750 bytes
c6         FLOAT [1,10,4,4]     640 bytes
```

`cgrid` is not a dead or purely display tensor. It is produced by
`Conv(input, lev1)` and consumed by both:

```text
Equal(cgrid, bc1f) -> bm
GridSample(cgrid, grid) -> cpatch
```

This blocks the obvious dtype rewrite: `GridSample` requires a floating input,
and casting the full one-hot input before color extraction would likely cost
more than the `cgrid` memory it saves.

## Candidate Re-Gate

Re-gated 105 existing `task205` candidates against the current accepted
baseline:

```text
blocked-local-fail: 97
blocked-no-gain:     8
submit-candidate:    0
```

Largest apparent reductions all changed semantics:

```text
task205_67_Add_ogrid_TO_mainpix.onnx
  cost 11550 -> 9386
  gain 0.207466
  n_fail 266
  decision blocked-local-fail

task205_001_Add_bc1_TO_i1b.onnx
  cost 11550 -> 10854
  gain 0.062152
  n_fail 238
  decision blocked-local-fail

task205_022_Add_grid_TO_offgrid.onnx
  cost 11550 -> 11128
  gain 0.037221
  n_fail 266
  decision blocked-local-fail

task205_52_And_seed_TO_inbox.onnx
  cost 11550 -> 11250
  gain 0.026317
  n_fail 266
  decision blocked-local-fail

task205_59_And_cross_TO_inbox.onnx
  cost 11550 -> 11310
  gain 0.020998
  n_fail 266
  decision blocked-local-fail
```

The only exact candidates were old cleanup outputs from before the accepted
case369 dtype compression. They now worsen the current baseline:

```text
task205_broadcast_compress.onnx  cost 11550 -> 12494, n_fail=0, blocked-no-gain
task205_cleanup.onnx             cost 11550 -> 12494, n_fail=0, blocked-no-gain
task205_conv1x1_to_gather.onnx   cost 11550 -> 12494, n_fail=0, blocked-no-gain
task205_identity_elem.onnx       cost 11550 -> 12494, n_fail=0, blocked-no-gain
```

## Parallel Scan Notes

A read-only side scan checked the Pad-dominated tasks suggested after case436:

```text
task102 cost=2527: existing fill-core reductions save enough but fail 81-146 cases
task190 cost=2527: existing ray-branch drops save enough but fail 119-250 cases
task062 cost=2536: existing ref/object simplifications save enough but fail 130-267 cases
```

Each would need only about 50 cost saved to clear `gain >= 0.020`, but the
already-observed above-threshold candidates are semantic removals, not safe
cleanup.

## Conclusion

No submit. `task205` should not be revisited through old alias pools. The
remaining large tensors are tied to color-grid extraction and patch sampling;
only a new equivalent formulation for `Conv(input, lev1) + GridSample` is worth
another cycle.

Continue with fresher mid-cost tasks that still have large internal tensors and
less prior alias saturation, such as `task174`, `task138`, `task216`, or
`task191`, while keeping the 15-minute cadence as a polling cadence rather than
a forced-submit rule.
