# 20260703 case516 plan - standalone search after micro-bundle error

Current accepted Public Score:

```text
7187.27
```

## Background

Case513 adopted `task187` and improved Public LB. Case514 then showed that a
locally exact low-gain micro-bundle can still produce Kaggle `ERROR`. The next
cycle should not spend a submit on another blind bundle.

## Hypothesis

Standalone candidates remain the best submit spend after the bundle error. The
search should focus on non-risk tasks that have not been covered by the latest
post-error sweep:

```text
T1 tasks 121-220
T2 tasks 221-300
parent targeted re-gate of prior high-gain scratch candidates
```

Priority is relative/log gain:

```text
gain = ln(old_cost / new_cost)
```

## Gate

Submit only if all are true:

```text
n_fail = 0
status = ok
no functions
no forbidden ops
not hard/review/runtime risk
decision = submit-candidate
```

Bank-low and review-mid candidates are recorded but not submitted in this cycle.
