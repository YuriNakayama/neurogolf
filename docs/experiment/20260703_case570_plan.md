# 20260703 case570 plan - task279 row7 repair

Accepted Public Score:

- `7187.59`

## Background

Case569 adopted task343 and expanded the parallel search pool. The task279 worker
then repaired the previous two-fail high-gain candidate into several exact
lower-cost candidates.

Best returned candidate:

```text
task279: 4827 -> 4597
gain:    0.048821
n_fail:  0
path:    /private/tmp/neurogolf_case569_task279/candidates/task279_row7_c4c7_gather_scatternd_patch.onnx
```

## Hypothesis

The previous `4315` candidate removed too much of the final flood-extension
logic and missed two row-7 cells. A small targeted row-7 patch can preserve most
of the relative gain while restoring exact correctness.

## Method

1. Re-gate the best task279 candidate against current accepted `data/output/onnx`
   after task343 adoption.
2. Build a full 400-task bundle replacing only `task279`.
3. Run full dry-run.
4. Submit if dry-run passes and only `task279` changes.
5. Adopt only if Kaggle Public Score improves over `7187.59`.
